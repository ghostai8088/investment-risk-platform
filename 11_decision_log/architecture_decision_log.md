# Architecture Decision Log

> Index of architecture decisions. Full text for the foundational decisions AD-003 … AD-010 is in
> [foundational_adrs.md](../03_architecture/foundational_adrs.md). **Status** values: Proposed, Accepted, Superseded.

| Decision ID | Date | Status | Decision | Options Considered | Rationale | Risks | Revisit Trigger | Supersedes / Superseded by |
|---|---|---|---|---|---|---|---|---|
| AD-001 | 2026-06-17 | Accepted | Build full-scope platform structure from day one | MVP wedge, prototype-first, full-scope architecture | Full-scope architecture reduces future rework and supports AI-assisted refinement | Risk of shallow breadth without depth | Revisit if architecture becomes too complex to maintain | — |
| AD-002 | 2026-06-17 | Accepted | Use phased construction sequencing, not phased scope | Narrow scope first, full architecture first | Platform requires integrated market, credit, liquidity, private markets, governance, lineage, and auditability | Requires strong backlog discipline | Revisit after first full architecture review | — |
| AD-003 | 2026-06-17 | Accepted (H-06) | Stack: **backend** Python 3.12+/FastAPI/Pydantic/SQLAlchemy + numpy/pandas/scipy; **calc engine** Python behind run interface; **frontend** TypeScript+React/Vite, WCAG 2.1 AA; REST/JSON | Python+TS; JVM; .NET; polyglot | Quant ecosystem fit, one language services+engine, typed UI/service split | Python batch performance | NFR-05 unmet or buyer constraint | — |
| AD-004 | 2026-06-17 | Accepted (H-06) | Datastore: **PostgreSQL SoR** (bitemporal, RLS) + **TimescaleDB** for market data (behind repo interface) + **S3-compatible** object store + **segregated append-only audit** (hash-chained) | Single store; multi-store; doc-first | As-of querying, reproducibility, audit segregation, one DB family initially | Timescale scale ceiling | Volume/perf exceeds Timescale | — |
| AD-005 | 2026-06-17 | Accepted (H-04) | **Selective bitemporality**: FR (bitemporal) for risk-driving inputs, IA (immutable append-only) for outputs/events/audit, EV (effective-dated) for reference/config | Bitemporal-everywhere; selective; valid-time only; event-sourcing | Reproducibility where it matters at proportionate cost | Entity misclassification | Stricter WORM regime | — |
| AD-006 | 2026-06-17 | Accepted (H-06) | Job-based deterministic, run-tracked, pluggable calculation engine; no calc in UI | Run-tracked engine; inline calc; vendor engine | Reproducibility + model governance by construction | Upfront engineering | Vendor engine integration | — |
| AD-007 | 2026-06-17 | Accepted (H-03) | Auth: **OIDC primary** (SAML supported) + enforced **MFA**, short-lived tokens, least privilege; agents scoped via FW-ENT | Federation; built-in identity; API-keys | Enterprise buyer requirement | IdP variance | Buyer IdP/passwordless mandate | — |
| AD-008 | 2026-06-17 | Accepted (H-06) | Multi-tenancy: **strong logical isolation** (tenant_id + Postgres RLS + entitlement scope) + single-tenant option | Logical+option; pooled; physical | Sales flexibility + isolation | Logical isolation defects | Data-residency rules | — |
| AD-009 | 2026-06-17 | Accepted (H-05) | AI-usage boundary: assistive only, BR-15 human gates, BR-16 logging, tiered access, cite-don't-invent; current Claude models for embedded AI | Bounded AI; autonomous AI; no AI | Accountability + auditability | Prompt injection/exfiltration | AI regulation; model change | — |
| AD-010 | 2026-06-17 | Accepted (H-06) | Deployment: **cloud-native containerized (Docker/Kubernetes)** + **Terraform IaC**, env parity, on-prem option, DR per NFR-03, no manual prod changes | Cloud+on-prem; cloud-only; on-prem-only | Enterprise addressability, reproducible envs | Multi-target maintenance | First buyer constraints | — |
| AD-011 | 2026-06-17 | Accepted (H-06) | Test/runtime DB strategy: unit tests on **in-memory SQLite** (engine-agnostic ORM); runtime + migrations on **PostgreSQL**; PG-only enforcement (RLS, append-only triggers) validated by the CI `migration` job | All-Postgres tests; SQLite-only; testcontainers everywhere | Fast hermetic unit tests + real PG enforcement validated separately | Model/migration drift; SQLite/PG behavioral gaps | Add autogenerate drift check (OD-052); revisit if gaps bite | — |
| AD-012 | 2026-06-17 | Accepted (H-06) | Audit hash chain scoped **one chain per tenant** (`chain_id = tenant_id`); SHA-256 `previous→payload→event` linkage; app + DB append-only | Global single chain; per-entity chains; per-tenant chain | Tenant isolation of the audit trail; bounded verification cost | Per-chain write concurrency at volume | Concurrency control decision (OD-051); sharding if volume requires | — |
| AD-013 | 2026-06-18 | Accepted (H-04) | **Hybrid reference-data tenancy**: global system reference (currencies, ISO codes, standard calendars/rating scales/classifications) shared read-only; investment reference (instruments, issuer/counterparty mappings, identifier precedence, internal classifications, private-asset IDs, overrides, custom hierarchies) tenant-scoped by default; tenant-override pattern for extending global; no cross-tenant sharing of proprietary/client/private data | All-global; all-tenant-scoped; hybrid | Standard taxonomies benefit from one curated source; investment data is client-specific and MNPI-adjacent so defaults to isolation (BR-17/AD-008) | Global tables RLS-exempt — must be write-restricted and leak-free | Regulatory data-residency; buyer requires full physical isolation | — |
| AD-013-R1 | 2026-06-22 | Accepted (H-04, P1B-0) | **Hybrid reference-data tenancy — implementation refinement of AD-013.** Approved global standard reference (**currency, calendar, rating_scale only**) is stored as `SYSTEM_TENANT_ID` rows inside the tenant-scoped tables under an **asymmetric RLS policy**: `USING` admits own-tenant **OR** `SYSTEM_TENANT_ID` for reads; `WITH CHECK` stays **single-tenant** so SYSTEM rows are writable only under system context (the shipped P1A-3 proof). Proprietary/client/private reference (legal_entity, issuer, counterparty, instrument, instrument_terms, identifier_xref) stays single-tenant and is **never** SYSTEM_TENANT/hybrid. Tenant overrides shadow a global of the same `code` and are resolved **application-layer** (tenant row wins; `UNIQUE(tenant_id, code)`) — **not** an automatic RLS merge. | AD-013 literal "separate RLS-exempt global tables"; replicate-per-tenant | Keeps FORCE RLS on every table (one proven isolation mechanism + the system-context-only write proof); avoids a second non-RLS security surface | Extended `USING` is not fully fail-closed with no context (returns only the non-proprietary global slice) — bounded by the proprietary-never-hybrid invariant; `data_source` is NOT hybrid | Regulatory residency / full physical isolation may force separate global tables | Refines AD-013 |
| AD-014 | 2026-06-18 | Accepted (H-06/H-04) | **No governed derived output without a bound, reproducible input snapshot.** Derived outputs (e.g., exposure aggregation PPM-004) may not be implemented until dataset-snapshot mechanics pin input record versions to the run | Allow derived outputs with nullable snapshot FKs now; block until snapshots exist | Enforces reproducibility (BR-6/BR-9, temporal §5); prevents non-reproducible governed numbers | Defers some value (PPM-004 → P2) | Minimal `dataset_snapshot` skeleton delivered (OQ-013a) | Realized by AD-018 (P2-3 `exposure_aggregate` is the first derived output under this gate; snapshot mechanics shipped P2-1 `0016`) |
| AD-015 | 2026-06-19 | Accepted (H-06/H-03) | **Dedicated `BYPASSRLS` ops DB role** for controlled cross-tenant ops only (audit verify/checkpoint, migration verification, diagnostics); the app/business paths never use it; ops credentials are separate, privileged, and audited | App role + per-tenant set_config only; superuser for ops; per-tenant loop | FORCE RLS blocks cross-tenant ops even for the owner; isolating the bypass to an audited privileged role preserves BR-17 | Privileged role is high-value — must be secured (BR-10) and never granted to the app | Need for finer-grained cross-tenant scoping | — |
| AD-016 | 2026-06-19 | Accepted (H-06) | **Per-session tenant-context propagation for RLS:** `set_config('app.current_tenant', tenant, true)` (transaction-local, auto-clears) set before queries, plus an explicit pool check-in `RESET app.current_tenant` (defense-in-depth for session-scoped/no-transaction paths) | Session-scoped set_config; SET (cannot bind); ORM-only tenant filters | Transaction-local context auto-clears (no stale-context leak across pooled connections); explicit RESET covers residual paths | A new code path that forgets context fails closed (false-deny), not open | Performance at scale; need for request-spanning multi-transaction context | — |
| AD-017 | 2026-06-23 | Accepted (H-06/H-04, P1C-0) | **P1C is a capture-only domain block.** Portfolio (EV), transaction (IA), position (FR) and valuation (FR) are **CAPTURED** as as-of-reconstructable records — positions/valuations are captured/reconstructable, **never derived analytics outputs**, never computed from prices, never derived from each other. P1C implements **NO** exposure aggregation, risk/VaR/ES, pricing or valuation models, corporate-action application, or `dataset_snapshot`. Exposure aggregation (PPM-004) + `dataset_snapshot` **stay P2 unless explicitly reopened** (AD-014 honored by exclusion). The portfolio hierarchy (single EV table, `node_type` + `parent_portfolio_id` subtree adjacency) is the entitlement portfolio-scope **ANCHOR**; portfolio-level ABAC **enforcement is deferred to P6+**. | Build positions/valuations as derived analytics now; pull exposure aggregation / a dataset_snapshot skeleton into P1C | Keeps P1C verifiable (stored as-of data) at proportionate cost; conforms to AD-005 (temporal classes), AD-013-R1 (proprietary=symmetric RLS), AD-014 (no derived output without a bound snapshot), AD-008/ENT-P-06 (scope) | Defers derived analytics (exposure/risk) to P2+; captured positions may disagree with the transaction log (no reconciliation in P1C — REQ-DQR-002/P7) | P2 dataset-snapshot mechanics arriving (reopens PPM-004); a confirmed need to derive positions | Conforms to / extends AD-005, AD-013-R1, AD-014, AD-008. **PPM-004 reopened/realized by AD-018 (P2-3 basic exposure — the reopen this trigger anticipated).** |
| AD-004-R1 | 2026-06-26 | Accepted (P2-0) | **Datastore storage-split refinement of AD-004 — Postgres-first behind the market-data repository interface.** ENT-020…025 market data + the ENT-049/050 reproducibility snapshots land in **PostgreSQL** (reusing the proven FORCE-RLS / audit / lineage / DQ rails) **through the AD-004-prescribed repo interface**, so a **TimescaleDB** backing can be swapped in **without a breaking change** when a measured volume/performance threshold is reached (AD-004's own revisit trigger). **Honest deviation from AD-004's "TimescaleDB initially":** Postgres-first for P2 volumes, Timescale **deferred, not rejected**. Resolves **OD-014** (and informs OD-046/OD-005). A future Timescale backing **MUST enforce tenant scoping in the repo interface** (hypertable chunks do not inherit FORCE-RLS for free). | AD-004 literal "Timescale initially"; split stores now | Keeps P2 on the proven Postgres RLS path at proportionate cost; the interface makes the future swap non-breaking; avoids a premature multi-store split before volume justifies it | Defers the time-series store; the Timescale leg carries an explicit tenant-scoping design obligation | Measured market-data volume/perf exceeds Postgres | Refines AD-004 |
| AD-019 | 2026-07-24 | Accepted (H-06/H-04) | **Analytical plane / warehouse strategy — hybrid, additive, read-only.** PostgreSQL stays the governed **system-of-record**; a future **analytical plane** (Snowflake the likely target) is added **later** as an *additive, read-only* serving layer fed by CDC/ELT — **the governed OLTP core never moves.** Rationale: the governance guarantees are enforced by four Postgres-native primitives a columnar warehouse does not replicate — FORCE-RLS tenant isolation (AD-008/016), `irp_prevent_mutation` append-only triggers (AD-012), `SELECT FOR UPDATE`/advisory-lock lifecycles, and enforced FK/UNIQUE backstops — all of which degrade from *engine-enforced* to *app-convention* on Snowflake. Consolidation = the plane becomes the single analytics/serving layer *downstream* of governed writes, NOT relocation of the writes. **Now (Phase 0): docs + discipline only** — capture this doctrine (diligence), freeze the governed-number **replication contract** (result rows + `calculation_run` + `dataset_snapshot(_component)` + `model_version`, lineage-intact, TR-15), protect the primary re-pointable read seam (`calc/reads.py`; covariance's by-run-id matrix reads are the recognized second read shape), and set build triggers. **Build the plane only when the FIRST fires:** a concrete external/DS/BI SQL consumer; a volume threshold (attach to OD-046); or a diligence date. Full text: [analytical_plane_strategy.md](../03_architecture/analytical_plane_strategy.md). | Full consolidation onto Snowflake (incl. OLTP core); PG-only forever; build the warehouse now | Keeps the governance thesis engine-enforced while giving BI/DS/external access a purpose-built store; the binding columns already port (NUMBER≈PreciseDecimal, uuid5/hash engine-independent) so the option is preserved at ~zero cost; the `calc/reads.py` seam makes the future read re-point a one-module change | Full consolidation would convert enforced invariants into conventions (rejected); a warehouse read that drops lineage violates TR-15; the plane must re-implement external-reader isolation (row-access policies + secure views) | A build trigger fires (external SQL consumer / volume threshold per OD-046 / diligence date) | Extends AD-004/AD-004-R1; broadens OD-046 |
| AD-018 | 2026-06-26 | Accepted (P2-3, ratified-in-planning) | **Basic exposure is the first governed derived number — `exposure_aggregate` (ENT-014).** Reopening/realizing PPM-004 under the AD-014 gate: `exposure_aggregate` is **IA, TRUE append-only** (in `APPEND_ONLY_TABLES`), **RUN-BOUND + SNAPSHOT-GATED** (no row without a non-null `input_snapshot_id` + a complete `calculation_run` binding `code_version` + the additive `environment_id`; `model_version` N/A — a model-less rollup). Grain = the per-holding atom `(portfolio_id, instrument_id, base_currency)` (no portfolio TOTAL rows in P2-3). **Convention = SIGNED market value v1** = `signed_quantity × captured valuation mark × effective captured FX rate` (`MARKET_VALUE` only); **no price lookup / valuation model / market-data ingestion / factor model**; gross/net/abs deferred. **Failure model split by timing:** pre-create refusal = zero run/result/audit; post-create FAILED = committed FAILED run + `CALC.RUN_STATUS_CHANGE` + zero result rows. Reuses `CALC.RUN_CREATE`/`CALC.RUN_STATUS_CHANGE` (+ an additive `calc/service.py` `outcome` param; `audit/service.py` FROZEN). Lineage `snapshot → run → result` (`DEPENDS_ON`/`ORIGIN`). **This is a GOVERNANCE-RATIFICATION entry only — no implementation; the P2-3 code is a later separate slice (migration `0018`).** | Defer exposure further; allow a derived number without a snapshot+run bind; mint a sham `model_version`; portfolio-TOTAL grain in v1; net/gross v1 | First governed derived number proves the AD-014/FW-RUN/TR-15 reproducibility gate end-to-end at proportionate cost; the per-holding atom is maximally auditable (1:1 to snapshot components) and a TOTAL is a deterministic Σ later; signed market value preserves the captured position sign | **NOT risk** — must not be read as VaR/ES/factor/stress/P&L/performance (scope-fenced by vocabulary + the `MARKET_VALUE`-only type + import fence); grain regret (mitigated — a coarser grain is additive); reproducibility depends on snapshot-pinned captured FX | A confirmed need for richer grain (subtree totals), gross/net/abs views, or per-result granular audit (`EXPOSURE.AGGREGATE_CREATE`/EVT-210, reserved) | Reopens/realizes AD-014 + AD-017 (PPM-004); conforms to AD-005 (IA), AD-006 (run-tracked engine), AD-008/AD-013-R1 (symmetric RLS) |
| AD-013-R2 | 2026-07-29 | Accepted (H-04, REF-1; USER-RATIFIED at the Wave-14 planning gate OQ-W14P-6(i) and the REF-1 gate OQ-REF-1-12) | **Hybrid reference-data tenancy — SECOND implementation refinement of AD-013: the approved global-standard-reference set grows from FIVE tables to SEVEN**, adding `classification_scheme` (ENT-066) + `classification_node` (ENT-067) under the SAME asymmetric RLS policy AD-013-R1 established (`USING` own-OR-SYSTEM; `WITH CHECK` single-tenant, so SYSTEM rows are writable only under system context). AD-013's own text is the warrant on BOTH halves: it classes "standard calendars/rating scales/**classifications**" as global system reference AND "**internal classifications**" as tenant-scoped — so a global standard SCHEME and tenant-scoped ASSIGNMENTS are one coherent reading, not two competing ones. `classification_assignment` (ENT-068) is therefore PROPRIETARY symmetric and **never** hybrid: it attaches to a firm's own issuers/instruments (OD-P1B-C's MNPI argument) and a vendor's issuer→code mapping is per-tenant licensed content (the `fx_rate` precedent). Tenant override is at **NODE grain** — a tenant row shadows one SYSTEM node under the same `scheme_id`, resolved application-layer (`dedupe_tenant_wins`, generalized at REF-1 to take a key function), never by RLS. | Leave classifications proprietary-symmetric (recorded consequence: no SYSTEM seed is readable, so there is NO curated global source and every tenant re-captures the taxonomy); AD-013's rejected "separate RLS-exempt global tables" (already refused at R1) | One curated global taxonomy with per-tenant override, on the one proven isolation mechanism (FORCE RLS everywhere + the system-context-only write proof) — the `rating_scale`/`rating_grade` precedent, which is the structural twin of a taxonomy vocabulary | Widens the surface where a SYSTEM literal appears in a `USING` arm. Bounded by the REF-1 floors: the EFFECTIVE write check (`COALESCE(with_check, qual)`) may never contain the SYSTEM literal, and every `tenant_id`-bearing table must be FORCE-RLS — both with executed negative controls | Regulatory data-residency or full physical isolation may still force separate global tables (unchanged from R1) | Refines AD-013 / AD-013-R1 |
