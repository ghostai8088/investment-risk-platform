# MF-1 Decision Record — multi-family factor exposure end-to-end (Wave-6 slice 3, final)

| | |
|---|---|
| **Status** | **CLOSED 2026-07-16 — SHIPPED AS RATIFIED.** Planning merged via **PR #49** = `0951784`; implementation `mf-1-impl` (tip `2898eb8`) merged via **PR #50** = `96b766c`, CI green (the required branch-protection checks gated the merge; the main-branch run reference is recorded at the roadmap row). NO migration (head stays `0040`). 4-finder review (all Fable): dispositions in Part 6; the 6 build deviations in Part 5.5. Ratified 2026-07-16 (OQ-MF-1-1…6, user: "All approved"). **Wave 6 is functionally COMPLETE — next = the Wave-6 close review.** |
| **Grounding** | Drafted 2026-07-16 on `main` = `cba6104` (FL-1 closed via PR #47 = `778891c`, CI #314). Census-verified (3 agents, all claims file:line or executed). **The chain has NO hard blocker — every stage is family-agnostic or already FL-1-widened**: covariance carries ZERO `factor_family`/currency references (`covariance_service.py` grep-verified; gates are DAILY factors `:324-328`, SIMPLE/DAILY pins `:183-191`, ≥ window common dates `snapshot/service.py:891-897` — no imputation); all six VaR-side files (`var_service.py`, `var_hs_service.py`, both kernels, `var_total_kernel.py`, `es_kernel.py`) carry ZERO family references, and the VaR binder wants any tenant-visible COMPLETED `FACTOR_EXPOSURE` run — no model-code whitelist (`var_service.py:703-708`, `:784-788`); `capture_factor_return` has NO family gate (`marketdata/factor.py:535-593`) and a factor's `currency_code` is OPTIONAL (`:395`, `:411-412`). The FL-1 substrate is live and test-proven end-to-end: the α=1 public-marks→OLS→promote chain (`test_factor_exposure_loadings.py:768+`), the through-VaR invariance (`:605-641`). **Six bite-points, all designable-around:** (1) the loadings COVERAGE GATE refuses any unloaded atom (`factor_service.py:411-434` — every instrument in the multi-family book needs a promoted head row BEFORE the loadings run); (2) the demo campaign is REFUSE-NOT-SKIP (`campaign.py:998-1002`) with a set-equality lock on exactly the 16 dossier codes (`:577-580`, loadings NOT among them) — MF-1 cannot re-run it and must extend ADDITIVELY; (3) the demo desmoothing version declares **α=0.4** (`campaign.py:234`), and α is model identity (OD-PA-1-E) ⇒ MF-1 registers a distinct α=1 version (the FL-1 test's exact shape, `test_factor_exposure_loadings.py:826-828`); (4) multi-family factors must be DAILY with SIMPLE daily returns spanning the declared windows (the covariance/HS discipline); (5) **the mixed-family-instrument conflict** (`factor_exposure_loadings_v1.md:136-142`): an instrument carrying BOTH currency proxy rows and non-CURRENCY loadings makes the PA-2 proxy family unrunnable — the existing private-asset chain instruments must NOT gain multi-family loadings; (6) scenario + active-risk REFUSE loadings runs (probe-pinned, `test_factor_exposure_loadings.py:644-746`) — evidence must not route through them. **The validation half:** there is NO mechanical condition-closure concept — closure is purely latest-outcome-wins at the recency read (`validation.py:379-397`; `model/service.py:556-570` reacts only to latest-REJECTED and latest-expired-EXCEPTION); conditions are ONE `String(2000)` text blob per record (`model/models.py:260`) whose flagship instances BUNDLE the CURRENCY-only clause with surviving posture riders; the AWC⇔conditions coupling is symmetric (`validation.py:163-174` — APPROVED forbids conditions), approving outcomes REQUIRE `next_review_due` (`:175-185`) under the write-time TIER_1 365-day ceiling (`:284-304`; all five flagships are TIER_1), the actor must be human (`:243-247` enforces `actor_type == "user"` ONLY — the `model.validate` permission is API-layer wiring; on the direct-service path the extension uses, the permission claim rests on the seeded 2L principal's role, `campaign.py:239`, the campaign's own recorded pattern), and every cited evidence run is re-resolved tenant-visible + COMPLETED pre-FK-stamp (`:209-227`). The TRIGGERED type has NO type-specific guard (`validation.py` — only EXCEPTION does). The 5 flagship AWCs sit on the campaign versions (`code_version="demo-mg1"`, `campaign.py:142`) of `risk.var.parametric/.historical/.parametric_total/.parametric_es/.parametric_es_total` with the shared condition core (`dossiers.py:199-205`) — the ONLY 5 validation rows containing the token 'FL-1' (test-pinned both directions, `test_demo_campaign_pg.py:182-204`). **The version grain is load-bearing**: the TRIGGERED records must target those exact `model_version_id`s or the AWCs stay operative. No exception expires anywhere near MF-1 (7 × ~2028-07, 3 × ~2029-07). **NO migration** — every table in the chain exists (heads: factor 0023 → validation 0039; alembic head `0040`). FL-1 debris for hygiene folds: two stale CURRENCY-only docstrings (`proxy_mapping.py:342-345`, `proxy_weight_service.py:202-203`) and the `_BINDING_PREDICATES` import-time length assert omits the loadings predicate (`snapshot/service.py:2666-2684`; the constant is 46 < 50 chars — latent, not live). |
| **Mandate** | Roadmap Part 2.9 slice 3, sized **M/L**: *"The headline payload on FL-1's substrate: a real equity/credit book through capture → loadings → exposure v2 → covariance → VaR/ES, with the PA-3 regression finally running on the Sharpe-1992 factor set its own citations name. Unlocks four blocked candidates … + the residual-shrinkage/EWMA + desmoothing-v2 ride-alongs teed since Wave 4. Ends with the TRIGGERED re-validation closing MG-1's CURRENCY-only condition — the full lifecycle demonstrated."* **The scope census (rule: read the recorded text, don't gloss it) resolves the row's two ambiguities:** (1) the four named candidates (FRTB liquidity horizons, partial ES by risk class, non-CURRENCY scenario shocks, the risk-class taxonomy) are recorded as UNLOCKED by multi-family, not delivered by it (`wave_5_close_review.md:111`: *"a hard prerequisite for four other candidates"*; the MAR33.12 constants are *"reference constants only, prose not code"*, `factor_exposure_loadings_v1.md:66-70`); (2) the residual-shrinkage/EWMA + desmoothing-v2 ride-alongs carry a genuine textual claim to ride (*"riding when it lands"*, `wave_4_close_review.md:157-158`) but are S/M candidates, not mandate clauses — adjudicated at OD-E, and they are NOT the covariance v2s, which Part 3 keeps explicitly wave-unassigned (`delivery_roadmap.md:165`). "§2.2's risk-class taxonomy" is the Wave-5 close's gloss — §2.2 itself states the SOTA bar, not a taxonomy; the taxonomy the platform adopted under that bar is FL-1's MAR33.14 five-class vocabulary, and the destination cite is thesis §2.1's sequencing consequence (*"the public-market factor/covariance/VaR machinery being built now IS the substrate"*). **"Real equity/credit book" means a genuine multi-family book SHAPE on synthetic/seeded data** — the user's 2026-07-15 scope note (roadmap Part 2.9 preamble) keeps real data and vendor feeds out of this wave. The only hard "Ends with" clause is the TRIGGERED re-validation. |

## Part 1 — Decisions at a glance (OD-MF-1-A…F)

| OD | Decision |
|---|---|
| **A — The book: a NEW dedicated multi-family portfolio in the living demo tenant; the Sharpe-1992 three-factor set; the mixed-family fence** | The demo tenant gains ONE new portfolio ("the multi-asset sleeve") holding **three NEW public instruments — two equities and one corporate-bond (credit) position — and NO existing instrument**, each with ~12 daily synthetic marks (economically plausible per TD-1), plus **three NEW factors: `MARKET` (equity broad index), `RATES` (rates index), `CREDIT_SPREAD` (credit index)** — the FRTB-named asset-class set the Sharpe-1992 lineage regresses on — captured `frequency=DAILY` with ~40 daily SIMPLE returns spanning every declared window (covariance + HS + the regression spans). **The mixed-family fence (bite-point 5, elevated to ratified design):** the existing private-asset-chain instruments (the PE fund, the two campaign equities) gain NO multi-family loading rows, so the shipped proxy-family and total-VaR history stays fully re-runnable and the FL-1 mixed-family limitation is never tripped in the living tenant; the fence is probe-pinned (a test asserts the legacy instruments carry zero non-CURRENCY head rows post-extension). Loadings for the three new instruments come from the REAL estimation chain (OD-B), not hand-captured weights — the roadmap's "the PA-3 regression finally running on the Sharpe-1992 factor set its own citations name". Every new-book atom therefore carries promoted loading rows before the loadings run — the coverage gate (bite-point 1) is satisfied by construction, and one test deliberately probes the refusal (an unloaded fourth instrument added to a THROWAWAY portfolio, not the sleeve). |
| **B — The mechanism: an ADDITIVE extension runner (`run_demo_multifamily_extension`), refuse-not-skip on its OWN footprint; the base campaign untouched** | The base campaign cannot be re-run (refuse-not-skip) and its 16-code set-equality lock is a ratified integrity check — both stay byte-untouched. MF-1 ships a SEPARATE extension entry point in `irp_shared/demo/` (+ a thin `scripts/` CLI, the campaign shim's shape): **requires** the base campaign to have been seeded (refuses on an empty tenant — the extension extends the LIVING tenant, it does not bootstrap one); **refuses** if its own footprint exists (probe: the loadings model code already registered in the tenant — the same refuse-not-skip posture, test-pinned with a double-run probe). The extension: (1) registers **`risk.factor_exposure.loadings`** in the demo tenant (the 17th code) + a distinct **α=1 `perf.return.desmoothed_geltner` version** (α is version identity; the campaign's α=0.4 version is untouched); (2) assigns tiers to what it registers (the MG-1 regime — nothing new stays untiered in the living tenant); (3) seeds the OD-A book; (4) runs the OD-C chain; (5) files the OD-D validation records. The 16-code campaign tests keep passing on a base-only tenant; a new PG suite pins the post-extension state (17 codes, the chain COMPLETED, the grep flip). |
| **C — The chain: estimation → loadings → covariance → the FIVE flagship numbers, each bound to the demo-mg1 versions the AWCs sit on** | Per new instrument: marks → the **α=1 desmoothing run** (identity transform, the disclosed FL-1 detour; n marks ⇒ n−1 observed ⇒ n−2 periods, `_MIN_MARKS=4`) → **PA-3 OLS on the FULL three-factor set** (k=3 ⇒ floor `max(N, 5)` = 5 with the reused demo-mg1 `min_observations=4`; 10 periods clears it) — the Sharpe-1992 shape: every subject regressed on the whole asset-class palette, near-zero betas on unloaded factors being honest OUTPUT, not excluded inputs (regressing each instrument only on its generator's true support would be epistemically circular for a demo — the verifier pass's correction, folded) → the ANALYST **promotes the structural coefficients** per instrument into `proxy_mapping` (REGRESSION method; one estimate run per instrument, k promote calls for the economically-loaded factors — the PA-3 deliberate-per-coefficient design used as designed; the near-zero coefficients stay first-class on the estimate rows, unpromoted; all of an instrument's promoted rows cite ONE estimate run — the exact shape the total-VaR single-cited-run gate requires, `snapshot/service.py:2246-2251`). Then: one `exposure_aggregate` run over the sleeve (atoms FX-uniform by construction) → the **loadings-family factor-exposure run** (the FL-1 predicate + coverage gate) → **covariance** over the three factors (DAILY/SIMPLE, the declared demo-mg1 window 30) → **VaR, HS-VaR, total VaR, ES, ES-total — one COMPLETED run each, bound to the SAME `demo-mg1` model versions that carry the AWCs** (the version grain: evidence attaches where the condition lives; each TRIGGERED record cites its OWN model's run — the MG-1 evidence pattern). Total/ES-total work by construction: the sleeve's loadings are REGRESSION-promoted with citable estimates (the PA-4 residual leg pays real residual variance for all three instruments), and estimate ages are fresh (the BT-2 gate passes). **Disclosed (the verifier pass's finding): the residual leg's per-instrument market value is the SUM of its factor-exposure rows = MV·Σw_f** (`var_service.py:744-747` feeding `var_total_kernel.py:45-52`) — shipped PA-4 behavior that first becomes material at multi-factor scale under unconstrained OLS (Σw ≠ 1); stated in the dossier prose and the loadings model's INITIAL findings, and the numeric finder re-derives with this scaling. Scenario and active-risk are deliberately NOT in the evidence chain (they refuse loadings runs — the recorded, probe-pinned posture). A single run per family is the honest evidence shape for a TRIGGERED re-validation (the INITIAL's 8-run series demonstrated operation; the TRIGGERED demonstrates the CAPABILITY the condition named) — disclosed in the dossier prose, not padded. |
| **D — The TRIGGERED re-validation: fresh AWC per flagship, the conditions RE-SCOPED (the CURRENCY-only clause removed, the surviving riders restated WITHOUT the 'FL-1' token); the loadings model gets its own INITIAL validation** | The census killed the tempting "APPROVED — condition closed" shape: APPROVED legally FORBIDS conditions (`validation.py:171-174`), and the flagship condition blobs bundle the CURRENCY-only clause with riders that remain TRUE post-MF-1 (joint-normality posture; the BT-2 read rule + v1 grandfather; the ES non-reconciling row; the HS window floor) — and the condition's own second clause ("specific/idiosyncratic factor risk = 0") is only PARTIALLY discharged (total VaR pays residual variance for REGRESSION-cited instruments; plain parametric still carries specific-risk=0 as a registered limitation). Erasing true riders to reach a conditions-free outcome would be exactly the false-closure MG-1's honesty posture forbids. **The ratified shape: one TRIGGERED + APPROVED_WITH_CONDITIONS record per flagship (×5), against the SAME `demo-mg1` version, whose conditions text is FRESHLY DRAFTED per model in the ratified dossier table** — NOT mechanically derived (the verifier pass killed "restated verbatim minus the CURRENCY clause": the still-true specific-risk clause is grammatically FUSED into the removed sentence, and three of the five riders are cross-referential or self-labelled non-conditions — a mechanical cut would erase true conditions or ship self-contradicting text). Each new conditions blob carries: **an explicit re-scoped specific-risk clause** (fully true for parametric/HS/ES: no residual variance term; the total/ES-total residue: non-proxied and MANUAL-method atoms carry zero idiosyncratic variance) + the surviving riders **restated in substance with the framing re-drafted** (historical's window floor and total's BT-2 read rule + v1 grandfather survive near-verbatim; parametric's normality posture, the ES non-reconciling row, and the ES-total cross-reference are re-drafted standalone) + **zero occurrences of 'FL-1'** anywhere in the new records (the closure prose says "the multi-family remediation (this slice)"). The grep discipline inverts on the CONDITIONS surface, mirroring the shipped pin: post-extension, a tenant-wide conditions grep finds the token in exactly the 5 HISTORICAL AWC rows (append-only; 3 of those also carry it in `scope_summary` via the constant-series note — recorded, the conditions-grep is the pinned surface) and the LATEST validation per flagship version is token-free — a test pins both directions. Findings: closure of the CURRENCY-only finding stated in prose + the surviving registered-limitation keys re-cited (the fail-loud key mechanism) **with the disclosure that the immutable demo-mg1 limitation texts predate the multi-family widening** (they say "CURRENCY-family indicator-loading factor model" — the finding prose instructs the re-read; the stale `bootstrap.py` limitation WORDING for new registrations is recorded FL-1 debris, adjudicated record-not-fix: editing shipped registrar text is behavioral, outside this fence — Part 3 item 11). Evidence: each record cites its OWN model's OD-C run (CALCULATION_RUN) + one DOCUMENT row referencing this record; `next_review_due` = filing-day + 365 (the TIER_1 write-time ceiling is strict-`>` against `utcnow` — filing-day arithmetic, never a fixed calendar date); scope_summary names the multi-family closure explicitly. **The loadings model itself** gets an **INITIAL validation (AWC)** — the criterion is the MG-1 precedent (the ratified dossier table IS the human validation judgment; evidence existence was never the INITIAL-vs-EXCEPTION differentiator — the verifier pass's correction), with findings drawn from its TWO registered limitation rows (the unmodeled loaded-atom residual; the price-return-betas + short-window-noise row — one row, two clauses), the loadings exposure run + through-VaR run cited, conditions = the residual/estimation posture, `next_review_due` = filing-day + 365 (within the expected TIER_2 730 ceiling; the Step-2 tier assignment lands BEFORE the validation — the ordering dependency stated). **The α=1 desmoothing version gets a time-boxed EXCEPTION** (the campaign's shipped mechanism, no 'FL-1' token by its shape) — validation is VERSION-grain and the α=0.4 version's exception does not cover the new `demo-mf1` version; without this, three chain runs would execute on the bare use-before-validation default forever, the exact regime hole MG-1's exceptions close (the verifier pass's finding, folded as ratified shape). The dossier texts live in `demo/dossiers.py` as an MF-1 section — **the user's OQ-3 ratification of that table IS the human validation judgment** (the MG-1 precedent; the seeded 2L principal records them). |
| **E — The ride-alongs: residual-shrinkage/EWMA + desmoothing-v2s do NOT ride; RE-TEED to the Wave-6 close with their materiality cap now LIFTED; the covariance-v2 disambiguation recorded** | The Wave-4 tee (*"S/M ride-alongs, materiality capped until multi-family lands"*) reads most strongly as riding-when-it-lands — but each is a genuine methodology slice (a new registered model version + referent + external-benchmark citations + dual-path goldens: Ledoit-Wolf/RiskMetrics-class work for the residual leg; GLM MA(q)/Okunev-White for desmoothing v2), and folding two S/M methodology slices into an M/L slice whose hard clause is a governance demonstration would either bloat MF-1 to L/XL or ship the v2s under-benchmarked — both worse than the honest re-tee. **Ratified: MF-1 ships NO new quantitative method; the ride-alongs are re-teed to the Wave-6 close as named Wave-7 candidates with their materiality cap explicitly LIFTED** (multi-family has landed — the cap's own condition is discharged; the close review must sequence them or record why not). **The disambiguation the census demanded:** *residual* shrinkage/EWMA (PA-4's OD-E/OD-G v2s — the idiosyncratic-residual estimate) and the *covariance* v2s (P3-4's OD-P — shrinkage/EWMA/correlation/annualization on the factor matrix) are two different objects sharing a word; the covariance v2s stay standing deferrals with no assigned wave (roadmap Part 3), unchanged by this slice. |
| **F — Scope fence + hygiene ride-alongs** | **NO migration** (census-confirmed: every chain table exists; head stays `0040`); NO new permission (`model.validate`/`risk.run`/`risk.view`/`perf.run`/`marketdata.ingest` all reused); NO new EVT/audit code (`audit/service.py` FROZEN); NO new canonical id (ENT-058 stays the paper mint); NO new model CODE (the loadings family and the α=1 version instantiate SHIPPED codes in a tenant); NO API/FE change (the runs view absorbs the new runs; the FE families all exist since FL-1); **scenario's two gates + active-risk's whitelist stay byte-untouched** (OD-D routes around them; the relaxation stays the recorded Wave-7-or-later candidate WITH the two-gate inventory); the allocation + proxy families byte-untouched; real data stays out (the user's scope note). **The FL-1-debris hygiene folds ride** (all census-cited, none behavioral): the stale CURRENCY-only docstrings corrected to name the FL-1 widening (`marketdata/proxy_mapping.py:342-345`; `risk/proxy_weight_service.py` at ~:202-203 AND its module docstring's two further stale lines at :28/:34 — the verifier pass's addition); the loadings predicate added to the `_BINDING_PREDICATES` import-time length assert (latent completeness gap — the constant is 46 chars, verifier-computed; the tuple feeds ONLY the assert, so the addition cannot fail at import). The stale CURRENCY-family WORDING inside the shipped registrar limitation constants (`risk/bootstrap.py:665-667` and the HS analogue) is NOT folded — editing it changes what new registrations record (behavioral) — and is instead disclosed in the TRIGGERED finding prose and recorded as debris (Part 3 item 11). **Review: 4-finder** (adversarial on the extension's refuse-not-skip + the version-grain targeting + the grep flip; numeric on the estimation→through-VaR chain over the new book; doctrine on the re-validation texts vs SR 26-2 posture + the census citations; scope-fence). |

## Part 2 — External benchmark research (rule 6)

**MF-1 ships NO new quantitative method** — the slice composes shipped, individually-benchmarked machinery (PA-1 desmoothing, PA-3 OLS, FL-1 loadings/FRTB vocabulary, P3-4 covariance, P3-5/PA-4/ES-1 VaR/ES, MG-1 validation). The external-benchmark burden was paid at those slices; the citations CARRIED here (all previously VERIFIED in their records, none re-fetched):

- **Sharpe (1992)** — returns-based style analysis on asset-class indices; public instruments are the paper's OWN primary use case (verified at PA-3 + the FL-1 pre-ratification pass). MF-1 is the first time the platform's regression actually runs on a multi-asset-class factor set — the lineage's home case, previously exercised only on CURRENCY factors. The unconstrained-OLS divergence from classic RBSA (Σw=1, w≥0) stays recorded in both referents.
- **BCBS d457 MAR33.14** (verified at FL-1) — the five broad risk classes; MF-1 populates MARKET/RATES/CREDIT_SPREAD with governed factors and real loadings, making the FL-1 vocabulary load-bearing for the first time. The names still confer NO capital semantics (FL-1 Part 3 item 9 carries forward; the MAR33.12 constants stay prose).
- **SR 26-2 / OCC 2026-13** (verified at VW-1/MG-1) — the re-validation posture: validation is ongoing, and material changes in use or scope trigger re-validation; MG-1's condition named its remediation, and the TRIGGERED record with fresh outcomes-analysis evidence is the recorded discharge shape. The remediation/finding-closure LIFECYCLE (SR 26-2 §VI) stays the named MG-2 candidate — MF-1 demonstrates closure-by-supersession, not a remediation-tracking feature (Part 3 item 4).
- **Negative check (the BT-2 lesson — verify the citation behind the honesty text):** no MF-1 text claims a regulatory basis for "closing a condition"; the closure semantics are HOUSE process (latest-outcome-wins is this platform's recorded read discipline), stated as such.

## Part 3 — Limitations, honesty items + out of scope (recorded)

1. **The TRIGGERED re-validations remain AWC** — multi-family closes the CURRENCY-only clause; the surviving riders (normality, the BT-2 read rule + v1 grandfather, the ES non-reconciling row, the HS window floor) and the partially-discharged specific-risk clause remain true and remain conditions. "Closed" means the named condition, not conditions-free.
2. **Closure is by supersession, not by a closure record** — no mechanical condition lifecycle exists (the MG-2 candidate); the historical AWC rows stay visible forever (append-only, surfaced by the list API). The new records state the closure in prose.
3. **The sleeve's loadings are price-return betas from short single-name regressions** (the FL-1 limitation at multi-family scale) — std errors + R² stay first-class on the estimate rows and are cited in the loadings model's INITIAL findings.
4. **The α=1 detour multiplies** (three new instantiations). The recorded trigger for the raw-return pin kind v2 ("if the α=1 read ever confuses a validator") has NOT fired — the detour is disclosed in both referents and now in the dossier prose; the v2 stays trigger-based.
5. **The mixed-family-instrument conflict is FENCED, not fixed** — the legacy instruments carry no multi-family loadings (OD-A, probe-pinned), so the proxy family stays runnable on the legacy book; the single-family-per-consumer / proxy-relaxation follow-on stays recorded and unassigned.
6. **Scenario stays CURRENCY-only; active-risk stays allocation-only** — both refusals are probe-pinned and byte-untouched; the scenario relaxation (BOTH gates + per-class shock methodology, no recorded acceptance criteria) is re-teed to Wave-7-or-later via the close review.
7. **FRTB liquidity horizons + partial ES by class stay unlocked-not-delivered** — the MAR33.12 constants remain prose; nothing in MF-1 implements capital arithmetic.
8. **The five evidence runs are single runs per family, not series** — the honest TRIGGERED shape (capability demonstration), disclosed in the dossiers; the INITIAL records' 8-run operational series are not re-produced.
9. **The demo tenant premise inverts mid-slice by design**: seeding non-CURRENCY factors makes the flagship condition's premise false until the TRIGGERED records land — acceptable INSIDE one slice (the FL-1 Grounding fence anticipated exactly this); the extension is single-commit (the campaign shim's shape: services flush, the CLI commits once at the end), so a mid-chain failure rolls back WHOLE — the tenant is clean and re-runnable, and the own-footprint probe is the belt-and-braces for any caller that commits early (registrations run first, so any partially-committed state contains the loadings model and the probe fires).
10. **The residual-shrinkage/EWMA + desmoothing-v2 materiality cap is LIFTED but the slices are NOT scheduled here** — the Wave-6 close review owns the sequencing decision (OD-E); recording the lift without acting on it is deliberate, not an omission.
11. **The immutable demo-mg1 limitation/finding texts predate the multi-family widening** — "CURRENCY-family indicator-loading factor model" is byte-frozen in the seeded rows AND in the shipped registrar constants (`risk/bootstrap.py:665-667` + the HS analogue); the TRIGGERED finding prose discloses the re-read ("the bound factor set"), and correcting the registrar WORDING for future registrations is recorded FL-1 debris for a hygiene slice (behavioral for new registrations — outside this fence).
12. **Two standing campaign-suite capabilities RETIRE at MF-1** (the verifier pass's finding, disclosed not discovered): the campaign fixture's tolerate-already-seeded mode (which names the CLI-created living tenant as a supported target) and the RD-3-era dirty-schema double-run both break permanently once the living tenant is extended — the campaign suite's pins (exactly-16 codes, exactly-5 AWCs, 16 tier events) are FALSE on an extended tenant. From MF-1 on, campaign-suite validation is fresh-schema-only, and the CI ordering is load-bearing: the extension's PG step runs AFTER the campaign step (pinned in ci.yml). The campaign suite itself stays byte-untouched (the OD-B fence).

## Part 4 — Open decisions (OQ-MF-1-1…6) — pending ratification

- **OQ-MF-1-1 — OD-A: the book** = a NEW dedicated multi-family portfolio (2 equities + 1 credit instrument, ~12 daily marks each, synthetic per TD-1) + the three-factor MARKET/RATES/CREDIT_SPREAD set (DAILY, ~40 SIMPLE returns), with the **mixed-family fence** (legacy instruments gain no multi-family rows; probe-pinned). *Recommend APPROVE. Sub-fork: also give the sleeve a CURRENCY leg (a fourth, FX factor) for a four-factor regression — richer but dilutes the "new capability" story and adds nothing the legacy book doesn't already prove; recommend not.*
- **OQ-MF-1-2 — OD-B: the additive extension runner**, refuse-not-skip on its own footprint, base campaign required and byte-untouched (the 16-code lock stays). *Recommend APPROVE — the only alternative (reset + re-run an extended campaign) destroys the living tenant's history, which is the demonstration.*
- **OQ-MF-1-3 — OD-C + OD-D: the evidence chain (k=3 Sharpe-1992 regressions; analyst-promoted structural coefficients; five single runs bound to the demo-mg1 flagship versions) + the validation set: TRIGGERED AWC ×5 with FRESHLY-DRAFTED per-model conditions (the explicit re-scoped specific-risk clause + the surviving riders re-drafted standalone; zero 'FL-1'), + the loadings model's INITIAL (AWC), + a time-boxed EXCEPTION for the α=1 desmoothing version** — the dossier-table ratification IS the human validation judgment (MG-1 precedent). *Recommend APPROVE — the census-verified alternative (APPROVED, riders demoted to findings) forces erasing still-true conditions to satisfy the outcome coupling; rejected as false closure. The verifier pass additionally killed the mechanical "verbatim minus the CURRENCY clause" derivation (it would erase the fused specific-risk clause) — the conditions are drafted per model in the dossier table you ratify. Sub-fork within: APPROVED for any flagship whose riders are all judged findings-grade — recommend not; consistency across the five beats per-model cleverness.*
- **OQ-MF-1-4 — OD-E: the ride-alongs do NOT ride** — re-teed to the Wave-6 close with the materiality cap recorded as LIFTED; the residual-vs-covariance shrinkage disambiguation recorded. *Recommend APPROVE — the alternative (fold one or both S/M methodology slices in) trades the slice's governance headline for under-benchmarked math or an L/XL slice. If you want one anyway, the residual-EWMA leg is the smaller; say so and Part 1 OD-E is re-cut before implementation.*
- **OQ-MF-1-5 — OD-F: the scope fence** (no migration / no new permission / no new code or canonical id / scenario + active-risk untouched / real data out) **+ the three hygiene folds** (two stale docstrings, the predicate-assert completeness gap). *Recommend APPROVE.*
- **OQ-MF-1-6 — the closure test discipline**: the pinned surface is the CONDITIONS column (mirroring the shipped `test_demo_campaign_pg.py:182-204` pin): post-extension, a tenant-wide conditions grep finds 'FL-1' in exactly the 5 historical AWC rows, the LATEST validation per flagship version is token-free, and every NEW record avoids the token everywhere; the base-campaign tests keep passing on a FRESH-SCHEMA base-only tenant, with the extension's CI step pinned AFTER the campaign step (Part 3 item 12 records the two retired campaign-suite capabilities). *Recommend APPROVE — this is the mechanical meaning of "the condition closes" and the only spec any record states.*

## Part 5.5 — Implementation deviations from the ratified plan

1. **The desmoothing registrar gained an optional `version_label` parameter** (default
   `DESMOOTHED_RETURN_VERSION_LABEL` — every existing caller byte-unchanged). The plan assumed the
   α=1 version registers like the FL-1 test's — but that test ran in a FRESH tenant; the demo
   tenant already holds the α=0.4 version under the fixed `v1` label, and the registrar's identity
   discipline (correctly) refuses a same-label different-(code_version, alpha) twin with "mint a
   new version_label instead". The fix follows the registrar's own prescription: the α=1 version
   registers as **`v1-alpha1`**, and the label mint stays INSIDE the family registrar (using the
   generic registration path would have been the squatted-twin pattern the registrars exist to
   prevent). Shared-code, backwards-compatible, discovered by the PG suite on first execution.
2. **The `_BINDING_PREDICATES` completeness fold found TWO missing constants, not one**: the
   loadings predicate (the census's find) AND `PROXY_WEIGHT_BINDING_PREDICATE` (PA-3-era debris
   the census missed — 45 chars, latent). Both added to the import-time length assert.
3. **Three small plan-text drifts, logged not hidden** (the scope finder): (a) the plan's
   "deterministic ids via `demo_id` + `mf1:` prefixes" clause was DROPPED — the campaign itself
   uses `demo_id` only for the tenant id, and re-seedability after a schema reset needs no fixed
   entity ids; (b) instrument codes shipped as `MF-EQ-A/MF-EQ-B/MF-CR-A` (the plan wrote
   `MF1-…`); (c) the factor-return span shipped as 51 calendar days (the plan's "~40") —
   over-covering every declared window, direction-safe.
4. **The PG prereq probe was replaced by unit-tier coverage** (the scope finder's MED-2): CI's
   own load-bearing ordering (campaign seeds before this suite) makes a PG prereq probe
   self-skip in every automated environment, so both refusal guards are covered in
   `test_demo_multifamily.py` (SQLite — the guards fire before any PG-specific work, and
   `set_tenant_context` is a no-op off PostgreSQL).
5. **The idiosyncratic cycles were shrunk 10× after the numeric finder REFUTED the "near-zero
   unloaded betas" claim at the original amplitude** (eps-projection bias `(X'X)⁻¹X'ε` put
   unloaded betas at ±0.24 and doubled one structural coefficient; bias scales linearly in eps,
   so |ε| ≤ 5e-5 puts every recovered beta within ~0.03 of its structural value). The promoted
   values always flowed self-consistently — the defect was characterization, fixed at the
   generator, not the prose.
6. **The MF-1 records carry their OWN independence-disclosure variant** (the doctrine finder's
   HIGH): the reused MG-1 constant attributed the human ratification to OQ-MG-1-6; the MF-1
   variant re-points the final clause at OQ-MF-1-3 (the MG-1 constant stays byte-untouched — the
   campaign's filed rows cite it correctly). The frequency-conversion note was also de-duplicated
   (conditions-only; it had been rendered twice per total/ES-total record).

## Part 6 — Review dispositions (the 4-finder pass — run 2026-07-16, ALL Fable; findings + dispositions, not verdict tallies)

**Adversarial** (executed probes: 131 filed-text surfaces token-grepped; concurrency, column-length,
identity and guard probes on SQLite): **ZERO HIGH.** Confirmed: the grep discipline (zero 'FL-1' in
every filed text); all 16 finding keys fail-loud-unique; the loadings registration is the FIRST
write so any partial commit trips the footprint probe; concurrent double-runs cannot double-file
(registrar savepoints + the portfolio/instrument unique constraints); zero `session.commit()` in
the touched service chain (the single-commit claim EXECUTED-verified); the version grain
mis-filing is impossible via `_resolve_base_version`; every record text fits its column (max
1632/2000). Folded: the fail-open TIER_1 default in the α=1 exception filing → refuse-on-drift
(the module's only silent default, killed); labelled refusals replace raw `MultipleResultsFound`
in both resolvers; a non-empty `version_label` guard + the per-label-identity docstring note in
the registrar; the version-grain pin added to the grep-flip test; the context-contract docstring
(the detach-replaces-caller-listener trap, inherited from the campaign shape — documented, not
re-engineered); the standalone-fresh + downgrade-smoke edge recorded in the suite docstring.
Accepted/recorded: the loadings model DESCRIPTION carries 'FL-1' into the tenant's `model` row
(`bootstrap.py` registrar constant — OUTSIDE the pinned conditions surface, behavioral to edit;
the Part 3 item 11 adjudication applies; disclosed here so a future tenant-wide grep does not
read it as a leak).

**Numeric** (independent re-implementation + numpy): the mark generator **byte-identical** (all 36
marks); the spot golden's value flow CONFIRMED exact (the stored atom `qty × 6dp-mark ×
identity-FX` is idempotent under the 6dp quantize, so the test's expectation is arithmetically
identical to the service computation); the covariance is PSD/non-singular (eigenvalues 3.2e-7 …
1.5e-5, cond 45) and window-start-invariant; VaR σ ≈ 0.15% of book, HS VaR nonzero with clean
tie handling; TD-1 realism confirmed. **One MED REFUTED-and-fixed: the original idiosyncratic
cycles biased the OLS** (unloaded betas to ±0.24; one structural coefficient doubled — the bias
is exactly `(X'X)⁻¹X'ε`, amplified by corr(MKT,RATES) = −0.81 over 10 observations) → the cycles
shrunk 10× (bias scales linearly; recovery now within ~0.03 everywhere), Part 5.5 item 5. Noted:
the shipped kernel fits WITH an intercept (the record's OD-C never claimed otherwise).

**Doctrine**: all five condition texts match the ratified OD-D shape with no surviving substance
dropped; no claim false against code (the diagonal-residual, REGRESSION-only-pin, and
zero-cross-correlation claims all code-verified); the frequency-conversion note factually right
(√(91·252/365) ≈ 7.9× understatement on daily-period estimates); the date arithmetic
off-by-one-safe; the referent amendment house-legal (the BT-2/FL-1 dated-amendment precedents).
**One HIGH FOLDED: the reused MG-1 independence disclosure misattributed the ratification**
(OQ-MG-1-6) behind every MF-1 record → the `MF1_NON_INDEPENDENCE_DISCLOSURE` variant re-points
the final clause at OQ-MF-1-3 (the MG-1 constant byte-untouched). LOWs folded: the
frequency-note de-duplication (conditions-only). Accepted/recorded: `EXCEPTION_CONDITIONS`
reused verbatim reads MG-1-era in its JUSTIFICATION clause (historically true; the fresh
version-grain justification lives in the record's own scope_summary); the MG-1 module
docstring's "greps for 'FL-1'" imprecision (a frozen file's comment; the runner resolves by
version, the TEST greps).

**Scope fence**: **all 14 clauses PASS** (campaign byte-untouched; dossiers purely additive; the
registrar diff exactly the optional parameter with all 15 existing call sites byte-equivalent;
no migration/permission/EVT/FE/API change; no stray files). MEDs folded: the loadings-snapshot
predicate pin added to the chain test; the dead PG prereq probe replaced by unit-tier SQLite
coverage of BOTH guards (Part 5.5 item 4); the three plan-text drifts logged (Part 5.5 item 3).
LOW folded: the duplication-not-import comment on the campaign helpers (byte-frozen fence +
extension-typed errors — zero behavioral divergence, diff-verified).

## Part 5 — Implementation readiness gate

Implementation follows `mf_1_implementation_plan.md` on ratification of OQ-MF-1-1…6. Merge preconditions: the 4-finder review folded, `make check`, full local-PG (fresh; the extension suite double-run for its refuse-not-skip probe), `alembic check` showing NO new revision, CI green. Closure follows the OQ-W5C-5 closure-stamp checklist, then the **Wave-6 close review** (the mandatory Part-4 rule-2 re-baseline, with the OD-E re-tee on its agenda).

### Planning verification (the 3-verifier pre-ratification pass — run 2026-07-16, three fresh-context Fable verifiers: chain-correctness / governance-doctrine / scope+citations; 30+ citations spot-checked, ZERO wrong line numbers)

| # | Finding | Disposition |
|---|---|---|
| V1 | HIGH (governance + scope, convergent): the conditions-surgery recipe ("restated verbatim minus the CURRENCY clause") erases the still-true specific-risk clause — it is grammatically FUSED into the removed sentence (`dossiers.py:200-202`) — and three of five riders cannot be restated verbatim (parametric's declares itself not-a-condition; es/es_total cross-reference the removed text) | FOLDED as ratified shape: OD-D re-cut — conditions FRESHLY DRAFTED per model in the OQ-3 dossier table, with the explicit re-scoped specific-risk clause per family |
| V2 | HIGH (governance): "base-only invariance" has an unstated CI-ordering dependency (all PG suites share one DB; four campaign pins break on an extended tenant) and silently retires two standing capabilities (the fixture's tolerate-living-tenant mode; the RD-3 dirty-schema double-run) | FOLDED: plan Step 7 pins the ci.yml step AFTER the campaign step; Part 3 item 12 records both retirements honestly |
| V3 | HIGH (scope) / MED (chain), convergent: the record said k=3, the plan said k=1-or-2 — a direct contradiction in ratifiable text | FOLDED to k=3 (the Sharpe-1992 shape the mandate names; per-instrument candidate sets equal to the generator's support would be epistemically circular); the analyst promotes the structural coefficients — the PA-3 deliberate-promote design used as designed |
| V4 | MED (chain + scope, convergent): the new α=1 `demo-mf1` desmoothing VERSION would end the slice with zero validation rows — binding forever on the naked SR 26-2 default, against MG-1's every-model-validated-or-excepted end state (validation is VERSION-grain) | FOLDED as ratified shape: a time-boxed EXCEPTION for the α=1 version rides OD-D |
| V5 | MED (chain): the total-VaR residual leg's per-instrument market value = MV·Σw_f (`var_service.py:744-747` → `var_total_kernel.py:45-52`) — shipped PA-4 behavior, first material at multi-factor scale under unconstrained OLS | FOLDED: disclosed in OD-C, the dossier prose, and the loadings INITIAL findings; the numeric finder re-derives with this scaling |
| V6 | MED (chain): mark/factor-return calendar alignment is a hard OLS precondition (per-period coverage, no zero-fill, `proxy_weight_service.py:293-306`) | FOLDED: plan Step 3 seeds factor returns calendar-daily (the campaign precedent) |
| V7 | MED (chain + governance, convergent): the 'FL-1' grep surface was underspecified — the token also lives in 3 historical `scope_summary`s via the constant-series note | FOLDED: OQ-6 pins the CONDITIONS surface (mirroring the shipped pin); new records avoid the token everywhere; the scope_summary occurrences recorded |
| V8 | MED (governance + scope, convergent): the loadings INITIAL omitted `next_review_due` — an approving outcome REFUSES without one (`validation.py:181-185`); the tier-before-validation ordering was unstated | FOLDED: filing-day + 365 (within TIER_2's 730); Step-2-before-Step-6 dependency stated; flagship dates re-worded to filing-day arithmetic (the ceiling is strict-`>` vs `utcnow`) |
| V9 | MED (governance): the re-cited finding texts embed the frozen CURRENCY-family wording; the registrar constants (`bootstrap.py:665-667` + HS analogue) are a third piece of FL-1 debris the census missed — but editing them is behavioral | FOLDED: the finding prose discloses the re-read; the registrar wording adjudicated record-not-fix (Part 3 item 11) |
| V10 | MED (governance): Step 7's `irp_app` grant-list instruction contradicts the campaign suite's own recorded pattern (a whole-surface writer runs as the OWNER engine, `test_demo_campaign_pg.py:3-7`) | FOLDED: the extension suite adopts the owner-engine pattern |
| V11 | LOWs (all three): "four/five" → five; evidence wording aligned to own-run-per-record (the MG-1 pattern); `validation.py:243-247` over-cite (human-only ONLY; the permission is API-layer) re-worded; the INITIAL-vs-EXCEPTION rationale corrected (the dossier ratification is the criterion, not evidence existence — MG-1 filed EXCEPTIONs for models WITH cited runs); the loadings findings map to TWO limitation rows, not three; `proxy_weight_service.py` module docstring carries two MORE stale lines (:28/:34) — added to the hygiene fold; "the tenant base" → explicit `base_currency` arg (no tenant-level base exists); Step 5's failure story upgraded to single-commit-rollback (stronger than claimed); the closeout checklist's own-Part-6-tail item restored; n−1 observed / n−2 desmoothed stated exactly | ALL FOLDED in place |
| V12 | CONFIRMED (the load-bearing claims that survived attack): the five flagship runs bind the demo-mg1 versions over new sleeve snapshots (no code whitelist; the total-family gates pass by construction and the one-estimate-run-per-instrument shape is REQUIRED, `snapshot/service.py:2246-2251`); demo HS = c=0.95/window 21, covariance window 30 — ~40 returns clear both; promote = one (instrument, factor) per call; the coverage gate is atom-grain and subset-loading tolerates; 12 marks ⇒ 10 α=1 periods; α ∈ (0,1] registrable; no FX rows needed single-currency; tier is MODEL-grain; TIER_2 for MEDIUM/MEDIUM matches the covariance precedent; the version-grain targeting is correct in both drafts; rule-6 carried-citations posture holds for a no-new-method slice; the OD-E re-tee is a defensible, genuinely-forked reading; NO migration confirmed table-by-table; NO API/FE change confirmed against `types.ts` | — |

Verdict: ratifiable after the folds above (all applied in this text before the OQ gate was briefed). The three convergent findings (V1, V3, V4) each changed ratified-shape text — the pass paid for itself again.
