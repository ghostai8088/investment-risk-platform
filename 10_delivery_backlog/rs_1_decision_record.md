# RS-1 Decision Record — residual shrinkage + EWMA (the OD-E/OD-G residual-estimator v2s; Wave-7 slice 3)

| | |
|---|---|
| **Status** | **RATIFIED 2026-07-17 (OQ-RS-1-1…7, user: "Approve all") — implementation underway.** Drafted 2026-07-17 on `main` (HEAD post-ES-HS-1 closeout `da61889`; head migration `0041_es_historical`). Scope ratified in-session: **BOTH estimators ship this slice** (EWMA + EB cross-sectional shrinkage), fully discharging the PA-4 OD-E/OD-G residual-estimator deferral with NO rider left; the fitting posture RESOLVED in-session (data-driven w / declared λ / method-as-identity — Part 4). Pre-ratification verifier pass: 6 findings ALL FOLDED + the focused EB-leg check (formula standard vs Efron-Morris; pin self-sufficient — `proxy_weight_service.py:486-488` persists n_obs/n_reg/residual_stdev on the ESTIMATION_SUMMARY row). Sized **M/L**. |
| **Grounding** | Census (a single thorough Explore pass, file:line-grounded) + the primary-source fetch DISCHARGED FIRST (the roadmap fetch-at-planning MUST). The raw-sample σ_e is produced at exactly one site — `proxy_weight_kernel.py:128-130` (`dof = n - m; s2 = ss_res/dof; residual_stdev = s2.sqrt()`), the UNBIASED sample residual variance with an `n − (k+1)` divisor, carried on `OlsEstimate.residual_stdev` (:141) and persisted to `ProxyWeightEstimateResult.residual_stdev` (`models.py:470`, `PreciseDecimal(20,12)`, on the `ESTIMATION_SUMMARY` singleton row). It is consumed ONLY by the PA-4 total-VaR/ES-total residual legs (`var_total_kernel.py:64/:86-115`), which √t-descale it and never re-estimate — so a new σ_e flows downstream with ZERO math change. The estimate family `risk.proxy_weight.regression` today declares identity by `min_observations` ALONE (`proxy_weight_service.py:20/:385`); an `estimator_convention` is NET-NEW to PA-3 (the ES-HS-1 `declared_es_hs_parameters` parse-gate + registrar-stamp is the verbatim template, `bootstrap.py:1052/:1112/:1212`). The variant-dispatch shape is the REGISTRY MAP (`_HS_FAMILIES`/`_VarFamily`, `var_hs_service.py:112-128`; docstring: "a try/except chain is the wrong shape"), never a chain. The pin key set is FROZEN + None-tolerant; the `_BINDING_PREDICATES` tuple (`snapshot/service.py:2666`, import-time varchar(50) assert :2683-2686) takes a new member without a schema change (`PROXY_WEIGHT_BINDING_PREDICATE = "v1:desmoothed-period-rows+factor-return-span"` :1978 is the sibling). Head migration `0041_es_historical`; **RS-1 needs NO migration** (both σ_e still land in `residual_stdev`; the EWMA λ + both conventions are declared assumption texts, the EB shrinkage intensity is COMPUTED + pin-reproduced with no declared/stored numeric; the shrinkage cohort pin is a new tuple member — the ES-1 no-migration precedent). Demo: `run_demo_multifamily_extension` estimates a 3-instrument sleeve one-instrument-at-a-time (`multifamily.py:440-470`) — the natural shrinkage cohort (the 2 equities) + the TRIGGERED-re-validation slot (`_file_records` :818). **NO new permission (risk.run/risk.view reuse), NO new ENT, NO new governed number** (RS-1 introduces new declared ESTIMATOR VERSIONS of an EXISTING family — the estimate is an INPUT to total VaR, not itself a governed output), `audit/service.py` FROZEN. |
| **Mandate** | Roadmap Part 2.10 slice 3 (`delivery_roadmap.md:165`), sized S/M in the ledger, RE-SIZED **M/L** by the ratified both-estimators scope. Discharges the PA-4 OD-PA-4-E/OD-PA-4-G v2s recorded at `pa_4_decision_record.md:59-65/:121-127/:253-259` and echoed on the live model rows (`bootstrap.py:1816-1818` VAR_TOTAL_LIMITATIONS; `:2092-2093` ES_TOTAL_LIMITATIONS): "Barra-style shrinkage / Axioma-style EWMA on the residual are recorded v2s." Remediates the raw-sample-σ_e rider on the flagship AWCs via a TRIGGERED re-validation — **the second lifecycle turn** (limitation recorded → remediation built → re-validation with real evidence; the MF-1 pattern re-run on the residual leg). |

## Part 1 — Decisions at a glance (OD-RS-1-A…G)

| OD | Decision |
|---|---|
| **A — EWMA residual variance (Axioma/RiskMetrics): a kernel-level weighting; declared λ REQUIRED; the biased/zero-mean convention disclosed; the OLS betas byte-preserved** | The specific-return (residual) variance becomes an exponentially-weighted mean of squared residuals: **σ²_e = Σ_{t=1..n} w_t·e²_t**, with weights **w_t = (1−λ)·λ^{n−t} / (1 − λ^n)** (normalized to Σw_t = 1; observation index in TIME order, most-recent = largest weight — the recorded orientation) — equivalently the RiskMetrics recursion σ²_t = λ·σ²_{t−1} + (1−λ)·e²_{t−1}. `σ_e = √(σ²_e)`. **The `s2` decoupling (the verifier's load-bearing catch): `s2 = ss_res/(n−m)` MUST SURVIVE.** The classical OLS residual variance `s2` at `proxy_weight_kernel.py:129` feeds NOT ONLY `residual_stdev` (:130) but the coefficient `std_errors` at :134-136 (`√(s2·[(X'X)⁻¹]_jj)`) — so a literal "replace the s2 block with the EWMA variance" would silently recompute the coefficient standard errors on a decayed variance, which is economically WRONG (OLS inference on the betas is defined by the OLS residual variance, never a decayed one) and would break the byte-preservation. The correct edit KEEPS `s2 = ss_res/(n−m)` for `std_errors` (and for the RAW-path `residual_stdev`) and introduces a SEPARATE `residual_var` that dispatches on `decay_lambda` and feeds ONLY `residual_stdev` (:130). This is the vendor-standard separation, not a compromise: Barra/Axioma take the factor loadings + their inferential std-errors from the (OLS) regression while modeling SPECIFIC RISK separately (EWMA/shrunk) — the two answer different questions (is this loading significant? vs how large is the forward specific risk?). Consequently: the design matrix / OLS `beta` / residual VECTOR (:107-126) AND `std_errors` (:134-136) AND `r_squared` (:131, on `ss_res/ss_tot`, never `s2`) are ALL byte-identical to v1 on BOTH paths; only `residual_stdev` diverges on the EWMA path. The edit spans :128-136 (not just :128-130 — the `residual_var`/`s2` split), the Axioma "EWMA stdev of daily specific returns" shape: the factor regression is unweighted; only the specific-risk variance is decayed. **Two declared modeling choices, both recorded, both departures from v1 that a NEW convention legitimizes (never silent drift):** (i) NO `n−k` DOF correction — EWMA normalizes by Σw_t = 1 (the RiskMetrics biased form; the effective sample size is 1/Σw_t², stated as a limitation, not a divisor); (ii) the residual MEAN is taken as zero (RiskMetrics convention; unweighted OLS residuals sum to zero by construction, but the EWMA-weighted mean does not — the zero-mean assumption is declared, the tiny weighted-mean term dropped by convention). **λ is DECLARED and REQUIRED — there is NO silent default**: the PA-3 residuals are per APPRAISAL PERIOD (the desmoothed marks), not per trading day, so RiskMetrics' λ=0.94-daily/0.97-monthly are NOT transferable constants — λ is a model-identity parameter the registrar stamps and the parse-gate enforces (a different λ = a different version). `estimator_convention = EWMA_RISKMETRICS`; the declared λ is a SECOND identity literal (`decay_lambda=…`, a `0<λ<1` well-formedness gate). **Why λ stays DECLARED (not RMSE-fitted — ratified 2026-07-17):** RiskMetrics fit λ ONCE across a large panel of long daily series and then FROZE it as a constant precisely because per-series fits are unstable; the PA-3 residual series is per appraisal PERIOD (≈12 marks), on which the forecast-RMSE-over-λ surface is nearly flat and its minimizer is noise — so fitting λ here would be LESS faithful to RiskMetrics (which prescribes a fixed λ) AND statistically worse than a declared λ. The data-driven leg is reserved for the shrinkage intensity (OD-B), where the fit IS the method's contribution. The whole computation stays inside the prec-50 localcontext with the existing single terminal quantize (the never-quantize-intermediates discipline). |
| **B — Cross-sectional shrinkage (Barra USE4 empirical-Bayes): a cohort transformation OVER raw σ_e's; a DATA-DRIVEN per-instrument intensity (method-as-identity, fit reproduced from the pin — the ratified choice); USE4-faithful on the intensity; NOT Ledoit-Wolf; NO migration** | The shrunk specific variance is the empirical-Bayes posterior mean pulling each instrument's raw sample variance toward the cross-sectional pool: **σ²_e,i(shrunk) = w_i·σ²_pool + (1−w_i)·s²_i**, where **σ²_pool = (1/N)·Σ_{j∈cohort} s²_j** (the equal-weighted cross-sectional mean; cap-weighting a declared v2) and the intensity **w_i is COMPUTED per instrument, NOT declared** (the ratified data-driven choice): **w_i = v_i / (v_i + τ²)** with **v_i = 2·s⁴_i /(n_i − k_i)** (the Gaussian sampling variance of a variance estimate — the residual df `n_i − k_i` = `n_observations − n_regressors` read from the pinned estimate, `models.py:468-469`) and **τ² = max(0, S²_cross − v̄)** (the empirical prior dispersion: `S²_cross` = the cross-sectional sample variance of the `s²_j`, `v̄` = the mean sampling variance) — the Morris/James-Stein empirical-Bayes shrinkage USE4's specific-risk model is a member of. **The data-driven properties (why this is now USE4-FAITHFUL on the intensity, not a simplification):** a noisier estimate (short series ⇒ large v_i) shrinks MORE; a widely-dispersed cohort (large τ²) shrinks LESS; a homogeneous cohort (τ²→0) shrinks fully to the pool — and w_i is HETEROGENEOUS across instruments (the USE4 "shrinkage is not equal for all assets" property; the prior fixed-w "disclosed simplification" is DISCHARGED). **Governance = method-as-identity (the ratified fit-governance choice):** the declared identity is the METHOD `estimator_convention = SHRINKAGE_CROSS_SECTIONAL_EB` — there is NO declared w; each w_i is computed, and the whole fit is REPRODUCIBLE FROM THE PINNED COHORT ALONE (the pin holds each member's `s²_i` + residual df); reproduction recomputes τ²/v_i/w_i/blend byte-identical. Validation attaches to the METHOD/version, NOT to any numeric w — the fitted intensity is NOT minted as a separate governed number (the ratified posture; no new governed-number surface inside RS-1). **Min-cohort FAIL-CLOSED (not degrade-to-declared):** τ² is only identifiable for a comparable cohort of size **N ≥ N_MIN = 3** (the James-Stein admissibility dimension — the variance-reduction guarantee holds for ≥3; below it the cross-sectional dispersion is not estimable); below N_MIN the run REFUSES (`ResidualShrinkageCohortError`, the HG-1 unmeasurable-shapes-fail-closed precedent) rather than smuggling an arbitrary w back in — a SINGLETON is the degenerate case of that refusal. **Architecture:** a governed TRANSFORMATION over a cohort of promoted raw estimate runs (the Geltner-desmoothing analogy), NOT an OLS change; the stored quantity is `residual_stdev` (a stdev, `models.py:470`) so the service SQUARES to variance before the fit and SQRTs the shrunk variance back. `run_residual_shrinkage` pins the cohort (new snapshot purpose `PURPOSE_RESIDUAL_SHRINKAGE_INPUT` + builder + `RESIDUAL_SHRINKAGE_BINDING_PREDICATE = "v1:cohort-residual-variances+dof"` (32 chars — now carries the per-member residual df the EB fit needs) on the `_BINDING_PREDICATES` tuple — a member, NOT a schema change), emits one shrunk-σ_e version per member. **Citation disposition:** the TARGET + CONCEPT + the DATA-DRIVEN INTENSITY are all Barra USE4 (MSCI 2011, verified Part 2) — USE4-faithful on the intensity; Ledoit-Wolf EXPLICITLY NOT cited (it leaves variances UNSHRUNK, `f_ii = s_ii`; the codebase reserves L-W for COVARIANCE shrinkage, `bootstrap.py:487`). **Three recorded limitations:** (i) the cohort MUST be a comparable-risk group — cross-asset-class pooling (a bond shrunk toward an equity pool) is a misuse the declaring analyst owns; (ii) the Gaussian `v_i = 2s⁴/(n−k)` assumes approximately-normal residuals — heavy tails understate v_i (under-shrink), disclosed; (iii) the equal-weighted pool is a simplification of USE4's cap-weighted target (a declared v2). Prec-50, deterministic Decimal throughout (the fit must be exact-reproducible — no float, no nondeterministic tie-break). |
| **C — The family + dispatch: both conventions as new declared versions of `risk.proxy_weight.regression`; the `estimator_convention` parse-gate net-new to PA-3; registry-map dispatch; the raw v1 grandfathered UNGATED** | Both estimators are new declared VERSIONS of the existing `risk.proxy_weight.regression` family (NOT new model codes — the estimate is an input, not a governed output; the version label carries the estimator). PA-3's identity gains an `estimator_convention` (+ the estimator-specific literal: `decay_lambda` for EWMA, `shrinkage_weight` for shrinkage) via a new `declared_proxy_weight_parameters` parse-gate that ADAPTS `declared_es_hs_parameters` (`bootstrap.py:1112`) — same well-formedness discipline (a present literal must parse strictly; malformed/ambiguous ⇒ the fail-closed `WrongModelVersionError`), but with **one DELIBERATE INVERSION the verifier flagged**: the es_hs template REQUIRES its `estimator_convention` (absent ⇒ raise, :1128), whereas RS-1 makes `estimator_convention` **OPTIONAL with a RAW default** (absent ⇒ the implicit v1) so the grandfather holds — the inverse of the template for THIS one literal, stated as a deviation, not a copy. Dispatch on the bound convention uses the REGISTRY-MAP shape (`_ResidualEstimator` map: `{RAW, EWMA_RISKMETRICS, SHRINKAGE_CROSS_SECTIONAL_EB}` → the compute path), the `_HS_FAMILIES`/`_VarFamily` precedent — NEVER a try/except chain. The EWMA carries a declared `decay_lambda`; the EB shrinkage carries NO numeric intensity (method-as-identity — its w_i are computed). **The raw-sample v1 is GRANDFATHERED UNGATED and BYTE-PRESERVED**: existing `risk.proxy_weight.regression` versions carry NO `estimator_convention` row, and the current `declared_min_observations` (`bootstrap.py:1676-1686`) parses only `min_observations=` — an absent convention raises NOWHERE today (verifier-CONFIRMED), so treating ABSENT ⇒ implicit `RAW` cannot break an existing parse (the immutable-referent / refuse-not-skip discipline — the BT-2 v1-grandfather precedent); the shipped PA-3 goldens re-run as regressions. New registrations MUST declare a convention. |
| **D — No migration; the scope-contained plumbing** | RS-1 adds NO migration (head stays `0041_es_historical`): both σ_e (EWMA and shrunk) still land in the existing `residual_stdev` column; the EWMA convention + λ are declared assumption texts (no column); the EB shrinkage convention is a declared method text with NO numeric intensity (the w_i are computed + pin-reproduced); the shrinkage cohort pin is a new `_BINDING_PREDICATES` tuple member (import-time varchar(50) assert satisfied — `"v1:cohort-residual-variances+dof"` is 32 chars). The pin serializer + its FROZEN key set are BYTE-UNTOUCHED (no new key). The ES-1 no-migration precedent governs. NEW registrar endpoints: `POST /risk/models/proxy-weight-ewma` and `POST /risk/models/proxy-weight-shrinkage` (the estimator convention + its literal are REGISTRAR-STAMPED, not caller-suppliable from the generic path); ONE new run endpoint `POST /risk/residual-shrinkage/runs` for the cohort transformation (the EWMA rides the EXISTING proxy-weight estimate run endpoint — same binder, dispatches on the bound version). Reads unchanged (the estimate GET is convention-generic). FE zero changes (grounded — the residual σ_e is not a rendered governed number; it surfaces only via the total-VaR decomposition it already feeds). |
| **E — The TRIGGERED re-validation: demo stage 5 remediates the raw-sample-σ_e rider on the living tenant; the second lifecycle turn** | A new demo stage 5 (`demo/rs1_stage5.py` + `scripts/run_demo_rs1.py`, the stage-4 shape) extends the LIVING multi-family tenant: registers the EWMA + shrinkage versions of `risk.proxy_weight.regression` for the demo tenant; re-estimates the equity sleeve's specific risk under EWMA (declared λ) and runs the empirical-Bayes cross-sectional shrinkage over the equity cohort — producing shrunk/EWMA σ_e that FEED a fresh total-VaR/ES-total run bound to the SAME downstream versions the flagship AWCs sit on (the version grain); **the cohort is sized for a GENUINE EB fit: stage 5 seeds the existing 2-equity sleeve UP to N ≥ N_MIN = 3 comparable equities (additively — one or two new listed-equity instruments + their per-instrument raw estimation chain) so the fitted per-instrument w_i are real, not a floor case; the existing corporate bond is EXCLUDED from the cohort (the comparable-risk-group rule — cross-class pooling is the recorded misuse), and its exclusion is asserted (the bond stays raw). Seeding fewer than 3 would force the OD-B fail-closed refusal — disclosed, and the reason the cohort is grown rather than run at the estimability floor;** then files the **TRIGGERED re-validations** closing the raw-sample-σ_e limitation clause on the affected model conditions, with FRESHLY-DRAFTED conditions (the MF-1 closure-by-supersession discipline — ZERO stale tokens, the conditions-grep flips), plus the INITIAL AWC dossiers for the two new estimator versions (genuinely-new versions ⇒ SOME record; the HG-1 OQ-5 false-ceremony bar does not apply). Additive, single-commit, refuse-not-skip both directions; requires the multifamily extension present. CI slots AFTER the stage-4 suite, BEFORE the downgrade smoke; the alpha-sort filename discipline observed (`test_demo_stage5_*` — 's5' sorts after 's4'). |
| **F — Constants + docs: the new dossier rows for both conventions; the pa_4 raw-sample-σ_e limitation DISCHARGED by a dated amendment; the new referent; the verified citations at honest grades** | New limitation rows drafted under the HG-1 fence (each finding key matching exactly ONE row; no duplicate key-substrings; zero 'FL-1' tokens) for the two conventions: EWMA — the effective-sample-size row (1/Σw_t² < n; a decayed estimate reacts faster but on FEWER effective observations), the declared-λ row (λ is not a transferable constant across observation frequencies), the biased/zero-mean row; SHRINKAGE (EB) — the comparable-cohort row (cross-class pooling is a misuse), the min-cohort-fail-closed row (N ≥ 3 or refuse — τ² unidentifiable below), the Gaussian-sampling-variance row (`v_i = 2s⁴/(n−k)` assumes near-normal residuals; heavy tails under-shrink), the equal-weighted-pool row (USE4's cap-weighted target is a declared v2). NB the intensity is now USE4-faithful (data-driven), so there is NO "fixed-w simplification" limitation — that clause is discharged, not carried. The `VAR_TOTAL_LIMITATIONS`/`ES_TOTAL_LIMITATIONS` "residual shrinkage + EWMA are v2s" rows (`bootstrap.py:1816-1818/:2092-2093`) are REWORDED to "REALIZED as declared `risk.proxy_weight.regression` estimator conventions (RS-1)" for NEW registrations (existing tenants keep old texts — immutable). **The verifier's doctrine hazard, recorded: `ES_TOTAL_LIMITATIONS` (:2092-2093) BUNDLES the still-open "calendar-aware per-period trading-day counts" v2 in the SAME string as shrinkage+EWMA — the reword MUST discharge ONLY the shrinkage/EWMA clause and PRESERVE the calendar-aware clause (an unshipped v2), splitting the row if needed to keep the HG-1 one-finding-key-one-row fence; `VAR_TOTAL_LIMITATIONS` (:1816-1818) is cleaner (shrinkage+EWMA is its own sentence).** The pa_4 raw-sample-σ_e limitation (`pa_4_decision_record.md:121-127/:253-259`) discharged by a DATED amendment (amend, never edit — the immutable-record discipline). NEW referent `05_analytics_methodologies/residual_estimation_v1.md` (the var_parametric_es_v1 template: the two conventions → the formulas → why-honestly → declared-identity → limitations; the rule-6 external-benchmark section carrying the Part-2 citations at their honest grades). |
| **G — The scope fence** | **NO migration** (head stays `0041`); **NO new permission** (risk.run/risk.view reuse); **NO new ENT / NO new governed number** (new declared VERSIONS of an existing family); RISK.VAR_CREATE untouched; **`audit/service.py` FROZEN**; the pin serializer + its frozen key set BYTE-UNTOUCHED (no new key — the shrinkage pin is a new PURPOSE/predicate, not a new var_result key); `METRIC_TYPES` untouched; the OLS regression output (betas/std_errors/r_squared/residual vector) BYTE-PRESERVED (the shipped PA-3 goldens re-run as regressions — EWMA collapses the same residual vector differently, the raw path unchanged); the total-VaR/ES-total residual legs consume σ_e UNCHANGED (no downstream math edit — `var_total_kernel.py:64-83` only √t-descales and `:86-115` only squares-sums-sqrts; the pre-existing binder magnitude/negativity envelope `var_service.py:422` (refuses a negative or over-large `residual_stdev` against `_MAX_RESIDUAL_STDEV_ABS`) STRENGTHENS the claim — both estimators satisfy it by construction: an EWMA of squares and a w∈[0,1] blend of non-negative variances both stay ≥ 0); the desmoothing/covariance/VaR/HS/ES/loadings/backtest families byte-untouched; FE zero changes; the campaign + multifamily + stage-3/4 demo modules byte-untouched (stage 5 is ADDITIVE). API: 2 new registrar endpoints + 1 new run endpoint + the existing estimate/read endpoints. |

## Part 2 — External benchmarks and primary sources (roadmap Part 4 rule 6)

**RiskMetrics EWMA — VERIFIED 2026-07-17.** J.P. Morgan/Reuters, *RiskMetrics — Technical Document*, 4th ed. (December 1996), §5 (exponential moving average / decay-factor selection). The EWMA variance recursion **σ²_t = λ·σ²_{t−1} + (1−λ)·r²_{t−1}** with the mean assumed zero; the decay factor **λ = 0.94 (daily) / 0.97 (monthly)** chosen by minimizing the RMSE of the variance forecast at the target horizon. Primary-document identity confirmed from the PDF metadata ("RiskMetrics Technical Document — Fourth Edition 1996, December"); the recursion + λ values cross-confirmed across multiple independent secondary sources. **Grade:** formula + λ values + selection method VERIFIED; the exact §/equation/table numbers to be pinned against the primary text at implementation (the LZW-compressed PDF did not text-extract for a character-exact §-number quote — disclosed, not laundered). **Axioma** (the pa_4-cited vendor benchmark, `pa_4_decision_record.md:59-65`) states specific risk as "the EWMA stdev of daily specific returns" — the Axioma shape RS-1's EWMA mirrors (equal-weighted v1 = the un-decayed limit λ→1⁻).

**Barra USE4 shrinkage — VERIFIED 2026-07-17.** MSCI, *The Barra US Equity Model (USE4) Methodology Notes* (August 2011). Bayesian shrinkage of specific risk **toward the (cap-weighted) cross-sectional mean specific volatility**, with a data-driven (empirical-Bayes) shrinkage intensity; after shrinkage the specific-risk bias statistics flatten. **Grade:** target + concept + direction VERIFIED; RS-1 ships the EQUAL-weighted target and a DECLARED (fixed) intensity — both disclosed as transparent simplifications of USE4's cap-weighted target + data-driven intensity, each a named declared v2. *[DATED CORRECTION 2026-07-18 (the doctrine finder's catch — this sentence predates the in-session fitting-posture ratification and was left stale):* the ratified and SHIPPED posture is the **DATA-DRIVEN** empirical-Bayes intensity (Part 4's resolved sub-decision; OD-B) — USE4-faithful on the intensity; only the EQUAL-WEIGHTED pool remains a disclosed simplification (a named v2), and the Gaussian sampling-variance approximation is a disclosed LIMITATION, not a v2.]* This is the pa_4-recorded "MSCI Barra … per-security specific risk … Bayesian shrinkage (size-decile means)" benchmark (`pa_4_decision_record.md:59-65`).

**Ledoit-Wolf 2004 — VERIFIED and EXPLICITLY NOT USED for RS-1.** Ledoit, O., & Wolf, M., "Honey, I Shrunk the Sample Covariance Matrix," *Journal of Portfolio Management* 30(4):110–119, 2004. The δF + (1−δ)S constant-correlation estimator shrinks CORRELATIONS while leaving the DIAGONAL VARIANCES UNSHRUNK (`f_ii = s_ii`, δ* = κ/T, κ = (π̂−ρ̂)/γ̂). **Therefore L-W is the WRONG primary for a residual-VARIANCE shrinkage** — a citation trap the RS-1 mandate flagged ("citations UNVERIFIED") and the census confirmed the codebase already avoids (`bootstrap.py:487` reserves L-W for COVARIANCE shrinkage, a distinct wave-unassigned deferral). Recorded here so the referent NEVER cites L-W for the specific-variance leg.

**Geltner/GLM/Okunev-White** — not touched by this slice (DS-2's obligation, unchanged). **Sharpe 1963** (the diagonal-residual single-index frame the total-VaR leg rests on) — VERIFIED-carried from PA-4.

## Part 3 — Census key facts (the load-bearing subset)

1. The raw-sample σ_e is ONE site: `proxy_weight_kernel.py:128-130` (`dof = n − m; s2 = ss_res/dof; residual_stdev = s2.sqrt()`), unbiased, `n − (k+1)` divisor; carried on `OlsEstimate.residual_stdev` (:141). The betas/residual vector are computed at :107-126, UNTOUCHED by RS-1.
2. Persistence: `ProxyWeightEstimateResult.residual_stdev` `PreciseDecimal(20,12)` on the `ESTIMATION_SUMMARY` row (`models.py:470`); metric_type vocab `WEIGHT/INTERCEPT/ESTIMATION_SUMMARY` (:404-409); RUN/SNAPSHOT/MODEL-bound (:441-449).
3. Consumption: `var_total_kernel.py:64` (`daily_residual_stdev`, √t descale) + `:86-115` (`total_var_residual`, Σ(MV·σ_e,daily)²) — no re-estimation; a new σ_e flows through with zero math change.
4. PA-3 identity is `min_observations` ALONE today (`proxy_weight_service.py:20/:385`, floor `max(min_observations, k+2)` :302) — `estimator_convention` is NET-NEW to this family.
5. The declared-identity template: `ESTIMATOR_ASSUMPTION_PREFIX = "estimator_convention="` (`bootstrap.py:1052`); `declared_es_hs_parameters` parse-gate (:1112, exactly-one-well-formed-each ⇒ `WrongModelVersionError`); registrar stamp (:1212). RS-1's `declared_proxy_weight_parameters` ADAPTS it with ONE inversion — `estimator_convention` is optional-with-RAW-default here (the grandfather), vs REQUIRED at :1128 (the es_hs leg); the current PA-3 parse `declared_min_observations` (`bootstrap.py:1676-1686`) reads only `min_observations=`, so an absent convention raises nowhere today (verifier-CONFIRMED). Not a verbatim mirror.
6. Registry-map dispatch shape: `_HS_FAMILIES`/`_resolve_hs_family` (`var_hs_service.py:112-128`, "a try/except chain is the wrong shape"); `_VarFamily`/`_resolve_var_family` (`var_service.py`). RS-1's `_ResidualEstimator` map mirrors it.
7. The pin binder registry: `_BINDING_PREDICATES` tuple (`snapshot/service.py:2666`, varchar(50) import-time assert :2683-2686); `PROXY_WEIGHT_BINDING_PREDICATE` (:1978). A new member is NOT a schema change.
8. Head migration `0041_es_historical` (`alembic heads`); the `0038_var_residual_variance` additive-column precedent exists IF one were ever needed — RS-1 needs none.
9. Demo cohort + re-validation slot: `run_demo_multifamily_extension` (`multifamily.py:747`); per-instrument `_estimate_and_promote` (:440, `run_proxy_weight_estimate` :470, `promote` :497); the 2-equity + 1-bond sleeve; `_file_records` (:818) mints TRIGGERED + INITIAL validations.
10. The OD-E/OD-G charter: `pa_4_decision_record.md:59-65` (Barra Bayesian shrinkage + Axioma EWMA benchmarks), `:105` (OD-PA-4-E scope), `:121-127` (the raw-sample-σ_e v1 limitation), `:253-259` (the still-open register — "residual shrinkage/EWMA … the ratified OD-E/OD-G v2s, unchanged"). In-code echoes: `bootstrap.py:1816-1818`, `:2092-2093`, `:487` (L-W reserved for covariance — distinct).
11. The demo pin-count discipline: campaign suite pins 16 set-equal, multifamily pins 17 (stage-4 added the 18th) — stage 5 is the next pin-safe slot AFTER stage 4, BEFORE the downgrade smoke; alpha-sort filename `test_demo_stage5_*`.
12. House planning-doc structure: OD-⟨slice⟩-A…G decision table + OQ-⟨slice⟩-N ratified open questions + Part 2 benchmarks + Part 3 census + Part 5 readiness gate + Part 5.5 deviations + Part 6 review dispositions (parts not strictly ordered in-file).

## Part 4 — Open decisions (OQ-RS-1-1…7) — RATIFIED 2026-07-17 (user: "Approve all")

- **OQ-RS-1-1 — OD-A: the EWMA estimator** (kernel-level σ²_e = Σ w_t·e²_t, w_t = (1−λ)λ^{n−t}/(1−λ^n), most-recent-largest; the OLS betas/residual vector byte-preserved; NO DOF correction + zero-mean, both declared; **λ DECLARED and REQUIRED — no silent default** given the appraisal-period observation frequency; `estimator_convention=EWMA_RISKMETRICS` + `decay_lambda`). *Recommend APPROVE.*
- **OQ-RS-1-2 — OD-B: the EB shrinkage estimator** (a COHORT TRANSFORMATION over raw σ_e's — not an OLS change; σ²_e,i(shrunk) = w_i·σ²_pool + (1−w_i)·s²_i with the **DATA-DRIVEN per-instrument w_i = v_i/(v_i+τ²)**, v_i = 2s⁴/(n−k), τ² = max(0, S²_cross − v̄) — the empirical-Bayes intensity; **method-as-identity `estimator_convention=SHRINKAGE_CROSS_SECTIONAL_EB`, NO declared w, fit reproduced from the pin**; the pin carries per-member s²+df, `RESIDUAL_SHRINKAGE_BINDING_PREDICATE="v1:cohort-residual-variances+dof"`; **N≥3 min-cohort FAIL-CLOSED**; **Barra USE4 — USE4-faithful on the intensity — NOT Ledoit-Wolf**; comparable-cohort + Gaussian-sampling-variance + equal-weighted-pool(v2) limitations recorded; the fitted intensity NOT a separate governed number). *Recommend APPROVE — the ratified data-driven-w/declared-λ split.*
- **OQ-RS-1-3 — OD-C: the family + dispatch** (both conventions as new declared VERSIONS of `risk.proxy_weight.regression`; `declared_proxy_weight_parameters` parse-gate net-new to PA-3, mirroring ES-HS-1; `_ResidualEstimator` registry-map dispatch; the raw v1 GRANDFATHERED UNGATED via absent-convention ⇒ implicit RAW, byte-preserved). *Recommend APPROVE.*
- **OQ-RS-1-4 — OD-D: no migration + the plumbing** (head stays `0041`; both σ_e in the existing column; the new cohort pin is a tuple member; the pin serializer/frozen key set byte-untouched; 2 registrar + 1 run endpoint; reads/FE unchanged). *Recommend APPROVE.*
- **OQ-RS-1-5 — OD-E: the TRIGGERED re-validation** (demo stage 5 remediates the raw-sample-σ_e rider on the living tenant — the second lifecycle turn; freshly-drafted closing conditions, conditions-grep flips; INITIAL dossiers for the two new versions; the pin-safe CI slot; alpha-sort filename). *Recommend APPROVE.*
- **OQ-RS-1-6 — OD-F: constants + docs** (the new fence-compliant dossier rows for both conventions; the VAR_TOTAL/ES_TOTAL "v2s" rows reworded to REALIZED for new registrations; the dated pa_4 raw-sample-σ_e discharge; the new `residual_estimation_v1.md` referent carrying the Part-2 citations at honest grades — RiskMetrics §-number to be pinned at impl, USE4 simplifications disclosed, L-W explicitly not used). *Recommend APPROVE.*
- **OQ-RS-1-7 — OD-G: the scope fence** (no migration; no permission/ENT/audit/governed-number mints; OLS output + downstream residual legs + pin serializer + FE all byte-untouched/preserved; the 4-finder review composition below). *Recommend APPROVE.*

**Standing sub-decision — RESOLVED 2026-07-17 (the fitting posture):** after an objective pros/cons on declared-vs-data-driven, the user ratified the SPLIT: **the shrinkage intensity is DATA-DRIVEN (empirical-Bayes, method-as-identity, fit reproduced from the pinned cohort — OD-B), and the EWMA λ stays DECLARED (OD-A).** The reasoning is the EWMA-vs-shrinkage asymmetry: the intensity IS the shrinkage method's contribution (a fixed w is an arbitrary dial), so data-driven is the faithful and defensible choice AND governable via method-as-identity + a pinned fit; whereas RiskMetrics prescribes a FIXED λ (it fit λ once across a huge panel then froze it) and fitting λ on a ≈12-mark per-period residual series is unstable and less faithful — so λ stays declared. The fitted intensity is NOT minted as a separate governed number (validation attaches to the METHOD/version; the pin carries reproducibility). Consequences carried: OD-B's `SHRINKAGE_CROSS_SECTIONAL_EB` computed-w_i design + the N≥3 min-cohort fail-closed + the Gaussian-sampling-variance limitation + the demo cohort grown to ≥3 comparable equities (OD-E). The RMSE-fitted-λ and cap-weighted-pool forms remain named declared v2s.

## Part 5 — Implementation readiness gate

### Planning verification (the standing pre-ratification pass) — RAN 2026-07-17, 6 findings ALL FOLDED
An adversarial verifier pass over this record RAN before ratification (the ES-1/ES-HS-1 discipline — it killed HIGH claims of my own at ES-1). Outcome: **2 REFUTED + 4 caveats, every one folded into this record and the plan above**:
1. **REFUTED (load-bearing) — the `s2` decoupling.** "EWMA modifies only :128-130 / std_errors byte-preserved" was self-contradictory: `std_errors` (:134-136) consumes `s2`, so replacing `s2` with the EWMA variance would silently corrupt OLS coefficient inference. FOLDED into OD-A + impl step 1 (keep `s2` for `std_errors`; a separate `residual_var` dispatches on λ and feeds ONLY `residual_stdev`; the vendor-standard separation).
2. **REFUTED (factual) — the predicate is 32 chars, not 34** (still ≤ the varchar(50) assert; the NO-migration conclusion stands). FOLDED at all sites.
3. **Caveat — grandfather is the INVERSE of the es_hs template** (which REQUIRES the convention; RS-1 makes it optional-with-RAW-default). FOLDED into OD-C + Part 3 item 5 as a stated deviation, not a mirror.
4. **Caveat — impl-step-1 line-range imprecision** (r_squared :131 / std_errors :134-136 sit OUTSIDE :107-126). FOLDED into the rewritten step 1.
5. **Caveat — `ES_TOTAL_LIMITATIONS` bundles the still-open calendar-aware v2 in the same string** as shrinkage+EWMA; the reword must not silently discharge it. FOLDED into OD-F.
6. **Caveat — the unit square/un-square** (stored `residual_stdev` is a stdev; the blend is over variances). FOLDED into OD-B + impl step 2.
CONFIRMED by the pass and worth noting: the `residual_stdev` column absorbs both estimators without a migration; the pin serializer frozen key set is byte-untouched; the downstream legs re-estimate nothing AND the pre-existing `var_service.py:422` magnitude envelope holds both estimators ≥ 0 by construction; the raw-v1 grandfather raises nowhere today; the registry-map + demo-cohort precedents are real.

### The 4-finder review composition (OD-G, ratified shape)
- **Adversarial** — on the registry-map dispatch (can a malformed/absent convention reach a wrong path, or defeat the raw-v1 grandfather?) + the shrinkage cohort pin (can a member be swapped post-pin? can the EB fit be made non-reproducible — any float/nondeterminism? is w_i recomputable from the pin alone? can the N≥3 floor be evaded?).
- **Numeric (Opus/High — the EB fit is the one genuinely new math)** — hand + exact-rational re-derivation: the EWMA weights sum to 1 and the λ→1⁻ limit recovers the equal-weighted (un-DOF-corrected) mean; the EB w_i = v_i/(v_i+τ²) is heterogeneous by df, τ²=max(0,S²_cross−v̄) floors at 0 (all-shrink), v_i=0 gives no-shrink, and the N<3 refusal fires; a fuzz over λ∈(0,1), cohort size ≥3, and df spread.
- **Doctrine** — the new dossier texts vs the HG-1 fence + the citation dispositions (L-W absent; USE4-faithful on the EB intensity; the cap-weighted-pool/RMSE-λ v2s + the Gaussian-sampling-variance assumption disclosed; the pa_4 discharge dated).
- **Scope-fence** — OD-G clause-by-clause (no migration; no mints; OLS/downstream/serializer/FE byte-checks; the demo additivity).

## Part 5.5 — Implementation deviations from the ratified plan

1. **The age-gate purpose widening (the pair)** — OD-G said the total-VaR residual leg consumes σ_e
   UNCHANGED; implementing OD-E surfaced that the gated `risk.var.parametric_total` v2
   (`max_estimate_age_days=400` on the demo flagship) REFUSES any citation whose snapshot purpose
   is not `PROXY_WEIGHT_INPUT` — so a shrinkage-cited estimate could never feed the GATED flagship,
   making the remediation unusable on the exact family whose rider it closes. DELIBERATE deviation:
   `var_service._estimate_age_days` gained `_MEASURABLE_ESTIMATE_PURPOSES = {PROXY_WEIGHT_INPUT,
   RESIDUAL_SHRINKAGE_INPUT}` (a `!=` → `not in` swap + the constant + the message reword — nothing
   else), and the HG-1 promote gate got the SAME two-purpose admission for consistency. SOUND
   because the shrinkage builder stamps `as_of_valuation_date` = the STALEST cohort member's
   regression span end (the conservative age by construction — adversarial-finder verified: a fresh
   cohort cannot hide a stale member, and the target must itself be a member). Scope-fence finder
   verified the edit is exactly this narrow.
2. **The per-instrument shrinkage grain** — ratified in-session (not a silent deviation): each
   shrinkage run targets ONE instrument, pins the whole cohort, emits ONE shrunk
   `ESTIMATION_SUMMARY`; promotion + the total-VaR builder stay byte-unchanged.
3. **Error-class naming** — OD-B's prose named `ResidualShrinkageCohortError`; the shipped classes
   are `ResidualShrinkageInputError` (422 — carries the cohort<3 kernel-structural refusal) and
   `ResidualShrinkageSnapshotError` (409). Behavior identical; the name folded into the house
   pre-create/snapshot error family instead.
4. **The referent is shared** — both new versions cite ONE `residual_estimation_v1.md`
   (`PROXY_WEIGHT_RESIDUAL_METHODOLOGY_REF`) rather than per-convention referents; plus routine
   additive re-exports in `risk/__init__.py` + `snapshot/__init__.py` (scope-fence-verified
   additive-only).

## Part 6 — Implementation review dispositions (the ratified 4-finder composition; ran 2026-07-18)

**Composition:** adversarial + numeric (exact-rational, executed) + doctrine + scope-fence, all four
over the full `rs-1-planning..HEAD` diff. **Totals: 2 HIGH, 5 MEDIUM, 7 LOW, 1 property caveat —
ALL folded (zero deferred); ZERO numeric defects; ZERO scope-fence violations.**

| # | Finder | Sev | Finding | Disposition |
|---|---|---|---|---|
| A1 | adversarial | HIGH | An AMBIGUOUS (>1) `estimator_convention` collapsed into the RAW grandfather via `sole_declared`'s absent/ambiguous conflation — a generically-minted double-declaration ran RAW while its dossier displayed EWMA (the exact OD-C "ambiguous ⇒ fail-closed" contract violation) | FOLDED: `declared_proxy_weight_parameters` now counts convention rows — 0 ⇒ RAW grandfather, >1 ⇒ `WrongModelVersionError`; + tests |
| A2 | adversarial | MEDIUM | Cohort distinctness enforced at the RUN grain, not INSTRUMENT — N≥3 satisfiable with <3 distinct instruments (re-estimation is the steady state; the pool double-counts); shrunk rows themselves admissible as members (shrink-of-shrunk) | FOLDED: `_parse_cohort` refuses duplicate-instrument cohorts; the builder refuses members that are themselves shrinkage outputs; + tests |
| A3 | adversarial | MEDIUM | The gate-widening comment cited "a recorded Part-5.5 deviation" before Part 5.5 existed — a false provenance claim under the immutable-record discipline | FOLDED: Part 5.5 item 1 above records it |
| A4 | adversarial | LOW | Recognized-but-inapplicable stray literals silently ignored (a `decay_lambda` on an implicit-RAW/EB version displays an identity the compute never uses) | FOLDED: the gate refuses stray `decay_lambda`/`min_observations` per branch; + tests |
| A5 | adversarial | LOW | The cohort builder's as-of fallback failed OPEN (unresolvable member as-of silently skipped; empty ⇒ today) — unreachable today (NOT-NULL columns) but inverted vs the HG-1 fail-closed doctrine on the exact anchor the widened gates read | FOLDED: the builder refuses a member without a resolvable span end |
| N1 | numeric | — | ZERO defects: 400+ exact-rational fuzz cases (worst err 5E-29), λ→1⁻ ⇒ /n biased mean proven symbolically, RAW byte-identity, τ² floor exact-iff, convex-blend bounds, prec-50 shielding, quantize half-ulp bound tight | — (executed evidence in the review record) |
| N2 | numeric | caveat | EB shrinkage is NOT order-preserving (w grows with s⁴ — same-side pairs can swap ranks; inherent to heterogeneous-intensity EB, USE4 shares it) | FOLDED: disclosed in the referent + a new EB limitation row |
| D1 | doctrine | HIGH | The MF-1 frequency-conversion disclosure (appraisal_days=91 on daily-period estimates understates the daily residual) vanished from the operative record — closure-by-supersession flipped it to historical with NO remediation | FOLDED: the caveat restated in BOTH fresh TRIGGERED condition blobs (carried forward, unremediated; calendar-aware counts named as the open lever) |
| D2 | doctrine | MEDIUM | "James-Stein dimension" over-claim for the N≥3 floor (τ² is MoM-defined from N=2; Stein's p≥3 is a Gaussian-mean admissibility theorem — an analogy, not a transferred guarantee) | FOLDED: softened at every site (kernel docstring/constant/error, limitation row, referent) to the honest df-poverty/prudence ground with Stein as analogy |
| D3 | doctrine | MEDIUM | "a total-VaR run … binds one of those versions" misstated the binding grain (the estimator version is bound by the CITED run, never the total run) | FOLDED: reworded in the VAR_TOTAL row + the referent |
| D4 | doctrine | MEDIUM | The two TRIGGERED filings carried no non-independence disclosure (the INITIALs did; MF-1 appended one to every record) | FOLDED: the disclosure appended to both TRIGGERED scope notes |
| D5 | doctrine | LOW | The referent minted a phantom "recorded v2" for the Gaussian approximation (a disclosed limitation, not a v2) | FOLDED |
| D6 | doctrine | LOW | Part 2's Barra sentence pre-dated the fitting-posture ratification (claimed a declared fixed intensity) + a df-notation shift unstated in the referent | FOLDED: dated correction in Part 2; the convention-shift note added at the formula site |
| D7 | doctrine | LOW | "applied AT its native frequency" glossed calendar-daily as RiskMetrics trading-day | FOLDED: exact wording (calendar-daily spacing vs trading-day fit, disclosed) at both sites |
| D8 | doctrine | LOW | The closure finding under-enumerated the raw members (MF-EQ-C is also raw) | FOLDED |
| D9 | doctrine | LOW | The fresh VaR-total condition compressed the BT-2 read rule to its name (the cross-referential-rider thinning MF-1's verifier killed) | FOLDED: the substance restored (suppressed/clustered + the EXCEPTION_INDICATOR surface) |
| S1 | scope-fence | — | All 11 OD-G clauses PASS; zero violations; `audit/service.py`/serializer/METRIC_TYPES/`var_total_kernel`/FE byte-untouched; the 3 deleted bootstrap lines are the OD-F-ratified rewords with the calendar clause preserved | — (the deviation list transcribed into Part 5.5) |

**Post-fold verification:** the full battery re-ran green after the folds (`make check` + the
five demo PG suites in CI order on a fresh schema + `alembic check` + the downgrade smoke — the
fold-pass numbers recorded in the closeout).
