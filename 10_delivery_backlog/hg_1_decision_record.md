# HG-1 Decision Record — the Wave-7 hygiene opener (slice 1)

| | |
|---|---|
| **Status** | **DRAFT — OQ-HG-1-1…6 pending user ratification.** |
| **Grounding** | Drafted 2026-07-17 on `main` = `3842b7d` (Wave-6 close ratified via PR #52/#53). Census: a 3-domain workflow fan-out, every claim file:line or executed. **Domain (a), the staleness gate:** `promote_proxy_weight_estimate` (`proxy_weight_service.py:539-590`) reads NOTHING off the cited run beyond existence/tenant/type/status — no age arithmetic of any kind exists on the promotion path; the regression SPAN END lives on the snapshot HEADER (`as_of_valuation_date = max period_end`, `snapshot/service.py:2067/:2117`), NOT the ESTIMATION_SUMMARY row (no span dates — the recorded reason `appraisal_days` is declared), and the promote path can reach it in ONE indexed read (`run.input_snapshot_id` → `resolve_snapshot`, the AD-014 true-append-only header — the BT-2 blessing); `input_snapshot_id` CAN be NULL (column nullable; `test_var_total.py:352-372` deliberately promotes a snapshotless run) and the header's PURPOSE must be checked (the BT-2 two-finder convergence). **The structural difference from BT-2: promotion has NO economic as-of** — BT-2's age subtracts two PINNED dates (covariance `window_end` − span end); a promote-time gate's "new" side can only be the promote-day (`utcnow`) or `valid_from` (caller-suppliable in the service, NOT exposed on the HTTP promote body) — a wall-clock anchor, so ANY mandatory default bound rots every fixed-date fixture (`test_proxy_weight.py` span end 2025-12-31, age ~198d and GROWING; the demo campaign's PE promote age ~108d at seed and growing — a 400d default breaks the re-seed ~2027-05, exactly the date-driven-failure mode the close's critic flagged). The bound-home shapes, precedents on file: declared-on-model = a v2 LABEL BUMP with grandfather machinery (the BT-2 `VAR_TOTAL` precedent — heavy for hygiene); caller promote-argument = policy-not-identity (precedent: the `weight` itself is the analyst's choice, and promotion is a CAPTURED write binding no model — OD-PA-3-D's identity bar does not transfer); fixed house constant (no declaration surface). **Expire-a-mapping facts (the Tier-3 question):** the write vocabulary is exactly {capture, supersede, correct} — no close-without-successor, nothing time-based anywhere; "no head" is semantically DIVERGENT per family (the proxy family silently degrades an instrument to the indicator path; the loadings family REFUSES the unloaded atom at the coverage gate), and a status/expiry column is a migration + a pin-serializer landmine of the recorded 0038 class. Every downstream family binds promoted weights ungated INDIRECTLY (the exposure snapshot pins current heads with zero age filter; the ONLY age gate in the codebase is the total/ES-total run binder). **Domain (b), the constants:** the sweep found **SEVEN CHANGE rows** (the verifier pass corrected the census's own "6" miscount) — the two known (`VAR_LIMITATIONS[0]` :666-668, `VAR_HS_LIMITATIONS[0]` :850-851) **plus a flat-FALSE new find: `PROXY_WEIGHT_LIMITATIONS[2]` (:1458-1459) still claims "a non-CURRENCY candidate fails the run closed"** — false since FL-1 (`proxy_weight_service.py:269-273` admits `LOADING_FACTOR_FAMILIES`; MF-1's Sharpe-1992 regression is exactly the refuted claim) — plus `VAR_ASSUMPTIONS_BASE[1]` :651, `ES_ASSUMPTIONS_BASE[2]` :1824, `ES_LIMITATIONS[0]` :1835-1838 (spliced into ES_TOTAL at :1878 — one edit fixes both), `VAR_TOTAL_LIMITATIONS[2]` :1613-1614 (attribution only); 2 parenthetical-only (`FACTOR_EXPOSURE_LIMITATIONS[1]`, `FACTOR_EXPOSURE_PROXY_LIMITATIONS[1]`), 1 phrasing on a deliberately-true gate (`SCENARIO_ASSUMPTIONS[2]` — the gate claim stays), 1 optional (`VAR_TOTAL_ASSUMPTIONS_BASE[1]`); the KEEP-list is enumerated with its enforcing gates (active-risk/allocation/proxy texts are TRUE of their whitelisted inputs). **The finding-key constraint is satisfiable everywhere**: all four key-carrying edits (`"SPECIFIC/IDIOSYNCRATIC RISK = 0"` ×2, `"it inherits"`, `"ZERO idiosyncratic risk"`) preserve their key verbatim — the stale text is the FRAMING clause, never the key; no edit may duplicate another row's key in the same set; the two re-seeding PG suites enforce all 33 key lookups fail-loud in CI; the EXISTING live tenant keeps old texts forever (immutable rows, refuse-not-skip). Coupled hazards: `MF1_CLOSURE_FINDING`'s frozen-wording note ("read 'CURRENCY-family' as…") goes stale on FRESH seeds after the rewording (OQ-ratified dossier content — amended at this gate, not silently); the 'FL-1' token stays out of every new text. Zero tests pin any stale substring; `test_var.py:1354` pins `"SPECIFIC/IDIOSYNCRATIC RISK = 0"` in the METHODOLOGY DOC — the phrase must survive any doc sweep; the MG-1 15-occurrence sweep is the shape precedent (uniform edit + pinned count + adjudicated keep-list). Companion doc debris enumerated (7 methodology-doc lines + 1 stale code comment `proxy_weight_service.py:246`). **Domain (c), the demo:** the campaign's genuinely-private chain (PE fund, α=0.4) has never touched a non-CURRENCY factor — the MF-1 sleeve used PUBLIC instruments via α=1; the mixed-family fence REQUIRES a NEW private instrument (the fence test iterates the fixed legacy tuple) which must gain ONLY non-CURRENCY rows; 7 quarterly marks clear both floors exactly (5 desmoothed periods = the k=3 floor; an 8th buys margin); the α=0.4 `demo-mg1` VERSION is REUSED (α is version identity — no new registration, the exact contrast with MF-1's α=1 mint); the OLS coverage rule needs **one SIMPLE return per period-end per MF factor (5×3 = 15 rows) — the campaign's exact `_OLS_RETURN_DATES` precedent**, safe for every existing window (covariance selects the N most recent COMMON dates — sparse historical rows never enter a window ending 2026-06-19); joining the SLEEVE portfolio tells the true story and breaks no pinned test (the MF-1 suite filters on `code_version == "demo-mf1"`; stage 3 stamps `demo-hg1`); the new instrument needs a carry mark ON 2026-06-19; the total/ES-total estimate-age gate passes at 80d vs the declared 400; the stage-3 footprint probe = the NEW instrument's code (no model/factor registration to probe on), seeded FIRST; CI slot: after the multifamily suite, before the downgrade smoke. **NO migration / NO permission / NO model code anywhere in the slice (verified table-by-table); the ONE API change is an additive optional field on the promote body.** Every bound version already carries a live latest outcome — no use-before-validation hole opens, no record is mechanically required, and an EXCEPTION "for nothing new" would be the false-ceremony shape MG-1 forbids. |
| **Mandate** | Roadmap Part 2.10 slice 1, sized **S**: the re-opened estimate-staleness residual's promotion-path age check (the expire-a-mapping clause = a Tier-3 question SURFACED here); the FL-1 registrar-constant debris under the finding-key compatibility constraint; the private-on-multi-family demo ride-along (a NEW private instrument; the mixed-family fence holds). The staleness item carries the register discipline's teeth — it went unpaid through its ASSIGNED wave (OQ-W6C-2). |

## Part 1 — Decisions at a glance (OD-HG-1-A…D)

| OD | Decision |
|---|---|
| **A — The promotion-path age check: ALWAYS measure + audit; an OPT-IN caller bound refuses; unmeasurable-with-bound fails CLOSED** | Three legs. **(1) The measurement, unconditional:** every promote resolves the cited run's `input_snapshot_id` → the PROXY_WEIGHT header (purpose-checked — the BT-2 lesson) and computes **`promotion_age_days`** = promote-day − span_end (the header's `as_of_valuation_date`) — the key is deliberately NOT named `estimate_age_days`: BT-2's persisted column of that name is a DIFFERENT-anchored number (covariance `window_end`, not promote-day) and an evidence reader must never see two same-named ages disagree by construction (the verifier pass's catch). The age (or `None` on any of the THREE unmeasurable shapes: NULL `input_snapshot_id`; a wrong-purpose header; a DANGLING snapshot id — `input_snapshot_id` is a bare non-FK column and `resolve_snapshot` raises `SnapshotNotFound`, the third shape the BT-2 precedent handles by name and the first draft missed) is merged into the promotion's NEW-version CREATE audit event's `after_value` ONLY when the promote path supplies it (key omitted on MANUAL capture/supersede writes — the payload is row-derived today, so both marketdata functions gain an optional defaulted parameter; no existing test pins those payload dicts). **(2) The policy, opt-in:** a new optional `max_promotion_age_days: int \| None = None` argument on `promote_proxy_weight_estimate` (service-side `< 1` refused — the BT-2 declaration-seam symmetry) + the HTTP promote body (additive field); absent ⇒ ungated (the status quo, so ZERO existing tests or demo seeds change behavior and no fixed-date fixture rots — the wall-clock hazard the census quantified); present ⇒ strict `>` refuses pre-write, and an UNMEASURABLE age (any of the three shapes) with a bound present fails CLOSED (the BT-2 gated⇒closed precedent — a tight policy cannot be defeated by citing a snapshotless run, the hole `test_var_total.py:352-372` mints and `:875-882` exercises). **The wire mechanism is a DISTINCT exception class** — `ProxyWeightStaleEstimateError`, with its own EXACT-TYPE entry in `_PROXY_MAPPING_WRITE_ERRORS` carrying an age-specific detail: the verifier pass proved the route's dispatcher discards `str(exc)` and substitutes the mapped FIXED detail (whose current `ProxyWeightInputError` text — "cited run is not a visible COMPLETED…" — would be FALSE for a staleness refusal), and its exact-type lookup means an unmapped subclass detonates as a 500; the exact numbers live in the service message + the audit trail, the wire detail names staleness. **The promote-day anchor is ratified as the honest "new" side** — promotion is a captured, audited FR write whose instant is already evidenced; `valid_from` is rejected as the anchor (caller-movable in either direction, and not even exposed on the HTTP body). **The declared-on-model shape (a regression-model v2 label bump with grandfather machinery) is recorded as the ESCALATION path**, not taken: promotion binds no model version (the captured-vs-governed fence), OD-PA-3-D's identity bar governs run parameters not capture policy, and a hygiene slice should not mint version labels. The gate composes with BT-2's bind-time gate rather than replacing it: promote-time = "was the data fresh when adopted"; bind-time = "is it fresh for THIS run's as-of". |
| **B — Expire-a-mapping: SURFACED with the census facts and DEFERRED — a design slice, not hygiene** | The Tier-3 facts, recorded for the future decision: nothing time-based exists to hang expiry on; the three structural shapes are (i) a close-without-successor FR write op — a protocol extension whose "no head" outcome means DIFFERENT things per family (proxy: silent degrade to the indicator path; loadings: a fail-closed coverage refusal — expiry would change governed numbers' input population asymmetrically), (ii) read-side age gating at every head-query site (a per-consumer policy sprawl), (iii) a status/expiry column — a migration plus a pin-serializer false-drift landmine of the recorded 0038 class. **Recommend DEFER with a named trigger** (the first real staleness incident, or MG-2's monitoring leg — where the close's benchmark judge already placed it); OD-A's always-audited age + opt-in bound is the honest hygiene-scale payment. |
| **C — The constants sweep: 7 CHANGE + 2 parenthetical + 1 phrasing + 1 optional, keys preserved VERBATIM; the keep-list recorded; the dossier note amended; the doc companions swept** | Every edit per the census table: the stale text in all four key-carrying rows is the framing clause, so each key survives verbatim (`"SPECIFIC/IDIOSYNCRATIC RISK = 0"` ×2, `"it inherits"`, `"ZERO idiosyncratic risk"`), with the no-cross-key-duplication rule checked at review and the two re-seeding PG suites enforcing all 33 lookups fail-loud in CI. The headline correction is the census's NEW find — `PROXY_WEIGHT_LIMITATIONS[2]`'s "a non-CURRENCY candidate fails the run closed" is flatly false since FL-1 and reads as a live capability denial. The KEEP-list ships in this record (active-risk/allocation/proxy/scenario texts true of their gated inputs — with scenario's "(v1 platform scope)" re-phrased to model scope, the gate claim untouched). **`MF1_CLOSURE_FINDING`'s frozen-wording note is AMENDED at this gate** (ratified content, ratified change): the note now covers both surfaces — rows filed before the rewording say "CURRENCY-family"; rows registered after it carry the corrected framing ("read any family-scoped framing as 'the bound factor set'"). **The canonical row numbering the plan executes** (the verifier pass demanded it embedded): CHANGE C1=`VAR_LIMITATIONS[0]`:666-668, C2=`VAR_HS_LIMITATIONS[0]`:850-851, C3=`PROXY_WEIGHT_LIMITATIONS[2]`:1458-1459, C4=`VAR_ASSUMPTIONS_BASE[1]`:651, C5=`ES_ASSUMPTIONS_BASE[2]`:1824, C6=`ES_LIMITATIONS[0]`:1835-1838 (spliced into ES_TOTAL:1878 — ONE edit, key-checked under BOTH the ES and ES_TOTAL searched sets), C7=`VAR_TOTAL_LIMITATIONS[2]`:1613-1614; PARENTHETICAL P1=`FACTOR_EXPOSURE_LIMITATIONS[1]`:111-112, P2=`FACTOR_EXPOSURE_PROXY_LIMITATIONS[1]`:273; PHRASING S1=`SCENARIO_ASSUMPTIONS[2]`:1353 (gate claim untouched); OPTIONAL O1=`VAR_TOTAL_ASSUMPTIONS_BASE[1]`:1591-1596 (taken). Assumptions rows are text-in-place edits ONLY — row count/order frozen (`ES_ASSUMPTIONS_BASE` is spliced into BOTH ES registrations and O1 sits inside a POSITIONAL `[:3]` slice at :2086). The executable conformance test works per SEARCHED SET (dossier code → its registered row set): each of the 4 key-carrying rows retains its resident key, no set gains a duplicate key-substring, and `'FL-1' not in` any edited constant (the token fence made executable — reworded texts surface as finding_text on fresh seeds, which nothing else greps). Companion sweep: the ELEVEN stale methodology-doc lines (the verifier pass re-counted the census's '7') get dated HG-1 amendments — `var_parametric_v1.md:67`,`:81-83`; `var_historical_v1.md:62-63`; `proxy_weight_regression_v1.md:14`,`:49`,`:56`; `var_parametric_es_v1.md:181-183`; `var_parametric_total_v2.md:21`,`:154` (the live mirror of C7 — sweeping the constant while leaving it would re-mint the drift); plus the two lines in the SUPERSEDED `var_parametric_total_v1.md:9`/`:119` (disposition: one superseded-doc banner note, not per-line edits). The `"SPECIFIC/IDIOSYNCRATIC RISK = 0"` phrase survives everywhere (`test_var.py:1354` pins it in `var_parametric_v1.md`) + the one stale code comment. The 'FL-1' token stays out of every new text (the OQ-MF-1-6 discipline). Existing tenants keep old texts (immutable, refuse-not-skip) — behavioral for NEW registrations only, the MG-1 sweep shape with the added key-preservation check. |
| **D — Stage 3 of the living tenant: the genuinely-private α=0.4 chain on multi-family factors — a NEW private-credit instrument joins the sleeve** | A new demo module (`demo/hg1_private.py` + CLI shim + PG suite): **`PC-BRIDGEWATER-II` (private credit fund LP interest)** — economically natural for a RATES+CREDIT_SPREAD loading structure — with **8 quarterly appraisal marks 2024-06-30…2026-03-31** — START-extended (the verifier pass killed the end-extended draft THREE ways: a 2026-06-30 mark puts the 2026-06-19 carry mark INSIDE any desmoothing window spanning the quarterlies — the snapshot pins EVERY current-head mark in the window, minting two sub-quarterly garbage periods, the exact overlap the campaign's own `_DESMOOTH_WINDOW` comment forbids; the mint arithmetic contradicted itself — n marks ⇒ n−2 periods with ends = marks 3..n, so the drafted marks-4..8 mint left the first period uncovered and the runner dies at the coverage gate; and the span end 2026-06-30 makes the new instrument's estimate age −11 days, a hidden LOOK-AHEAD masked by the run echo's max() — incoherent in the slice whose headline is age honesty). The coherent shape: 8 marks ⇒ 6 desmoothed periods (ends = marks 3..8 = 2024-12-31…2026-03-31, df=2) ⇒ an **18-row quarterly mint** (one SIMPLE return per period-end per MF factor, every date ≤ 2026-03-31 — provably invisible to all daily windows), the desmoothing window = the campaign's exact (2024-06-01, 2026-04-30] shape, the estimate age = 2026-06-19 − 2026-03-31 = **80 days** (true again), a position in the SLEEVE portfolio (`DEMO-MULTIASSET` — the true story: a genuinely-private asset inside the multi-family book) and a carry mark ON 2026-06-19 (the exact-date boundary read, OUTSIDE the desmoothing window; validity base = the campaign `_T0` pattern so every row is valid before the boundary read). The chain, all shipped services, `code_version="demo-hg1"`: the **α=0.4 `demo-mg1` desmoothing version REUSED** (no registration — the contrast with MF-1's α=1 mint is the demo's point) over the quarterly window → the k=3 OLS on MARKET/RATES/CREDIT_SPREAD with the **15-row quarterly return mint** (one SIMPLE return per desmoothed-period-end per factor — the campaign's `_OLS_RETURN_DATES` precedent, provably invisible to every existing daily window) → the analyst promotes the structural RATES + CREDIT_SPREAD coefficients (one estimate run, the single-cited-run shape) → a fresh exposure run (4 atoms) → the loadings-family run → covariance → the five flagship runs bound to the `demo-mg1` versions. **Runner guards:** requires BOTH prior stages (campaign models present; the loadings code + sleeve + MF factors resolvable); refuse-not-skip on its OWN footprint = the new instrument's code, seeded FIRST (any partial commit contains the probe); single-commit. Mark generation at α=0.4 is honest about the new wrinkle: observed marks are PRE-SMOOTHED from a declared true-return structure (`r_obs = α·r_true + (1−α)·r_obs,prev`) with **NO idiosyncratic term** (the verifier pass: at df=2 — 6 periods, intercept + 3 slopes — an eps term at any useful amplitude swamps recovery; the only noise is 6dp mark quantization amplified ×1/α=2.5), the three declared quarterly factor paths verified NON-COLLINEAR over the 6 period-ends, and the recovery asserted at |β̂−β| ≤ 0.05 in the suite — the tolerance named at design time, the MF-1 eps lesson applied. **No validation record is filed (OQ-5)**: every bound version carries a live latest outcome; the runs ARE the demonstration. The MF-1 suite's pins stay true on a stage-3-extended tenant (no 18th code, no `demo-mf1` runs, token-free — verified against its filters); the CAMPAIGN suite keeps its recorded fresh-schema-only status and runs FIRST in CI order (its 16-code pin is false on ANY extended tenant — the MF-1 Part 3 item 12 posture, restated not re-derived); the stage-3 suite slots after the multifamily suite in CI, before the downgrade smoke, and carries the recorded role_permission/downgrade KNOWN-EDGE note in its docstring (the standalone-fresh sequence). **The (a)/(c) coupling stated:** stage-3 promotes pass NO bound (ungated, like both prior stages) — the gate is exercised by ITS tests with controlled dates, so no demo seed ever rots against the calendar. |

## Part 2 — External benchmark research (rule 6)

**HG-1 ships no new quantitative method** — the age check is bookkeeping over shipped arithmetic (the BT-2 age semantics, VERIFIED-carried at BT-2, reused with the promote-day anchor); the constants sweep corrects prose to shipped behavior; the demo composes shipped services. Carried citations: SR 26-2 §V ongoing-monitoring posture (VERIFIED-carried, MG-1) frames the always-audited age; Geltner 1991 α-smoothing (VERIFIED-carried, PA-1) is the pre-smoothing generator's model. The negative check: no text claims a regulatory basis for the specific bound-shape choice — opt-in policy is HOUSE process, stated as such.

## Part 3 — Limitations, honesty items + out of scope (recorded)

1. **The opt-in bound is policy, not identity** — an ungated promote remains legal (the status quo); the declared-on-model v2 shape is the recorded escalation if opt-in proves too weak in practice. The always-audited age makes ungated-but-stale VISIBLE, which is the hygiene-scale honest payment.
2. **Expire-a-mapping stays open** (OD-B) — deferred with the three structural shapes + the per-family no-head divergence recorded; trigger: the first real staleness incident or MG-2's monitoring leg.
3. **The existing live tenant keeps the stale constant texts forever** (immutable rows) — the frozen-wording notes in the filed records remain the read instruction there; only fresh registrations carry the corrected texts.
4. **The α=0.4 recovery assertion is tolerance-grade (|β̂−β| ≤ 0.05), not byte-grade** — a zero-idiosyncratic generator, 6dp mark rounding amplified ×2.5 by the inversion, df=2; the suite asserts the named tolerance, the dossier prose states the noise honestly (the MF-1 eps lesson applied at design time).
5. **The stage-3 demo files no validation record** — recorded reasoning in OD-D/OQ-5; the optional loadings TRIGGERED evidence-refresh remains available to a future slice if the user wants the ceremony.
6. **The promote gate does not retro-check existing heads** — it gates new promotions only; existing promoted weights stay as they are (their ages become visible the next time they are re-promoted).

## Part 4 — Open decisions (OQ-HG-1-1…6) — pending ratification

- **OQ-HG-1-1 — OD-A: the promote age check** (always-measure + audit; opt-in `max_promotion_age_days` on service + HTTP body; unmeasurable-with-bound fails closed; promote-day anchor; declared-on-model recorded as escalation). *Recommend APPROVE.*
- **OQ-HG-1-2 — OD-B: expire-a-mapping DEFERRED with the recorded facts + named trigger.* *Recommend APPROVE — it is a design slice with migration-adjacent options, not hygiene.*
- **OQ-HG-1-3 — OD-C: the constants sweep as tabled** (7+2+1+1; keys verbatim; keep-list; the `MF1_CLOSURE_FINDING` note amendment; the 7 doc companions + 1 comment). *Recommend APPROVE.*
- **OQ-HG-1-4 — OD-D: stage 3 as shaped** (PC-BRIDGEWATER-II joins the sleeve; 8 quarterly marks; α=0.4 reuse; the 15-row quarterly mint; instrument-code footprint probe; `demo-hg1`). *Recommend APPROVE.*
- **OQ-HG-1-5 — no validation record for stage 3** (the runs are the demonstration; every bound version carries a live outcome; an EXCEPTION would be false ceremony). *Recommend APPROVE — the alternative (an optional loadings TRIGGERED evidence-refresh citing the first genuinely-private multi-family chain) is defensible under SR 26-2's material-use-change trigger; say so and OD-D gains it.*
- **OQ-HG-1-6 — the scope fence**: NO migration (head stays `0040`); NO permission/EVT/model-code/canonical-id; the ONE API change = the additive optional promote-body field; `audit/service.py` FROZEN; the mixed-family fence + both scenario gates + the active-risk whitelist byte-untouched; legacy instruments untouched; 4-finder review (adversarial on the gate + the runner guards; numeric on the pre-smoothed generator + recovery; doctrine on the reworded texts vs the key constraint + the dossier note; scope-fence). *Recommend APPROVE.*

## Part 5 — Implementation readiness gate

Implementation follows `hg_1_implementation_plan.md` on ratification. Merge preconditions: the 4-finder review folded; `make check`; full local-PG fresh incl. the three demo suites in CI order; `alembic check` no new revision; downgrade smoke; CI green. Closure per the OQ-W5C-5 checklist incl. the grep-for-"pending" item.

### Planning verification (the standing pre-ratification pass)

The pass ran 2026-07-17 (three fresh-context verifiers at xhigh: gate / constants+keys / demo-chain):

| # | Finding | Disposition |
|---|---|---|
| V1 | HIGH (gate): the "naming 422" dies at the HTTP boundary — the marketdata dispatcher discards `str(exc)` and substitutes the mapped FIXED detail (which would be FALSE for a staleness refusal), and its EXACT-TYPE lookup makes an unmapped subclass detonate as a 500 | FOLDED as ratified mechanism: `ProxyWeightStaleEstimateError` + its own exact-type `_PROXY_MAPPING_WRITE_ERRORS` entry with an age-specific detail |
| V2 | HIGH (demo): the end-extended 2026-06-30 quarterly series broke THREE ways — the carry mark landed INSIDE the desmoothing window (two garbage sub-quarterly periods; the campaign's own window comment forbids the overlap); the marks-4..8 mint left period 1 uncovered (n−2 periods end at marks 3..n — the runner dies at the coverage gate); the estimate age went NEGATIVE (−11d, a hidden look-ahead masked by the echo's max()) | FOLDED as ratified shape: START-extended 8 marks 2024-06-30…2026-03-31, window (2024-06-01, 2026-04-30], the 18-row mint at marks 3..8, age 80d true |
| V3 | HIGH (constants): the census's "6 CHANGE rows" enumerated SEVEN, and the plan's numbering pointed at a table embedded nowhere | FOLDED: counts 7+2+1+1; the canonical C1..C7/P1..P2/S1/O1 numbering embedded in OD-C |
| V4 | MED (gate): the THIRD unmeasurable shape was missing — a DANGLING `input_snapshot_id` (bare non-FK column; `resolve_snapshot` raises `SnapshotNotFound`, unmapped = a 500 AND an ungated-path regression); the BT-2 precedent handles it by name | FOLDED into OD-A: three shapes, ungated = None / gated = closed |
| V5 | MED (gate): "the existing audit-payload path" did not exist — the payloads are closed row-derived dicts; both marketdata functions gain an optional defaulted parameter merged into the NEW-version CREATE event ONLY when supplied (MANUAL writes unstamped); no test pins those dicts | FOLDED into OD-A + the plan |
| V6 | MED (demo): "the MF-1 + campaign suites pass unmodified on the extended tenant" was half-false — the campaign suite is fresh-schema-only since MF-1 | FOLDED: restated per suite |
| V7 | MED (demo): the recovery claim needed its noise model — at df=2 the generator must carry NO idiosyncratic term; tolerance named; non-collinearity verified | FOLDED into OD-D |
| V8 | MED (constants): the doc-companion count was wrong (NINE live + 2 superseded lines, not 7 — incl. `var_parametric_total_v2.md:154`, C7's live mirror) and the conformance test needed per-SEARCHED-SET semantics (C6 checked under BOTH the ES and ES_TOTAL sets) | FOLDED into OD-C |
| V9 | LOWs (all three): the audit key renamed `promotion_age_days` (BT-2's persisted `estimate_age_days` is a DIFFERENT-anchored number); the service-side `< 1` refusal; `_ERROR_MAP` corrected to `_PROXY_MAPPING_WRITE_ERRORS`; the test cite split (:352-372 mints, :875-882 exercises); the `valid_from`-freeze test hint struck (it contradicted the ratified anchor — monkeypatch `utcnow`); the 'FL-1' fence made executable in the conformance test; assumptions row-order frozen (the positional `[:3]` splice); the KNOWN-EDGE docstring carry; the validity-base requirement stated | ALL FOLDED |
| V10 | CONFIRMED (the load-bearing survivors): the one-indexed-read claim; the span-end home + purpose check; negative-ages-pass + the max() echo; the floor arithmetic (floor 5; 8 start-extended marks = 6 periods, one spare); PRIVATE_CREDIT legal (plain String(50)); every census line-cite byte-accurate at head; zero tests pin any stale substring; the key-preservation strategy sound for all four key rows; the demo-mg1 declared 400d bound + the α-version reuse; the CI ordering + footprint-probe design | — |

Verdict: ratifiable after the folds above (all applied in this text before the OQ gate was briefed). The pass changed the ratified shape three times (V1's wire mechanism, V2's date redesign, V3's numbering) — the standing lesson holds: it pays for itself every slice.
