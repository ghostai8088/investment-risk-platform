# Wave-5 Close Review — "the numbers govern themselves"

| | |
|---|---|
| **Status** | **RATIFIED 2026-07-15 — OQ-W5C-1…6 all APPROVED by the user** (merged as DRAFT via **PR #42** = `cf6e3b6`; ratified immediately after). **OQ-W5C-6 resolved to sub-fork (A): governance-first — MG-1 → FL-1 → MF-1** — on the Max-effort reassessment recorded below, which REVERSED this document's drafted (B) lean. The user's contemporaneous scope note is recorded: the platform targets enterprise use and will eventually demo REAL data, but not before the build completes — which explicitly defers (B)'s strongest surviving argument (buyer-demo substance) past the build. The mandatory Part-4 rule-2 wave re-baseline. |
| **Method** | A **4-auditor parallel close audit** (shipped-vs-ratified / deferral register / outward benchmark / thesis destination), every claim verified against **code, tests, git and CI — never against a record's claim about itself** (the standing rule: *model confidence is not evidence*). Sources were re-fetched and re-extracted, not trusted. |
| **Ground truth** | `main` = **`2abc1b1`**, **CI green** (Backend / DB migration / Frontend / Documentation / Secret scan, verified via the check-runs API). Migration chain re-walked from the files: **40 revisions, single head `0040_var_estimate_age`**. `make check` **1448 passed / 293 skipped**; full local-PG **1741 passed** (= 1448 + 293, reconciled) on a fresh schema **and** on a dirty double-run. |

## Part 1 — Did Wave 5 ship what was ratified?

**All four slices shipped, in the ratified order, with zero re-sequencing.** Per-slice verdicts:

| Slice | Merge | Migration | Verdict |
|---|---|---|---|
| **RD-3** — hygiene | PR #34 `29bc5a2` | none | **SHIPPED-AS-RATIFIED** (OD-A…E verified live) |
| **VW-1** — SR 11-7 validation workflow | PR #36 `a9a00eb` | `0039` | **SHIPPED-AS-RATIFIED**; its one deviation is recorded, honest and exact |
| **BT-2** — total-series backtest + σ_e age gate | PR #39 `4035f10` | `0040` | **SHIPPED-AS-RATIFIED on substance**; three record defects (below) |
| **ES-1** — parametric ES | PR #41 `2abc1b1` | **none** | **SHIPPED-AS-RATIFIED**; Part 5.5's 5 deviations verified |

**Verified clean, mechanically:** the **audit fence held all wave** (`audit/service.py` last touched at P0.5 `1caae23`; zero diff in all four slice ranges). Every scope fence held (RD-3 and ES-1: zero migrations, zero entitlement; VW-1: only `0039`, exactly 3 `create_table`; BT-2: only `0040`, exactly ONE additive nullable column + a clean downgrade). **Counts confirmed by census, not by increment: 14 governed numbers, 16 registered model codes, 11 IA result tables** (unchanged — ES needed none). ES-1's six constants independently re-derived (stdlib AS241 — an algorithm the repo does not use): all six match to 12dp. VW-1's SOD-03 verified from `ROLE_TEMPLATES` (no non-admin role holds both register and validate); its REJECT gate is real at one seam and mapped to 422 at all **12** run endpoints (9 risk + 3 perf).

### The findings — ALL RECORD/COMMENT HYGIENE, ZERO shipped-code defects. All fixed at this close.

1. **HIGH — BT-2 had NO closure stamp, at four sites** (record Status, record Part 6 tail, roadmap row, amendment log). **This is the THIRD consecutive close to find a missing closure stamp** (Wave-4 found PA-4's; RD-3 merged unstamped and was fixed at the VW-1 closeout — and *its own record was still missed*, found again here as a second finding). The mechanism is now understood: **BT-2 was the only Wave-5 slice with no closeout PR of its own** and fell through the gap between the VW-1 and ES-1 closeouts. See the process fix in Part 5.
2. **MEDIUM — the roadmap cited an ORPHAN commit** for BT-2's planning (`29c58c0`: on no branch, unreachable from any ref, would not exist in a fresh clone). The real one is `da258ff` via **PR #38** = `26d89b4`. Every other Wave-5 hash checked out. **Fixed.**
3. **MEDIUM — BT-2 falsified a shipped-code claim and did not update it.** `snapshot/serialize.py`'s pin-exclusion note argued *"…and the backtest binder refuses `VAR_PARAMETRIC_TOTAL` rows in v1 anyway"* — **BT-2's own OD-A admitted them**, making the clause false at head. The exclusion itself remains correct (false drift is a sufficient reason, and it is test-pinned), and BT-2 also added `estimate_age_days` as a second excluded column without naming it. **This is the same class as the MAR32.27 stretch BT-2 was proudest of catching — self-inflicted, in the code BT-2 changed.** Struck and recorded, not quietly deleted.
4. **MEDIUM — BT-2 ratified a recording obligation and dropped it.** OD-G required *"record the discharge"* of BT-1 deferral B; Part 6 never mentioned it. The underlying state is fine (verified: `begin_nested()` savepoints + `IntegrityError` recovery; all three Wave-5 registrars route through `resolve_or_register_version` — the pattern HELD). **The discharge is now recorded.** The defect was the omission, not the code.
5. **LOW — RD-3's record miscounted the dispatch it hardened** (19/15 branches; actual **18/14**, verified live at three commits). The **shipped code comment was always correct**; only the record was off. Same class as Wave-3's RD-1 census undercount. **Fixed.**
6. **LOW — the amendment log's Wave-5 block was out of order twice**: BT-2 and VW-1 transposed (against both the ratified sequence and ship order), and **"WAVE 4 CLOSED" — the event that *ratified* Wave 5 — had sunk below all four slices it authorized**, because each new row was inserted above it. A reader reconstructing the wave got the wrong order. **Fixed.**

## Part 2 — The deferral register, reconciled

**PAID this wave:** P3-8's `_reresolve_content` parse-hardening (in the review-corrected *scoped* form); PA-1's instrument-guard fold; MD-H1-C's adoption breadth (with a durable skip-list); PA-4 deferral 1 (total-series backtest → BT-2); PA-4 deferral 3's **ES-total half** (→ ES-1); **BT-2's residual coverage gap — both halves, verified non-vacuous** (ES-1's own review caught the first version's vacuity by mutation and moved it: the register working correctly).

**Still OPEN, triggers NOT fired** (verified by counting consumers, not by reading records): BT-1 deferral A / P3-8 return-shape dedup (still exactly 2 consumers); P3-7 deferral B covariance-pin adjudicator (still exactly 2 — ES-1 *reused* rather than copied); VW-1's Part-3 residuals; BT-2's look-ahead and v1-grandfather (both by design, both test-pinned).

**⚠️ The headline register correction — "the estimate-staleness item is CLOSED" was an OVERCLAIM.** Code-verified: the minted item had **three** clauses and Wave 5 paid **one**.

- ✅ *records the estimate age at total-VaR time* — **PAID** (`0040` + the echo).
- ⚠️ *forces a promoted REGRESSION weight to cite a recent estimate* — paid **only on the total-VaR-v2 bind path**. `promote_proxy_weight_estimate` has **no age check at all**, and `var_service` sets `max_estimate_age_days = None` off the total family — so the **same promoted weight** drives `risk.factor_exposure.proxy` → plain VaR / **plain ES** / active risk / scenario **entirely ungated**.
- ❌ *expires one* — **UNPAID**. Nothing time-based closes a `proxy_mapping`; `valid_to` moves only on an explicit human supersede.

The item's own headline — *"a years-stale σ_e drives current total VaR indefinitely and silently"* — is now false for total-v2 binds and **still true everywhere else**. **RE-OPENED as "estimate staleness — residual", TIPPED.** Corrected at all five claim sites.

**⚠️ RD-3's deliberately-skipped NaN bug is still LIVE and its trigger has NOT fired.** `perf/benchmark_relative_service.py:303` parses bench rows with a raw `Decimal(...)`. `Decimal("NaN")` does **not** raise, so it slips the malformed-pin wrapper, then detonates at the magnitude gate (`InvalidOperation`) *outside* any catching `try` — and `execute_governed_run` calls `compute` with no try **after** `create_run` + RUNNING + lineage ⇒ **a raw 500 plus a RUNNING orphan**: verbatim the BT-1 HIGH that `parse_strict_decimal` exists to prevent. The *portfolio* side of the same function **is** guarded — RD-3 adopted there and skipped here. **Precision correction to RD-3's record: only NaN detonates, not Infinity** (`abs(±Inf) >= 1E7` → the gate fires and produces a correct FAILED run). RD-3's fence (no silent behaviour changes in a hygiene slice) was right to skip it; **it is now the oldest live known bug on the board and should be paid in Wave 6**, not left to a trigger that has not fired in a wave.

**Newly minted:** BT-3 candidates (**Christoffersen — TIPPED**, since BT-2 shipped the very clustering pathology it diagnoses; appraisal-frequency pairing — **blocked** on multi-day VaR); ES-over-HS/MC; and **ES-1's `ES_HISTORICAL` migration landmine — verified real**: `ck_var_result_parametric_not_null` (0028, never dropped, **ORM-invisible**) exempts only the literal `'VAR_HISTORICAL'`, so an `ES_HISTORICAL` row would violate it. That leg needs a migration ES-1 did not.

## Part 3 — Outward benchmark (rule 6, at wave scale)

**The Wave-4 claim — "three nearest gaps, all ingredient-complete" — was accurate, and all three shipped.** BT-2 and ES-1 landed as scoped. **VW-1 needs an honest restatement.**

**VW-1 is a real validation *record + gate*, not a validation *program*.** What a supervisor would recognize: the REJECT gate has genuine teeth at one seam; outcomes analysis is correctly anchored (SR 26-2 §V, verbatim: *"Outcomes analysis can take many forms, including… standalone activities such as back-testing"*); the independence basis is verbatim-correct (*"The quality of validation process depends on the rigor and effectiveness of the review rather than on organizational structure"*). What it does **not** do:

1. **Zero validations exist.** **16/16 model codes sit at `UNVALIDATED`.** Wave 5 shipped the *ability* to validate; nothing has been validated. **The wave's own headline — "the numbers govern themselves" — overstates the delivered state: the numbers can now *be* governed; they are not governed.** A supervisor's first question ("show me a validation") returns an empty table.
2. **No materiality/tiering.** The `tier` column exists, is nullable, and gates nothing (`model/service.py` says so). **Materiality is SR 26-2's central organizing principle**, so VW-1 is one-size-fits-all — no risk-based approach at all.
3. No data-level independence (MG-04); **cadence unanchored (OD-033 open)** — `overdue` is a read-time display flag consumed nowhere, so **an overdue model keeps running**; no remediation lifecycle (SR 26-2 §VI expressly contemplates *"tracking of recommendations, responses, and exceptions"*); conditions recorded, not enforced; **no FE surface at all**.

**⚠️ Two citation findings, both the MAR32.27 class, both milder:**

- **VW-1's SR 26-2 exception citation is verbatim-accurate but SCOPE-STRETCHED.** The text frames use-before-validation as a *per-model exception for certain circumstances (e.g. an urgent business need)*, with attention to limitations, stakeholder notice and controls. VW-1 applies it as a **permanent steady state to 100% of models**, with no urgency claim, no per-model justification and no expiry. Direction right, scope overstated. Cheap fix: make it per-model and time-boxed (it is already `next_review_due`-shaped), or state plainly that this is a POC posture the citation does not fully cover.
- **SR 26-2 contains ZERO occurrences of "tier"/"tiering" and ZERO of "annual"** (extraction-verified; "materiality" ×7). It dropped the annual default and made rigor **and cadence** functions of **materiality** — exactly what VW-1 deferred. **So OD-033's cadence question is now un-answerable without materiality.**

**⚠️ THE FINDING THAT CHANGES WAVE 6 — the roadmap's stated Wave-6 blocker is factually WRONG.** The roadmap and the Wave-4 close both say multi-family *"REQUIRES an equity/credit factor-returns capture slice first."* **Verified false, by execution:** `FACTOR_FAMILIES` already carries STYLE/INDUSTRY/COUNTRY/MACRO/MARKET/CURRENCY/OTHER; `capture_factor_return` has **no family check whatsoever**; the shipped test suite **already registers an MSCI_BARRA STYLE/MOMENTUM factor**; and an auditor drove `estimate_covariance` over an equity + credit-spread pair with **zero CURRENCY involvement — it computes today**. The covariance, VaR and ES kernels never see a family at all.

**The real blocker is the loadings side, which is a different and larger thing.** `build_factor_index` keys on `currency_code`, and `allocate_atom` maps each atom to **exactly one** factor at `INDICATOR_LOADING = 1`. The kernel's own docstring claim to be *"dimension-generic"* holds **only for another partitioning dimension** (COUNTRY, ASSET_CLASS — one value per instrument); it is **false for STYLE/MARKET/credit**, where loadings are fractional and non-exclusive. **The project already knew this and the Wave-4 close lost it** — P3-3's record says *"NOT vendor betas (no factor-loading input has ever been captured — a named prerequisite captured-data slice for v2)"*, and the roadmap still lists *"vendor-beta/regression factor exposures (needs a loadings capture slice)"* as a standing deferral. **Ratifying Wave 6 on the current text would plan the wrong slice.**

**Also: the factor-family vocab does not reach FRTB's risk classes.** MAR33.12/33.14's classes are *interest rate, equity, FX, commodity, credit spread*; there is no `CREDIT_SPREAD`, `RATES` or `COMMODITY` family — the roadmap's own "equity/rates/credit/spread" names three that do not exist. Good news: `factor_family` is `String(30)` with **no DB CHECK** ⇒ the extension is **one tuple, zero migration** (the BT-2 `METRIC_TYPES` pattern exactly).

## Part 4 — Destination check (the thesis)

**"Wave 5 added no new private-asset capability — it added self-governance" is the right read, and it is verifiable.** Wave 5 touched the private-asset chain in exactly two places, both governance-of-time: BT-2's age gate and VW-1's stand-down. **Zero new transformation math, zero new factor coverage.** The §2.2 gap is CONFIRMED unchanged and **hard-enforced at four independent refusal sites**. Wave 4 said what remained was "fidelity and governance-of-time"; **Wave 5 paid governance-of-time, so the remaining destination debt is now almost purely fidelity** — which sharpens the Wave-6 question rather than leaving it open.

**⚠️ The chain is demonstrated in two halves that meet at a hand-minted seam.** Real chained runs exist for marks → desmooth → OLS estimate → promote → `proxy_mapping` → factor exposure → covariance → VaR (including PA-2's byte-exact private-vs-public invariance with an independent hand-derived anchor). **But `run_proxy_weight_estimate` is called ZERO times** in `test_var_total.py`, `test_var_total_pg.py` and `test_es.py` — PA-4/ES-1's residual leg is driven entirely by `_mint_estimation_summary`. **No single test carries appraisal marks through to total VaR or ES.** That is a defensible test-design choice (the goldens need a *known* `residual_stdev`), but the Wave-4 phrase "the full-chain integration proof" invites a reader to hear more than it says. **Verdict: PLAUSIBLE over-read, not a false claim.** One integration test chaining a real PA-3 run into a total VaR retires it permanently — a near-free Wave-6 ride-along.

**The honest gaps a buyer/validator would find, ranked:**

1. **CURRENCY-only factors** — four hard-coded refusals. **This single gap caps the credibility of 9 of the 14 numbers simultaneously.** An equity book's VaR is an FX VaR. Sharpest form: **PA-3 implements Sharpe 1992 with the Sharpe 1992 factors removed** — a reviewer who reads our own citation section finds the gap before we point at it.
2. **Nothing runs on a schedule.** The worker is a scaffold whose `run_once()` returns `{"status": "idle"}` and whose docstring says it performs no calculations. All 14 numbers are synchronous HTTP POSTs. "What is my VaR this morning?" has no answer that does not involve a human calling an API. VW-1's `overdue` is a field nothing sweeps.
3. **No limits/breaches/alerts** (ENT-031…034 unrealized) — "computes risk, cannot act on it".

**Frontend reality check — the FE has not been touched since PA-1 (Wave 3).** PA-2/PA-3/PA-4/RD-3/VW-1/BT-2/ES-1 shipped **zero** frontend. **Runnable: 0 of 14** (read-only by ratified design — fine). **Visible: 13 of 14**, and:

- **`PROXY_WEIGHT_ESTIMATE` is a dead-end row** — listed by the backend, but no `RUN_TYPE_TO_FAMILY` entry ⇒ renders un-clickable with no detail view. **The 12th governed number is the one row in the UI that goes nowhere.** PA-3's fold added it to the backend listing and stopped.
- **PA-4's `residual_variance` and BT-2's `estimate_age_days` are not rendered** — the decomposition evidence (the whole point of the additive column) and the staleness echo (the surface VW-1's sunset lever reads) are invisible.
- **VW-1 is entirely invisible** — the wave's headline has no UI; a 2L validator cannot file a validation from the frontend.
- **⚠️ The sharpest one: an ES row renders a `z` that is not its own multiplier.** The FE shows z, Sigma and VaR side by side; for an ES row `z × Sigma ≠ var_value` — **off by ~15% at c=0.99** — with nothing on the row to explain it. The `z_score` echo is DB-FORCED by the ORM-invisible CHECK. ES-1 recorded the cost honestly ("an ES row does NOT reconcile against its own columns") **but did not notice that the frozen FE renders exactly the three columns that invite the wrong arithmetic**. This is the only place in the platform where a governed number **presents arithmetic that does not check out** — and "every number reconciles" is the pitch.

*(Under **§2.3 the missing UI is not a gap** — the thesis defines AI-ready as agent-consumable and verifiable, and on that bar the platform is complete: an agent can drive all 14 numbers and read every assumption. The FE findings are §2.2/buyer-facing. The dead-end row and missing columns are **drift**: FE-1's `permissionFamily` altitude fix was supposed to make new families absorb automatically, and it does for source selection — but a new family still needs a `FAMILIES` entry, so the mechanism silently stopped working the moment PA-3 added a run type, and four slices shipped past it.)*

## Part 5 — Process finding: the missing closure stamp, three closes running

Wave-4 found PA-4's missing. RD-3 then merged unstamped (fixed at the VW-1 closeout — **but its own record was still missed, and is fixed only now**). BT-2 shipped with **four** unstamped sites. The pattern is not carelessness; it is **structural**: a slice's closure hashes do not exist until its PR merges, so they cannot be written in the impl PR — and the *next* slice's closeout is the only thing that stamps them, which fails whenever a slice has no closeout PR of its own (BT-2) or the stamper only fixes the roadmap and not the record (RD-3).

**Ratified fix (OQ-W5C-5): every slice gets its own closeout commit, and the closeout's definition of done is a CHECKLIST, not a habit** — (1) record `Status` → CLOSED + PR + merge hash, (2) the record's own Part-6 tail (the "pending PR merge" line — *the site missed twice*), (3) the roadmap row, (4) the amendment-log entry **inserted in chronological position** (not at the top of the block), (5) `current_state.md`, (6) memory. A close review should not be the thing that finds these.

## Part 6 — Open decisions (OQ-W5C-1…6)

- **OQ-W5C-1 — The audit's findings are RECORDED and the close fixes are ACCEPTED as applied** (BT-2 + RD-3 closure stamps; the orphan hash; the falsified serializer clause struck; BT-1 deferral B's discharge recorded; the RD-3 branch count; the amendment-log order; ES-1's Part-3 HS omission). *Recommend APPROVE — all six findings are record/comment hygiene; the audit found **zero shipped-code defects** across four slices.*
- **OQ-W5C-2 — The "estimate-staleness item is CLOSED" claim is CORRECTED to an overclaim and the item is RE-OPENED as "estimate staleness — residual" (TIPPED).** *Recommend APPROVE — code-verified: 1 of 3 clauses paid. The claim is currently false at five sites, all corrected. This is exactly the drift a wave close exists to catch.*
- **OQ-W5C-3 — VW-1's outward posture is RESTATED honestly**: a validation *record + gate*, not a validation *program*; **zero validations exist**; the SR 26-2 exception citation is scope-stretched (per-model exception applied as a blanket steady state); SR 26-2 has no "tier"/"annual" and organizes on **materiality**. *Recommend APPROVE — the honest version is weaker but true, and this is the citation class that has now bitten twice.*
- **OQ-W5C-4 — The roadmap's teed Wave-6 prerequisite is WRONG and is struck.** Multi-family does NOT need a factor-returns capture slice (capture is already family-generic — proven by execution). The real prerequisite is a **loadings substrate + a fractional, non-partitioning exposure kernel v2**, plus the zero-migration vocab extension to FRTB's risk classes. *Recommend APPROVE — ratifying Wave 6 on the current text would plan the wrong slice.*
- **OQ-W5C-5 — The closure-stamp checklist (Part 5) is adopted as standing process.** *Recommend APPROVE — third consecutive close finding the same class.*
- **OQ-W5C-6 — WAVE 6, and this is the real fork.** **Both auditors independently recommend multi-family factor capture as the headline, and — for the first time in three closes — the thesis and the supervisory gap point the SAME way.** Multi-family is not merely "the biggest gap": it is a **hard prerequisite for four other candidates** (FRTB liquidity horizons are assigned *by risk class*; partial ES is defined *over risk classes*; scenario shocks are CURRENCY-only; and §2.2's bar *is* the risk-class taxonomy). Nothing else on the board unlocks four things. The sub-fork is **what comes first**:
  - **(A) Governance-first — MG-1 → FL-1 → MF-1.** Lead with **MG-1: model materiality + the first validation campaign** (S/M; `tier` and ENT-037 already exist), then the loadings substrate, then multi-family. *Argument:* it finishes Wave 5's own headline rather than leaving a capability with an empty table, removes the single most embarrassing supervisory answer ("we have never validated anything"), and **SR 26-2 makes OD-033's cadence un-answerable without materiality**. *Counter-case, stated not hidden:* it is **governance-on-governance — a third consecutive rails slice right after a full governance wave**, and it is not thesis-differentiating.
  - **(B) Payload-first — FL-1 → MF-1, with MG-1 as a ride-along or Wave 7.** *Argument:* Wave 5 was entirely rails; the thesis debt is now **purely fidelity**; and the CURRENCY-only gap caps 9 of 14 numbers *today*. *Counter-case:* the first PERIODIC validation of `risk.var.parametric` will have to confront specific-risk=0 on a one-family universe — **Wave 5 built the instrument that will indict the factor universe**, and shipping validation records against a one-family model documents the weakness in an append-only table.
  - *The DRAFT recommended **(B)**, narrowly, against the benchmark auditor's lean. **A Max-effort reassessment (user-requested, pre-ratification) REVERSED that to (A)**, on three grounds: (1) the draft's load-bearing "indictment" argument INVERTS under scrutiny — the CURRENCY-only weakness is ALREADY documented in append-only form at ~6 sites (P3-5's registered limitations, PA-3/PA-4's records, two wave closes), so a validation finding adds no new disclosure, only governed structure (a condition, a due date, a re-validation trigger); sequencing the fix BEFORE the validation is the anti-thesis ordering — validate-first → conditions → remediate (FL-1/MF-1) → TRIGGERED re-validation demonstrates the entire flywheel, including the two validation types nothing has exercised; (2) the "more math on a bad substrate" argument does not apply to (A), which keeps FL-1 in-wave one slice later with only governance in front of it — at this project's demonstrated cadence the fidelity delay is roughly a day; (3) two of this audit's own findings are standing CITATION/POSTURE defects only MG-1 retires properly (the F3 scope-stretched SR 26-2 exception needs materiality-gated time-boxed exceptions; OD-033's cadence is un-answerable without materiality), and known misstatements outrank honest recorded debt under the house discipline. (A) also matches the shipped wave shape (Waves 3/4/5 all banked a small opener before the L headline). What survives of (B): overrun asymmetry and buyer-demo substance — the latter explicitly deferred by the user's real-data scope note.* Wave 5 already bought the governance rails; the platform's remaining thesis debt is fidelity, and MG-1's value is largely **unlocked rather than lost** by ordering it second — whereas every wave FL-1 is deferred, another slice of math gets built on a substrate that cannot express an equity factor. **But this is genuinely the user's call**, and (A) has the better supervisory story.
  - **RATIFIED: (A).** Wave 6 = **MG-1 → FL-1 → MF-1** (roadmap Part 2.9).
  - **Named ride-alongs either way** (all S, none needing a migration unless noted): the **RD-3 NaN bug** (the oldest live known bug; its trigger has not fired in a wave — pay it, do not wait); the **FE drift trio** (the dead-end PA-3 row; the missing `residual_variance`/`estimate_age_days` columns; **the ES `z × Sigma ≠ var_value` display**, which is the only place a governed number shows arithmetic that does not check out); and the **one integration test** closing the estimate seam. **ES-HS-1** (S/M, needs the one `ES_HISTORICAL` migration) is the highest value-per-effort payload on the board — today's ES is a unit conversion; over HS it becomes a real number and a genuine Acerbi-Szekely ES backtest becomes meaningful.

## Part 7 — Citation hygiene (carried for whoever plans Wave 6)

**VERIFIED** (extracted and read at the close): SR 26-2 §V/§VI; BCBS d457 MAR33.3/33.9/33.12/33.14, MAR32.4/32.5/32.18; d219 (incl. the Annex-2 subadditivity sentence ES-1's review corrected); bcbs265 §1.4(i); Embrechts-McNeil-Straumann; Acerbi-Tasche Prop. 3.1 / Cor. 5.3(i) / Prop. 4.1. **SECONDARY — do NOT cite as-is without fetching to paragraph:** EBA/GL/2018/04 (search summary only); PRA SS1/23's "tiering" (inherited from VW-1's own verification, not re-fetched at this close).
