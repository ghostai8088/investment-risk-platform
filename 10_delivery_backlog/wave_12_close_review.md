# Wave 12 Close Review — "Operations, Reachable" (the mandatory Part-4 rule-2 re-baseline)

| | |
|---|---|
| Status | **RATIFIED 2026-07-26 — OQ-W12C-1/2/3 approved (all as recommended)** |
| Wave | 12 (OQ-W11C-1=A "OPERATIONS, REACHABLE") — API-2/API-2b → NOTIF-1 → CAD-1 → OPS-1, plus the in-wave CI-parity hardening (PR #128) |
| Window | `dd63ba7..6f8d923` (PRs #119–#128; 108 files, ~21k insertions) |
| Method | **Ten close auditors under ultracode (Fable 5)**: five slice verifiers + four cross-cutting (integration/adversarial re-probe, security/doctrine, deferral register, docs/stamps/CI) + one agenda-claims verifier over the metrics gap map; refute-by-default, every material claim checked against code/tests/migrations/git at HEAD with empirical probes (eslint --stdin, live fixture replays, PG negative controls). **Every HIGH/MED finding independently attacked by two adversarial refuters** (correctness + materiality lenses); survivors re-verified by hand before folding. 32 agents, ~2.0M tokens, 798 tool uses. |
| Counts | **23/38/109 UNCHANGED** across the wave (machine-pinned by the PPF-3 census; no slice minted a governed number — the wave built transport, alarm, ignition and glass over the Wave-11 engine, correctly). |

## 1. Slice verification — all five SHIPPED-AS-RATIFIED on the runtime axis; TWO guard-layer HIGHs found and FOLDED AT THIS CLOSE

**The headline, stated honestly:** the 8th consecutive close with **zero runtime/control defects** — every gate, guard and invariant held under adversarial probing END-TO-END. But the clean streak does NOT extend to the guard layer this wave: two shipped *guards that did not guard* (both OPS-1 review folds whose delivered form failed its own ratified claim) were found, empirically confirmed, and folded at this close. Both had zero runtime exposure today; both are exactly the standing angle (c) class — a conformance gate never tested against its actual failure form.

- **API-2** (PR #120, no migration) — VERIFIED. Deny-by-default permission singletons on every route; the approve gate genuinely reaches MG-3's person-level maker-checker; bypass probes all held (activate-via-edit → 422 `extra="forbid"`; activate-via-resume refused; demote-suppression refused; the route inventory pinned as a closed set; a maker HOLDING `limit.approve` still 409s — person-level, not role-level). The MED-3 payment verified at the ratified locus: `_canonical_actor_id` in the ACTOR DATACLASSES (`limit/events.py:173-188`), stamp==compare symmetric by construction, with `require_uuid_principal_id` fail-closing non-UUID principals to 401. The error-map MRO trap closed (every subclass has its OWN key; `map_refusal` walks the MRO).
- **API-2b** (PR #121, no migration) — VERIFIED, unit suites re-run 52/52. The P1 deadlock restructure real and non-vacuously tested (the two-breach cycle regression; the GUC re-arm pinned by a test whose fail-open outcome would be `[]`); the P2 epoch guard unbypassed (the respond→REJECT→escalate→ACCEPT defeat refused at both tiers); `expected_seq` checked UNDER the lock with its own error key; OQ-1/2/4=A all in code.
- **NOTIF-1** (PR #123, migration `0052`) — VERIFIED. Phase 4 per-tenant inside the tick (no cross-tenant sweep anywhere); 0052 carries the full 0001 pattern (FORCE RLS symmetric policy + append-only trigger + NO ops-role grant, PG-pinned); the cursor lesson genuinely closed — oldest-first gap-free scan, per-event top-level transactions, break-on-failure, the leapfrog pinned on PG with an injected mid-batch failure; the LOG sink honestly labeled (SENT = "the LOG sink accepted", never a claimed email); `NOTIFY.DISPATCH` (EVT-270) the wave's ONLY mint, taxonomy-recorded.
- **CAD-1** (PR #125, no migration) — VERIFIED, worker suite re-run 18/18. Per-tenant isolation with the summary write OUTSIDE the try; fail-closed empty tenant list at BOTH layers; failure-streak WARN; both carries PAID (see §2); `main.py` retired; Dockerfile/compose/env consistent; CTRL-031 moved INVOCABLE→OPERATING with an accurate description.
- **OPS-1 + CI-parity** (PRs #127/#128, no migration) — runtime VERIFIED (single write surface, `expectedSeq` required at the type level; all four folded HIGHs present; api-prefixes bidirectional set-compare; the audit-allowlist doubly fail-closed on expiry; the demo mints nothing and refuses on a missing catalog code; the CI pin's negative control reproduced independently). **Two guard-layer HIGHs found, both surviving 2-refuter adversarial verification, both FOLDED AT THIS CLOSE:**
  - **H-C1 — the write fence was bypassable.** The eslint rule enumerated three literal specifiers (depths 2–3 only); empirical probes showed `./api/client` (the natural src-root form — 9 files sit there) and every depth-4+ form passed lint CLEAN, while the decision record ratified the fence as enforcing OQ-2=A "verified with a probe import" (a probe that covered only a listed form). **Fold:** the rule now uses a `patterns` group covering every relative spelling (24 form×location probes all BLOCKED; namespace form BLOCKED; `writes.ts` exemption and `apiGet` still clean), plus **`write-fence.test.ts`** — the fence unit-tested through the real ESLint API against the ACTUAL bypass forms as negative controls (verified: 3 of 9 tests fail against the old rule, 9/9 pass against the new).
  - **H-C2 — the refusal-detail pin was half-vacuous.** The backend test claiming to pin the FE-load-bearing 409 `detail` strings guarded its SoD assertion behind `if status == 409:` — a branch PROVEN dead by replaying the exact fixture (the dual-hat never responded, so their review legally returned 200); the illegal-transition and both limits-side details were pinned by NO executed assertion anywhere (FE tests match hand-copies — circular). Four independent auditors converged on this class (≥3-finder convergence = CONFIRMED). **Fold:** the breaches pin rewritten with one UNCONDITIONAL exact-string assertion per marker (the dual-hat now responds first, guaranteeing SoD reachability), and a symmetric limits-side pin added (`test_the_limit_refusal_detail_strings_are_a_pinned_contract`) covering SoD/duplicate/illegal-transition.

**Also folded at this close (the surviving MEDs):**
- **M-C1 — the residual tick×HTTP lock edge (integration audit).** The ratified "phases 1–2 take NO row locks" claim is FALSE: a new-breach INSERT takes FK `FOR KEY SHARE` on the parent `limit_definition` row, which conflicts with the limit verbs' `FOR UPDATE`→advisory order — with the advisory held from an earlier same-transaction emit, a reachable 40P01 (operationally correlated: an operator suspends a limit exactly when it breaches). Bounded and self-healing tick-side (per-limit SAVEPOINT, retried next tick) but the limits router returned a raw 500. **Fold:** `deadlock_503` hoisted to `deps.py` and applied on all five limit write verbs (503 + Retry-After, OpenAPI regenerated, fail-loud non-40P01 pinned); the scheduler docstring corrected to the honest statement. The full PG-tier interleave regression is named OPS-H1 scope (§5).
- **M-C2 — the closure-stamp teeth had narrowed.** `check_docs.py` matched only the `| **Status** |` table row; CAD-1 and OPS-1 use a `**Status:**` prose line, so BOTH newest records sat outside the gate (their CLOSED stamps existed by discipline, not machine — empirically demonstrated: a RATIFIED-stamped prose record does not fire the gate). The 8th appearance of the closure-stamp class, in a new form: shape-blindness of the teeth themselves. **Fold:** both shapes recognized; unit-tested against the actual failure forms (prose-RATIFIED fires, prose-CLOSED clean) per the OQ-W10C-5 doctrine.
- **M-C3 — the control-matrix sweep was skipped at 2 of 4 closeouts.** API-2 and OPS-1 never touched the matrix; CTRL-021 (the wave's centerpiece SoD control) recorded neither its HTTP nor its UI reachability leg, asymmetric with CTRL-031's exemplary per-slice annotations. **Fold:** CTRL-021 backfilled (API-2 HTTP leg + OPS-1 UI leg + the wire-detail pin), CTRL-031 given its OPS-1 operable-from-UI leg. Process rule at §6.
- **Pin hygiene (LOW cluster):** the CI-parity pin now strips full-line comments (comment-out == deletion, negative-controlled) and counts every `.py` token of a multi-path pytest line.

## 2. Deferral / carry register — 5 PAID / 15 OPEN-and-legitimate / 3 TIPPED (all three slotted at this close)

**PAID, verified in code (refute-by-default; no claimed-paid-but-unpaid item exists):** (a) MG-3 MED-3 actor canonicalization — at the ratified dataclass locus, two-tier defeat tests incl. the twice-carried PG case-variance HTTP replay; (b) the `create_schedule` P3-5 FK guard — both referents re-resolved pre-insert at the sole constructor, PG-proven under forced RLS; (c) OQ-a worker `--tenant` canonicalization — three layers (CLI exit-2, supervisor parse, L4 in-tick defense); (d) the `*.manage`/`breach.*`/`limit.approve` API forward-gate — realized with non-vacuous distinct-gate tests and ZERO new permission minted; (e) the notifications EVIDENCE leg (the register's actual wording) — the real wire channel stays provisioning-coupled OPEN by ratified design.

**TIPPED — trigger fired in-wave, slotted NOW (§5):** (1) **`select_overdue_breaches` N+1 + the phases-1–2 tick-stall revisit** — the Wave-11 "bound in Wave 12" intention lapsed and CAD-1's cadence made it a recurring per-interval cost (full unfiltered scan incl. CLOSED breaches, 1–2 extra queries each, every tick) → **OPS-H1**; (2) **React-19/react-router-8 migration** — deadline-parked (allowlist expiry **2026-10-24**, CI-enforced fail-closed; the OQ-1=C in-build refutation forecloses the downgrade escape permanently) → **FE-M1**; (3) **demo `_NOW` freshness** — the frozen-clock seeded breach will auto-escalate + notify on the first real tick against the demo tenant (mitigated today by the fail-closed empty `IRP_TENANT_IDS` default) → **OPS-H1**, with the interim standing rule at §6d.

**OPEN-and-legitimate (each re-verified as trigger-parked):** SCH-1 family-agnostic schema deferrals (no second family yet); the REAL notification channel (no `app_user` contact column; BR-10 forbids the credentials now); `limit_utilization` ENT-032; reviewer ≠ closer (v2 stringency, ratified); manual-2L-ESCALATE + per-limit SLA; calendar cadence + subtree scope; PPF-3 v2 leverage seams (tips AT real data); ROPC `directAccessGrantsEnabled` (local-only); JWKS live-IdP test; stage9z presence-not-precision; vendor-beta capture; WORM/anchored audit; gitleaks OD-049; API-2 provisioning preconditions (F3/F4 hard gates); API-2b named v2s (SLA verbs, re-assign, `(tenant_id, detected_at)` index, required-`expected_seq`-at-API once no unconditioned consumer remains). **New LOWs recorded to the register this close (from the audit, none warranting code now):** the dev_header tenant id arms the RLS GUC un-canonicalized (dev-mode-only, refused outside `app_env=local` — the OQ-a class's third boundary, OPS-H1); `run_in_tenant`/`tenant_session`/`run_tenant_job` lack the L4 defensive canonicalization (no external-string caller at HEAD, OPS-H1); the ops-stage teardown deletes ALL demo-tenant role wiring (inherited campaign pattern, CI-safe, OPS-H1); no census re-pin after stage 14 (counts hold structurally, OPS-H1); a skipped `IRP_TENANT_IDS` entry has only a one-shot startup signal; the L3 summary-line co-change hazard (no compose restart policy); NOTIF-1 recipient-granularity under concurrent same-tenant ticks (unreachable in the shipped sequential supervisor, v2-sink-relevant); OPS-1 L-7 notifications pager / L-8 explain-branch view tests / L-9 `client.ts` success-parse placement; no FE `assign` consumer (a DETECTED breach cannot be assigned from the UI — weigh at the next ops-UI slice against the "a demo that cannot reach a control" lesson).

## 3. Cross-slice integration — COHERENT; every end-to-end probe held

One tick entrypoint, four ordered phases (schedules+detection | escalation | notification), phases 3–4 as per-item top-level transactions under `persistent_tenant_context` re-arm (PG-proven in CI); uniform row→advisory order on every per-item transaction — phase 4 delivers lock-free FIRST, then records (the API-2b anti-pattern kept out). **Probes that held:** maker self-approve via every route/verb combination; 1L self-review/close against the all-time responder set incl. PG case-variance; the seq/epoch weave (the REJECT→escalate→ACCEPT laundering refused through HTTP; no in-repo consumer relies on the ratified `expected_seq=None` path — the UI requires the token at the type level); canonicalization at every RLS-arming entry; the Wave-11 anti-laundering property COMPOSES through phase 4 (a breach on a since-demoted limit keeps escalating AND keeps notifying — `pending_alarm_events` is structurally blind to limit status). The refusal-contract chain (backend `_ERROR_MAP` → `flattenDetail` → `classifyRefusal` → Refusal view) is now wire-pinned at both ends (the H-C2 fold).

## 4. Outward benchmark + destination (rule 6b)

**Wave 11's named distance-to-frontier — "the controls have no consumption surface" — is CLOSED.** The engine is reachable (a governed HTTP surface over every Wave-11 control), alarmed (durable per-recipient proof-of-alert evidence + a hash-chained dispatch ledger — the SR 11-7 alert leg now EXISTS as evidence), running (a configured, evidenced cadence — CTRL-031 OPERATING; BCBS 239 timeliness now has an operating frequency), and visible (a demo a supervisor can actually drive, with the platform's first FE write path conditioned on `expected_seq` and honest refusal explanations). Against typical vendor risk platforms the SoD story remains AHEAD (person-level, DB-linearizable, reachability-proven through HTTP and UI); lineage/reproducibility AT/AHEAD. **Honest residuals:** the alert channel is a LOG sink — provable alerting to a ledger, not yet to a human (provisioning-coupled); the supervisor is single-replica in-process; assign has no UI consumer.

**The new biggest distance-to-frontier is CREDIBILITY OF THE NUMBERS, on two axes:** (1) everything still runs on synthetic demo data — real market/portfolio data is now the gating credibility step (the standing tee); (2) analytics breadth vs any commercial platform — the USER-directed gap (2026-07-26): no rolling vol/returns, no drawdown, no Sharpe, no sector/geography exposure, no concentration. The close verified the gap map (§5): the first two metrics families are computable TODAY from shipped governed series; the dimensional ones genuinely require the real-data wave.

## 5. Re-baseline — WAVE 13 PROPOSED: "ANALYTICS BREADTH ON THE GOVERNED RAILS" (OQ-W12C-2)

Verified slotting facts (agenda auditor, two corrections folded): PM-1's `portfolio_return_result` is a dated, per-period, fully governed series — rolling volatility/returns and max drawdown need **zero new capture**; IR + ex-ante/ex-post TE are ALREADY SHIPPED (P3-7/P3-8); **Sharpe's blocker is smaller than mapped** — a risk-free series rides the EXISTING ENT-052 benchmark capture (open `(code, source)` identity, vendor-published TOTAL-basis returns, already snapshot-pinnable) or ENT-021 curve points, so NO new capture family; **sector exposure is 0% computable today** (not "partial" — no fixture creates a single issuer row, and no aggregation engine reads sector), industry/country-of-risk columns don't exist — the dimensional metrics genuinely arrive WITH vendor data. Drawdown has no realizing REQ row (CAP-5.4 capability prose only) — the slice mints one.

1. **RM-1 — rolling metrics** (rolling return/volatility + max drawdown over the governed PM-1 series): the 21st governed number family, P3-8-shaped (result family + methodology doc + registered model; reuses `perf.run`/`perf.view`). Carries IN-slice: the day-count/irregular-spacing convention (Tier-3), the annualization decision (re-opens the recorded REQ-PRF deferral), a demo-history extension (8 one-day sub-periods is too thin for a demonstrable window), and the drawdown REQ mint.
2. **SR-1 — Sharpe ratio**: capture a risk-free series through the EXISTING ENT-052 benchmark path (a T-bill/cash TR index as just another benchmark; the yield→period-return alternative belongs to the registered model_version if ENT-021 is chosen — Tier-3 fork at planning), then the Sharpe calc. The 22nd governed number.
3. **OPS-H1 — operations hygiene** (Part-4 rule-3 insertion; the MD-H1 precedent): the fired-trigger cluster — the `select_overdue_breaches` bound (the D9 greatest-n-per-group template is already shipped for `list_breaches`) + the honest tick-stall statement + a PG regression for the M-C1 phase-2 interleave + demo `_NOW` regenerate-on-seed — plus the recorded LOW cluster (§2: dev_header canonicalization symmetry, the L4 sibling helpers, teardown narrowing, census re-pin, notifications pager, `client.ts` L-9).
4. **FE-M1 — React 19 + react-router 8 migration**: the deadline debt, done well before the 2026-10-24 expiry; the audit gate re-run on the migrated tree IN the decision record (the §6a rule applied).

**Wave-14 tee (named now, not ratified):** real-data onboarding — sector/industry/geography-of-risk reference dimensions + concentration (REQ-CRD-003) + liquidity tiers (REQ-LIQ-001/002) ride it as its analytic payoff, per the user's 2026-07-26 direction that these are IN scope and the verified fact that their data arrives with the vendor feed.

## 6. Process observations (OQ-W12C-3)

- **(a) Recommendation-before-verification — ratify as STANDING, generalized to review folds.** Two Wave-12 instances (OQ-1=C ratified before the audit gate ran on the downgraded tree; the OPS-1 demo asserting SoD controls it couldn't reach) plus this close's two HIGHs (folds whose enforcement claim was never negative-controlled) are ONE rule: **a ratification option or a shipped guard that is cheaply testable must carry its executed test IN the record — a fence/pin/gate ships WITH its negative control run against the actual bypass/failure form.** (OQ-W10C-5 generalized from CI teeth to all guard code.)
- **(b) The conformance-pin pattern — ratify as the standing answer to hand-mirrored contracts.** The family now has four members (api-prefixes ↔ nginx; PG suites ↔ ci.yml; install list ↔ suite imports; refusal details ↔ FE markers; the write fence ↔ its bypass forms). Standing rule: any contract hand-mirrored between two artifacts gets a machine pin with a negative control. The remaining harness asymmetry — **CI Python 3.12 vs local 3.13** — is accepted-as-recorded (CI is the authoritative gate; a deliberate version-bump slice can align them later).
- **(c) The closure-stamp class: outcome axis clean, mechanism axis fixed again.** No unstamped record shipped this wave (streak holds), but the teeth had silently narrowed over the new record shape — caught and broadened WITH shape tests. Corollary rule: **the closeout control-matrix sweep must leave a trace** — each closeout either touches the matrix or states "no control moved" in the record (the API-2/OPS-1 skips left the flagship SoD row stale).
- **(d) Interim standing rule until OPS-H1 ships:** `DEMO_TENANT_ID` is never placed in `IRP_TENANT_IDS` (the frozen-clock demo breach would escalate + page on the first real tick and mutate the curated walk). **RETIRED 2026-07-28 at OPS-H1 (H1-4, OQ-OPS-H1-2): the stage clock is now seed-time-relative (backdated two days, so the curated walk is preserved exactly — the breach still reads overdue at seed). The PROHIBITION is replaced by a DOCUMENTED CONSEQUENCE: enrolling the demo tenant in `IRP_TENANT_IDS` runs its lifecycle — the overdue breach escalates on the first tick as governed, CORRECT behavior, not a bug — and an operator who wants the pristine walk back re-seeds. Regeneration removed the absurdity (a year-stale instant escalation), not the mutation; the config flip remains an operator choice.**

## 7. Ratified outcomes

- **OQ-W12C-1 = A** — the close verdict accepted: five slices shipped-as-ratified, zero runtime defects (8th consecutive clean close on the runtime axis), two guard-layer HIGHs + three MEDs folded AT this close (§1) with all gates re-verified green (`make check` 2036; full-PG **2463/0** fresh-schema; `fe-check` 148 incl. the 9 fence pins; downgrade smoke clean), register 5 PAID / 15 OPEN / 3 TIPPED-and-slotted.
- **OQ-W12C-2 = A** — Wave 13 = RM-1 → SR-1 → OPS-H1 → FE-M1 ("analytics breadth on the governed rails" + the debt); Wave-14 tee = real-data onboarding carrying the dimensional analytics; Sharpe rides existing capture (no new family).
- **OQ-W12C-3 = A (all four)** — the process batch: (a) recommendation-before-verification incl. the executed-negative-control rule for shipped guards; (b) the conformance-pin standing answer + accept the Python 3.12/3.13 asymmetry; (c) the control-matrix sweep trace requirement; (d) the interim demo-tenant tick exclusion.

Wave 12 made the Wave-11 engine reachable, alarmed, running and visible — and its close made the guards guard. The platform's next distance to close is credibility: broader analytics on the governed rails, then real data through them.
