# ES-HS-1 Decision Record — ES over historical simulation (the Wave-7 headline, slice 2)

| | |
|---|---|
| **Status** | **RATIFIED — OQ-ES-HS-1-1…7 ALL APPROVED 2026-07-17 (user: "Approve all"; planning merged via PR #57 = `7568c49`).** Implementation proceeds per `es_hs_1_implementation_plan.md`. |
| **Grounding** | Drafted 2026-07-17 on `main` = `e71455f` (HG-1 closed via PR #55/#56). Census: a 4-domain workflow fan-out (seam / migration / backtest / surface — 91 claims, every one file:line or executed). **The primary source obligation is DISCHARGED FIRST** (the roadmap's fetch-at-planning MUST): Acerbi-Szekely 2014 is now **verified-via-reproduction** — the primary (Risk 27(11) 76–81; the MSCI Research Insight) is GATED at both hosts, so the Test-2 statistic was verified VERBATIM through Moldenhauer-Pitera (arXiv:1709.01337v3 p.15 Eq. 6.2, quoting "the test statistic given in Equation (6) in Acerbi & Szekely (2014)") with the null-expectation identity independently confirming the shape, plus Kratz-Lok-McNeil (arXiv:1611.04851) and the MathWorks implementation docs — full extraction note in Part 2. **Domain (a), the seam:** the HS kernel (`var_hs_kernel.py:68-112`) sorts the per-date P&L vector ascending and selects the k-th order statistic (k=⌈N(1−c)⌉, exact-Decimal `order_statistic_index` :51-65) inside a prec-50 localcontext with ONE HALF_UP 6dp quantize — but the SORTED vector is a local, NOT exposed (`HsVarEstimate` returns only n/k/var_value, :41-48), so the ES leg needs a sibling kernel function over the same vector. The HS binder `run_var_historical` is SINGLE-family (asserts `VAR_HS_MODEL_CODE` only, hardcodes `METRIC_TYPE_VAR_HISTORICAL` with NULL covariance_run_id/z_score/sigma — `var_hs_service.py:291-296/:352-381`); the ratified dispatch shape for adding a family is the REGISTRY MAP (ES-1's `_VAR_FAMILIES` `var_service.py:569-604`; FL-1's `_ExposureFamily`), never a try/except chain. `build_var_hs_snapshot` does NOT read the model registry (its only model-derived input is window_observations — `snapshot/service.py:2566-2659`), so the ES-HS run reuses `PURPOSE_VAR_HS_INPUT` + the builder BYTE-UNCHANGED — symmetric-safe ONLY because the pinned content is identical across the two families (unlike the plain/total predicate split; recorded as an adjudicated reuse, not an accident). **The estimator is PRE-PINNED in three shipped registered texts** (`es_kernel.py:21-34` docstring; `bootstrap.py:628-633` + `ES_LIMITATIONS[3]` :1867-1876; `es_1_decision_record.md:13`): **Acerbi-Tasche Prop. 4.1 — floor count + FRACTIONAL boundary weight over losses sorted worst-first, ÷ n·a — NEVER the mean of the worst ⌈n·a⌉ losses** (that quantity IS the forbidden TCE and understates ES ~14% at n=41, the platform's own HS floor at c=0.975). `ES_HISTORICAL` appears in ZERO code files (grep-executed; docs only). The row shape needs NO new column: var_value=ES + the exact VAR_HISTORICAL NULL trio (`models.py:220-274`), rides the FROZEN pin key set byte-unchanged (`serialize.py:468-512`, None-tolerant since 0028; `test_var_result_pin_key_set_is_frozen`). Reads flow metric_type-generically through the existing GET family and the FE renders a new metric_type with ZERO frontend change (the only special case annotates `ES_PARAMETRIC`'s z echo — moot for a NULL-z row; `RunDetail.tsx:10-28`). **Domain (b), the migration:** the 0028 CHECK is `"metric_type = 'VAR_HISTORICAL' OR (z_score IS NOT NULL AND sigma IS NOT NULL AND covariance_run_id IS NOT NULL)"` (`0028_var_historical.py:43-48`), migration-only and ORM-INVISIBLE (`models.py:240-241`; SQLite schema = `Base.metadata.create_all`, `conftest.py:27-32`) — an ES_HISTORICAL row VIOLATES it in PG while passing the ENTIRE SQLite battery, exactly as ES-1 Part 3 item 4 pre-recorded ("the no-migration headline is ES-1's alone and must not be inherited by assumption", `es_1_decision_record.md:45`). PG cannot alter a CHECK in place ⇒ 0041 = drop + recreate under the SAME short name `parametric_not_null` (name stability — 4 doc/test citations grep on `ck_var_result_parametric_not_null`); NO other constraint/index/trigger on var_result mentions metric_type (executed grep); the grain UNIQUE already admits the row. The downgrade MUST copy 0028's 6-statement sandwich (`0028:51-75`): ES_HISTORICAL rows are unrepresentable under the narrow CHECK ⇒ destructive DELETE with the append-only trigger AND FORCE RLS transactionally disabled (FORCE binds the table owner — a non-superuser role otherwise silently deletes ZERO rows), delete BEFORE re-adding the narrow CHECK (PG validates existing rows at ADD time), then ENABLE + FORCE both restored; 0041 touches NO nullability (unlike 0028). CI's downgrade smoke runs `alembic downgrade base` over the fully-seeded demo tenant as the job's LAST step (`ci.yml:319-322`) — **the stage-4 demo (OD-F) seeds real ES_HISTORICAL rows, so CI exercises 0041's destructive delete on every run** (under the container superuser — the exact path that masked the 0028 RLS bug, `0028:59`; a non-superuser downgrade-path PG test closes that recorded gap for the new delete). **Domain (c), the fork:** the AS Test-2 backtest needs the PAIRED per-day (VaR_t, ES_t) + realized X_t (M-P verbatim: "Test 2 framework require IM methodology for both VaR and ES methodologies"). At HEAD that pairing is DOUBLE-blocked: two-series pinning trips BOTH the one-method gate (`var_backtest_service.py:275-280`) AND the duplicate-window_end gate (:313-317 — a VaR-HS and an ES-HS row at the same as-of share window_end); a paired-VaR column on var_result is a SECOND schema change beyond the ratified ONE-migration envelope AND collides with the frozen pin key set (adding a key falsifies every historical BT-1/BT-2 pin — the 0038/0040 false-drift landmine); admitting ES_HISTORICAL to `METRIC_TYPES` would silently make the KUPIEC path accept an ES series (the kernel is method-agnostic — statistically meaningless; the tuple's own comment forbids completing the vocabulary without a ratified slice confronting pairing semantics, `events.py:117-125`); and an in-slice AS statistic needs its own model identity + registered threshold constants at the three-route verification bar + its OWN doctrine/READ-RULE text (the BT-2 appraisal-bias doctrine is scoped to the KUPIEC/BASEL verdict, yet Z2's X_t numerator consumes the identical DIETZ P&L — suppressed between marks, clustered on marks). An ES_HISTORICAL pin reaching the backtest binder today falls to the GENERIC "unknown VaR metric_type" refusal (:294) rather than a deliberate-scope-out like ES_PARAMETRIC's (:282-293) — ES-1's own review standard (distinguish unknown from ratified-excluded) requires the deliberate branch either way. **Domain (d), the surface:** the campaign PG suite pins EXACTLY 16 codes set-equal (+16 tier-events) and the multifamily suite pins EXACTLY 17 — the ONLY pin-safe demo slot is a NEW stage AFTER the HG-1 stage-3 suite, BEFORE the downgrade smoke (`ci.yml:299-322`; the HG-1 stage-3 precedent). Registration is per-tenant row-minting via the bootstrap register_* functions (dossier constants are code-side only) — a new registrar flips NOTHING by itself. A NEW code+version starts with zero validation rows: the MG-1/MF-1 precedents require SOME record for a genuinely-new code (the loadings INITIAL precedent; the naked use-before-validation default is the regime hole MF-1's V4 closed), while HG-1's OQ-5 forbids records "for nothing new" — ES-HS is genuinely new ⇒ an INITIAL dossier is the honest shape. The new-code dossier must satisfy the HG-1 conformance fence (its limitations tuple as a new searched set; each finding key matching exactly ONE row; no duplicate key-substring; zero 'FL-1' tokens) and the fail-loud lookup executes at every CI re-seed. `VAR_HS_LIMITATIONS[4]` ("ES … is a recorded seam for this family too; backtesting (Kupiec/traffic-light) is a recorded later slice…", `bootstrap.py:865-867`) goes DOUBLY stale at ship — the ES clause by this slice, and the backtesting clause has been false since BT-1 admitted VAR_HISTORICAL — and the row is KEY-FREE (the `risk.var.historical` resident keys live in rows [0]/[1]/[2]: "SPECIFIC/IDIOSYNCRATIC RISK = 0" / "Equal weighting reacts SLOWLY" / "worst scenario IN the window" — `bootstrap.py:855-861`, keys at `dossiers.py:271-275`; two verifiers independently corrected the census's own index list), so ONE key-safe reword fixes both clauses for NEW registrations (existing tenants keep old texts — immutable, refuse-not-skip). Count-bump surfaces at closeout: current_state (14→15 governed numbers), roadmap ledger, ENT-027 6th realization, REQ-MKT-001 narrowing to MC-only, the `var_parametric_es_v1.md:201-203` reservation discharged by a DATED AMENDMENT (immutable referent). **NO new permission (risk.run/risk.view reuse), NO new ENT (ENT-027 realization #6), RISK.VAR_CREATE stays reserved-not-emitted, `audit/service.py` FROZEN. The ONE migration is 0041 (CHECK widening only).** |
| **Mandate** | Roadmap Part 2.10 slice 2, sized **M** — the Wave-7 HEADLINE: converts the platform's tail measure from a unit conversion (k_c·σ over the SAME normality as the VaR it accompanies) into a REAL empirical number — the first tail statistic on the platform that can disagree with its VaR about the shape of the tail. Realizes `ES_HISTORICAL` over the shipped VAR-HS machinery; the ONE `0041` migration; the α-tail-mean estimator pre-pinned at ES-1 inherits; the Acerbi-Szekely backtest is the in-slice fork (OD-D). |

## Part 1 — Decisions at a glance (OD-ES-HS-1-A…G)

| OD | Decision |
|---|---|
| **A — The number: `ES_HISTORICAL` = the Acerbi-Tasche Prop. 4.1 empirical tail mean over the pinned HS scenario P&Ls; a sibling kernel function; ONE quantize; the same adequacy floor** | The metric: with the window's per-date P&L vector sorted ascending (`pnls[0]` worst — the shipped sort), a = 1−c, n = window_observations, m = ⌊n·a⌋, w = n·a − m (both EXACT Decimal — n·a never touches float, the `order_statistic_index` discipline), **ES = −( Σ_{i≤m} pnls[i−1] + w·pnls[m] ) / (n·a)** — the floor count + fractional boundary weight, the EXACT discrete α-tail-mean integral, NEVER the mean of the worst ⌈n·a⌉ (TCE, forbidden at three registered sites). m+1 ≤ n always (a<1) and the same strict floor N·(1−c)>1 (inherited UNCHANGED from VAR-HS: k≥2 ⇒ m≥1, so the tail spans ≥2 scenarios and the FRACTIONAL weight is exercised at the floor: N=21/c=0.95 ⇒ n·a=1.05, ES ≈ (L₍₁₎+0.05·L₍₂₎)/1.05). The whole computation runs inside the SAME prec-50 localcontext with ONE HALF_UP quantize to 6dp at the end (the never-quantize-intermediates discipline, `var_service.py:812-816` / `var_hs_kernel.py:111`). Implementation: a sibling function `compute_historical_es` in `var_hs_kernel.py` sharing the P&L-accumulation + sort with `compute_historical_var` (extract the shared private helper; the plain-VaR path stays BYTE-IDENTICAL — regression-pinned against the shipped HS goldens), returning `HsEsEstimate(n_observations, tail_floor_count, es_value)`. **The invariant ES ≥ VaR on the same window** (provable from the sorted order for every n·a; equality occurs at TIED tail scenarios — the worst m+1 P&Ls equal — as well as at 6dp-quantize ties; the verifier's executed counterexample: two tied worst P&Ls at 21/0.95 give ES_raw = VaR exactly) is asserted **`>=` at RAW precision** in the kernel suite — the raw values re-derived IN-TEST from the fixture (the kernel returns only the quantized value; HALF_UP quantize is monotone — verifier-executed 0/200,000 order inversions — so the quantized ordering inherits), with one LABELED tied-boundary fixture exercising the equality branch. The row: `var_value` = the ES (the VAR_HISTORICAL generic-by-metric_type precedent), NULL z_score/sigma/covariance_run_id — the exact HS NULL shape 0041 legalizes; NO new column, NO pin-serializer change (the frozen key set is None-tolerant since 0028). **The honesty gain over ES-1 is recorded**: unlike the parametric ES row (which reproduces only THROUGH its registered k_c), the ES-HS row is FULLY reproducible from its own pinned snapshot content + declared parameters — the pin holds every scenario input. The magnitude gate: the ES routes through the SAME committed-FAILED envelope as the HS VaR (never a PG overflow 500). |
| **B — The family: `risk.var.historical_es` v1 through the SAME HS binder via a registry map; declared identity = confidence/horizon/window + `estimator_convention`; VAR_HS_INPUT + builder reused byte-unchanged** | A NEW model code `risk.var.historical_es` (the `parametric`→`parametric_es` suffix precedent), fixed version label v1 (the VAR_HS fixed-label registrar shape — same-label different-declaration = 409 `ModelVersionConflictError`, never a 500). Declared parameters: `confidence_level` (the shared VAR_Z_SCORES vocabulary {0.9500, 0.9750, 0.9900} — vocabulary membership only; NO z arithmetic), `horizon_days=1` verbatim, `window_observations` under the SAME strict floor (registration AND bind-time, both — the POST /models arbitrary-assumption hole), and **`estimator_convention=TAIL_MEAN_ACERBI_TASCHE_P41`** — a NEW declared literal (the `quantile_convention='LOWER_ORDER_STATISTIC'` precedent): a different estimator (interpolated, simple-average, kernel-smoothed) is a NEW version by construction, never silent drift. NO es_multiplier and NO z_score declaration — there is NO registered constant in this family (the tail mean is empirical; the es_multiplier identity machinery is parametric-family machinery and does NOT transfer). Dispatch: `run_var_historical` gains a registry map `_HS_FAMILIES = {VAR_HS_MODEL_CODE: (es=False), ES_HS_MODEL_CODE: (es=True)}` resolved by the `_resolve_var_family` loop shape (first miss re-raises the plain code's refusal) — the single-code assert replaced, the plain path's behavior BYTE-PRESERVED. API: the ES-HS run enters through the EXISTING `POST /risk/vars-historical/runs` (the binder dispatches on the bound model — the ES-1 same-binder precedent); ONE new registrar endpoint `POST /risk/models/var-historical-es` mirroring the HS registrar's request shape (confidence/horizon/window; the estimator convention is REGISTRAR-STAMPED, not caller-suppliable). Snapshots: `PURPOSE_VAR_HS_INPUT` + `build_var_hs_snapshot` reused BYTE-UNCHANGED — **an adjudicated reuse**: the builder never reads the model registry and the pinned content is IDENTICAL for both families (the same exposure rows + the same aligned factor-return windows), so purpose-only symmetric consumption is safe (the reason the plain/total split needed predicates and this does not is RECORDED: different pinned content there, identical here). Reads: the existing GET var family, metric_type-generic (zero backend read changes); FE: zero changes (grounded). |
| **C — Migration 0041: drop + recreate `parametric_not_null` with the two-literal exemption; destructive RLS-safe downgrade; the non-superuser downgrade-path PG test closes the recorded 0028 gap** | Upgrade: `op.drop_constraint("parametric_not_null", "var_result", type_="check")` then `op.create_check_constraint("parametric_not_null", "var_result", "metric_type IN ('VAR_HISTORICAL', 'ES_HISTORICAL') OR (z_score IS NOT NULL AND sigma IS NOT NULL AND covariance_run_id IS NOT NULL)")` — the SAME short name (the deployed `ck_var_result_parametric_not_null` name is cited at 4 grep-verified sites and MUST survive; the IN-list is the natural reading and NO test greps the reflected constraint text — decided and pinned here: PG normalizes IN to `= ANY (ARRAY[…])` in `pg_get_constraintdef`, which nothing reads). Downgrade: the 0028 6-statement sandwich VERBATIM — drop the widened CHECK; `DISABLE TRIGGER var_result_append_only`; `DISABLE ROW LEVEL SECURITY`; `DELETE FROM var_result WHERE metric_type = 'ES_HISTORICAL'` (unrepresentable rows — the ratified 0028 destructive precedent EXTENDS, recorded not assumed); `ENABLE ROW LEVEL SECURITY`; `FORCE ROW LEVEL SECURITY` (BOTH restored — enable-without-force is a silent security regression); `ENABLE TRIGGER`; recreate the 0028-form CHECK (delete BEFORE re-add — PG validates existing rows at ADD CONSTRAINT). NO nullability changes in either direction (0041 is CHECK-only — the three columns stay nullable both ways). The ≤63-identifier import-time assert; the docstring acknowledges the pin-landmine does NOT trip (no column, no serializer key). **The test obligation, both new**: (1) a PG test inserting a real ES_HISTORICAL row (NULL trio intact) proving it SURVIVES the widened CHECK and that a non-exempt row still refuses (the ES-1 `test_var_total_pg.py:460-489` flavor — the ONLY tier that can see the CHECK); (2) a **non-superuser downgrade-path PG test with the verifier-settled mechanics**: the naive shapes both fail — a DML-grant role (the `irp_app` shape) dies "must be owner" on the FIRST `ALTER TABLE` (which is NOT the recorded gap), and the alembic CLI reconnects as the superuser `DATABASE_URL` user (re-masking the exact gap the test exists to close). The honest shape: a `NOSUPERUSER NOBYPASSRLS` login role **granted MEMBERSHIP in the table-owner role** (role ATTRIBUTES do not inherit through membership, but ownership checks pass via `has_privs_of_role` — the role can run the DDL while staying RLS-bound, the 0028:56-59 gap's exact geometry), driving the REAL `0041` migration module's `downgrade()` through an alembic `MigrationContext` bound to a connection opened AS that role; assert the ES_HISTORICAL row ACTUALLY deleted + the narrow CHECK re-added, **then restore the widened state before the test returns** (the whole body inside ONE transaction with assert-then-ROLLBACK — PG DDL is transactional — or the 0041 `upgrade()` re-run in a finally): the suite shares the CI job's DB with the stage-4 demo step that seeds real ES_HISTORICAL rows AFTER it, so an unrestored narrow CHECK turns CI red on the first run. The module's leg ordering is pinned state-self-sufficient. CI additionally exercises the real path every run: the stage-4 demo seeds ES_HISTORICAL rows and the downgrade smoke runs LAST (OD-F coupling). |
| **D — The fork: the Acerbi-Szekely backtest is TEED as BT-3 (named, prerequisite-paid, design-input recorded) — NOT shipped in-slice; the in-slice piece is the DELIBERATE refusal branch for ES_HISTORICAL** | **The recommendation is the tee, and the reasoning is structural, not effort-shy.** What shipping in-slice would actually require, census-grounded: (1) the pairing rework — two-series pinning is DOUBLE-blocked (the one-method gate AND the duplicate-window_end gate both refuse; pairing must be re-keyed by (metric_type, window_end)); (2) a NEW backtest model identity (the AS Z2 statistic is not Kupiec — new declared parameters, new decision vocabulary on ENT-055's columns) + the AS threshold constants (−0.70/−1.8) REGISTERED at the three-independent-routes verification bar; (3) its OWN doctrine referent — the BT-2 appraisal-bias READ RULE is scoped to the KUPIEC/BASEL verdict, yet Z2's numerator Σ X_t·I_t/ES_t consumes the IDENTICAL flow-adjusted DIETZ P&L (suppressed between marks, clustered on marks — the read rule must be re-derived for a tail-magnitude statistic, where clustering bites HARDER: one quarter-sized mark-day loss enters the sum divided by a 1-day ES); (4) schema: a paired-VaR column is barred TWICE (the one-migration envelope; the frozen pin key set). That is a full slice riding inside a slice — exactly the conflation BT-2's OD-F refused ("burying one methodology decision inside another"), and the register's answer is a NAMED tee with teeth, not silence. **BT-3 is minted as the named candidate** carrying: the AS material at its HONEST verification grades (the Z2 formula + the paired-data requirement VERIFIED via the M-P verbatim reproduction + the identity — that part of the fetch obligation is PAID; **Z1's structure verified but its exact transcription is VENDOR-NORMALIZED, and the −0.70/−1.8 threshold VALUES rest on M-P's attribution alone** — BOTH must be re-verified against the primary or a second independent source BEFORE registration at BT-3's three-route constant bar; the tee records exactly what remains); TIPPED Christoffersen (the Wave-6 close re-tee lands here — its final home); and **the pairing design input the census settled**: pair the VaR-HS and ES-HS runs by IDENTICAL `input_snapshot_id` (two runs bound to the SAME pinned snapshot see the SAME scenario set by construction — the coherent (VaR_t, ES_t) pair exists without any schema change; recorded so BT-3 does not re-census). ES-1's shipped OD-F text records that a genuine ES backtest "earns its place" when the non-elliptical ES-over-HS leg exists (the `events.py:122-123` METRIC_TYPES comment; the record's OD-F row says "becomes meaningful… a BT-3-adjacent candidate") — that leg is THIS slice, and the tee is how it earns the place: the ES_t series EXISTS after ES-HS-1; BT-3 needs only the statistic. **The in-slice piece**: the backtest binder gains the DELIBERATE refusal branch for `ES_HISTORICAL` (mirroring ES_PARAMETRIC's, message pointing at the BT-3 tee) — ES-1's own review standard: a ratified exclusion must never present as an unknown vocabulary miss. `METRIC_TYPES` is NOT touched. |
| **E — Constants + docs: the new ES-HS dossier sets (fence-compliant, key-unique, zero 'FL-1'); the ONE key-free reword of `VAR_HS_LIMITATIONS[4]` fixing BOTH stale clauses; the dated ES-1 referent amendment; the new referent with the verified-via-reproduction citation discipline** | New `ES_HS_ASSUMPTIONS_BASE` + `ES_HS_LIMITATIONS` in bootstrap.py, drafted fresh under the HG-1 fence rules (each dossier finding key matches exactly ONE row in the new searched set; no duplicate key-substrings; 'FL-1' appears in ZERO new texts). The limitation content, honest by design: (i) the tail-mass honesty row — at the adequacy floor the tail spans barely more than ONE scenario (n·a=1.05 at 21/0.95: **≈95% of the estimate's WEIGHT sits on the single worst scenario** — a weight claim, not a value claim (at tied tails the ES equals the worst scenario exactly); the floor is a statistical MINIMUM and the effective tail mass is n·a scenario-equivalents — window size is the lever); (ii) the window-bound row — the ES cannot exceed the worst scenario IN the pinned window (the tail-mean inherits the HS regime-blindness with SHARPER teeth: the statistic lives entirely in the window's extreme tail); (iii) specific-risk = 0 (the factor-substrate inheritance, framed per the post-HG-1 wording discipline); (iv) the equal-weight/slow-reaction inheritance; (v) the validation_status row (the standing VW-1/MG-1 enforcement framing); (vi) the no-backtest row NAMING the BT-3 tee (the OD-D message discipline). **`VAR_HS_LIMITATIONS[4]` is reworded ONCE** — the row is KEY-FREE (grounded) and doubly stale: the ES clause ("a recorded seam") is discharged BY this slice, and the backtesting clause ("a recorded later slice") has been FALSE since BT-1 admitted VAR_HISTORICAL to `METRIC_TYPES` — the new text states ES is REALIZED as the sibling `risk.var.historical_es` family, Kupiec/Basel backtesting of THIS family is LIVE (BT-1), and Monte-Carlo remains gated (the surviving TRUE clause). Existing tenants keep old texts (immutable, refuse-not-skip) — behavioral for NEW registrations only. Docs: NEW referent `05_analytics_methodologies/var_historical_es_v1.md` (the var_parametric_es_v1 template: convention pinned → estimator → why-honestly → no-backtest-with-the-tee → shape → limitations; the rule-6 external-benchmark section carries the **verified-via-reproduction disposition at its HONEST per-source grades** — the primary GATED; **the Z2 formula verified via ONE verbatim reproduction** (M-P Eq. 6.2) + the null-expectation identity + the vendor docs; **Kratz-Lok-McNeil grounds ONLY the significance procedure and the elicitability posture** (it reproduces no formula); **Z1's structure verified but its exact transcription is VENDOR-NORMALIZED** (from a render the note itself records as +1-corrupted — flagged for re-verification at BT-3); both extractions are arXiv texts (no journal-publication claim made); the sign-convention disclosure (Part 2) carried into the referent — the honest citation shape when a primary is paywalled, disclosed rather than laundered; the TCE-divergence magnitude stated as "order 10% (up to ~14% depending on the tail shape) at n=41", never the bare ~14% as a reproducible fact and never test-pinned — the verifier's executed constructions span −7.5%..−11.9% with the DIRECTION uniform); a DATED AMENDMENT to `var_parametric_es_v1.md` discharging its :201-203 reservation (the immutable-referent discipline — amend, never edit); the REQ-MKT-001 RTM/backbone rows narrow "ES-over-HS/MC open" to MC-only at closeout. |
| **F — The demo: stage 4 of the living tenant — the 18th code, its flagship-style INITIAL dossier (tier table for ratification), the pin-safe CI slot, and the downgrade-smoke coupling** | A NEW stage-4 module (`demo/eshs_stage4.py` + `scripts/run_demo_eshs.py` CLI shim, the stage-3 shape): registers `risk.var.historical_es` v1 for the demo tenant (the 18th registered code — registration is per-tenant row-minting; the code-side registrar alone flips nothing), builds a fresh `VAR_HS_INPUT` snapshot over the campaign's existing governed exposure substrate (same window/confidence as the flagship HS run: 21/0.95 — the fractional weight EXERCISED at n·a=1.05), runs the ES-HS number, and files the **INITIAL validation dossier** (AWC — the MF-1 loadings-code precedent: a genuinely NEW code requires SOME record, and HG-1's OQ-5 false-ceremony bar does not apply because everything here IS new; evidence = this model's OWN COMPLETED run, the MG-1 evidence-runs shape; finding keys drawn from the NEW registered limitation rows, fail-loud at every CI re-seed). **The tier table for ratification (OQ-5): materiality HIGH / complexity MEDIUM — deriving TIER_1 under the ratified MG-1 matrix** (HIGH materiality ⇒ TIER_1 regardless of complexity, `model/models.py:131-144` — stated so the ratings are ratified WITH their consequence: the strictest 365-day cadence ceiling lands on the 18th code): the parametric-ES twin's exact rating shape (`dossiers.py:93-99`): flagship tail-severity number on the flagship book (HIGH); the HS family's mechanics + an empirical tail mean with NO estimation chain and NO registered constant (MEDIUM). The filing mechanics follow MF-1 V8: **tier-assign via the 2L verb BEFORE filing**, and the INITIAL's `next_review_due` = filing-day + 365 (an approving outcome REFUSES without one, and TIER_1's ceiling is strict — `validation.py:181-185/:284-302`). Runner guards: refuse-not-skip both directions on its OWN footprint (the probe = the ES-HS model code registration, minted FIRST inside the single commit); requires the campaign (stage-1 models present). CI: the stage-4 suite slots AFTER the stage-3 suite, BEFORE the downgrade smoke (the ONLY pin-safe slot in the CI step order — the campaign suite pins 16 set-equal, the multifamily suite pins 17; each suite asserts at its own point in the shared-DB order). **The ordering discipline gains a FILENAME leg (the verifier's catch)**: a single-invocation local full-PG run collects ALPHABETICALLY, so the suites are named **`test_demo_stage4_eshs.py` / `test_demo_stage4_eshs_pg.py`** ('s' sorts after 'h' and 'm' — a `test_demo_eshs*` name would run BEFORE the hg1 and multifamily suites locally, seed the 18th code early, and break the multifamily EXACTLY-17 pin on every local battery; the constraint is recorded beside the CI ordering comment). Placement mechanics, both verifier-settled: the stage-4 `TierDossier` is a SEPARATE module constant (the `MF1_LOADINGS_TIER` shape) and MUST NOT join `TIER_DOSSIERS` (the campaign suite derives its 16-pin from that dict itself); the fail-loud key lookup is NOT shared machinery — the stage-4 module carries its OWN `_findings_from_registry` copy (the multifamily duplication precedent, `multifamily.py:335`). **The coupling is a feature**: stage 4 seeds durable ES_HISTORICAL rows, so CI's `alembic downgrade base` exercises 0041's destructive delete against REAL rows on EVERY run (with the OD-C non-superuser PG test covering the RLS-safe path the superuser smoke cannot). The stage-4 conformance test mirrors the HG-1 fence for the NEW searched set (own test file; the existing `_SEARCHED_SETS` 6-code map is untouched — no existing pin moves). |
| **G — The scope fence** | **ONE migration (0041, CHECK-widening only — head `0040`→`0041`); NO new permission** (risk.run/risk.view reuse — the ES-1 precedent); **NO new ENT** (ENT-027 realization #6 on `var_result`); RISK.VAR_CREATE stays reserved-not-emitted; **`audit/service.py` FROZEN**; the pin serializer BYTE-UNTOUCHED (no new key — the frozen-set test keeps passing); `METRIC_TYPES` BYTE-UNTOUCHED (OD-D adds only the refusal branch); the plain-HS family behavior BYTE-PRESERVED (the shipped HS goldens re-run as regressions; the kernel refactor extracts a shared helper without moving a digit); the parametric families byte-untouched; allocation/proxy/loadings/scenario/active-risk/backtest binders untouched except the ONE refusal branch; FE zero changes; the campaign + multifamily + stage-3 demo modules byte-untouched (stage 4 is ADDITIVE). The API surface: ONE new registrar endpoint + the existing run/read endpoints. 4-finder review (adversarial on the binder dispatch + the migration downgrade; numeric on the kernel — hand + float cross-check @1e-9, the fractional weight exercised at BOTH boundaries incl. integer n·a where w=0; doctrine on the new dossier texts vs the fence + the citation dispositions; scope-fence). |

## Part 2 — External benchmarks and primary sources (roadmap Part 4 rule 6)

**Acerbi-Szekely 2014 ("Back-testing Expected Shortfall", Risk 27(11) 76–81; the MSCI Research Insight, Dec 2014) — VERIFIED-VIA-REPRODUCTION 2026-07-17.** The primary is GATED at both hosts (risk.net paywall; MSCI form-gate). The verification route, disclosed: **(1) Moldenhauer-Pitera (arXiv:1709.01337v3, p.15 Eq. 6.2)** reproduces the Test-2 statistic VERBATIM with attribution ("the test statistic given in Equation (6) in Acerbi & Szekely (2014)"): **Z2 = ( (1/T)·Σ_t X_t·1{X_t + VaR_t < 0} / (α·ES_t) ) + 1** — the +1 OUTSIDE the sum; the null-expectation identity independently confirms the shape (under a correct model E[X_t·I_t] = −α·ES_t ⇒ the sum → −1 ⇒ E[Z2] = 0; negative Z2 = underestimation, one-sided). M-P also carry the AS traffic-light thresholds ("Following authors' suggestion we have set values 0.7 and 1.8" in their FLIPPED sign convention — AS convention: **−0.70 / −1.8**) and the fork-deciding data requirement VERBATIM: "Test 2 framework require IM methodology for both VaR and ES methodologies". **(2) Kratz-Lok-McNeil (arXiv:1611.04851)** confirms the significance procedure ("suggest the use of Monte Carlo hypothesis tests") and the elicitability posture (model comparison ≠ model testing; the emerging consensus paragraph). **(3) The MathWorks Risk Toolbox implementation docs** confirm Test 1 (conditional): Z1 tested conditional on breaches, E[Z1|N_breach>0]=0, UNDEFINED (auto-accept) at zero breaches, two-part (the VaR count test must ALSO pass) — and the table-based no-simulation variant: "The critical values for the unconditional test statistic are stable across a range of distributions, which is the basis for the table-based tests" (presimulated under normal and t(ν=3)) — the stability fact that makes FIXED registered thresholds the conforming house shape (the CHI2_1DF_CRITICALS precedent) if/when BT-3 ships. A parse caveat is recorded: the fetched MathWorks HTML transcriptions mis-attached the "+1" (a rendered-math artifact); the M-P verbatim reproduction + the identity govern — **which grades Z1's exact transcription VENDOR-NORMALIZED, not reproduced** (the structure — conditional-on-breaches, E=0 under the null, undefined at zero breaches, two-part — is grounded; the character-exact formula is not; flagged for re-verification at BT-3). **A sign-convention disclosure the referent carries verbatim**: M-P p.15's PROSE describes Z in their flipped convention ("non-positive under the null") while the DISPLAYED Eq. 6.2 is in AS's — the same page is internally sign-inconsistent; this record's transcription follows the EQUATION, with the direction derived from the formula + the null-expectation identity, not from the prose (the identity alone cannot distinguish Z2 from −Z2 — the assignment is the equation's). The −0.70/−1.8 threshold VALUES rest on M-P's attribution ("Following authors' suggestion we have set values 0.7 and 1.8" — ambiguous between AS suggesting the VALUES vs the traffic-light SCHEME) and are carried at that grade — NOT registrable constants until re-verified (OD-D). **Negative check**: no claim in this record rests on the primary's unfetched pages; every AS fact cited here traces to one of the three named sources at the grade stated. Fissler-Ziegel 2016 joint elicitability confirmed via Nolde-Ziegel (arXiv:1608.05498 p.4).

**Acerbi-Tasche 2002 (Prop. 4.1, the discrete α-tail-mean)** — VERIFIED-carried from ES-1 (the estimator was extraction-verified there; the three registered in-code sites quote it). **BCBS d457/FRTB** — VERIFIED-carried (ES-1/BT-2): MAR33 97.5% ES is the capital measure while backtesting remains VaR-based (MAR32.4/32.5/32.18) — the OD-D tee's regulatory footing. **Geltner/GLM/Okunev-White** — not touched by this slice (DS-2's obligation, unchanged).

**HS-ES literature disposition** (why Prop. 4.1 and not an alternative empirical estimator): the interpolated/kernel-smoothed tail estimators are DIFFERENT estimator conventions — admissible later as NEW declared `estimator_convention` literals (new versions by construction, OD-B), never silent replacements; the platform ships the exact empirical integral first (the ES-1 ratified convention, and the estimator AS 2014's own Z-statistics assume).

## Part 3 — Census key facts (the full 91-claim census lives in the planning workflow journal; the load-bearing subset)

1. The HS kernel sorts and selects but does NOT expose the sorted vector (`var_hs_kernel.py:41-48/:109-112`) — the ES needs a sibling function; the shared accumulation+sort is extracted, the VaR path byte-preserved.
2. `order_statistic_index` does n·(1−c) in EXACT Decimal (`:51-65`) — the n·a/m/w arithmetic in OD-A follows the same no-float discipline.
3. The estimator mandate is registered at `es_kernel.py:21-34`, `bootstrap.py:628-633` + `:1867-1876` (ES_LIMITATIONS[3] with the ~14%-at-n=41 teeth), and ratified at `es_1_decision_record.md:13`.
4. ES-1 Part 3 item 4 (`es_1_decision_record.md:45`) pre-records this slice's migration; `var_parametric_es_v1.md:201-203` reserves the leg with the same note.
5. The 0028 CHECK text + its migration-only/ORM-invisible status: `0028_var_historical.py:43-48`; `models.py:240-241`; SQLite `create_all` blindness `conftest.py:27-32`; the ES-1 PG-test precedent docstring `test_var_total_pg.py:460-465`.
6. The 0028 downgrade sandwich (the 0041 template): `0028:51-75` — trigger + FORCE RLS transactionally disabled around the DELETE; delete before re-add; BOTH enable+force restored.
7. NO other metric_type-bearing DDL on var_result (executed grep over migrations + ORM); the grain UNIQUE admits the row; 0038/0040 columns don't interact.
8. The pin key set is FROZEN and None-tolerant (`serialize.py:468-512`; `test_var_total.py:1718`) — no serializer change, no new key, no false drift.
9. The HS binder is single-family with hardcoded metric/NULLs (`var_hs_service.py:291-296/:352-381`); the registry-map dispatch precedents: `var_service.py:569-604`, `factor_service.py:115-136`.
10. `build_var_hs_snapshot` is model-registry-free; pinned content identical across the two HS families (`snapshot/service.py:2566-2659`) — the purpose-only reuse adjudication in OD-B.
11. The extra-row-on-existing-runs shortcut is DISQUALIFIED at HEAD (the builder pins every row, no metric filter `snapshot/service.py:1693-1709/:1775-1776`; the binder refuses mixed methods `var_backtest_service.py:275-280` AND duplicate window_end `:313-317`) — the ES-1 OD-C force re-verified.
12. An ES_HISTORICAL pin today falls to the GENERIC unknown-metric refusal (`:294`), not a deliberate one (`:282-293` is ES_PARAMETRIC-only) — the OD-D in-slice branch.
13. `METRIC_TYPES` + its completing-the-vocabulary discipline note: `events.py:105-130`.
14. The AS-threshold registered-constant shape precedent (declared assumption + exact-equality identity check + three-route verification): `bootstrap.py:1904-1930/:635-640`; `CHI2_1DF_CRITICALS` `var_backtest_kernel.py:31-36`; QS-18 seeded-MC exists on paper but has ZERO shipped risk precedent and would fight TR-09 re-run invariance (`numerical_quant_standards.md:88-90`; `var_backtesting_v1.md:115-116`).
15. The BT-2 READ RULE is scoped to the KUPIEC/BASEL verdict (`var_backtesting_v1.md:53-77`) — an AS statistic needs its OWN doctrine text (OD-D tee content).
16. Christoffersen's current tee state: BT-2 OD-F → BT-3; the Wave-6 close re-tee → "rides the ES-HS-1 planning fork" (`bt_2_decision_record.md:18`; `wave_6_close_review.md:36`; roadmap `:164/:248`) — OD-D lands it in BT-3 FINALLY.
17. Campaign suite pins 16 set-equal + 16 tier-events (`test_demo_campaign_pg.py:49-53/:135/:156`); multifamily pins exactly 17 (`test_demo_multifamily_pg.py:148-149`); the hg1 suite pins run counts only — the stage-4 slot is the only pin-safe one (`ci.yml:299-322`).
18. Registration is per-tenant row-minting via bootstrap register_* calls (`campaign.py:485-571`); dossier constants are code-side — the new registrar alone flips nothing.
19. The validation-record posture for a NEW code: MF-1 loadings INITIAL (the criterion = the ratified dossier table as the human judgment); the naked use-before-validation default is the MF-1 V4 regime hole; HG-1 OQ-5 bars records for nothing-new — ES-HS is genuinely new (OD-F).
20. The HG-1 conformance fence mechanics + the fail-loud key lookup (`test_demo_hg1.py:306-354`; `campaign.py:843-863`); the new searched set gets its OWN mirror test (no existing pin moves).
21. `VAR_HS_LIMITATIONS[4]` is key-free (resident keys in rows [0]/[1]/[2] — `bootstrap.py:855-861`, `dossiers.py:271-275`) and DOUBLY stale (the backtesting clause false since BT-1) — the OD-E one-row reword.
22. FE + reads are metric_type-generic (`risk.py:1489-1515`; `RunDetail.tsx:10-28`; `types.ts:45-53/:152-165`; `queries.py:30-41` — run family VAR needs no vocabulary change).
23. Count-bump surfaces at closeout: `current_state.md` (14→15), roadmap ledger, `canonical_data_model_standard.md:119` (realization #6), RTM `:50` + backbone `:166` (narrow to MC-only).
24. HS window floor + vocabulary: the floor function is confidence-generic (`bootstrap.py:918-929`); 0.9750 is registrable ("verified, not exercised" `:606-609`) — the ES-HS fixtures exercise fractional n·a DELIBERATELY (the boundary where the wrong estimator diverges most).

## Part 4 — Open decisions (OQ-ES-HS-1-1…7) — RATIFIED 2026-07-17 (user: "Approve all")

- **OQ-ES-HS-1-1 — OD-A: the number** (Prop-4.1 empirical tail mean; sibling kernel function with the shared helper extracted and the VaR path byte-preserved; exact-Decimal n·a/m/w; ONE quantize; the same strict floor; ES≥VaR asserted raw; var_value=ES + the HS NULL trio; NO new column; the full-pin-reproducibility honesty note). *Recommend APPROVE.*
- **OQ-ES-HS-1-2 — OD-B: the family** (`risk.var.historical_es` v1; registry-map dispatch in the HS binder; declared identity = confidence/horizon/window + registrar-stamped `estimator_convention=TAIL_MEAN_ACERBI_TASCHE_P41`; NO es_multiplier/z declarations; `POST /vars-historical/runs` reuse + ONE new registrar endpoint; VAR_HS_INPUT + builder reused byte-unchanged with the identical-content adjudication recorded). *Recommend APPROVE.*
- **OQ-ES-HS-1-3 — OD-C: migration 0041** (drop+recreate under the SAME short name; the IN-list form pinned; the 0028 downgrade sandwich verbatim with the destructive-delete precedent EXTENDED-and-recorded; no nullability changes; the two new PG tests incl. the NON-superuser downgrade path closing the recorded 0028:59 gap). *Recommend APPROVE.*
- **OQ-ES-HS-1-4 — OD-D: the fork — TEE the Acerbi-Szekely backtest as BT-3** (named candidate carrying the AS material at its honest grades — Z2 formula + paired-data requirement VERIFIED; Z1 transcription vendor-normalized and the −0.70/−1.8 values single-attributed, BOTH re-verified before BT-3 registers anything — plus TIPPED Christoffersen finally homed, and the shared-`input_snapshot_id` pairing design input; in-slice = ONLY the deliberate refusal branch for ES_HISTORICAL pointing at the tee; `METRIC_TYPES` untouched). **This is the Tier-3 fork the roadmap left open.** *Recommend APPROVE the tee — the in-slice alternative is a full backtest-methodology slice (new model identity + registered thresholds + its own appraisal-bias doctrine + a pairing rework of two binder gates) buried inside the headline slice, the exact conflation BT-2's OD-F refused; the tee pays the fetch obligation NOW so BT-3 starts formula-verified.* If you want the AS backtest shipped in Wave 7 regardless, say so and BT-3 becomes a NAMED WAVE-7 INSERTION after RS-1 rather than a rider candidate — the record will carry that sequencing consequence explicitly.
- **OQ-ES-HS-1-5 — OD-F: the demo stage 4 + the tier table** (the 18th code; INITIAL AWC dossier with evidence = its own run; **materiality HIGH / complexity MEDIUM** — the parametric-ES twin shape; the pin-safe CI slot; the downgrade-smoke coupling as a feature). *Recommend APPROVE.*
- **OQ-ES-HS-1-6 — OD-E: constants + docs** (the new fence-compliant dossier sets with the tail-mass honesty row; the ONE key-free `VAR_HS_LIMITATIONS[4]` reword fixing BOTH stale clauses; the dated `var_parametric_es_v1.md` amendment; the new referent carrying the verified-via-reproduction citation disposition PLAINLY). *Recommend APPROVE.*
- **OQ-ES-HS-1-7 — OD-G: the scope fence** (ONE migration; no permission/ENT/audit mints; pin serializer + METRIC_TYPES + plain-HS behavior + FE all byte-untouched/preserved; the 4-finder review composition as tabled). *Recommend APPROVE.*

## Part 5.5 — Implementation deviations from the ratified plan

1. **Stage 4 consumes the flagship HS run's EXISTING snapshot rather than building a fresh
   one** (the ratified OD-F text said "builds a fresh `VAR_HS_INPUT` snapshot"; the shipped
   `eshs_stage4.py` binds `snapshot_id=hs_row.input_snapshot_id`). DELIBERATE: the
   consume-existing shape demonstrates the OD-D BT-3 shared-`input_snapshot_id` pairing LIVE
   in the living tenant (the design input the tee records), and the dossier scope truthfully
   describes what shipped; the build path is exercised by the unit/PG suites. Adjudicated
   here, not silently (the review's DOC-1).
2. **The seeded demo window's worst two scenarios TIE, so the flagship (VaR, ES) pair is
   EXACTLY EQUAL** (113.239146 both) — the fractional boundary weight is APPLIED but
   numerically invisible in the demo pair; the tied-tail equality is itself the recorded
   equality case, now DISCLOSED in the stage-4 docstring + the dossier scope note (the
   review's NUM-2). The weight's numeric effect is pinned by the kernel suite's untied
   fixtures (490.476190 ≠ the 400 TCE).
3. **The "understates at every fractional n·a" TCE claim was softened to the WEAK form at
   all three new sites** (never exceeds; strictly below at every fractional n·a with an
   UNTIED (m+1)-boundary; equal at fully-tied tails) — the review's executed algebra (426
   equality hits in 2,400 fuzz cases, all tied/m=0; the demo tenant itself is a live
   counterexample to the strict form). The SAME imprecision survives verbatim in ES-1's
   shipped texts (`es_kernel.py:32`, the `VAR_ES_MULTIPLIERS` comment) — recorded as a
   NAMED text-touch candidate for the next slice that re-registers those constants, not
   edited here (out of the ratified scope fence).
4. **The DQ rule_code is SHARED across the two HS families** (`risk.var_hs.completeness` —
   the binder's scaffold parameter, method-agnostic envelope sanity). The plan did not pin a
   per-family rule; recorded as the shipped choice.
5. Recorded-not-changed residuals (the review): the prec-50 accumulation is exact only while
   single-scenario partial sums stay below ~1E32 (true for any portfolio with < ~140
   near-envelope-max factors — the declared-regime posture, precedented); the registrar-leg
   endpoint reuses the pre-existing `model.inventory.register` permission alongside
   `risk.run`/`risk.view` (the fence's enumeration was short one member; NO mint — the
   review's FEN-1).

## Part 6 — Implementation review dispositions (the ratified 4-finder composition)

**8 findings: 0 HIGH, 3 MEDIUM, 5 LOW — ALL folded or recorded above; ZERO shipped math
defects.** The numeric finder re-derived every golden byte-exact in independent
exact-rational (Fraction) arithmetic and fuzzed the kernel against an independent reference
over 2,400+ windows (all vocabulary confidences, both n·a shapes, tied tails, all-gains,
near-envelope magnitudes, n to 5,000): ZERO value deviations, ZERO ES<VaR violations raw or
quantized; the living tenant's stage-4 ES recomputed independently from its own pinned
snapshot content is byte-equal to the stored row. The adversarial finder executed 11
generic-mint/twin attacks (all refused fail-closed), ran the REAL 0041 downgrade()+upgrade()
in a rollback-wrapped transaction against live PG (exactly the ES rows deleted; the
constraint restored byte-identical), and confirmed the refusal precedence matches ES-1's.
The scope-fence finder executed every OD-G clause: the fence holds intact.

| F | Finding | Disposition |
|---|---|---|
| F1 | MEDIUM (adversarial): the ES-HS family shipped without the generic-mint BIND-refusal test the plain-HS family carries (the behavior is correct — all 11 executed attacks refuse — but a future parse edit could silently open the floor/estimator surface with the battery green) | FOLDED: `test_es_hs_generic_mint_declared_parse_and_twin_refusals` added (8 malformed-declaration bind refusals + the squatted-twin registrar refusal) |
| F2 | MEDIUM (doctrine): the ratified RAW-precision ES≥VaR assertion did not ship — the grid test compared only quantized outputs and its docstring overclaimed | FOLDED: the raw comparison added to the grid loop (both sides unquantized at prec-50, re-derived from the fixture independent of the kernel's quantize); docstring corrected |
| F3 | MEDIUM (doctrine): the stage-4 snapshot deviation was unrecorded | FOLDED: Part 5.5 item 1 |
| F4 | LOW ×2 (numeric+doctrine convergence): the strict TCE-understatement claim is false at fully-tied tails (the demo tenant is a live counterexample) | FOLDED: all three new sites softened to the weak form (Part 5.5 item 3); the ES-1 carried imprecision NAMED, not touched |
| F5 | LOW (numeric): the flagship demo pair ties exactly (ES = VaR) — the fractional weight numerically invisible there | FOLDED: disclosed in the stage-4 docstring + dossier scope note (Part 5.5 item 2) |
| F6 | LOW (numeric): a pathological prec-50 last-digit flip exists beyond ~1E32 single-scenario partial sums — the declared-regime posture, precedented | RECORDED (Part 5.5 item 5), no change |
| F7 | LOW (fence): the permission-reuse enumeration was short `model.inventory.register` | RECORDED (Part 5.5 item 5); the NO-mint invariant holds |

## Part 5 — Implementation readiness gate

Implementation follows `es_hs_1_implementation_plan.md` on ratification. Merge preconditions: the 4-finder review folded; `make check`; full local-PG fresh incl. the four demo suites in CI order + the two new 0041 PG tests; `alembic check` clean with the ONE new revision; downgrade smoke (now exercising the destructive delete against stage-4 rows); CI green. Closure per the OQ-W5C-5 checklist incl. grep-for-"pending".

### Planning verification (the standing pre-ratification pass)

A 4-lens adversarial verifier workflow ran over this draft BEFORE the OQ gate was briefed (mechanism / numeric / doctrine+citations / census-conformance+scope; 22 findings: 1 HIGH, 7 MEDIUM, 14 LOW — ALL folded into the text above before ratification). The numeric core survived every executed attack (the estimator's index mapping proven EXACTLY Prop 4.1 — max relative deviation vs an independent float implementation 2.5e-14 over 4,000 random windows; m≥1 and m+1≤n exhaustively verified n=2..2999 across the vocabulary; ES≥VaR held in 4,000/4,000 trials and is theorem-provable; the Decimal discipline airtight — 1−c and n·a exact for every vocabulary confidence; ~30 spot-checked citations exact at HEAD save one index list).

| V | Finding | Disposition |
|---|---|---|
| V1 | **HIGH (doctrine): "the formulas verified through TWO independent peer-reviewed reproductions" was FALSE** — only M-P reproduces a formula (Z2 only); KLM grounds procedure/elicitability only; Z1 is vendor-normalized from a render the note itself records as corrupted; "peer-reviewed" unproven (arXiv texts) | FOLDED: OD-E + Part 2 now carry per-source grades; Z1 + the threshold values flagged for BT-3 re-verification |
| V2 | MEDIUM ×2 (mech+conform convergence): the non-superuser downgrade test was NOT EXECUTABLE as drafted — the DDL needs table OWNERSHIP (a DML-grant role dies "must be owner", which is NOT the recorded gap; the alembic CLI reconnects as the superuser, re-masking it) — and the body leaves the NARROW CHECK live in the shared DB, turning CI red at the stage-4 step | FOLDED: OD-C now specifies the owner-via-membership role shape (`has_privs_of_role`; attributes don't inherit), `MigrationContext` driving the real `downgrade()`, and the transactional assert-then-restore |
| V3 | MEDIUM (conform): the ALPHA-ORDER trap — `test_demo_eshs*` sorts BEFORE the hg1/multifamily suites, so a single-invocation local battery seeds the 18th code early and breaks the multifamily EXACTLY-17 pin | FOLDED: suites named `test_demo_stage4_eshs*`; the ordering discipline's new FILENAME leg recorded |
| V4 | MEDIUM (doctrine): the derived TIER was unstated — HIGH materiality ⇒ TIER_1 under the MG-1 matrix (365-day ceiling), and the INITIAL's `next_review_due` was unspecified (an approving outcome REFUSES without one) | FOLDED: OD-F/OQ-5 state TIER_1 + filing-day+365 + the tier-assign-before-filing ordering |
| V5 | MEDIUM ×2 (numeric+mech convergence): the ES=VaR raw-equality parenthetical was FALSE (tied tail scenarios give raw equality — executed counterexample); the plan's magnitude-gate mechanism was WRONG (InvalidOperation is an unhandled 500, not the FAILED gate — the prec-50 context makes it unreachable and the gate is the binder's abs()≥1E22 comparison) | BOTH FOLDED (record + plan) |
| V6 | MEDIUM (doctrine): OD-D's "the fetch obligation does not recur at BT-3" contradicted its own three-route registration bar for single-attributed thresholds | FOLDED: the tee now records exactly what remains to verify |
| V7 | LOWs (all folded): the run-endpoint/module docstrings ship false contracts if byte-untouched ("no functional change" wording + docstring updates added); refusal-PRECEDENCE unpinned across the kernel refactor (precedence test added); the ~14% TCE magnitude is fixture-dependent (graded "order 10%, up to ~14%"); the ≈95% weight-vs-value conflation; the raw-assertion re-derivation mechanics; the dossier key row indices [0]/[2]/[3]→[0]/[1]/[2] (two verifiers independently); the "earns its place" quote re-pointed to `events.py:122-123`; the M-P sign-inconsistency disclosure added; the stage-4 TierDossier as a SEPARATE constant (never in `TIER_DOSSIERS`); the stage-4 module's own `_findings_from_registry` copy | ALL FOLDED |

Verdict: ratifiable after the folds above (all applied in this text before the OQ gate was briefed). What the pass failed to break, on the record: the Z2 transcription, the dispatch/registrar/migration mechanics, the double-block on paired series, the pin-safety analysis (as corrected), the FE-zero-changes claim, and the OD-D tee's consistency with the ratified fork wording.
