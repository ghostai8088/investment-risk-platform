# Delivery Roadmap (rolling-wave)

| Field | Value |
|---|---|
| Status | **RATIFIED by the user (2026-07-08: "Proceed" on the full package — Wave 1 order + the Part 4 re-sequencing rules — after a plain-language briefing incl. the documentation-alignment audit)** |
| Date | 2026-07-08 |
| Purpose | The operative near-term slice sequence + the re-baselining rules, so slice-boundary "what next?" decisions stop being ad-hoc menus (user direction 2026-07-08: a plan to avoid drift and rabbit holes, explicitly NOT immutable). |
| Relationship | `build_sequence.md` = the ratified RTM **theme/phase map** (what the platform must eventually contain, and roughly in what order — unchanged). `docs/project_memory/build_plan.md` = the **executed ledger** (what has shipped). THIS document = the **operative sequence** for the next wave of slices. The per-slice discipline (decision record + plan + OQ ratification + adversarial review, then commit+push under the 2026-07-12 delivery-autonomy grant → the USER merges the PR) is UNCHANGED by this document. |
| Basis | All P0.5–P3 records; the consolidated deferral registers; the FE-1 walking-skeleton direction; the user's standing preferences (plain-language gate briefings; objective recommendations; ask on ambiguity with a recommendation attached). |

---

## Part 1 — Principle: rolling-wave, not big-bang planning

High fidelity for the NEAR term (the ratified Wave 1 below — named slices, order, rationale); deliberately COARSE
beyond it (themes only). Detail is added when a wave closes, not before — a detailed twelve-month software plan is
fiction, and treating fiction as commitment produces its own drift (building to the plan after the facts have
changed). Phase boundaries were always revisit-able (`build_sequence.md` §7 has said so since genesis); this
document makes the revisiting DISCIPLINED (Part 4) instead of a menu at every slice boundary.

**Numbering note (drift, now reconciled):** the executed phases (P0.5, P1A/B/C, P2, P3…) diverged from
`build_sequence.md`'s RTM phase numbers early (the RTM map's P2 theme "public market data + market-risk core" was
executed as OUR P2+P3; the RTM's P5 stress theme is why stress carries the "RTM-P5" caveat). Cross-references
below always name which numbering they use.

## Part 2 — Wave 1: the ratified near-term sequence

Each slice still gets its own decision record + plan + gates; this table fixes ORDER and INTENT only.

| # | Slice | What / why | Size | Dependencies |
|---|---|---|---|---|
| 1 | **TC-1 — FE toolchain bump** ✅ **DONE** (`c34b346`, CI #112) | vite 5→current + vitest 2→current majors (closes the recorded dev-only advisory chain), CI Node alignment, + a **production-deps `npm audit` CI step** (runtime supply-chain issues turn CI red on their own). Mostly mechanical; do it while the frontend context is fresh. Keep-Vite/Vitest decision already accepted by the user (2026-07-08). | S | none |
| 2 | **VAR-HS-1 — historical-simulation VaR** | The second VaR method (user-directed roadmap 2026-07-07): factor-based historical simulation over the captured factor-return windows — its own registered model family + methodology doc + declared parameters (window adequacy, quantile interpolation). No new UI work needed: hist-sim runs surface in the FE-1 view automatically (same VAR run family). | M/L | none (data already captured) |
| 3 | **P3-C2 — hardening bundle** ✅ **DONE** (`6fb1a13`, CI green) | The accumulated recorded follow-ups swept in one consolidation slice (the P3-C1 pattern): exposure-family scaffold/`failure_reason` adoption (scaffold relocated risk→calc); exposure runs in the FE listing (`exposure.view` handling; source-switch); captured-input-table `PreciseDecimal` parity (incl. `transaction` via the review); the DQ-rule first-registration savepoint race. NO migration. Full 6-finder review, 9 folds. | M | none |
| 3.5 | **TD-1 — test-data realism audit** ✅ **DONE** (`ac92e0b` + follow-up `4534a38`, CI green) | A hygiene insertion (Part 4 rule 3) ahead of P2-7 implementation: retrospectively audited + remediated existing economic-value fixtures to the new fixture-realism standing rule (three-bucket classify-then-fix; test-and-docs-only diff fence). The base fixtures were already plausible; 8 signal-forcing/ordinary values in the factor-return/covariance/VaR/sensitivity/exposure test fixtures were remediated. NO golden re-derivation (chosen values preserve invariance/inequality/pinning asserts). 4 independent finder passes caught 2 author errors + 2 same-class completeness misses — all folded. NEW `08_testing_qa/test_data_realism.md` + a standing review-angle. TD-2 not needed. | M/L | none |
| 4 | **P2-7 — benchmark price/level capture** ✅ **DONE** (plan `04c4135`; impl `ea2863d`, CI green) | The captured-input slice (`benchmark_level`/`benchmark_return`, NET-NEW **ENT-052**) that unblocks every return-based benchmark-relative analytic. FR/bitemporal; captured returns ONLY (no calc from levels); migration `0029`; no new permission; six additive `MARKET.BENCHMARK_LEVEL_*`/`_RETURN_*` audit codes. Full 6-finder review, ~10 folds. **OD-G precision amendment (2026-07-09):** P2-7 unblocked the **ex-ante** benchmark-relative half only — the ex-ante tracking error consumes the benchmark **membership** (P2-6); the **ex-post** measure (realized TE / active return / IR / tracking difference) additionally requires a governed **portfolio-return series**, a separately-planned performance-measurement slice where `benchmark_return`'s first governed consumer lands. | M | none |
| 5 | **P3-7 — benchmark-relative analytics** ✅ **DONE** (plan `552b954`; impl `65e6dbe`, CI green) | Ex-ante active risk / tracking error `TE = √(wₐᵀΣwₐ)` over the existing factor-exposure + covariance engine and the captured benchmark membership — the P3 plan's final **ex-ante** analytic leg (`active_risk_result`, ENT-027; migration `0030`; `COMPONENT_KIND_BENCHMARK` minted; run family `ACTIVE_RISK`, metric `TRACKING_ERROR`). FULL max-effort multi-agent review (10 finders + 6 verifiers + sweep): 21 folds, 3 deferred findings recorded in the decision record Part 6 (var_service V2/V5 twins; shared covariance adjudicator; lineage batching). The **ex-post** leg (realized TE / active return / IR) is DEFERRED on the portfolio-return prerequisite (OD-G). | M/L | slice 4 |
| 6 | **P3-6 — stress/scenario** → **MOVED to Wave 2 slot 5** (Wave-1 close review, 2026-07-09 — the pre-authorized outcome, with a positive reason: scenario P&L is richer once realized-return machinery exists) | ENT-029/030 (scenario definitions + results). Sequenced LAST in the wave deliberately: our own P3-0 record flags it as RTM-P5-phase (possibly later); doing it after benchmark-relative completes the P3 analytics story without pulling it ahead of cheaper wins. If the Wave-1 close review argues for deferring it into Wave 2, that is an expected outcome, not a failure. | L | none (independent) |

**Wave 1 CLOSED (2026-07-09):** `wave_1_close_review.md` **RATIFIED** (OQ-W1C-1…6) — honest state audit + the
reconciled deferral register + the outward benchmark review + the destination check. Slices 1–5 + two ratified
insertions (TD-1, P3-C3) shipped CI-green; P3-6 moved to Wave 2 (pre-authorized). The TC-1 OD-D obligation
discharged (`npm audit` full tree, all severities = 0 at `6a864c9`).

## Part 2.5 — Wave 2: the ratified sequence (2026-07-09; same per-slice discipline)

**The organizing fact (from the close review Part 4):** ONE missing primitive — a governed portfolio-return
series — is the prerequisite for THREE recorded destinations (ex-post TE/IR per P3-7 OD-G; VaR backtesting;
the private-asset desmoothing machinery). Wave 2 builds it first, then its consumers, then begins the thesis
destination in dependency order.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **PM-1 — governed portfolio-return series** ✅ **DONE** (impl `b2445c7` + ruff-format `f5e16b6`, CI **#137** green) | Performance-measurement v1: chain-linked TWR (Modified-Dietz within N caller-supplied exposure-run boundaries) over pinned exposure atoms + TRANSFER_IN/OUT transactions — the NEW `perf.return.twr` v1 family (ENT-053 `portfolio_return_result`, migration `0031`; `perf.run`/`perf.view` R-07 mint; new **CAP-20** + REQ-PRF-001). **v1 = SINGLE-portfolio book** (subtree deferred). FULL ultrareview (5 finders): folded 3 HIGH (return magnitude gate; portfolio_id cross-tenant FK; duplicate-run-id) + 1 MED + minors, each with a regression test. Rule-6 externals: GIPS 2020. The triple unlock. | M/L |
| 2 | **P3-8 — ex-post benchmark-relative** ✅ **DONE** (plan `4880b36` via PR #1; impl `86ef3ec` merged via PR #2 = `d769f59`, CI **#142** green) | Realized TE / active return / tracking difference / IR over PM-1 + `benchmark_return` (its FIRST governed consumer — closes P3-7 OD-G). Rule-6: ESMA 2012/832 applies directly. ENT-054, migration `0032`; `perf.run`/`perf.view` REUSED (no mint). | S/M |
| 3 | **BT-1 — VaR backtesting** ✅ **DONE** (plan `3e81ef4` via PR #4; impl `e7b615d` merged via PR #5 = `868f892`, CI green) | Kupiec POF + Basel traffic-light over realized P&L vs ONE shipped VaR method per run (both methods supported; comparing them is two runs). The P7 model-validation prerequisite (SR 11-7 "outcomes analysis") — closes the Wave-1 close review's named nearest supervisory gap. ENT-055, migration `0033`; `risk.run`/`risk.view` REUSED (no mint). | M |
| 4 | **PA-0 — private-asset foundations** ✅ **DONE** (plan `07e5d6a` via PR #7 = `7a422aa`; impl `c9d41a7` merged via PR #8 = `ad3d3fe`, CI green) | The thesis destination begins: ENT-019 `proxy_mapping` REALIZED (FR captured private→public factor proxies, migration `0034`; the CURRENCY-family v1 scope ENFORCED); the appraisal/NAV leg needed NO new schema (an ordinary `instrument` + `valuation` under a documented private `asset_class` convention); the Geltner-AR(1)-first desmoothing disposition RECORDED for the **PA-1** follow-on (the ratified capture-first split — the roadmap's own "may split capture-first" outcome). `marketdata.view`/`.ingest` REUSED (no mint). **Okunev-White citation flagged UNVERIFIED — resolve before PA-1.** | M/L |
| 4.5 | **MD-H1 — marketdata/registrar hardening** | A hygiene insertion (Part 4 rule 3; user-ratified 2026-07-11) paying the three bug-shaped deferral-register items before the wave close: the FR supersede window-coherence guard (all six FR series — the only integrity-flavored open item), the shared IntegrityError→409 mapping (every marketdata capture endpoint), and the registrar first-registration concurrency race (all governed families). NO migration; guards + shared mappings + tests. Dedup/perf register rows stay trigger-based. | S/M |
| 5 | **P3-6 — stress/scenario** ✅ **DONE** (plan merged via PR #12 `28c5225`; impl `fe3bb55` merged via **PR #13** = `0f66220`, CI green) | The TENTH governed number: deterministic LINEAR factor-shock P&L — `scenario_definition`/`scenario_shock` (ENT-029, EV header + FR shocks) + `scenario_result` (ENT-030, IA), migration `0035`; run family `SCENARIO` reusing `risk.run`/`risk.view` (no mint); `risk.scenario.factor_shock` v1. Versioned saved scenarios applied to a COMPLETED factor-exposure run over a `SCENARIO_INPUT` snapshot; partial-coverage = unnamed-factor-unchanged with loud coverage counts; TR-09 both sides. DECLARED linear shocks only — historical-window replay (v2), worst-case search (v3), reverse stress, ES, multi-period, non-CURRENCY families DEFERRED. | L |
| — | **Hygiene ride-alongs** (no slice slot): runtime npm-audit CI gate `high`→`moderate` (applied AT this close — the tree is at zero); branch protection on `main` (OD-050) **✅ DONE 2026-07-10** — `enforce_admins=everyone` + all 5 CI checks required; no direct pushes to `main`, PR flow binds everyone from P3-8 on; ES leg DEFERRED (OQ-W1C-4). | | S |

**Wave-2 close = the same mandatory review + re-baseline** (incl. the rule-6(b) outward benchmark review + the
destination-progress check). **CLOSED 2026-07-12 — `wave_2_close_review.md` RATIFIED (OQ-W2C-1…5):** all five
ratified slices + MD-H1 shipped CI-green, zero deviations; ten governed numbers; the return-series triple unlock
delivered; the thesis destination began (PA-0).

## Part 2.6 — Wave 3: the ratified sequence (2026-07-12; same per-slice discipline)

**The organizing fact (from the Wave-2 close, Part 4):** the thesis §2.1 destination — private assets no longer
understating their own risk — now has BOTH prerequisites in place (a governed return series from PM-1; a
private→public proxy substrate from PA-0). Wave 3 builds the **transformation math itself** — the differentiation
payload — after one small hygiene slice so it lands on clean rails.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-1 — dedup hygiene (early)** ✅ **DONE** (`0584cfe` merged via **PR #17** = `82f5f1d`, CI green) | Paid the TIPPED `resolve_*_run` helper family (3 near-verbatim copies — var_backtest/active_risk/scenario — meeting the P3-4-R0 3rd-consumer rule) → one shared `calc/` resolver with an injectable error class (the `assert_portfolio_in_tenant(error=…)` precedent). Test-and-refactor only; NO migration. Done first so PA-1 builds on the cleaned resolver. The FR-membership engine (P3-6 D-2) stays trigger-based unless RD-1 elects the larger extraction. | S/M |
| 2 | **PA-1 — private-asset desmoothing (the thesis payload)** ✅ **DONE** (plan merged via PR #18 `366a5c4`; impl `34e8830` merged via **PR #19** = `f8bc20d`, CI green) | The ELEVENTH governed number: Geltner AR(1) unsmoothing of the captured appraisal mark series — `desmoothed_return_result` (ENT-056, IA), migration `0036`; declared-α identity (`perf.return.desmoothed_geltner` v1); run family `DESMOOTHED_RETURN` reusing `perf.run`/`perf.view` (no mint); the honest-uncertainty stdev pair on every run; Okunev-White citation RESOLVED (an iterative Geltner extension — PA-0's tentative substance corrected). 4-finder review: 10 folds (incl. the FE runs-list altitude fix) + 2 deferrals (RD-2 candidate; instrument-guard next-touch). | M/L |
| 3 | **PA-2 — private holdings on the public factor substrate (end-to-end)** ✅ **DONE** (`1ac7b7f`→`b20c736` merged via **PR #22** = `96b2bd2`, CI green; **NO migration**) | Projected a private holding through `proxy_mapping` onto CURRENCY factors and ran the governed exposure→factor-exposure→VaR chain — the thesis §2.1 END-TO-END demonstration, with a **byte-exact invariance proof** (a proxied private book yields the identical VaR to a public book holding the same factor vector). A NEW registered model family (`risk.factor_exposure.proxy` v1, code-version identity) dispatched through the SAME `run_factor_exposure` binder (no new table); proxy rows pinned via a load-bearing binding-predicate switch. FULL 4-finder review: ~24 raw → 13 distinct, **ALL FOLDED, zero deferrals** (incl. the magnitude/envelope gate, the symmetric predicate gate, and the active-risk partition guard `_assert_partitioning_exposure_run`). Three v2 register items minted (proxy-aware active-risk; regression-estimated weights; residual/idiosyncratic variance). | M |
| — | **Coarse beyond Wave 3** (themes, not sequenced): the **P7 validation workflow** (BT-1 now feeds it); ES closed-form leg; credit/counterparty (RTM-P3); limits/breach/SoD (RTM-P6); reporting (RTM-P8); SSO/OIDC before internet-facing (RTM-P9). | | — |

**Wave-3 close = the same mandatory review + re-baseline** (incl. the rule-6(b) outward benchmark review + the
destination-progress check). **CLOSED 2026-07-13 — `wave_3_close_review.md` RATIFIED (OQ-W3C-1…5):** all three
ratified slices shipped CI-green in sequence; PA-1 + PA-2 SHIPPED-AS-RATIFIED; RD-1 shipped with a note-level
census undercount (folded into RD-2); the PA-2 fold-#8 half-application was found and fixed at the close; the
thesis §2.1 destination REACHED in v1. **Wave 4 ratified (Part 2.7, fork option A): RD-2 → PA-3 → a v2 companion.**

## Part 2.7 — Wave 4: the ratified sequence (2026-07-13; same per-slice discipline)

**The organizing fact (from the Wave-3 close, Part 4):** Wave 3 REACHED the thesis §2.1 destination — a private
holding now flows capture → desmooth → proxy → factor risk → VaR, byte-exact against its public equivalent — but
two links are still the honest-stub choice: proxy **weights are captured, not estimated** (so PA-1's desmoothed
series is not yet an input to anything), and the factor universe is **CURRENCY-only**. The user ratified fork
**option A** at the close (2026-07-13): DEEPEN the private-asset payload toward best-in-class (thesis §2.2), after
one small hygiene slice.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-2 — declared-parameter + resolver dedup (early hygiene)** ✅ **DONE** (`11f890d` merged via **PR #26** = `cc9679b`, CI green; NO migration) | Paid the two TIPPED items in one pass: the declared-parameter parse-back family (PA-1 D-1 — 5 identical `select(ModelAssumption)` loads + 2 byte-identical `_single` helpers + 3 inline extractions → the shared `model/assumptions.py`) and the four RD-1 residual resolver copies (the Wave-3-close census undercount, discharged — 2 byte-identical read folds + 2 consumed-guard folds with the ratified OQ-3 message normalization). Net −63 lines across the two bootstraps. 4-finder review: 3 CLEAN + the sweep's 3 test-hardening folds. | S |
| 2 | **PA-3 — regression-estimated proxy weights (the loop-closer)** ✅ **DONE** (`15ba13f`→`77149d6` merged via **PR #28** = `a98d380`, CI green; migration `0037`) | The TWELFTH governed number: unconstrained OLS of the **desmoothed** appraisal return series (PA-1) on candidate CURRENCY factor returns → `proxy_weight_estimate_result` (ENT-057, IA; per-coefficient standard errors + R²), the FIRST downstream consumer of desmoothing. Estimates are MODEL OUTPUT, promoted into `proxy_mapping` ONLY by the deliberate `risk.promote_proxy_weight_estimate` step (activating PA-0's reserved `REGRESSION` method + the additive `source_calculation_run_id`). Declared `min_observations` identity (RD-2 rails); `n≥max(N,k+2)`. Rule-6: Sharpe 1992, Scholes-Williams 1977/Dimson 1979, Asness-Krail-Liew 2001. FULL 4-finder review incl. a Fable-class numeric finder (independent Fraction re-derivation, math CONFIRMED): 1 real governance defect folded (the supersede/correct REGRESSION-citation leak) + 5 smaller folds; 2 deferrals. `make check` 1330 passed. | M/L |
| 3 | **PA-4 — total parametric VaR (residual/idiosyncratic variance)** ✅ **DONE** (`d3a6eae`→`c04768e` merged via **PR #30** = `8ef70db6`, CI green; migration `0038`) | The planning-time call went to **residual variance** (OQ-PA-4-1; proxy-aware active-risk stays a registered trigger-based item). The THIRTEENTH governed number: `risk.var.parametric_total` v1 — total VaR = the plain factor variance PLUS the diagonal idiosyncratic residual variance of REGRESSION-proxied instruments' cited PA-3 estimates (Sharpe 1963), `σ_e,daily = σ_e,period/√(appraisal_days·252/365)` with `appraisal_days` a DECLARED identity parameter (in-build OD-D refinement — the pinned summary carries no span). SAME binder (the PA-2 dispatch precedent), SAME `var_result` + ONE additive `residual_variance` column (decomposition evidence). The P3-5 specific-risk=0 limitation is PARTIALLY discharged — REGRESSION-cited instruments only. **The `1−Σw` partial-proxy leg remains honestly UNMODELED** (the PA-2 register wording sketched that variant; the ratified PA-4 models the regression residual instead — `factor_exposure_proxy_v1.md`'s limitation stands). FULL 4-finder Fable review: 26 findings → 23 folded, ZERO HIGH math defects (goldens independently re-derived byte-exact; 6000-case invariance fuzz). | M |
| — | **Coarse beyond Wave 4** (themes, not sequenced): the **P7 validation workflow** (fork option B — nearest supervisory ingredient, BT-1 feeds it); **multi-family proxying** (needs an equity/credit factor-returns capture slice); ES closed-form leg; credit/counterparty (RTM-P3); limits/breach/SoD (RTM-P6); reporting (RTM-P8); SSO/OIDC before internet-facing (RTM-P9). | | — |

**Wave-4 close = the same mandatory review + re-baseline.** Delivery runs under the full-autonomy grant
(2026-07-12, EXTENDED 2026-07-14): Claude self-drives plan→implement→review→commit→push AND opens + merges the
PRs (the adversarial review + `make check` + full-PG + CI-to-green gates replace the human merge gate; branch
protection's required checks stay on). The USER still signs off Tier-3 (methodology/model/grain/entity
decisions) and genuine design forks.

## Part 2.8 — Wave 5: the ratified sequence (2026-07-14; same per-slice discipline)

**Wave 4 CLOSED (2026-07-14):** `wave_4_close_review.md` **RATIFIED** (OQ-W4C-1/3/4/5/6 approved as
recommended; OQ-W4C-2 answered **"A: Self-governance"**). All three Wave-4 slices shipped-as-ratified;
9 deferrals PAID, 3 TIPPED (→ RD-3), 1 newly minted (estimate-staleness governance); 5 record-hygiene
fixes applied at the close.

**The organizing fact (from the close review Parts 3–4):** the private-asset chain is structurally
complete and vendor-aligned — but the flagship number is unvalidated (all 13 governed models sit at a
non-enforcing `UNVALIDATED`), exempt from backtesting, and missing the FRTB-preferred ES measure, while
every ingredient those three need already shipped. Wave 5 = **"the numbers govern themselves."**

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-3 — hygiene (early)** ✅ **DONE** (planning `39533c8` merged via **PR #33** = `df6afd6`; impl `d751fde` merged via **PR #34** = `29bc5a2`, CI green; NO migration) | The three TIPPED items: `_reresolve_content` parse-hardening (verify-path 500s — scoped narrowly via a new `MalformedPinError` + `_parsed_pin`/`_pinned_row_ids` on the 4 composite branches that actually parse `captured_content`, NOT the whole 19-branch dispatch: a review correction, since the first pass would have masked live-data serializer bugs as drift); the `proxy_mapping` instrument-guard fold + message normalization; the MD-H1-C adoption sweep (7 provably-identical `parse_strict_decimal` sites; every SKIP recorded with its reason in the record's Part 5, incl. a genuine latent NaN-detonation bug found at `benchmark_relative_service.py` and deferred as out-of-scope-fence). Ride-along: the PG cross-module seed-collision fixed at cause (whole-seed no-op guard + `resolve_or_insert` on the 2 SYSTEM-tenant fixtures), deterministic uuid5 ids untouched — the **dirty-schema double-run is now a standing local-validation capability**. 4-finder review: 3 HIGH + 3 MED folded. | S/M |
| 2 | **VW-1 — model-validation workflow (P7 / SR 11-7)** ✅ **DONE** (planning `9f0e024` merged via **PR #35** = `58900b1`; impl `726373f` + CI fix `93ab215` merged via **PR #36** = `a9a00eb`, CI green; migration `0039`) | The headline — realizes the reserved **ENT-037 `model_validation`** (+ IA `model_validation_finding`/`_evidence` children) as append-only SR 11-7 validation records at `model_version` grain: outcome {APPROVED, APPROVED_WITH_CONDITIONS, REJECTED} × type {INITIAL, PERIODIC, TRIGGERED}, findings, and evidence that can cite COMPLETED governed runs (a BT-1 backtest becomes first-class outcomes-analysis evidence — the FRTB MAR32 pattern). **The gate with teeth: a latest-outcome REJECTED refuses new governed runs** at the ONE shared `assert_model_version_of` seam (all 12 model-bound binders inherit it); **UNVALIDATED keeps running** — the documented **SR 26-2** use-before-validation exception posture (rule-6 found SR 11-7 was superseded 2026-04-17 by SR 26-2 / OCC 2026-13; the record cites both). R-07 mint **`model.validate`** (2L `risk_manager_2l` only — SOD-03 withheld from the 1L register-holder); reserved **`MODEL.VALIDATE`** activated; human-only v1 (BR-15/MG-07). Doc advances: ENT-037 REALIZED, REQ-MDG-003 → In-Progress, **CTRL-022 → Operational**, taxonomy `.VALIDATE` activated, SoD mint record, independence-policy note. 4-finder review (1 adversarial on the gate): **1 HIGH** — `RejectedModelVersionError` was unmapped at the risk/perf run endpoints (raw 500, not the promised 422; the seam-only tests hid it) — fixed + end-to-end 422 tests through a real risk AND perf endpoint; 5 more folds. Staleness fork **A**: the σ_e estimate-age GATE rides BT-2. | M |
| 3 | **BT-2 — total-series backtest** ✅ **DONE** (planning `da258ff` merged via **PR #38** = `26d89b4`; impl `3cb9844` via **PR #39** = `4035f10`, CI green; migration `0040`) | Extends outcomes analysis to `VAR_PARAMETRIC_TOTAL` (the PA-4 `METRIC_TYPES` exclusion DISCHARGED — one tuple, zero schema change: ENT-055's `var_metric_type` was already unconstrained and the kernel is method-agnostic) — **the honest work is the doctrine, not the constant swap**. Every VaR is a hard-enforced 1-DAY forecast, so a daily total-series read on an appraisal-marked book is biased TWO WAYS by construction: exceptions SUPPRESSED between marks (flat private P&L) and CLUSTERED on mark dates (a quarter's move vs a 1-day allowance). Registered as limitations + a dated methodology scope amendment with the READ RULE: the unconditional Kupiec/Basel verdict is **NOT valid evidence of adequacy in EITHER direction** on such books (validity degrades with the private-leg share); the dated per-pair `EXCEPTION_INDICATOR` rows are the evidence surface. Appraisal-frequency pairing + a Christoffersen leg = named **BT-3 candidates** (the stale "Christoffersen = BT-2" label fixed at 5 sites). **Ride-along (VW-1 fork A) PAID:** the σ_e estimate-age GATE — a DECLARED `max_estimate_age_days` on a new `risk.var.parametric_total` **v2** (v1 grandfathered ungated; VW-1's REJECT is the sunset lever); age = pinned covariance `window_end` − the cited estimate's regression SPAN END; pre-create 422; + migration `0040`'s `estimate_age_days` echo. **The estimate-staleness register item is ~~CLOSED~~ — an OVERCLAIM corrected at the Wave-5 close: 1 of its 3 clauses is paid (the age echo); the promotion path has no age check, every non-total family binds the same weight ungated, and nothing expires a `proxy_mapping`. RE-OPENED as "estimate staleness — residual", TIPPED for Wave 6.** | S/M |
| 4 | **ES-1 — parametric ES leg** ✅ **DONE** (planning `dcd9634`+`eec25a1` merged via **PR #40** = `bbc1d1b`; impl `a0723ac` + folds `17d48bc` merged via **PR #41** = `2abc1b1`, CI green; **NO migration**) | The closed-form seam over BOTH σ and σ_total, realizing the `ES_PARAMETRIC` value reserved since P3-5 — the **14th governed number** and the first that answers what VaR structurally refuses (a cut-off says nothing about the magnitude beyond it). `ES_c = k_c·σ`, `k_c := φ(Φ⁻¹(c))/(1−c)`, **`c` = CONFIDENCE** (the P3-5 seam never defined `α` and the literature is split — ES-1 pins it; ES is the α-TAIL-MEAN integral, never TCE, so a later HS leg inherits the right estimator). **NO MIGRATION** — the grain already permitted the value and every column existed. TWO codes through the SAME binder (`risk.var.parametric_es` + `..._es_total` v1, the latter carrying BT-2's gate REQUIRED from birth); the extra-row-on-the-VaR-run alternative was **census-DISQUALIFIED** (it would break every BT-1/BT-2 backtest). `k_c` = a REGISTERED 12dp constant, identity-checked at bind — **no runtime normal function of any kind**. **NO ES backtest**: a ratified omission on **FRTB precedent + parametric redundancy, explicitly NOT non-elicitability** (which is false). **The honest work was the research**: the obvious pitch ("VaR is incoherent") is WRONG for this platform's own delta-normal VaR (normal ⊂ elliptical ⇒ already coherent, Embrechts-McNeil-Straumann), and BCBS never argues coherence at all — ES here buys tail severity + robustness-if-normality-fails. **A 3-verifier planning pass ran BEFORE ratification and killed 2 HIGH claims of my own** ("the VaR families' behaviour is unchanged" — false, one shared z table feeds four families and 0.975 was two shipped tests' off-vocabulary probe; and a "byte-exact portfolio-independent golden" that cannot pass at row level under 6dp quantization). Both headline claims survived attack; all six constants confirmed to the last digit. **Ride-along: BT-2's fired PG trigger PAID (both halves).** | S arithmetic / M surface |

Then the **Wave-5 close review**, with **multi-family factor capture teed as the Wave-6 headline**
(residual shrinkage/EWMA + desmoothing v2s riding when it lands). Delivery runs under the full-autonomy
grant as extended 2026-07-14 (Part 2.7 note); the USER signs off Tier-3 decisions.

## Part 2.9 — Wave 6: the ratified sequence (2026-07-15 at the Wave-5 close, OQ-W5C-6 sub-fork **(A) governance-first**; same per-slice discipline — decision record + plan + **pre-ratification verifier pass (the ES-1 lesson)** + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist)

> **The tee was corrected before ratification (OQ-W5C-4):** the previously-recorded prerequisite — "an
> equity/credit factor-returns capture slice" — was **factually wrong** (capture is already family-generic;
> proven by execution at the close). The real prerequisite is the **loadings substrate + a fractional,
> non-partitioning exposure kernel v2**. Wave 6 is planned on the corrected text.
>
> **Scope note (user, 2026-07-15):** the platform targets **enterprise** use and will eventually run and demo
> **real data** — but not before the build completes. No vendor adapters/live feeds ride this wave; the
> captured-data posture (RTM-P9) stands, and the demo environment is the synthetic/seeded tenant.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **MG-1 — model materiality + the first validation campaign** ✅ **DONE** (planning `7435e0a` via **PR #43** = `91c39fa`; impl `56cda62`→`e490c73` via **PR #44** = `46ce296`, CI green; **NO migration**) | Finishes Wave 5's own headline instead of leaving a capability with an empty table (**16/16 model codes sit UNVALIDATED**). Materiality assignment (the existing nullable `tier` gains meaning: rigor + cadence scale with it — **SR 26-2's organizing principle**, which contains zero occurrences of "tier"/"annual"); **the first validation campaign** — real validation records against the flagship VaR family citing **real BT-1/BT-2 runs as outcomes-analysis evidence**, with honest findings drawn from the registered limitations and a CURRENCY-only **condition whose remediation IS FL-1/MF-1** (the flywheel: governance generates the backlog, then TRIGGERED re-validation closes the loop); **OD-033 cadence anchored on materiality** (open since VW-1, un-answerable without this); the **F3 fix** (the scope-stretched SR 26-2 exception becomes per-model + time-boxed). **Ride-alongs: the RD-3 NaN bug** (the oldest live known bug — `Decimal("NaN")` slips the malformed-pin wrapper in `benchmark_relative_service.py` and detonates as a raw 500 + RUNNING orphan; pay it, stop waiting for the trigger) **+ the estimate-seam integration test** (one test chaining a REAL PA-3 regression run into a total VaR — retires the "two halves meet at a hand-minted seam" ambiguity permanently). | S/M |
| 2 | **FL-1 — factor-loading substrate + fractional exposure kernel v2** ✅ **DONE** (planning via **PR #46** = `ec01053`, CI #303; impl `fl-1-impl` tip `ca07529` via **PR #47** = `778891c`, **CI #314 green**; **NO migration** — head stays `0040`) | **The REAL multi-family prerequisite** (the corrected tee): a loadings capture/estimation substrate (design fork at planning: vendor-beta capture vs PA-3's regression machinery pointed at public instruments) + an exposure kernel v2 whose loadings are **fractional and non-exclusive** (`build_factor_index` keys on `currency_code` and `allocate_atom` maps each atom to exactly ONE factor at loading 1 — dimension-generic ONLY for partitioning dimensions, false for STYLE/MARKET/credit) + relaxing `proxy_mapping`'s CURRENCY gate + the **zero-migration vocab extension** to FRTB's risk classes (MAR33.12: rates/equity/FX/commodity/credit-spread — `factor_family` is `String(30)` with no DB CHECK; the BT-2 `METRIC_TYPES` pattern). **Ride-along: the FE drift trio** (the dead-end PROXY_WEIGHT_ESTIMATE row; the missing `residual_variance`/`estimate_age_days` columns; **the ES `z×σ ≠ var_value` display** — the only place a governed number presents arithmetic that does not check out). | L |
| 3 | **MF-1 — multi-family factor exposure end-to-end** ✅ **DONE** (planning via **PR #49** = `0951784`; impl `mf-1-impl` tip `2898eb8` via **PR #50** = `96b766c`, **CI #323 green**; **NO migration** — head stays `0040`) | The headline payload on FL-1's substrate: a real equity/credit book through capture → loadings → exposure v2 → covariance → VaR/ES, with the PA-3 regression finally running on the Sharpe-1992 factor set its own citations name. **Unlocks four blocked candidates** (FRTB liquidity horizons, partial ES by risk class, non-CURRENCY scenario shocks, §2.2's risk-class taxonomy) + the residual-shrinkage/EWMA + desmoothing-v2 ride-alongs teed since Wave 4. Ends with the **TRIGGERED re-validation** closing MG-1's CURRENCY-only condition — the full lifecycle demonstrated. | M/L |

Then the **Wave-6 close review** — **DONE and RATIFIED 2026-07-17** (`wave_6_close_review.md`, OQ-W6C-1…6;
merged as DRAFT via **PR #52** = `9d561bf`, ratified immediately after). The Wave-7 sequence it ratified is
Part 2.10 below.

## Part 2.10 — Wave 7: the ratified sequence (2026-07-17 at the Wave-6 close, OQ-W6C-6 fork **(A) "deepen the mathematics"**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist, now including the grep-for-"pending" item)

> **The two ratified riders:** (1) **SC-2 (non-CURRENCY scenario shocks) is the named pull-forward** if the
> wave underruns — the one fork-B slice with a genuine methodology question (per-class shock semantics; BOTH
> gates + probe moves); (2) **commitment/capital-call handling is the PRESUMPTIVE WAVE-8 HEADLINE** — thesis
> §2.1's third transformation leg, zero code after six waves, named so it stops being the quiet one.
> The user's real-data scope note (Part 2.9 preamble) stands.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **HG-1 — the hygiene opener** ✅ **DONE** (plan `4c8829f` via **PR #54**; impl `9e3200a` merged via **PR #55** = `8260ea6`, CI green run #339; NO migration) | The re-opened **estimate-staleness residual** (OQ-W6C-2: the promotion path gains an age check — the expire-a-mapping clause is a Tier-3 design question surfaced at planning); the **FL-1 registrar-constant CURRENCY-wording debris** (`risk/bootstrap.py:665-667` + the HS analogue — behavioral for NEW registrations, the finding-key compatibility constraint applies); the **private-on-multi-family demo ride-along** (the genuinely-private appraisal chain has never exercised a non-CURRENCY factor; the capability shipped at FL-1 — mind the mixed-family fence: a NEW private instrument, never the legacy PE fund). Shipped: OD-A the opt-in promote-day age gate (`ProxyWeightStaleEstimateError`; `promotion_age_days` always-measured+audited; three unmeasurable shapes fail closed under a bound), OD-B DEFERRED, the 11-row constants sweep (keys verbatim), stage 3 of the living tenant (`PC-BRIDGEWATER-II`, α=0.4 on multi-family factors). 4-finder review, zero HIGH. | S |
| 2 | **ES-HS-1 — ES over historical simulation (the headline)** ✅ **DONE** (plan `7568c49` via **PR #57**; impl `c7b8c15` merged via **PR #58** = `dc2a494`, CI green run #348; migration `0041`) | Converts the platform's tail measure from a unit conversion (`k_c·σ`) into a real empirical number; realizes the `ES_HISTORICAL` metric over the shipped VAR-HS machinery; **the ONE migration (0041) widening the verified 0028 CHECK** (`ck_var_result_parametric_not_null` exempts only the literal `'VAR_HISTORICAL'` — an ES_HISTORICAL row violates it in PG while passing the whole SQLite battery); the α-tail-mean estimator convention pre-pinned at ES-1 inherits; **the genuine Acerbi-Szekely ES backtest is the in-slice fork** (ship it here or tee as BT-3 with TIPPED Christoffersen). **Fetch Acerbi-Szekely 2014 to paragraph at planning (UNVERIFIED).** Shipped: the Acerbi-Tasche Prop-4.1 empirical tail mean; `risk.var.historical_es` v1 via the registry-map dispatch; the AS backtest TEED as BT-3 (Christoffersen finally homed; pairing via shared `input_snapshot_id`); demo stage 4 (the 18th code); AS 2014 verified-via-reproduction (primary gated). 4-finder review, zero HIGH. | M |
| 3 | **RS-1 — residual shrinkage/EWMA** ✅ **DONE** (planning **PR #60** = `8014b51`, branch tip `fdfbfa9` — merge sha corrected at the Wave-7 close; impl **PR #61** = `9c15658`, CI green run #365; **NO migration** — head stays `0041`) | The PA-4 OD-E/OD-G v2s REALIZED as two declared estimator conventions on `risk.proxy_weight.regression`: **`EWMA_RISKMETRICS`** (Axioma/RiskMetrics decay-weighted specific variance; declared λ; the s2 decoupling keeps OLS std-errors classical; raw v1 grandfathered via absent-convention) + **`SHRINKAGE_CROSS_SECTIONAL_EB`** (Barra USE4 empirical-Bayes cross-sectional shrinkage; DATA-DRIVEN per-instrument w_i via method-as-identity; N≥3-DISTINCT-instrument fail-closed; per-target transform run pinning a `RESIDUAL_SHRINKAGE_INPUT` cohort). NO new governed number/model code/permission/ENT/migration. **Primary sources VERIFIED** (the ledger's UNVERIFIED flag discharged): RiskMetrics TD 4th ed §5 + Axioma (EWMA), Barra USE4 (shrinkage) — **Ledoit-Wolf verified and explicitly NOT used** (it leaves variances unshrunk — the wrong primary for a residual-variance shrinkage). Demo **stage 5** = the SECOND lifecycle turn (the raw-sample-σ_e rider closed by 2 TRIGGERED re-validations; the `hostage…` finding flipped to historical, both directions test-pinned; + 2 INITIAL AWC dossiers for the new versions). 4-finder review: **2 HIGH + 5 MED + 7 LOW all folded, zero numeric defects, zero fence violations** (decision record Part 6). | S/M → M/L |
| 4 | **DS-2 — estimated-α desmoothing** ✅ **DONE** (planning **PR #63** = `0f199aa`; impl `ds-2-impl` via **PR #64** = `5120baa`, CI green run #381; migration head `0041`→**`0042_desmoothing_estimated_alpha`**) | The declared-α rider REMEDIATED: two declared estimator conventions on `perf.return.desmoothed_geltner` — **`AR1_ESTIMATED`** (α̂ = 1−ρ̂₁ in-run, T-denominator Box-Jenkins; fail-closed on ρ̂₁ ≤ 0/constant series; the CONSERVATIVE Bartlett 1/√n band persisted as `alpha_stderr`; the Kendall/Marriott-Pope small-n UPWARD bias of α̂ a registered limitation, never corrected in-run) + **`OKUNEV_WHITE_ITERATIVE`** (declared max order 1..4, ONE deterministic lag-i pass per order ascending; the '−' quadratic root settled by derivation + execution; the length-vs-order structural floor; alpha NULL on OW rows). **GLM MA(k) stays the named v2** — extraction-verified to equation numbers (Eqs. 21–23, 34, 47–58); its MLE needs constrained numerical optimization, the recorded determinism/TR-09 obstacle. NO new governed number/model code/permission/ENT (the RS-1 two-versions shape). Migration `0042`: alpha → nullable + `alpha_stderr` + the summary-only CHECK; the 0028 destructive RLS-safe downgrade sandwich proven under a non-superuser owner-member role. Demo **stage 6**: `PE-HARBORVIEW-IX` (16 marks, α_true = 0.4, searched fixture ρ̂₁ = 0.5038 ⇒ α̂ = 0.4962), the three-way declared/estimated/OW comparison, 2 INITIAL AWCs; **NO TRIGGERED — the census-proved no-closable-condition honesty** (the desmoothing versions carry EXCEPTIONs, not conditions-bearing AWCs; deliberate contrast with the MF-1/RS-1 flywheel). 4-finder review: **ZERO HIGH, ZERO MEDIUM** (adversarial 2 LOW + 4 INFO, numeric 0 defects + 3 caveats, doctrine 2 LOW, fence 9/9) + the missing-CI-step catch at the pre-push battery (the P3-7 class — the new 0042 PG suite had no ci.yml step). | S/M → M |

Then the **Wave-7 close review**. Delivery runs under the full-autonomy grant; the USER signs Tier-3 decisions.

## Part 2.11 — Wave 8: the ratified sequence (2026-07-19 at the Wave-7 close, OQ-W7C-6 fork **(A) "fund the third leg"**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist incl. grep-for-"pending" AND the OQ-W7C-3 CI-run-id item)

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **BT-3 — the Acerbi-Szekely ES backtest** ✅ **DONE** (planning **PR #68** = `b493c78`; impl `bt-3-impl` tip `c5533b5` via **PR #69** = `109d11d`, **CI green run #399**; migration head `0042`→**`0043_es_backtest`**) | The ratified planning MUSTs DISCHARGED: the AS 2014 Z1 transcription re-verified two routes + the **null-expectation identity settling the '+1' grouping** (E[X\|X<−VaR]=−ES ⇒ '+1' OUTSIDE the sum; the corrupted inside-denominator grouping evaluates to ≈ −0.7004 at N(0,1)/a=0.025 — numerically COINCIDING with the −0.70 critical, so the exact-Fraction identity regression is the mandatory mechanical defense); the −0.70/−1.8 threshold VALUES at the three-route constant bar (M-P attribution + Lund Table 1 + this pass's executed seeded MC). **`risk.es_backtest` = the SIXTEENTH governed number** (the BT-1 mint precedent, OD-D ⇒ CC-2 becomes the SEVENTEENTH): Z2 + Z1 evidence rows with the verdict **DOMAIN-GATED to (paired confidence 0.9750 ∧ n_pairs 250)** — the planning verifier's ratification-blocking HIGH: the AS criticals are α-, T-, AND df-dependent (executed MC: 5% critical ≈ −3.68 at T=10 vs −0.70 only at T=250; a short-series verdict would spuriously REJECT the platform's own flagship ES); off-domain runs persist Z evidence + `ES_PAIR_COUNT` and NO verdict (the BASEL_ZONE read-rule precedent); the per-(α,T) critical table = the named v2 under a governed OFFLINE MC derivation (TR-09 — no runtime simulation). **Christoffersen ships in-slice as `risk.var_backtest` v2** (declared `independence=CHRISTOFFERSEN_MARKOV`, the COUNTING tri-state parse; LR_IND in the nonnegative orientation + `LR_CC = LR_UC + LR_IND` composed from the STORED 6dp legs; degenerate 2×2 ⇒ None; v1 byte-preserved; both Evers-Rohde source wobbles disclosed at the code site + referent) — the twice-re-teed item DISCHARGED. Sibling-pair gates (OD-C): VaR-HS/ES-HS legs pair per-as-of in BIJECTION on IDENTICAL `input_snapshot_id`, confidence uniform ACROSS legs, each leg model-version-UNIFORM; the behavior-preserving `adjudicate_return_side` extraction (BT-1 45/45 golden byte-identical). ONE additive migration **0043** (`es_value` echo). The OQ-W7C-2 tie-break fix folded BY NAME (`window_end DESC, system_from DESC, calculation_run_id DESC`). **Demo stage 7 = the DOMAIN-GATE HONESTY demo** — deliberately NOT a verdict (Z2 = −127.09 verdict-WITHHELD off-domain; the LR_CC joint-power lesson live at n=3: NEITHER Kupiec 3.66 nor LR_IND 2.77 alone rejects at χ²(1)=3.841 but the joint 6.43 > χ²(2)=5.991 DOES); 4 INITIAL AWCs; **NO TRIGGERED, census-proved** (the DS-2 honesty pattern); the 19th registered code. 4-finder review **ZERO HIGH** (the t3-attribution doctrine MED folded pre-merge; 2 named D-F4 next-touch deferrals → the Wave-8 close register). Battery: `make check` **1627**; fresh-schema local PG **1960/0/0** (all SEVEN demo suites in CI order); downgrade smoke clean. | S/M |
| 2 | **CC-1 — captured commitments / capital calls / distributions** ✅ **DONE** (planning **PR #71** + the rule-7 amendment **PR #73**; impl `cc-1-impl` tip `6fac50e` via **PR #74** = `1cdc95b`, **CI green run #420**; migration head `0043`→**`0044_private_capital`**) | The capture-first half of the Wave-8 headline (the PA-0 split) SHIPPED AS RATIFIED: **ENT-015 `commitment`** (FR bitemporal on the **(portfolio, instrument) stable identity** — the planning verifier's structural HIGH moved event linkage off FR version-row FKs; **currency CHAIN-IMMUTABLE**, no close-out op in v1, chain-terminate = the recorded v2) + **ENT-016 `capital_call`/`distribution`** (TWO truly-immutable IA tables under P0001 triggers; the provenance-only `commitment_version_id` echo, deliberately NOT an FK; the **negation FULL-reversal** correction — amount = exact negation, Σ self-correcting, one-reversal-per-event partial-unique race fence). The Tier-3 forks resolved as ratified: grain = (portfolio, fund-instrument); permission = the **`commitment.edit`/`.record`/`.view` three-code R-07 mint** (the FR/IA verb doctrine — identical maker holder sets, auditor_3l excluded). REQ-PRV-001/002 advanced to In-Progress (the computed-unfunded + liquidity-forecast clauses explicitly OPEN → CC-2); the `PRIVATE.*`/**EVT-240** seven-code audit family ACTIVATED (distinct REVERSE verbs; the PA-0 correction lessons pinned from birth); **rule-7 entity-filtered list reads from birth** (the first slice under it); the sanctioned `_raise_mapped_write` hoist to the shared `api/write_errors.py` (byte-identical, the 344 marketdata endpoint tests the golden). Demo **stage 8** = the CAPTURE half of the ratified commitment walk (PE-MERIDIAN-X; the mis-capture/REVERSE/recapture teaching pair live; the recallable distribution as data; **counts pinned UNCHANGED 19/34/95** — capture-only honesty + the no-auto-bridge zero-transactions assertion). **4-finder review: ZERO HIGH — 8 MED + ~10 LOW ALL folded** (the post-quantize (20,6) amount envelope closing the zero-amount-immutable-row + unmapped-500 holes; the honest no-close-out wording; the read rule at every promised surface; the per-op event-count pins; scope-fence 12/12). Battery: `make check` **1660**; fresh-schema local PG **2005/0/0** (all EIGHT demo suites in CI order); downgrade smoke clean. | M |
| 3 | **CC-2 — the governed commitment-pacing projection (the HEADLINE)** ✅ **DONE** (planning **PR #76**; impl `cc-2-impl` tip `a266b56` via **PR #77** = `1eaa703`, **CI green run #432**; closeout `cc-2-closeout`; migration head `0044`→**`0045_pacing_projection`**) | **`pacing.commitment_projection` = the SEVENTEENTH governed number** (ENT-059), the FIRST private-capital derived output — it discharges the REQ-PRV-001 "computed" leg CC-1 left open. **The deterministic Takahashi-Alexander recursion (JPM 28(2):90–100) — the TA fetch MUST DISCHARGED via reproduction** (primary gated; R1 Jaeckel + R2 Tamarix equation-level, R3 Luxenberg-Boyd structural, R4 FRG input-list; per-source grades in the referent), so the fallback rider did NOT fire: `C(t)=RC(t)·Unfunded(t−1)`; `RD(t)=max(Y,(t/L)^B)`; `D(t)=RD(t)·NAV(t−1)(1+G)`; `NAV(t)=NAV(t−1)(1+G)+C(t)−D(t)`. **FUTURE-ONLY from a pin-derived deterministic age** (the planning verifier's period-geometry HIGH resolved by delivery, TR-09-safe), QUANTIZE-THEN-ROLL 6dp, ANNUAL, NO optimizer. **The FIVE declared parameters ARE the version identity** (parsed back fail-closed); **NO TA constant minted** — only the `functional_form=TAKAHASHI_ALEXANDER` marker. Tier-3 forks resolved as ratified: the new peer `pacing` family (neither risk nor perf); the **`pacing.run`/`pacing.view` R-07 mint** (a governed-output `.view` INCLUDING `auditor_3l` — the decisive contrast with CC-1's captured-input `commitment.view`). `PACING.*`/**EVT-250** RESERVED-not-minted (`CALC.RUN_*` reused). Consumes ONLY the `PACING_INPUT` snapshot (commitment + call/distribution events + the latest mark); the mid-life re-anchoring (`Unfunded(0)=committed−ΣPaidIn+Σrecallable`; `NAV(0)` from the pinned same-currency mark, funded-without-mark REFUSED) = OUR documented adaptation. **Rule 7 all three legs IN-SLICE + the platform's FIRST latest-resolver** (`list_pacing_projections`/`latest_pacing_projection`, `as_of`-aware, cross-run aggregation a CONSUMER ERROR). **Demo stage 9** = the projection half of the commitment walk on the stage-8 PE-MERIDIAN-X commitment — **the counts MOVE 19/34/95 → 20/35/96** (the governed contrast with stage 8's capture-only-unchanged is the demo's own story). **4-finder review: ZERO surviving HIGH** — the adversarial+doctrine finders converged on ONE real FAILED-magnitude-gate cluster (the envelope `1e26` sat ABOVE the `Numeric(28,6)` column cap ~1e22 → PG overflow, AND extreme growth overflowed the Decimal context before the gate → 500; fixed: envelope→`1E21` the var/es-backtest precedent + a kernel `_MAGNITUDE_CEILING` so runaway params FAIL the run) + 3 LOW (`-0` identity, register/bind magnitude symmetry, Feb-29 clamp) ALL folded with tests (incl. the previously-untested FAILED path); math CONFIRMED (both hand-goldens re-derived), scope-fence CLEAN. Battery: `make check` **1716**; fresh-schema local PG green (all NINE demo suites in CI order); `0045` downgrade clean. | M/L |

**Ratified riders (OQ-W7C-6):** SC-2 stays the **named pull-forward on underrun** (carried verbatim — its Wave-7 condition EXPIRED UNSPENT); the **stage-7 demo obligation** (backtest the flagship ES at BT-3; walk one commitment capture→projection at CC-1/CC-2 — converting the two largest recorded gaps into living-tenant evidence; the 5 zero-run registered codes stay the named demo-completeness ride-along candidate, OQ-W7C-5); the **slot-zero option** (an HG-2-sized opener precedes BT-3 only if TIPPED debt surfaces — none carried in). Then the **Wave-8 close review**. Delivery runs under the full-autonomy grant; the USER signs Tier-3 decisions. **WAVE 8 CLOSED + RATIFIED 2026-07-19→20** (`wave_8_close_review.md`; the fourth consecutive zero-shipped-defect close) — all three slices DONE; Wave 9 ratified below.

## Part 2.12 — Wave 9: the ratified sequence (2026-07-20 at the Wave-8 close, OQ-W8C-3 — the **read-surface + front-end + identity** wave, funding the UI-is-core premise; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist)

**The organizing fact (from the Wave-8 close, Parts 3–4):** the private-capital + backtest machinery is now BUILT but not yet LEGIBLE — the governed read surface is strictly run-centric (F1: 13 families read only by run/result id; CC-2 shipped the FIRST latest-resolver as the template), the governance story is the least-readable part of the API (F2), the FE types are hand-mirrored and drifting (F3), and identity is a dev-placeholder shim (F4). Wave 9 makes the differentiator legible to a screen AND an agent (thesis §2.3).

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **API-1 — the governed read surface + the F2 governance reads** ✅ **DONE** (planning **PR #81**; impl `api-1-impl` tip `0288f29` via **PR #82** = `ae588a5`, **CI green run #449**; NO migration) | Back-filled entity/time-centric reads across the **8 entity-native families** (Class A: perf ×3, risk-backtests ×2, factor-exposure, proxy-weight, exposure — the shared `calc/reads.py` helper factoring the CC-2 `list_pacing_projections`/`latest_pacing_projection` template) + **Class-B whole-run/curve latest-run resolvers** (covariance — no entity filter; scenario — `scenario_definition_id`; sensitivity — curve selector) + the **2 by-id parity GETs** (`scenario-results/{id}`, `proxy-weight-estimates/{id}`) closing the house-pattern asymmetry. The **four F2 governance reads**: validation findings/evidence detail (`GET /models/{id}/validations/{vid}`, gated `model.inventory.view`); `tier`+`validation_status` added to the inventory list DTO; a snapshot listing (`GET /snapshots?purpose&as_of_valuation_date`, gated `snapshot.view`, headers only — no portfolio filter, the API-1b scope-column gap); the **audit-trail read** — a NEW read-only `audit/queries.py` (the FROZEN `audit/service.py` NEVER imported) + `api/audit.py` router (`GET /audit/events`, filtered + paginated, gated the RATIFIED `lineage.view` reuse — no new R-07 mint; metadata-only DTO, `before_value`/`after_value`/`justification` deliberately NOT surfaced). **PURE read-only: no migration, no new writes, no permission-posture change.** **RIDER PAID (OQ-W8C-4 / OQ-W7C-5): demo stage 10** executes the 5 registered-but-never-run codes (sensitivity/active-risk/scenario/benchmark-relative/proxy-exposure) — RUNS-ONLY, counts move **20/35/96 → 20/35/101** — so the new reads render non-empty; the stage-10 prereq chain (a captured curve; one benchmark head carrying BOTH membership + a DIETZ-aligned return series; a scenario definition + shocks) resolves BEFORE any run write, factor-set-matches the covariance run to `{FX_USD,FX_EUR}`, and scopes the proxy run to the campaign's currency-only DEMO-GLOBAL book (the family admits currency factors only). **The flagship VaR/active-risk entity read is DEFERRED to API-1b** (verifier F1 HIGH: subtree-scoped runs, no recorded root portfolio — not read-only-resolvable; user-ratified 2026-07-20). **4-finder review: ZERO HIGH from any lens** — doctrine/security CLEAN (all 6 hard invariants independently verified: `audit/service.py` byte-unchanged + never imported, `lineage.view` reuse only, every new read tenant-scoped + metadata-only, zero new mutating routes); ONE MED caught by two finders (the audit `_iso` reader didn't mirror the FROZEN writer's naive-datetime-as-UTC guard — a real off-UTC compliance-window bug, now fixed + regression-tested) + one MED test-coverage gap (exposure + perf desmoothed/benchmark-relative HTTP reads shipped untested — closed with real-data + smoke tests) + one LOW dead constant, ALL folded. Battery: `make check` **1736**; fresh-schema full-PG battery green (all TEN demo suites in CI order, `stage9z` filename collating last — the CC-2-recorded stage10 zero-pad hazard); downgrade-base smoke clean with stage-10 rows present. | M/L |
| 1b | **API-1b — the flagship VaR/active-risk entity reads** (the named fast-follow; OD-API-1-H) | "Latest VaR / active-risk for portfolio P" — the single most-wanted UI/agent read. Needs ONE additive column `calculation_run.scope_portfolio_id` (the ROOT portfolio a run was scoped to, binder-stamped at run creation; historical runs stay NULL, honestly unresolvable), then the Class-C `list`/`latest` reads resolve `var_result`/`active_risk_result` → `scope_portfolio_id == P` (correct for hierarchical books). Read + ONE small migration + a bounded exposure/var/active-risk binder touch; run-id reads + TR-09 untouched. Deferred ONLY because it needs the scope column — not dropped. | S/M |
| 2 | **FE-2 — OpenAPI-generated FE types** ✅ **DONE** (planning + impl on `fe-2-impl`, **PR #84** = `2ce9e4a`, **CI green run #456**; NO migration) | **Precondition CONFIRMED**: every governed response decimal already serializes as `string` (zero response `*RowOut` fields are `number`; only the ~50 unconsumed `*In` request bodies carry the Pydantic-default `number\|string` union) — so codegen introduces NO `Number()` corruption risk on the read path. Pipeline: `scripts/dump_openapi.py` (deterministic `app.openapi()` dump) → `openapi-typescript` → committed `openapi.json` + `api-types.d.ts`, with a NEW dedicated CI job **"API type drift"** (Python+Node — the Frontend job is Node-only) that regenerates + `git diff --exit-code`s, turning backend↔FE drift RED. `FAMILY_ROW_COLUMNS` column keys **bound** to the generated `*RowOut` types (`keyof FamilyRowOut[F]`) — **the FL-1 kill, PROVEN**: a bogus column key fails `tsc` TS2322. The GET-only hand-written `apiGet` client is **KEPT** (no `openapi-fetch`/orval — preserves the read-only fence, no new runtime dep). OD-FE-1-G superseded-in-part (dated); the strings-verbatim decimal contract (OQ-FE-1-7) mechanized as a compile-time guard. **4-finder review: 1 HIGH folded** — the adversarial finder proved the initial 8-field decimal guard sampled too narrowly (~35 displayed decimals unguarded; a `VarRowOut.sigma`→`number` regression compiled clean, reopening the exact FL-1 corruption class) — fixed with an **EXHAUSTIVE** guard (`OnlyCountsAreNumbers` over all 14 `*RowOut`: the only `number` fields anywhere may be known integer counts), proven to bite on the exact regression scenario. Also folded: 1 MED (disclose `openapi-typescript`'s dev-tree `@redocly` advisories — 3 HIGH, dev-only, CI production audit stays 0, codegen-time-only/never-shipped) + 2 LOW doc corrections. The determinism risk (would CI's Python 3.12 regenerate byte-identically?) was **discharged** by reproduction (pinned pydantic/fastapi + `sort_keys` + `npm ci` → byte-identical). Battery: `make fe-check` green (typecheck/lint/format/65 tests/build); `make gen-api-check` no drift; bundle byte-identical (types-only, tree-shaken). | S/M |
| 3 | **SSO-1 — real identity (OIDC / AD-007)** ✅ **DONE** (planning + impl on `sso-1`, **PR #86** = `422c164`, **CI green run #466**; NO migration) | Replaced the dev-placeholder header shim with a real **OAuth2 resource server**: `get_principal` dispatches on `auth_mode` — `oidc` (the new default, fail-closed) verifies an `Authorization: Bearer` JWT against the issuer JWKS (PyJWT; RS256-only allow-list — `alg=none`/HS-confusion rejected; `iss`/`aud`/`exp`/`nbf` + required subject+tenant claims; `leeway=60s`; optional `acr`/`amr` MFA assertion) and resolves the verified `sub`→`app_user.external_subject` within the verified tenant claim, building `Principal(user_id=app_user.id, ...)` — NOT the raw `sub`, since `has_permission` joins on `app_user.id`. `dev_header` (the old shim) survives as an explicit fallback, permitted **only** when `app_env=='local'`, enforced by a fail-closed startup guard (`validate_auth_config`, run from the FastAPI lifespan — never at import, which would explode CI at collection) plus a per-request backstop. Enforcement behind the shim (`require_permission`, FORCE RLS) was **untouched** — a bounded identity-swap, not a rebuild, exactly as scoped. Closes **OD-048** (local-dev OIDC provider = **Keycloak**, `infra/keycloak/`, profile-gated compose + realm export + runbook, NOT a CI dependency — CI verifies with locally-signed RS256 test keys). Demo `_seed_principals` sets `external_subject` on the two demo principals (a column set only — counts held at 101 runs). **4-finder review: 2 HIGH folded.** (1) **RLS false-deny**: the `tenant_isolation` policy compares `tenant_id::text` (PostgreSQL renders a uuid lowercase-hyphenated) against the RAW `app.current_tenant` GUC — a valid-but-non-canonical UUID tenant claim (uppercase, no-hyphens) was RLS-hidden, silently locking out a legitimate user. Fixed by canonicalizing `str(uuid.UUID(claims.tenant))` before the GUC bind; a NEW PG-tier regression (`test_oidc_tenant_canonicalization_pg.py`, run under a constrained non-superuser/non-BYPASSRLS role) proves the raw form is hidden and the canonical form resolves — SQLite cannot catch this class (RLS is a no-op there). **`_principal_from_token` is the first caller ever to feed `set_tenant_context` a raw externally-controlled string** — every prior caller sourced the tenant from a DB round-trip, always already canonical. (2) **CI drift**: the new `authorization` header dependency is stamped by FastAPI into every operation's OpenAPI schema (231 param blocks), staling the committed `openapi.json` + generated FE types and turning FE-2's own "API type drift" job red — regenerated and committed; `make gen-api-check` clean and deterministic on re-run. Also folded: **4 MED** (require `OIDC_AUDIENCE` in oidc mode — an unset audience let a token minted for a different client of the same issuer authenticate, a confused-deputy gap; a pathological nested `amr` claim crashed the MFA check to 500 instead of denying; `oidc_require_mfa` without `oidc_acr_values` booted clean then 500'd every request — now caught at startup; the oidc `app_user` resolution had zero Postgres-tier RLS coverage before the fix above) + several **LOW** (the `dev_header` per-request `app_env` backstop; the advertised `exp`/`nbf` clock-skew leeway was actually zero — now a real 60s; a JWKS/discovery outage returned 500 — now an honest 503; new tests for the lifespan wiring, `build_verifier`/discovery, `amr` acceptance, and the demo seed). Battery: `make check` **1778 passed** (+15 auth tests over pre-fold); full-PG demo battery green (demo tenant holds at 101 runs); `make fe-check` green (65 tests + build) after the OpenAPI regen; `make gen-api-check` clean. **Lesson: any code path that arms a Postgres RLS tenant GUC from an externally-controlled string must canonicalize it first — the RLS policy's `::text` cast and the raw GUC string are not guaranteed to agree in format even when they represent the same UUID.** | M |
| 4 | **FE-3 — the product UI** ✅ **DONE** (planning + impl on `fe-3`, **PR #88** = `a0f31b5`, **CI green run #475** (Actions run id 29852439513); NO migration) | The first surface a non-developer sees. Replaces the generic run browser with a six-step **governance-narrative walk** over the living demo book (Capture → Exposures → Numbers → Backtest → Validation → Limitations — OD-FE-3-A, the IA ratified at planning as OQ-FE-3-1, the Tier-3 USER decision deferred from the Wave-8 close). The differentiator lives in `GovernedValue` (OD-FE-3-C): every number renders with value verbatim + run/model provenance + a structurally-unfakeable `snapshotVerified` mark (only shown when a real `snapshotId` is present) + the model's validation badge and disclosed limitations. VaR is shown honestly as the seeded run series across all books with the API-1b entity-read gap stated inline — no synthetic "latest for this portfolio." Every pane degrades gracefully on a 403 to a calm permission note (OD-FE-3-E), proven live against a demo `auditor_3l` read-only viewer principal (OD-FE-3-H, folded pre-review). The GET-only `apiGet` fence and the FE-2 generated-type binding are KEPT and extended. **4-finder review: ZERO HIGH** — both existential invariants proven independently (decimal-strings-verbatim on every new screen, incl. the EXHAUSTIVE guard extended to `PositionOut`/`ValuationOut`; no fabricated verification/verdict, structurally prevented not just avoided). Folded: **2 MED** (RunDetail's "back to runs" link stale after FE-3 moved `/` from the run list to the walk overview; `useModelValidations` took the FIRST validated model version instead of the LATEST — would have shown a stale outcome for a re-versioned + re-validated model, the exact lifecycle the Validation step narrates) + **6 LOW** (the decimal-guard extension, a WCAG 2.4.7 visible-focus fix, non-unique-key→index-key fix on limitation lists, 3 honesty wording fixes). Battery: `make fe-check` green (97 tests+build); `make check` (Python) green; `make gen-api-check` clean (FE-only); `package.json` byte-identical (no new runtime dep, OD-FE-1-F held). **One post-review fix before merge**: the fold commit's re-wrapped object literals/JSX across 9 files hadn't been re-run through Prettier, so CI's `format:check` went red — a pure-reflow fix (`3275741`), zero semantic change, all gates re-verified green. Closed Wave 9. | L |

**Ratified riders (OQ-W8C):** the API-1 5-code demo-completeness rider (OQ-W8C-4) — **PAID by API-1's demo stage 10**; FE-3's IA as a deferred Tier-3 USER decision (OQ-W8C-5) — **RATIFIED at FE-3 planning as OQ-FE-3-1**; the two planning-gate caveats (API-1 scope split — resolved: Class C deferred to API-1b; FE-2 decimal-as-string — **CONFIRMED, resolved**). Carried debt into the Wave-9 close register: the BT-3 D-F4 registered-string reword (a dedicated ES/var-backtest next-touch, OQ-W8C-2); the FE-2 dev-tree `@redocly` advisory disclosure; the SSO-1 `pyjwt`/`cryptography` advisory surface — the first Python runtime deps with one, no `pip-audit` CI gate exists to check them against; the FE-3 `auditor_3l`-template read-gap (a demo-only viewer principal, not a general onboarding template — flag if a second read-only role is ever needed). The no-FE-expansion rule LIFTS for this wave (it IS the FE wave). **SSO-1 DONE 2026-07-21** (F4 discharged); **FE-3 DONE 2026-07-21** (see the slice-4 row above) — **WAVE 9 FUNCTIONALLY COMPLETE, all four slices shipped**. **WAVE 9 CLOSED + RATIFIED 2026-07-21** (`wave_9_close_review.md`, OQ-W9C-1/2/4/5 "Approve all" + OQ-W9C-3 fork A; the FIFTH consecutive zero-shipped-defect close). Next = **Wave 10 (Part 2.13): API-1b → FE-3b → a §2.1 private/public-unification headline**.

## Part 2.13 — Wave 10: the ratified sequence (2026-07-21 at the Wave-9 close, OQ-W9C-3 fork A "finish the surface first" — **complete the read-surface + identity product Wave 9 opened, then open the §2.1 destination**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist)

**The organizing fact (from the Wave-9 close, Part 4):** Wave 9 made the differentiator *legible* (F1–F4 discharged; governance readable to a screen AND an agent, thesis §2.3) but left **two named legibility holes** — the flagship "latest VaR/active-risk for portfolio P" read still 404s (the deferral was a verifier *refutation*, not a punt), and no browser can actually *log in* (the demo rides `dev_header`). Wave 10 finishes the surface, then pivots to the §2.1 private/public-unification destination the whole math roadmap declared itself the substrate for.

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **API-1b — the flagship VaR/active-risk entity reads** ✅ **DONE** (OD-API-1-H, the named fast-follow; planning + impl on `api-1b-planning`/`api-1b-impl`, **PR #92** = `f1e830f`, **CI green run #488**; migration `0046_run_scope_portfolio`) | "Latest VaR / active-risk for portfolio P" — the single most-wanted UI/agent read, resolved at the **write** boundary (API-1's verifier had refuted read-only resolution). **ONE additive column `calculation_run.scope_portfolio_id`** (nullable; the ROOT portfolio a run was scoped to) threaded through the single `create_run`/`execute_governed_run` choke point and stamped by all five binders — `run_exposure` from its direct `portfolio_id` arg, `run_factor_exposure`/`run_var`/`run_var_historical`/`run_active_risk` copying it forward from their resolved upstream run (proven correct in BOTH the build and snapshot-consume input paths). The Class-C `list`/`latest` reads resolve `var_result`/`active_risk_result` → `scope_portfolio_id == P` via the EXISTING `calc/reads.py` helper (zero helper change); `active_risk`'s native `benchmark_id` filter also lands. Historical-NULL honesty (OD-API-1b-D): a snapshot-consume-rooted chain (exposure OR factor) stays NULL and is disclosed-unresolvable, not back-fabricated (OQ-API-1b-1 = A, ratified) — the fully build-in-request chain (demo/UI/default-API) always stamps a real root. **Riders (OQ-W9C-4/5, both landed here):** a `pip-audit` CI gate on the INSTALLED ENVIRONMENT (review-corrected from `-r requirements-dev.txt`, which missed the `python-multipart` runtime dep — true npm-parity) + a closure-discipline docs-check (filename-keyed, row-anchored; fails on a DONE-in-roadmap record still reading "DRAFT for ratification" — teeth for the 5th-consecutive stamp miss, unit-tested to actually FIRE). **Pre-ratification verifier pass RAN**: the crux (copy-forward correctness) + TR-09 neutrality + migration neutrality + read non-shadowing all HELD; 2 COMPLICATED findings folded as wording/spec fixes (the exposure-snapshot 2nd NULL-origin; the closure-check mechanic). **4-finder review: ZERO HIGH** — write-path (copy-forward correct across all 5 binders + both paths, immutable, TR-09-neutral, complete), doctrine/security (all 6 hard invariants held; the cross-tenant probe confirmed `scope_portfolio_id` is NOT a security boundary — RLS + explicit tenant filter double-bind it), read-correctness (filters/latest-resolution/route-ordering all correct; zero shadowing), CI-riders+honesty (found the real pip-audit target hole + the untested closure-check teeth). Folded 5 MED + 1 LOW: the pip-audit target fix; the closure-check's teeth were untested (added a failure-path test proving it FIRES); the closure-check's guarantee was over-scoped in its own comment (corrected to the go-forward cadence); the record's "`/latest` 404" wording was a mis-cite (the shipped list-shaped `/latest` returns `[]`, matching the covariance/sensitivity/factor-exposure/var-backtest siblings — pacing's 404 is for a single-object resolver); the copy-forward tests strengthened from non-null to value-equality. Battery: `make check` green (Python); `make fe-check` green (97 tests+build); `make gen-api-check` clean (4 new paths, deterministic); full-PG affected-family battery green; `0046` downgrade/upgrade smoke + `alembic check` clean; `pip-audit` (installed env) clean (one disclosed allowlist: `PYSEC-2026-1845`, a dev-only pytest advisory — `pyjwt`/`cryptography`, the identity surface, audit CLEAN and are NOT ignored); `pydantic-settings` bumped 2.14.1→2.14.2 clearing a real advisory the gate surfaced. Counts UNCHANGED (17/20/35/101 — pure read + one scope-provenance column, no new runs/codes/records). | S/M |
| 2 | **FE-3b — the SPA OIDC/PKCE browser login** ✅ **DONE** (the named fast-follow; planning + impl on `fe-3b-planning`/`fe-3b-impl`, **PR #95** = `2cbb68c`, **CI green**; NO migration) | Turns SSO-1's real resource server into something a non-developer can actually reach: the hand-rolled browser auth-code + PKCE flow (Web Crypto, zero new runtime dep — OQ-FE-3b-1=A, OD-FE-1-F held) against the Keycloak `irp-frontend` public client, sending `Authorization: Bearer <token>` instead of the `X-User-Id`/`X-Tenant-Id` dev-header shim. A discriminated-union `Session = DevSession | OidcSession` (`kind` discriminant) swaps the wire bytes at the single `client.ts` choke point; `DevSession → Session` widened across 16 pass-through signatures (type-only, mechanical). `/callback` lands ABOVE the session gate: validates `state` (CSRF) before the exchange, strips `?code` from the URL synchronously, single-use verifier+state, StrictMode ref-guard. `dev_header` mode preserved unchanged (`DevBanner` shown only there — a verified Bearer session hides it, since showing "unverified" over it would be a lie). Demo end-to-end (OQ-FE-3b-3=A): the `demo-auditor` realm user + BR-10-marked local passwords + the compose backend oidc-profile env INCLUDING `OIDC_JWKS_URI=http://keycloak:8080/…/certs` (the issuer-consistency fix a naive `ISSUER`/`AUDIENCE`-only wiring would 503 on every login). **4-finder review folded 1 HIGH** (the deployed nginx image shipped no SPA history-fallback, so the `/callback` redirect 404'd — the ratified demo login could not complete; fixed via `infra/docker/frontend-nginx.conf` with `try_files … /index.html` + a backend read-proxy for the 13 governed prefixes) **+ 3 MED** (the OIDC logout redirect-back mismatch; `beginLogin` untested — a silent PKCE downgrade would have slipped; the stale "~10 files" footprint claim) **+ LOWs** (a non-secure-context guard; a `no-referrer` meta tag). Gates: `make fe-check` green (tests + build); `make check`/`make gen-api-check` no-op (NO backend logic change); `package.json` byte-identical (zero new runtime dep); `npm audit --omit=dev` 0. Counts UNCHANGED (17/20/35/101). | M |
| 3 | **The §2.1 private/public unification** (the headline; name minted at planning) — ✅ **ALL 3 ARC SLICES DONE** ✅ **PPF-1** (**PR #98** = `9d64b49`, **CI green**; migration `0047`) · ✅ **PPF-2** (planning **PR #100**; impl **PR #101** = `7aefd1c`, **CI green run #519**; the 19th governed number `risk.covariance.private`; reuses `covariance_result`, **NO migration**) · ✅ **PPF-3** (planning **PR #103**; impl **PR #104** = `633e855`, **CI green, all 6 jobs**; migration `0048_var_private_variance`; the **20th governed number**, `risk.var.parametric_unified`; counts 22/37/104 → **23/38/109**) | **THE §2.1 DESTINATION IS SHIPPED.** The differentiator's endgame — the unified public+private **portfolio-level** risk number — closed the 3-slice arc: **PPF-1 = the pure-private factor RETURN series** (the 18th governed number, `risk.factor_return.pure_private`) → **PPF-2 = the private covariance block Ω_pp** (the 19th; a fail-closed SIBLING reusing the generic covariance kernel + the shared `covariance_result` table, `frequency=APPRAISAL`/`run_type=COVARIANCE_PRIVATE`; block-diagonal-with-Σ disclosed as an APPROXIMATION) → **PPF-3 = the unified number** `σ_unified = √(x'Σx + p'(Ω_pp/d_t)·p + residual_over_non-private-members)`, its own binder `run_var_unified`, a REPARTITION (not a naive assembly — two pre-ratification verifiers refuted the naive additive formula as a variance double-count). **4-finder review: 1 HIGH folded** — three finders independently converged on a consume-path re-opening of the double-count (the builder's repartition exclusion wasn't re-enforced at the `snapshot_id` trust boundary; now refused on both paths) — **+ 2 MED** (a held-segment off-diagonal completeness gate, parity with the public leg; a disclosure-honesty overstatement in a REGISTERED model assumption + the governed validation record, corrected — the unified number's value over total-VaR is the Ω_pp OFF-DIAGONAL cross-fund co-movement PLUS a smaller diagonal re-estimation, not the off-diagonal alone). Demo stage 13 (`DEMO-UNIFIED-PPF3`, two existing private funds, zero new instruments/segments/Ω_pp) proves the headline live: σ_unified=85.758098 ≠ σ_total=85.685795, unified `residual_variance` EXACTLY 0 vs total's 155.35 — the double-count fix end-to-end. Full 4-finder review per slice. | L |

**Ratified riders / carries (OQ-W9C):** the **Python `pip-audit` CI gate** (OQ-W9C-4) and the **closure-discipline CI docs-check** (OQ-W9C-5, teeth for the 5th-consecutive missing-stamp class) — **BOTH LANDED on API-1b, 2026-07-21** (see the dated log below; the pip-audit gate audits the installed environment, review-corrected from a `-r requirements-dev.txt` scan that missed a runtime dep). Carried standing: the BT-3 D-F4 registered-string reword (a dedicated ES/var-backtest touch, OQ-W8C-2); the FE-2 `@redocly` dev-tree advisory (dev-only, disclosed, no action); the FE-3 `auditor_3l` demo-viewer (demo-scoped; promote to an onboarding template only if a second read-only role is genuinely needed); the two Wave-9 LOW completeness residuals (the JWKS live-IdP integration test → naturally FE-3b territory; the stage9z presence-not-precision read proof); MG-2 remediation-lifecycle (erosion, earliest overdue 2027-07-19); the first scheduler; OD-B expire-a-mapping. **API-1b DONE 2026-07-21** (see the slice-1 row above). **FE-3b DONE 2026-07-21** (see the slice-2 row above) — **WAVE 10 SLICES 1+2 COMPLETE**. **PPF-1 DONE 2026-07-22** (see the slice-3 row above) — the §2.1 headline's arc slice 1 of 3 (the pure-private factor return, the 18th governed number). **PPF-2 DONE 2026-07-22** (see the slice-3 row above) — arc slice 2 of 3 (the private covariance block Ω_pp, the 19th governed number). **PPF-3 DONE 2026-07-23** (see the slice-3 row above) — arc slice 3 of 3, THE CAPSTONE (the unified public+private VaR, the 20th governed number) — **WAVE 10 FULLY COMPLETE, all three slices shipped, THE §2.1 DESTINATION THE WHOLE PLATFORM THESIS POINTED AT IS NOW SHIPPED**. Carried forward from FE-3b: `directAccessGrantsEnabled:true` on the public `irp-frontend` client (ROPC open, pre-existing SSO-1 realm, used by the README curl example — consider disabling post-demo). **The user's standing note holds: real-data use + demos come AFTER the build — the §2.1 unification is a build-first slice, not a data-ingestion project.** **NEXT = the Wave 10 close review** (the mandatory Part-4 rule-2 re-baseline).

## Part 2.14 — Wave 11: the ratified sequence (2026-07-23 at the Wave-10 close, OQ-W10C-4 fork A "OPERATIONALIZE" — make the governed engine RUN, not just compute; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist, now with the OQ-W10C-broadened CI teeth)

**The organizing fact (from the Wave-10 close, Part 4):** the §2.1 destination is shipped, so the platform's largest distance-to-frontier is no longer *methodology* — it is that a governed engine which computes 20 numbers beautifully **cannot run on a cadence, enforce a limit, or carry a remediation to term**. Wave 11 builds that operational half — the first thing in the platform that is not request/response reuse.

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | ✅ **SCH-1 DONE** (PR #108 `96965cf`, CLOSED 2026-07-23) — **the first scheduler** (the prerequisite primitive) | Background/cadenced execution: nothing in the platform currently runs unless hand-invoked, so "current risk" is only as fresh as the last manual run. The FIRST genuinely-new architectural primitive (not a request/response reuse) — a governed, auditable, tenant-scoped scheduled-run mechanism that re-runs a governed number on a cadence and records provenance. A prerequisite for limit monitoring (needs fresh numbers), remediation deadlines, and any future data refresh. Design forks at planning: the execution surface (the existing worker vs a new one); at-least-once vs exactly-once semantics + idempotency against the governed-run scaffold; how a scheduled run binds its snapshot (re-pin fresh vs carry-forward); the cadence/calendar model. External practice to cite: BCBS 239 risk-data timeliness, SR 11-7 ongoing-monitoring cadence. | M/L |
| 2 | ✅ **LIM-1 DONE** (PR #111 `218afc9`, CLOSED 2026-07-23) — **limits / breach workflow** (the most conspicuous missing product capability, RTM-P6) | A governed limit entity + breach detection over the numbers SCH-1 keeps fresh: "alert me when VaR (or active risk) breaches X." The first governed *write-side workflow* the platform has never had — a `limit_definition` (ENT-031, EV, threshold + scope + metric-selector + `breach_direction` + HARD/SOFT), a deterministic breach evaluation (a PHASE of the per-tenant operational tick, over pinned `calculation_run` results — Fable demand #2), and a self-describing IA append-only `breach` record (ENT-033, FKs the evaluated `calculation_run_id`). R-07 mint `limit.manage` (2L-maker SoD) / `.view` / `breach.view`; migration `0050`; NOT a governed number (counts UNCHANGED 23/38/109). Ratified: OQ-1=A (EV header + self-describing echo; history via `LIMIT.CHANGE`) / OQ-2=A (exact `scope_portfolio_id`) / OQ-3=A (VAR/`var_value` + ACTIVE_RISK/`te_value`, hardcoded map + fail-closed unit assert) / OQ-4=A (DETECT+record+read; lifecycle + `LIMIT.APPROVE` deferred to MG-2). 4-finder ZERO HIGH, 6 MED folded (silent-green fail-open: `limit_health` recomputes from source; precision overflow `(28,6)→(34,12)`; P3-5 cross-tenant FK guard). | M/L |
| 3 | ✅ **MG-2 DONE** (PR #113 `aa6503f`, CLOSED 2026-07-23) — **the breach remediation lifecycle** (the governance-workflow-with-teeth; the eroding register item PAID) | Carry a breach to term: realize the reserved **ENT-034 `breach_action`** (IA append-only) as the **DEP-WFL** 5-state machine `DETECTED→ASSIGNED→RESPONDED(1L)→REVIEWED(2L)→CLOSED` + an orthogonal `ESCALATED`, with **auto-escalation** riding the SCH-1 cadence (a THIRD tick phase, a `response_due < now` deadline check) and the platform's **FIRST person-level SoD** (reviewer/closer ≠ any prior 1L responder — REQ-BRC-002/SOD-02). R-07 mint `breach.respond` (1L) / `breach.review` (2L); migration `0051`; audit codes genesis-reserved → REALIZED; NOT a governed number (counts UNCHANGED 23/38/109). Ratified OQ-1=B (lifecycle now; **LIMIT.APPROVE split to MG-3**) / OQ-2=A (append-only action log, recency state) / OQ-3=A (the transition/role/SoD map) / OQ-4=A (auto-escalate). Pre-ratification verifier folded 3 BLOCKING concurrency holes (nondeterministic recency state → a monotonic `seq`; escalate TOCTOU → a `(breach_id, response_due)` epoch + per-breach `FOR UPDATE`; SoD latest-responder bypass → the all-responders set). | M/L |
| 4 | ✅ **MG-3 DONE** (PR #115 `96679e2`, CLOSED 2026-07-24) — **the LIMIT.APPROVE maker-checker gate** (split from MG-2 at ratification, 2026-07-23; the final Wave-11 slice) | Closed the one letter LIM-1 left formally unmet (`REQ-LIM-001`/`BX-SOD` "limit **changes** are maker-checked"): a DRAFT→ACTIVE limit lifecycle gated by a distinct approver. `create_limit` now yields **DRAFT** (not evaluated); `approve_limit` transitions DRAFT→ACTIVE, human-only (BR-15), approver ∉ the maker SET `{created_by, updated_by}` (SOD-02), from-state re-read under a `FOR UPDATE` lock. **Ratified OQ-5=A: gate CHANGES too** — a material governing-field edit to a live limit auto-demotes it to DRAFT for a non-editor re-approval (the full requirement, not just creation); OQ-2=A person-level SoD on the same `risk_manager_2l`. Mints `limit.approve` (R-07); realizes the genesis-reserved `LIMIT.APPROVE` code; **NO migration** (code-only — `0002` re-seeds from the live bootstrap catalog; head stays `0051`); NOT a governed number (counts UNCHANGED 23/38/109). Pre-ratification verifier folded 3 blocking holes (no-migration / stale-read-under-lock / create-side bypass); **4-finder: 1 HIGH (3 finders converged — suspend→edit→resume + no-op-status bypassed the change-gate) + 4 MED folded** (update_limit TOCTOU; no-op-resave demote; SoD last-editor-only → the maker SET; audit `checked_makers`). | S/M |

**Ratified riders / carries (OQ-W10C):** the **PPF-3 v2 seams (leverage-led)** — the destination number's disclosed refinements, deferred as the fork-B alternative (leverage is the one load-bearing math gap but needs new captured leverage data; a strong candidate for a later wave or a real-data slice, per the user's standing "real-data after the build" note). Carried standing: the BT-3 D-F4 registered-string reword (OQ-W8C-2); the FE-3b `directAccessGrantsEnabled:true` ROPC hygiene (disable if anything goes internet-facing); the demo-only `covariance_result` factor-set read (LOW, cannot leak Ω_pp); OD-B expire-a-mapping; SC-2 (expired unspent again); the `@redocly` dev-tree advisory (no action); the `auditor_3l` demo-viewer (demo-scoped); the JWKS live-IdP integration test; the stage9z presence-not-precision proof. **Process (OQ-W10C-5):** a mechanical governance gate must be unit-tested against the ACTUAL failure forms it exists to catch (the closure-teeth were broadened + tested this close after a sixth recurrence); a slice's closeout must stamp its own decision record CLOSED (now CI-enforced). **SCH-1 DONE + CLOSED 2026-07-23 (PR #108 `96965cf`, CI green; OQ-1=B infra-driven dispatch, app 100% non-BYPASSRLS). A post-close independent FOUNDATION AUDIT (Fable, 2026-07-23) judged SCH-1 as the base for LIM-1/MG-2: verdict SOUND — proceed straight to LIM-1, NO SCH-1b shim.** Findings: the ledger shape is right (LIM-1 must discover results via `calculation_run`/`calc/reads.py`, NOT `scheduled_run` — so MANUAL runs are limit-checked too; `scheduled_run` is provenance-only, correctly); MG-2's expected-tick set is reconstructible arithmetically from the pure `current_tick` + the frozen grid (no row-per-expected-tick needed); OQ-1=B generalizes via ONE per-tenant operational tick; the "family-agnostic" claim is true of the MECHANISM (tick math, idempotency, SAVEPOINT isolation, EV/IA split) but not the SCHEMA (NOT-NULL `scope_portfolio_id` unrepresentable for tenant-global families; VaR-named resolved-run columns; the inline dispatch body needs a registry) — all additive-fixable, DEFERRED to whichever slice first ships family 2. **Four audit demands LIM-1 planning MUST carry: (1) breach discovery via `calculation_run` reads, breach records FK the evaluated `calculation_run_id`, idempotency = LIM-1's own IA `(limit_id, calculation_run_id)` unique key; (2) breach evaluation is a PHASE of the same per-tenant tick, NOT a `Schedule` row (no `SCHEDULABLE_RUN_TYPES` expansion; no model_version to bind) — ratify `run_scheduler_for_tenant` → the single per-tenant operational tick as the OQ-1=B evolution rule BEFORE a second CLI entrypoint gets minted (infra wiring is still zero — the wall only materializes if concerns accrete separate entrypoints); (3) notifications assembled per-tenant INSIDE the tick, recorded as IA attempt evidence — never an in-app cross-tenant sweep (OQ-1=B forbids cross-tenant DB reads, not outbound egress); (4) ratify grid-frozen-for-life ("re-cadence = new schedule") as a STANDING invariant, not a v1 convenience — MG-2's expected-tick arithmetic silently depends on it.** **LIM-1 DONE + CLOSED 2026-07-23 (PR #111 `218afc9`, CI green all 6; migration `0050`; all four Fable audit demands discharged). MG-2 RATIFIED 2026-07-23 (OQ-1=B) — the breach remediation lifecycle THIS slice; the `LIMIT.APPROVE` maker-checker gate SPLIT to a new slice MG-3 (the platform's first person-level SoD + a limit-evaluation-path change deserves its own review). Wave 11 is now 4 slices (SCH-1 ✅ → LIM-1 ✅ → MG-2 ✅ → MG-3). **MG-2 DONE + CLOSED 2026-07-23 (PR #113 `aa6503f`, CI green all 6; migration `0051`): the breach remediation lifecycle — ENT-034 `breach_action`, the DEP-WFL 5-state machine, the third auto-escalation tick phase, the first person-level SoD; pre-ratification verifier folded 3 blocking concurrency holes + the 4-finder folded 1 HIGH (vacuous-SoD close path) + 4 MED.** **MG-3 DONE + CLOSED 2026-07-24 (PR #115 `96679e2`, CI green all 6; NO migration, head stays `0051`): the `LIMIT.APPROVE` DRAFT→ACTIVE maker-checker gate — `create_limit` yields DRAFT, `approve_limit` (approver ∉ the `{created_by, updated_by}` maker SET, under a `FOR UPDATE` lock) activates it; RATIFIED OQ-5=A gates limit CHANGES too (a material edit to a live limit auto-demotes to DRAFT for re-approval — the full REQ-LIM-001, not just creation); mints `limit.approve`, realizes the genesis-reserved `LIMIT.APPROVE` code; NOT a governed number. 4-finder folded 1 HIGH (3 finders converged on a suspend→edit→resume / no-op-status change-gate bypass) + 4 MED. **WAVE 11 IS FUNCTIONALLY COMPLETE (SCH-1 ✅ → LIM-1 ✅ → MG-2 ✅ → MG-3 ✅).** A separate additive doctrine landed alongside: **AD-019** (PR #116 `466999d`) — the analytical-plane / Snowflake strategy (hybrid, additive, read-only; PG stays the governed system-of-record; docs-only, no code). **WAVE 11 CLOSED + RATIFIED 2026-07-24** (`wave_11_close_review.md`, OQ-W11C-1/2/3 approved): 4-auditor parallel close audit — all four slices SHIPPED-AS-RATIFIED, **ZERO shipped-code defects (7th consecutive clean close)**, MG-3's change-gate survived an adversarial re-probe with no bypass; register 1 PAID / 11 OPEN / 0 TIPPED; cross-slice integration COHERENT (+ the anti-laundering regression test added). **WAVE 12 RATIFIED (Part 2.15, OQ-W11C-1=A "Operations, Reachable"): Limit/Breach/Approve API → notification → cadence wiring → operations UI** — make the latent engine reachable before new math. **NEXT = Wave-12 slice 1 planning** (the Limit/Breach/Approve API — pays MG-3's `actor_id` canonicalization + the `create_schedule` FK guard at the auth boundary).

## Part 2.15 — Wave 12: the ratified sequence (2026-07-24 at the Wave-11 close, OQ-W11C-1=A "OPERATIONS, REACHABLE" — make the Wave-11 operational engine reachable, self-driving, and demonstrable BEFORE adding new math; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist + the new closeout control-matrix sweep)

**The organizing fact (from the Wave-11 close, §4):** Wave 11 built the operational engine block — scheduler, limits, breach lifecycle, person-level maker-checker — but left it LATENT: no deployed cadence turns the tick (`apps/worker/.../main.py` is still a placeholder), no API/UI reaches any of it (22 routers, none governance-operational), and no notification path exists. A supervisor can't see open breaches or prove the CRO was alerted. Wave 12 builds the consumption surface — the ignition, the dashboard, and the alarm.

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | ✅ **API-2 DONE** (impl **PR #120** `20da275`, CLOSED 2026-07-24; NO migration; counts 23/38/109) — **the Limit/Approve API** (the limit half of the slice; **OQ-1=B split: the breach lifecycle surface = API-2b**, the immediate fast-follow) | The first HTTP surface over the Wave-11 controls: limit create(→DRAFT)/PATCH(auto-demote)/approve/suspend/resume + reads (list-by-status ⇒ approval queue, get, health) — thin pass-throughs to the gated service fns, no second write path (`status` never a DTO field; `extra="forbid"`). **PAID: MG-3's MED-3 / Fable-D1 `actor_id` canonicalization** — in the ACTOR DATACLASSES (`__post_init__`, lenient `str(uuid.UUID(x))`) so stamp==compare for every caller and API-2b's breach SoD inherits it; router fail-closes non-UUID principal → 401. Ratified OQ-2=A/OQ-3=A (SoD refusal=409)/OQ-4=A (one-human-one-row doctrine; schema tightening = provisioning-slice precondition). 4-finder: ZERO functional HIGH (the SoD held against every HTTP probe); 1 coverage HIGH + 4 MED folded. **The `create_schedule` P3-5 FK guard + audit OQ-a (worker `--tenant`) were re-scoped at ratification to the cadence-wiring slice** (no schedule endpoint ships here — OQ-W11C-2's "endpoint + guard together" honored by moving BOTH together). | M/L |
| 2 | ✅ **NOTIF-1 DONE** (impl **PR #123** `c025ba0`, CLOSED 2026-07-25; migration `0052_breach_notification`; counts 23/38/109) — **breach notification / alerting** | The in-tick, per-tenant, IA-evidenced egress leg (Fable demand #3). A decoupled audit-stream consumer as tick **phase 4** turns each unnotified `BREACH.DETECT`/`BREACH.ESCALATE` audit event into a durable, hash-chained `breach_notification` (ENT-063) attempt row per (event, recipient) — the SR 11-7 / BCBS 239 "prove the CRO was alerted" leg. OQ-1=A record-first (`LoggingNotificationSink`; a real EMAIL/WEBHOOK channel is a provisioning-coupled v2) / OQ-2=A DETECT+ESCALATE alarms / OQ-3=A recipients = in-tenant `breach.review` holders / OQ-4=B derived-`MAX` cursor / OQ-5=A mint `NOTIFY.DISPATCH` (EVT-270). NO new permission (read reuses `breach.view`); FROZEN audit writer untouched. 4-finder folded 1 HIGH (the derived-`MAX` + continue-past-failure silent-no-notify → `break`-on-failure fail-closed). full-PG 2416/0. | M |
| 3 | ✅ **CAD-1 DONE** (impl **PR #125** `74372b2`, merged `b637bac`, CLOSED 2026-07-25; **NO migration** — head stays `0052`; counts 23/38/109) — **cadence wiring** | **The literal ignition of the whole Wave-11/12 investment: the engine now TICKS.** An in-process **supervisor** (`irp_worker/supervisor.py`) drives `run_operational_tick_for_tenant` (schedules → breach detection → deadline escalation → notification) every `IRP_TICK_INTERVAL_SECONDS` over the configured tenants, with per-tenant fault isolation — retiring the `irp_worker/main.py` heartbeat placeholder. **OQ-1=A** (supervisor loop PLUS the one-shot `scheduler --tenant` kept for external schedulers), **OQ-2=A** (tenant list from config env `IRP_TENANT_IDS` — no DB sweep, no BYPASSRLS; OQ-SCH-1-1=B preserved), **OQ-3=A** (a malformed id is skipped-and-logged so the rest keep ticking; the one-shot fails closed exit 2; an EMPTY list fails closed at startup — no silently-dead engine). **PAYS BOTH STANDING CARRIES: the OQ-a worker `--tenant` canonicalization** (`str(uuid.UUID(x))` before the RLS GUC is armed — SSO-1's 2nd instance, a fail-open) **and the `create_schedule` P3-5 cross-tenant FK guard** (OQ-W11C-2 — `assert_portfolio_in_tenant` + the NEW `assert_model_version_in_tenant`; `environment_id` is a free label, correctly unguarded). Pre-ratification verifier folded 2 (the BLOCKING `test_worker.py` collection break on retirement; the empty-list fail-closed). 4-finder **ZERO HIGH**; M1 (main() fail-closed tests) / M2 (per-tenant consecutive-failure WARN) / L3 / L4 / L5 folded. `make check` 2021/416; full-PG GREEN. | S/M |
| 4 | ✅ **OPS-1 DONE** (impl **PR #127**, merged `8b889ed`, CLOSED 2026-07-26; NO migration; counts 23/38/109) — **the operations UI** | **The first genuinely VISIBLE deliverable and the platform's FIRST frontend write path.** Breach queue · breach detail (identity/arithmetic echo + the remediation timeline + the NOTIF-1 proof-of-alert + the lifecycle actions) · limit health · the approval queue. Ratified **OQ-2=A** (a dedicated write module sharing ONE identity/error core with reads — the read module keeps its no-write property), **OQ-3=A** (refusals rendered as first-class plain-language explanations naming the control; no `/me` endpoint — a visible SoD refusal IS the governance demo), **OQ-4=A** (a demo operations extension so the screens open on real seeded data), **OQ-5=A** (**Assign dropped** — no user-directory endpoint exists anywhere, and under OIDC the FE cannot learn its own `app_user.id`), **OQ-6=A** (the **Tier-3 IA sign-off**: Operations becomes the FIRST nav group, the walk becomes the explainer, and the book chip is scoped to the walk since it is false over a tenant-wide queue). **OQ-1 was ratified as C and REFUTED IN BUILD** (pinning `react-router-dom` below the advisory range re-exposes SIX advisories incl. a HIGH DoS + two REACHABLE open-redirects) → the slice shipped on **OQ-1=A**, so the **`GHSA-qwww-vcr4-c8h2` exception REMAINS LIVE, expiry 2026-10-24**. Pre-ratification verifier folded **6 BLOCKING** holes (the vite/nginx proxy gap that reads as "API unreachable"; stale-seq wire-identical to an illegal transition; the unreachable `expected_seq` token; no refetch; the demo's 403s + DRAFT-never-evaluates; no assignee source). 4-finder folded **4 HIGH + 5 MED** (incl. a live CI break: `fe-check` lacked the `prettier --check` CI runs; the demo's SoD showcase was structurally unreachable; the Approve button fabricated sign-off evidence; the empty queue asserted an unsupported all-clear). | M/L |

**A post-close independent FOUNDATION AUDIT (Fable, 2026-07-24 — the SCH-1→LIM-1 precedent, spent on the use-it-or-lose-it allocation) audited the slice-1 API auth boundary BEFORE planning: verdict GO, conditional on D1–D3 blocking.** Headline: the person-level SoD compares RAW strings while the permission gate compares the same identity case-INsensitively (GUID) — a non-canonical string form of the SAME principal passes `require_permission` but escapes the maker/responder SET → self-approval; fix = ONE canonicalization dependency onto `app_user.id` (D1) + the one-human-one-row provisioning doctrine (D2) + no-second-write-path DTO discipline (D3). **NEW discovery (OQ-a): the worker's raw `--tenant` string is the SSO-1 bug's SECOND instance, silent-FAIL-OPEN** — a non-canonical tenant id makes the tick fire/evaluate/escalate NOTHING while printing a normal summary; a named BLOCKING carry for this wave. **Ten demands (D1–D10) + six gate OQs (a–f) recorded in [`wave_12_api_boundary_audit.md`](wave_12_api_boundary_audit.md) — slice-1 planning MUST carry them as ratification inputs.**

**Carry (2026-07-24, supply-chain): react-router v7→v8 migration.** A new High advisory (GHSA-qwww-vcr4-c8h2, RSC-mode CSRF) landed against `react-router` `>=7.12.0,<8.3.0` and broke the OD-TC-1-D audit gate on `main` (CI #578/#579) on an UNCHANGED tree (npm audit queries a live DB). Dispositioned as a **time-bound, reachability-justified exception** (`audit-allowlist.json` + `scripts/check_frontend_audit.mjs`, review_by 2026-10-24): the app is a client-only Vite SPA using the declarative router API (no RSC, no data-router actions) so the vuln cannot execute. The real fix is a breaking v7→v8 migration (import restructure `react-router-dom`→`react-router`; `react-router-dom@8` does not exist) — **fold into the Wave-12 Operations-UI slice (slice 4) or a dedicated FE-hardening slice before the exception expires.**

**✅ API-2b DONE** (impl **PR #121** `b3f818a`, CLOSED 2026-07-25; NO migration; counts 23/38/109) — **the breach lifecycle API** (the OQ-1=B fast-follow completing Wave-12 slice 1): `POST /breaches/{id}/assign|respond|review|close` + the batched/paginated queue reads (`GET /breaches`, `/{id}`, `/{id}/actions`). A **3-finder Fable foundation audit** (2026-07-24/25, the use-it-or-lose-it allocation) drove it; TWO blocking pre-fixes landed FIRST: **P1** — the tick×HTTP advisory×row-lock **deadlock** (the tick held the audit-chain advisory lock across breach row locks while HTTP verbs acquire row→advisory → reachable 40P01); fixed by committing phases 1–2 then running phase 3 as per-breach top-level txns (uniform lock order), with `persistent_tenant_context` re-arming the txn-local RLS GUC (else phase 3 silently escalates nothing — the OQ-a fail-open pattern); 40P01→503. **P2** — the **epoch-aware review guard** (`reject→escalate→ACCEPT` could ratify a formally-rejected response; now a current-epoch 1L response is required). Ratified OQ-1=A (REJECT carries the owner) / OQ-2=A (per-observation lifecycle, queue groups siblings) / OQ-3=A (tick restructure) / OQ-4=A (`expected_seq`). 4-finder ZERO HIGH; folds incl. the `BreachAssigneeError`→422 taxonomy + the twice-carried PG-tier case-variance HTTP replay. full-PG 2393/0. **The `create_schedule` P3-5 FK guard + audit OQ-a (worker `--tenant`) remain carried to the cadence-wiring slice (slice 3).**

Then the **WAVE-12 CLOSE REVIEW**. **Named now for later (Wave-13 candidate):** the standing "real-data after the build" note — with the math destination and the operational surface built, running on real data is the natural next credibility step. Credit/liquidity/counterparty families and the PPF-3 v2 leverage seams remain later / real-data-gated. **✅ CAD-1 DONE + CLOSED 2026-07-25** (impl **PR #125** `74372b2`, merged `b637bac`; NO migration; counts 23/38/109) — see the slice-3 row above. **The engine now TICKS:** an in-process supervisor drives the per-tenant operational tick on a real cadence over config `IRP_TENANT_IDS` (OQ-1/2/3=A), retiring the `main.py` placeholder while keeping the one-shot for external schedulers. **BOTH standing carries are now PAID** — the OQ-a worker `--tenant` canonicalization and the `create_schedule` P3-5 cross-tenant FK guard (OQ-W11C-2, the one inventoried register asymmetry) are **CLOSED**. 4-finder ZERO HIGH. **✅ OPS-1 DONE + CLOSED 2026-07-26** (PR #127, merged `8b889ed`) — see the slice-4 row above. **WAVE 12 IS FUNCTIONALLY COMPLETE: API-2/API-2b ✅ → NOTIF-1 ✅ → CAD-1 ✅ → OPS-1 ✅**, all four slices shipped with counts UNCHANGED at 23/38/109 (none was a governed number — the wave made the existing engine reachable, alarmed, running and visible). A follow-on **CI-parity hardening** slice then closed the harness drift a five-reader audit surfaced (six PG-gated suites — incl. the ENTIRE Wave-11/12 RLS/append-only enforcement layer — were never run by CI because its PG tier is a hand-enumerated allowlist while the local gate is a wildcard battery; now added, plus a conformance pin that FAILS if any PG suite is missing from `ci.yml`). **NEXT = the WAVE-12 CLOSE REVIEW** (the mandatory Part-4 rule-2 re-baseline). Its agenda carries: the harness-parity audit results; a **recommendation-before-verification** process rule (OQ-1=C was ratified before the audit gate was run on the downgraded tree); the **React-19 / react-router-8 migration** as its own slice before the 2026-10-24 allowlist expiry; and — **on the user's 2026-07-26 direction** — an **analytics-breadth ratification**: rolling volatility / rolling returns / drawdown are computable from the existing governed return series TODAY, Sharpe needs one captured risk-free series, and sector / industry / geography-of-risk exposure + concentration need reference-data dimensions that arrive WITH real data (so they fold into the Wave-13 real-data onboarding candidate rather than being built twice). **WAVE 12 CLOSED + RATIFIED 2026-07-26** (`wave_12_close_review.md`, OQ-W12C-1/2/3 approved): ten close auditors under ultracode — all five slices SHIPPED-AS-RATIFIED, **zero RUNTIME defects (the 8th consecutive clean close on that axis)**; two guard-layer HIGHs (the OPS-1 write fence bypassable by unlisted import forms; the refusal-detail pin's SoD assertion a provably dead branch) + three MEDs (the phase-2 FK-KEY-SHARE tick×HTTP 40P01 edge with no limits-router 503 map; the closure-teeth blind to the `**Status:**` prose shape; the control-matrix sweep skipped at API-2/OPS-1) **all FOLDED AT THE CLOSE** with executed negative controls; register 5 PAID / 15 OPEN / 3 TIPPED-and-slotted; gates re-verified (make check 2036, full-PG 2463/0, fe-check, downgrade smoke). **WAVE 13 RATIFIED (Part 2.16, OQ-W12C-2=A): RM-1 rolling metrics → SR-1 Sharpe → OPS-H1 operations hygiene → FE-M1 React-19/router-8** — analytics breadth on the governed rails + the deadline debt; **Wave-14 tee = real-data onboarding** carrying the dimensional analytics. **NEXT = RM-1 planning.**

## Part 2.16 — Wave 13: the ratified sequence (2026-07-26 at the Wave-12 close, OQ-W12C-2=A "ANALYTICS BREADTH ON THE GOVERNED RAILS" — the user-directed metrics breadth first, on verified slotting facts, plus the deadline debt; same per-slice discipline incl. the OQ-W12C-3a executed-negative-control rule and the OQ-W12C-3c control-matrix sweep trace)

**The organizing facts (from the Wave-12 close, §4–5, all schema-verified):** the new biggest distance-to-frontier is CREDIBILITY OF THE NUMBERS — analytics breadth vs any commercial platform, then real data through the governed rails. PM-1's `portfolio_return_result` is a dated per-period fully governed series, so rolling metrics need ZERO new capture; Sharpe's risk-free series rides the EXISTING ENT-052 benchmark capture (open `(code, source)` identity, vendor-published TOTAL-basis returns, snapshot-pinnable) or ENT-021 curve points — NOT a new capture family; sector exposure is **0% computable today** (no fixture creates an issuer; no engine reads sector) and industry/country-of-risk columns don't exist — the dimensional metrics genuinely arrive WITH vendor data (Wave 14).

**AMENDED 2026-07-26 at the RM-1 ratification gate (rule 3 insertion).** RM-1's planning surfaced that a month-aligned return series requires exposure runs struck at month-ends, and **nothing on the platform can produce them** — `SCHEDULABLE_RUN_TYPES = {VAR}` and `CADENCE_KINDS = {INTERVAL}`, and an interval trigger cannot land on months of 28–31 days. Shipping RM-1 first would mint a governed number fed only by human discipline, against the platform's own standing doctrine. The USER ratified the operability work INTO scope as a **preceding** slice, **SCH-2**. Verified at ratification: SCH-2 is **not** the "small" slice the gate assumed — `Schedule.interval_days` is NOT NULL (so a calendar cadence needs a migration) and `dispatch_one` hardcodes `run_var` with VaR-specific pinning (so a second family needs the **dispatch registry**). SCH-2 is therefore **the slice that first ships family 2**, discharging the SCH-1 family-agnostic schema/dispatch deferral the Wave-11 close recorded as "DEFERRED to whichever slice first ships family 2" — real debt paid, not new scope.

| # | Slice | What it is | Size |
|---|---|---|---|
| 0 | **SCH-2 — month-end cadence + the dispatch registry** ✅ **DONE** *(inserted 2026-07-26; ratified 2026-07-27; **PR #133** = `8c8c17b`, **CI green run #645**; migration `0053`)* | Makes RM-1 feedable and discharges the SCH-1 family-2 deferral the Wave-11 close parked. Ships: a `CALENDAR_MONTH_END` cadence ticking on the **last weekday** (the QS-11 `preceding` convention over a weekend-only predicate — a pure calendar month-end would fail **30.6%** of real months; the holiday residual is 2.8%, next collision 2027-05-31, full ENT-006 resolution rides Wave-14); `EXPOSURE_AGGREGATE` (**the real run_type**, OQ-8 — not a new short name, avoiding two meanings behind the `target_run_type` column already shipped on `limit_definition`/`breach` and rendered in the OPS-1 UI) admitted to the schedulable set; and the **family dispatch registry** replacing `dispatch_one`'s hardcoded `run_var`. Two NOT-NULL relaxations (`model_version_id`, `interval_days`) under **total-enumeration** CHECKs, with the service validation AND the CAD-1 FK guard both driven off the registry declaration (never off the value — a CTRL-003 fail-open trap). Mints no governed number; model/validation counts unchanged — **but demo stage 15 adds ONE COMPLETED `EXPOSURE_AGGREGATE` run, so the run total is 110, not 109 (corrected at RM-1, measured on a fresh-schema battery; the sole absolute-count pin collates BEFORE stage 15 and never saw it)**. **Size M/L, revised up at the verifier pass**: the fence explicitly includes a **demo stage** (nothing outside unit tests exercises schedules today) and a dedicated **downgrade-body PG suite** (the two-table cascade destroys IA ledger evidence, and CI currently deletes zero rows so the smoke is blind). A burned month is NOT re-firable (OQ-7 = accept + runbook + a minimal read so a failure is visible when it happens) — the read (`GET /schedules`, `GET /schedules/runs?outcome=FAILED`) is gated on the SCH-1-minted `schedule.view` and is that permission's FIRST consumer, **no new R-07 mint**. **4-finder review: 2 HIGH, 8 MED, 5 LOW, all folded.** The HIGHs converged: a hand-written holder-set comment wrong in both directions, and the **DC leak it was hiding** — `auditor_3l` holds `schedule.view` while holding no valuation/position/marketdata view, and the worker wrote raw driver text (statement + BOUND PARAMETERS + PG `DETAIL:` row quote) into `failure_reason`, **write-only until this slice gave it a reader**; now redacted at the write boundary. Also folded: an `OverflowError` escape killing a tenant's whole tick cycle (int32 column vs `timedelta`'s day cap — the ratified B3 class through another door, now a three-layer `MAX_INTERVAL_DAYS` envelope); `produces_run_on_failure` REMOVED as false on the only scheduled path (the pre-create gate is the snapshot BUILD's completeness gate, not upstream resolution — the repo's own shipped test refutes the premise); three guards proven by MUTATION never to fire; and the downgrade suite never asserting the re-tighten (a 0053→0052 rollback would have left `scheduled_run` mutable and both tables cross-tenant readable). **Every new guard was mutation-tested — 4/4 killed their tests.** Two ratified decisions REVERSED BY EXECUTION and recorded at the gate (Part 4b): the `as_of_known_at` pin (building the demo produced an `EmptySnapshotError`; the verifier's motivating two-fires scenario is unreachable under `uq(schedule_id, scheduled_for)`) and `produces_run_on_failure`. Battery: `make check` **2070**; fresh-schema full-PG **2520 passed / 0 failed**; `alembic downgrade base` clean **and destructive for real** — 2 unrepresentable schedules + 2 ledger rows deleted, where the pre-SCH-2 smoke deleted ZERO and went green testing nothing. **Lesson: a decision endorsed by reasoning AND by an adversarial verifier can still be refuted by the first execution — which is the argument for the demo stage being mandatory scope, not decoration.** | M/L |
| 1 | **RM-1 — rolling metrics** ✅ **DONE** (**PR #135** = `b6e7ba0`, **CI green run #652**; migration `0054`) | The 21st governed number family: rolling return / rolling volatility + max drawdown over the governed PM-1 series (P3-8-shaped: result family + methodology doc + registered model; reuses `perf.run`/`perf.view`; rule-7 entity/time reads in-slice). Carries IN-slice: the day-count/irregular-spacing convention (Tier-3), the annualization decision (re-opens the recorded REQ-PRF deferral), a demo-history extension (8 one-day sub-periods is too thin for a demonstrable window), and a REQ mint for drawdown (CAP-5.4 has no realizing REQ row). | M |
| 2 | **✅ SR-1 — Sharpe ratio — DONE + CLOSED 2026-07-28** *(impl **PR #139** = `b86aa28`, CI #663; migration `0055`; ENT-065; counts 24/39/132 → **25/40/133**; OQ-SR-1-1…6; `sr_1_decision_record.md`; sizing revised S/M → **M** — correctly, it ran M)* | The 22nd governed number: capture a risk-free series through the EXISTING ENT-052 benchmark path (a T-bill/cash TR index as just another benchmark; the ENT-021 curve + registered yield→return conversion is the alternative — Tier-3 fork at planning), then the Sharpe calc over the PM-1 series. | S/M |
| 3 | **✅ OPS-H1 — operations hygiene — DONE + CLOSED 2026-07-28** *(impl **PR #141** = `03da139`, CI #669; NO migration; counts UNCHANGED 25/40/133; `ops_h1_decision_record.md`)* | A Part-4 rule-3 insertion (the MD-H1 precedent) paying the fired-trigger cluster from the Wave-12 close register: the `select_overdue_breaches` N+1 bound (the D9 greatest-n-per-group template) + the honest tick-stall statement + a PG regression for the phase-2 FK-KEY-SHARE interleave (M-C1) + demo `_NOW` regenerate-on-seed (retires the OQ-W12C-3d interim rule); plus the recorded LOW cluster — dev_header GUC canonicalization symmetry, the `run_in_tenant`/`tenant_session`/`run_tenant_job` L4 siblings, ops-stage teardown narrowing, a census re-pin after stage 14, the notifications pager (L-7), `client.ts` success-parse placement (L-9). | S/M |
| 4 | ✅ **FE-M1 DONE** (CLOSED 2026-07-28; **PR #143** = `44ee905` + **PR #144** = `bd1073b`, CI green all six on both; NO migration; counts UNCHANGED 25/40/133) — **React 19 + react-router 8 migration** | The deadline debt PAID ~3 months early, **by fix rather than by expiry**: `react-router@8.3.0` on React 19.2.8, `npm audit --omit=dev` **2 High → 0**, `audit-allowlist.json` emptied. Ratified OQ-FE-M1-1…6 all as recommended. **The pre-ratification pass ran as a full EXECUTED DRY RUN in a throwaway workspace copy and refuted two of the plan's own claims before the gate** (V-1: a plain `npm install` leaves a DUPLICATE React that fails 73/150 tests yet passes `tsc` AND `vite build` — `npm dedupe` is load-bearing; V-2: the natural fix for V-1 moves 61 packages, 49 unrelated, contradicting the slice's own scope boundary — the dedupe path moves the 12 DECLARED deltas — though the Wave-13 close measured the merged lockfile at 22 entry-level changes, the extra 7 being unattributable dev-transitive bumps plus a hoist relocation the declared gate never caught; see the record's corrected lockfile-delta section). Three fences shipped with executed negative **and positive** controls (eslint ban; manifest pin; single-React pin) because `npm audit`'s own `fixAvailable` field *recommends* the refuted 7.11.0 downgrade. Two findings the register never held: the shipped container built on **EOL `node:20-slim`** below react-router 8's `>=22.22.0` engines floor, invisible to CI because CI never builds the image (→ `node:24-slim`); and **the fail-closed audit gate that this slice exists to satisfy had no test at all** while about to enter its untested empty-allowlist state (→ evaluator extracted pure, 13 cases incl. every failure path). Plus the first `BrowserRouter` coverage — every routing test used `MemoryRouter`, so the router the app actually deploys was mounted by nothing. | M |

**Wave-14 tee — RATIFIED at the Wave-13 close (2026-07-29):** see Part 2.17 below.

## Part 2.17 — Wave 14: RATIFIED DIRECTION (2026-07-29 at the Wave-13 close) — "REAL DATA THROUGH THE GOVERNED RAILS"

**Direction ratified; the slice sequence ratifies at Wave-14 planning** (per-slice discipline unchanged, now incl. the six Wave-13-close standing rules P1–P6 in `claude_operating_instructions.md`). Scope named at ratification: vendor data onboarding as the payoff wave — reference dimensions (sector/industry/country-of-risk), concentration (REQ-CRD-003), liquidity tiers (REQ-LIQ-001/002), the ENT-006 holiday calendar (dated forcing function: the next real month-end collision is **2027-05-31**), and the risk-free capture vendor-diligence obligation (the uniform-shift limitation is undetectable in-data BY DESIGN — the control is onboarding diligence). Prior text of this line: "real-data onboarding — sector/industry/geography-of-risk reference dimensions + concentration (REQ-CRD-003) + liquidity tiers (REQ-LIQ-001/002) ride it as its analytic payoff (the user's 2026-07-26 in-scope direction; classificati…" (kept for provenance).

## Part 2.18 — Wave 14: the ratified sequence (2026-07-29 at the Wave-14 planning gate, OQ-W14P-1…8 ALL approved as recommended; same per-slice discipline incl. P1–P6; the full plan, decision ledger, and verifier findings ledger live in `wave_14_planning.md`)

**The organizing facts (recon-verified against code at `2411d00`, P3 discharge):** no reference dimension exists (the sole sector column is a free-text unused `issuer.sector`; no industry/country-of-risk/liquidity column anywhere); no REQ covers classification vocabularies (a mint rides REF-1); concentration is a NEW governed family (`exposure_aggregate`'s grain/vocab structurally exclude it; a new snapshot COMPONENT_KIND is required; the `ck_schedule_model_version_by_family` CHECK needs a migration per new family); the limit machinery has no dimensional selector; ENT-006 is vocabulary-only with a create-once holiday write path and TWO hand-mirrored weekend-only month-end predicates whose conventions must move atomically with RM-1's `is_month_end` (next real collision **2027-05-31**); the rf vendor-diligence obligation is open by design. **Wave-level ratifications:** real data = authoritative external datasets through governed capture, NO live adapters (trigger: a real vendor contract); open/vendor-neutral taxonomy schemes with a `scheme` discriminator (licensed GICS/ICB = a user procurement decision, additive later); REQ-CRD-003 SPLITS (spread-sensitivity minted separately, curve-feed trigger); REQ-LIQ-002 deferred + HOMED to the Part 3 RTM-P4 theme (trigger below); **scheme tenancy = extend the hybrid set via an explicit AD-013-R2 + CLAUDE.md closed-set invariant amendment + the closed-set mirror collapse, EXECUTED AT REF-1's GATE (ratified **N = 7**)** — ***amended 2026-07-29 at the REF-1 gate: migration 0008's `HYBRID_TABLES` is DDL driving its own policy loop, NOT a mirror, so 0008 stays byte-untouched and REF-1's own migration carries its policies; the surface is 38 sites across 36 files*** (assignments stay per-tenant symmetric; holiday sets land SYSTEM only if the source is public — the `fx_rate` licensed-data precedent otherwise); demo campaign extends with the count-pin relay; the stale-register re-sync rides the planning PR tier-split (status flips Tier 0/1; the `ingestion/models.py` docstring, the Wave-13-close ES-multiplier misattribution, and the SR-1 wave-named curve pointers as declared Tier-2 items); FE toolchain debt re-deferred (trigger: first FE feature slice or a security advisory).

| # | Slice | What it is | Size |
|---|---|---|---|
| 0 | **REF-1 — reference dimensions + the vendor-classification capture rail** ✅ **DONE + CLOSED 2026-07-29** *(**PR #148** = `727f3c9`, merged-main CI **30482058389** green all six; migration `0056`; ENT-066/067/068; the closed hybrid set 5 → 7 under AD-013-R2; counts UNCHANGED 25/40/133; full-PG 2719/0; P1 verify-on-main clean)* | The substrate: classification REQ mint + new entities (ENT-066+) for sector/industry/country-of-risk under a controlled taxonomy; capture with lineage + DQ; the demo's FIRST issuer-creating stages; the `issuer.sector` disposition; the rf **vendor-onboarding diligence control** (checklist + control-matrix row; convention-field option = second migration). Slice-gate forks: grain; storage + temporal class (AD-005 §2A); concrete scheme; onboarding rail (per-row binders vs staged→canonical completion — the latter fires split trigger (a): REF-1a/REF-1b); RLS mechanics under the ratified hybrid-extension direction (AD-013-R2 executes here); completeness/gap DQ rule type in-or-out. Split trigger (b): the diligence control separates first if the slice runs long. | L |
| 1 | **CON-1 — concentration, the 23rd governed number family** | New result family (table + migration `0057`; ONE new snapshot PURPOSE, FOUR pinned shapes; ***the CHECK amendment + FAMILY_REGISTRY entry DROPPED at ratification — schedulability deferred, OQ-CON-1-17***): per issuer / sector / country share-of-total, top-N, HHI-class metrics — the exact set is the slice's Tier-3 methodology gate WITH the rule-6a external-benchmark research section (UCITS 5/10/40-class regimes). Derived-of-derived over an ***EXPLICITLY SELECTED*** exposure run (the "latest COMPLETED" spec was REVERSED at ratification — CON-1 record Part 6b item 5; latest survives only as the API convenience default). Registered `model_version` WITH declared parameters (the model fork resolved at the gate). The dimension-identity representation ratifies WITH the LIM-2 selector shape as a named acceptance constraint (REQ-CRD-003 "limits-ready"). Rule-7 reads + FE in-slice; PG-tier pins on non-String filters. Immediate-issuer grain; ultimate-parent rollup stays deferred (REQ-SMR-002). **DESCOPED 2026-07-30 (user-ratified — the stopping rule after TWO consecutive refuted denominator foundations, 46 then 47 findings):** ONE share, `share_invested_long` on the invested-long denominator with a `denominator_basis` controlled vocabulary (sole v1 value `INVESTED_LONG`); explicitly NOT a regulatory ratio (the registered assumptions say so in those words); regulatory-shaped limits REFUSED fail-closed at LIM-2 until a cash/liability/NAV entity makes a matching basis computable. **RATIFIED 2026-07-30 (OQ-CON-1-1…28 all as recommended, v6 after FOUR verifier passes incl. the first citation-lane execution):** `_METRIC_MAP` registration REVERSED to LIM-2 (REQ-CRD-003 advances to "produced, bindable at LIM-2"); the three-code R-07 mint with recomputed holder sets (auditor_3l excluded from issuer-identity reads); the row_kind/bucket_code grain; the basis discipline; two REF-1 hardenings paid in-slice; the minimal FE read KEPT (decision point 8); counts 25/40/133 → 26/41/136. **CLOSED 2026-07-30 — merged PR #152 = `19fb4f7`, merged-main CI 30581831315 green all six; migration head `0057`; next free id ENT-070; counts 26/41/136 MEASURED; fresh-schema full-PG 2,776 passed / 0 failed; the P1 verify-on-main sweep clean across all seven ledgers.** The adversarial review fold landed one BLOCKING (the ratified OQ-CON-1-24(i) mixed-VERSION refusal was structurally UNFIREABLE — its discriminator read a set filtered to the requested scheme, so the second version could never appear; reimplemented over LIVE heads, mutation-proven), one defect only EXECUTION found (0057's CHECK names double-prefixed and PG-truncated — three reading lanes compared migration text to ORM text and called them identical; the standing gate now reads the live `pg_constraint` catalog), and ten ratified-but-undelivered controls incl. OQ-REF-1-29's demo role census (recorded "paid" by two successive slices, built by neither). See the record's Part 8. | M/L |
| 1.5 | **PERF-0 — the scale probe (INSERTED 2026-07-30, user-ratified, Part 4 rule 3)** | Generate a synthetic 5–10k-position, multi-year book through the capture rails in its own tenant; run the exposure → factor → covariance → VaR → perf chain end-to-end; record MEASURED wall-clock/memory ceilings against OD-046/NFR-05. AD-003 accepted the Python-batch/Decimal-grain performance risk with a revisit trigger only a measurement can fire — nothing beyond a 3-instrument demo book has ever executed, and a grain-level fix later touches every governed family, so the probe lands BEFORE further kernel families ship. No new governed family; every number a dated reading, re-measured if cited later (P4's binding clause). **CLOSED 2026-08-01 — merged PR #157 = `e6ea7c0` (the IMPLEMENTATION: the review's F5 found PR #154 had merged only the 205-line planning record, so the seed/harness/tests/readings lived unmerged for two days while the roadmap said otherwise). The measured answers: batch 21.4 min at 10,000 positions = 8.90% of the ratified 4h budget (one-date daily ≈ 16.2 min = 6.74%) — AD-003's revisit trigger has NOT fired; the ONE-TIME SEED is 3.87 h = 10.87× the batch (~14.4× one-date) — the real cost is INGESTION, the opposite of what AD-003 anticipated; capture rail linear (1.005/0.974/0.990); batch exponents 0.928/0.948; memory flat (<12 MB tracemalloc, <120 MB RSS). ALL FOUR verdicts adjudicated as STANDING by a three-lane Fable refute-by-default review (26 findings), whose real defects were the probe's own guard layer: the CI smoke's two-portfolio claim was VACUOUS (rung 3 = ONE portfolio at default packing; fixed with a parameterized packing + an IN-TEST portfolio-count assertion, mutation-proven) and its "all six COMPLETED" claim checked 3 of 6 run types (fixed with an exact six-family census + zero-non-COMPLETED assertion + statuses folded into each segment's ok, negative control executed). Named PERF-1 carries recorded in Part 9: audit-chain attribution BEFORE parallelizing; the write-count basis defined in the harness; status census never throw-based ok; guards assert their preconditions in code.** | M |
| 2 | **LIM-2 — concentration limits: the dimensional selector** | Dimension selector columns on `limit_definition` + echo columns on `breach` (both migrations; OD-I frozen-identity extends to them; the missing portfolio-scope echo addressed in the same design); `_METRIC_MAP`/`_resolve_latest` re-founded on a registry (the SCH-2 pattern); ENT-032 `limit_utilization` realization is the slice-gate call. P4 dry run on the double-table ALTER — the wave's highest-risk migration. **Named acceptance constraint from the CON-1 descope (2026-07-30):** a limit binds ONLY to a `denominator_basis` whose semantics its threshold was written against; regulatory-shaped thresholds are REFUSED fail-closed until a matching basis exists. **RATIFIED 2026-07-31 (OQ-LIM-2-1…8 all as recommended; TWO reversals of CON-1 positions taken AT the gate: named-bucket SHARE limits admitted — the exclusion's stated reason died with the `bucket_code` selector — and issuer-named limits admitted with the fence EXTENDED to the limit/breach reads; plus the OQ-LIM-2-8 correction that CON-1 recorded the WRONG deferral reason for schedulability — the real cost is that `schedule` has nowhere to put `scheme_by_dimension`).** **CLOSED 2026-08-01 — merged PR #155 = `b4905e3`, merged-main CI observed green; migration head `0058` (the limit tables' FIRST CHECK constraints, suffix-only names; the downgrade REVERSED from an RLS-blind refusal to the repo's sandwiched destructive delete after the review EXECUTED the guard into failure — it counted ZERO as the non-superuser owner and had "passed" the P4 dry run only because the container role is a superuser, which RLS exempts); NO id minted (ENT-032 stays the sole reservation, next free ENT-070); counts 26/41/136 UNCHANGED — the stronger final-position pin; 2,834-test full-PG green; P1 sweep clean across all seven ledgers, merged tree byte-identical to the validated tree.** Shipped: the `LimitFamily` registry + exact set-equality census (killing the two-family if/else whose `else` was a comment); ten concentration metrics registered FRACTION/no-benchmark; the evaluation-time basis match written against the ROW; `limit_health` gained REFUSED + `latest_run_failed` + `scheme_drift` as ORTHOGONAL fields (the ratified "distinct state" refined at record 3.5 — an enum would hide a real breach behind a staleness flag); the staleness check re-keyed to the RESOLVED run PLATFORM-WIDE (repairing shipped VaR + active-risk limits); the issuer fence at the QUERY on the limit, health AND breach reads (a fenced point-read 404s indistinguishably from a missing id — a 403 would itself disclose); demo stage 20 (7 limits, 3 breaches through the real evaluator, the NAV refusal demonstrated, entitlement teardown per OQ-REF-1-29). **TWO adversarial passes were the slice's story: #1 (6 lanes, 82 agents) found the first implementation NOT SOUND — 3 BLOCKING: the resolver FABRICATED ZEROS for unmatched selectors (silent green on ceilings; FALSE BREACHES into the append-only lifecycle on floors — and every limit anywhere in the slice was a ceiling until the fold), the breach reads were never fenced while `observed_value` IS the fenced issuer share, and the RLS-blind downgrade. #2 over the repair (37 agents) found R1 BLOCKING: the node-existence fix caught the EASY case (a non-node string) and missed the LIKELY one — a REAL level-2 division (the code the classification screen shows) vs level-1 bucketing; the negative control had tested the easy input and the stub that "proved" it returned only `code`. The resolver now requires a LEVEL-1 node of the resolved run's scheme, refusing with the ancestor hint, mutation-proven against a REAL seeded scheme.** THREE false verification claims were caught and corrected IN PLACE (kept as history, not edited away). Carries RATIFIED 2026-08-01: the classification-basis selector (trigger: the first two-basis tenant) + the breach DTO echoes (trigger: the first wire consumer, likely RPT-1). Open anomaly recorded: the registry test's `requires_basis=False` twice under the full battery, reproducing nowhere. | M |
| 3 | **CAL-1 — ENT-006 holiday-calendar resolution** | Real holiday data + business-day logic, atomically: the holiday set (source licensing checked at the gate); the holiday-refresh write path (children are create-once today); a shared business-day/roll helper in a leaf module (the mirror + conformance-pin pattern); the scheduler predicate AND RM-1's `is_month_end` moving in the SAME slice as a governed convention change to shipped RM-1/SR-1 registered assumptions (new/amended model_version — Tier-3; GIPS 2.A.23.b is the rule-6a citation); the calendar↔schedule binding (per-schedule FK = a `schedule` migration if taken) + the tenant-override-moves-a-live-grid hazard; the P3-8 benchmark-series completeness OQ. Split line if it runs L: CAL-1a (data + write path) / CAL-1b (the atomic convention move). NO hard dependency on REF-1 (SYSTEM-chain seed path) — sequenced by payoff, and 2027-05-31 is comfortably post-wave. | M/L |
| 3.5 | **DATA-1 — the first genuinely-sourced external dataset (INSERTED 2026-07-30, user-ratified, Part 4 rule 3)** | ONE authoritative public dataset — candidate: a U.S. Treasury bill monthly yield series (source + licensing verified at the slice gate) — ingested through the governed capture rails as the real risk-free series, with the vendor-onboarding diligence checklist (the CAL-1 control) EXECUTED against it as an auditable artifact. This converts the wave's "real data" claim from taxonomy seed rows into genuinely external market data on the governed rails; NO live adapter rides (the vendor-contract trigger for REQ-INT-002/003 stands unchanged). Sequenced after CAL-1 so the diligence control it reuses exists. | S/M |
| 4 | **LQ-1 — liquidity tiers (REQ-LIQ-001)** | Captured half: tier assignment as a captured input (grain fork at the gate) with a controlled vocabulary, list reads from birth. Governed half: **% illiquid** as a new governed family — table + migration + COMPONENT_KIND + registry/CHECK amendment + the model fork at the gate + Rule-7 reads + FE + demo stage + PG-tier pins. The denominator convention adopts or explicitly diverges from CON-1's ratified denominator (decided at LQ-1's gate with CON-1's record cited). REQ-LIQ-002 does NOT ride. | M/L |

## Part 3 — Beyond Wave 1 (coarse; themes from the RTM map, NOT sequenced yet)

- **COMMITTED Wave-15 openers (user-ratified 2026-07-30; their order within Wave 15 sets at the Wave-14
  close, but BOTH open the wave — this is a commitment with a date, not a theme):**
  - **DEP-1 — the deployment floor:** CI builds all three Docker images (the class that let an EOL
    `node:20-slim` base ship unnoticed — CI never builds what it tests); ONE scripted deploy to a real
    environment; a PROVEN PostgreSQL backup/restore; the NotificationSink wired to one real delivery
    channel (a webhook counts). Until DEP-1 ships, every "operational" claim in the records carries an
    implicit dev-only qualifier — the 2026-07-30 build assessment's finding that the engine "runs" on a
    laptop only.
  - **RPT-1 — the first reproducible risk report (REQ-RPT-001 minimal core):** one run-ID-bound,
    snapshot-pinned report over the shipped governed numbers — the first artifact a board, regulator, or
    prospective buyer asks for, currently wholly unowned; also the first end-to-end exercise of the
    thesis §2.3 reproducibility claim by a human-consumable artifact.
- **VaR completions (on demand):** √h multi-horizon; component/marginal VaR; Monte-Carlo (GATED: needs a
  seeded simulator + revaluation engine, QS-18). *(Rewritten at the Wave-6 close: ES SHIPPED at ES-1 and
  backtesting at BT-1/BT-2 — ES-over-HS is the named Wave-7 headline candidate, not a theme item.)*
- **Credit & counterparty risk** (RTM-P3 theme — now also carries the spread-sensitivity REQ minted out of REQ-CRD-003 at the Wave-14 planning gate, OQ-W14P-4; trigger: genuine curve feeds) · **Private assets + liquidity** (RTM-P4 — now also HOMES **REQ-LIQ-002 redemption stress & waterfall**, deferred at the Wave-14 planning gate OQ-W14P-5; trigger: the first scenario slice extending CAP-9 beyond linear factor shocks, or a user redemption-stress ask) · **Limits, breach
  workflow + SoD enforcement** (RTM-P6) · **Full model governance / validation workflow + DQ reconciliation**
  (RTM-P7) · **Reporting & dashboards** (RTM-P8) · **Real SSO/OIDC + admin + vendor adapters** (RTM-P9 — the dev
  header shim is replaced HERE at the latest, before anything internet-facing) · **BAU AI + hardening** (RTM-P10).
- **Frontend growth** *(REWRITTEN 2026-07-20 — the user set the core-product premise: "the front end UI will be
  a core part of this product")*: the FE-1-era "nearly free ride-alongs" posture is SUPERSEDED by the recorded
  **UI/read-surface assessment** (`ui_read_surface_assessment.md`) — the governed read surface is run-centric
  (no portfolio/as-of reads, no time-series, no summary; the compounding gap), the governance surfaces are the
  least readable part of the API (findings/evidence write-only; NO audit read; no snapshot list), FE types are
  hand-mirrored under an expired five-endpoint rationale (the FL-1 drift trio), and identity is the dev shim.
  **The named candidate Wave-9 sequence — API-1 (governed read surface) → FE-2 (generated types) → SSO-1
  (OIDC) → FE-3 (the product UI; the IA is a Tier-3 USER decision) — is weighed at the Wave-8 close per rule
  2.** Until that wave ratifies, the standing no-FE-expansion rule HOLDS; rule 7 (below) stops the read-shape
  gap growing in the meantime. Dashboards/reporting remain the RTM-P8 theme, realized through that wave.
- Standing deferrals with NO assigned wave (each needs its own trigger): **QS-10 day-count conventions** (the
  other half of REQ-SMR-004's deferral — the QS-11 holiday/business-day half is homed at Wave-14 CAL-1; trigger:
  first accrual/pricing methodology needing a declared convention — recorded at the Wave-14 planning gate);
  **FE toolchain majors** (TS→7, eslint→10, jsdom→30 + the six untypechecked root guard tests; trigger: first FE
  feature slice or a security advisory — re-deferred at the Wave-14 planning gate, OQ-W14P-8); covariance v2s (shrinkage/EWMA/
  correlation/annualization); vendor-beta loadings CAPTURE (the FL-1-recorded v2 — the REGRESSION leg SHIPPED
  at FL-1/MF-1; rewritten at the Wave-6 close); computed
  factor returns (needs adjusted prices); vol surface capture (ENT-022); PAR_RATE/interpolation/instrument-level
  sensitivity attribution; WORM/anchored audit hardening; gitleaks (OD-049). *(Branch protection
  OD-050 removed from this list at the Wave-6 close — PAID 2026-07-10, stamped at the Wave-1 close row.)*

## Part 4 — Re-sequencing rules (what makes this a plan, not a straitjacket)

1. **The default is the sequence.** At each slice close, the next Wave-1 slice proceeds on the user's go — no
   options menu is re-presented.
2. **Re-sequencing triggers (any of):** a wave closes (mandatory re-baseline); an adversarial review or build
   surfaces something structural; a dependency proves blocked or an assumption false; the user changes priorities.
3. **Every re-sequence is a recorded decision:** this document is amended with a one-line dated rationale and the
   change is briefed in plain language for ratification. Small hygiene insertions (a TC-1-sized slice) may be
   PROPOSED between slices but still ratify before starting.
4. **Ambiguity rule (user-stated, 2026-07-08):** where a slice hits a genuine fork, ask the user — always with an
   objective recommendation attached; the user decides.
5. **What never moves without its own gate:** the per-slice planning/review/commit discipline; the hard
   invariants (frozen audit service; no BYPASSRLS; R-07 for permissions/audit codes; the governed derived-number
   contract).

6. **Thesis alignment (ratified 2026-07-08 — see `01_product_strategy/differentiation_thesis.md`):**
   (a) every METHODOLOGY slice's decision record includes a **cited external-benchmark research section**
   (published/academic/regulatory sources + dates checked; deviations fixed or justified) — explicitly including
   transformation/proxy math when private-asset slices begin. **STRENGTHENED 2026-07-30 (user-ratified — the
   CON-1 double-refutation): every regulatory/academic citation enters the record ONLY as a VERBATIM quoted
   passage with a paragraph/page locator, and the pre-ratification verifier pass includes a citation lane that
   reads ONLY the cited source (never the draft's framing) and answers "does it say what the record claims?".**
   The class this kills: RM-1's truncated GIPS quote; CON-1 v1's three citations read for the OPPOSITE of what
   they say; the ratified repair refuted on the adjacent paragraph (¶87). A confidently wrong citation launders
   a guess as authority — worse than none, on the thesis's own §2.2 terms;
   (b) every wave-close re-baseline includes an
   **outward-facing benchmark review** (architecture / data design / methodologies / security / engineering
   practice vs. current published best practice) and **evaluates progress toward the public+private destination**
   — the Wave-1 close must explicitly weigh pulling private-asset foundations forward.

7. **The read-shape definition-of-done (user-approved 2026-07-20 — see `ui_read_surface_assessment.md`):**
   every slice that mints a governed number ships, IN THE SAME SLICE, the entity/time-centric read alongside
   the run-id read — filter by `portfolio_id` + as-of/date-range, plus a latest-resolver where the family has
   a meaningful "current" (the positions/valuations read shape, `api/positions.py:242-247` /
   `api/valuations.py:227-231`, is the house template); captured-input slices ship entity-filtered list reads
   from birth. Rationale: the run-centric-only read surface is the assessment's compounding finding (F1) — it
   blocks screens AND the thesis-§2.3 agent-consumable obligation, and every governed slice shipped without
   this widens the API-1 back-fill by one family. Effective from CC-1 (captured lists) and CC-2 (the 17th
   number) forward; the shipped families' back-fill is API-1's scope, not a per-slice retrofit.

## Part 5 — Amendment log

> Rows before 2026-07-27 live in `delivery_roadmap_amendment_archive.md` (moved 2026-07-30, the
> user-ratified document-surface shrink). This log stays append-only; new rows land HERE.

| Date | Change | Why |
|---|---|---|
| 2026-07-27 | **SCH-2 RATIFIED (OQ-SCH-2-1…8 approved as revised), `sch_2_decision_record.md` on `sch-2-planning`.** Wave-13 slice 0 — the month-end cadence + `EXPOSURE_AGGREGATE` + the family dispatch registry, discharging the SCH-1 family-2 deferral. **A two-lane pre-ratification verifier pass returned NOT RATIFIABLE on the first draft: 8 BLOCKING + ~14 MATERIAL, including THREE false claims in the draft itself** — all folded before the gate. Both lanes independently refuted "`anchor_date`'s two roles genuinely separate" (**≥2-finder convergence = CONFIRMED**): under INTERVAL the `tick ≥ anchor` invariant is held structurally by the `if k < 0: k = 0` clamp, a calendar grid has no analogue, and all three gates compare `now` — not the tick — to the anchor, so **341 of 365 anchor dates (93%) would fire a tick dated before the schedule existed**, minting a governed run dated before the book was configured. Also blocking: the **unspecified tick instant** (UTC midnight makes the month-end mark invisible via `valid_from <= valid_at`, failing every month — and **structurally undetectable by the demo**, whose fixtures seed every mark at a far-past `_T0`); `current_tick` raising **outside** the per-schedule SAVEPOINT (`select_active_due` is in the `for` header), killing all four tick phases for a tenant; the **two-table downgrade cascade** (child FK is `NO ACTION`, children are append-only + FORCE RLS) which also **destroys IA ledger evidence** — now disclosed; **CI unable to exercise that downgrade at all** (zero demo stages create a schedule, so the smoke deletes nothing — the draft claimed the opposite); a **net loss of unit-tier enforcement** (CHECKs are migration-only, SQLite builds from ORM metadata); the **unconditional CAD-1 FK guard** blocking EXPOSURE outright, whose obvious fix is a CTRL-003 fail-open; and the **implication-form CHECK failing OPEN** for any unenumerated family (fixed to a total enumeration). Ratified: the design package as revised; **OQ-7 = accept the burned month + runbook + a minimal read** (a failed fire permanently occupies its bucket and is not re-firable; RM-1 refuses a series with a missing month, so the only detector today would be a refusal months later); **OQ-8 = `EXPOSURE_AGGREGATE`**, the real run_type, avoiding two meanings behind the `target_run_type` column already shipped on `limit_definition`/`breach` and rendered in the OPS-1 UI. **Size honestly revised M → M/L** and sequencing re-confirmed (SCH-2 before RM-1) after that revision. **NEXT = SCH-2 implementation.** | The RM-1 gate's operability decision, planned and gated; the verifier pass caught 8 blocking defects incl. 3 self-inflicted, which is the pass working as designed. |
| 2026-07-28 | **FE-M1 DONE — React 19 + react-router 8, and the supply-chain exception retired BY FIX (Wave-13 slice 4, the LAST; NO migration; counts UNCHANGED at 25/40/133).** `react-router-dom@7.18.1` → `react-router@8.3.0` on React 19.2.8: 18 import specifiers, 5 declared packages, a lockfile delta **DECLARED at 12 before implementation — and CORRECTED at the Wave-13 close: the merged lockfile carries 22 entry-level changes, 15 realizing the declared 12 plus 4 unattributable dev-transitive bumps and a pluginutils hoist; the declared a-13th-is-a-defect gate did not fire**. `npm audit --omit=dev` **2 High → 0**; `audit-allowlist.json` emptied ~3 months before its 2026-10-24 cliff. **The pre-ratification pass was run as a FULL EXECUTED DRY RUN in a throwaway workspace copy — and refuted two of the plan's own claims before the user ever saw the gate.** (V-1) A plain `npm install` over the old lockfile leaves `@testing-library/react` holding a stale `react@18` beside the app's `react@19` — **two React copies, 73 of 150 tests failing, yet `tsc --noEmit` passes AND `vite build` emits a bundle**; `npm dedupe` is load-bearing and is now a written step. (V-2) The obvious fix for V-1 — regenerate the lockfile — moves **61 packages, 49 unrelated**, silently contradicting the slice's own ratified scope boundary; the dedupe path moves exactly the 12 attributable ones. Four further verifier folds landed pre-gate (V-3 the eslint fence would have inherited the `writes.ts` exemption hole; V-4 the Node-floor finding was overstated — no `.npmrc`, so `engines` are advisory and the old build warned rather than failed; V-5 the README named `react-router-dom` as current truth; V-6 the single-React pin was about to be justified on the false premise that the suite cannot see a duplicate). **Two findings the register never held:** the shipped container built on **EOL `node:20-slim`**, below react-router 8's `>=22.22.0` engines floor — invisible to CI *because CI never builds the image* (→ `node:24-slim`, rebuilt with **zero EBADENGINE**, deep-link `curl` serving the 287,953-byte migrated bundle); and **the fail-closed audit gate this entire slice exists to satisfy had NO automated test**, its failure paths proven by hand once in a past session and never committed, while FE-M1 was about to put its never-exercised `exceptions: []` path into service (→ evaluator extracted pure, 13 cases covering every failure path plus the inclusive expiry boundary). Three fences with executed negative **and positive** controls — `npm audit`'s own `fixAvailable` recommends the refuted `react-router-dom@7.11.0`, so the reintroduction path is advised by the tooling. Plus the first `BrowserRouter` coverage in the platform's history: every routing test used `MemoryRouter`, so the router `main.tsx` actually ships was mounted by nothing. FE **28 files/150 tests → 32/190** (+37, accounted for line by line; the migration itself added and removed zero; **R-4 then rewrote one of them rather than adding one**). **Merged in TWO PRs — #143 = `44ee905`, then #144 = `bd1073b` — because an independent `/code-review ultra` found R-4 AFTER #143 had merged:** the one test written to cover the `BrowserRouter` `useParams` deep link booted at `/runs/risk/{uuid}`, but `risk` is a `permissionFamily` VALUE in `FAMILIES`, not a key, so `RunDetail` bailed out WITHOUT fetching and rendered "Unknown run family" — a page that satisfies both of the test's by-absence assertions, meaning a real break in the exact thing it exists to pin would have left it green. Fixed to a real family key with by-evidence assertions (fetch called once with `/risk/vars/runs/{uuid}`). | **Three lessons. (1) For a migration or dependency-floor slice, READING IS NOT ENOUGH — reading, `grep` and the upstream upgrade guide all missed both blocking findings, and running the thing in a throwaway tree found them in ten minutes. (2) A slice's own scope promise can be unachievable by the route that fixes its own blocker; OQ-6=A survived only because the alternative was measured rather than assumed. (3) The independence ladder is not theory — the in-context pass found the holes in guards it had not authored (R-1/R-2/R-3); the INDEPENDENT pass found the vacuous assertion in the author's own new test (R-4). And the fold RACED its own merge, so the *verify-the-fix-is-on-`main`* clause is a CLOSEOUT step covering review folds, run after the LAST merge — third appearance of that class, first on the race axis.** NEXT = **the WAVE-13 CLOSE REVIEW**. |
| 2026-07-28 | **OPS-H1 DONE — the ten-item operations-hygiene slice (Wave-13 slice 3; **PR #141** = `03da139`, CI #669 green; NO migration; counts UNCHANGED at 25/40/133).** The fired-trigger cluster paid: `select_overdue_breaches` is ONE portable statement (the D9 template; equivalence with an independent inline oracle over SEVEN lifecycle shapes incl. the REJECT re-assign epoch; the statement COUNT asserted; an 8-row lock-order pin sized up after the ORDER-BY mutant survived a 2-row fixture at even odds); **the TRUE M-C1 tick×HTTP interleave FORCED — a real 40P01, deterministically, no fallback needed** — with both recoveries asserted on the REAL error object; the honest tick-stall survey VERIFIED (one current-truth site, already honest); the demo stage-14 clock seed-relative and BACKDATED so the walk is preserved while the OQ-W12C-3d prohibition retires into a documented consequence. The LOW cluster paid: every RLS-arming boundary canonicalizes (dev-header + the three L4 seams, spy-observed, mutant killed); the ops teardown narrowed to the stage's own wiring with role codes IMPORTED; the FIRST demo-tenant role census — **whose first exact value was WRONG in the instructive way: a reviewer cited 11 campaign perms for `auditor_3l`, the live battery measured 2** (the campaign wires its auditor via the template layer); the alerts screen pages at the API cap with a visible truncation notice, conformance-pinned to `le=200` (negative control executed); `client.ts` guards the success parse (200-with-HTML → a server-shape error, never "unreachable"). 2-finder review 0 HIGH / 5 MED / 8 LOW, all folded. Gates: make check 2193/0; full-PG fresh-schema **2637/0**; FE 150/150. | **Two lessons: measured beats cited — for a census exactly as for the demo counts; and a planning-time register entry is a CLAIM about the code that can be stale the day it is written (H1-9's "missing pager" had been shipped by NOTIF-1 before OPS-1 even recorded it).** NEXT = **FE-M1** (React-19/router-8, allowlist expiry 2026-10-24), then the Wave-13 close. |
| 2026-07-28 | **SR-1 DONE — the 22nd governed number: the Sharpe ratio over the PM-1 series against a CAPTURED risk-free benchmark (Wave-13 slice 2; **PR #139** = `b86aa28`, CI #663 green; migration `0055`; ENT-065; counts 24/39/132 → **25/40/133** MEASURED).** Sharpe (1994)'s differential-return form — σ of the EXCESS series, so RM-1's persisted volatility is deliberately NOT reused — with the n−1 divisor divergence from the paper's own endnote DISCLOSED in the registered assumptions; single-quantization (the executed DivisionByZero refutation); month-key rf join with binder-enforced completeness/uniqueness; magnitude gate + PG enforcement suite FROM BIRTH; 20/20 mutants killed. **4-finder review: 4 HIGH / 8 MED / ~12 LOW, all folded — and the two worst HIGHs were PRODUCT defects in the snapshot builder** (the rf window wrong at BOTH edges: pinning d₀'s never-measured month made a legal continuous vendor capture a PERMANENTLY unrunnable immutable snapshot; truncating at span_end lost the final month for a last-business-day book ~5 months in 12), unreachable from the demo because the demo derived its rf dates FROM the book's own month-ends — a fixture built from the subject cannot test the subject (now calendar-end-dated with a differing-sets pin). The other two HIGHs were HOLLOW GUARDS: the divisor test never called the kernel; the "platform-wide" GS2 test scanned 5/18 run types (an injected collision stayed green) — now package-discovered with a census pin. **The in-slice ledger sweep found the RM-1 session's own sweep commit was NEVER MERGED** (PR #137 carried only the incidental fixes) — current_state sat 4 PRs stale, ENT-061…064 had no registry rows; all carried forward, and the sweep gains a final step: *verify the fix is on `main`*. **Process failure recorded (Part 9a): a `git add -A` during the review committed a live finder mutation + a scratch file, and a stale `.pyc` served a mutated constant — every gate re-run from purged bytecode** (make check 2185/0; full-PG battery 0 on a reset schema; alembic check 0). | **Three standing lessons. (1) A fixture derived from the thing under test cannot test it — the demo's tautological rf dates hid both builder defects. (2) A census claim ("every existing X satisfies Y") belongs in the verifier pass, not in the fold that depends on it — third slice running. (3) Mutation testing and a wildcard `git add` cannot share a tree; grep the COMMIT, purge `__pycache__`, re-run.** NEXT = **OPS-H1** (operations hygiene), then FE-M1 (React-19/router-8, expiry 2026-10-24). |
| 2026-07-28 | **SR-1 RATIFIED (OQ-SR-1-1…6 approved as revised), `sr_1_decision_record.md` on `sr-1-planning`.** The 22nd governed number: Sharpe (1994)'s differential-return form over the RM-1 monthly grid against a CAPTURED risk-free benchmark series (ENT-052 path — zero schema change), NEW ENT-065 `sharpe_ratio_result` with rf provenance NOT NULL, RM-1's suppress-and-disclose for the undefined ratio (P3-8 divergence recorded), √12 iid annualization. **The two-lane verifier pass returned NOT RATIFIABLE on the math lane's first read — 3 BLOCKING — the FOURTH first-draft failure in a row, and again the worst defects were the author's own reasoning:** the "[V] Sharpe-1994" fidelity claim was refuted by the paper's OWN ENDNOTE (population σ, divisor T, vs our n−1 — a ~4.3% divergence headed for registered assumptions, now a DISCLOSED divergence); the declared operands let a NON-constant series raise DivisionByZero (quantized σ = 0E-12 — fixed by single-quantization with the predicate naming the same σ); and the ratio is UNBOUNDED on admitted inputs (executed: SR = 10¹⁰) with no magnitude gate in the draft. Engineering lane: 8 MATERIAL, all specification precision — the rf month-JOIN semantics were entirely absent, the "duplicate-month refusal is structural" claim was FALSE, the 19-vs-18 rf row count was the RM-1 off-by-one class, and `test_synthetic.py` would go red on migration 0055. **Recon verdict folded into the record: "rides existing capture" is true of SCHEMA, misleading about WORK** — no risk-free data exists anywhere and none of the captured series overlaps the only book SR-1 can run on; sizing revised S/M → M at the gate. Counts will move 24/39/132 → **25/40/133**. NEXT = SR-1 implementation. | The verifier pass caught 3 blocking defects incl. the author's own fidelity over-claim before ratification — the pass working as designed, four slices running. |
| 2026-07-27 | **RM-1 DONE — the 21st governed number: rolling return / rolling volatility / maximum drawdown over the PM-1 series (Wave-13 slice 1; **PR #135** = `b6e7ba0`, **CI green run #652**; migration `0054`; ENT-064; counts 23/38/110 → 24/39/132).** The governed return series finally has a RISK reading: a calendar-month relink with a five-condition alignment gate, n−1 volatility ×√12 from the stored value (so the emitted pair reconciles exactly), geometric return annualization never below 12 months, and window-local maximum drawdown with V₀ as an observation. **Post-implementation 4-finder review: 8 HIGH, 10 MED, 6 LOW — the largest of any slice to date, all folded**, and the findings were defects in the NUMBER, not polish: the alignment gate admitted a ONE-DAY "month" (two month-ends can share a month under the GIPS business-day allowance; the ratified rule said "d₀ is the LAST boundary of the preceding month" and the code checked only "d₀ IS a month-end"); the magnitude gate caught the wrong exception class so the extreme band escaped AFTER `create_run`, leaving the run in RUNNING; **the governed VALUE was never asserted anywhere** — an arithmetic-sum mutant replacing the geometric link shipped a 2.8pp error with every tier green; **ENT-064 had NO PostgreSQL suite**, the only governed result family without one (now four controls, each with its reject side executed, plus two negative controls — a dropped CHECK and a superuser bypassing RLS — so neither assertion can be a seeding artefact); the rule-7 read surface shipped with ZERO tests; and a `decimal-contract.ts` rewrite made a SHIPPED guard WEAKER while claiming to strengthen it (a derived `*RowOut` mapped type silently dropped 7 curated DTOs incl. `BreachOut`/`PositionOut`). Two corrections reached beyond the slice: **SCH-2's merged record claimed "counts unchanged at 23/38/109" while its stage 15 adds one COMPLETED run** (corrected there; the reason nothing caught it is that the platform's only absolute-count pin collated BEFORE that stage, pinning an INTERMEDIATE total it could never fail on — RM-1 adds a pin AFTER the last stage plus one isolating its own contribution), and **`MDD_36 ≥ MDD_12` does NOT prove window-local rebasing** — it holds under a run-global peak too, a claim that had been written into four places including a REGISTERED model_assumption. Also shipped: `perf/stats_kernel.py`, the estimator lift out of P3-8 that also removes DS-2's pre-existing cross-family borrow (bit-identity pinned against main); demo stage 16 (SEVEN `z`) with 19 month-ends + 1 mid-month so one month genuinely relinks two sub-periods and a designed multi-month drawdown; REQ-MKT-006 + REQ-PRF-003 in backbone AND RTM. Gates: `make check` **2137**; fresh-schema full-PG **2595 / 0** on the exact commit; downgrade-to-base clean. | **Two standing lessons. (1) Mutation-test the NUMBER, not only the guards you thought of — the refusal logic was well covered and the positive computational path was unpinned. (2) A measurement that contradicts a ratified expectation is evidence of a defect SOMEWHERE, and assuming it sits in the EXPECTATION is the comfortable reading: measuring 38 validations against a ratified 39, I 'corrected' the record down, when the real defect was my own missing INITIAL validation record — the correction would have quietly ratified the gap.** NEXT = **SR-1** (Sharpe), riding the existing ENT-052/ENT-021 capture. |
| 2026-07-27 | **SCH-2 DONE — month-end cadence + the family dispatch registry (Wave-13 slice 0; **PR #133** = `8c8c17b`, **CI green run #645**; migration `0053`; counts unchanged 23/38/109 — corrected at RM-1 and restated at the Wave-13 close: stage 15 adds one COMPLETED run, true post-SCH-2 total 110).** The engine can now tick a CALENDAR grid, and a SECOND family is dispatchable — RM-1 lands into a platform that can feed it. **Post-implementation 4-finder review: 2 HIGH, 8 MED, 5 LOW, ALL folded.** Both HIGHs ≥2-finder convergent, and they were the same defect twice: a hand-written `schedule.view` holder-set comment wrong in BOTH directions (`ops` does not hold it; `data_steward`/`risk_analyst_1l` omitted), and — surfaced only by chasing the TRUE holder set — the **DC leak it was hiding**: `auditor_3l` holds `schedule.view` while deliberately holding NO `valuation.view`/`position.view`/`marketdata.view`, and the worker recorded `f"{type(exc).__name__}: {exc}"` into `scheduled_run.failure_reason`, which for a DB error embeds the failing STATEMENT, its BOUND PARAMETERS and PG's `DETAIL:` row quote (confirmed by execution). That field was **write-only until this slice gave it its first reader** — so the leak was introduced by the read surface, not inherited. Redaction now runs at the WRITE boundary, where no caller can bypass it and no later reader can undo it. Other MEDs: an **`OverflowError` escape** (the column is int32, `timedelta` caps at 999,999,999 days, so values in between raised a NON-`ScheduleError` that escaped skip-and-report, escaped the worker's `for` header and aborted all four tick phases for the tenant — the ratified B3 failure class reached through a different door; now a three-layer `MAX_INTERVAL_DAYS` envelope); **`produces_run_on_failure` REMOVED** as false on the only path the scheduler uses (the pre-create gate is the snapshot BUILD's completeness gate, not upstream resolution — the repo's own `test_pre_create_refusal_incomplete_snapshot` refutes the premise, and the distinction is not a family property at all); **three guards proven by MUTATION never to fire** (B3 poll isolation, the dispatch-time CTRL-003 refusal, the cadence-vocab CHECK) plus `OUTCOME_UNRECORDED` shipped untested; the **downgrade suite never asserting the RE-TIGHTEN** (a realistic 0053→0052 rollback would have left `scheduled_run` MUTABLE and both tables cross-tenant readable, invisible to CI because 0049 then drops the tables); and PG fixture grants green only by **leakage from ~30 earlier CI suites**. **Every new guard was then mutation-tested — 4/4 killed their tests.** **Two ratified decisions were REVERSED BY EXECUTION** and recorded at the gate in Part 4b rather than only in a docstring: the `as_of_known_at` pin (building the demo produced an `EmptySnapshotError` — known-time means "as RECORDED at T", so pinning makes a late-arriving month-end mark permanently invisible; and the verifier's motivating two-fires scenario is UNREACHABLE under `uq(schedule_id, scheduled_for)`) and `produces_run_on_failure`. Battery: `make check` **2070**; fresh-schema full-PG **2520 passed / 0 failed**; `alembic downgrade base` clean **and destructive for real** — 2 unrepresentable schedules + 2 ledger rows deleted, where the pre-SCH-2 smoke deleted ZERO rows and went green testing nothing. | **Lesson: a decision endorsed by reasoning AND by an adversarial verifier can still be refuted by the first execution — the argument for the demo stage as mandatory scope. Second lesson: mutating the source to check a test actually fails found three vacuous guards no amount of reading did; make it standard in the review brief.** NEXT = **RM-1 implementation** (ENT-064, migration `0054`). |
| 2026-07-29 | **WAVE 13 CLOSED + RATIFIED (P1–P6 approved as recommended; WAVE 14 RATIFIED = "REAL DATA THROUGH THE GOVERNED RAILS"), `wave_13_close_review.md` on `wave-13-close`.** The mandatory Part-4 rule-2 close after Wave 13 shipped SCH-2 → RM-1 → SR-1 → OPS-H1 → FE-M1. Method: **121-agent ultracode audit** (5 slice verifiers + 5 cross-cutting, refute-by-default, every HIGH/MED attacked by THREE adversarial refuters with distinct lenses; 11 survived / 26 killed / 20 LOWs recorded unrefuted; 3 of 6 thin-margin kills OVERTURNED by hand re-adjudication), then a single-thread fold phase under the P2 shared-tree rules with **11 executed mutation controls, all killed**. **Verdict: THE RUNTIME-CLEAN STREAK ENDED AT EIGHT** — `calc/reads.py`'s blanket `str()` bind 500'd all four new RM-1/SR-1 read endpoints on PostgreSQL while every gate was green (SQLite column affinity makes the unit tier structurally blind; the fix is PG-pinned where a unit pin cannot fail). Guard layer: the closure-stamp gate was blind to the WHOLE wave (7th recurrence — SIX unstamped records found, five stale from earlier waves incl. PM-1; widened + NON-VACUITY FLOORS), RM-1's headline alignment fix was deletable suite-green against its own mutation-tested claim, the FE audit gate failed OPEN on malformed exceptions, the import fences took their THIRD un-enumerated bypass axis (dynamic import + .mts/.cts), SR-1's purpose gate had no negative control, the NaN asymmetry between RM-1 and SR-1 over the same pin shape, and the R-4 vacuous-assertion class recurred twice more. FOUR false claims in governed records corrected in place (the registered sharpe_v1.md's refuted overflow arithmetic; "refused at capture"; CTRL-002's three conflicting stories; FE-M1's "exactly 12" lockfile delta that measured 22 with the declared gate never firing). All 34 findings FOLDED (batches `396d513`→`b131e89`). Gates: make check 2201/480; fresh-schema full-PG **2681/0** `PYTEST_EXIT=0`; downgrade smoke + `alembic check` clean; fe-check 32/204; CI run **30455596382** observed green all six. **Process: P1–P6 ratified and WRITTEN into `claude_operating_instructions.md` as standing sections** (the six-ledger sweep + verify-on-main-after-last-merge; shared-tree mutation rules; register-entries-are-claims; executed dry runs + the re-measure clause; assert-by-evidence + positive controls; non-vacuity floors on enumerating guards). **NEXT = Wave-14 planning** (real-data onboarding, Part 2.17). | The mandatory wave-close re-baseline; user ratified 2026-07-29 ("Proceed" on the briefed recommendations, all as recommended). |
| 2026-07-29 | **WAVE 14 SLICED + RATIFIED (OQ-W14P-1…8 ALL approved as recommended), `wave_14_planning.md` on `wave-14-planning`.** The sequence: **REF-1** (reference dimensions + the vendor-classification capture rail, L) → **CON-1** (concentration, the 23rd governed family, M/L) → **LIM-2** (the dimensional limit selector, M) → **CAL-1** (ENT-006 holiday resolution incl. the atomic scheduler+perf convention move, M/L) → **LQ-1** (liquidity tiers + governed % illiquid, M/L), written as Part 2.18. Method: 6-lane recon fan-out (P3 register-verification against code at `2411d00`) + single-threaded draft + a 4-lane refute-by-default verifier pass — **24 findings (2 BLOCKING, 4 HIGH, 12 MED, 6 LOW), ALL folded**; the BLOCKINGs hand re-verified (the slice-0 id collided with the shipped Wave-3 RD-1 → renamed REF-1; the taxonomy-tenancy recommendation contradicted Accepted AD-013 uncited → OQ-W14P-6 rebuilt around the AD-013/AD-013-R1 fork). Wave-level ratifications: no live adapters (trigger: a real vendor contract); open taxonomy schemes with a `scheme` discriminator; REQ-CRD-003 splits (spread REQ minted at REF-1, curve-feed trigger); REQ-LIQ-002 deferred + homed to the Part 3 RTM-P4 theme; **scheme tenancy = EXTEND THE HYBRID SET via AD-013-R2 + the CLAUDE.md closed-set invariant amendment + 0008/ORM mirror updates, executing at REF-1's gate** (assignments stay per-tenant; holiday sets SYSTEM only if the source is public); the demo campaign extends (count-pin relay); the stale-register re-sync rides the planning PR tier-split; FE toolchain debt re-deferred with triggers. Planning-branch gates: docs-check + secret-scan green; CI run **30464515268** observed green all six on `489c7fd`. **NEXT = REF-1 planning.** | The Part 2.17 direction required slicing at Wave-14 planning; user ratified 2026-07-29 ("Proceed" on the briefed gate, all eight as recommended). |
| 2026-07-29 | **REF-1 PLANNING RATIFIED (OQ-REF-1-1…30 all as recommended), `ref_1_decision_record.md` on `ref-1-planning`.** Wave-14 slice 0 planned to the Tier-3 gate: ISIC Rev. 5 as the canonical scheme with sector/industry as LEVELS OF ONE HIERARCHY (so REF-1 ships a bounded ancestor resolver CON-1 consumes); classification ASSIGNMENTS as a separate FR bitemporal table (decided on drift-on-verify, test-proven both ways — an EV column would permanently redden the governance walk on every historical concentration run); country-of-risk CAPTURED with a NOT NULL `basis` (no authoritative rule is computable on today's schema); countries as an ISO-3166-1 scheme rather than a fourth table (**closed hybrid set N = 7**); THREE R-07 permission codes split by tenancy class; instrument-grain polymorphic assignments. Method: 6-lane recon fan-out (**52 corrections to the wave plan ratified hours earlier** — the P3 rule paying for itself) + single-threaded draft + 4-lane refute-by-default verifier pass: **61 findings (3 BLOCKING, 12 HIGH, 22 MED, 24 LOW), ALL folded**, the BLOCKINGs hand re-verified by execution. The BLOCKINGs: a single `.view` code would have handed `auditor_3l` its first proprietary-identity read, invisible because SoD pins are per-code; the grain fork's decisive reason was refuted by the ratified CON-1 scope and self-refuted by the draft's own demo OQ; and **the record's own status line was invisible to the closure-stamp gate — recurrence EIGHT** (executed `_status_lines` → `[]`), fixed and the missing non-vacuity floor added to REF-1's scope. Also folded: the draft's own OVER-correction of the wave plan's DQ fact, withdrawn on executed evidence. **Split trigger (b) FIRED — the rf diligence control moves to CAL-1**, whose registered-assumption versioning machinery it requires. Wave-plan amendments executed (`wave_14_planning.md` Part 7, 13 items). | The per-slice discipline; user ratified 2026-07-29 ("proceed" on the briefed gate, all thirty as recommended). |
| 2026-07-29 | **REF-1 IMPLEMENTED — the platform's FIRST governed reference DIMENSIONS (Wave-14 slice 0; migration `0056`; ENT-066/067/068; counts UNCHANGED 25/40/133).** `classification_scheme` + `classification_node` (EV, HYBRID) + `classification_assignment` (FR bitemporal, PROPRIETARY symmetric). **AD-013-R2 takes the closed hybrid set from FIVE tables to SEVEN** — user-ratified, `CLAUDE.md` amended, and executed the CORRECT way: migration 0008 stays byte-untouched because its tuple is DDL driving its own policy loop, so 0056 polices only its own tables and the parity test asserts declaration == union(migrations). 31 hand-mirrored copies of the set collapsed into that one declaration. Sector/industry ship as ONE hierarchy (ISIC Rev. 5) with a bounded cycle-safe ancestor walk — CON-1's per-sector bucket is the level-1 ancestor of an assigned leaf; country-of-risk is CAPTURED with a NOT NULL `basis` because no authoritative rule is computable on today's schema. **THREE platform floors (P6):** the EFFECTIVE write check `COALESCE(with_check, qual)`, FORCE-RLS coverage over every tenant table, and a closure-stamp COVERAGE floor added because **this slice's own record was invisible to the closure gate — recurrence EIGHT**, which the two count floors could never catch. **R-07: THREE permission codes split by tenancy class** (a single view code would have handed `auditor_3l` its first proprietary-identity read, invisible because SoD pins are per-code — the verifier's BLOCKING find). REQ-SMR-006 + REQ-CRD-005 minted. Demo stage 18 is the first issuer-creating stage and backfills `issuer_id` so CON-1 computes over a classified book; the final-position count pin relays at unchanged counts. **Method: 6-lane recon (52 corrections to a wave plan ratified hours earlier) + 4-lane refute-by-default verifier pass (61 findings, 3 BLOCKING, ALL folded) + 3 executed mutants.** Gates: make check green; fresh-schema full-PG **2719 passed / 0 failed** `PYTEST_EXIT=0`; alembic check clean; P4 dry run 0056 up→rows staged→down→up. **NEXT = CON-1**, carrying REF-1's named obligations (the instrument→issuer pin decision; fail-closed refusal of mixed-scheme-version aggregation; the `CLASSIFICATION` component kind). | The ratified Part 2.18 sequence; OQ-REF-1-1…30 ratified 2026-07-29 ("proceed" on the briefed gate). |
| 2026-07-30 | **STANDING RULES AMENDED (user-ratified): P7 "lessons are recorded as acts, not facts" + its pre-flight-manifests companion; the P1 sweep gains a SEVENTH ledger (every delivery claim in a record cites its artifact against the MERGED diff); the both-tier-before-push commit rule (the `dbce327` lesson); Part 4 rule 6a STRENGTHENED (citations enter records only as verbatim quotes with locators + an independent citation-verification lane).** Grounded in a 4-lane error-trend audit over all 23 session logs, the operating instructions, and the memory corpus. Its measured findings: mechanization stops an ENUMERATION, not a class (the closure-stamp gate recurred FIVE times post-mechanization at its enumeration boundary; exact set-equality censuses have zero recorded recurrences); procedural prose binding an executed act to a trigger moment has zero recorded recurrences, while declarative "remember that X" prose recurs every time; the finding-count rise tracks verifier intensity (~100×) and claim density (~5×), NOT declining generation quality — the escape rate is roughly flat. | The user asked (2026-07-30) what instruction change would address "generating and catching more and more errors"; answered by measurement, not impression, and ratified as proposed. |
| 2026-07-30 | **WAVE 14 RE-SEQUENCED + the WAVE-15 OPENERS COMMITTED (user-ratified, Part 4 rules 2/3): CON-1 DESCOPED to the `share_invested_long` core with a `denominator_basis` controlled vocabulary — the stopping rule after TWO consecutive refuted denominator foundations (the 2026-07-29 dual-share ratification was itself refuted: `sum(long_amount)` ≠ total assets, which include cash/cash-items/receivables per IRC §851(b)(3)(A)(i), so `share_total_assets` OVERSTATES shares and LIM-2 would have written FALSE BREACHES into an append-only, non-withdrawable lifecycle); PERF-0 (the measured scale probe) INSERTED as slice 1.5; DATA-1 (the first genuinely-sourced external dataset — a real T-bill series through the governed capture rails + the executed vendor-diligence artifact) INSERTED as slice 3.5; DEP-1 (the deployment floor) + RPT-1 (the first reproducible run-ID-bound risk report) COMMITTED as the Wave-15 openers.** | The 2026-07-30 four-lane build assessment (user-directed): the plan is coherent but increasingly inward-facing — everything requiring the outside world sat behind triggers nobody was firing; the wave named "real data" was shipping only taxonomy seed rows; performance at any realistic size is unmeasured while each new Decimal-kernel family raises the cost of a grain-level fix; reporting — the artifact a CRO/board/regulator consumes — was wholly unowned. |
| 2026-07-30 | **DOCUMENT-SURFACE SHRINK (user-ratified): Part 5 rows before 2026-07-27 moved to `delivery_roadmap_amendment_archive.md` (63 rows, ~170KB out of the operative file); `current_state.md` capped to the CURRENT-TRUTH block + live standing sections, history to `current_state_archive.md`; `build_sequence.md`'s hand-mirrored Status column RETIRED to a roadmap pointer; `00_ai_operating_model/` marked HISTORICAL.** Done-set invariance verified BEFORE the split: archived rows contribute no real slice-id to the closure gate's parsed done-set (55 ids remain; floor 38). | The operative roadmap was 310KB — unreadable in one pass even by its maintainer — and a ratified map asserting shipped capabilities "Not started" is active misinformation: the P3 register-entries-are-claims class applied to our own planning surface. The truth surface had outgrown maintainability; the register re-sync workstream existed largely to service it. |
| 2026-08-01 | **PERF-0 CLOSED (Wave-14 slice 1.5) — merged PR #157 = `e6ea7c0`, the platform's SECOND autonomous merge. Every headline stands under adversarial re-computation: the 4h budget trigger has NOT fired (8.90%; 6.74% one-date) and INGESTION dominates risk compute (~10.9–14.4×) — the deferred "Python batch performance" risk was aimed at the wrong half of the system. AND: the operational pattern's first executed evidence — PRs #156 and #157 were created, check-watched and merged with no human in the loop (the Wave-6 workaround retired; root cause of every prior auth failure was a root-owned `~/.config` from 2021).** | Part 9 of `perf_0_decision_record.md`; the readings; NEXT = CAL-1 |
| 2026-08-01 | **LIM-2 CLOSED — concentration limits, the dimensional selector (Wave-14 slice 2). Merged PR #155 = `b4905e3`, merged-main CI observed green; migration head `0058`; counts 26/41/136 UNCHANGED; 2,834-test full-PG; P1 sweep clean, merged tree byte-identical to validated.** The slice that made CON-1's numbers bindable — and the wave's sharpest lesson in guard quality: two adversarial passes found FOUR blocking defects in code that had already passed CI green, every one behind a claim the author had written and believed ("fail-closed", "structural", "mutation-proven" — each true of something, just not the thing it named). The terminal form: **a negative control that tests the EASY wrong input proves little (the 'TECH' string vs the real level-2 'C26'); a mutation proof is only as good as the input it mutates against; a stub thin enough to hide the distinction makes the proof inherit the blindness.** Also: the same session RETIRED the button-pushing merge pattern — `gh` installed + allowlisted, branch protection unchanged (required checks stay the machine gate), first autonomous PR pending. Two carries ratified with triggers; one open anomaly recorded. | Parts 6–8 of `lim_2_decision_record.md`; NEXT = PERF-0 fold + implementation PR (the review found #154 merged only the PLANNING record — the implementation was never merged), then CAL-1 |
| 2026-07-30 | **CON-1 CLOSED — the 23rd governed number family (Wave-14 slice 1). Merged PR #152 = `19fb4f7`; merged-main CI 30581831315 green all six; migration head `0057`; ENT-069 minted, next free ENT-070; demo counts 26/41/136 MEASURED; fresh-schema full-PG 2,776 passed / 0 failed; the P1 verify-on-main sweep clean across all seven ledgers.** Shipped as the DESCOPED form: `share_invested_long` + `denominator_basis` (explicitly NOT a regulatory ratio), with `_METRIC_MAP` registration REVERSED to LIM-2 so shipped fail-closed code refuses every concentration limit until the basis column exists (REQ-CRD-003 = "produced, BINDABLE AT LIM-2", deliberately not Done). **The review fold is the slice's lesson:** all three refute-lanes independently found that the RATIFIED mixed-scheme-VERSION refusal was structurally unfireable (its discriminator read the pinned set, which is filtered to the requested scheme, so the discriminating case could never appear in it) while four shipped surfaces advertised the control — reimplemented over LIVE current heads and mutation-proven. **And the defect none of the three READING lanes could see:** migration 0057 passed full constraint names into `op.create_table` while the naming convention prepends `ck_<table>_` itself, so every CHECK landed double-prefixed and the longest was PG-truncated at 63 chars; the lanes byte-compared migration text against ORM text and reported parity. The standing countermeasure is mechanical (P7): a test that reads the LIVE `pg_constraint` catalog and asserts set-equality against the ORM's declared names. Ten further ratified-but-undelivered controls were delivered in the same fold, incl. OQ-REF-1-29's demo role census + teardown, which TWO successive slices had recorded as "paid" and neither had built. | Part 8 of `con_1_decision_record.md`; NEXT = PERF-0 |
| 2026-08-01 | **CAL-1 PLANNING RATIFIED (OQ-CAL-1-1…12 ALL as recommended), `cal_1_decision_record.md` on `cal-1-planning`.** Wave-14 slice 3 planned to the Tier-3 gate: the atomic set = the scheduler roll + its perf mirror + `is_month_end` WIDENED (never substituted) + the conformance pin; **v2 as NEW version labels on the same codes** (`perf.rolling_risk`/`perf.sharpe`, v1 grandfathered — the roadmap row's "new/amended" disjunction closed by code: NO amend path exists), the convention carried as registrar-stamped assumption literals (`month_end_convention=BUSINESS` + `holiday_calendar=XNYS`, absent ⇒ v1, ambiguous ⇒ fail-closed) — never label-parsed; **the scheduler carrier = a NEW cadence kind `BUSINESS_MONTH_END`** (EXPOSURE is model-less — the model_version mechanism cannot carry it; `CALENDAR_MONTH_END` grandfathered, live grids never move, retire-and-recreate is the named transition); per-schedule `calendar_id` FK (create-only, kind-gated, the SECOND symmetric→hybrid FK after 0056) with FAIL-CLOSED head-resolution + declared `holidays_complete_through` coverage; **a DB-grain period key** (partial unique) as the double-fire backstop; **AD-014-CONFORMANT reproducibility: a `HOLIDAY_CALENDAR` snapshot COMPONENT_KIND pins the holiday set into every v2 run**; hand-encoded XNYS 2024–2035 from the published per-year NYSE table with NEGATIVE census pins (2027-12-31/2032-12-31 are trading days — Rule 7.2); **CTRL-034 minted at the gate (R-10 act, H-05 APPROVED — the first control mint since P0.5)**; P3-8 completeness + `RULE_TYPE_COMPLETENESS` re-deferred to DATA-1; **SPLIT ratified: CAL-1a (dataset + refresh verb + the executed diligence control, no migration, M) → CAL-1b (the atomic convention move, migration 0059, L)**. Method: 6-lane recon (185 facts at `8637b67`) + single-threaded draft + 4-lane refute-by-default verifier — **2 BLOCKING, 6 HIGH, 5 MED, 8 LOW, ALL folded** (the BLOCKINGs: OQ-6 had recommended an unpinned mutable-EV input to a governed convention with AD-014 UNCITED; OQ-2 had decided the carrier without deciding WHICH calendar governs a v2 run). Part 6 records the citation lane catching the draft fabricating a quotation against the wave plan — inside a record citing rule 6a. | The per-slice discipline; user ratified 2026-08-01 ("proceed" on the briefed gate, all twelve as recommended). NEXT = CAL-1a implementation |
