# Delivery Roadmap (rolling-wave)

| Field | Value |
|---|---|
| Status | **RATIFIED by the user (2026-07-08: "Proceed" on the full package — Wave 1 order + the Part 4 re-sequencing rules — after a plain-language briefing incl. the documentation-alignment audit)** |
| Date | 2026-07-08 |
| Purpose | The operative near-term slice sequence + the re-baselining rules, so slice-boundary "what next?" decisions stop being ad-hoc menus (user direction 2026-07-08: a plan to avoid drift and rabbit holes, explicitly NOT immutable). |
| Relationship | `build_sequence.md` = the ratified RTM **theme/phase map** (what the platform must eventually contain, and roughly in what order — unchanged). `docs/project_memory/build_plan.md` = the **executed ledger** (what has shipped). THIS document = the **operative sequence** for the next wave of slices. The per-slice discipline (decision record + plan + OQ ratification + adversarial review, then commit+push under the 2026-07-12 delivery-autonomy grant → the USER merges the PR) is UNCHANGED by this document. |
| Basis | All P0.5–P3 records; the consolidated deferral registers; the FE-1 walking-skeleton direction; the user's standing preferences (plain-language gate briefings; objective recommendations; ask on ambiguity with a recommendation attached). |

---

## Part 1 — Principle: rolling-wave, not big-bang planning

High fidelity for the NEAR term (the ratified Wave 1 below — named slices, order, rationale); deliberately COARSE
beyond it (themes only). Detail is added when a wave closes, not before — a detailed twelve-month software plan is
fiction, and treating fiction as commitment produces its own drift (building to the plan after the facts have
changed). Phase boundaries were always revisit-able (`build_sequence.md` §7 has said so since genesis); this
document makes the revisiting DISCIPLINED (Part 4) instead of a menu at every slice boundary.

**Numbering note (drift, now reconciled):** the executed phases (P0.5, P1A/B/C, P2, P3…) diverged from
`build_sequence.md`'s RTM phase numbers early (the RTM map's P2 theme "public market data + market-risk core" was
executed as OUR P2+P3; the RTM's P5 stress theme is why stress carries the "RTM-P5" caveat). Cross-references
below always name which numbering they use.

## Part 2 — Wave 1: the ratified near-term sequence

Each slice still gets its own decision record + plan + gates; this table fixes ORDER and INTENT only.

| # | Slice | What / why | Size | Dependencies |
|---|---|---|---|---|
| 1 | **TC-1 — FE toolchain bump** ✅ **DONE** (`c34b346`, CI #112) | vite 5→current + vitest 2→current majors (closes the recorded dev-only advisory chain), CI Node alignment, + a **production-deps `npm audit` CI step** (runtime supply-chain issues turn CI red on their own). Mostly mechanical; do it while the frontend context is fresh. Keep-Vite/Vitest decision already accepted by the user (2026-07-08). | S | none |
| 2 | **VAR-HS-1 — historical-simulation VaR** | The second VaR method (user-directed roadmap 2026-07-07): factor-based historical simulation over the captured factor-return windows — its own registered model family + methodology doc + declared parameters (window adequacy, quantile interpolation). No new UI work needed: hist-sim runs surface in the FE-1 view automatically (same VAR run family). | M/L | none (data already captured) |
| 3 | **P3-C2 — hardening bundle** ✅ **DONE** (`6fb1a13`, CI green) | The accumulated recorded follow-ups swept in one consolidation slice (the P3-C1 pattern): exposure-family scaffold/`failure_reason` adoption (scaffold relocated risk→calc); exposure runs in the FE listing (`exposure.view` handling; source-switch); captured-input-table `PreciseDecimal` parity (incl. `transaction` via the review); the DQ-rule first-registration savepoint race. NO migration. Full 6-finder review, 9 folds. | M | none |
| 3.5 | **TD-1 — test-data realism audit** ✅ **DONE** (`ac92e0b` + follow-up `4534a38`, CI green) | A hygiene insertion (Part 4 rule 3) ahead of P2-7 implementation: retrospectively audited + remediated existing economic-value fixtures to the new fixture-realism standing rule (three-bucket classify-then-fix; test-and-docs-only diff fence). The base fixtures were already plausible; 8 signal-forcing/ordinary values in the factor-return/covariance/VaR/sensitivity/exposure test fixtures were remediated. NO golden re-derivation (chosen values preserve invariance/inequality/pinning asserts). 4 independent finder passes caught 2 author errors + 2 same-class completeness misses — all folded. NEW `08_testing_qa/test_data_realism.md` + a standing review-angle. TD-2 not needed. | M/L | none |
| 4 | **P2-7 — benchmark price/level capture** ✅ **DONE** (plan `04c4135`; impl `ea2863d`, CI green) | The captured-input slice (`benchmark_level`/`benchmark_return`, NET-NEW **ENT-052**) that unblocks every return-based benchmark-relative analytic. FR/bitemporal; captured returns ONLY (no calc from levels); migration `0029`; no new permission; six additive `MARKET.BENCHMARK_LEVEL_*`/`_RETURN_*` audit codes. Full 6-finder review, ~10 folds. **OD-G precision amendment (2026-07-09):** P2-7 unblocked the **ex-ante** benchmark-relative half only — the ex-ante tracking error consumes the benchmark **membership** (P2-6); the **ex-post** measure (realized TE / active return / IR / tracking difference) additionally requires a governed **portfolio-return series**, a separately-planned performance-measurement slice where `benchmark_return`'s first governed consumer lands. | M | none |
| 5 | **P3-7 — benchmark-relative analytics** ✅ **DONE** (plan `552b954`; impl `65e6dbe`, CI green) | Ex-ante active risk / tracking error `TE = √(wₐᵀΣwₐ)` over the existing factor-exposure + covariance engine and the captured benchmark membership — the P3 plan's final **ex-ante** analytic leg (`active_risk_result`, ENT-027; migration `0030`; `COMPONENT_KIND_BENCHMARK` minted; run family `ACTIVE_RISK`, metric `TRACKING_ERROR`). FULL max-effort multi-agent review (10 finders + 6 verifiers + sweep): 21 folds, 3 deferred findings recorded in the decision record Part 6 (var_service V2/V5 twins; shared covariance adjudicator; lineage batching). The **ex-post** leg (realized TE / active return / IR) is DEFERRED on the portfolio-return prerequisite (OD-G). | M/L | slice 4 |
| 6 | **P3-6 — stress/scenario** → **MOVED to Wave 2 slot 5** (Wave-1 close review, 2026-07-09 — the pre-authorized outcome, with a positive reason: scenario P&L is richer once realized-return machinery exists) | ENT-029/030 (scenario definitions + results). Sequenced LAST in the wave deliberately: our own P3-0 record flags it as RTM-P5-phase (possibly later); doing it after benchmark-relative completes the P3 analytics story without pulling it ahead of cheaper wins. If the Wave-1 close review argues for deferring it into Wave 2, that is an expected outcome, not a failure. | L | none (independent) |

**Wave 1 CLOSED (2026-07-09):** `wave_1_close_review.md` **RATIFIED** (OQ-W1C-1…6) — honest state audit + the
reconciled deferral register + the outward benchmark review + the destination check. Slices 1–5 + two ratified
insertions (TD-1, P3-C3) shipped CI-green; P3-6 moved to Wave 2 (pre-authorized). The TC-1 OD-D obligation
discharged (`npm audit` full tree, all severities = 0 at `6a864c9`).

## Part 2.5 — Wave 2: the ratified sequence (2026-07-09; same per-slice discipline)

**The organizing fact (from the close review Part 4):** ONE missing primitive — a governed portfolio-return
series — is the prerequisite for THREE recorded destinations (ex-post TE/IR per P3-7 OD-G; VaR backtesting;
the private-asset desmoothing machinery). Wave 2 builds it first, then its consumers, then begins the thesis
destination in dependency order.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **PM-1 — governed portfolio-return series** ✅ **DONE** (impl `b2445c7` + ruff-format `f5e16b6`, CI **#137** green) | Performance-measurement v1: chain-linked TWR (Modified-Dietz within N caller-supplied exposure-run boundaries) over pinned exposure atoms + TRANSFER_IN/OUT transactions — the NEW `perf.return.twr` v1 family (ENT-053 `portfolio_return_result`, migration `0031`; `perf.run`/`perf.view` R-07 mint; new **CAP-20** + REQ-PRF-001). **v1 = SINGLE-portfolio book** (subtree deferred). FULL ultrareview (5 finders): folded 3 HIGH (return magnitude gate; portfolio_id cross-tenant FK; duplicate-run-id) + 1 MED + minors, each with a regression test. Rule-6 externals: GIPS 2020. The triple unlock. | M/L |
| 2 | **P3-8 — ex-post benchmark-relative** ✅ **DONE** (plan `4880b36` via PR #1; impl `86ef3ec` merged via PR #2 = `d769f59`, CI **#142** green) | Realized TE / active return / tracking difference / IR over PM-1 + `benchmark_return` (its FIRST governed consumer — closes P3-7 OD-G). Rule-6: ESMA 2012/832 applies directly. ENT-054, migration `0032`; `perf.run`/`perf.view` REUSED (no mint). | S/M |
| 3 | **BT-1 — VaR backtesting** ✅ **DONE** (plan `3e81ef4` via PR #4; impl `e7b615d` merged via PR #5 = `868f892`, CI green) | Kupiec POF + Basel traffic-light over realized P&L vs ONE shipped VaR method per run (both methods supported; comparing them is two runs). The P7 model-validation prerequisite (SR 11-7 "outcomes analysis") — closes the Wave-1 close review's named nearest supervisory gap. ENT-055, migration `0033`; `risk.run`/`risk.view` REUSED (no mint). | M |
| 4 | **PA-0 — private-asset foundations** ✅ **DONE** (plan `07e5d6a` via PR #7 = `7a422aa`; impl `c9d41a7` merged via PR #8 = `ad3d3fe`, CI green) | The thesis destination begins: ENT-019 `proxy_mapping` REALIZED (FR captured private→public factor proxies, migration `0034`; the CURRENCY-family v1 scope ENFORCED); the appraisal/NAV leg needed NO new schema (an ordinary `instrument` + `valuation` under a documented private `asset_class` convention); the Geltner-AR(1)-first desmoothing disposition RECORDED for the **PA-1** follow-on (the ratified capture-first split — the roadmap's own "may split capture-first" outcome). `marketdata.view`/`.ingest` REUSED (no mint). **Okunev-White citation flagged UNVERIFIED — resolve before PA-1.** | M/L |
| 4.5 | **MD-H1 — marketdata/registrar hardening** | A hygiene insertion (Part 4 rule 3; user-ratified 2026-07-11) paying the three bug-shaped deferral-register items before the wave close: the FR supersede window-coherence guard (all six FR series — the only integrity-flavored open item), the shared IntegrityError→409 mapping (every marketdata capture endpoint), and the registrar first-registration concurrency race (all governed families). NO migration; guards + shared mappings + tests. Dedup/perf register rows stay trigger-based. | S/M |
| 5 | **P3-6 — stress/scenario** ✅ **DONE** (plan merged via PR #12 `28c5225`; impl `fe3bb55` merged via **PR #13** = `0f66220`, CI green) | The TENTH governed number: deterministic LINEAR factor-shock P&L — `scenario_definition`/`scenario_shock` (ENT-029, EV header + FR shocks) + `scenario_result` (ENT-030, IA), migration `0035`; run family `SCENARIO` reusing `risk.run`/`risk.view` (no mint); `risk.scenario.factor_shock` v1. Versioned saved scenarios applied to a COMPLETED factor-exposure run over a `SCENARIO_INPUT` snapshot; partial-coverage = unnamed-factor-unchanged with loud coverage counts; TR-09 both sides. DECLARED linear shocks only — historical-window replay (v2), worst-case search (v3), reverse stress, ES, multi-period, non-CURRENCY families DEFERRED. | L |
| — | **Hygiene ride-alongs** (no slice slot): runtime npm-audit CI gate `high`→`moderate` (applied AT this close — the tree is at zero); branch protection on `main` (OD-050) **✅ DONE 2026-07-10** — `enforce_admins=everyone` + all 5 CI checks required; no direct pushes to `main`, PR flow binds everyone from P3-8 on; ES leg DEFERRED (OQ-W1C-4). | | S |

**Wave-2 close = the same mandatory review + re-baseline** (incl. the rule-6(b) outward benchmark review + the
destination-progress check). **CLOSED 2026-07-12 — `wave_2_close_review.md` RATIFIED (OQ-W2C-1…5):** all five
ratified slices + MD-H1 shipped CI-green, zero deviations; ten governed numbers; the return-series triple unlock
delivered; the thesis destination began (PA-0).

## Part 2.6 — Wave 3: the ratified sequence (2026-07-12; same per-slice discipline)

**The organizing fact (from the Wave-2 close, Part 4):** the thesis §2.1 destination — private assets no longer
understating their own risk — now has BOTH prerequisites in place (a governed return series from PM-1; a
private→public proxy substrate from PA-0). Wave 3 builds the **transformation math itself** — the differentiation
payload — after one small hygiene slice so it lands on clean rails.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-1 — dedup hygiene (early)** ✅ **DONE** (`0584cfe` merged via **PR #17** = `82f5f1d`, CI green) | Paid the TIPPED `resolve_*_run` helper family (3 near-verbatim copies — var_backtest/active_risk/scenario — meeting the P3-4-R0 3rd-consumer rule) → one shared `calc/` resolver with an injectable error class (the `assert_portfolio_in_tenant(error=…)` precedent). Test-and-refactor only; NO migration. Done first so PA-1 builds on the cleaned resolver. The FR-membership engine (P3-6 D-2) stays trigger-based unless RD-1 elects the larger extraction. | S/M |
| 2 | **PA-1 — private-asset desmoothing (the thesis payload)** ✅ **DONE** (plan merged via PR #18 `366a5c4`; impl `34e8830` merged via **PR #19** = `f8bc20d`, CI green) | The ELEVENTH governed number: Geltner AR(1) unsmoothing of the captured appraisal mark series — `desmoothed_return_result` (ENT-056, IA), migration `0036`; declared-α identity (`perf.return.desmoothed_geltner` v1); run family `DESMOOTHED_RETURN` reusing `perf.run`/`perf.view` (no mint); the honest-uncertainty stdev pair on every run; Okunev-White citation RESOLVED (an iterative Geltner extension — PA-0's tentative substance corrected). 4-finder review: 10 folds (incl. the FE runs-list altitude fix) + 2 deferrals (RD-2 candidate; instrument-guard next-touch). | M/L |
| 3 | **PA-2 — private holdings on the public factor substrate (end-to-end)** ✅ **DONE** (`1ac7b7f`→`b20c736` merged via **PR #22** = `96b2bd2`, CI green; **NO migration**) | Projected a private holding through `proxy_mapping` onto CURRENCY factors and ran the governed exposure→factor-exposure→VaR chain — the thesis §2.1 END-TO-END demonstration, with a **byte-exact invariance proof** (a proxied private book yields the identical VaR to a public book holding the same factor vector). A NEW registered model family (`risk.factor_exposure.proxy` v1, code-version identity) dispatched through the SAME `run_factor_exposure` binder (no new table); proxy rows pinned via a load-bearing binding-predicate switch. FULL 4-finder review: ~24 raw → 13 distinct, **ALL FOLDED, zero deferrals** (incl. the magnitude/envelope gate, the symmetric predicate gate, and the active-risk partition guard `_assert_partitioning_exposure_run`). Three v2 register items minted (proxy-aware active-risk; regression-estimated weights; residual/idiosyncratic variance). | M |
| — | **Coarse beyond Wave 3** (themes, not sequenced): the **P7 validation workflow** (BT-1 now feeds it); ES closed-form leg; credit/counterparty (RTM-P3); limits/breach/SoD (RTM-P6); reporting (RTM-P8); SSO/OIDC before internet-facing (RTM-P9). | | — |

**Wave-3 close = the same mandatory review + re-baseline** (incl. the rule-6(b) outward benchmark review + the
destination-progress check). **CLOSED 2026-07-13 — `wave_3_close_review.md` RATIFIED (OQ-W3C-1…5):** all three
ratified slices shipped CI-green in sequence; PA-1 + PA-2 SHIPPED-AS-RATIFIED; RD-1 shipped with a note-level
census undercount (folded into RD-2); the PA-2 fold-#8 half-application was found and fixed at the close; the
thesis §2.1 destination REACHED in v1. **Wave 4 ratified (Part 2.7, fork option A): RD-2 → PA-3 → a v2 companion.**

## Part 2.7 — Wave 4: the ratified sequence (2026-07-13; same per-slice discipline)

**The organizing fact (from the Wave-3 close, Part 4):** Wave 3 REACHED the thesis §2.1 destination — a private
holding now flows capture → desmooth → proxy → factor risk → VaR, byte-exact against its public equivalent — but
two links are still the honest-stub choice: proxy **weights are captured, not estimated** (so PA-1's desmoothed
series is not yet an input to anything), and the factor universe is **CURRENCY-only**. The user ratified fork
**option A** at the close (2026-07-13): DEEPEN the private-asset payload toward best-in-class (thesis §2.2), after
one small hygiene slice.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-2 — declared-parameter + resolver dedup (early hygiene)** ✅ **DONE** (`11f890d` merged via **PR #26** = `cc9679b`, CI green; NO migration) | Paid the two TIPPED items in one pass: the declared-parameter parse-back family (PA-1 D-1 — 5 identical `select(ModelAssumption)` loads + 2 byte-identical `_single` helpers + 3 inline extractions → the shared `model/assumptions.py`) and the four RD-1 residual resolver copies (the Wave-3-close census undercount, discharged — 2 byte-identical read folds + 2 consumed-guard folds with the ratified OQ-3 message normalization). Net −63 lines across the two bootstraps. 4-finder review: 3 CLEAN + the sweep's 3 test-hardening folds. | S |
| 2 | **PA-3 — regression-estimated proxy weights (the loop-closer)** ✅ **DONE** (`15ba13f`→`77149d6` merged via **PR #28** = `a98d380`, CI green; migration `0037`) | The TWELFTH governed number: unconstrained OLS of the **desmoothed** appraisal return series (PA-1) on candidate CURRENCY factor returns → `proxy_weight_estimate_result` (ENT-057, IA; per-coefficient standard errors + R²), the FIRST downstream consumer of desmoothing. Estimates are MODEL OUTPUT, promoted into `proxy_mapping` ONLY by the deliberate `risk.promote_proxy_weight_estimate` step (activating PA-0's reserved `REGRESSION` method + the additive `source_calculation_run_id`). Declared `min_observations` identity (RD-2 rails); `n≥max(N,k+2)`. Rule-6: Sharpe 1992, Scholes-Williams 1977/Dimson 1979, Asness-Krail-Liew 2001. FULL 4-finder review incl. a Fable-class numeric finder (independent Fraction re-derivation, math CONFIRMED): 1 real governance defect folded (the supersede/correct REGRESSION-citation leak) + 5 smaller folds; 2 deferrals. `make check` 1330 passed. | M/L |
| 3 | **PA-4 — total parametric VaR (residual/idiosyncratic variance)** ✅ **DONE** (`d3a6eae`→`c04768e` merged via **PR #30** = `8ef70db6`, CI green; migration `0038`) | The planning-time call went to **residual variance** (OQ-PA-4-1; proxy-aware active-risk stays a registered trigger-based item). The THIRTEENTH governed number: `risk.var.parametric_total` v1 — total VaR = the plain factor variance PLUS the diagonal idiosyncratic residual variance of REGRESSION-proxied instruments' cited PA-3 estimates (Sharpe 1963), `σ_e,daily = σ_e,period/√(appraisal_days·252/365)` with `appraisal_days` a DECLARED identity parameter (in-build OD-D refinement — the pinned summary carries no span). SAME binder (the PA-2 dispatch precedent), SAME `var_result` + ONE additive `residual_variance` column (decomposition evidence). The P3-5 specific-risk=0 limitation is PARTIALLY discharged — REGRESSION-cited instruments only. **The `1−Σw` partial-proxy leg remains honestly UNMODELED** (the PA-2 register wording sketched that variant; the ratified PA-4 models the regression residual instead — `factor_exposure_proxy_v1.md`'s limitation stands). FULL 4-finder Fable review: 26 findings → 23 folded, ZERO HIGH math defects (goldens independently re-derived byte-exact; 6000-case invariance fuzz). | M |
| — | **Coarse beyond Wave 4** (themes, not sequenced): the **P7 validation workflow** (fork option B — nearest supervisory ingredient, BT-1 feeds it); **multi-family proxying** (needs an equity/credit factor-returns capture slice); ES closed-form leg; credit/counterparty (RTM-P3); limits/breach/SoD (RTM-P6); reporting (RTM-P8); SSO/OIDC before internet-facing (RTM-P9). | | — |

**Wave-4 close = the same mandatory review + re-baseline.** Delivery runs under the full-autonomy grant
(2026-07-12, EXTENDED 2026-07-14): Claude self-drives plan→implement→review→commit→push AND opens + merges the
PRs (the adversarial review + `make check` + full-PG + CI-to-green gates replace the human merge gate; branch
protection's required checks stay on). The USER still signs off Tier-3 (methodology/model/grain/entity
decisions) and genuine design forks.

## Part 2.8 — Wave 5: the ratified sequence (2026-07-14; same per-slice discipline)

**Wave 4 CLOSED (2026-07-14):** `wave_4_close_review.md` **RATIFIED** (OQ-W4C-1/3/4/5/6 approved as
recommended; OQ-W4C-2 answered **"A: Self-governance"**). All three Wave-4 slices shipped-as-ratified;
9 deferrals PAID, 3 TIPPED (→ RD-3), 1 newly minted (estimate-staleness governance); 5 record-hygiene
fixes applied at the close.

**The organizing fact (from the close review Parts 3–4):** the private-asset chain is structurally
complete and vendor-aligned — but the flagship number is unvalidated (all 13 governed models sit at a
non-enforcing `UNVALIDATED`), exempt from backtesting, and missing the FRTB-preferred ES measure, while
every ingredient those three need already shipped. Wave 5 = **"the numbers govern themselves."**

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **RD-3 — hygiene (early)** ✅ **DONE** (planning `39533c8` merged via **PR #33** = `df6afd6`; impl `d751fde` merged via **PR #34** = `29bc5a2`, CI green; NO migration) | The three TIPPED items: `_reresolve_content` parse-hardening (verify-path 500s — scoped narrowly via a new `MalformedPinError` + `_parsed_pin`/`_pinned_row_ids` on the 4 composite branches that actually parse `captured_content`, NOT the whole 19-branch dispatch: a review correction, since the first pass would have masked live-data serializer bugs as drift); the `proxy_mapping` instrument-guard fold + message normalization; the MD-H1-C adoption sweep (7 provably-identical `parse_strict_decimal` sites; every SKIP recorded with its reason in the record's Part 5, incl. a genuine latent NaN-detonation bug found at `benchmark_relative_service.py` and deferred as out-of-scope-fence). Ride-along: the PG cross-module seed-collision fixed at cause (whole-seed no-op guard + `resolve_or_insert` on the 2 SYSTEM-tenant fixtures), deterministic uuid5 ids untouched — the **dirty-schema double-run is now a standing local-validation capability**. 4-finder review: 3 HIGH + 3 MED folded. | S/M |
| 2 | **VW-1 — model-validation workflow (P7 / SR 11-7)** ✅ **DONE** (planning `9f0e024` merged via **PR #35** = `58900b1`; impl `726373f` + CI fix `93ab215` merged via **PR #36** = `a9a00eb`, CI green; migration `0039`) | The headline — realizes the reserved **ENT-037 `model_validation`** (+ IA `model_validation_finding`/`_evidence` children) as append-only SR 11-7 validation records at `model_version` grain: outcome {APPROVED, APPROVED_WITH_CONDITIONS, REJECTED} × type {INITIAL, PERIODIC, TRIGGERED}, findings, and evidence that can cite COMPLETED governed runs (a BT-1 backtest becomes first-class outcomes-analysis evidence — the FRTB MAR32 pattern). **The gate with teeth: a latest-outcome REJECTED refuses new governed runs** at the ONE shared `assert_model_version_of` seam (all 12 model-bound binders inherit it); **UNVALIDATED keeps running** — the documented **SR 26-2** use-before-validation exception posture (rule-6 found SR 11-7 was superseded 2026-04-17 by SR 26-2 / OCC 2026-13; the record cites both). R-07 mint **`model.validate`** (2L `risk_manager_2l` only — SOD-03 withheld from the 1L register-holder); reserved **`MODEL.VALIDATE`** activated; human-only v1 (BR-15/MG-07). Doc advances: ENT-037 REALIZED, REQ-MDG-003 → In-Progress, **CTRL-022 → Operational**, taxonomy `.VALIDATE` activated, SoD mint record, independence-policy note. 4-finder review (1 adversarial on the gate): **1 HIGH** — `RejectedModelVersionError` was unmapped at the risk/perf run endpoints (raw 500, not the promised 422; the seam-only tests hid it) — fixed + end-to-end 422 tests through a real risk AND perf endpoint; 5 more folds. Staleness fork **A**: the σ_e estimate-age GATE rides BT-2. | M |
| 3 | **BT-2 — total-series backtest** ✅ **DONE** (planning `da258ff` merged via **PR #38** = `26d89b4`; impl `3cb9844` via **PR #39** = `4035f10`, CI green; migration `0040`) | Extends outcomes analysis to `VAR_PARAMETRIC_TOTAL` (the PA-4 `METRIC_TYPES` exclusion DISCHARGED — one tuple, zero schema change: ENT-055's `var_metric_type` was already unconstrained and the kernel is method-agnostic) — **the honest work is the doctrine, not the constant swap**. Every VaR is a hard-enforced 1-DAY forecast, so a daily total-series read on an appraisal-marked book is biased TWO WAYS by construction: exceptions SUPPRESSED between marks (flat private P&L) and CLUSTERED on mark dates (a quarter's move vs a 1-day allowance). Registered as limitations + a dated methodology scope amendment with the READ RULE: the unconditional Kupiec/Basel verdict is **NOT valid evidence of adequacy in EITHER direction** on such books (validity degrades with the private-leg share); the dated per-pair `EXCEPTION_INDICATOR` rows are the evidence surface. Appraisal-frequency pairing + a Christoffersen leg = named **BT-3 candidates** (the stale "Christoffersen = BT-2" label fixed at 5 sites). **Ride-along (VW-1 fork A) PAID:** the σ_e estimate-age GATE — a DECLARED `max_estimate_age_days` on a new `risk.var.parametric_total` **v2** (v1 grandfathered ungated; VW-1's REJECT is the sunset lever); age = pinned covariance `window_end` − the cited estimate's regression SPAN END; pre-create 422; + migration `0040`'s `estimate_age_days` echo. **The estimate-staleness register item is ~~CLOSED~~ — an OVERCLAIM corrected at the Wave-5 close: 1 of its 3 clauses is paid (the age echo); the promotion path has no age check, every non-total family binds the same weight ungated, and nothing expires a `proxy_mapping`. RE-OPENED as "estimate staleness — residual", TIPPED for Wave 6.** | S/M |
| 4 | **ES-1 — parametric ES leg** ✅ **DONE** (planning `dcd9634`+`eec25a1` merged via **PR #40** = `bbc1d1b`; impl `a0723ac` + folds `17d48bc` merged via **PR #41** = `2abc1b1`, CI green; **NO migration**) | The closed-form seam over BOTH σ and σ_total, realizing the `ES_PARAMETRIC` value reserved since P3-5 — the **14th governed number** and the first that answers what VaR structurally refuses (a cut-off says nothing about the magnitude beyond it). `ES_c = k_c·σ`, `k_c := φ(Φ⁻¹(c))/(1−c)`, **`c` = CONFIDENCE** (the P3-5 seam never defined `α` and the literature is split — ES-1 pins it; ES is the α-TAIL-MEAN integral, never TCE, so a later HS leg inherits the right estimator). **NO MIGRATION** — the grain already permitted the value and every column existed. TWO codes through the SAME binder (`risk.var.parametric_es` + `..._es_total` v1, the latter carrying BT-2's gate REQUIRED from birth); the extra-row-on-the-VaR-run alternative was **census-DISQUALIFIED** (it would break every BT-1/BT-2 backtest). `k_c` = a REGISTERED 12dp constant, identity-checked at bind — **no runtime normal function of any kind**. **NO ES backtest**: a ratified omission on **FRTB precedent + parametric redundancy, explicitly NOT non-elicitability** (which is false). **The honest work was the research**: the obvious pitch ("VaR is incoherent") is WRONG for this platform's own delta-normal VaR (normal ⊂ elliptical ⇒ already coherent, Embrechts-McNeil-Straumann), and BCBS never argues coherence at all — ES here buys tail severity + robustness-if-normality-fails. **A 3-verifier planning pass ran BEFORE ratification and killed 2 HIGH claims of my own** ("the VaR families' behaviour is unchanged" — false, one shared z table feeds four families and 0.975 was two shipped tests' off-vocabulary probe; and a "byte-exact portfolio-independent golden" that cannot pass at row level under 6dp quantization). Both headline claims survived attack; all six constants confirmed to the last digit. **Ride-along: BT-2's fired PG trigger PAID (both halves).** | S arithmetic / M surface |

Then the **Wave-5 close review**, with **multi-family factor capture teed as the Wave-6 headline**
(residual shrinkage/EWMA + desmoothing v2s riding when it lands). Delivery runs under the full-autonomy
grant as extended 2026-07-14 (Part 2.7 note); the USER signs off Tier-3 decisions.

## Part 2.9 — Wave 6: the ratified sequence (2026-07-15 at the Wave-5 close, OQ-W5C-6 sub-fork **(A) governance-first**; same per-slice discipline — decision record + plan + **pre-ratification verifier pass (the ES-1 lesson)** + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist)

> **The tee was corrected before ratification (OQ-W5C-4):** the previously-recorded prerequisite — "an
> equity/credit factor-returns capture slice" — was **factually wrong** (capture is already family-generic;
> proven by execution at the close). The real prerequisite is the **loadings substrate + a fractional,
> non-partitioning exposure kernel v2**. Wave 6 is planned on the corrected text.
>
> **Scope note (user, 2026-07-15):** the platform targets **enterprise** use and will eventually run and demo
> **real data** — but not before the build completes. No vendor adapters/live feeds ride this wave; the
> captured-data posture (RTM-P9) stands, and the demo environment is the synthetic/seeded tenant.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **MG-1 — model materiality + the first validation campaign** ✅ **DONE** (planning `7435e0a` via **PR #43** = `91c39fa`; impl `56cda62`→`e490c73` via **PR #44** = `46ce296`, CI green; **NO migration**) | Finishes Wave 5's own headline instead of leaving a capability with an empty table (**16/16 model codes sit UNVALIDATED**). Materiality assignment (the existing nullable `tier` gains meaning: rigor + cadence scale with it — **SR 26-2's organizing principle**, which contains zero occurrences of "tier"/"annual"); **the first validation campaign** — real validation records against the flagship VaR family citing **real BT-1/BT-2 runs as outcomes-analysis evidence**, with honest findings drawn from the registered limitations and a CURRENCY-only **condition whose remediation IS FL-1/MF-1** (the flywheel: governance generates the backlog, then TRIGGERED re-validation closes the loop); **OD-033 cadence anchored on materiality** (open since VW-1, un-answerable without this); the **F3 fix** (the scope-stretched SR 26-2 exception becomes per-model + time-boxed). **Ride-alongs: the RD-3 NaN bug** (the oldest live known bug — `Decimal("NaN")` slips the malformed-pin wrapper in `benchmark_relative_service.py` and detonates as a raw 500 + RUNNING orphan; pay it, stop waiting for the trigger) **+ the estimate-seam integration test** (one test chaining a REAL PA-3 regression run into a total VaR — retires the "two halves meet at a hand-minted seam" ambiguity permanently). | S/M |
| 2 | **FL-1 — factor-loading substrate + fractional exposure kernel v2** ✅ **DONE** (planning via **PR #46** = `ec01053`, CI #303; impl `fl-1-impl` tip `ca07529` via **PR #47** = `778891c`, **CI #314 green**; **NO migration** — head stays `0040`) | **The REAL multi-family prerequisite** (the corrected tee): a loadings capture/estimation substrate (design fork at planning: vendor-beta capture vs PA-3's regression machinery pointed at public instruments) + an exposure kernel v2 whose loadings are **fractional and non-exclusive** (`build_factor_index` keys on `currency_code` and `allocate_atom` maps each atom to exactly ONE factor at loading 1 — dimension-generic ONLY for partitioning dimensions, false for STYLE/MARKET/credit) + relaxing `proxy_mapping`'s CURRENCY gate + the **zero-migration vocab extension** to FRTB's risk classes (MAR33.12: rates/equity/FX/commodity/credit-spread — `factor_family` is `String(30)` with no DB CHECK; the BT-2 `METRIC_TYPES` pattern). **Ride-along: the FE drift trio** (the dead-end PROXY_WEIGHT_ESTIMATE row; the missing `residual_variance`/`estimate_age_days` columns; **the ES `z×σ ≠ var_value` display** — the only place a governed number presents arithmetic that does not check out). | L |
| 3 | **MF-1 — multi-family factor exposure end-to-end** ✅ **DONE** (planning via **PR #49** = `0951784`; impl `mf-1-impl` tip `2898eb8` via **PR #50** = `96b766c`, **CI #323 green**; **NO migration** — head stays `0040`) | The headline payload on FL-1's substrate: a real equity/credit book through capture → loadings → exposure v2 → covariance → VaR/ES, with the PA-3 regression finally running on the Sharpe-1992 factor set its own citations name. **Unlocks four blocked candidates** (FRTB liquidity horizons, partial ES by risk class, non-CURRENCY scenario shocks, §2.2's risk-class taxonomy) + the residual-shrinkage/EWMA + desmoothing-v2 ride-alongs teed since Wave 4. Ends with the **TRIGGERED re-validation** closing MG-1's CURRENCY-only condition — the full lifecycle demonstrated. | M/L |

Then the **Wave-6 close review** — **DONE and RATIFIED 2026-07-17** (`wave_6_close_review.md`, OQ-W6C-1…6;
merged as DRAFT via **PR #52** = `9d561bf`, ratified immediately after). The Wave-7 sequence it ratified is
Part 2.10 below.

## Part 2.10 — Wave 7: the ratified sequence (2026-07-17 at the Wave-6 close, OQ-W6C-6 fork **(A) "deepen the mathematics"**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist, now including the grep-for-"pending" item)

> **The two ratified riders:** (1) **SC-2 (non-CURRENCY scenario shocks) is the named pull-forward** if the
> wave underruns — the one fork-B slice with a genuine methodology question (per-class shock semantics; BOTH
> gates + probe moves); (2) **commitment/capital-call handling is the PRESUMPTIVE WAVE-8 HEADLINE** — thesis
> §2.1's third transformation leg, zero code after six waves, named so it stops being the quiet one.
> The user's real-data scope note (Part 2.9 preamble) stands.

| # | Slice | What / why | Size |
|---|---|---|---|
| 1 | **HG-1 — the hygiene opener** ✅ **DONE** (plan `4c8829f` via **PR #54**; impl `9e3200a` merged via **PR #55** = `8260ea6`, CI green run #339; NO migration) | The re-opened **estimate-staleness residual** (OQ-W6C-2: the promotion path gains an age check — the expire-a-mapping clause is a Tier-3 design question surfaced at planning); the **FL-1 registrar-constant CURRENCY-wording debris** (`risk/bootstrap.py:665-667` + the HS analogue — behavioral for NEW registrations, the finding-key compatibility constraint applies); the **private-on-multi-family demo ride-along** (the genuinely-private appraisal chain has never exercised a non-CURRENCY factor; the capability shipped at FL-1 — mind the mixed-family fence: a NEW private instrument, never the legacy PE fund). Shipped: OD-A the opt-in promote-day age gate (`ProxyWeightStaleEstimateError`; `promotion_age_days` always-measured+audited; three unmeasurable shapes fail closed under a bound), OD-B DEFERRED, the 11-row constants sweep (keys verbatim), stage 3 of the living tenant (`PC-BRIDGEWATER-II`, α=0.4 on multi-family factors). 4-finder review, zero HIGH. | S |
| 2 | **ES-HS-1 — ES over historical simulation (the headline)** ✅ **DONE** (plan `7568c49` via **PR #57**; impl `c7b8c15` merged via **PR #58** = `dc2a494`, CI green run #348; migration `0041`) | Converts the platform's tail measure from a unit conversion (`k_c·σ`) into a real empirical number; realizes the `ES_HISTORICAL` metric over the shipped VAR-HS machinery; **the ONE migration (0041) widening the verified 0028 CHECK** (`ck_var_result_parametric_not_null` exempts only the literal `'VAR_HISTORICAL'` — an ES_HISTORICAL row violates it in PG while passing the whole SQLite battery); the α-tail-mean estimator convention pre-pinned at ES-1 inherits; **the genuine Acerbi-Szekely ES backtest is the in-slice fork** (ship it here or tee as BT-3 with TIPPED Christoffersen). **Fetch Acerbi-Szekely 2014 to paragraph at planning (UNVERIFIED).** Shipped: the Acerbi-Tasche Prop-4.1 empirical tail mean; `risk.var.historical_es` v1 via the registry-map dispatch; the AS backtest TEED as BT-3 (Christoffersen finally homed; pairing via shared `input_snapshot_id`); demo stage 4 (the 18th code); AS 2014 verified-via-reproduction (primary gated). 4-finder review, zero HIGH. | M |
| 3 | **RS-1 — residual shrinkage/EWMA** ✅ **DONE** (planning **PR #60** = `8014b51`, branch tip `fdfbfa9` — merge sha corrected at the Wave-7 close; impl **PR #61** = `9c15658`, CI green run #365; **NO migration** — head stays `0041`) | The PA-4 OD-E/OD-G v2s REALIZED as two declared estimator conventions on `risk.proxy_weight.regression`: **`EWMA_RISKMETRICS`** (Axioma/RiskMetrics decay-weighted specific variance; declared λ; the s2 decoupling keeps OLS std-errors classical; raw v1 grandfathered via absent-convention) + **`SHRINKAGE_CROSS_SECTIONAL_EB`** (Barra USE4 empirical-Bayes cross-sectional shrinkage; DATA-DRIVEN per-instrument w_i via method-as-identity; N≥3-DISTINCT-instrument fail-closed; per-target transform run pinning a `RESIDUAL_SHRINKAGE_INPUT` cohort). NO new governed number/model code/permission/ENT/migration. **Primary sources VERIFIED** (the ledger's UNVERIFIED flag discharged): RiskMetrics TD 4th ed §5 + Axioma (EWMA), Barra USE4 (shrinkage) — **Ledoit-Wolf verified and explicitly NOT used** (it leaves variances unshrunk — the wrong primary for a residual-variance shrinkage). Demo **stage 5** = the SECOND lifecycle turn (the raw-sample-σ_e rider closed by 2 TRIGGERED re-validations; the `hostage…` finding flipped to historical, both directions test-pinned; + 2 INITIAL AWC dossiers for the new versions). 4-finder review: **2 HIGH + 5 MED + 7 LOW all folded, zero numeric defects, zero fence violations** (decision record Part 6). | S/M → M/L |
| 4 | **DS-2 — estimated-α desmoothing** ✅ **DONE** (planning **PR #63** = `0f199aa`; impl `ds-2-impl` via **PR #64** = `5120baa`, CI green run #381; migration head `0041`→**`0042_desmoothing_estimated_alpha`**) | The declared-α rider REMEDIATED: two declared estimator conventions on `perf.return.desmoothed_geltner` — **`AR1_ESTIMATED`** (α̂ = 1−ρ̂₁ in-run, T-denominator Box-Jenkins; fail-closed on ρ̂₁ ≤ 0/constant series; the CONSERVATIVE Bartlett 1/√n band persisted as `alpha_stderr`; the Kendall/Marriott-Pope small-n UPWARD bias of α̂ a registered limitation, never corrected in-run) + **`OKUNEV_WHITE_ITERATIVE`** (declared max order 1..4, ONE deterministic lag-i pass per order ascending; the '−' quadratic root settled by derivation + execution; the length-vs-order structural floor; alpha NULL on OW rows). **GLM MA(k) stays the named v2** — extraction-verified to equation numbers (Eqs. 21–23, 34, 47–58); its MLE needs constrained numerical optimization, the recorded determinism/TR-09 obstacle. NO new governed number/model code/permission/ENT (the RS-1 two-versions shape). Migration `0042`: alpha → nullable + `alpha_stderr` + the summary-only CHECK; the 0028 destructive RLS-safe downgrade sandwich proven under a non-superuser owner-member role. Demo **stage 6**: `PE-HARBORVIEW-IX` (16 marks, α_true = 0.4, searched fixture ρ̂₁ = 0.5038 ⇒ α̂ = 0.4962), the three-way declared/estimated/OW comparison, 2 INITIAL AWCs; **NO TRIGGERED — the census-proved no-closable-condition honesty** (the desmoothing versions carry EXCEPTIONs, not conditions-bearing AWCs; deliberate contrast with the MF-1/RS-1 flywheel). 4-finder review: **ZERO HIGH, ZERO MEDIUM** (adversarial 2 LOW + 4 INFO, numeric 0 defects + 3 caveats, doctrine 2 LOW, fence 9/9) + the missing-CI-step catch at the pre-push battery (the P3-7 class — the new 0042 PG suite had no ci.yml step). | S/M → M |

Then the **Wave-7 close review**. Delivery runs under the full-autonomy grant; the USER signs Tier-3 decisions.

## Part 2.11 — Wave 8: the ratified sequence (2026-07-19 at the Wave-7 close, OQ-W7C-6 fork **(A) "fund the third leg"**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the OQ-W5C-5 closure-stamp checklist incl. grep-for-"pending" AND the OQ-W7C-3 CI-run-id item)

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **BT-3 — the Acerbi-Szekely ES backtest** ✅ **DONE** (planning **PR #68** = `b493c78`; impl `bt-3-impl` tip `c5533b5` via **PR #69** = `109d11d`, **CI green run #399**; migration head `0042`→**`0043_es_backtest`**) | The ratified planning MUSTs DISCHARGED: the AS 2014 Z1 transcription re-verified two routes + the **null-expectation identity settling the '+1' grouping** (E[X\|X<−VaR]=−ES ⇒ '+1' OUTSIDE the sum; the corrupted inside-denominator grouping evaluates to ≈ −0.7004 at N(0,1)/a=0.025 — numerically COINCIDING with the −0.70 critical, so the exact-Fraction identity regression is the mandatory mechanical defense); the −0.70/−1.8 threshold VALUES at the three-route constant bar (M-P attribution + Lund Table 1 + this pass's executed seeded MC). **`risk.es_backtest` = the SIXTEENTH governed number** (the BT-1 mint precedent, OD-D ⇒ CC-2 becomes the SEVENTEENTH): Z2 + Z1 evidence rows with the verdict **DOMAIN-GATED to (paired confidence 0.9750 ∧ n_pairs 250)** — the planning verifier's ratification-blocking HIGH: the AS criticals are α-, T-, AND df-dependent (executed MC: 5% critical ≈ −3.68 at T=10 vs −0.70 only at T=250; a short-series verdict would spuriously REJECT the platform's own flagship ES); off-domain runs persist Z evidence + `ES_PAIR_COUNT` and NO verdict (the BASEL_ZONE read-rule precedent); the per-(α,T) critical table = the named v2 under a governed OFFLINE MC derivation (TR-09 — no runtime simulation). **Christoffersen ships in-slice as `risk.var_backtest` v2** (declared `independence=CHRISTOFFERSEN_MARKOV`, the COUNTING tri-state parse; LR_IND in the nonnegative orientation + `LR_CC = LR_UC + LR_IND` composed from the STORED 6dp legs; degenerate 2×2 ⇒ None; v1 byte-preserved; both Evers-Rohde source wobbles disclosed at the code site + referent) — the twice-re-teed item DISCHARGED. Sibling-pair gates (OD-C): VaR-HS/ES-HS legs pair per-as-of in BIJECTION on IDENTICAL `input_snapshot_id`, confidence uniform ACROSS legs, each leg model-version-UNIFORM; the behavior-preserving `adjudicate_return_side` extraction (BT-1 45/45 golden byte-identical). ONE additive migration **0043** (`es_value` echo). The OQ-W7C-2 tie-break fix folded BY NAME (`window_end DESC, system_from DESC, calculation_run_id DESC`). **Demo stage 7 = the DOMAIN-GATE HONESTY demo** — deliberately NOT a verdict (Z2 = −127.09 verdict-WITHHELD off-domain; the LR_CC joint-power lesson live at n=3: NEITHER Kupiec 3.66 nor LR_IND 2.77 alone rejects at χ²(1)=3.841 but the joint 6.43 > χ²(2)=5.991 DOES); 4 INITIAL AWCs; **NO TRIGGERED, census-proved** (the DS-2 honesty pattern); the 19th registered code. 4-finder review **ZERO HIGH** (the t3-attribution doctrine MED folded pre-merge; 2 named D-F4 next-touch deferrals → the Wave-8 close register). Battery: `make check` **1627**; fresh-schema local PG **1960/0/0** (all SEVEN demo suites in CI order); downgrade smoke clean. | S/M |
| 2 | **CC-1 — captured commitments / capital calls / distributions** ✅ **DONE** (planning **PR #71** + the rule-7 amendment **PR #73**; impl `cc-1-impl` tip `6fac50e` via **PR #74** = `1cdc95b`, **CI green run #420**; migration head `0043`→**`0044_private_capital`**) | The capture-first half of the Wave-8 headline (the PA-0 split) SHIPPED AS RATIFIED: **ENT-015 `commitment`** (FR bitemporal on the **(portfolio, instrument) stable identity** — the planning verifier's structural HIGH moved event linkage off FR version-row FKs; **currency CHAIN-IMMUTABLE**, no close-out op in v1, chain-terminate = the recorded v2) + **ENT-016 `capital_call`/`distribution`** (TWO truly-immutable IA tables under P0001 triggers; the provenance-only `commitment_version_id` echo, deliberately NOT an FK; the **negation FULL-reversal** correction — amount = exact negation, Σ self-correcting, one-reversal-per-event partial-unique race fence). The Tier-3 forks resolved as ratified: grain = (portfolio, fund-instrument); permission = the **`commitment.edit`/`.record`/`.view` three-code R-07 mint** (the FR/IA verb doctrine — identical maker holder sets, auditor_3l excluded). REQ-PRV-001/002 advanced to In-Progress (the computed-unfunded + liquidity-forecast clauses explicitly OPEN → CC-2); the `PRIVATE.*`/**EVT-240** seven-code audit family ACTIVATED (distinct REVERSE verbs; the PA-0 correction lessons pinned from birth); **rule-7 entity-filtered list reads from birth** (the first slice under it); the sanctioned `_raise_mapped_write` hoist to the shared `api/write_errors.py` (byte-identical, the 344 marketdata endpoint tests the golden). Demo **stage 8** = the CAPTURE half of the ratified commitment walk (PE-MERIDIAN-X; the mis-capture/REVERSE/recapture teaching pair live; the recallable distribution as data; **counts pinned UNCHANGED 19/34/95** — capture-only honesty + the no-auto-bridge zero-transactions assertion). **4-finder review: ZERO HIGH — 8 MED + ~10 LOW ALL folded** (the post-quantize (20,6) amount envelope closing the zero-amount-immutable-row + unmapped-500 holes; the honest no-close-out wording; the read rule at every promised surface; the per-op event-count pins; scope-fence 12/12). Battery: `make check` **1660**; fresh-schema local PG **2005/0/0** (all EIGHT demo suites in CI order); downgrade smoke clean. | M |
| 3 | **CC-2 — the governed commitment-pacing projection (the HEADLINE)** ✅ **DONE** (planning **PR #76**; impl `cc-2-impl` tip `a266b56` via **PR #77** = `1eaa703`, **CI green run #432**; closeout `cc-2-closeout`; migration head `0044`→**`0045_pacing_projection`**) | **`pacing.commitment_projection` = the SEVENTEENTH governed number** (ENT-059), the FIRST private-capital derived output — it discharges the REQ-PRV-001 "computed" leg CC-1 left open. **The deterministic Takahashi-Alexander recursion (JPM 28(2):90–100) — the TA fetch MUST DISCHARGED via reproduction** (primary gated; R1 Jaeckel + R2 Tamarix equation-level, R3 Luxenberg-Boyd structural, R4 FRG input-list; per-source grades in the referent), so the fallback rider did NOT fire: `C(t)=RC(t)·Unfunded(t−1)`; `RD(t)=max(Y,(t/L)^B)`; `D(t)=RD(t)·NAV(t−1)(1+G)`; `NAV(t)=NAV(t−1)(1+G)+C(t)−D(t)`. **FUTURE-ONLY from a pin-derived deterministic age** (the planning verifier's period-geometry HIGH resolved by delivery, TR-09-safe), QUANTIZE-THEN-ROLL 6dp, ANNUAL, NO optimizer. **The FIVE declared parameters ARE the version identity** (parsed back fail-closed); **NO TA constant minted** — only the `functional_form=TAKAHASHI_ALEXANDER` marker. Tier-3 forks resolved as ratified: the new peer `pacing` family (neither risk nor perf); the **`pacing.run`/`pacing.view` R-07 mint** (a governed-output `.view` INCLUDING `auditor_3l` — the decisive contrast with CC-1's captured-input `commitment.view`). `PACING.*`/**EVT-250** RESERVED-not-minted (`CALC.RUN_*` reused). Consumes ONLY the `PACING_INPUT` snapshot (commitment + call/distribution events + the latest mark); the mid-life re-anchoring (`Unfunded(0)=committed−ΣPaidIn+Σrecallable`; `NAV(0)` from the pinned same-currency mark, funded-without-mark REFUSED) = OUR documented adaptation. **Rule 7 all three legs IN-SLICE + the platform's FIRST latest-resolver** (`list_pacing_projections`/`latest_pacing_projection`, `as_of`-aware, cross-run aggregation a CONSUMER ERROR). **Demo stage 9** = the projection half of the commitment walk on the stage-8 PE-MERIDIAN-X commitment — **the counts MOVE 19/34/95 → 20/35/96** (the governed contrast with stage 8's capture-only-unchanged is the demo's own story). **4-finder review: ZERO surviving HIGH** — the adversarial+doctrine finders converged on ONE real FAILED-magnitude-gate cluster (the envelope `1e26` sat ABOVE the `Numeric(28,6)` column cap ~1e22 → PG overflow, AND extreme growth overflowed the Decimal context before the gate → 500; fixed: envelope→`1E21` the var/es-backtest precedent + a kernel `_MAGNITUDE_CEILING` so runaway params FAIL the run) + 3 LOW (`-0` identity, register/bind magnitude symmetry, Feb-29 clamp) ALL folded with tests (incl. the previously-untested FAILED path); math CONFIRMED (both hand-goldens re-derived), scope-fence CLEAN. Battery: `make check` **1716**; fresh-schema local PG green (all NINE demo suites in CI order); `0045` downgrade clean. | M/L |

**Ratified riders (OQ-W7C-6):** SC-2 stays the **named pull-forward on underrun** (carried verbatim — its Wave-7 condition EXPIRED UNSPENT); the **stage-7 demo obligation** (backtest the flagship ES at BT-3; walk one commitment capture→projection at CC-1/CC-2 — converting the two largest recorded gaps into living-tenant evidence; the 5 zero-run registered codes stay the named demo-completeness ride-along candidate, OQ-W7C-5); the **slot-zero option** (an HG-2-sized opener precedes BT-3 only if TIPPED debt surfaces — none carried in). Then the **Wave-8 close review**. Delivery runs under the full-autonomy grant; the USER signs Tier-3 decisions. **WAVE 8 CLOSED + RATIFIED 2026-07-19→20** (`wave_8_close_review.md`; the fourth consecutive zero-shipped-defect close) — all three slices DONE; Wave 9 ratified below.

## Part 2.12 — Wave 9: the ratified sequence (2026-07-20 at the Wave-8 close, OQ-W8C-3 — the **read-surface + front-end + identity** wave, funding the UI-is-core premise; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist)

**The organizing fact (from the Wave-8 close, Parts 3–4):** the private-capital + backtest machinery is now BUILT but not yet LEGIBLE — the governed read surface is strictly run-centric (F1: 13 families read only by run/result id; CC-2 shipped the FIRST latest-resolver as the template), the governance story is the least-readable part of the API (F2), the FE types are hand-mirrored and drifting (F3), and identity is a dev-placeholder shim (F4). Wave 9 makes the differentiator legible to a screen AND an agent (thesis §2.3).

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **API-1 — the governed read surface + the F2 governance reads** ✅ **DONE** (planning **PR #81**; impl `api-1-impl` tip `0288f29` via **PR #82** = `ae588a5`, **CI green run #449**; NO migration) | Back-filled entity/time-centric reads across the **8 entity-native families** (Class A: perf ×3, risk-backtests ×2, factor-exposure, proxy-weight, exposure — the shared `calc/reads.py` helper factoring the CC-2 `list_pacing_projections`/`latest_pacing_projection` template) + **Class-B whole-run/curve latest-run resolvers** (covariance — no entity filter; scenario — `scenario_definition_id`; sensitivity — curve selector) + the **2 by-id parity GETs** (`scenario-results/{id}`, `proxy-weight-estimates/{id}`) closing the house-pattern asymmetry. The **four F2 governance reads**: validation findings/evidence detail (`GET /models/{id}/validations/{vid}`, gated `model.inventory.view`); `tier`+`validation_status` added to the inventory list DTO; a snapshot listing (`GET /snapshots?purpose&as_of_valuation_date`, gated `snapshot.view`, headers only — no portfolio filter, the API-1b scope-column gap); the **audit-trail read** — a NEW read-only `audit/queries.py` (the FROZEN `audit/service.py` NEVER imported) + `api/audit.py` router (`GET /audit/events`, filtered + paginated, gated the RATIFIED `lineage.view` reuse — no new R-07 mint; metadata-only DTO, `before_value`/`after_value`/`justification` deliberately NOT surfaced). **PURE read-only: no migration, no new writes, no permission-posture change.** **RIDER PAID (OQ-W8C-4 / OQ-W7C-5): demo stage 10** executes the 5 registered-but-never-run codes (sensitivity/active-risk/scenario/benchmark-relative/proxy-exposure) — RUNS-ONLY, counts move **20/35/96 → 20/35/101** — so the new reads render non-empty; the stage-10 prereq chain (a captured curve; one benchmark head carrying BOTH membership + a DIETZ-aligned return series; a scenario definition + shocks) resolves BEFORE any run write, factor-set-matches the covariance run to `{FX_USD,FX_EUR}`, and scopes the proxy run to the campaign's currency-only DEMO-GLOBAL book (the family admits currency factors only). **The flagship VaR/active-risk entity read is DEFERRED to API-1b** (verifier F1 HIGH: subtree-scoped runs, no recorded root portfolio — not read-only-resolvable; user-ratified 2026-07-20). **4-finder review: ZERO HIGH from any lens** — doctrine/security CLEAN (all 6 hard invariants independently verified: `audit/service.py` byte-unchanged + never imported, `lineage.view` reuse only, every new read tenant-scoped + metadata-only, zero new mutating routes); ONE MED caught by two finders (the audit `_iso` reader didn't mirror the FROZEN writer's naive-datetime-as-UTC guard — a real off-UTC compliance-window bug, now fixed + regression-tested) + one MED test-coverage gap (exposure + perf desmoothed/benchmark-relative HTTP reads shipped untested — closed with real-data + smoke tests) + one LOW dead constant, ALL folded. Battery: `make check` **1736**; fresh-schema full-PG battery green (all TEN demo suites in CI order, `stage9z` filename collating last — the CC-2-recorded stage10 zero-pad hazard); downgrade-base smoke clean with stage-10 rows present. | M/L |
| 1b | **API-1b — the flagship VaR/active-risk entity reads** (the named fast-follow; OD-API-1-H) | "Latest VaR / active-risk for portfolio P" — the single most-wanted UI/agent read. Needs ONE additive column `calculation_run.scope_portfolio_id` (the ROOT portfolio a run was scoped to, binder-stamped at run creation; historical runs stay NULL, honestly unresolvable), then the Class-C `list`/`latest` reads resolve `var_result`/`active_risk_result` → `scope_portfolio_id == P` (correct for hierarchical books). Read + ONE small migration + a bounded exposure/var/active-risk binder touch; run-id reads + TR-09 untouched. Deferred ONLY because it needs the scope column — not dropped. | S/M |
| 2 | **FE-2 — OpenAPI-generated FE types** ✅ **DONE** (planning + impl on `fe-2-impl`, **PR #84** = `2ce9e4a`, **CI green run #456**; NO migration) | **Precondition CONFIRMED**: every governed response decimal already serializes as `string` (zero response `*RowOut` fields are `number`; only the ~50 unconsumed `*In` request bodies carry the Pydantic-default `number\|string` union) — so codegen introduces NO `Number()` corruption risk on the read path. Pipeline: `scripts/dump_openapi.py` (deterministic `app.openapi()` dump) → `openapi-typescript` → committed `openapi.json` + `api-types.d.ts`, with a NEW dedicated CI job **"API type drift"** (Python+Node — the Frontend job is Node-only) that regenerates + `git diff --exit-code`s, turning backend↔FE drift RED. `FAMILY_ROW_COLUMNS` column keys **bound** to the generated `*RowOut` types (`keyof FamilyRowOut[F]`) — **the FL-1 kill, PROVEN**: a bogus column key fails `tsc` TS2322. The GET-only hand-written `apiGet` client is **KEPT** (no `openapi-fetch`/orval — preserves the read-only fence, no new runtime dep). OD-FE-1-G superseded-in-part (dated); the strings-verbatim decimal contract (OQ-FE-1-7) mechanized as a compile-time guard. **4-finder review: 1 HIGH folded** — the adversarial finder proved the initial 8-field decimal guard sampled too narrowly (~35 displayed decimals unguarded; a `VarRowOut.sigma`→`number` regression compiled clean, reopening the exact FL-1 corruption class) — fixed with an **EXHAUSTIVE** guard (`OnlyCountsAreNumbers` over all 14 `*RowOut`: the only `number` fields anywhere may be known integer counts), proven to bite on the exact regression scenario. Also folded: 1 MED (disclose `openapi-typescript`'s dev-tree `@redocly` advisories — 3 HIGH, dev-only, CI production audit stays 0, codegen-time-only/never-shipped) + 2 LOW doc corrections. The determinism risk (would CI's Python 3.12 regenerate byte-identically?) was **discharged** by reproduction (pinned pydantic/fastapi + `sort_keys` + `npm ci` → byte-identical). Battery: `make fe-check` green (typecheck/lint/format/65 tests/build); `make gen-api-check` no drift; bundle byte-identical (types-only, tree-shaken). | S/M |
| 3 | **SSO-1 — real identity (OIDC / AD-007)** ✅ **DONE** (planning + impl on `sso-1`, **PR #86** = `422c164`, **CI green run #466**; NO migration) | Replaced the dev-placeholder header shim with a real **OAuth2 resource server**: `get_principal` dispatches on `auth_mode` — `oidc` (the new default, fail-closed) verifies an `Authorization: Bearer` JWT against the issuer JWKS (PyJWT; RS256-only allow-list — `alg=none`/HS-confusion rejected; `iss`/`aud`/`exp`/`nbf` + required subject+tenant claims; `leeway=60s`; optional `acr`/`amr` MFA assertion) and resolves the verified `sub`→`app_user.external_subject` within the verified tenant claim, building `Principal(user_id=app_user.id, ...)` — NOT the raw `sub`, since `has_permission` joins on `app_user.id`. `dev_header` (the old shim) survives as an explicit fallback, permitted **only** when `app_env=='local'`, enforced by a fail-closed startup guard (`validate_auth_config`, run from the FastAPI lifespan — never at import, which would explode CI at collection) plus a per-request backstop. Enforcement behind the shim (`require_permission`, FORCE RLS) was **untouched** — a bounded identity-swap, not a rebuild, exactly as scoped. Closes **OD-048** (local-dev OIDC provider = **Keycloak**, `infra/keycloak/`, profile-gated compose + realm export + runbook, NOT a CI dependency — CI verifies with locally-signed RS256 test keys). Demo `_seed_principals` sets `external_subject` on the two demo principals (a column set only — counts held at 101 runs). **4-finder review: 2 HIGH folded.** (1) **RLS false-deny**: the `tenant_isolation` policy compares `tenant_id::text` (PostgreSQL renders a uuid lowercase-hyphenated) against the RAW `app.current_tenant` GUC — a valid-but-non-canonical UUID tenant claim (uppercase, no-hyphens) was RLS-hidden, silently locking out a legitimate user. Fixed by canonicalizing `str(uuid.UUID(claims.tenant))` before the GUC bind; a NEW PG-tier regression (`test_oidc_tenant_canonicalization_pg.py`, run under a constrained non-superuser/non-BYPASSRLS role) proves the raw form is hidden and the canonical form resolves — SQLite cannot catch this class (RLS is a no-op there). **`_principal_from_token` is the first caller ever to feed `set_tenant_context` a raw externally-controlled string** — every prior caller sourced the tenant from a DB round-trip, always already canonical. (2) **CI drift**: the new `authorization` header dependency is stamped by FastAPI into every operation's OpenAPI schema (231 param blocks), staling the committed `openapi.json` + generated FE types and turning FE-2's own "API type drift" job red — regenerated and committed; `make gen-api-check` clean and deterministic on re-run. Also folded: **4 MED** (require `OIDC_AUDIENCE` in oidc mode — an unset audience let a token minted for a different client of the same issuer authenticate, a confused-deputy gap; a pathological nested `amr` claim crashed the MFA check to 500 instead of denying; `oidc_require_mfa` without `oidc_acr_values` booted clean then 500'd every request — now caught at startup; the oidc `app_user` resolution had zero Postgres-tier RLS coverage before the fix above) + several **LOW** (the `dev_header` per-request `app_env` backstop; the advertised `exp`/`nbf` clock-skew leeway was actually zero — now a real 60s; a JWKS/discovery outage returned 500 — now an honest 503; new tests for the lifespan wiring, `build_verifier`/discovery, `amr` acceptance, and the demo seed). Battery: `make check` **1778 passed** (+15 auth tests over pre-fold); full-PG demo battery green (demo tenant holds at 101 runs); `make fe-check` green (65 tests + build) after the OpenAPI regen; `make gen-api-check` clean. **Lesson: any code path that arms a Postgres RLS tenant GUC from an externally-controlled string must canonicalize it first — the RLS policy's `::text` cast and the raw GUC string are not guaranteed to agree in format even when they represent the same UUID.** | M |
| 4 | **FE-3 — the product UI** ✅ **DONE** (planning + impl on `fe-3`, **PR #88** = `a0f31b5`, **CI green run #475** (Actions run id 29852439513); NO migration) | The first surface a non-developer sees. Replaces the generic run browser with a six-step **governance-narrative walk** over the living demo book (Capture → Exposures → Numbers → Backtest → Validation → Limitations — OD-FE-3-A, the IA ratified at planning as OQ-FE-3-1, the Tier-3 USER decision deferred from the Wave-8 close). The differentiator lives in `GovernedValue` (OD-FE-3-C): every number renders with value verbatim + run/model provenance + a structurally-unfakeable `snapshotVerified` mark (only shown when a real `snapshotId` is present) + the model's validation badge and disclosed limitations. VaR is shown honestly as the seeded run series across all books with the API-1b entity-read gap stated inline — no synthetic "latest for this portfolio." Every pane degrades gracefully on a 403 to a calm permission note (OD-FE-3-E), proven live against a demo `auditor_3l` read-only viewer principal (OD-FE-3-H, folded pre-review). The GET-only `apiGet` fence and the FE-2 generated-type binding are KEPT and extended. **4-finder review: ZERO HIGH** — both existential invariants proven independently (decimal-strings-verbatim on every new screen, incl. the EXHAUSTIVE guard extended to `PositionOut`/`ValuationOut`; no fabricated verification/verdict, structurally prevented not just avoided). Folded: **2 MED** (RunDetail's "back to runs" link stale after FE-3 moved `/` from the run list to the walk overview; `useModelValidations` took the FIRST validated model version instead of the LATEST — would have shown a stale outcome for a re-versioned + re-validated model, the exact lifecycle the Validation step narrates) + **6 LOW** (the decimal-guard extension, a WCAG 2.4.7 visible-focus fix, non-unique-key→index-key fix on limitation lists, 3 honesty wording fixes). Battery: `make fe-check` green (97 tests+build); `make check` (Python) green; `make gen-api-check` clean (FE-only); `package.json` byte-identical (no new runtime dep, OD-FE-1-F held). **One post-review fix before merge**: the fold commit's re-wrapped object literals/JSX across 9 files hadn't been re-run through Prettier, so CI's `format:check` went red — a pure-reflow fix (`3275741`), zero semantic change, all gates re-verified green. Closed Wave 9. | L |

**Ratified riders (OQ-W8C):** the API-1 5-code demo-completeness rider (OQ-W8C-4) — **PAID by API-1's demo stage 10**; FE-3's IA as a deferred Tier-3 USER decision (OQ-W8C-5) — **RATIFIED at FE-3 planning as OQ-FE-3-1**; the two planning-gate caveats (API-1 scope split — resolved: Class C deferred to API-1b; FE-2 decimal-as-string — **CONFIRMED, resolved**). Carried debt into the Wave-9 close register: the BT-3 D-F4 registered-string reword (a dedicated ES/var-backtest next-touch, OQ-W8C-2); the FE-2 dev-tree `@redocly` advisory disclosure; the SSO-1 `pyjwt`/`cryptography` advisory surface — the first Python runtime deps with one, no `pip-audit` CI gate exists to check them against; the FE-3 `auditor_3l`-template read-gap (a demo-only viewer principal, not a general onboarding template — flag if a second read-only role is ever needed). The no-FE-expansion rule LIFTS for this wave (it IS the FE wave). **SSO-1 DONE 2026-07-21** (F4 discharged); **FE-3 DONE 2026-07-21** (see the slice-4 row above) — **WAVE 9 FUNCTIONALLY COMPLETE, all four slices shipped**. **WAVE 9 CLOSED + RATIFIED 2026-07-21** (`wave_9_close_review.md`, OQ-W9C-1/2/4/5 "Approve all" + OQ-W9C-3 fork A; the FIFTH consecutive zero-shipped-defect close). Next = **Wave 10 (Part 2.13): API-1b → FE-3b → a §2.1 private/public-unification headline**.

## Part 2.13 — Wave 10: the ratified sequence (2026-07-21 at the Wave-9 close, OQ-W9C-3 fork A "finish the surface first" — **complete the read-surface + identity product Wave 9 opened, then open the §2.1 destination**; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist)

**The organizing fact (from the Wave-9 close, Part 4):** Wave 9 made the differentiator *legible* (F1–F4 discharged; governance readable to a screen AND an agent, thesis §2.3) but left **two named legibility holes** — the flagship "latest VaR/active-risk for portfolio P" read still 404s (the deferral was a verifier *refutation*, not a punt), and no browser can actually *log in* (the demo rides `dev_header`). Wave 10 finishes the surface, then pivots to the §2.1 private/public-unification destination the whole math roadmap declared itself the substrate for.

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | **API-1b — the flagship VaR/active-risk entity reads** ✅ **DONE** (OD-API-1-H, the named fast-follow; planning + impl on `api-1b-planning`/`api-1b-impl`, **PR #92** = `f1e830f`, **CI green run #488**; migration `0046_run_scope_portfolio`) | "Latest VaR / active-risk for portfolio P" — the single most-wanted UI/agent read, resolved at the **write** boundary (API-1's verifier had refuted read-only resolution). **ONE additive column `calculation_run.scope_portfolio_id`** (nullable; the ROOT portfolio a run was scoped to) threaded through the single `create_run`/`execute_governed_run` choke point and stamped by all five binders — `run_exposure` from its direct `portfolio_id` arg, `run_factor_exposure`/`run_var`/`run_var_historical`/`run_active_risk` copying it forward from their resolved upstream run (proven correct in BOTH the build and snapshot-consume input paths). The Class-C `list`/`latest` reads resolve `var_result`/`active_risk_result` → `scope_portfolio_id == P` via the EXISTING `calc/reads.py` helper (zero helper change); `active_risk`'s native `benchmark_id` filter also lands. Historical-NULL honesty (OD-API-1b-D): a snapshot-consume-rooted chain (exposure OR factor) stays NULL and is disclosed-unresolvable, not back-fabricated (OQ-API-1b-1 = A, ratified) — the fully build-in-request chain (demo/UI/default-API) always stamps a real root. **Riders (OQ-W9C-4/5, both landed here):** a `pip-audit` CI gate on the INSTALLED ENVIRONMENT (review-corrected from `-r requirements-dev.txt`, which missed the `python-multipart` runtime dep — true npm-parity) + a closure-discipline docs-check (filename-keyed, row-anchored; fails on a DONE-in-roadmap record still reading "DRAFT for ratification" — teeth for the 5th-consecutive stamp miss, unit-tested to actually FIRE). **Pre-ratification verifier pass RAN**: the crux (copy-forward correctness) + TR-09 neutrality + migration neutrality + read non-shadowing all HELD; 2 COMPLICATED findings folded as wording/spec fixes (the exposure-snapshot 2nd NULL-origin; the closure-check mechanic). **4-finder review: ZERO HIGH** — write-path (copy-forward correct across all 5 binders + both paths, immutable, TR-09-neutral, complete), doctrine/security (all 6 hard invariants held; the cross-tenant probe confirmed `scope_portfolio_id` is NOT a security boundary — RLS + explicit tenant filter double-bind it), read-correctness (filters/latest-resolution/route-ordering all correct; zero shadowing), CI-riders+honesty (found the real pip-audit target hole + the untested closure-check teeth). Folded 5 MED + 1 LOW: the pip-audit target fix; the closure-check's teeth were untested (added a failure-path test proving it FIRES); the closure-check's guarantee was over-scoped in its own comment (corrected to the go-forward cadence); the record's "`/latest` 404" wording was a mis-cite (the shipped list-shaped `/latest` returns `[]`, matching the covariance/sensitivity/factor-exposure/var-backtest siblings — pacing's 404 is for a single-object resolver); the copy-forward tests strengthened from non-null to value-equality. Battery: `make check` green (Python); `make fe-check` green (97 tests+build); `make gen-api-check` clean (4 new paths, deterministic); full-PG affected-family battery green; `0046` downgrade/upgrade smoke + `alembic check` clean; `pip-audit` (installed env) clean (one disclosed allowlist: `PYSEC-2026-1845`, a dev-only pytest advisory — `pyjwt`/`cryptography`, the identity surface, audit CLEAN and are NOT ignored); `pydantic-settings` bumped 2.14.1→2.14.2 clearing a real advisory the gate surfaced. Counts UNCHANGED (17/20/35/101 — pure read + one scope-provenance column, no new runs/codes/records). | S/M |
| 2 | **FE-3b — the SPA OIDC/PKCE browser login** ✅ **DONE** (the named fast-follow; planning + impl on `fe-3b-planning`/`fe-3b-impl`, **PR #95** = `2cbb68c`, **CI green**; NO migration) | Turns SSO-1's real resource server into something a non-developer can actually reach: the hand-rolled browser auth-code + PKCE flow (Web Crypto, zero new runtime dep — OQ-FE-3b-1=A, OD-FE-1-F held) against the Keycloak `irp-frontend` public client, sending `Authorization: Bearer <token>` instead of the `X-User-Id`/`X-Tenant-Id` dev-header shim. A discriminated-union `Session = DevSession | OidcSession` (`kind` discriminant) swaps the wire bytes at the single `client.ts` choke point; `DevSession → Session` widened across 16 pass-through signatures (type-only, mechanical). `/callback` lands ABOVE the session gate: validates `state` (CSRF) before the exchange, strips `?code` from the URL synchronously, single-use verifier+state, StrictMode ref-guard. `dev_header` mode preserved unchanged (`DevBanner` shown only there — a verified Bearer session hides it, since showing "unverified" over it would be a lie). Demo end-to-end (OQ-FE-3b-3=A): the `demo-auditor` realm user + BR-10-marked local passwords + the compose backend oidc-profile env INCLUDING `OIDC_JWKS_URI=http://keycloak:8080/…/certs` (the issuer-consistency fix a naive `ISSUER`/`AUDIENCE`-only wiring would 503 on every login). **4-finder review folded 1 HIGH** (the deployed nginx image shipped no SPA history-fallback, so the `/callback` redirect 404'd — the ratified demo login could not complete; fixed via `infra/docker/frontend-nginx.conf` with `try_files … /index.html` + a backend read-proxy for the 13 governed prefixes) **+ 3 MED** (the OIDC logout redirect-back mismatch; `beginLogin` untested — a silent PKCE downgrade would have slipped; the stale "~10 files" footprint claim) **+ LOWs** (a non-secure-context guard; a `no-referrer` meta tag). Gates: `make fe-check` green (tests + build); `make check`/`make gen-api-check` no-op (NO backend logic change); `package.json` byte-identical (zero new runtime dep); `npm audit --omit=dev` 0. Counts UNCHANGED (17/20/35/101). | M |
| 3 | **The §2.1 private/public unification** (the headline; name minted at planning) — ✅ **ALL 3 ARC SLICES DONE** ✅ **PPF-1** (**PR #98** = `9d64b49`, **CI green**; migration `0047`) · ✅ **PPF-2** (planning **PR #100**; impl **PR #101** = `7aefd1c`, **CI green run #519**; the 19th governed number `risk.covariance.private`; reuses `covariance_result`, **NO migration**) · ✅ **PPF-3** (planning **PR #103**; impl **PR #104** = `633e855`, **CI green, all 6 jobs**; migration `0048_var_private_variance`; the **20th governed number**, `risk.var.parametric_unified`; counts 22/37/104 → **23/38/109**) | **THE §2.1 DESTINATION IS SHIPPED.** The differentiator's endgame — the unified public+private **portfolio-level** risk number — closed the 3-slice arc: **PPF-1 = the pure-private factor RETURN series** (the 18th governed number, `risk.factor_return.pure_private`) → **PPF-2 = the private covariance block Ω_pp** (the 19th; a fail-closed SIBLING reusing the generic covariance kernel + the shared `covariance_result` table, `frequency=APPRAISAL`/`run_type=COVARIANCE_PRIVATE`; block-diagonal-with-Σ disclosed as an APPROXIMATION) → **PPF-3 = the unified number** `σ_unified = √(x'Σx + p'(Ω_pp/d_t)·p + residual_over_non-private-members)`, its own binder `run_var_unified`, a REPARTITION (not a naive assembly — two pre-ratification verifiers refuted the naive additive formula as a variance double-count). **4-finder review: 1 HIGH folded** — three finders independently converged on a consume-path re-opening of the double-count (the builder's repartition exclusion wasn't re-enforced at the `snapshot_id` trust boundary; now refused on both paths) — **+ 2 MED** (a held-segment off-diagonal completeness gate, parity with the public leg; a disclosure-honesty overstatement in a REGISTERED model assumption + the governed validation record, corrected — the unified number's value over total-VaR is the Ω_pp OFF-DIAGONAL cross-fund co-movement PLUS a smaller diagonal re-estimation, not the off-diagonal alone). Demo stage 13 (`DEMO-UNIFIED-PPF3`, two existing private funds, zero new instruments/segments/Ω_pp) proves the headline live: σ_unified=85.758098 ≠ σ_total=85.685795, unified `residual_variance` EXACTLY 0 vs total's 155.35 — the double-count fix end-to-end. Full 4-finder review per slice. | L |

**Ratified riders / carries (OQ-W9C):** the **Python `pip-audit` CI gate** (OQ-W9C-4) and the **closure-discipline CI docs-check** (OQ-W9C-5, teeth for the 5th-consecutive missing-stamp class) — **BOTH LANDED on API-1b, 2026-07-21** (see the dated log below; the pip-audit gate audits the installed environment, review-corrected from a `-r requirements-dev.txt` scan that missed a runtime dep). Carried standing: the BT-3 D-F4 registered-string reword (a dedicated ES/var-backtest touch, OQ-W8C-2); the FE-2 `@redocly` dev-tree advisory (dev-only, disclosed, no action); the FE-3 `auditor_3l` demo-viewer (demo-scoped; promote to an onboarding template only if a second read-only role is genuinely needed); the two Wave-9 LOW completeness residuals (the JWKS live-IdP integration test → naturally FE-3b territory; the stage9z presence-not-precision read proof); MG-2 remediation-lifecycle (erosion, earliest overdue 2027-07-19); the first scheduler; OD-B expire-a-mapping. **API-1b DONE 2026-07-21** (see the slice-1 row above). **FE-3b DONE 2026-07-21** (see the slice-2 row above) — **WAVE 10 SLICES 1+2 COMPLETE**. **PPF-1 DONE 2026-07-22** (see the slice-3 row above) — the §2.1 headline's arc slice 1 of 3 (the pure-private factor return, the 18th governed number). **PPF-2 DONE 2026-07-22** (see the slice-3 row above) — arc slice 2 of 3 (the private covariance block Ω_pp, the 19th governed number). **PPF-3 DONE 2026-07-23** (see the slice-3 row above) — arc slice 3 of 3, THE CAPSTONE (the unified public+private VaR, the 20th governed number) — **WAVE 10 FULLY COMPLETE, all three slices shipped, THE §2.1 DESTINATION THE WHOLE PLATFORM THESIS POINTED AT IS NOW SHIPPED**. Carried forward from FE-3b: `directAccessGrantsEnabled:true` on the public `irp-frontend` client (ROPC open, pre-existing SSO-1 realm, used by the README curl example — consider disabling post-demo). **The user's standing note holds: real-data use + demos come AFTER the build — the §2.1 unification is a build-first slice, not a data-ingestion project.** **NEXT = the Wave 10 close review** (the mandatory Part-4 rule-2 re-baseline).

## Part 2.14 — Wave 11: the ratified sequence (2026-07-23 at the Wave-10 close, OQ-W10C-4 fork A "OPERATIONALIZE" — make the governed engine RUN, not just compute; same per-slice discipline — decision record + plan + pre-ratification verifier pass + OQ ratification + adversarial review + the closure-stamp checklist, now with the OQ-W10C-broadened CI teeth)

**The organizing fact (from the Wave-10 close, Part 4):** the §2.1 destination is shipped, so the platform's largest distance-to-frontier is no longer *methodology* — it is that a governed engine which computes 20 numbers beautifully **cannot run on a cadence, enforce a limit, or carry a remediation to term**. Wave 11 builds that operational half — the first thing in the platform that is not request/response reuse.

| # | Slice | What it is | Size |
|---|---|---|---|
| 1 | ✅ **SCH-1 DONE** (PR #108 `96965cf`, CLOSED 2026-07-23) — **the first scheduler** (the prerequisite primitive) | Background/cadenced execution: nothing in the platform currently runs unless hand-invoked, so "current risk" is only as fresh as the last manual run. The FIRST genuinely-new architectural primitive (not a request/response reuse) — a governed, auditable, tenant-scoped scheduled-run mechanism that re-runs a governed number on a cadence and records provenance. A prerequisite for limit monitoring (needs fresh numbers), remediation deadlines, and any future data refresh. Design forks at planning: the execution surface (the existing worker vs a new one); at-least-once vs exactly-once semantics + idempotency against the governed-run scaffold; how a scheduled run binds its snapshot (re-pin fresh vs carry-forward); the cadence/calendar model. External practice to cite: BCBS 239 risk-data timeliness, SR 11-7 ongoing-monitoring cadence. | M/L |
| 2 | ✅ **LIM-1 DONE** (PR #111 `218afc9`, CLOSED 2026-07-23) — **limits / breach workflow** (the most conspicuous missing product capability, RTM-P6) | A governed limit entity + breach detection over the numbers SCH-1 keeps fresh: "alert me when VaR (or active risk) breaches X." The first governed *write-side workflow* the platform has never had — a `limit_definition` (ENT-031, EV, threshold + scope + metric-selector + `breach_direction` + HARD/SOFT), a deterministic breach evaluation (a PHASE of the per-tenant operational tick, over pinned `calculation_run` results — Fable demand #2), and a self-describing IA append-only `breach` record (ENT-033, FKs the evaluated `calculation_run_id`). R-07 mint `limit.manage` (2L-maker SoD) / `.view` / `breach.view`; migration `0050`; NOT a governed number (counts UNCHANGED 23/38/109). Ratified: OQ-1=A (EV header + self-describing echo; history via `LIMIT.CHANGE`) / OQ-2=A (exact `scope_portfolio_id`) / OQ-3=A (VAR/`var_value` + ACTIVE_RISK/`te_value`, hardcoded map + fail-closed unit assert) / OQ-4=A (DETECT+record+read; lifecycle + `LIMIT.APPROVE` deferred to MG-2). 4-finder ZERO HIGH, 6 MED folded (silent-green fail-open: `limit_health` recomputes from source; precision overflow `(28,6)→(34,12)`; P3-5 cross-tenant FK guard). | M/L |
| 3 | ✅ **MG-2 DONE** (PR #113 `aa6503f`, CLOSED 2026-07-23) — **the breach remediation lifecycle** (the governance-workflow-with-teeth; the eroding register item PAID) | Carry a breach to term: realize the reserved **ENT-034 `breach_action`** (IA append-only) as the **DEP-WFL** 5-state machine `DETECTED→ASSIGNED→RESPONDED(1L)→REVIEWED(2L)→CLOSED` + an orthogonal `ESCALATED`, with **auto-escalation** riding the SCH-1 cadence (a THIRD tick phase, a `response_due < now` deadline check) and the platform's **FIRST person-level SoD** (reviewer/closer ≠ any prior 1L responder — REQ-BRC-002/SOD-02). R-07 mint `breach.respond` (1L) / `breach.review` (2L); migration `0051`; audit codes genesis-reserved → REALIZED; NOT a governed number (counts UNCHANGED 23/38/109). Ratified OQ-1=B (lifecycle now; **LIMIT.APPROVE split to MG-3**) / OQ-2=A (append-only action log, recency state) / OQ-3=A (the transition/role/SoD map) / OQ-4=A (auto-escalate). Pre-ratification verifier folded 3 BLOCKING concurrency holes (nondeterministic recency state → a monotonic `seq`; escalate TOCTOU → a `(breach_id, response_due)` epoch + per-breach `FOR UPDATE`; SoD latest-responder bypass → the all-responders set). | M/L |
| 4 | **MG-3 — the LIMIT.APPROVE maker-checker gate** (split from MG-2 at ratification, 2026-07-23; the NEW final slice) | Close the one letter LIM-1 left formally unmet (`REQ-LIM-001`/`BX-SOD` "limit changes are maker-checked"): a DRAFT→ACTIVE limit lifecycle gated by a distinct approver. Mints `limit.approve` (R-07) and realizes the genesis-reserved `LIMIT.APPROVE` audit code; reuses MG-2's person-level SoD primitive (approver ≠ the `created_by` drafter). Split from MG-2 because it is a distinct governance primitive that changes the limit EVALUATION path (a DRAFT limit is not evaluated) and warrants its own adversarial review. NOT a governed number. | S/M |

**Ratified riders / carries (OQ-W10C):** the **PPF-3 v2 seams (leverage-led)** — the destination number's disclosed refinements, deferred as the fork-B alternative (leverage is the one load-bearing math gap but needs new captured leverage data; a strong candidate for a later wave or a real-data slice, per the user's standing "real-data after the build" note). Carried standing: the BT-3 D-F4 registered-string reword (OQ-W8C-2); the FE-3b `directAccessGrantsEnabled:true` ROPC hygiene (disable if anything goes internet-facing); the demo-only `covariance_result` factor-set read (LOW, cannot leak Ω_pp); OD-B expire-a-mapping; SC-2 (expired unspent again); the `@redocly` dev-tree advisory (no action); the `auditor_3l` demo-viewer (demo-scoped); the JWKS live-IdP integration test; the stage9z presence-not-precision proof. **Process (OQ-W10C-5):** a mechanical governance gate must be unit-tested against the ACTUAL failure forms it exists to catch (the closure-teeth were broadened + tested this close after a sixth recurrence); a slice's closeout must stamp its own decision record CLOSED (now CI-enforced). **SCH-1 DONE + CLOSED 2026-07-23 (PR #108 `96965cf`, CI green; OQ-1=B infra-driven dispatch, app 100% non-BYPASSRLS). A post-close independent FOUNDATION AUDIT (Fable, 2026-07-23) judged SCH-1 as the base for LIM-1/MG-2: verdict SOUND — proceed straight to LIM-1, NO SCH-1b shim.** Findings: the ledger shape is right (LIM-1 must discover results via `calculation_run`/`calc/reads.py`, NOT `scheduled_run` — so MANUAL runs are limit-checked too; `scheduled_run` is provenance-only, correctly); MG-2's expected-tick set is reconstructible arithmetically from the pure `current_tick` + the frozen grid (no row-per-expected-tick needed); OQ-1=B generalizes via ONE per-tenant operational tick; the "family-agnostic" claim is true of the MECHANISM (tick math, idempotency, SAVEPOINT isolation, EV/IA split) but not the SCHEMA (NOT-NULL `scope_portfolio_id` unrepresentable for tenant-global families; VaR-named resolved-run columns; the inline dispatch body needs a registry) — all additive-fixable, DEFERRED to whichever slice first ships family 2. **Four audit demands LIM-1 planning MUST carry: (1) breach discovery via `calculation_run` reads, breach records FK the evaluated `calculation_run_id`, idempotency = LIM-1's own IA `(limit_id, calculation_run_id)` unique key; (2) breach evaluation is a PHASE of the same per-tenant tick, NOT a `Schedule` row (no `SCHEDULABLE_RUN_TYPES` expansion; no model_version to bind) — ratify `run_scheduler_for_tenant` → the single per-tenant operational tick as the OQ-1=B evolution rule BEFORE a second CLI entrypoint gets minted (infra wiring is still zero — the wall only materializes if concerns accrete separate entrypoints); (3) notifications assembled per-tenant INSIDE the tick, recorded as IA attempt evidence — never an in-app cross-tenant sweep (OQ-1=B forbids cross-tenant DB reads, not outbound egress); (4) ratify grid-frozen-for-life ("re-cadence = new schedule") as a STANDING invariant, not a v1 convenience — MG-2's expected-tick arithmetic silently depends on it.** **LIM-1 DONE + CLOSED 2026-07-23 (PR #111 `218afc9`, CI green all 6; migration `0050`; all four Fable audit demands discharged). MG-2 RATIFIED 2026-07-23 (OQ-1=B) — the breach remediation lifecycle THIS slice; the `LIMIT.APPROVE` maker-checker gate SPLIT to a new slice MG-3 (the platform's first person-level SoD + a limit-evaluation-path change deserves its own review). Wave 11 is now 4 slices (SCH-1 ✅ → LIM-1 ✅ → MG-2 ✅ → MG-3). **MG-2 DONE + CLOSED 2026-07-23 (PR #113 `aa6503f`, CI green all 6; migration `0051`): the breach remediation lifecycle — ENT-034 `breach_action`, the DEP-WFL 5-state machine, the third auto-escalation tick phase, the first person-level SoD; pre-ratification verifier folded 3 blocking concurrency holes + the 4-finder folded 1 HIGH (vacuous-SoD close path) + 4 MED. NEXT = MG-3 planning** (the `LIMIT.APPROVE` DRAFT→ACTIVE maker-checker gate LIM-1/MG-2 deferred — mints `limit.approve`, realizes the genesis-reserved `LIMIT.APPROVE` code, reuses MG-2's person-level SoD primitive; a DRAFT limit is not evaluated). Then the mandatory Wave-11 close review.

## Part 3 — Beyond Wave 1 (coarse; themes from the RTM map, NOT sequenced yet)

- **VaR completions (on demand):** √h multi-horizon; component/marginal VaR; Monte-Carlo (GATED: needs a
  seeded simulator + revaluation engine, QS-18). *(Rewritten at the Wave-6 close: ES SHIPPED at ES-1 and
  backtesting at BT-1/BT-2 — ES-over-HS is the named Wave-7 headline candidate, not a theme item.)*
- **Credit & counterparty risk** (RTM-P3 theme) · **Private assets + liquidity** (RTM-P4) · **Limits, breach
  workflow + SoD enforcement** (RTM-P6) · **Full model governance / validation workflow + DQ reconciliation**
  (RTM-P7) · **Reporting & dashboards** (RTM-P8) · **Real SSO/OIDC + admin + vendor adapters** (RTM-P9 — the dev
  header shim is replaced HERE at the latest, before anything internet-facing) · **BAU AI + hardening** (RTM-P10).
- **Frontend growth** *(REWRITTEN 2026-07-20 — the user set the core-product premise: "the front end UI will be
  a core part of this product")*: the FE-1-era "nearly free ride-alongs" posture is SUPERSEDED by the recorded
  **UI/read-surface assessment** (`ui_read_surface_assessment.md`) — the governed read surface is run-centric
  (no portfolio/as-of reads, no time-series, no summary; the compounding gap), the governance surfaces are the
  least readable part of the API (findings/evidence write-only; NO audit read; no snapshot list), FE types are
  hand-mirrored under an expired five-endpoint rationale (the FL-1 drift trio), and identity is the dev shim.
  **The named candidate Wave-9 sequence — API-1 (governed read surface) → FE-2 (generated types) → SSO-1
  (OIDC) → FE-3 (the product UI; the IA is a Tier-3 USER decision) — is weighed at the Wave-8 close per rule
  2.** Until that wave ratifies, the standing no-FE-expansion rule HOLDS; rule 7 (below) stops the read-shape
  gap growing in the meantime. Dashboards/reporting remain the RTM-P8 theme, realized through that wave.
- Standing deferrals with NO assigned wave (each needs its own trigger): covariance v2s (shrinkage/EWMA/
  correlation/annualization); vendor-beta loadings CAPTURE (the FL-1-recorded v2 — the REGRESSION leg SHIPPED
  at FL-1/MF-1; rewritten at the Wave-6 close); computed
  factor returns (needs adjusted prices); vol surface capture (ENT-022); PAR_RATE/interpolation/instrument-level
  sensitivity attribution; WORM/anchored audit hardening; gitleaks (OD-049). *(Branch protection
  OD-050 removed from this list at the Wave-6 close — PAID 2026-07-10, stamped at the Wave-1 close row.)*

## Part 4 — Re-sequencing rules (what makes this a plan, not a straitjacket)

1. **The default is the sequence.** At each slice close, the next Wave-1 slice proceeds on the user's go — no
   options menu is re-presented.
2. **Re-sequencing triggers (any of):** a wave closes (mandatory re-baseline); an adversarial review or build
   surfaces something structural; a dependency proves blocked or an assumption false; the user changes priorities.
3. **Every re-sequence is a recorded decision:** this document is amended with a one-line dated rationale and the
   change is briefed in plain language for ratification. Small hygiene insertions (a TC-1-sized slice) may be
   PROPOSED between slices but still ratify before starting.
4. **Ambiguity rule (user-stated, 2026-07-08):** where a slice hits a genuine fork, ask the user — always with an
   objective recommendation attached; the user decides.
5. **What never moves without its own gate:** the per-slice planning/review/commit discipline; the hard
   invariants (frozen audit service; no BYPASSRLS; R-07 for permissions/audit codes; the governed derived-number
   contract).

6. **Thesis alignment (ratified 2026-07-08 — see `01_product_strategy/differentiation_thesis.md`):**
   (a) every METHODOLOGY slice's decision record includes a **cited external-benchmark research section**
   (published/academic/regulatory sources + dates checked; deviations fixed or justified) — explicitly including
   transformation/proxy math when private-asset slices begin; (b) every wave-close re-baseline includes an
   **outward-facing benchmark review** (architecture / data design / methodologies / security / engineering
   practice vs. current published best practice) and **evaluates progress toward the public+private destination**
   — the Wave-1 close must explicitly weigh pulling private-asset foundations forward.

7. **The read-shape definition-of-done (user-approved 2026-07-20 — see `ui_read_surface_assessment.md`):**
   every slice that mints a governed number ships, IN THE SAME SLICE, the entity/time-centric read alongside
   the run-id read — filter by `portfolio_id` + as-of/date-range, plus a latest-resolver where the family has
   a meaningful "current" (the positions/valuations read shape, `api/positions.py:242-247` /
   `api/valuations.py:227-231`, is the house template); captured-input slices ship entity-filtered list reads
   from birth. Rationale: the run-centric-only read surface is the assessment's compounding finding (F1) — it
   blocks screens AND the thesis-§2.3 agent-consumable obligation, and every governed slice shipped without
   this widens the API-1 back-fill by one family. Effective from CC-1 (captured lists) and CC-2 (the 17th
   number) forward; the shipped families' back-fill is API-1's scope, not a per-slice retrofit.

## Part 5 — Amendment log

| Date | Change | Why |
|---|---|---|
| 2026-07-08 | Document created; Wave 1 = TC-1 → VAR-HS-1 → P3-C2 → P2-7 → P3-7 → P3-6. | User direction: replace per-slice option menus with a ratified rolling-wave sequence. |
| 2026-07-08 | Part 4 rule 6 added (thesis alignment: per-methodology external-benchmark research; wave-close outward benchmark review + public/private destination check). TC-1 marked DONE. | The ratified differentiation thesis (`01_product_strategy/differentiation_thesis.md`); user-directed best-in-breed review approach. |
| 2026-07-08 | VAR-HS-1 (`29ae31b`, #117) then P3-C2 (`6fb1a13`, CI green) marked DONE. Wave-1 slices 1–3 complete; next = P2-7 (benchmark price/level capture). | Sequential slice completion per the ratified sequence. |
| 2026-07-09 | P2-7 planning ratified + committed (`04c4135`). **TD-1 (test-data realism audit) INSERTED as slice 3.5** ahead of P2-7 implementation (OQ-TD-1-1…6 ratified). | Part 4 rule 3 hygiene insertion: a new user standing rule (fixtures economically plausible by default) warranted a retrospective audit+remediation slice; user directed a full hygiene slice now. |
| 2026-07-09 | **TD-1 DONE** (`ac92e0b` + follow-up `4534a38`, CI green). Next = P2-7 implementation. | Slice complete: base fixtures already plausible; 8 fixtures remediated; 4 independent finder passes; new fixture-realism reference doc + standing review-angle. |
| 2026-07-09 | **P2-7 DONE** (`ea2863d`, CI green): ENT-052 benchmark_level/return, migration 0029. Wave-1 slices 1–4 complete; next = P3-7 (benchmark-relative analytics), now unblocked. | Sequential slice completion per the ratified sequence. |
| 2026-07-09 | **P3-7 DONE** (`65e6dbe`, CI green): ex-ante active risk / tracking error — `active_risk_result` (ENT-027), migration 0030, `COMPONENT_KIND_BENCHMARK`. First user-directed FULL max-effort multi-agent review ("ultrareview"): 21 folds incl. the run_type=ACTIVE_RISK family/metric split + 3 missing CI PG steps; 3 deferred findings recorded (decision record Part 6). Wave-1 slices 1–5 complete; remaining = P3-6 (stress) then the Wave-1 close review. | Sequential slice completion; the ultrareview folds ratified by the user 2026-07-09 ("fix them all"). |
| 2026-07-09 | **P3-C3 DONE** (`1bf172b`, CI #132 green): a hardening CARRY-IN (Part 4 rule 3) paying the P3-7 ultrareview's item-A deferral — binder adjudication consistency (`TypeError` + `base_currency` 3-letter shape gate across var/var_hs/factor so all binders fail-close identically on malformed pins; factor_service also gained the malformed-pin wrapper it entirely lacked — an OD-A Part-3 discovery). Test-and-binder only; NO migration/permission/audit. Items B (shared covariance adjudicator) + C (lineage batching) formally re-deferred (record OD-E). | User chose to pay the debt while fresh (Option 3, item A); B/C deferred on the P3-4-R0 tipping-point rule + scale-driven timing. |
| 2026-07-09 | **WAVE 1 CLOSED — `wave_1_close_review.md` RATIFIED (OQ-W1C-1…6)**: honest audit (5 slices + 2 insertions shipped, all CI-green; ~90 review findings folded; npm audit 0 at all severities); deferral register reconciled (all P3-3/P3-5/P3-C1 deferrals PAID in-wave; open items now trigger-based incl. P3-7 B+C); the outward benchmark review + the thesis destination check. **Wave 2 ratified (Part 2.5): PM-1 → P3-8 → BT-1 → PA-0 → P3-6** — organized around the return-series triple unlock (ex-post TE/IR + backtesting + desmoothing substrate share ONE missing primitive). P3-6 moved (pre-authorized). Ride-alongs: npm CI gate high→moderate applied at this close; branch protection = a user GitHub-settings action; ES deferred. | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-09 ("Approved"). |
| 2026-07-09 | **PM-1 DONE** (`b2445c7` + ruff-format fix `f5e16b6`, CI #137 green): the SEVENTH governed number + FIRST non-risk (the `perf` family) — governed chain-linked TWR (`portfolio_return_result` ENT-053, migration 0031, `perf.run`/`perf.view` R-07 mint, CAP-20 + REQ-PRF-001); FULL 5-finder ultrareview (3 HIGH + 1 MED folds). The ruff-format red on `b2445c7` (#136) established the full-`make lint` pre-commit gate. Next = P3-8 planning. | Wave-2 slice 1 complete per the ratified sequence. |
| 2026-07-10 | **P3-8 DONE** (plan `4880b36` via **PR #1**; impl `86ef3ec` merged via **PR #2** = `d769f59`, CI #142 green — the FIRST slices through the mandatory branch-protection PR flow): the EIGHTH governed number — ex-post benchmark-relative AR/TD/TE/IR (`benchmark_relative_result` ENT-054, migration 0032, run family `BENCHMARK_RELATIVE` reusing `perf.run`/`perf.view`; the FIRST governed consumer of ENT-052 `benchmark_return`; closes P3-7 OD-G). FULL 4-finder local review (user-authorized in lieu of the cloud ultrareview): 5 folds incl. the HIGH evidence-echo magnitude gate + MED contiguity enforcement. Follow-up: the cleanup+closeout PR (3 dedup folds per the clean-code standing bar). Next = BT-1 planning. | Wave-2 slice 2 complete; the user set the clean-code standing bar 2026-07-10 ("as clean as possible" — proof-of-concept build), reactivating the deferred dedup folds. |
| 2026-07-11 | **BT-1 DONE** (plan `3e81ef4` via **PR #4** = `1da87c7`; impl `e7b615d` merged via **PR #5** = `868f892`, CI green): the NINTH governed number — VaR backtesting (`var_backtest_result` ENT-055, migration 0033, run family `VAR_BACKTEST` reusing `risk.run`/`risk.view`; Kupiec POF + Basel traffic-light zone over realized flow-adjusted P&L vs ONE VaR method's pinned forecasts; closes the Wave-1 close review's named nearest supervisory gap, SR 11-7 outcomes analysis). FULL 4-finder local review: 14 findings, 13 folded (HIGH: a hand-minted NaN VaR value detonated as a raw 500, now quantize-gated; the Basel zone gate was horizon-blind; the BASEL_ZONE emission branch had zero test coverage until folded; the snapshot builder accepted an empty VaR-run list). 2 findings deferred with recorded reasons (the P3-8/BT-1 return-shape adjudication dedup — tipping-point rule, 2nd of 3 consumers; a pre-existing concurrent-registration race shared by all six family registrars). Closeout PR follows (docs + memory). | Wave-2 slice 3 complete per the ratified sequence; the review folds — incl. catching my own unnecessary FK-name shortening and a stale-docs claim — followed the same discipline as P3-8. |
| 2026-07-11 | **PA-0 DONE** (plan `07e5d6a` via **PR #7** = `7a422aa`; impl `c9d41a7` merged via **PR #8** = `ad3d3fe`, CI green — merged planning-first after a rebase, keeping main's decision-record citations always resolvable): the FIRST private-asset foundation (differentiation-thesis destination §2.1) — ENT-019 `proxy_mapping` REALIZED (FR bitemporal captured private→public factor proxy weights, migration 0034, multi-factor blend, NO sum-to-1, `MANUAL_PROXY` ORIGIN lineage, `MARKET.PROXY_MAPPING_*` audit, `marketdata.view`/`.ingest` reused). CAPTURE-FIRST split ratified: the desmoothing TRANSFORM (Geltner AR(1) v1 recorded) is the PA-1 follow-on; capital calls/IRR redirected to PA-1 with a paper trail. Proportionate 2-finder review: no HIGH bugs, 4 folds (correction-audit action convention; the CURRENCY-family scope now ENFORCED; symmetric correction payload; dead order_by) + 2 precedent-consistent deferrals (FR supersede window-coherence — family-wide; IntegrityError→409 — family-wide). Next = P3-6 planning (the LAST Wave-2 slice) on explicit direction, then the Wave-2 close review. | Wave-2 slice 4 complete; the thesis destination now has its substrate entity. |
| 2026-07-11 | **MD-H1 (marketdata/registrar hardening) INSERTED as slice 4.5** ahead of P3-6, at the PA-0 closeout gate. Scope = the three bug-shaped deferral-register items: (a) the FR supersede **window-coherence guard** across all six FR series (a backdated `effective_at` below the superseded version's `valid_from` silently inverts its validity window — the only integrity-flavored open item); (b) the shared **IntegrityError→409** mapping for every marketdata capture endpoint (duplicate-open-head captures currently 500); (c) the **concurrent first-registration race** shared by all governed-family registrars (the loser 500s). NO migration; guards + shared mappings + tests. The dedup/perf register rows (P3-7 B/C covariance adjudicator + lineage batching; P3-8/BT-1 return-shape dedup) stay trigger-based — their tipping points haven't arrived. | Part 4 rule 3 hygiene insertion (the TD-1/P3-C3 "pay the debt while fresh" precedent); user ratified 2026-07-11 ("closeout, then MD-H1") after asking for an objective bug-register review before the last Wave-2 slice. |
| 2026-07-12 | **MD-H1 DONE** (PR #11 merged): the three bug-shaped register items paid — FR supersede window-coherence guard (all six FR series), shared IntegrityError→409 mapping (marketdata capture endpoints), concurrent first-registration race (all governed registrars). Option-A extension (11 supersede fns) + 6 annex items; 4-finder review, 13 folds. NO migration. | The ratified slice-4.5 hygiene insertion completed. |
| 2026-07-12 | **P3-6 DONE** (plan PR #12 `28c5225`; impl `fe3bb55` merged via **PR #13** = `0f66220`; closeout PR #14; CI green): the TENTH governed number — deterministic factor-shock scenario P&L (`scenario_definition`/`scenario_shock` ENT-029 + `scenario_result` ENT-030, migration 0035, run family `SCENARIO` reusing `risk.run`/`risk.view`). FULL 4-finder review: 7 folded (incl. HIGH per-factor quantize detonation → committed FAILED; HIGH always-drift scenario snapshot-verify) + 3 deferred to the RD-1 dedup slice. The LAST Wave-2 slice. | Wave-2 slice 5 complete per the ratified sequence. |
| 2026-07-12 | **Delivery autonomy GRANTED** (PR #15): the per-artifact "commit/push only on approval" rule replaced with full delivery autonomy — Claude self-drives plan→implement→commit→push; the USER opens+merges every PR to `main` (branch protection) + signs off Tier-3 decisions; other hard invariants unchanged. Encoded in `CLAUDE.md` + `claude_operating_instructions.md`. | User direction 2026-07-12: the commit ceremony was redundant with the PR-merge gate. |
| 2026-07-12 | **WAVE 2 CLOSED — `wave_2_close_review.md` RATIFIED (OQ-W2C-1…5)**: honest audit (5 slices + MD-H1 shipped CI-green, zero deviations; ten governed numbers; the return-series triple unlock delivered; the thesis destination began via PA-0); deferral register reconciled (MD-H1 paid 3 bug-shaped items in-wave; RD-1 `resolve_*_run` family newly TIPPED at 3 consumers; rest trigger-based); outward benchmark review (BT-1 closed the nearest supervisory gap; P7 validation workflow next) + destination check (both PA-1 prerequisites now in place). **Wave 3 ratified (Part 2.6): RD-1 → PA-1 → PA-2** — the private-asset transformation payload after one hygiene slice. | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-12 ("Approved"). |
| 2026-07-12 | **RD-1 DONE** (`0584cfe` merged via **PR #17** = `82f5f1d`, CI green): the run-resolver dedup — 10 near-verbatim copies (8 `resolve_*_run` reads + 2 COMPLETED-guards) collapsed into `calc/runs.py` (injectable error classes; byte-identical behavior); +3 contract tests; net −30 lines. The first slice delivered under the 2026-07-12 delivery-autonomy grant. | Wave-3 slice 1 complete per the ratified sequence. |
| 2026-07-12 | **PA-1 DONE** (plan RATIFIED + merged via **PR #18** `366a5c4`; impl `af155d2`→`34e8830` merged via **PR #19** = `f8bc20d`, CI green): the ELEVENTH governed number and the thesis §2.1 payload — Geltner AR(1) desmoothing (`desmoothed_return_result` ENT-056, migration 0036, declared-α identity, `perf.run`/`perf.view` reused, the honest-uncertainty stdev pair). Okunev-White RESOLVED at planning (the PA-0 honesty flag discharged; the tentative substance was wrong and corrected). FULL 4-finder review: golden math independently re-derived digit-exact ×2; 10 folds (PortfolioNotVisible 404; the FE runs-list permissionFamily altitude fix; the ArithmeticError→FAILED wrap; mark-echo hardening; parsed-date dedupe; α-echo stability; the missing FAILED-path + gate tests; `reference/guards` mint; docstring/record corrections) + 2 deferrals (RD-2 candidate: the 6-copy declared-parameter parse-back; instrument-guard next-touch). Next = PA-2 (proxy-risk end-to-end). | Wave-3 slice 2 complete per the ratified sequence. |
| 2026-07-13 | **PA-2 DONE** (`1ac7b7f`→`8b45b0f`→`7855ed1`→`b20c736` merged via **PR #22** = `96b2bd2`, CI green; **NO migration**, alembic head stays `0036`): the thesis §2.1 END-TO-END demonstration — private holdings projected through `proxy_mapping` onto CURRENCY factors, run through the governed exposure→factor-exposure→VaR chain, with the **byte-exact invariance proof** (`test_end_to_end_var_equals_public_equivalent` + an independent first-principles hand-derived VaR anchor). Mints NO new table: a NEW registered family `risk.factor_exposure.proxy` v1 (code-version identity) dispatched through the SAME `run_factor_exposure` binder (the VAR-HS-1 one-binder-dispatches-on-bound-model precedent), with proxy rows pinned via a load-bearing binding-predicate switch (symmetric refusal both directions). FULL 4-finder review: ~24 raw → 13 distinct, **ALL FOLDED, ZERO deferrals** (the magnitude/envelope gate on `weight×atom`; the symmetric predicate gate; the active-risk partition guard `_assert_partitioning_exposure_run` — the highest-value finding, preventing silent residual redistribution on a partial-proxy run; the zero-weight-is-a-no-op semantics correction; duplicate/no-atom pin gates; the hand-derived VaR anchor; the FX-realism fix; `reference/guards.assert_instrument_in_tenant` mint). New v2 register items: proxy-aware active-risk; regression-estimated proxy weights (consumes the PA-1 desmoothed series); residual/idiosyncratic variance. Next = Wave-3 close review. | Wave-3 slice 3 (final) complete per the ratified sequence. |
| 2026-07-13 | **WAVE 3 CLOSED — `wave_3_close_review.md` RATIFIED (OQ-W3C-1…5)**: honest audit (all three ratified slices shipped CI-green in sequence, via a 3-finder parallel close audit — PA-1 + PA-2 SHIPPED-AS-RATIFIED; RD-1 shipped with a note-level duplication-census undercount; the PA-2 fold-#8 half-application found and FIXED at the close; the Okunev-White flag discharged honestly); deferral register reconciled (RD-1 residual resolver copies + RD-2 declared-param parse-back both TIPPED → folded into RD-2; the PA-2 v2 register minted); outward benchmark review + destination check → **the thesis §2.1 destination is REACHED in v1** (capture→desmooth→proxy→factor risk→VaR, byte-exact invariance), with two honest-stub links (captured-not-estimated weights; CURRENCY-only factors). **Wave 4 ratified (Part 2.7, fork option A): RD-2 → PA-3 (regression-estimated proxy weights, the loop-closer) → a v2 companion** — deepen the private-asset payload toward best-in-class. | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-13 (fork A: "Deepen private assets"). |
| 2026-07-13 | **RD-2 DONE** (`11f890d` merged via **PR #26** = `cc9679b`, CI green; NO migration): the Wave-4 early hygiene slice — both TIPPED register items paid in one pass. NEW shared `model/assumptions.py` (`load_assumption_texts`/`sole_declared`/`require_declared`); the five `declared_*` parse-backs (Geltner α, VaR, HS-VaR, covariance window, Kupiec α) delegate byte-identically; the four RD-1 residual resolver copies folded onto `calc/runs.py` (the Wave-3-close census-undercount deviation DISCHARGED; consumed-guard messages normalized per the ratified OQ-RD-2-3). Net −63 lines across the two bootstraps; +4 shared-contract tests (+3 review-fold hardenings: mid-string-prefix rejection, uuid fixture, fullmatch trailing-garbage). Proportionate 4-finder review: 3 CLEAN, sweep's 3 test gaps folded, zero shipped-code defects. Next = PA-3 planning (the Wave-4 headline payload). | Wave-4 slice 1 complete per the ratified sequence. |
| 2026-07-13 | **PA-3 DONE — the loop-closer (`15ba13f`→`77149d6` merged via **PR #28** = `a98d380`, CI green 2026-07-14)**: the TWELFTH governed number, `proxy_weight_estimate_result` (ENT-057, migration `0037`) — unconstrained OLS (prec-50 Decimal, per-coefficient std errors + R²) of PA-1's DESMOOTHED series on candidate CURRENCY factor returns (compounded per appraisal period). Estimate→promote: an estimate is MODEL OUTPUT (snapshot/run/model-bound), promoted into `proxy_mapping` ONLY by the deliberate `risk.promote_proxy_weight_estimate` step, activating PA-0's reserved `MAPPING_METHOD_REGRESSION` (+ additive `proxy_mapping.source_calculation_run_id`; the run-TYPE gate lives in `risk`, respecting the marketdata↛calc fence). Declared `min_observations` identity on the RD-2 `model/assumptions.py` rails; `n≥max(N,k+2)`. Full stack: kernel + snapshot pins (COMPONENT_KIND_DESMOOTHED_RETURN) + service + API (register/run/GET + `/proxy-mappings/promote-estimate`) + methodology/ENT-057/RTM docs + the battery (integration golden vs independent OLS recompute; TR-09 both sides; singular; magnitude-FAILED; coverage-gap; constant-target; PG RLS/append-only leg). **FULL 4-finder review incl. a Fable-class numeric finder** (independent `Fraction` re-derivation across 60+ cases → math CONFIRMED): 3 finders CLEAN; ONE real governance defect FOLDED (supersede/correct could mint a NULL-cited REGRESSION weight → `_reject_regression_revision` fail-closes both) + 5 smaller folds (RISK_RUN_TYPES listing, 2 doc-claimed refusal tests, stale docstring, dead error-map entry, parse harmonization); 2 recorded deferrals. `make check` 1330 passed / 274 skipped (count harmonized to the record's contemporaneous figure at the Wave-4 close). Rule-6: Sharpe 1992, Scholes-Williams 1977/Dimson 1979, Asness-Krail-Liew 2001. | Wave-4 slice 2 (the headline payload) complete per the ratified sequence. |
| 2026-07-14 | **PA-4 DONE — total parametric VaR (`d3a6eae`→`232c3ea`→`c04768e` merged via **PR #30** = `8ef70db6`, CI green; migration `0038_var_residual_variance`)**: the THIRTEENTH governed number, `risk.var.parametric_total` v1 — total VaR = the plain factor variance `x'Σx` (byte-untouched) + the DIAGONAL idiosyncratic residual variance of REGRESSION-proxied instruments' cited PA-3 estimates (Sharpe 1963; `σ_e,daily = σ_e,period/√(appraisal_days·252/365)`, both ratio constants declared). SAME binder (the PA-2 one-binder-dispatches-on-bound-model precedent), SAME `var_result` under metric `VAR_PARAMETRIC_TOTAL` + ONE additive nullable `residual_variance Numeric(38,20)` (decomposition evidence: `σ_factor² ≈ σ_total² − residual_variance`). TWO in-build OD refinements recorded as Part-6 amendments: `appraisal_days` DECLARED (the pinned `ESTIMATION_SUMMARY` carries no span — supersedes pin-the-span OD-D) and MV_i = the instrument's total pinned factor-exposure sum (supersedes the exposure-atom pin, OD-C). New `COMPONENT_KIND_PROXY_WEIGHT` + load-bearing symmetric `VAR_TOTAL_BINDING_PREDICATE`; build-time citation adjudication (missing/ambiguous/wrong-instrument cited run) fails closed pre-write. The P3-5 SPECIFIC-RISK=0 limitation PARTIALLY discharged (REGRESSION-cited instruments only; the `1−Σw` partial-proxy leg stays honestly unmodeled). **FULL 4-finder Fable review** (numeric/adversarial/governance/test-completeness): 26 findings → **23 folded** (headliners: the `mapping_method=REGRESSION` pin gate — a probe-verified MANUAL-pin smuggle; the `appraisal_days≥1` parse-time identity floor; the missing CI PG step — the recurring P3-7 miss-class; magnitude→FAILED coverage; TR-09 side 2; consume≡build+re-run repro; the registered no-FX limitation row) + 3 accepted/deferred; **ZERO HIGH math defects** — every golden survived independent 120-digit re-derivation byte-exact, zero-proxy byte-invariance confirmed by 6000-case fuzz, 12/12 adversarial probes. Full battery green incl. local-PG; `alembic check` + downgrade smoke clean. **First slice delivered under the EXTENDED autonomy grant (2026-07-14): Claude opened + merged PR #30** (branch-protection required checks green). Next = the Wave-4 close review. | Wave-4 slice 3 (final) complete per the ratified sequence. |
| 2026-07-14 | **WAVE 4 CLOSED — `wave_4_close_review.md` RATIFIED (OQ-W4C-1…6)**: three-auditor parallel close audit, every claim code/CI-verified — RD-2 + PA-3 + PA-4 all SHIPPED-AS-RATIFIED (PA-3's OD-E amendment + PA-4's two OD refinements properly recorded; 8 findings all record-hygiene class, 5 fixed at the close incl. the missing PA-4 closure stamp and two stale "USER merges" lines); deferral register reconciled (9 PAID incl. both PA-2 v2 payloads; 3 TIPPED → RD-3; **estimate-staleness/refresh-cadence governance NEWLY MINTED** — the one gap no record had named); outward benchmark (nearest gaps: P7 validation workflow, total-series backtest, parametric ES — all ingredient-complete) + destination check (**the private-asset chain is structurally COMPLETE and vendor-aligned**; Wave-3 stub #1 captured-not-estimated weights DISCHARGED by PA-3; CURRENCY-only factors remains the biggest §2.2 gap). **Wave 5 ratified (Part 2.8, fork A "Self-governance"): RD-3 → VW-1 (SR 11-7 validation workflow) → BT-2 (total-series backtest) → ES-1 (parametric ES)**; multi-family capture teed for Wave 6. | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-14 (fork A). |
| 2026-07-14 | **RD-3 DONE — the Wave-5 hygiene slice (planning `39533c8` via **PR #33** = `df6afd6`; impl `d751fde` via **PR #34** = `29bc5a2`, CI green; NO migration)**: the three TIPPED register items + the seed-collision ride-along, all census-verified. (a) `verify_snapshot` malformed/gone pins now report DRIFT not a raw 500 — scoped via a NEW `MalformedPinError` + `_parsed_pin`/`_pinned_row_ids` helpers on the four branches that parse `captured_content`; **two finders independently caught the first pass over-widening the except-tuple to bare `KeyError/TypeError/ValueError/ArithmeticError` around all 19 branches**, which would have silently masked any future live-data serializer bug as "drift" — the fix keeps the other 15 branches raising loudly. (b) proxy_mapping instrument-guard fold + ratified message normalization. (c) `parse_strict_decimal` adopted at 7 provably-behavior-identical sites; **every skipped site recorded with its reason** in the record Part 5 (message-pinned by a test / registration-validated / no wrapper present / no error class in scope) — incl. a **genuine latent NaN→raw-500 bug found at `benchmark_relative_service.py:292`, NOT fixed** (fixing it would be a silent behavior change, forbidden by the hygiene scope fence) and deferred trigger-based. (d) The local dirty-schema double-run collision fixed at cause: a whole-seed short-circuit in `build_synthetic_dataset` (per-row idempotency was rejected — several seed calls are supersede/correct, whose "run twice" semantics are a real design question, not a hygiene fix) + `resolve_or_insert` on the two SYSTEM-tenant `GLOBAL_OK` fixtures; the deterministic uuid5 ids (a documented product feature) untouched. 4-finder review: **3 HIGH** (the except-tuple over-widening; OD-B's message-normalization claim untested; OD-D's DQ/lineage idempotency had no CI-exercised double-call test) **+ 3 MED**, all folded. Battery: `make check` 1372; full local-PG schema-reset run + **dirty-schema double-run green — the OD-D regression proof, now a standing capability**. | Wave-5 slice 1 complete per the ratified sequence. (Row + log stamped at the VW-1 closeout — the Wave-4 close's "closure stamp" lesson recurring: RD-3 merged without its roadmap stamp.) |
| 2026-07-15 | **VW-1 DONE — the SR 11-7 model-validation workflow (planning `9f0e024` via **PR #35** = `58900b1`; impl `726373f` + CI fix `93ab215` via **PR #36** = `a9a00eb`, CI green; migration `0039_model_validation`)**: the Wave-5 headline and the "nearest supervisory gap" named at three consecutive closes. Realizes reserved **ENT-037 `model_validation`** + IA `model_validation_finding`/`_evidence` at `model_version` grain — a CAPTURED governance judgment (binds no snapshot/run/methodology: the capture-vs-governed fence), outcome {APPROVED, APPROVED_WITH_CONDITIONS, REJECTED} × type {INITIAL, PERIODIC, TRIGGERED}, symmetric blur guards (conditions ⇔ AWC; `next_review_due` required-for-approving/refused-for-REJECTED; evidence CALCULATION_RUN⇔run_id / DOCUMENT⇔reference), evidence runs re-resolved tenant-visible+COMPLETED pre-FK-stamp (the PA-3 precedent) so a **BT-1 backtest becomes first-class outcomes-analysis evidence** (FRTB MAR32 pattern). **OD-B, the gate with teeth: a latest-outcome REJECTED refuses NEW governed runs at the ONE shared `assert_model_version_of` seam** — all 12 model-bound binders inherit it, the model-less exposure binder is exempt, and a REJECTED VaR model's already-COMPLETED runs stay backtestable (the re-validation evidence loop, adversarially confirmed). **UNVALIDATED keeps running** — the documented **SR 26-2** use-before-validation exception posture (rule-6's headline find: SR 11-7 was SUPERSEDED 2026-04-17 by SR 26-2 / OCC 2026-13, which also rescinds the 2021 Comptroller's Handbook; the record cites both, plus PRA SS1/23, ECB EGIM 4.1, BCBS d457/MAR32, OSFI E-23, Derman 1996 — all verified; McKinsey + EU 529/2014 flagged UNVERIFIED). R-07 mint **`model.validate`** → `risk_manager_2l` (ROLE-MV) + admin ONLY, **SOD-03 role-level**: withheld from the sole register-holder `risk_analyst_1l` (parity test pins "no non-admin role holds both"); reserved **`MODEL.VALIDATE`** ACTIVATED (`.APPROVE/.RESTRICT/.RETIRE` stay reserved); **human-only v1** (BR-15/MG-07 fail-safe — with no tier stamped, every model is potentially Tier-1). Docs: ENT-037 REALIZED, REQ-MDG-003 → In-Progress, **CTRL-022 Planned → Operational**, taxonomy activation record, SoD mint record, independence-policy VW-1 note (MG-04 role-level not data-level; OD-033 cadence still OPEN). **4-finder review (1 adversarial on the gate): 1 HIGH + 5 folds, all applied.** The HIGH: `RejectedModelVersionError` was raised at the seam but absent from the risk/perf run-endpoint except-tuples AND `_ERROR_MAP`s → the slice's core enforcement returned a **raw 500, not the promised 422**, at the real client-facing path; the seam-only tests hid it. Fixed across all 12 run endpoints + **end-to-end 422 tests through a real risk AND a real perf endpoint** (the "one risk + one perf" integration proof the plan intended). Others: the recency tiebreaker is deterministic ≠ write-order-recency (docstrings corrected + an anti-correlated-id regression test), symmetric evidence blur, UTC `overdue`, zero-DQ assert, cross-model 404. A **post-push CI-only failure** (caught by CI-watch, fixed in `93ab215`): the gate's read made every binder bind touch `model_validation`, but each family PG suite's constrained `irp_app` fixture granted only its own pre-VW-1 tables → `InsufficientPrivilege`; the local full-suite run masked it because the model-validation suite's fixture granted the table first (alphabetical) — a NEW form of the recurring CI-vs-local isolation lesson (**grant leakage across suites**); fixed in all 12 fixtures and re-verified CI-style (each suite alone, fresh schema, role dropped). Battery: `make check` **1407**; full local-PG schema-reset + dirty-schema double-run + `alembic check` + downgrade/upgrade smoke. Staleness fork **A**: VW-1 pays the generic ongoing-monitoring leg (`next_review_due` + `overdue`); the σ_e estimate-age GATE rides **BT-2**. | Wave-5 slice 2 (the headline) complete per the ratified sequence. Next = BT-2. |
| 2026-07-15 | **BT-2 DONE — the total-series backtest + the σ_e estimate-age gate (planning `da258ff` merged via **PR #38** = `26d89b4`; impl `3cb9844` via **PR #39** = `4035f10`, CI green; migration `0040_var_estimate_age`)**: ends the flagship number's exemption from the platform's OWN outcomes analysis (**PA-4 deferral 1 DISCHARGED**) and pays the estimate-staleness register item's mechanical half (the VW-1 fork-A ride-along) — **CLOSING that item** (VW-1 governance half + BT-2 mechanical half). The census found the mechanical half is ONE line (`METRIC_TYPES`): ENT-055's `var_metric_type` was already an unconstrained `String(30)`, the kernel never sees the method, the API has no method parameter, every pairing gate applies to TOTAL rows unchanged — **zero schema change for the admit**. **The honest half (OD-B, the ratified doctrine — "not the constant swap"):** every VaR is a HARD-ENFORCED 1-day forecast while an appraisal-marked book's private leg moves only on mark dates, so the daily read is biased TWO WAYS BY CONSTRUCTION — exceptions SUPPRESSED between marks (flat private P&L cannot breach) and CLUSTERED on mark dates (a quarter's move vs a 1-day allowance). Shipped as registered limitations + a dated `var_backtesting_v1.md` scope amendment carrying the **READ RULE**: on such books the unconditional Kupiec/Basel verdict is **NOT valid evidence of adequacy in EITHER direction** (validity degrades with the private-leg share); the dated per-pair `EXCEPTION_INDICATOR` rows are the evidence surface. SR 11-7's compensating-controls posture is the frame and **VW-1 is the control surface**. Appraisal-frequency pairing + a Christoffersen leg = named **BT-3 candidates** (the stale "Christoffersen = BT-2" label — which predated Wave 5 — fixed at 5 sites). **The gate:** DECLARED `max_estimate_age_days` on a NEW `risk.var.parametric_total` **v2** (+ the `var_parametric_total_v2.md` referent; the immutable v1 survives UNGATED — the recorded grandfather, whose sunset lever is VW-1's REJECTED verdict); age = pinned covariance `window_end` − the cited estimate's **regression SPAN END** (*how old is the DATA*, not when it was computed), live-read via the PINNED snapshot id (AD-014 holds — the header is true-append-only); strict `>` ⇒ pre-create 422; migration `0040` echoes `estimate_age_days`, EXCLUDED from the pin serializer per the 0038 false-drift landmine (now test-pinned). **4-finder review (adversarial + numeric + doctrine + scope-fence): 5 MEDIUM + 3 LOW, ALL folded; zero HIGH.** The adversarial finder could not break the refusal direction or AD-014; the numeric finder independently re-derived every age + the `BT1_VAR99` golden + the two-instrument residual decomposition; the scope fence cleared all five clauses and the VW-1 grant-leakage lesson was a NON-issue (`run_var` already read `dataset_snapshot`). **Two convergences (two finders each):** (1) the gate never checked the cited header's **purpose** — a hand-minted pin citing a fresh `VAR_INPUT` header defeated a tight policy (probe-verified); (2) the ungated partial-resolve echo was a **SUBSET max** understating staleness on the exact surface VW-1's sunset lever reads → now `None` (UNKNOWN outranks a known max). **The most important fold was in the honesty text itself:** the doctrine cited **MAR32.27** as banning "smoothing non-daily marks into daily P&L" — but its subject is *valuation adjustments* (CVA/reserves), NOT marks, and its direction is OPPOSITE (it REQUIRES a non-daily item to enter ACTUAL P&L unsmoothed, which Basel then backtests daily; our carried marks are already MAR32.27-shaped). A validator checking the citation behind the limitation would have found it forbids a practice we do not perform — discrediting the very text that IS this slice. Removed as footing from all three sites and **the rejection RECORDED**; the doctrine now rests on BCBS 22's stated per-day independence assumption (directly on point) + FRTB's RFET/NMRF carve-out. Also folded: SR 11-7's SR 26-2 supersession caveat reached the shipped referent; the stale ENT-027 catalog row (v1 / no `0040` / missing the 5th declared parameter) corrected; the parse pattern tightened to the ratified spec; the TOTAL golden anchored to the hand-derived plain-family value (it had asserted its own row); and a **fixture-realism fold the purpose check exposed** — PA-4's estimate fixtures minted `purpose=TEST` snapshots dated AFTER the VaR as-of, modelling an estimate that cannot exist. Battery: `make check` **1421**; full local-PG fresh + **dirty-schema double-run** + `alembic check` + downgrade/upgrade smoke. | Wave-5 slice 3 complete per the ratified sequence. Next = **ES-1** (parametric ES), then the Wave-5 close review. |
| 2026-07-15 | **ES-1 DONE — the parametric Expected Shortfall leg (planning `dcd9634`+`eec25a1` via **PR #40** = `bbc1d1b`; impl `a0723ac` + review folds `17d48bc` via **PR #41** = `2abc1b1`, CI green; **NO migration** — alembic head stays `0040`, 40 revisions)**: the **14th governed number** and the LAST Wave-5 slice — the first that answers what VaR structurally refuses (VaR is a cut-off: a book losing 1,200 in the tail and one losing 120,000,000 report the identical VaR). `ES_c = k_c·σ`, `k_c := φ(Φ⁻¹(c))/(1−c)`, over BOTH σ_p and PA-4's σ_total, realizing the `ES_PARAMETRIC` value RESERVED since P3-5. **The census headline — NO MIGRATION:** the grain's UNIQUE `(calculation_run_id, metric_type)` already permitted the value, `var_value` is NOT NULL and generic, and `VAR_HISTORICAL` already reuses it. **The convention pinned (OD-A):** the P3-5 seam `ES = σ·φ(z)/(1−α)` is correct but **never defined `α`**, and the literature is genuinely split on that exact symbol (Acerbi-Tasche: TAIL probability; Gneiting: CONFIDENCE) — ES-1 pins `c` = CONFIDENCE and the code relies on the definition, not the symbol; ES is recorded as the **α-TAIL-MEAN integral, never `E[L|L>VaR]`** (that is TCE, not coherent for discontinuous distributions) so a later ES-over-HS leg inherits the right estimator. **Shape (OD-C):** two NEW model codes through the SAME binder (`risk.var.parametric_es` + `risk.var.parametric_es_total` v1, the latter carrying BT-2's staleness gate **REQUIRED from birth** — no grandfather can exist on a code born with the declaration, made true BY CONSTRUCTION rather than by an inherited backstop). The tempting alternative — an extra ES row on the EXISTING VaR run — was **census-DISQUALIFIED, not dispreferred**: the snapshot builder pins EVERY `var_result` row of a run with no metric_type filter and the backtest binder then refuses mixed methods (and, independently, duplicate as-ofs), so every BT-1/BT-2 backtest over a post-ES parametric run would break and a shipped v1 model would silently emit two rows. **`k_c` is a REGISTERED 12dp constant** (the `z_c` twin), declared as `es_multiplier=` and **identity-checked at bind** against the table for the version's OWN confidence — a generically-minted version cannot pair c=0.99 with the 0.95 multiplier. **NO runtime normal function of ANY kind**: not the inverse CDF (barred since P3-5) and not the forward PDF φ — the tail arithmetic lives under model governance, not in code. **NO ES backtest (OD-F): a ratified omission on FRTB precedent + parametric redundancy, explicitly NOT non-elicitability** — "ES can't be backtested" is FALSE (Acerbi-Szekely 2014; Fissler-Ziegel 2016), and the refusal message now says "deliberately not backtestable" rather than "unknown". **The honest work was the research, and it cost us the obvious pitch:** "we add ES because VaR is incoherent" is **WRONG for this platform's own delta-normal VaR** — normal ⊂ elliptical, and Embrechts-McNeil-Straumann state VaR IS coherent there, so under our own modelling assumption our VaR is already subadditive; and **BCBS never argues coherence at all** (zero occurrences across d457/bcbs265/d219 — their stated rationale is tail capture only). ES here buys tail severity + **robustness-if-normality-fails** (ES's coherence is unconditional; ours is contingent on normality being true). **A 3-verifier adversarial pass ran BEFORE ratification, and killed 2 HIGH claims of my own** — both confirmed by execution: (1) "the VaR families' behaviour is unchanged" was FALSE (ONE shared z table gates four families, and 0.975 was the exact off-vocabulary probe of two shipped tests — both moved to 0.98); (2) the advertised "byte-exact, portfolio-independent golden" **cannot pass at row level** under independent 6dp quantization (0 hits in a 200,000-σ scan; the repo's own σ=500 fixture gives 1.004923991862, not ...931) — it belongs at the CONSTANTS level, where it is genuinely exact, and the rows get a derived quantum bound. Both headline claims (no-migration; the OD-C disqualification) survived attack, and **all six constants were confirmed to the last digit** by three independent routes (Decimal bisection with π via Machin; stdlib AS241; Simpson tail integration with no closed form), correctly ROUNDED not truncated (load-bearing at c=0.99). 8 MED + 6 LOW also folded, incl. **a live PG-only CHECK the census had missed** (`ck_var_result_parametric_not_null`, 0028, ORM-INVISIBLE) — it does not break the no-migration finding (ES rows satisfy it) but it makes the `z_score` echo **DB-FORCED, not a choice**, and it means a future `ES_HISTORICAL` row WOULD violate it (recorded: that leg needs a migration this one did not). **Recorded cost, stated not hidden:** an ES row does NOT reconcile against its own columns (`var_value = k·σ` but `k` is on no column) — it is reproducible THROUGH its model_version, never from the row alone. **Ride-along: BT-2's fired PG trigger PAID (both halves — the `estimate_age_days` PG round-trip AND the API assertion).** Battery: `make check` + full local-PG fresh + dirty-schema double-run + `alembic check` showing NO new revision + downgrade smoke. | Wave-5 slice 4 (final) complete per the ratified sequence. Next = the **Wave-5 close review**. |
| 2026-07-15 | **WAVE 5 CLOSED — `wave_5_close_review.md` RATIFIED (OQ-W5C-1…6)**, merged as DRAFT via **PR #42** = `cf6e3b6`, ratified immediately after: 4-auditor parallel close audit, every claim verified against code/tests/git/CI — all four slices SHIPPED-AS-RATIFIED, **zero shipped-code defects**; 6 findings all record/comment hygiene, all fixed at the close (headline: BT-2 unstamped at four sites — the THIRD consecutive close finding a missing closure stamp ⇒ the OQ-W5C-5 **closure-stamp checklist** is now standing process; an orphan planning hash; BT-2's falsified serializer clause struck). **Two material corrections:** (1) "the estimate-staleness item is CLOSED" was an OVERCLAIM (1 of 3 clauses paid — the promotion path has NO age check, every non-total family binds the same weight ungated, nothing expires a `proxy_mapping`) → RE-OPENED as "estimate staleness — residual", TIPPED; (2) **the teed Wave-6 prerequisite was factually WRONG** (capture is already family-generic — proven by execution; the real blocker is the loadings substrate + a fractional exposure kernel). Honest restatements: VW-1 is a validation record+gate, not a program (**16/16 UNVALIDATED** — "the numbers govern themselves" overstated the delivered state); its SR 26-2 exception citation is scope-stretched; the 14-number chain meets at a hand-minted seam; the FE (untouched since Wave 3) renders an ES row whose `z×σ ≠ var_value`. **WAVE 6 RATIFIED (Part 2.9, OQ-W5C-6 = fork A "governance-first"): MG-1 (materiality + the first validation campaign) → FL-1 (loadings substrate + fractional kernel v2) → MF-1 (multi-family end-to-end)** — the DRAFT recommended payload-first; a user-requested Max-effort reassessment REVERSED it (the "indictment" argument inverts: the weakness is already recorded at ~6 append-only sites, so validate-first → condition → remediate → TRIGGERED re-validation demonstrates the flywheel rather than hiding from it), and the user ratified (A) with the real-data scope note (enterprise target; real-data demos deferred past the build). | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-15. |
| 2026-07-16 | **MG-1 DONE — model materiality + the FIRST validation campaign (planning `7435e0a` via **PR #43** = `91c39fa`; impl `56cda62`→`e490c73` via **PR #44** = `46ce296`, CI green; **NO migration** — head stays `0040`)**: the governance-first Wave-6 opener. **Tiering is REAL**: dual ratings (materiality per SR 26-2 = exposure+purpose; complexity per SS1/23 P1.3(c)) derive Tier 1/2/3 via the ratified HOUSE matrix, assigned by a new audited 2L verb (`model.validate` reused — the 1L register-time tier write CLOSED at both API and service layers); the ratings live in the minted **MODEL.TIER_ASSIGN** payload (`audit/service.py` untouched). **OD-032 + OD-033 CLOSED**: tier-bounded cadence ceilings {365/730/1095} on `next_review_due`, enforced at record time — TIER_1 anchored on EGIM MR §4.2 ¶90 (voluntary adoption), 730/1095 explicitly HOUSE POLICY (no citable multi-year source exists — the census's decisive negative fact); untiered ⇒ the TIER_1 fail-safe. **The F3 fix shipped**: the use-before-validation EXCEPTION as a `model_validation` type (AWC + expiry shape; the substitution guard; the PMA limb disclosed as not-adopted), **with teeth** — an EXPIRED exception refuses new binds at the shared seam (mapped 422 at all 12 run endpoints; overdue revalidation stays display-only, the recorded asymmetry). **The empty table is EMPTY NO MORE**: a living demo tenant (a governed runner driving the REAL service layer end-to-end — the full private-asset chain incl. real marks→desmooth→OLS→promote, real BT-1 AND BT-2 backtests at N=8, ES/ES-total runs) holds **16 registered models / 16 tiered / 6 INITIAL validations + 10 time-boxed EXCEPTIONs / 49 COMPLETED evidence runs**, with all 27 findings drawn from registered limitations (nothing invented — content-audited against the stored rows) and the 5 flagship AWC conditions carrying the CURRENCY-only condition **whose remediation IS FL-1/MF-1** (the MF-1 TRIGGERED re-validation greps for exactly those 5). **Ride-alongs PAID**: the RD-3 NaN bug (mutation-tested; only NaN orphaned — the census precision) + the estimate-seam integration test (the Wave-5 close's hand-minted-seam finding retired). **4-finder review in two rounds** (the first hit the Fable usage limit mid-run; re-run on Opus): **ZERO HIGH, ZERO MEDIUM across all four finders** — the adversarial finder's executed probes could not make anything bind that shouldn't, 500 that should 422, or leak across tenants; 2 MEDIUM + 8 LOW folded (headliners: the campaign PG test's teardown could strip the LIVING tenant's role wiring in dirty mode — gated on a seeded-by-this-run flag; the provably-unreachable second exception guard REMOVED as dead code; the swept limitation text updated to name the expired-exception branch; the constant-forecast-series fixture disclosed in the flagship dossiers; the re-tier-does-not-shorten-existing-grants coupling recorded as Part 3 item 9 → the MG-2 reassessment candidate). Battery: `make check` **1471**; full local-PG **1772** fresh AND dirty double-run; no drift; downgrade smoke clean. | Wave-6 slice 1 complete per the ratified sequence. Next = **FL-1** (the loadings substrate + fractional exposure kernel v2 — the corrected multi-family prerequisite). |
| 2026-07-16 | **FL-1 DONE — the loadings substrate + the fractional exposure family (planning via **PR #46** = `ec01053`, CI #303; impl `fl-1-impl` tip `ca07529` via **PR #47** = `778891c`, **CI #314 green**; **NO migration** — head stays `0040`)**: the corrected multi-family prerequisite, shipped as ratified (OD-FL-1-A…F). **The THIRD factor-exposure family** `risk.factor_exposure.loadings` — the proxy projection GENERALIZED (fractional signed multi-factor loadings over the widened admitted families) — through the SAME `run_factor_exposure` binder, the two-arm try/except dispatch REPLACED by the `_EXPOSURE_FAMILIES` registry map (the ES-1 `_resolve_var_family` precedent). New predicate `v1:exposure-run-atoms+factor-list+loading-rows` with the **3×3 symmetry** (each family requires exactly its predicate, refuses the other two — probe-pinned all nine arms). **The coverage gate**: an UNLOADED pinned atom refuses the run CLOSED (no indicator fallback, no silent VaR under-count); a captured ZERO loading IS coverage. **The vocab**: `RATES`/`CREDIT_SPREAD`/`COMMODITY` minted, CURRENCY≡FX + MARKET≡equity ALIASED (BCBS d457 MAR33.14, names-as-vocabulary — zero capital semantics); `LOADING_FACTOR_FAMILIES` allow-list; 3 of the 5 CURRENCY gates relaxed to explicit allow-lists (scenario's TWO stay closed, the two-gate inventory recorded for MF-1); ENT-019 WIDENED (`private_instrument_id` = the recorded pin-serializer-key misnomer; ENT-058 stays the paper v2). **The loadings source**: PA-3's OLS pointed at public instruments via the **α=1 desmoothing identity detour** (`r_true = r_observed`; the referent discloses the semantic stretch); price-return betas + short-window noise recorded, std errors/R² first-class. **The through-VaR invariance golden**: a loadings run over CURRENCY weights yields a VaR BYTE-IDENTICAL to the proxy run (450.495329). Allocation family byte-untouched (invariance regression); active-risk + scenario refuse loadings runs fail-closed. **The FE drift trio PAID**: the `proxy-weight-estimates` family end-to-end; residual_variance/estimate_age_days/model_version_id vars columns; the ES row's `z_score` cell now a per-row metric_type-aware annotated echo (the `z×σ ≠ var_value` invitation retired) + the missing ES-row FE test. **4-finder review (ALL Fable — the MG-1 budget rule lifted): zero shipped math defects** — the numeric finder re-derived every golden byte-exact (through-VaR by hand + numpy; the α=1 betas to 12dp via lstsq); 4 HIGH folded pre-merge (the PA-3 referent's missing FL-1 amendment ×2 — the doctrine finder; the loadings registrar 409 + endpoint tests; the silent-discard CONTENT fence) + the adversarial duplicate-atom gate + all MED/LOW; 5 build deviations in Part 5.5, dispositions in Part 6. **Demo tenant stayed CURRENCY-only by design** (the MF-1 premise holds). Battery: `make check` **1488**; full local-PG **1784** fresh (the schema-reset PUBLIC-grant gotcha bit again — memory sharpened); no drift; downgrade smoke clean; FE 64 vitest + lint + tsc + prettier + build. | Wave-6 slice 2 complete per the ratified sequence. Next = **MF-1** (multi-family end-to-end, ending with the TRIGGERED re-validation that closes MG-1's CURRENCY-only condition). |
| 2026-07-16 | **MF-1 DONE — multi-family factor exposure end-to-end + the TRIGGERED re-validation (planning via **PR #49** = `0951784`; impl `mf-1-impl` tip `2898eb8` via **PR #50** = `96b766c`, **CI #323 green**; **NO migration** — head stays `0040`)**: the LAST Wave-6 slice, shipped as ratified (OD-MF-1-A…F) — **the full governance lifecycle demonstrated** (limitation recorded → remediation built → TRIGGERED re-validation with real evidence). **The living demo tenant went multi-family**: an ADDITIVE extension runner (`run_demo_multifamily_extension`, refuse-not-skip both directions, single-commit; the base campaign byte-untouched) seeded the multi-asset sleeve (2 equities + 1 corporate bond, single-currency; 3 FRTB-family factors MARKET/RATES/CREDIT_SPREAD with calendar-daily returns), ran the REAL estimation chain per instrument (12 daily marks → the α=1 desmoothing detour on a NEW `v1-alpha1` version → the **k=3 Sharpe-1992 OLS on the full asset-class palette** → the analyst promoted the STRUCTURAL coefficients, one estimate run per instrument — the exact shape total-VaR's single-cited-run gate requires), then the evidence chain: loadings-family factor exposure (the FL-1 predicate + coverage gate) → covariance → **one VaR/HS/total/ES/ES-total run each, bound to the SAME demo-mg1 versions the flagship AWCs sit on** (the version grain). **The closure**: 5 TRIGGERED + AWC re-validations with FRESHLY-DRAFTED per-model conditions (the re-scoped specific-risk clause + surviving riders standalone; ZERO 'FL-1' tokens — the conditions-grep now finds the token in exactly the 5 HISTORICAL rows, both directions test-pinned at the version grain) + the loadings model's INITIAL (AWC, TIER_2) + a time-boxed EXCEPTION for the α=1 version; 17 codes registered, 14 new COMPLETED runs. The mixed-family fence held (legacy instruments carry zero non-CURRENCY rows — probe-pinned; the proxy family stays runnable). **4-finder review (all Fable), the two headline folds:** the doctrine HIGH (the reused independence disclosure misattributed the ratification → the MF1 variant re-points at OQ-MF-1-3) and the numeric REFUTATION of the "near-zero unloaded betas" claim (eps-projection bias `(X'X)⁻¹X'ε` at ±0.24 → the idiosyncratic cycles shrunk 10×, recovery now within ~0.03); adversarial: ZERO HIGH (131 filed-text surfaces token-grepped; the single-commit + version-grain + concurrency claims all executed-verified); scope fence: all 14 clauses PASS. 6 deviations in Part 5.5 (headliners: the desmoothing registrar's optional `version_label`; the PG prereq probe → unit-tier SQLite coverage; the second missing `_BINDING_PREDICATES` constant — PA-3 debris). **Two standing capabilities retired, disclosed**: the campaign suite's tolerate-living-tenant mode + the dirty-schema double-run (fresh-schema-only from MF-1 on; the extension's CI step is ordering-pinned after the campaign step). Battery: `make check` **1490**; full local-PG fresh **1794/0/0**; alembic no-drift; downgrade smoke clean over the extended tenant. | Wave-6 slice 3 (final) complete per the ratified sequence — **Wave 6 is functionally COMPLETE**. Next = the **Wave-6 close review** (the mandatory Part-4 rule-2 re-baseline; the OD-E ride-along re-tee — residual-shrinkage/EWMA + desmoothing-v2s, materiality cap LIFTED — on its agenda; ES-HS-1 the named Wave-7 headline candidate). |
| 2026-07-17 | **WAVE 6 CLOSED — `wave_6_close_review.md` RATIFIED (OQ-W6C-1…6)**, merged as DRAFT via **PR #52** = `9d561bf`, ratified immediately after: the FIRST full-ultracode close audit (78 agents: 6 domains, 2-refuter verification per finding, 2 judges with 16/16 claim-checks surviving, completeness critic) — **all three slices SHIPPED-AS-RATIFIED, ZERO shipped-code defects for the second consecutive close**; every surviving finding record/doc/process hygiene, fixed at the close (headliners: the living demo tenant was UNSEEDED in any durable DB — re-seeded via both CLIs, 17/23/63, the extension CLI's first production-shape run; MF-1's "pending ratification" header — the FOURTH consecutive close finding a stamp-class miss, the checklist gains grep-for-"pending"; MG-16's dead second guard; the RTM REQ-MDG-001 flip; roadmap Part-3 register hygiene ×3). **Deferral register reconciled (44 items): the TIPPED estimate-staleness residual went UNPAID through its assigned wave — RE-OPENED and ASSIGNED to Wave-7 HG-1** (the register discipline's teeth); Christoffersen re-teed to the ES-HS-1 planning fork; P3-7 deferral C (lineage batching) restored after being silently dropped at the Wave-5 close. Outward benchmark: the Wave-4 five-gap list is FULLY CONSUMED (a first); the frontier moved from missing structure → missing governance → **missing depth** (2003-vintage transformation math, the real ES number, remediation tracking, the first scheduler). Destination: Wave 6 paid breadth+governance on the axis ranked first at the prior close; the largest remaining gap = transformation/tail fidelity, and **commitment/capital-call handling is the last thesis-§2.1 leg with zero code**. **WAVE 7 RATIFIED (Part 2.10, OQ-W6C-6 fork A "deepen the mathematics"): HG-1 → ES-HS-1 → RS-1 → DS-2**, discharging the MF-1 OD-E contractual re-tee by sequencing; riders: SC-2 the named pull-forward, capital-calls the presumptive Wave-8 headline. Process: `phase_status.md`/`next_actions.md` stubbed to pointers (OQ-W6C-4 — one honest entry point beats three half-stale ones). | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-17 ("Approve all"). |
| 2026-07-17 | **HG-1 DONE — the Wave-7 hygiene opener (planning via **PR #54** = `4c8829f`; impl `hg-1-impl` tip `9e3200a` via **PR #55** = `8260ea6`, **CI green 5/5**; **NO migration** — head stays `0040`)**: the first Wave-7 slice, shipped as ratified (OD-HG-1-A…D). **Three domains.** (a) **The promotion-path age gate** — the re-opened estimate-staleness residual (OQ-W6C-2) paid: `promote_proxy_weight_estimate` now ALWAYS measures `promotion_age_days` (promote-day − the cited estimate's regression span end, resolved through the pinned snapshot header with a purpose check — the BT-2 shape) and audits it in the CREATE event's `after_value` only when the promote path supplies it; an OPT-IN `max_promotion_age_days` (service + additive HTTP promote-body field, `ge=1`) refuses a stale estimate pre-write, and an UNMEASURABLE age (NULL `input_snapshot_id` / wrong-purpose header / dangling snapshot id — three shapes) with a bound present fails CLOSED. The wire is a DISTINCT `ProxyWeightStaleEstimateError` with its own EXACT-TYPE entry in the marketdata write-error map (a subclass would 500; a reused class would give a false detail). Promote-day is the ratified anchor (`valid_from` rejected — caller-movable, not on the HTTP body); the audit key is deliberately `promotion_age_days`, NOT BT-2's differently-anchored `estimate_age_days`. Absent bound ⇒ ungated status quo, so no fixed-date fixture rots against the wall clock. **OD-B (expire-a-mapping) DEFERRED** with the recorded census (no time-based op exists; a status/expiry column is a migration + pin-serializer landmine). (b) **The FL-1 constants sweep** — the 11-row registrar-constant debris corrected (7 CHANGE + 2 parenthetical + 1 phrasing + 1 optional), all finding keys preserved VERBATIM (the framing clause reworded, never the key; the 'FL-1' token kept out of every new text; the executable per-searched-set conformance fence — which caught its own author's rewrite); the headline new find `PROXY_WEIGHT_LIMITATIONS[2]` (flat-false "a non-CURRENCY candidate fails the run closed" since FL-1); the `MF1_CLOSURE_FINDING` frozen-wording note amended; 6 methodology docs dated-amended. (c) **Stage 3 of the living demo tenant** — a NEW private-credit instrument `PC-BRIDGEWATER-II` runs the genuinely-private α=0.4 desmoothing chain on multi-family (RATES+CREDIT_SPREAD) factors (the one combination nothing had demonstrated: MF-1's sleeve used α=1 public instruments), 8 quarterly marks → 6 desmoothed periods → the 18-row OLS mint → promote → the loadings-family evidence chain, `code_version="demo-hg1"`, joined to the sleeve portfolio; NO validation record filed (OQ-5 — every bound version already carries a live outcome; an EXCEPTION would be false ceremony). **4-finder review (adversarial gate + numeric generator/recovery + doctrine on the reworded texts + scope-fence): ZERO HIGH.** The pre-ratification verifier pass changed the ratified shape three times (the wire mechanism; the demo date redesign from end- to start-extended, killing a hidden −11d look-ahead; a 7-vs-6 census count correction); the numeric finder decorrelated a corr≈0.99 factor pair (cond 1147→317) and carried NO eps term at df=2 (small-fixture-noise lesson). Battery: `make check` ~1490; full local-PG fresh **1810/0/0** (incl. the three demo suites in CI order — stage 3 seeded FIRST, after the multifamily suite, before the downgrade smoke); alembic no new revision; downgrade smoke clean. | Wave-7 slice 1 complete per the ratified sequence. Next = **ES-HS-1** (ES over historical simulation — the Wave-7 headline; needs the ONE `0041` migration widening the `0028` `ck_var_result_parametric_not_null` CHECK; fetch Acerbi-Szekely 2014 to paragraph at planning). |
| 2026-07-17 | **ES-HS-1 DONE — the Wave-7 headline: ES over historical simulation (planning via **PR #57** = `7568c49`; impl `es-hs-1-impl` tip `c7b8c15` via **PR #58** = `dc2a494`, **CI green 5/5**; migration head `0040`→`0041_es_historical`)**: the **15th governed number** and the platform's FIRST empirical tail measure — the Acerbi-Tasche **Prop-4.1 α-tail-mean** (floor count + FRACTIONAL boundary weight, ÷ n·a — NEVER the mean of the worst ⌈n·a⌉, which is the TCE the ES-1 convention forbids) over the SAME scenario P&L distribution the HS VaR selects from. `metric_type='ES_HISTORICAL'` on the shared `var_result` grain, `var_value` holds the ES with the exact VAR_HISTORICAL NULL trio; **fully reproducible from its pinned snapshot alone** (a reproducibility gain over the parametric ES, which reproduces only through its registered k_c). **The ONE migration 0041** widened the 0028 metric-conditional CHECK (drop+recreate, same short name; the destructive RLS-safe downgrade deleting ES_HISTORICAL rows, the 0028 6-statement sandwich verbatim — proven under a NON-superuser owner-via-membership role driving the real `downgrade()`, the FORCE-RLS zero-row trap demonstrated live, closing the recorded superuser-only-smoke gap). `risk.var.historical_es` v1 through the SAME HS binder via the `_HS_FAMILIES` registry-map dispatch (the ES-1 shape; the plain-HS path byte-preserved via a shared accumulation helper); declared identity = confidence/horizon/window + `estimator_convention=TAIL_MEAN_ACERBI_TASCHE_P41`; the VAR_HS_INPUT snapshot + builder reused byte-unchanged (an adjudicated purpose-only reuse — identical pinned content). ONE new registrar endpoint; the run/read endpoints dispatch on the bound model (docstrings updated, no functional change); FE zero changes. **The in-slice fork RESOLVED = TEE the Acerbi-Szekely backtest as BT-3** (named, prerequisite-paid: AS 2014 verified-via-reproduction at planning — the primary is GATED, so the Z2 formula was verified via the Moldenhauer-Pitera verbatim reproduction + the null-expectation identity; Z1 vendor-normalized + the thresholds single-attributed, both flagged for BT-3 re-verification); TIPPED Christoffersen finally homed in BT-3; the pairing design input recorded (pair the ES-HS and VaR-HS runs by shared `input_snapshot_id`); the backtest binder gains the DELIBERATE ES_HISTORICAL refusal naming the tee (never an unknown-vocabulary miss). **Demo stage 4** = the 18th registered code (TIER_1; the INITIAL AWC dossier; the flagship ES run bound to the SAME snapshot as the flagship HS VaR — the BT-3 pairing demonstrated live; the seeded window's tied worst-two scenarios land the pair on the recorded ES=VaR equality case, disclosed). **4-finder review (adversarial + numeric + doctrine + scope-fence): 0 HIGH, 3 MEDIUM, 5 LOW — ALL folded; ZERO shipped math defects** — every golden byte-exact under independent exact-rational re-derivation + a 2,400-window fuzz (zero deviations, zero ES<VaR violations); the living tenant's stage-4 ES recomputed byte-equal from its own pins. Headline folds: the generic-mint bind-refusal test the family lacked; the RAW-precision ES≥VaR assertion the plan promised; the strict TCE-understatement claim softened to the weak form (the demo tenant is a live tied-tail counterexample). Battery: `make check` **1524**; full local-PG fresh green (the CI-order downgrade smoke exercises 0041's destructive delete on every run — stage 4 seeds the rows); `alembic check` clean. Scope fence held: ONE migration, no permission/ENT/audit mints, `audit/service.py` + the pin serializer + `METRIC_TYPES` byte-untouched. | Wave-7 slice 2 (the headline) complete per the ratified sequence. Next = **RS-1** (residual shrinkage/EWMA — the PA-4 OD-E/OD-G v2s; fetch Ledoit-Wolf/RiskMetrics to paragraph at planning). |
| 2026-07-18 | **RS-1 DONE — residual shrinkage + EWMA (planning **PR #60** = `8014b51` — branch tip `fdfbfa9`, merge sha corrected at the Wave-7 close; impl `rs-1-impl` via **PR #61** = `9c15658`, **CI green run #365**; **NO migration** — head stays `0041_es_historical`)**: the PA-4 **OD-E/OD-G residual-estimator v2s REALIZED** as two declared estimator conventions on `risk.proxy_weight.regression` — **NO new governed number / model code / permission / ENT** (the estimate is an INPUT to the total-VaR family, not a governed output; the convention rides the version). **`EWMA_RISKMETRICS`** (Axioma/RiskMetrics decay-weighted specific variance; declared `decay_lambda`; **the s2 decoupling** — `s2=ss_res/(n−m)` retained for OLS `std_errors`, a separate `residual_var` dispatches on λ into `residual_stdev` ONLY, so loadings/std-errors/R² are byte-identical; RAW v1 grandfathered via absent-convention). **`SHRINKAGE_CROSS_SECTIONAL_EB`** (Barra USE4 empirical-Bayes cross-sectional shrinkage; the **DATA-DRIVEN** per-instrument intensity `w_i = v_i/(v_i+τ²)` — method-as-identity, NO declared numeric, every `w_i` recomputed from the pin; a per-TARGET transform run `run_residual_shrinkage` pinning a NEW `RESIDUAL_SHRINKAGE_INPUT` cohort snapshot + `v1:cohort-residual-variances+dof` predicate; **N≥3-DISTINCT-instrument fail-closed**, no shrink-of-shrunk; the shrunk row carries the target's identity so promotion + the total-VaR pin are byte-unchanged). **The age gates (total-VaR + HG-1 promote) WIDENED** to admit `RESIDUAL_SHRINKAGE_INPUT` citations — as-of = the STALEST cohort member's span end (conservative by builder construction; the recorded Part-5.5 deviation, else a shrunk estimate could never feed a GATED total run). **Primary sources VERIFIED, the ledger flag discharged**: RiskMetrics TD 4th ed §5 + Axioma (EWMA), Barra USE4 (shrinkage, USE4-faithful on the intensity) — **Ledoit-Wolf verified and EXPLICITLY NOT used** (it leaves variances unshrunk — the wrong primary for a residual-variance shrinkage; reserved for covariance shrinkage). Demo **stage 5** = the **SECOND lifecycle turn**: the sleeve grown to 3 comparable equities (MF-EQ-C), MF-EQ-B EWMA-re-estimated + MF-EQ-A EB-shrunk over the 3-equity cohort (the corporate bond EXCLUDED + asserted-raw — the comparable-cohort fence live), fresh gated flagship total-VaR/ES-total evidence, **2 TRIGGERED re-validations closing the raw-sample-σ_e rider** (the `hostage to the PA-3 estimate quality` finding flipped to historical — both directions test-pinned at the version grain; the MF-1 frequency-conversion caveat restated, carried-forward-unremediated) + 2 INITIAL AWC dossiers for the new versions; NO new code (the 16/17/18 count pins hold). **4-finder review (adversarial + numeric + doctrine + scope-fence): 2 HIGH + 5 MEDIUM + 7 LOW — ALL folded; ZERO numeric defects (exact-rational, executed, 400+ fuzz cases); ZERO scope-fence violations** (decision record Part 6). The two HIGHs: an AMBIGUOUS (>1) `estimator_convention` had collapsed into the RAW grandfather (now counts rows → fail-closed); the MF-1 frequency-conversion disclosure had silently dropped from the fresh TRIGGERED conditions (restored). The not-order-preserving EB property disclosed. Battery: `make check` **1554**; full local-PG fresh (all five demo PG suites green in CI order — stage 5 after stage 4, before the downgrade smoke); `alembic check` clean; downgrade-base + re-upgrade smoke clean over the stage-5-extended tenant. | Wave-7 slice 3 complete per the ratified sequence. Next = **DS-2** (desmoothing v2 — the LAST Wave-7 slice; the Geltner/GLM/Okunev-White primary-source obligation; fetch to paragraph at planning). |
| 2026-07-19 | **DS-2 DONE — estimated-α + Okunev-White desmoothing (planning **PR #63** = `0f199aa`; impl `ds-2-impl` via **PR #64** = `5120baa`, **CI green**; migration head `0041`→**`0042_desmoothing_estimated_alpha`**)**: the declared-α rider REMEDIATED — the platform starts ESTIMATING the smoothing it corrects. Two declared estimator conventions on `perf.return.desmoothed_geltner` (**NO new governed number/model code/permission/ENT** — the RS-1 two-versions shape): **`AR1_ESTIMATED`** (α̂ = 1−ρ̂₁ IN-RUN from the pinned marks, T-denominator Box-Jenkins; fail-closed ρ̂₁ ≤ 0/constant series — no silent clamp; the CONSERVATIVE Bartlett 1/√n band persisted as `alpha_stderr` — an identification convention, not an exact CI; **the planning verifier's R1 headline**: the draft's "Bartlett understates" direction was BACKWARDS (executed MC), and the genuinely-unstated hazard — ρ̂₁'s Kendall/Marriott-Pope small-n DOWNWARD bias ⇒ α̂ biased UPWARD, E[α̂] ≈ 0.58 at n=15/α_true=0.4 — is a REGISTERED limitation, never corrected in-run; declared `min_periods` ≥ 6) + **`OKUNEV_WHITE_ITERATIVE`** (declared `ow_max_order` 1..4, ONE deterministic lag-i pass per order ascending; the '−' root of ρ_i·c²−(1+ρ_2i)·c+ρ_i = 0 — the lag-i algebra SETTLED BY DERIVATION + EXECUTION against the vendor reproduction's transcription artifact; Vieta sole-admissible-root; fail-closed on negative discriminant / c_i ≥ 1 / the length-vs-order structural floor n ≥ m(m+1)/2+2; ρ_i < 0 = deliberate whitening; alpha NULL on OW rows). **GLM MA(k) stays the named v2** — GLM 2004 extraction-verified to equation numbers (the ledger's never-extraction-verified flag DISCHARGED); its MLE requires constrained numerical optimization, the recorded determinism/TR-09 obstacle. **The gate** adapts RS-1's verbatim (absent ⇒ DECLARED grandfather byte-preserved; ambiguity + stray literals refused from birth; `band_convention` registrar-stamped identity). **Migration `0042`**: alpha → nullable + `alpha_stderr` + the summary-only CHECK; the 0028 destructive RLS-safe downgrade sandwich, proven under a NON-superuser owner-via-membership role; the pin serializer changed by exactly the ONE adjudicated None-tolerance (`alpha_stderr` EXCLUDED — the 0038/0040 landmine). Demo **stage 6**: `PE-HARBORVIEW-IX` (16 quarterly marks at KNOWN α_true = 0.4; searched fixture ρ̂₁ = 0.5038 ⇒ α̂ = 0.4962, drift-tripwired), the three-way declared/estimated/OW comparison in one tenant, 2 INITIAL AWC dossiers claiming **estimation-with-honest-uncertainty, deliberately NOT recovery** (the R1 reframe — the platform showing its OWN estimator's finite-sample weakness); **NO TRIGGERED re-validation, recorded honestly** (census-proved: no closable condition names the rider — the versions carry EXCEPTIONs; the HG-1 OQ-5 false-ceremony bar; deliberate contrast with the MF-1/RS-1 flywheel). **4-finder review: ZERO HIGH, ZERO MEDIUM** — adversarial 2 LOW + 4 INFO, numeric 0 defects + 3 property caveats (exact-rational at max rigor; the ρ̂₁=1 guard PROVED dead; every frozen demo number reproduced; the E[α̂] MC confirmed at 0.5763), doctrine 0/0 + 2 LOW, scope-fence 9/9 — all folds `b8141d6`; **+ the missing-CI-step catch at the pre-push battery** (the new 0042 PG suite — the summary-only CHECK + the non-superuser downgrade body — had NO ci.yml step; the P3-7 missing-PG-step class; added at the 0041-precedent placement, recorded Part 5.5). Battery: `make check` **1588**; fresh-schema local PG (the 0042 estimation suite + ALL SIX demo suites green in CI order); `alembic check` clean; downgrade-base + re-upgrade smoke clean over the stage-6-extended tenant. | **Wave-7 slice 4 (the LAST) complete — the ratified sequence HG-1 → ES-HS-1 → RS-1 → DS-2 is FULLY DELIVERED. Next = the WAVE-7 CLOSE REVIEW** (the mandatory Part-4 rule-2 re-baseline; riders at the close: SC-2 the named pull-forward, commitment/capital-call the presumptive Wave-8 headline). |
| 2026-07-19 | **WAVE 7 CLOSED — `wave_7_close_review.md` RATIFIED (OQ-W7C-1…6, "Approve all")**, merged as DRAFT via **PR #66** = `cc251b2` (after the DS-2 closeout **PR #65** = `b4433fc`), ratified immediately after — the second full-ultracode close audit (**71 agents**: 4 per-slice audits + register + stamps domains; 2-refuter verification per finding — 3 killed / 1 downgraded; two judgment passes with 24 claim-checks — 22 survived, 2 corrected; completeness critic — 8 gaps all dispositioned): **all FOUR slices SHIPPED-AS-RATIFIED, ZERO shipped-code defects in the wave diff — the THIRD consecutive clean close.** All 16 surviving findings record/doc/process hygiene; **14 FIXED at the close** (headliners: the banner's 28-vs-30 count contradiction adjudicated to the DB-verified 30; the canonical data model's ZERO RS-1/DS-2 amendments — ENT-056/057/027 dated-amended; RTM REQ-MKT-001's discharged shrinkage/EWMA clause + backbone REQ-PRV-005's four-slice staleness; `var_parametric_total_v2.md`/`proxy_weight_regression_v1.md` RS-1 amendments; the four stamps' CI-run-ids backfilled #339/#348/#365/#381 — the FL-1 convention's silent lapse, now an explicit checklist item per OQ-W7C-3; the RS-1 merge-sha correction `8014b51`; CLAUDE.md's ten-day-stale "origin is SSH"); the ONE code-behavior finding (the stage-4 flagship-pair uuid4 tie-break — seed-nondeterministic demo selection; the record's 113.239146 demo value is selection-dependent) **ASSIGNED TO BT-3 BY NAME** (OQ-W7C-2). Register re-baselined (OQ-W7C-4): the estimate-staleness residual → a clause LEDGER (1 PAID at HG-1; 2 ACCEPTED-RESIDUAL absorbed into OD-B; 3 OD-B trigger-based); the α=1-detour count corrected to ≥5 (DS-2 does NOT retire the detour); **the SC-2 pull-forward rider EXPIRED UNSPENT** (Wave 7 did not underrun); the stressed-window calibration rung newly NAMED. Benchmark verdict: the Wave-6 mathematics gaps (1–2) CONSUMED, the operational/governance trio (3–5) carries — the frontier = the remediation lifecycle (MG-2; trigger unfired, earliest overdue 2027-07-19 executed, but ERODING) + the scheduler (fourth close) + **the FRTB nuance that desk backtesting is VaR-based, so the missing ES backtest trails the ACADEMIC frontier, not the Basel floor**. Demo honesty: **5 of 18 registered codes have zero living-tenant runs** (scenario, active-risk, sensitivity, benchmark-relative, + the census-masked proxy exposure family) — the named demo-completeness ride-along (OQ-W7C-5). **WAVE 8 RATIFIED (Part 2.11, OQ-W7C-6 fork A "fund the third leg"): BT-3 → CC-1 → CC-2 (Takahashi-Alexander pacing, the 16th-governed-number headline)** with the six ratified riders (BT-3's Z1/threshold re-verification MUST; CC-2's named Tier-3 forks; the TA-fetch fallback; SC-2 the pull-forward; the stage-7 demo obligation; the slot-zero opener option). | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-19 ("Approve all"). |
| 2026-07-19 | **BT-3 DONE — the Acerbi-Szekely ES backtest + the Christoffersen v2 (planning **PR #68** = `b493c78`; impl `bt-3-impl` tip `c5533b5` via **PR #69** = `109d11d`, **CI green run #399**; migration head `0042`→**`0043_es_backtest`**)**: **`risk.es_backtest` = the SIXTEENTH governed number** (the BT-1 mint precedent, OD-D — CC-2's candidate number becomes the SEVENTEENTH, the Part-2.11 row corrected by dated note), closing the 15th governed number's exemption from the platform's own outcomes analysis. The ratified fetch MUSTs DISCHARGED: Z1 re-verified two routes + the null-expectation identity settling the '+1' grouping (the corrupted grouping evaluates to ≈ −0.7004 — numerically coinciding with the −0.70 critical, so the exact-Fraction identity regression is the mandatory mechanical defense); the −0.70/−1.8 thresholds at the three-route constant bar (M-P + Lund Table 1 + executed seeded MC; the doctrine finder's t3-misattribution catch folded PRE-merge, before the limitation row seeds immutable). **The Tier-3 headline (OD-B): EVIDENCE-FIRST + a NARROW domain-gated verdict** — the AS criticals are α-, T-, AND df-dependent (executed MC: 5% critical ≈ −3.68 at T=10, −0.70 only at T=250), so the verdict fires ONLY at (paired confidence 0.9750 ∧ n_pairs 250); off-domain runs persist Z2/Z1 evidence + `ES_PAIR_COUNT` and NO verdict (the BASEL_ZONE read-rule precedent; a short-series verdict would spuriously REJECT the platform's own flagship ES); the per-(α,T) table = the named v2 under a governed OFFLINE MC derivation (TR-09). **Christoffersen 1998 ships in-slice as `risk.var_backtest` v2** (declared `independence=CHRISTOFFERSEN_MARKOV`, the COUNTING tri-state parse; LR_IND nonnegative orientation — both Evers-Rohde source wobbles disclosed; `LR_CC = LR_UC + LR_IND` from the STORED 6dp legs; degenerate 2×2 ⇒ None; v1 byte-preserved) — the twice-re-teed item DISCHARGED. Sibling-pair gates (OD-C): per-as-of BIJECTION on IDENTICAL `input_snapshot_id`, cross-leg confidence uniformity, per-leg model-version uniformity, the behavior-preserving `adjudicate_return_side` extraction (BT-1 45/45 byte-identical). ONE additive migration **0043** (`es_value` echo + the downgrade-honesty docstring). The OQ-W7C-2 tie-break fix folded BY NAME (`window_end DESC, system_from DESC, calculation_run_id DESC` — `VarResult` has `system_from`, not `created_at`). **Demo stage 7 = the DOMAIN-GATE HONESTY demo**, deliberately NOT a verdict (Z2 = −127.09 verdict-WITHHELD off-domain; the LR_CC joint-power lesson LIVE at n=3: neither Kupiec 3.66 nor LR_IND 2.77 alone rejects at χ²(1)=3.841, the joint 6.43 > χ²(2)=5.991 DOES); 4 INITIAL AWCs; **NO TRIGGERED, census-proved** (the DS-2 honesty pattern); the 19th registered code. **4-finder review: ZERO HIGH** — adversarial 0 MED + 4 LOW, numeric 0 defects (full 2×2 enumeration 2,304+96, 1,400-series exact-Fraction fuzz, the stage-7 numbers byte-re-derived from pins; the 05-22 breach margin 9.61× VaR) + 4 precision caveats (referent-stated), doctrine 1 MED + 3 LOW, scope-fence 11/11; ALL folded except the 2 named **D-F4 next-touch deferrals** (the ES-1 stale "(a BT-3 candidate)" forward refs — the registered-row reword needs its OWN finding-key fence check; → the Wave-8 close register). Battery: `make check` **1627**; fresh-schema local PG **1960/0/0** (all SEVEN demo suites in CI order; stage 7 seats after stage 6, before the downgrade smoke); `alembic check` clean; downgrade-base + re-upgrade smoke clean; the living tenant re-seeded seven-stage — **19 codes / 34 validation records (11 EXC + 16 INIT + 7 TRIG) / 95 COMPLETED runs, DB-verified**. | Wave-8 slice 1 complete per the ratified sequence. Next = **CC-1 planning** (captured commitments/calls/distributions — ENT-015 FR-bitemporal + ENT-016 IA; mint REQ-PRV-001/002; capture-first, the PA-0 shape; Tier-3 forks at planning: commitment grain fund-vs-instrument + permission reuse-vs-mint). |
| 2026-07-20 | **The UI/read-surface fresh look (user-directed: "the front end UI will be a core part of this product … take a fresh (objective) look") — assessment RECORDED (`ui_read_surface_assessment.md`), rule 7 ADDED, the Part-3 frontend-growth bullet REWRITTEN.** The census (every GET across all routers, file:line-grounded) found: **(F1)** the governed read surface is strictly RUN-CENTRIC — all 13 result families read only by run/result id; NO portfolio/as-of/date-range filter, NO latest-resolver, NO metric time-series, NO cross-family summary exists anywhere, while the CAPTURED families already have the right entity/time-centric shape (`/positions/as-of`, `/valuations`, `/portfolios/{id}/holdings`) — the compounding gap, one family wider per governed slice, blocking screens AND the thesis-§2.3 agent-consumable obligation equally; **(F2)** the governance story is the least readable part of the API (validation findings/evidence accepted on POST but returned by NO read; NO audit read endpoint; no snapshot listing; `tier` absent from the model-inventory list); **(F3)** FE types are hand-written mirrors under FE-1's expired "five endpoints" rationale — now 12 families across four hand-synced registries, with the FL-1 drift trio the recorded evidence (incl. the ES `z×σ` display — the one place a governed number visibly failed to check out); **(F4)** identity is the self-labeled DEV-PLACEHOLDER header shim (enforcement behind it real; verified identity absent — AD-007). **Changed NOW (user-approved): Part-4 rule 7** — every governed number ships its entity/time-centric read IN-SLICE from CC-2 forward, captured slices ship entity-filtered lists from birth (CC-1's plan amended pre-ratification: step-6 list endpoints take `portfolio_id`/`instrument_id` filters); shipped-family back-fill = API-1's scope. **Weighed at the Wave-8 close (rule 2), NOT ratified today: the candidate Wave-9 sequence API-1 (governed read surface + the F2 governance reads; read-only, RLS/permission posture unchanged) → FE-2 (OpenAPI-generated types; retires OD-FE-1-G dated) → SSO-1 (OIDC/AD-007; the hard gate before any non-developer sees the product) → FE-3 (the product UI — the IA is a Tier-3 USER decision; the recommended spine is the living tenant's own capture→numbers→backtest→validation→limitations walk, not a generic run browser).** The no-FE-expansion rule HOLDS until that wave ratifies; Wave 8's ratified sequence is unchanged. | The user set a new premise (UI = core product); the FE-1-era ride-along posture no longer fits it. Rule 7 stops the F1 gap growing during the remaining math slices; everything wave-sized waits for the mandatory close-review weighing per rule 2. |
| 2026-07-20 | **CC-1 DONE — captured commitments / capital calls / distributions (planning **PR #71** + the rule-7 amendment **PR #73**; impl `cc-1-impl` tip `6fac50e` via **PR #74** = `1cdc95b`, **CI green run #420**; migration head `0043`→**`0044_private_capital`**)**: the capture-first half of the Wave-8 headline (the PA-0 split) — **ENT-015/016 REALIZED as captured inputs** (NO governed number / model code / run family / snapshot; the counts stay 16 numbers / 19 codes / 34 records / 95 runs, DB-verified). **The planning verifier pass materially changed the ratified shape pre-implementation** (2 HIGH: event linkage moved off FR version-row FKs onto the stable (portfolio, instrument) identity + the provenance-only `commitment_version_id` echo; currency made CHAIN-IMMUTABLE — the immutable events validate against it); the reversal ships as the transaction NEGATION convention (amount = exact negation; Σ self-correcting; one-reversal-per-event partial-unique). **The Tier-3 forks resolved as ratified (user "Proceed" 2026-07-20):** grain = (portfolio, fund-instrument); permission = the `commitment.edit`/`.record`/`.view` **three-code R-07 mint** (the FR/IA verb doctrine — `.edit` the FR maker, `.record` the IA maker, identical holder sets data_steward+admin, auditor_3l excluded per the captured-input precedent). REQ-PRV-001/002 → In-Progress (computed-unfunded + liquidity-forecast clauses OPEN → CC-2; "Events immutable" DISCHARGED test-pinned both layers); the `PRIVATE.*`/**EVT-240 decade ACTIVATED** (seven codes incl. the distinct REVERSE verbs — the TRANSACTION.REVERSE precedent; the PA-0 `action="correct"` + symmetric-payload lessons pinned from birth); **rule 7 applied from birth** (all three list reads entity-filtered; silent-empty on unknown ids — the positions/valuations precedent, the plan's 422 over-reach corrected as a fold); the sanctioned `_raise_mapped_write` hoist to `api/write_errors.py` (byte-identical; the 344 marketdata endpoint tests the golden). **Demo stage 8** = the capture half of the ratified commitment walk (PE-MERIDIAN-X, 25M USD by DEMO-GLOBAL; the mis-capture/REVERSE/recapture teaching pair; the recallable distribution captured as data; the no-auto-bridge zero-transactions assertion — the OD-CC-1-D read rule mechanically visible). **4-finder review: ZERO HIGH — 8 MED + ~10 LOW ALL folded** (adversarial: the de-inherited no-op `valid_from`; the honest no-close-out-op wording — a first-capture currency fat-finger is permanent, chain-terminate = the recorded v2; numeric EXECUTED: the post-quantize (20,6) envelope — a sub-quantum input would have minted a PERMANENT zero-amount immutable row; doctrine: the read rule at every promised surface + the per-op event-count pins; scope-fence 12/12, zero out-of-fence). Battery: `make check` **1660**; fresh-schema local PG **2005/0/0** (all EIGHT demo suites in CI order; stage 8 after stage 7, before the downgrade smoke); `alembic check` drift-clean; downgrade-base + re-upgrade smoke clean. Close-register notes: the demo-registrar maker-verb doctrine echo (A-9); the SoD table's pre-existing blank-line break (D-L5). | Wave-8 slice 2 complete per the ratified sequence. Next = **CC-2 planning** (the SEVENTEENTH-governed-number HEADLINE: the Takahashi-Alexander commitment-pacing projection — **fetch TA (JPM 28(2):90–100) to paragraph/equation level FIRST, the ratified planning MUST**; the declared-parameter-pacing fallback rider if the primary fails the citation bar; named Tier-3 forks: the new ENT id (ENT-059) + the family/permission question; the stage-8/9 demo obligation = the projection half of the commitment walk on THIS seeded commitment). |
| 2026-07-20 | **CC-2 DONE — the governed commitment-pacing projection (the Wave-8 HEADLINE; planning **PR #76**; impl `cc-2-impl` tip `a266b56` via **PR #77** = `1eaa703`, **CI green run #432**; closeout `cc-2-closeout`; migration head `0044`→**`0045_pacing_projection`**)**: **`pacing.commitment_projection` = the SEVENTEENTH governed number** (ENT-059), the FIRST private-capital derived output — it DISCHARGES the REQ-PRV-001 "computed" leg + the Funded/unfunded test CC-1 left open (per-period `unfunded_end` persisted; the "aggregated" portfolio rollup honestly kept OPEN → v2) AND the REQ-PRV-002 "feed liquidity forecast" clause. **The ratified TA-fetch MUST DISCHARGED via reproduction** (the JPM primary is gated — R1 Jaeckel + R2 Tamarix equation-level, R3 Luxenberg-Boyd structural, R4 FRG input-list; the ES-HS-1/AS-2014 precedent; per-source grades in the referent), so the declared-parameter fallback rider did NOT fire — the wave kept its headline. **The deterministic Takahashi-Alexander recursion** (`C=RC·Unfunded`; `RD=max(Y,(t/L)^B)`; `NAV` roll with growth) — **FUTURE-ONLY from a pin-derived deterministic age** (the planning verifier's period-geometry HIGH resolved by DELIVERY, not rationalized; TR-09-safe), QUANTIZE-THEN-ROLL 6dp, ANNUAL, NO optimizer. **The FIVE declared parameters ARE the version identity** (parsed back fail-closed; register/bind magnitude symmetry a review fold); **NO TA constant minted** — only the `functional_form=TAKAHASHI_ALEXANDER` marker. Tier-3 forks as ratified: the new PEER `pacing` family (neither risk nor perf; AST-fenced from `private_capital` — reads only through the pin); the **`pacing.run`/`pacing.view` R-07 mint** (a governed-output `.view` INCLUDING `auditor_3l` — the decisive contrast with CC-1's captured-input `commitment.view`). `PACING.*`/**EVT-250** RESERVED-not-minted (`CALC.RUN_*` reused; `audit/service.py` FROZEN). Consumes ONLY the `PACING_INPUT` snapshot; the mid-life re-anchoring (Σcalls, recallable restoration, the pinned same-currency NAV mark; funded-without-mark REFUSED) = OUR documented adaptation. **Rule 7 all three legs IN-SLICE + the platform's FIRST latest-resolver** (`as_of`-aware; cross-run aggregation a CONSUMER ERROR). **Demo stage 9** = the projection half of the commitment walk on the stage-8 PE-MERIDIAN-X commitment — **counts MOVE 19/34/95 → 20/35/96, DB-verified** (the governed contrast with stage 8's capture-only-unchanged is the demo's own story). **4-finder review: ZERO surviving HIGH** — the adversarial + doctrine finders converged on ONE real FAILED-magnitude-gate cluster: the envelope `1e26` sat ABOVE the `Numeric(28,6)` column capacity (~1e22) → a value in [1e22,1e26) passed the gate then raised a PG `numeric field overflow` (invisible on SQLite), AND extreme declared growth compounded past the Decimal context → `InvalidOperation` INSIDE the kernel before the gate → 500; fixed by envelope→`1E21` (the var/es-backtest `_MAX_RESULT_ABS` precedent for the same column shape) + a kernel `_MAGNITUDE_CEILING` that turns runaway params into a committed FAILED run; the previously-UNTESTED FAILED path now covered. 3 LOW folded (`-0` growth identity, register/bind symmetry, the Feb-29 clamp against `nth_anniversary`). Math CONFIRMED correct (both hand-goldens re-derived to the last digit); scope-fence CLEAN (frozen files zero-diff, head-pin sweep complete, `TIER_DOSSIERS` 16-pin intact). Battery: `make check` **1716**; fresh-schema local PG green (all NINE demo suites in CI order; stage 9 after stage 8, before the downgrade smoke); `0045` downgrade clean; the living tenant seeded nine-stage. | Wave-8 slice 3 (the headline) complete per the ratified sequence — **all three Wave-8 slices DONE**. Next = the **Wave-8 close review** (its register carries: BT-3's 2 D-F4 stale-ref deferrals; the CC-1 close-register A-9/D-L5 notes; the local downgrade-base smoke's pre-existing 0002-permission-seed vs app-bootstrap-grant FK conflict — CI-green throughout, environmental to the local container). |
| 2026-07-20 | **WAVE 8 CLOSED — `wave_8_close_review.md` RATIFIED (OQ-W8C-1…6, user "Approve all")**, pushed on `wave-8-close`. The rule-2 mandatory close after Wave 8 shipped BT-3 → CC-1 → CC-2 (the private-capital third leg + the 17th governed number). Three cross-cutting close auditors over the wave diff `cc251b2..06b40b1` (Opus-only, proportionate — each slice already 4-finder-reviewed with zero surviving HIGH): **cross-slice integration CLEAN (zero HIGH, zero MED — the FOURTH consecutive zero-shipped-defect close)**; all six composition seams verified (linear migration chain `0043←0044←0045` no cross-slice FK orphan; the snapshot serializer purely additive — 0038 landmine untripped; the demo count-flow structurally order-independent via stage 9's idempotent-replay fixture; no EVT-decade collision; the pacing↔private_capital AST fence holds; both R-07 parity tests present). **At-close hygiene folded** (make check 1716 green): the SoD-table-breaking blank line that was orphaning the two Wave-8 rows from their header (CC-1 D-L5, upgraded); the canonical "next free id" → ENT-060 (ENT-059 minted); three fence-safe stale "BT-3 candidate" refs (doc + two code comments — part of BT-3 D-F4); the current_state banner refreshed to the CC-2 truth with the prior block demoted to history. **Register dispositions:** the BT-3 D-F4 registered-limitation-string reword DEFERRED to a dedicated ES/var-backtest touch (finding-key fence pre-verified safe; the Christoffersen-shipped-vs-appraisal-pairing-v2 split is a content edit, not a hygiene sweep); CC-1 A-9 ACCEPTED as the standing demo-seeding convention. **Corrected at close:** the local `alembic downgrade base` FK conflict is a local-container artifact, NOT a real defect — **disproven by CI #432**, whose downgrade-smoke step passed after full nine-stage demo seeding. **Completeness:** the OQ-W7C-5 demo-completeness rider is unpaid a second wave — **5 of 20 codes have zero living-tenant runs** (sensitivity/active-risk/scenario/benchmark-relative/proxy-exposure); the highest-value UI-wave carry-in (API-1's reads/FE-3's screens render empty for them otherwise). **WAVE 9 RATIFIED (OQ-W8C-3, the read-surface + FE + identity wave): API-1 → FE-2 → SSO-1 → FE-3** (Part 2.12) — API-1 carries the 5-code demo rider (OQ-W8C-4, pays OQ-W7C-5); FE-3's IA is a deferred Tier-3 USER decision (OQ-W8C-5); planning-gate caveats: API-1 M/L-vs-L scope split; FE-2's OpenAPI decimal-as-string precondition. The no-FE-expansion rule LIFTS for Wave 9. | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-20 ("Approve all"). |
| 2026-07-21 | **API-1 DONE — the governed read surface + the F2 governance reads (Wave 9 slice 1; planning **PR #81**; impl `api-1-impl` tip `0288f29` via **PR #82** = `ae588a5`, **CI green run #449**; NO migration)**: the read-only back-fill discharging F1 (run-centric-only reads) + F2 (the governance story's illegibility) from the UI/read-surface assessment. **Class A** (8 entity-native families) + **Class B** (covariance/scenario/sensitivity latest-run resolvers) + **2 by-id parity GETs**, all through the shared `calc/reads.py` helper (factoring CC-2's `list_pacing_projections`/`latest_pacing_projection` — the platform's first latest-resolver, now the house template for seventeen families). The **F2 governance reads**: validation findings/evidence detail, `tier`+`validation_status` on the inventory list, a snapshot listing, and the **audit-trail read** — a NEW `audit/queries.py` (the FROZEN `audit/service.py` NEVER imported) + `api/audit.py`, gated the RATIFIED `lineage.view` reuse (no new R-07 mint; the accepted maker-holder consequence per OD-API-1-C), metadata-only (no payload-body leak). **PURE read-only** — no migration, no new writes, no permission-posture change; run-id reads + TR-09 reproducibility untouched. **RIDER PAID (OQ-W8C-4/OQ-W7C-5): demo stage 10** executes the five registered-but-never-run codes — RUNS-ONLY, **counts move 20/35/96 → 20/35/101** (the deliberate contrast with CC-2's code+record+run mint) — so the API-1 reads render non-empty for every family; the `stage9z` filename collates last in a shared-DB local battery (the CC-2-recorded stage10 zero-pad hazard). **The flagship VaR/active-risk entity read is DEFERRED to API-1b** (OD-API-1-H) — the pre-ratification verifier REFUTED read-only feasibility (subtree-scoped runs, no recorded root portfolio); ratified 2026-07-20, needs one additive `calculation_run.scope_portfolio_id` column. **4-finder review: ZERO HIGH from any lens** — doctrine/security lens CLEAN (all 6 hard invariants independently re-verified against the merged diff); one MED (the audit `_iso` reader's naive-datetime handling diverged from the FROZEN writer's guard — an off-UTC compliance-window bug, folded with a regression test) caught by two independent finders; one MED (three new HTTP reads — exposure, perf desmoothed-returns, perf benchmark-relative — shipped without endpoint tests, closed); one LOW (a dead constant) removed. Battery: `make check` **1736**; fresh-schema full-PG battery green (all TEN demo suites, correct `stage9z` collation order); downgrade-base smoke clean with the stage-10 rows present. | Wave-9 slice 1 complete per the ratified sequence. Next = **FE-2 planning** (OpenAPI-generated FE types; the hard precondition — verify PreciseDecimal serializes as OpenAPI `string` before committing to codegen). |
| 2026-07-21 | **FE-2 DONE — OpenAPI-generated FE types (Wave 9 slice 2; planning + impl `fe-2-impl`, **PR #84** = `2ce9e4a`, **CI green run #456**; NO migration)**: the hard precondition CONFIRMED first (every governed response decimal already serializes `string`; only the unconsumed `*In` request bodies carry `number\|string`), so codegen introduces zero `Number()`-corruption risk on the read path. `openapi-typescript` (types-only) generates from a deterministic `scripts/dump_openapi.py` dump; `openapi.json` + `api-types.d.ts` COMMITTED with a NEW dedicated "API type drift" CI job (Python+Node) that regenerates + `git diff --exit-code`s — backend↔FE drift now turns CI RED. `FAMILY_ROW_COLUMNS` bound to the generated `*RowOut` types (**the FL-1 kill, PROVEN**: a bogus column key fails `tsc`). The GET-only hand-written client KEPT (no `openapi-fetch` — the read-only fence preserved, zero new runtime dep). OD-FE-1-G superseded-in-part (dated; the strings-verbatim + GET-only clauses explicitly PRESERVED). **4-finder review: 1 HIGH folded** — the adversarial finder proved the first-pass decimal guard sampled only 8 of ~43 governed decimal fields, so a `VarRowOut.sigma`-class regression (`string`→`number`) would compile clean and silently reach the DOM (the exact FL-1 corruption class, reopened); fixed with an EXHAUSTIVE compile-time guard (`OnlyCountsAreNumbers` — the ONLY `number` fields permitted anywhere across all 14 `*RowOut` are curated integer counts), proven to bite on the exact regression. Also folded: 1 MED (the `openapi-typescript`→`@redocly/openapi-core` dev-tree advisory disclosed — 3 HIGH, dev-only, CI production audit stays 0, codegen-time-only/never-shipped — flagged for the close register) + 2 LOW doc corrections. The determinism risk (CI's Python 3.12 vs local regeneration) was DISCHARGED by reproduction (pinned pydantic/fastapi + sorted-key dump + `npm ci` → byte-identical artifacts, verified on a fresh 3.12 venv). Battery: `make fe-check` green (typecheck/lint/format/65 tests/build); `make gen-api-check` no drift; bundle byte-identical pre/post (types-only, fully tree-shaken). | Wave-9 slice 2 complete per the ratified sequence. Next = **SSO-1 planning** (real identity / OIDC, AD-007 — the hard gate before any non-developer sees the product; enforcement behind the dev shim is already real, so this is a bounded swap). |
| 2026-07-21 | **SSO-1 DONE — real identity / OIDC (Wave 9 slice 3, F4 discharged; planning + impl on `sso-1`, **PR #86** = `422c164`, **CI green run #466**; NO migration)**: the dev-placeholder `X-User-Id`/`X-Tenant-Id` header shim replaced by a real **OAuth2 resource server** — `get_principal` verifies an `Authorization: Bearer` JWT against the issuer JWKS (PyJWT; RS256-only allow-list; `iss`/`aud`/`exp`/`nbf`+required-claims; `leeway=60s`; optional MFA `acr`/`amr` assertion) and resolves the verified `sub`→`app_user.external_subject` within the verified tenant claim to `Principal(user_id=app_user.id, ...)` (NOT the raw `sub` — `has_permission` joins on `app_user.id`). `dev_header` survives as an explicit local-only fallback (fail-closed startup guard in the FastAPI lifespan + a per-request backstop). Enforcement behind the shim (`require_permission`, FORCE RLS) UNCHANGED — a bounded swap, exactly as scoped. Closes **OD-048** (local-dev IdP = Keycloak, `infra/keycloak/`, profile-gated compose, NOT a CI dependency). **4-finder review: 2 HIGH folded.** (1) **RLS false-deny**: the `tenant_isolation` policy compares `tenant_id::text` (PostgreSQL canonicalizes lowercase-hyphenated) against the RAW `app.current_tenant` GUC — a valid-but-non-canonical UUID tenant claim was RLS-hidden, silently denying a legitimate user; fixed by canonicalizing `str(uuid.UUID(claims.tenant))` before the GUC bind, proven by a NEW PG-tier regression under a constrained non-superuser/non-BYPASSRLS role (SQLite cannot catch this class — RLS is a no-op there). **`_principal_from_token` is the first caller ever to feed `set_tenant_context` a raw externally-controlled string** — every prior caller sourced the tenant from a DB round-trip, always already canonical. (2) **CI drift**: the new `authorization` header dependency stamped 231 param blocks into the committed OpenAPI schema, staling the FE types and turning FE-2's own drift-check red — regenerated and committed, `make gen-api-check` clean. Also folded: 4 MED (require `OIDC_AUDIENCE` — a confused-deputy gap; a pathological `amr` claim crashed the MFA check to 500 instead of denying; `require_mfa` without `acr_values` booted clean then 500'd every request; zero PG-tier RLS coverage on the oidc path before the fix above) + several LOW (a `dev_header` per-request backstop; the advertised clock-skew leeway was actually zero, now a real 60s; a JWKS/discovery outage returned 500, now an honest 503; new tests for the lifespan wiring, `build_verifier`/discovery, `amr` acceptance, and the demo `external_subject` seed). Battery: `make check` **1778 passed**; full-PG demo battery green (demo tenant holds at 101 runs, `external_subject` seeded — a column set, no count change); `make fe-check` green (65 tests+build) after the OpenAPI regen; `make gen-api-check` clean. **Lesson: any code path that arms a Postgres RLS tenant GUC from an externally-controlled string must canonicalize it first — the RLS policy's `::text` cast and the raw GUC string are not guaranteed to agree in format even when they represent the same UUID.** | Wave-9 slice 3 complete per the ratified sequence — **F4 (the last read-surface finding) discharged**. Next = **FE-3 planning** (the product UI; its information architecture is a deferred Tier-3 USER decision, OQ-W8C-5). Then the **Wave-9 close review**. |
| 2026-07-21 | **FE-3 DONE — the product UI, the governance-narrative walk (Wave 9 slice 4, the LAST slice; planning + impl on `fe-3`, **PR #88** = `a0f31b5`, **CI green run #475**; NO migration)**: replaces the generic run browser as the FE's primary surface with a six-step walk over the living demo book — Capture → Exposures → Numbers → Backtest → Validation → Limitations (**OD-FE-3-A**, the IA ratified at planning as **OQ-FE-3-1**, the Tier-3 USER decision deferred from the Wave-8 close per OQ-W8C-5). The differentiator lives in `GovernedValue` (**OD-FE-3-C**): every number renders value-verbatim + run/model provenance + a `snapshotVerified` ✓/✗ mark that is structurally unfakeable (rendered ONLY when a real `snapshotId` is present — never hardcoded true/false) + the model's validation badge and disclosed limitations, assembled by `useModelValidations` chaining `/models` → `/models/{id}` → the LATEST validated version → its findings+evidence detail (the F2 governance reads API-1 shipped write-only-no-more). VaR is shown honestly as the seeded run series **across all books** with the API-1b entity-read gap (no `scope_portfolio_id` yet) stated inline in the UI copy — no synthetic "latest for this portfolio" (**OD-F**, the no-fabrication doctrine). Every pane degrades gracefully on a 403 to a calm "you need permission X" note rather than an error (**OD-FE-3-E**), proven live against a NEW demo read-only viewer principal `demo-auditor` / role `auditor_3l` seeded with exactly the walk's 11 read permissions (**OD-FE-3-H**, a Step-0 finding folded pre-review — the worker/tenant-context demo path never exercised the HTTP permission gate, so every prior demo principal had ZERO HTTP grants). Client posture KEPT: the GET-only `apiGet` fence (no non-GET call, no `Authorization` header set client-side) and the FE-2 generated-type binding, extended with `verbatim`/`shortId` formatters and a `useApiGet` staleness-guarded hook. **4-finder review (2×Opus decimal-contract + no-fabrication, 2×Sonnet partial-entitlement + correctness/a11y/CI): ZERO HIGH from any lens.** Both existential invariants independently proven: (a) the decimal-strings-verbatim contract holds on every new screen — zero `Number()`/`parseFloat`/`toFixed`/arithmetic on a governed value, all through `verbatim`; the EXHAUSTIVE `OnlyCountsAreNumbers` guard (FE-2's fix) extended to `PositionOut`/`ValuationOut` — FE-3 is the first slice to render captured-input decimals (`quantity`/`cost_basis`/`mark_value`), so `record_version` (a bitemporal version integer) joins `CountKey`; (b) no fabricated verification/verdict — the fake-"✓ reproduces" mark, a fabricated ES backtest verdict, and a synthetic "latest VaR" are each structurally UNREACHABLE, not merely avoided by convention. Folded: **2 MED** — RunDetail's "back to runs" link still pointed at `/`, which FE-3 moved from the run list to the walk overview, so a user opening a VaR run and clicking back landed on the walk instead (fixed to `/runs`); `useModelValidations` picked the FIRST validated model version (`.find`) instead of the LATEST (versions are `version_label`-ascending), so a re-versioned + re-validated model — the exact governance lifecycle the Validation step narrates — would have shown the OLDEST outcome and dropped the rest (latent today: one version per model in the seed). Plus **6 LOW**: the decimal-guard extension above; a WCAG 2.4.7 fix (the skip-link target had `outline:none`, now a visible `:focus-visible` ring); limitation-list keys switched from non-unique text to index (no unique constraint on the text); three honesty wording fixes (the Numbers-step blurb named ES, which lives in Backtest, corrected to "Portfolio return, covariance, VaR"; the VaR series note now explicitly states "across all books"; WalkStep's stale "placeholder" doc comment refreshed). Battery: `make fe-check` green (**97 tests** + build); `make check` (Python) green; `make gen-api-check` clean (FE-only, zero backend drift); `package.json` byte-identical (no new runtime dep — **OD-FE-1-F held**). **One post-review, pre-merge fix**: the fold commit's re-wrapped object literals/JSX across 9 files hadn't been re-run through Prettier, so CI's `format:check` step went red on push — a pure-reflow fix (`3275741`, `git diff -w` empty on every assertion), zero semantic change; all six CI jobs (Backend/Frontend/migration/API-type-drift/docs/secret-scan) re-verified green before merge. Counts UNCHANGED (17/20/35/101). | Wave-9 slice 4 complete per the ratified sequence — **the LAST slice; WAVE 9 IS NOW FUNCTIONALLY COMPLETE** (API-1 → FE-2 → SSO-1 → FE-3, all four DONE). Next = the **Wave-9 close review** (roadmap Part 4 rule 2, mandatory) — register carry-ins: the BT-3 D-F4 reword, the FE-2 `@redocly` dev-tree advisory, the SSO-1 `pyjwt`/`cryptography` advisory surface (no `pip-audit` CI gate), the new FE-3 `auditor_3l`-template read-gap. Then Wave 10 sequencing (named fast-follow candidates: API-1b the flagship VaR/active-risk entity reads; FE-3b SPA OIDC/PKCE login). |
| 2026-07-21 | **WAVE 9 CLOSED — `wave_9_close_review.md` RATIFIED (OQ-W9C-1/2/4/5 "Approve all" + OQ-W9C-3 fork A), pushed on `wave-9-close`.** The mandatory Part-4 rule-2 close after Wave 9 shipped API-1 → FE-2 → SSO-1 → FE-3 (the read-surface + FE + identity wave; **F1–F4 all discharged**). Method: **four cross-cutting close auditors** over the wave diff `d47cf66..9d210e4` — cross-slice integration, security/doctrine (a dedicated lens for the platform's FIRST auth boundary), doc/register coherence, completeness + Wave-10 readiness — on top of each slice's own 4-finder review; Opus-only, proportionate (ultracode off); **every material finding independently re-verified by the synthesizer**. **Verdict: ZERO shipped-code defects — the FIFTH consecutive clean close.** Integration CLEAN (all six composition seams; the OpenAPI→codegen→FE pipeline drift-free + deterministic at HEAD; gates green — `make check` **1778** / `make fe-check` **97** / `make gen-api-check` clean / `alembic check` clean, single head `0045`). Security/doctrine CLEAN (all six hard invariants held; `audit/service.py` byte-frozen + never imported by the new read path; **BR-10 sweep on the new Keycloak realm/config/compose clean** — public PKCE client no `clientSecret`, `admin/admin` an env-overridable marked local-dev placeholder, RS256 test keys generated at test time; the auth boundary fail-closed on all four sub-checks). Completeness: the three existential contracts (real RS256 identity, decimals-verbatim-to-DOM, 403 graceful degradation) each PROVEN by real non-mocked tests. **At-close folds (2, doc-hygiene):** the **API-1 decision record's Status was still "DRAFT for ratification"** — stamped CLOSED (PR #82/CI #449) — **the FIFTH consecutive missing-closure-stamp class**; and FE-3's CI cite normalized from the long Actions run id `29852439513` to the short **#475** (verified via REST: run_number 475, head_sha `a0f31b5`, success — monotonic with #449/#456/#466). **Register:** the demo-completeness gap (OQ-W7C-5) is CLOSED (API-1's stage 10 paid it, 96→101); the headline forward item = **no Python dependency-audit CI gate** (the JS supply chain is gated, the Python one isn't, on the wave that made Python the identity surface — `pyjwt`/`cryptography` now runtime) → the `pip-audit` gate rides Wave-10 API-1b (OQ-W9C-4); the BT-3 D-F4 reword still deferred; the `@redocly` dev-tree advisory no-action; the `auditor_3l` viewer demo-scoped. **Process:** the pre-ratification verifier held its value a 4th wave (API-1's Class-C refutation reshaped scope pre-implementation); the auth boundary — the wave's highest-risk change — shipped with ZERO doctrine/secrets findings under independent re-verification; **the missing-stamp class recurred a 5th time → OQ-W9C-5 ratified CI teeth** (a docs-check that fails on an un-stamped "DRAFT for ratification" record, riding API-1b). Benchmark/destination: the differentiator is now legible to a screen AND an agent; the frontier is not more math but productionizing the surface (API-1b + FE-3b) then the §2.1 private/public unification. **WAVE 10 RATIFIED (Part 2.13, OQ-W9C-3 fork A "finish the surface first"): API-1b → FE-3b → a §2.1 private/public-unification headline.** | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-21 ("Approve all" + fork A). |
| 2026-07-21 | **API-1b DONE — the flagship VaR/active-risk entity reads (Wave 10 slice 1; planning `api-1b-planning`; impl `api-1b-impl`, **PR #92** = `f1e830f`, **CI green run #488**; migration `0046_run_scope_portfolio`)**: pays the ONE read API-1 deferred — "latest VaR / active-risk for portfolio P" — resolved at the **write** boundary (API-1's verifier had refuted read-only resolution: `var_result`/`active_risk_result` carry no `portfolio_id`, and their upstream runs are subtree-scoped with no recorded root). **ONE additive nullable `calculation_run.scope_portfolio_id`** column (the `environment_id`/`failure_reason` precedent — no RLS/grant/trigger change) threaded through the SINGLE `create_run`/`execute_governed_run` choke point and stamped by all FIVE binders: `run_exposure` from its direct `portfolio_id` arg (the subtree ROOT); `run_factor_exposure`/`run_var`/`run_var_historical`/`run_active_risk` COPYING it forward from their resolved upstream run (the pre-ratification verifier proved this holds in BOTH the build and snapshot-consume input paths — the write-boundary crux that API-1's read-boundary could not resolve). The Class-C `list_var_results`/`latest_var_for_portfolio` + `list_active_risk_results`/`latest_active_risk_for_portfolio` reads resolve via the EXISTING `calc/reads.py` helper (zero helper change — a `scope_portfolio_id == P` equality filter); `active_risk`'s native `benchmark_id` filter also lands. **OQ-API-1b-1 = A "honest-NULL"** (ratified): a snapshot-consume-rooted chain (exposure OR factor) stays NULL and is disclosed-unresolvable — the fully build-in-request chain (demo/UI/default-API) always stamps a real root; no data back-fill. **Both ratified Wave-10 CI riders landed here**: the `pip-audit` gate (OQ-W9C-4) and the closure-discipline docs-check (OQ-W9C-5). **Pre-ratification verifier pass RAN** (6 adversarial claims): the copy-forward crux + TR-09 hash-neutrality + migration neutrality + read non-shadowing all HELD; 2 COMPLICATED findings folded pre-implementation as wording/spec fixes (a second snapshot-consume NULL-origin at the exposure tier, not just factor; the closure-check's filename-keyed row-anchored mechanic, needed to dodge a demonstrated false-positive trap — "API-1b" appears as prose inside two other slices' `✅ DONE` rows). **4-finder review: ZERO HIGH** — write-path (the copy-forward proven correct across all 5 binders + both paths, immutable-after-creation, TR-09-neutral by grep, complete — no unstamped run creator); doctrine/security (all 6 hard invariants held; the cross-tenant probe confirmed `scope_portfolio_id` is NOT a security boundary — tenant isolation stays the RLS `tenant_id` policy, double-bound with an explicit tenant filter, so a foreign `portfolio_id` is silent-empty with no existence oracle); read-correctness (filter/run_type/latest-run-selection all correct; `/latest` declared before `/{id}`, zero shadowing; OpenAPI regen deterministic, 4 new paths, zero new `number`-typed decimal field); CI-riders+honesty (found and fixed a REAL gate hole — the pip-audit step originally scanned `-r requirements-dev.txt`, which MISSES a package's runtime pins declared only in its own `pyproject.toml`; `python-multipart` (the `/ingest/upload` form-parse surface, CVE history) slipped the gate entirely — fixed to audit the INSTALLED ENVIRONMENT, true npm-production parity). Folded **5 MED + 1 LOW**: the pip-audit target fix (above); the closure-check's own FAILURE-path teeth were untested (an inverted implementation would have passed both prior tests — added a test proving the rule actually FIRES on a DONE-in-roadmap + still-DRAFT record); the closure-check's guarantee was over-claimed in its own comment (rescoped to the go-forward cadence — pre-Wave-9 roadmap-row shapes are out of scope, not silently covered); the record's "`/latest` 404 (the pacing precedent)" wording was a mis-cite (the shipped list-shaped `/latest` correctly returns `[]`, matching the covariance/sensitivity/factor-exposure/var-backtest siblings — pacing's `/latest` is a single-object resolver, hence 404); the copy-forward endpoint tests strengthened from asserting non-null to asserting VALUE-equality against the upstream exposure run's stamp. Also disclosed: the one `pip-audit` allowlist entry is `PYSEC-2026-1845` (a dev-only `pytest==8.3.3` advisory, fix is a risky 8→9 major bump, deferred to its own hygiene slice) — `pyjwt`/`cryptography` (the identity-verification surface SSO-1 shipped) audit CLEAN and are explicitly NOT ignored; `pydantic-settings` bumped 2.14.1→2.14.2 clearing a real advisory the gate surfaced (GHSA-4xgf-cpjx-pc3j). Battery: `make check` green; `make fe-check` green (97 tests+build); `make gen-api-check` clean; full-PG affected-family battery green; `0046` downgrade `-1` + re-upgrade smoke clean, `alembic check` clean; the pre-existing `test_synthetic.py` no-migration guard advanced 0046→0047 (comment-only, per-slice ledger convention). **Counts UNCHANGED (17/20/35/101 — a pure read + one write-side scope-provenance column, no new runs/codes/records).** | Wave-10 slice 1 complete per the ratified sequence — **the single most-wanted UI/agent read now resolves.** Next = **FE-3b planning** (the SPA OIDC/PKCE browser login — turns the real OIDC backend SSO-1 shipped into something a non-developer can actually reach, replacing the `dev_header` demo session). Then the §2.1 private/public-unification headline. |
| 2026-07-21 | **FE-3b DONE — the SPA OIDC/PKCE browser login (Wave 10 slice 2; planning `fe-3b-planning`; impl `fe-3b-impl`, **PR #95** = `2cbb68c`, **CI green** — Backend/Frontend/migration/API-type-drift/docs/secret-scan all success; NO migration)**: turns SSO-1's real OIDC resource server into something a non-developer can actually reach — the browser auth-code + PKCE flow, hand-rolled over Web Crypto (**OQ-FE-3b-1 = A**, zero new runtime dep, `package.json` byte-identical, OD-FE-1-F held). A discriminated-union `Session = DevSession \| OidcSession` (`kind` discriminant) swaps the wire bytes at the ONE `client.ts` choke point (`kind==='dev'` → `X-User-Id`/`X-Tenant-Id`; `kind==='oidc'` → `Authorization: Bearer <token>`; `method:"GET"` stays hard-coded — the read-only fence is the METHOD, not the header); the `DevSession → Session` annotation widened across 16 pass-through signatures (type-only, mechanical, runtime pass-through unchanged). `beginLogin`/`completeLogin` (`auth/oidc.ts`): generate a `code_verifier`+S256 `code_challenge`+`state`, stash single-use in `sessionStorage`, redirect to the Keycloak `irp-frontend` public client's `/authorize`; a `/callback` route declared ABOVE the session gate in `App.tsx` validates `state` (CSRF) BEFORE the exchange, strips `?code` from the URL synchronously (`history.replaceState`, referrer-leak hygiene), exchanges via a public-client POST (no secret), clears the single-use verifier+state on every path, decodes `sub`/`exp` for chrome only (the backend is the verifier) — a StrictMode `ran` ref-guard prevents a double-exchange. `dev_header` mode is BYTE-UNCHANGED (`SessionForm`+`DevBanner`); oidc mode hides `DevBanner` (a verified Bearer session IS a security boundary — showing "unverified" over it would be a lie) and routes "End session" through a real Keycloak `end_session_endpoint` logout. Demo end-to-end (**OQ-FE-3b-3 = A**): the `demo-auditor` realm user + BR-10-marked local password credentials on the 3 demo users + the compose backend oidc-profile env — **`OIDC_JWKS_URI=http://keycloak:8080/realms/irp-local/protocol/openid-connect/certs`** is the load-bearing piece (a naive `ISSUER`/`AUDIENCE`-only wiring 503s every login, since in-compose JWKS auto-discovery to `localhost:8080` hits the backend container, not Keycloak). **Pre-ratification verifier pass RAN** (5 adversarial claims): the token-contract crux HELD (the realm's 3 client-level mappers auto-cover a new `demo-auditor` user); PKCE/state design HELD; 2 COMPLICATED findings folded pre-implementation (the JWKS-URI issuer-consistency split; the true 16-file type-only footprint, corrected from an initial "~10 files" undercount) — no redesign. **4-finder review folded 1 HIGH + 3 MED + LOWs**: **HIGH** — the deployed nginx image shipped NO SPA history-fallback, so Keycloak's redirect to `/callback` 404'd (stock nginx looked for a file literally named `callback`) and the ratified demo login could not complete; fixed via `infra/docker/frontend-nginx.conf` (`try_files … /index.html` + a `proxy_pass` for the 13 governed read prefixes to `backend:8000`, COPY'd in `frontend.Dockerfile`) — `docker compose --profile oidc up` now genuinely completes a login AND renders the walk. **MED** — the OIDC logout sent a bare-origin `post_logout_redirect_uri` that didn't match the realm's `/*` pattern (fixed: send `${origin}/` + register `post.logout.redirect.uris` on the client); `beginLogin` (the redirect-assembly half) was untested, so a silent PKCE downgrade (plain vs S256) would have slipped past CI (added a test asserting the stashed verifier/state + the authorize URL's `code_challenge_method=S256`, plus a non-secure-context refusal test); the OD-FE-3b-H footprint sentence still read the stale "~10 files" estimate (corrected to the true 16). **LOW** — a `window.isSecureContext===false` guard in `beginLogin` (a clear error, not an opaque `TypeError`, on a plain-HTTP deploy); a `<meta name="referrer" content="no-referrer">` in `index.html` (belt-and-suspenders against a future cross-origin `?code` leak). **Security crux HELD under independent re-verification** (finder 1, zero HIGH/MED of its own): the RFC 7636 Appendix-B known-answer vector passes through the real `codeChallengeS256`; `state` validated before the exchange; `?code` stripped synchronously with no cross-origin subresource load; the token never logged/URL'd, only the `Authorization` header. **Doctrine fences HELD** (finder 4): zero backend-logic diff, `audit/service.py` byte-unchanged, `package.json` byte-identical, no governed mint, BR-10 clean. Carried (LOW, disclosed, not folded): `directAccessGrantsEnabled:true` on the public `irp-frontend` client leaves ROPC/password-grant open — pre-existing (SSO-1's realm), used by the README curl example, accepted for the local demo, consider disabling post-demo. Gates: `make fe-check` green (110 FE tests + build); `make check` (Python) a no-op; `make gen-api-check` clean (zero endpoint drift); `npm audit --omit=dev` 0. **Counts UNCHANGED (17/20/35/101 — FE + realm/compose only, no new runs/codes/records).** | Wave-10 slice 2 complete per the ratified sequence — **WAVE 10 SLICES 1+2 COMPLETE** (API-1b → FE-3b, both DONE; the demo now has both the flagship entity read AND a real browser login). Next = **the §2.1 private/public-unification headline** (Wave-10 slice 3) — fetch/cite the unification approach first (Tier-3 methodology), then plan the governed number. |
| 2026-07-22 | **PPF-1 DONE — the pure-private factor return, §2.1 unification arc slice 1 of 3 (Wave 10 slice 3; planning `ppf-1-planning`; impl `ppf-1-impl`, **PR #98** = `9d64b49`, **CI green** — all 6 jobs; migration `0047_private_factor_return`)**: opens the §2.1 headline. The fetch/cite pass found the unification embryo already shipped (`risk.var.parametric_total`, PA-4 — public-factor variance + a diagonal idiosyncratic residual leg) but missing the MSCI Private Equity Factor Model's (Shepard 2014/2025) "pure private" leg — systematic private risk a public proxy cannot capture. The user ratified **fork B** (deepen the math) over a **3-slice arc**: PPF-1 = the pure-private factor RETURN series (**the 18th governed number**, `risk.factor_return.pure_private`) → PPF-2 = the private covariance block Ω_pp → PPF-3 = the unified number `√(x'Σx + p'Ω_pp·p + residual)`. **The construction**: per member per appraisal period, `pp_i,t = desmoothed_i,t − Σ_f w_i,f·R_f,t` (the desmoothed appraisal return, PA-1, minus the proxy-implied return — the member's current-head REGRESSION proxy blend, public factor returns compounded over the half-open `(period_start, period_end]` window via a shared alignment helper extracted from PA-3), pooled **EQUAL-WEIGHT** across members sharing the **identical interval** (RETAIN_ALPHA — the liquidity premium stays in the factor, per MSCI; both declared identity, OQ-PPF-1-2/3). A new `PRIVATE` factor family + `APPRAISAL` frequency, fail-closed OUT of every DAILY covariance/VaR gate until PPF-2/3 mint the conversion. **The pre-ratification verifier REFUTED the naive isolation premise** (an unguarded PRIVATE segment-membership row — captured as a weight-1 MANUAL `proxy_mapping` row — would refuse every new PA-2/FL-1 exposure run pre-create) and forced a redesign into **three guards**: the exposure-snapshot-builder family filter; splitting the capture-admission family set off `LOADING_FACTOR_FAMILIES` (deliberately overturning a prior shared-verbatim invariant, keeping the FL-1/RBSA gates byte-identical); the PRIVATE⇒MANUAL capture invariant (keeping total-VaR's REGRESSION-method pin filter safe). **4-finder review: ZERO HIGH**, 1 MED + 2 LOW folded: **MED** — `update_factor` admitted `factor_family`/`frequency` as updatable, so a public factor with an existing REGRESSION blend could be FLIPPED to PRIVATE/APPRAISAL in place, retroactively bypassing the capture-time guards (stranding a REGRESSION row on a now-PRIVATE factor; refusing a previously-valid public exposure run) — fixed by FREEZING both as gate-admission identity (byte-identical for all shipped usage, every existing call only amends `description`/`region`). **LOW** — the magnitude gate's `1E8` envelope equaled the `Numeric(20,12)` column cap (a raw value in the last quantum could pass the gate then quantize up into a PG-only overflow, the CC-2 lesson) — fixed to gate the quantized values; a by-segment read ordered on `metric_type` alone — fixed to `(metric_type, period_start)`. Demo stage 11 (`ppf1_stage11.py`) pools **both segments at min_members=1 with ZERO new seeded book data** (PE-HARBOR-IV/PRIVATE_EQUITY, blend→FX_USD; PC-BRIDGEWATER-II/PRIVATE_CREDIT, blend→MF_RATES_GOV+MF_CRSPD_IG — both already carried a COMPLETED desmoothing run + a promoted REGRESSION blend), disclosing the member count on every row; files ONE INITIAL AWC (TIER_2, MEDIUM/MEDIUM). Counts moved **20/35/101 → 21/36/103** (1 new code + 1 record + 2 runs), PG-proven including the CI-order sequencing (`stage9zz` sorts after `stage9z`, so the API-1 20/35/101 pin asserts before PPF-1 seeds). Rule-7 reads (`/risk/private-factor-returns[/latest,/{id}]`) + the exhaustive FE decimal guard extended. Battery: `make check` 1802 passed; `make fe-check` 110+build; `make gen-api-check` clean; the full demo PG chain in CI order green; migration 0047 downgrade/upgrade smoke + `alembic check` clean; the ENT-060 RLS/append-only PG test green. | Wave-10 slice 3, arc slice 1 of 3 complete — **the §2.1 headline's return-series substrate is real.** Next = **PPF-2 planning** (the private covariance block Ω_pp — needs a new covariance snapshot-builder input path over the governed `private_factor_return_result` table + the minted appraisal→daily frequency-conversion declared parameter). |
| 2026-07-22 | **PPF-2 DONE — the private covariance block Ω_pp, §2.1 unification arc slice 2 of 3 (Wave 10 slice 3; planning `ppf-2-planning`, **PR #100**; impl `ppf-2-private-covariance`, **PR #101** = `7aefd1c`, **CI green run #519** — all 6 jobs; **NO migration**)**: the 19th governed number, `risk.covariance.private`, over PPF-1's pure-private return series. Designed as a fail-closed **SIBLING family**: it reuses the generic `estimate_covariance` kernel **byte-for-byte** (the kernel needed only aligned `(date, Decimal)` series, N≥2 — no daily/factor-return coupling) and the shared `covariance_result` table (`frequency=APPRAISAL`, `run_type=COVARIANCE_PRIVATE`), keeping the public `risk.covariance.sample` path untouched. **The construction**: per PRIVATE segment pair, the equal-weight unbiased (N−1) sample covariance of the pooled pure-private APPRAISAL return series over the segments' COMMON `(period_start, period_end]` intervals (the N most-recent common periods — the OD-P3-0-L rule carried to appraisal grain); block-diagonal treatment of Ω_pp against the public Σ is disclosed as an **APPROXIMATION, not orthogonal-by-construction** (PPF-1's promoted proxy weights are a strict subset of the OLS fit, so a pure-private series retains a small dropped-factor public component — the Part-5 verifier's forced honesty correction). **Step 1 closed a latent shared-table read bug BEFORE any private row could exist**: `latest_covariances`/`resolve_covariance` (+, after the 4-finder fold, the by-run-id `list_covariances`) gained a `run_type` filter — behavior-identical for all pre-PPF-2 data, and now self-defending rather than merely caller-ordered. The private binder mirrors this symmetrically (`latest_private_covariances`/`resolve_private_covariance`/`list_private_covariances`, all `run_type=COVARIANCE_PRIVATE`-filtered) — proven to leak NEITHER direction, and the VaR/active-risk consumers are doubly guarded (a `run_type`-gated run-resolve PLUS a DAILY-frequency backstop). The APPRAISAL-aware adjudicator (the binder's one substantive departure from the public twin) groups pinned `PURE_PRIVATE_RETURN` components by segment, re-keys on `period_end` after asserting the FULL interval vector identical, and asserts each paired factor pin is PRIVATE/APPRAISAL. **4-finder review: ZERO HIGH**, 2 MED + 4 LOW folded: **MED** — `verify_snapshot`'s except-tuple omitted the new component kind's resolver exception, so a gone pinned pure-private row would 500 instead of report drift (fixed + a drift-not-500 test); the methodology doc's validation section claimed a hand-computed reference, the consume-existing path, and TR-09 invariance as tested when none of the three shipped (the consume branch was GENUINELY untested — all suite calls used build-in-request) — fixed by reconciling the doc to inherited-vs-new legs AND adding the missing consume==build + `verify_snapshot` round-trip tests. **LOW** — the by-run-id list reads hardened with the same `run_type` self-defense; the data-derived "no adequacy floor beyond N≥2" property recorded as an explicit v1 scope-out; a doc miscount fixed. Demo stage 12 (`ppf2_stage12.py`) runs **ONE** Ω_pp over the two PPF-1-seeded segments (PE-HARBOR-IV, PC-BRIDGEWATER-II) — **ZERO new book data**, the declared window computed dynamically from the live common-interval intersection (**N=5**, 2024-12-31→2026-03-31, matching the Part-5 verifier's prediction exactly); files ONE INITIAL AWC (MEDIUM/MEDIUM, the pure-private substrate tier). Counts moved **21/36/103 → 22/37/104** (1 new code + 1 record + 1 run — a covariance run IS the matrix identity), PG-proven including `stage9zzz` sorting correctly last. Rule-7 reads (`/risk/private-covariances[/latest,/{id}]`, reusing `CovarianceRowOut`) + the OpenAPI/FE contract regenerated with zero new schema. Battery: `make check` 1826 passed; `make fe-check` green (build); `make gen-api-check` clean; the full demo PG chain in CI order green; `test_covariance_pg.py` green (incl. the step-1 regression under the new join); `alembic check` clean (**genuinely NO migration**); `pip-audit` clean. | Wave-10 slice 3, arc slice 2 of 3 complete — **the private-risk variance substrate is real.** Next = **PPF-3 planning** (the unified number `√(x'Σx + p'Ω_pp·p + residual)` — owns the appraisal→daily frequency conversion + the VaR-gate widening to admit the private block; may close Wave 10 or roll to Wave 11 on appetite). |
| 2026-07-23 | **PPF-3 DONE — the unified public+private parametric VaR, §2.1 unification arc slice 3 of 3, THE CAPSTONE (Wave 10 slice 3; planning `ppf-3-planning`, **PR #103**; impl `ppf-3-unified-var`, **PR #104** = `633e855`, **CI green** — all 6 jobs; migration `0048_var_private_variance`)**: the 20th governed number, `risk.var.parametric_unified` = `√(x'Σx + p'(Ω_pp/d_t)·p + residual_over_non-private-members)`, its OWN binder `run_var_unified` (the plain/total `run_var` byte-untouched save one per-family predicate fold). **Two pre-ratification verifiers REFUTED the naive additive three-leg formula as a variance DOUBLE-COUNT**: PA-4's residual σ_e² is the WHOLE non-public residual (`Var(PurePrivate)+Var(AssetSpecific)`), already inside Ω_pp — the corrected design is a **REPARTITION**, not an assembly: a pure-private-segment member's non-public variance lives in the Ω_pp block ALONE, and leg 3 (the residual) sums ONLY non-private-segment members. The demo scope FLIPPED to a two-fund book (OQ-3=B, the verifier's own recommendation) so the cross-segment covariance — the only genuinely new quantity — is exercised non-tautologically. **4-finder review folded 1 HIGH**: three finders INDEPENDENTLY CONVERGED on the same gap — the repartition was enforced on the BUILD path (`build_var_unified_snapshot` excludes private members' residual pins) but NOT on the CONSUME path (`run_var_unified(snapshot_id=...)` re-derived both legs without cross-checking disjointness, so a hand-minted snapshot could pin one instrument in both legs and re-open the ratify-blocking double-count) — fixed by refusing the leg-2/leg-3 overlap at adjudication + a hand-mint regression test. **+ 2 MED**: a held-segment Ω_pp OFF-DIAGONAL completeness gate (an absent cross-fund pair was silently imputed as zero co-movement, understating the number — parity added with the public leg's full-pairwise coverage); a **disclosure-honesty overstatement** carried into a REGISTERED model assumption AND the governed INITIAL validation scope_note — "the value over total-VaR is *exactly* the Ω_pp off-diagonal" — when `σ²_unified − σ²_total` also carries a non-zero DIAGONAL re-estimation (Ω_pp's sample variance, ÷(N−1), vs PA-4's OLS residual, ÷(N−k) — two different estimators), corrected across the assumption/dossier/methodology/demo, with the demo's headline test now ALSO asserting the off-diagonal is non-zero (not just that the numbers differ). **+ LOWs**: two kernel "guardrail" tests were mislabeled (they hold under the naive formula too — the naive-vs-repartition difference is in binder ASSEMBLY, not the kernel; relabeled as conditional identities, with the real anti-double-count enforcement now the build+consume tests); the tier re-rated MEDIUM→**HIGH** materiality (the flagship public+private VaR, on SR 26-2 exposure/purpose grounds matching its sibling VaR flagships — the platform's own rubric rejects "non-enforcing in the demo" as a materiality ground). **Everything else the finders attacked HELD**: the build-path repartition, the quadratic form + 1/d_t de-scale + unit consistency across all three legs, cross-tenant Ω_pp provenance (both paths re-resolve under `acting_tenant` — the SSO-1/API-1 lesson), migration 0048 (FK 55 chars, CHECK already satisfied, `var_result_content` excludes the two new columns — no false BT-1 drift), the API/FE exhaustive decimal contract, Rule-7 scope (no new read function — `latest_var_for_portfolio` resolves the new `metric_type` for free), the three-way predicate isolation (a unified/total/plain snapshot each refused by the other two binders, both directions), and every hard governance invariant (no BYPASSRLS, `audit/service.py` untouched, no new permission/role/mint, immutable model identity). Demo stage 13 (`DEMO-UNIFIED-PPF3`) seeds ONE new portfolio holding the two EXISTING private funds (PE-HARBOR-IV, PC-BRIDGEWATER-II — **ZERO new instruments/segments/Ω_pp**), runs the union public chain (exposure → LOADINGS factor-exposure → the campaign's REUSED window-30 DAILY covariance, its `version_label` fixed so the stage instead extends FX_USD's daily returns 5 honestly-disclosed days to reach the 30-obs overlap) + a PA-4 total-VaR contrast + the unified run. Live numbers: σ_unified=85.758098 vs σ_total=85.685795 (leg 2's correlated 167.75 replacing leg 3's independent 155.35); unified `residual_variance` EXACTLY 0 (both funds pure-private) vs total's 155.35 — the double-count fix, proven end-to-end on real PG. Files ONE INITIAL AWC (HIGH/HIGH). Counts moved **22/37/104 → 23/38/109** (1 new code + 1 record + the 5-run chain), PG-proven on a clean schema alongside PPF-2's count pin (`stage9zzzz` sorting correctly last). Battery: `make check` 1849 passed; `make fe-check` green; `make gen-api-check` clean; the affected-family PG battery green on a clean schema; `alembic check` clean; `pip-audit` clean (one pre-existing dev-only `pytest` advisory, not shipped). | Wave-10 slice 3, arc slice 3 of 3 complete — **the §2.1 destination the whole platform thesis pointed at is now shipped.** WAVE 10 FULLY COMPLETE (all three slices). Next = **the Wave 10 close review** (the mandatory Part-4 rule-2 re-baseline). |
| 2026-07-23 | **WAVE 10 CLOSED + RATIFIED; WAVE 11 RATIFIED (OQ-W10C-1/2/3/5 approved + OQ-W10C-4 fork A "OPERATIONALIZE"), on `wave-10-close` (`1150cce`, awaiting USER merge).** The mandatory Part-4 rule-2 close after Wave 10 shipped **API-1b → FE-3b → the §2.1 arc (PPF-1 → PPF-2 → PPF-3)** — the wave that shipped **the platform's DECLARED DESTINATION**, the unified public+private VaR (the 20th governed number). Method: **four cross-cutting close auditors** over the wave diff `11967b8..4070438` (integration/composition, security/doctrine — a dedicated lens for the FE-3b auth flow + the PPF private-asset math, doc/register/stamp, completeness/destination) on top of each slice's own shipped 4-finder review; Opus-only, proportionate (ultracode off); **every material finding independently re-verified by hand** (the frozen-file diff, the migration chain, the counts, `gen-api-check`, `make check`, the stamp state). **Verdict: ZERO shipped-CODE defects — the SIXTH consecutive clean close on the code axis.** All prior folds present + correct at HEAD; every hard invariant held (frozen audit byte-untouched + never imported; no BYPASSRLS/new-mint; immutable model identity; the load-bearing private-family isolation across all shared-table reads; cross-tenant Ω_pp provenance re-resolved before every hard-FK write); gates green (`make check` **1849/1850** / full-PG **~2231** / `fe-check` / `gen-api-check` clean / `alembic` single linear head `0045→0048`); counts **23/38/109**. **The one HIGH was doc/PROCESS, fixed at the close: the closure-stamp class recurred a SIXTH consecutive close** — PPF-3's own closeout swept `current_state`/`roadmap` but left its decision record at "RATIFIED", never CLOSED — **AND the OQ-W9C-5 CI teeth were structurally BLIND to it on two counts** (they matched only the literal "DRAFT for ratification"; the done-set keyed on the leading `✅ **DONE**` shape, so the arc row's inline `✅ **PPF-N**` marks left all three arc slices out of scope entirely). Folded: `check_docs.py` BROADENED (fires on any DONE-but-not-CLOSED Status cell; recognizes the arc-row inline shape) + a regression test proving both; the broadened gate then surfaced a genuine HISTORICAL miss too — **CC-2** (a Wave-8 record also left at "RATIFIED") — both PPF-3 and CC-2 now stamped CLOSED (the only two records it flags, no cascade). **Meta-lesson (OQ-W10C-5): a mechanical gate only ends a recurring class if it is UNIT-TESTED against the ACTUAL failure forms** (OQ-W9C-5's teeth were tested against a synthetic DRAFT row but never the arc shape or the RATIFIED stamp — the two forms the recurrence took). **Destination check (rule 6b): the §2.1 destination is GENUINELY SHIPPED as a defensible v1 — not a toy** (the pre-implementation double-count catch proves the team refused the naive number; the Ω_pp OFF-DIAGONAL — private books co-moving in their pure-private risk — IS the thesis payload). Three honest v2 seams, **leverage the one load-bearing gap** (a levered private book's real risk not yet answered; needs new captured data). Honest note: the demo headline delta is tiny (~0.08% — the off-diagonal of a 2×2 low-correlation two-single-member book) — the ENGINE is proven, a compelling NUMBER is not yet. **Outward benchmark: the frontier has CROSSED from methodology into OPERATIONS** — 20 governed numbers, but nothing runs on a cadence / enforces a limit / carries a remediation to term; that operational half is now the largest distance-to-frontier. **WAVE 11 RATIFIED (Part 2.14, OQ-W10C-4 fork A): SCH-1 the first scheduler → LIM-1 limits/breach workflow → MG-2 remediation lifecycle** — make the governed engine RUN, not just compute; the PPF-3 v2 seams (leverage-led) deferred as the fork-B alternative. **NEXT = SCH-1 planning** (the first scheduler, a genuinely-new architectural primitive — Tier-3 methodology/architecture sign-off at planning). | The mandatory Part-4 rule-2 wave-close re-baseline; user ratified 2026-07-23 (OQ-W10C-1/2/3/5 approved + fork A "operationalize"). |
| 2026-07-23 | **SCH-1 DONE — the FIRST scheduler, Wave-11 slice 1 "operationalize" (planning+impl `sch-1-planning`, **PR #108** = merge `96965cf`, **CI green** — all 6 jobs; migration `0049_scheduling`)**: the platform's first genuinely-new architectural primitive beyond request/response reuse — cadenced governed background execution. Produces **NO new governed number** (counts UNCHANGED **23/38/109**): it makes the *existing* numbers PRODUCIBLE on a cadence. New entities: an EV `schedule` (ENT-061, config header, entity-versioned-in-place) + an IA append-only `scheduled_run` (ENT-062, one row per fired grid tick, `uq(schedule_id, scheduled_for)` idempotency backstop); an R-07 permission mint (`schedule.manage`/`.view`) + an R-07 audit-taxonomy mint (a new `SCHEDULE` category, EVT-260, genuinely EMITTED — a control-plane lifecycle, not a CALC run). **THE CRUX (OQ-1, Tier-3 doctrine): the census proved there is NO tenant registry and the app role CANNOT enumerate tenants, so cross-tenant dispatch is inherently an AD-015 ops-role question. Two pre-ratification verifiers REFUTED the draft's framing of Option A (in-app ops-role cross-tenant read) as a settled AD-015 reuse — it is a genuine 3-part doctrine EXPANSION (a first-ever ops grant on non-audit tables + inverting the test-enforced no-grant invariant + a cross-tenant read of governed-run provenance). User RATIFIED OQ-1=B (infra-driven per-tenant dispatch): the deploy layer invokes the worker once per tenant, the whole poll+dispatch runs inside ONE tenant's NON-BYPASSRLS `run_in_tenant` context — the app stays 100% non-BYPASSRLS, the standing `test_ops_role_has_no_grant_on_*` isolation invariant is PRESERVED (SCH-1 ships its own such test for the two scheduling tables).** The verifiers ALSO caught TWO blocking cadence defects (a backfill that would manufacture a fraudulent daily series of identical re-pins wearing different date stamps; a pause/resume backfill storm) — BOTH folded by ONE fix: **the no-backfill / coalesce-to-`current_tick` model** (a fresh number is inherently as-of-now, so each poll fires at most the CURRENT grid tick and leaves missed grid points as honest ledger gaps; the FRTB daily series accrues PROSPECTIVELY). **INV-SCH-1** (ratified): `scheduled_for` = the pure `current_tick(anchor, interval, now)` grid value, NEVER a wall clock — load-bearing for both idempotency and the pure-function test firewall. **4-finder impl review: ZERO HIGH, 4 MED + LOWs folded** — the doctrine finder found ZERO HIGH/MED (frozen `audit/service.py` empty diff, app 100% non-BYPASSRLS, RLS/append-only/grants correct); the substantive findings converged on the worker poll-loop error handling: (1) an over-broad `except IntegrityError` masked non-dedup constraint failures → could hot-loop a failing tick forever with no FAILED evidence — fixed with a constraint-name check (`_is_tick_dedup`, only `uq_scheduled_run_schedule_tick` is the benign dedup); (2) the failure-recording path caught only `IntegrityError` → a non-integrity error there could escape the loop, abort `run_in_tenant`'s single commit, and unwind every sibling schedule's work — fixed by making `_record_failed` FULLY catch-all (the starvation guarantee); (3) INV-SCH-1 unenforced at the write boundary → `_assert_current_tick` self-enforces `tick == current_tick`; (4) the advertised resilience behaviors were untested → 4 new tests (FAILED record-and-continue, starvation isolation, post-create-FAILED, PG unique-constraint dedup). **A build-time bug was caught by the recon + fixed pre-review** (dispatch resolved a plain EXPOSURE run when `run_var` needs a FACTOR_EXPOSURE run). **Post-merge CI fix (PR #108 re-land):** PR #107 had merged ONLY the planning DRAFT (`a382b93`); the first push also had a CI-collection bug — the dispatch test imported the VaR-chain seed as `from tests.test_var`, which resolves locally (cwd on `sys.path`) but NOT under CI's repo-root `python -m pytest` (`ModuleNotFoundError: No module named 'tests'`) — switched to the repo's `from test_var` sibling-module convention; the full implementation re-landed via **PR #108** (CI green). **Lessons: (1) a fail-closed invariant that spans a per-item isolation loop must classify the SPECIFIC benign exception (constraint name), never swallow a whole exception class, and its failure-recording path must itself be un-escapable — the isolation guarantee is only as strong as its narrowest `except`; (2) a cross-test-module import MUST use the bare sibling form (`from test_x import`), never `from tests.test_x import` (only resolves when cwd is on `sys.path`, which CI's repo-root invocation is not).** Battery at close: `make check` green (ruff, mypy 222 files, docs-check, secret-scan); pytest **1453 passed / 388 skipped** (SQLite); scheduler PG battery (RLS/append-only-trigger/ops-no-grant/unique-tick) + affected chain green on a clean schema; `alembic check` clean (single linear head `0049`). | Wave-11 slice 1 complete — **the platform's operational half is opening: a governed number can now RUN on a cadence, auditably.** Next = **LIM-1 planning** (the governed limit + breach workflow over the numbers SCH-1 keeps fresh). |
| 2026-07-23 | **MG-2 RATIFIED + Wave-11 re-scoped to 4 slices (OQ-1=B split).** MG-2's ratified scope was "carry a breach to term AND the formal `LIMIT.APPROVE` maker-checker gate." At the ratification gate the scope was re-opened as a Part-4-rule-3 STRUCTURAL finding — MG-2 is genuinely TWO distinct governance primitives: (a) the breach remediation lifecycle (the eroding register item, REQ-BRC-001/002/003, earliest overdue 2027-07-19), and (b) the `LIMIT.APPROVE` DRAFT→ACTIVE maker-checker gate, which is the platform's FIRST person-level (approver≠author) SoD AND changes the limit EVALUATION path (a DRAFT limit is not polled). User ratified **OQ-1=B**: ship (a) as **MG-2**, split (b) to a new focused slice **MG-3** so the first person-level SoD gets its own adversarial review. Also ratified OQ-2=A (IA append-only `breach_action` transition log, current state recency-derived — forced by `breach` being append-only + the VW-1 precedent), OQ-3=A (the 5-transition machine `DETECTED→ASSIGNED→RESPONDED→REVIEWED→CLOSED`+`ESCALATED`; mint `breach.respond`[1L]/`breach.review`[2L]; person-level SoD reviewer/closer ≠ any prior 1L responder; closure requires evidence), OQ-4=A (auto-escalation with teeth — a THIRD SCH-1 tick phase auto-escalates on a passed `response_due = assign + SLA(limit_kind)` deadline; fixes the toothless-overdue gap VW-1/MG-1 left open). **The pre-ratification verifier pass RAN and found THREE BLOCKING concurrency holes — all folded BEFORE ratification (the ES-1 lesson honored): B-1 nondeterministic recency state (the `uuid4` id is not monotonic; the SYSTEM auto-ESCALATE creates a same-`occurred_at` tie VW-1 never had) → a DB-assigned monotonic `seq` identity column ordered `seq DESC`; B-2 escalate-idempotency TOCTOU + an unresolved "unique-index OR pre-check" → ONE constraint-backed epoch `uq_breach_escalation(breach_id, response_due)` (the deadline IS the `scheduled_for` grid analog) + a per-breach `SELECT … FOR UPDATE` lock; B-3 person-level SoD compared only the LATEST 1L responder (X-responds→reject→Y-responds→X-reviews slips through) → compare vs the SET of ALL prior 1L responders.** Plus hardening folds (concurrent CLOSED-resurrection closed by the same `FOR UPDATE` lock; the recency read tenant-filtered; `breach.status` deprecated-in-place as a stale second source of truth; phase 3 mirrors the phase-2 single-layer isolation shape; the escalation count surfaced through `_work`/`main()`; the role-partition non-co-grant invariant). NOT a governed number (counts UNCHANGED 23/38/109); migration `0051`; audit codes genesis-reserved → REALIZED. Planning draft + verifier pass on `mg-2-planning` (`d1c1556`). | Ratification gate 2026-07-23; user chose OQ-1=B (lifecycle now) + OQ-4=A (auto-escalate). **NEXT = MG-2 implementation** under the standing autonomy grant, then MG-3, then the Wave-11 close review. |
| 2026-07-23 | **MG-2 DONE — the breach remediation lifecycle, Wave-11 slice 3 "operationalize" (planning+impl `mg-2-planning`, **PR #113** = merge `aa6503f`, **CI green** — all 6 checks; migration `0051_breach_action`)**: carry a breach to term — the FIRST governance-workflow-with-teeth. Produces **NO new governed number** (counts UNCHANGED **23/38/109** — a breach action is a control-plane event binding no snapshot/run/model). **REALIZES the genesis-reserved ENT-034 `breach_action`** (IA TRUE append-only) as the **DEP-WFL** state machine `DETECTED → ASSIGNED → RESPONDED(1L) → REVIEWED(2L) → CLOSED` + an orthogonal `ESCALATED`; the OPERATIVE current state is the recency-derived latest action `to_state` by a per-breach monotonic `seq` (assigned `max+1` under a parent-breach `FOR UPDATE` lock; `breach.status` deprecated-in-place). Auto-escalation is a **THIRD phase of the SCH-1 per-tenant operational tick** (`poll_tenant_breach_deadlines`, a plain `response_due < now` check — NOT `current_tick`), escalate-once-per-epoch via `uq_breach_escalation(breach_id, epoch_seq)`. R-07 permission mint `breach.respond` (1L) / `breach.review` (2L), **NEVER co-granted** (the SOD-03 partition) + the platform's **FIRST person-level SoD** (reviewer/closer ≠ any prior 1L responder — REQ-BRC-002/SOD-02). ACTIVATES the genesis-reserved `BREACH.ASSIGN`/`.1L_RESPONSE`/`.2L_REVIEW`/`.ESCALATE`/`.CLOSE` audit codes (REALIZE, no taxonomy mint). **Pre-ratification verifier folded 3 BLOCKING concurrency holes BEFORE ratify** (nondeterministic recency → monotonic `seq`; escalate TOCTOU → the epoch key + FOR UPDATE lock; SoD latest-responder bypass → the all-responders set). **4-finder review: 1 HIGH + 4 MED, all folded** — HIGH (F1): a breach could reach CLOSED with ZERO 1L response, making the person-level SoD vacuous (a single 2L assign→review→close) → `review_breach` now REFUSES with no prior 1L response (REQ-BRC-002). MED: the escalation epoch was keyed on the DERIVED `response_due` (two epochs computing the same due-time silently suppress a real re-escalation) → re-keyed to the governing ASSIGN's monotonic `epoch_seq` (F1); no `ORDER BY` in the overdue scan → a lock-ordering deadlock under concurrent same-tenant ticks → `ORDER BY Breach.id` (F3); the FOR UPDATE lock was exercised by ZERO concurrency tests → a two-connection `FOR UPDATE NOWAIT` proof (F3); the audit payload shape + ESCALATED-review branches untested → payload/severity/actor_type + ESCALATED-review + reject-recovery-to-close tests (F4). **Lesson: a state machine layered on an append-only log needs (1) a DB-monotonic ordering key — not a random uuid or a caller timestamp — for deterministic recency, (2) a per-item write lock for linearizable transitions under concurrent ticks, and (3) an idempotency epoch keyed on a monotonic id, never a derived value; and a "requires a prior step" control must be enforced explicitly, since an empty set-check passes vacuously.** Battery at close: `make check` green (**1915 passed**); local PG (0051 up/down smoke, `alembic check` zero-drift, breach+limit families green incl. the FOR UPDATE lock proof + the append-only trigger + cross-tenant + uq-escalation + ops-no-grant); closure-docs gate green. **`LIMIT.APPROVE` split to MG-3.** | Wave-11 slice 3 complete — **the platform can now carry a breach to term with teeth: owned, responded, independently reviewed, auto-escalated, closed with evidence.** Next = **MG-3 planning** (the `LIMIT.APPROVE` maker-checker gate), then the mandatory Wave-11 close review. |
| 2026-07-23 | **LIM-1 DONE — the FIRST governed write-side workflow: the governed LIMIT + BREACH control, Wave-11 slice 2 "operationalize" (planning+impl `lim-1-planning`, **PR #111** = merge `218afc9`, **CI green** — all 6 jobs; migration `0050_limit_breach`)**: the platform computed 20 governed numbers but had NO limit to measure them against — it could not answer the first question a CRO asks ("are we within appetite?"). LIM-1 realizes the DETECTION half. Produces **NO new governed number** (counts UNCHANGED **23/38/109** — a breach is a control-plane predicate over an already-governed `calculation_run`, binding no new snapshot/run/model). **REALIZES the genesis-reserved entities** ENT-031 `limit_definition` (EV — threshold + `(target_run_type, metric_type, benchmark_id?)` metric-selector + exact `scope_portfolio_id` + `breach_direction` + `limit_kind`, entity-versioned-in-place, `LIMIT.DEFINE`/`.CHANGE` audited) + ENT-033 `breach` (IA TRUE append-only — SELF-DESCRIBING: echoes the metric IDENTITY AND the comparison arithmetic at detection, FKs the evaluated `calculation_run_id`, `uq(limit_definition_id, calculation_run_id)` idempotency) and ACTIVATES the genesis-reserved `LIMIT.*` (EVT-060) + `BREACH.*` (EVT-070) audit decades (first emission — the SCH-1/SCHEDULE realization precedent). R-07 permission mint `limit.manage` (**2L-maker SoD** — `risk_manager_2l` + `platform_admin` ONLY, author≠limit-setter, DIVERGES from SCH-1's 1L `schedule.manage`) / `limit.view` / `breach.view`. **All FOUR Fable foundation-audit demands discharged: (1)** breach discovery via `calculation_run`/`calc/reads.py` (so MANUAL runs are limit-checked too), breach records FK `calculation_run_id`, idempotency = the IA `(limit_id, run_id)` unique key; **(2)** breach evaluation is a PHASE of the ONE per-tenant operational tick (`run_operational_tick_for_tenant` = `poll_tenant_schedules` then `poll_tenant_breaches` under one `run_in_tenant` terminal commit — NO second entrypoint, NO `Schedule` row); **(3)** per-tenant assembly inside the tick (no cross-tenant sweep — OQ-1=B preserved, app 100% non-BYPASSRLS); **(4)** grid-frozen-for-life ratified standing. **Ratified OQ-LIM-1-1…4:** OQ-1=A (EV header + self-describing echo; threshold history = the `LIMIT.CHANGE` trail, NOT FR-bitemporal) / OQ-2=A (exact `scope_portfolio_id`, portfolio-scoped; firm rollup v2) / OQ-3=A (v1 metric set VAR/`var_value` + ACTIVE_RISK/`te_value`, hardcoded `_METRIC_MAP` + fail-closed `column_unit == threshold_unit` assert + REQUIRED `benchmark_id` for ACTIVE_RISK) / OQ-4=A (DETECT + record + read; the formal `LIMIT.APPROVE` maker-checker gate AND the breach ASSIGN/1L/2L/ESCALATE/CLOSE lifecycle DEFERRED to MG-2). **Pre-ratification verifier pass folded TWO blocking correctness holes BEFORE ratification:** the breach predicate's `LTE/GTE` naming was an inversion trap (a mis-code fires on every healthy book, silent on real breaches) → replaced by `breach_direction` naming the BREACH condition directly (ABOVE ⟺ `observed > threshold` strict; BELOW ⟺ `observed < threshold`), a two-line total function tested against its table from birth; and active-risk was under-specified without a benchmark (`active_risk_result.benchmark_id` NOT-NULL, a portfolio computes TE vs MANY benchmarks) → `benchmark_id` added to the selector + echo, resolver called with `benchmark_id=limit.benchmark_id`. **4-finder impl review: ZERO HIGH, 6 MED folded** — the two load-bearing safety themes: **the silent-green fail-open** (`limit_health` now RECOMPUTES its verdict from the source of truth via `_breaches(observed, …)`, NEVER inferred from the presence of a breach row; the breach poll uses a constraint-SPECIFIC dedup `_is_breach_dedup` — only `uq_breach_limit_run` is benign — plus `logging` on any non-dedup/eval failure, never a blanket swallow) and **the precision overflow** (`threshold_value`/`observed_value` widened `(28,6)→PreciseDecimal(34,12)`: 22 integer digits cover `var_value`'s `(28,6)` range with no loss, 12 scale holds the `te_value` fraction — a currency-vs-fraction unit-agnostic store); plus the **P3-5 cross-tenant FK guard** restored on the NEW `create_limit` write path (`assert_portfolio_in_tenant` + a raw-SQL tenant-filtered `benchmark` existence check — kept OFF the marketdata import fence — + a duplicate-code pre-check) and coverage folds (active-risk `te_value`/FRACTION e2e + a floor-direction e2e). **Two conformance-test catches during impl:** a local `_json_safe` dup (the `test_no_binder_redefines_json_safe_locally` gate) → canonical `irp_shared.audit.payload.json_safe`; a `from irp_shared.marketdata.benchmark` import (the `test_nothing_imports_marketdata` fence) → the raw-SQL benchmark check. **Lesson: a health/status surface for a fail-open control must RECOMPUTE its verdict from the source of truth, never infer it from the presence of an evidence row — and its echo/store columns must carry at least the integer-range of every source they copy.** Battery at close: `make check` green; full suite exit-0 on a FRESH PG schema (the shared-DB-accumulation artifact rule); scheduler+limit PG battery (RLS/append-only-trigger/ops-no-grant/uq double-detect/forged-tenant) green; `alembic check` clean (single linear head `0050`, chain `…0048→0049→0050`). | Wave-11 slice 2 complete — **the platform can now enforce a limit and record a breach, auditably, over the numbers SCH-1 keeps fresh.** Next = **MG-2 planning** (Wave-11 slice 3, FINAL: carry a breach / a validation finding to term — the breach lifecycle state machine + the formal `LIMIT.APPROVE` maker-checker gate, riding the SCH-1 cadence; then the mandatory Wave-11 close review). |
