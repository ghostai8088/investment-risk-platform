# DS-2 Decision Record — estimated-α + Okunev-White desmoothing (Wave-7 slice 4, the LAST)

| | |
|---|---|
| **Status** | **CLOSED 2026-07-19 — SHIPPED AS RATIFIED + REVIEW-FOLDED.** Planning merged via **PR #63** (`0f199aa`); implementation `ds-2-impl` merged via **PR #64** = `5120baa`, **CI green (run #381 — id added at the Wave-7 close)**. Migration head `0041_es_historical` → **`0042_desmoothing_estimated_alpha`** (alpha nullable + `alpha_stderr` + the summary-only CHECK; destructive RLS-safe downgrade proven under a non-superuser owner-member role). **NO new governed number / model code / permission / ENT** — DS-2 adds two declared ESTIMATOR CONVENTIONS (`AR1_ESTIMATED`, `OKUNEV_WHITE_ITERATIVE`) as versions of `perf.return.desmoothed_geltner`, discharging the declared-α rider; GLM MA(k) stays the named v2 (determinism obstacle recorded). Ratified 2026-07-18 (OQ-DS-2-1…7, user: "Approve all"). 4-finder review (Part 6): **ZERO HIGH, ZERO MEDIUM** — adversarial 2 LOW + 4 INFO, numeric 0 defects + 3 caveats, doctrine 0/0 + 2 LOW, scope-fence 9/9 PASS; all folds landed (`b8141d6`); + the missing-CI-step catch at the pre-push battery (Part 5.5 item 5). Battery at tip: `make check` **1588** / fresh-schema local PG (the 0042 estimation suite + ALL SIX demo suites green in CI order) / `alembic check` clean / downgrade-base + re-upgrade smoke clean over the stage-6-extended tenant. **Wave-7 slice 4 (the LAST) COMPLETE — next = the Wave-7 close review.** Sized **M**. |
| **Grounding** | Census (one thorough Explore pass, file:line-grounded) + the primary-source fetch DISCHARGED FIRST (the roadmap fetch-at-planning MUST — the ledger's "record-cited at PA-1, never extraction-verified" flag). The declared-α Geltner family (PA-1, ENT-056, `perf.return.desmoothed_geltner`): kernel `desmoothing_kernel.py` (`desmooth_geltner` :60-75, `r_t = (r_a,t − (1−α)·r_a,t−1)/α`, domain `0<α≤1` :65, α=1 identity boundary :14-15; `observed_returns` :44-57 from marks, ≥2 marks, strictly positive; prec-50 + 12dp HALF_UP). Binder `desmoothing_service.py`: `run_desmoothed_return` :220-423, `PURPOSE_DESMOOTHING_INPUT` pins `COMPONENT_KIND_VALUATION` marks, `_MIN_MARKS = 4` (:85, OD-PA-1-H), declared-α parse-back `declared_desmoothing_alpha` (`perf/bootstrap.py:325-340`, `alpha=` prefix :282, pattern :286, domain re-check :338). Rows: n−2 `DESMOOTHED_PERIOD` + ONE `DESMOOTHING_SUMMARY` (`metric_value`=desmoothed stdev, `observed_stdev`, `n_periods`); **`alpha` is a NOT-NULL scalar echo on every row; NO stderr/CI column exists** (the band needs a column). Registrar `register_desmoothed_return_model` :343-423 (caller-suppliable `version_label` — the MF-1 `v1-alpha1` precedent; per-label conflict discipline :414-422). **The declared-α rider verbatim** (`bootstrap.py:312-313`): "alpha is DECLARED, not estimated in-run - an offline mis-estimated alpha propagates directly into every desmoothed value"; the single-lag rider :309-311 names GLM MA(q) + Okunev-White as the recorded v2s; the referent's v2 register `desmoothing_geltner_v1.md:93-100` ("an α confidence band is a v2 candidate"). **Downstream:** PA-3 is the SOLE governed consumer of `DESMOOTHED_PERIOD` rows and reads ONLY metric_type/period grain/`metric_value` (`proxy_weight_service.py:151-155/:340`); `alpha` is pinned as provenance (`serialize.py:460`) but never read into the OLS — **an estimated-α/OW output series flows through PA-3 byte-unchanged**. **No closable validation condition exists for the declared-α rider** (unlike FL-1's `_CURRENCY_CONDITION` closure hook): the desmoothing versions carry use-before-validation EXCEPTIONs (campaign α=0.4 `v1`; MF-1 α=1 `v1-alpha1`), not AWCs-with-conditions — so there is NO TRIGGERED re-validation to file (the HG-1 OQ-5 false-ceremony bar; the lifecycle-turn pattern does NOT apply here, honestly recorded). The gate pattern to mirror is RS-1's `declared_proxy_weight_parameters` (`risk/bootstrap.py:1897-1947`): optional-with-default convention, **ambiguity refused** (>1 row never collapses into the grandfather), stray-literal refusal; perf uses the shared `model.assumptions` helpers directly. Demo series: campaign 7 marks→5 periods; HG-1 8→6; MF-1 12→10 — **5-11 periods ⇒ SE(ρ̂₁) ≈ 1/√n ≈ 0.30-0.45**, so the α̂ band on existing series is honestly enormous (a feature for the thesis, disclosed; the stage-6 demo seeds a LONGER series). **NO new permission/ENT/model code/governed number** (two new declared VERSIONS of the existing family — the RS-1 shape verbatim); `audit/service.py` FROZEN. |
| **Mandate** | Roadmap Part 2.10 slice 4 (`delivery_roadmap.md:166`), sized S/M in the ledger, confirmed **M** by the ratified two-estimator scope + the migration. "GLM MA(q) / Okunev-White: the platform starts ESTIMATING the smoothing it corrects (the 1991→2003/4 fidelity gap named at the close); remediates the declared-α rider; the α confidence band rides. Fetch GLM 2004 + OW 2003 to paragraph at planning." Both fetches DISCHARGED (Part 2). The LAST Wave-7 slice — the wave close review follows. |

## Part 1 — Decisions at a glance (OD-DS-2-A…G)

| OD | Decision |
|---|---|
| **A — `AR1_ESTIMATED`: α̂ = 1 − ρ̂₁ computed IN-RUN from the pinned observed series, with the Bartlett band; fail-closed on a non-smoothing signal; the SAME Geltner inversion** | The estimated convention runs the IDENTICAL Geltner AR(1) inversion with α computed in-run instead of declared: **ρ̂₁** = the lag-1 sample autocorrelation of the observed return series `r_a,t` (built from the pinned marks exactly as v1), under the DECLARED estimator convention — mean-centered, **T-denominator (Box-Jenkins) form**: ρ̂₁ = Σ_{t=1..n−1}(x_t−x̄)(x_{t+1}−x̄) / Σ_{t=1..n}(x_t−x̄)², |ρ̂₁| ≤ 1 by Cauchy-Schwarz; **α̂ = 1 − ρ̂₁**, quantized ONCE to the 12dp column scale and echoed in the EXISTING `alpha` column (the echo = what the run actually used, semantics preserved). **Fail-closed domain (no silent clamp):** ρ̂₁ ≤ 0 ⇒ pre-create 422 ("no positive first-order autocorrelation — nothing to desmooth; the declared-α version remains available"); ρ̂₁ = 1 exactly (α̂ = 0, the explosive boundary) ⇒ 422; a constant observed series (zero denominator) ⇒ 422. Together these guarantee α̂ ∈ (0,1) — inside the kernel's domain by construction. **The band (the ratified rider):** `alpha_stderr` = SE(ρ̂₁) under the **Bartlett white-noise convention** ≈ 1/√n (declared as the band convention literal; the delta method gives SE(α̂) = SE(ρ̂₁) since |dα/dρ| = 1), persisted on the SUMMARY row in a NEW nullable column (OD-D); the CI multiplier is the reader's (the referent states the construction) — no z is declared or applied in-run. **The verifier's direction correction (executed MC):** at lag 1 the white-noise band is CONSERVATIVE under AR(1) — it OVERSTATES SE(ρ̂₁) (empirical SD 0.046 at φ=0.8 vs the 1/√n 0.071; the exact-AR1 band √((1−φ²)/n) is NARROWER and is the named v2). The genuinely-unstated error is the **small-n DOWNWARD BIAS of ρ̂₁** (Kendall 1954 / Marriott-Pope 1954, ≈ −(1+4φ)/n), which biases **α̂ UPWARD** — at n=15/φ=0.6 the executed MC gives E[α̂] ≈ 0.58 vs the true 0.40 — REGISTERED as a first-class limitation, never corrected in-run (a bias-corrected estimator is a named v2). The ρ̂₁ = 1 exclusion is a DEFENSIVE assert, not a live edge (equality forces the constant series, already refused; and ρ̂₁ ≤ cos(π/(n+1)) < 1 — verifier-proved dead, labeled per the MG-1 dead-guard lesson); the α̂ ∈ (0,1) statement is pre-quantization (a 12dp-floored α̂ = 0 needs n ≳ 10⁶ observations — unreachable, asserted defensively). **A declared `min_periods` floor** (registrar-stamped, structural floor ≥ 6 observed returns; the PA-3 declared-floor precedent) gates the estimation pre-create — ρ̂₁ on fewer points is noise, refused not disclaimed. Fully reproducible from the pinned marks alone (deterministic closed form — no optimizer). |
| **B — `OKUNEV_WHITE_ITERATIVE`: the closed-form higher-order filter with a DECLARED max order m and a FIXED deterministic pass sequence; the derived lag-i algebra; fail-closed edges** | The OW convention transforms the observed series by ONE pass per order i = 1..m (m = the DECLARED `ow_max_order`, registrar-stamped, small-int gate 1..4): pass i computes the CURRENT series' autocorrelations ρ_i and ρ_2i (the same T-denominator convention as OD-A) and filters **r*_t = (r_t − c_i·r_{t−i}) / (1 − c_i)** with **c_i the root of ρ_i·c² − (1+ρ_2i)·c + ρ_i = 0 chosen so |c_i| ≤ 1** — the root that zeroes the transformed series' i-th autocorrelation. **The lag-index question is SETTLED BY DERIVATION** (the planning verifier's obligation, discharged in Part 2): the quadratic's dependence on (ρ_i, ρ_2i) is exactly what a LAG-i filter produces (Cov(r_t − c·r_{t−i}, r_{t−i} − c·r_{t−2i}) = γ_i(1+c²) − c(γ_0+γ_2i)), so the filter lag is **t−i**, and the vendor reproduction's rendered `t−1` is identified as a transcription artifact — recorded at its honest grade. **Deterministic by construction:** the pass sequence is FIXED (one pass per order, ascending, never repeat-until-tolerance — OW's iterate-until-near-zero variant is recorded as a DISCLOSED simplification: a later pass perturbs earlier orders slightly, stated, not hidden; the repeat-until variant is a named v2). **Fail-closed edges (the verifier-completed set):** discriminant (1+ρ_2i)² − 4ρ_i² < 0 ⇒ 422 (inadmissible autocorrelation structure — PSD-reachable, a live guard); **c_i ≥ 1 ⇒ 422** (not equality-only: near disc≈0 the computed root can land an ulp above 1 and flip the filter's sign — the post-selection assert is |c_i| ≤ 1); ρ_i = 0 exactly ⇒ c_i = 0 and the pass is the identity (the algebraic limit, applied deterministically — the pass count stays fixed); **the STRUCTURAL length-vs-order floor (the verifier's catch):** pre-create refuse unless n_observed ≥ m(m+1)/2 + 2 (each pass i drops its first i values — the CUMULATIVE loss is Σi = m(m+1)/2, and TWO values must survive for the (n−1) summary stdev) AND each pass's current length exceeds 2i (else ρ̂_2i is an EMPTY-SUM artifact = 0 and the identity pass would fire on no evidence — exactly the silent skip OD-B forbids). **ρ_i < 0 is ADMISSIBLE and deliberate (verifier-executed):** both roots are negative, the '−' branch is still the |c| ≤ 1 root (Vieta: the roots are reciprocals — exactly one admissible root exists whenever disc > 0, both signs), (1−c_i) > 1 so no sign flip, and the pass WHITENS negative autocorrelation — the asymmetry vs OD-A's ρ̂₁ ≤ 0 refusal is INTENTIONAL and recorded: AR1_ESTIMATED refuses because α̂ would leave the Geltner domain; OW's objective IS whitening, both signs. Each pass loses the first i observations' filtered values (the recursion seed, the v1 no-imputation treatment extended; the model docstring's n−2 row-count comment gains a dated amendment — OW rows number n−1−m(m+1)/2). **The OW rows carry `alpha` = NULL** (the convention has no single α — OD-D relaxes the column) and the c_i coefficients are NOT persisted: they are fully reproducible from the pinned marks + the declared identity (the ES-HS-1 reproducible-from-pin standard); the summary row persists as in v1 (desmoothed stdev / observed stdev / n_periods), `alpha_stderr` NULL. Geltner one-pass with c = ρ̂₁ is the m=1 special case **under exact AR(1) structure only** (ρ_2 = ρ_1² factors the quadratic as (ρ₁c−1)(c−ρ₁); on SAMPLE data ρ̂₂ ≠ ρ̂₁² so OW m=1 ≠ AR1_ESTIMATED — verifier-measured; the coherence statement carries the qualifier and is never test-asserted on realistic fixtures). |
| **C — The convention gate + registrars: `estimator_convention` net-new to the desmoothing family; the RS-1 gate pattern VERBATIM (absent ⇒ DECLARED grandfather; ambiguity + stray literals refused); two fixed-label registrars** | `declared_desmoothing_parameters` ADAPTS RS-1's `declared_proxy_weight_parameters` (`risk/bootstrap.py:1897-1947`) into `perf/bootstrap.py`: **ABSENT `estimator_convention` (zero rows) ⇒ the implicit `DECLARED` grandfather** (the existing `alpha=` identity, byte-preserved — every shipped version parses exactly as today); **>1 convention row ⇒ fail-closed** `WrongModelVersionError` (ambiguity never collapses into the grandfather — the RS-1 adversarial HIGH, folded from birth here); per-convention companions strictly validated with **stray-literal refusal** (an `alpha=` on an AR1_ESTIMATED/OW version is a lying identity ⇒ refused; a `min_periods=` on DECLARED/OW refused; an `ow_max_order=` on DECLARED/AR1_ESTIMATED refused). Conventions: `DECLARED` (implicit; companion `alpha=`), `AR1_ESTIMATED` (companions `min_periods=`, `band_convention=BARTLETT_WHITE_NOISE` — the band convention is registrar-stamped IDENTITY so a future exact-AR1 band is a NEW version, never silent drift), `OKUNEV_WHITE_ITERATIVE` (companion `ow_max_order=`). Two new registrars, fixed default labels `v2-ar1-estimated` / `v2-okunev-white` (caller-suppliable label, the house shape); conventions + companions REGISTRAR-STAMPED, never caller-suppliable from the generic /models path; same-label-different-declaration ⇒ 409. The service dispatches on the parsed convention via a registry map (the `_HS_FAMILIES`/RS-1 shape — never a try/except chain): DECLARED/AR1_ESTIMATED/OW all run through `run_desmoothed_return` (ONE binder — the estimators differ in how α/the series transform is derived, not in the run shape; the ES-1 same-binder precedent). |
| **D — Migration `0042_desmoothing_estimated_alpha`: `alpha` → nullable + ADD nullable `alpha_stderr`; the 0028/0041 destructive RLS-safe downgrade sandwich + the non-superuser downgrade test; the TWO pin-serializer landmines handled** | The ONE migration: on `desmoothed_return_result` (ENT-056) **(i)** `alpha` Numeric(20,12) NOT NULL → **nullable** (OW rows carry NULL — no single α exists; the 0028 nullability-relaxation precedent) and **(ii)** ADD **`alpha_stderr`** Numeric(20,12) nullable (the AR1_ESTIMATED band, summary row only; the 0038 additive-nullable precedent). **Downgrade** = tighten `alpha` back to NOT NULL ⇒ the OW rows (alpha IS NULL) are unrepresentable ⇒ **destructive DELETE with the 0028 6-statement RLS sandwich verbatim** (append-only trigger + FORCE RLS transactionally disabled; delete BEFORE the NOT-NULL re-tighten; ENABLE + FORCE both restored) + drop `alpha_stderr`; proven by a **non-superuser owner-via-membership downgrade-path PG test** driving the real `downgrade()` (the 0041 mechanics verbatim — MigrationContext(target_metadata), assert-then-restore, ordered before the demo-seed steps that follow in the shared CI DB). **A DB-level CHECK carries the 0028 precedent forward (the verifier's U7):** 0042 also adds `ck_desmoothed_result_stderr_summary_only` — `alpha_stderr IS NULL OR metric_type = 'DESMOOTHING_SUMMARY'` — so the summary-only invariant stays DB-enforced, not binder-discipline-only (the reason 0028 shipped its CHECK). **The None-tolerance landmines, all THREE handled (the verifier found the third):** (i) `alpha_stderr` is **EXCLUDED from `desmoothed_return_content`** (adding a key falsifies every historical pin — the 0038/0040 false-drift lesson, test-pinned); (ii) the EXISTING `alpha` pin key becomes **None-tolerant** for OW rows (the 0028 None-tolerance precedent: existing rows pin byte-identically — alpha non-null everywhere today; only NEW OW rows pin `alpha: null`; the key SET is unchanged, drift-tested both directions); **(iii) the perf READ endpoint (the verifier's R3):** `api/perf.py:743` formats `alpha` un-guarded (`f"{row.alpha:f}"`) and `DesmoothedReturnRowOut.alpha` is non-optional — an OW read would 500; the row-out schema gains alpha None-tolerance (Optional + the guard every sibling nullable column already has), an ADJUDICATED read-surface change recorded in OD-G. SQLite stays blind to the nullability change (`create_all` — the recorded ORM-visible/PG-only asymmetry does NOT arise here because nullability IS ORM-declared: the ORM column becomes `nullable=True`, consistent across both tiers). |
| **E — Demo stage 6: a NEW long-series private instrument; both new conventions run live; INITIAL AWC dossiers for the two versions; NO TRIGGERED ceremony (recorded honestly)** | Stage 6 (`demo/ds2_stage6.py` + `scripts/run_demo_ds2.py`; suites `test_demo_stage6_ds2*` — the alpha-sort filename discipline, after stage 5, before the downgrade smoke) extends the living tenant: seeds **`PE-HARBORVIEW-IX`**, a NEW private-equity instrument with **16 quarterly marks** (TD-1-realistic, generated deterministically with GENUINE built-in AR(1) smoothing at a known α_true = 0.4 — and the demo claim is **ESTIMATION WITH HONEST UNCERTAINTY, deliberately NOT recovery** (the verifier's R1 correction: at n=15 observed the small-n ρ̂₁ bias pushes E[α̂] ≈ 0.58 vs the true 0.40 — a systematic +0.18 ≈ 0.7× the band, so a "recovery" claim would ride on seed luck); the dossier states the deterministic draw's α̂, α_true, the SE ≈ 0.26 band, AND the expected upward bias — the "stated honestly" thesis live in its sharpest form: the platform showing its OWN estimator's finite-sample weakness on appraisal-length series); registers the two new versions (`v2-ar1-estimated` with min_periods=8; `v2-okunev-white` with ow_max_order=2); runs BOTH conventions on the new series + the DECLARED v1 on the same series (the three-way comparison in one tenant: declared vs estimated vs higher-order); files **INITIAL AWC dossiers for the two new versions** (the RS-1 new-version-INITIAL precedent; evidence = their own COMPLETED runs; finding keys from their NEW registered limitation rows, fail-loud). **NO TRIGGERED re-validation is filed and the record says WHY:** no existing validation condition names the declared-α rider (the census fact — the desmoothing versions carry EXCEPTIONs, not conditions-bearing AWCs), so there is nothing to close-by-supersession; forcing one would be the false ceremony HG-1's OQ-5 bars. The rider's remediation is recorded where it actually lives: the limitation-row rewords (OD-F) + the INITIAL dossiers. Additive, single-commit, refuse-not-skip on the stage's own footprint (the AR1_ESTIMATED version probe). |
| **F — Constants + docs: the two new dossier sets; the family limitation rewords (declared→REALIZED, surviving clauses preserved); the dated pa_1 partial discharge; the new referent at honest citation grades** | New limitation rows under the HG-1 fence (keys unique per searched set; zero stale tokens): AR1_ESTIMATED — the **sampling-error row** (SE(ρ̂₁) ≈ 1/√n; on short appraisal series the band is wide and the estimate is honest-but-weak; `min_periods` is the declared floor, length the lever), the **white-noise-band row** (the Bartlett 1/√n band is CONSERVATIVE — it overstates SE(ρ̂₁) under AR(1) at lag 1; the narrower exact-AR1 band √((1−φ²)/n) is a named v2), the **small-n-bias row** (ρ̂₁ is biased DOWNWARD ≈ −(1+4φ)/n — Kendall/Marriott-Pope — so α̂ is biased UPWARD on short series; disclosed, never corrected in-run; a bias-corrected estimator is a named v2), the **structure row** (single-lag AR(1) still ASSUMED — estimation does not fix mis-specification; GLM MA(q)/OW address structure); OW — the **fixed-sequence row** (one pass per order, ascending; a later pass perturbs earlier orders slightly — the repeat-until-tolerance variant is a named v2), the **vendor-normalized-transcription row** (the per-pass formula verified by derivation + a vendor reproduction; the SSRN primary is gated — re-verify before any extension), the **series-shortening row** (each order-i pass loses its first i filtered values). **The family rewords (NEW registrations only; existing tenants immutable):** `bootstrap.py:312-313` ("alpha is DECLARED, not estimated in-run") → "the DECLARED convention declares α; the AR1_ESTIMATED convention (DS-2) estimates it in-run with a persisted Bartlett band"; `:309-311` (single-lag) → "…the Okunev-White iterative filter is REALIZED as a declared convention (DS-2); the Getmansky-Lo-Makarov MA(q) profile remains the recorded v2 — its MLE requires constrained numerical optimization, a determinism obstacle this runtime has not yet admitted" (the GLM clause PRESERVED — the RS-1 ES_TOTAL bundling lesson). **Dated pa_1 amendment** (additive, never edit): the v2 register partially discharged — OW REALIZED, estimated-α REALIZED, GLM still open with the determinism note; the referent's "α confidence band is a v2 candidate" line discharged. **NEW referent `05_analytics_methodologies/desmoothing_estimated_v1.md`** carrying Part 2's citations at their honest per-source grades. |
| **G — The scope fence** | **ONE migration (0042)**; **NO new permission / ENT / model code / governed number** (two declared VERSIONS of the existing family — the RS-1 shape); `audit/service.py` FROZEN; the DECLARED path BYTE-PRESERVED (the shipped PA-1 goldens re-run as regressions; the kernel's declared-α path untouched — the estimation happens BEFORE the same inversion; the OW transform is a NEW kernel function, the Geltner function untouched); PA-3 consumption byte-unchanged (it reads metric_value only — census-verified); the `var_result` pin serializer + `METRIC_TYPES` + all risk families byte-untouched; the desmoothed pin serializer changed ONLY by the adjudicated None-tolerance (OD-D, drift-tested); FE zero changes; campaign/multifamily/hg1/stage4/stage5 demo modules byte-untouched (stage 6 additive). API: 2 new registrar endpoints; the run endpoint unchanged (dispatch on the bound version); the desmoothing READ row-out schema gains ONLY the adjudicated alpha None-tolerance + the alpha_stderr echo (OD-D iii — recorded, not silent). 4-finder review: adversarial (the gate + OW edges + the destructive downgrade + the grandfather) · numeric (ρ̂₁/α̂/band + the OW quadratic + pass algebra, exact-rational + fuzz; the recovery demo re-derivation) · doctrine (citations at grade; the rewords; the no-TRIGGERED honesty) · scope-fence (clause-by-clause). |

## Part 2 — External benchmarks and primary sources (roadmap Part 4 rule 6 — fetched 2026-07-18)

**Getmansky-Lo-Makarov 2004 — EXTRACTION-VERIFIED to equation numbers (the ledger's never-extraction-verified flag DISCHARGED).** Getmansky, M., Lo, A.W., Makarov, I., "An econometric model of serial correlation and illiquidity in hedge fund returns," *JFE* 74(3):529–609 (2004); the published PDF openly hosted at MIT (fetched + parsed). The smoothing model: observed `R^o_t = θ₀R_t + θ₁R_{t−1} + … + θ_kR_{t−k}` (Eq. 21), `θ_j ∈ [0,1]` (Eq. 22), `Σθ_j = 1` (Eq. 23), over the single-factor true-return model (Eq. 20a/b). The smoothing index `ξ = Σθ_j² ∈ [0,1]` (Eq. 34, the Herfindahl form; lower = more smoothing). **Estimation (§5.1): MLE of the demeaned observed series (Eq. 47) as an MA(k) with Gaussian innovations (Eqs. 48–50); the likelihood (Eq. 51) is scale-invariant (Eq. 52), identified by the Σθ=1 economic restriction (Eq. 49); evaluated via the Brockwell-Davis innovations algorithm and CONCENTRATED (Eqs. 53–55), minimized "using standard numerical optimization packages" (Matlab, per the text) subject to Σθ=1 AND imposed invertibility; Proposition 3 (Eqs. 56–58) gives the closed-form asymptotic covariance V_θ for k=2.** DISPOSITION: the model + index + Prop-3 band are fully verified; **the estimator requires constrained numerical optimization — the determinism/TR-09 obstacle that keeps GLM a NAMED v2** (a deterministic pure-Decimal optimizer design is the v2's own prerequisite, recorded). Nothing in DS-2 ships GLM math; the verification is banked for the v2.

**Okunev-White 2003 — method identity VERIFIED at PA-1 (OD-PA-1-J); the per-pass formula REPRODUCED VENDOR-NORMALIZED + SETTLED BY DERIVATION 2026-07-18.** Okunev, J. & White, D., "Hedge Fund Risk Factors and Value at Risk of Credit Trading Strategies," SSRN 460641 (Oct 2003); published as Loudon, Okunev & White, *J. Fixed Income* 16(2):46–61 (2006). The primary is GATED; the iterative procedure was reproduced from a technical vendor source (Portfolio Optimizer): per order i, `r*_t = (r_t − c_i·r_{t−i})/(1−c_i)` with `c_i = [(1+ρ_2i) ± √((1+ρ_2i)² − 4ρ_i²)]/(2ρ_i)`, the root with |c_i| ≤ 1. **The reproduction's rendered lag (`t−1`) conflicts with its own quadratic; the ambiguity is SETTLED BY FIRST-PRINCIPLES DERIVATION** (recorded here, the verifier's check): for the lag-i filter, `Cov(r_t − c·r_{t−i}, r_{t−i} − c·r_{t−2i}) = γ_i(1+c²) − c(γ_0 + γ_2i)`, whose zero is exactly the quoted quadratic in (ρ_i, ρ_2i) — so the filter lag is **t−i** and the rendered t−1 is a transcription artifact. Geltner one-pass with c = ρ₁ is the m=1 special case (the vendor source states it; consistent with PA-1's α ≈ 1−ρ₁ convention via α = 1−c). GRADE: formula verified-by-derivation + vendor reproduction; the primary's own equation numbers NOT sighted — re-verify against the primary or a second independent source before any v2 extension (the ES-HS-1 AS-threshold discipline).

**Geltner 1991/1993 — VERIFIED-carried from PA-1** (HIGH confidence, unchanged; the v1 filter + the α ≈ 1−ρ₁ offline convention DS-2 brings in-run). **Bartlett band** — the SE(ρ̂₁) ≈ 1/√n white-noise convention (Bartlett 1946; the Box-Jenkins standard identification band): DECLARED as the band convention (`BARTLETT_WHITE_NOISE`); verifier-executed MC confirms 1/√n at φ=0 and shows the band is CONSERVATIVE (overstating) under AR(1) at lag 1 — the narrower exact-AR1 band √((1−φ²)/n) is the named v2; never presented as an exact CI. **Small-n bias** — ρ̂₁'s downward bias ≈ −(1+4φ)/n (Kendall 1954; Marriott-Pope 1954) ⇒ α̂ biased upward on appraisal-length series (executed: E[α̂] ≈ 0.58 at n=15/α_true=0.4) — a REGISTERED limitation and the reason OD-E's demo claims estimation-with-uncertainty, not recovery.

## Part 3 — Census key facts (the load-bearing subset)

1. The kernel: `desmooth_geltner` (`desmoothing_kernel.py:60-75`), domain `0<α≤1` (:65), α=1 identity (:14-15); `observed_returns` (:44-57); prec-50/12dp; the summary stdev reuses `sample_stdev` (:16-18).
2. The binder: `run_desmoothed_return` (`desmoothing_service.py:220-423`); `_MIN_MARKS = 4` (:85); declared-α parse-back quantized once (:263 — the echo-drift lesson); refusal battery :150-217; magnitude gates :76/:79.
3. Rows: n−2 `DESMOOTHED_PERIOD` + 1 `DESMOOTHING_SUMMARY` (:348-397); **`alpha` NOT-NULL on every row; no stderr/CI column** — OD-D's migration is forced by the schema, not chosen.
4. The declared-α rider: `perf/bootstrap.py:312-313` (verbatim in Grounding); the single-lag rider :309-311; the referent's band-v2 line `desmoothing_geltner_v1.md:99-100`.
5. Registrar: `register_desmoothed_return_model` (:343-423), `alpha=` prefix (:282), pattern (:286), caller-suppliable label (the `v1-alpha1` precedent), per-label conflict (:414-422).
6. Downstream: PA-3 reads metric_type/period/`metric_value` ONLY (`proxy_weight_service.py:151-155/:340`); `alpha` pinned as provenance (`serialize.py:460`), never computed on — the new series flow through byte-unchanged.
7. NO closable condition carries the declared-α rider (census, phrasing verifier-tightened: the desmoothing versions hold EXCEPTION-form AWC records — `outcome=APPROVED_WITH_CONDITIONS` with the generic `EXCEPTION_CONDITIONS`, which DELIBERATELY carries no closure token — campaign :967-976; MF-1 `_file_alpha1_exception` :700-733); the FL-1 `_CURRENCY_CONDITION` closure-hook pattern has no desmoothing analog ⇒ OD-E's no-TRIGGERED call.
8. The gate template: RS-1 `declared_proxy_weight_parameters` (`risk/bootstrap.py:1897-1947`) — ambiguity refused (:1912-1914), stray literals refused, optional-with-default grandfather; perf imports `model.assumptions` directly (no perf-local helpers).
9. Demo series lengths: 7/8/12 marks (5/6/10 periods) — the SE(ρ̂₁) sizing fact; `min_periods` + the 16-mark stage-6 instrument answer it.
10. Version labels in use: `v1` (α=0.4, campaign default), `v1-alpha1` (α=1, MF-1); HG-1/RS-1 reuse them. Stage-6 mints `v2-ar1-estimated` + `v2-okunev-white`.
11. Migration head `0041_es_historical`; the 0028 nullability + destructive-downgrade sandwich and the 0041 non-superuser downgrade-test mechanics are the 0042 templates; the 0038/0040 serializer-key landmine governs `alpha_stderr`'s exclusion.
12. House structure: OD table + OQ ratification + Part 2 benchmarks + Part 3 census + Part 5 readiness + Part 5.5 deviations + Part 6 dispositions.

## Part 4 — Open decisions (OQ-DS-2-1…7) — RATIFIED 2026-07-18 (user: "Approve all")

- **OQ-DS-2-1 — OD-A: the AR1_ESTIMATED convention** (α̂ = 1−ρ̂₁ in-run, T-denominator Box-Jenkins ρ̂₁; fail-closed on ρ̂₁ ≤ 0 / constant series — NO silent clamp (ρ̂₁ = 1 verifier-proved dead, kept as a labeled defensive assert); α̂ ∈ (0,1) pre-quantization by construction; the CONSERVATIVE Bartlett band persisted as `alpha_stderr` + the small-n UPWARD-bias limitation registered (the verifier's R1); declared `min_periods` ≥ 6; the same Geltner inversion; reproducible from pins). *Recommend APPROVE.*
- **OQ-DS-2-2 — OD-B: the OKUNEV_WHITE_ITERATIVE convention** (declared `ow_max_order` 1..4; ONE deterministic pass per order ascending — the repeat-until-tolerance variant a named v2, the perturbation disclosed; the lag-i filter with the '−' quadratic root — **settled by derivation AND execution** (the lag-2-vs-lag-1 decisive test); fail-closed on a negative discriminant / **c_i ≥ 1** / **the length-vs-order structural floor** (n_observed ≥ m(m+1)/2 + 2; per-pass length > 2i — the verifier's R4); ρ_i = 0 ⇒ the identity pass; ρ_i < 0 = deliberate whitening, recorded; alpha = NULL on OW rows; coefficients reproducible-from-pin, not persisted). *Recommend APPROVE.*
- **OQ-DS-2-3 — OD-C: the gate + registrars** (`estimator_convention` net-new to the desmoothing family; the RS-1 pattern verbatim — absent ⇒ DECLARED grandfather byte-preserved, ambiguity + stray literals refused from birth; `band_convention` as registrar-stamped identity; registry-map dispatch; ONE binder). *Recommend APPROVE.*
- **OQ-DS-2-4 — OD-D: migration 0042** (alpha → nullable + `alpha_stderr` added; the 0028 destructive downgrade sandwich + the 0041 non-superuser test; `alpha_stderr` EXCLUDED from the pin serializer; the alpha key None-tolerant with byte-identical existing pins — both landmines test-pinned). *Recommend APPROVE.*
- **OQ-DS-2-5 — OD-E: demo stage 6** (the 16-quarterly-mark `PE-HARBORVIEW-IX` with built-in known-α smoothing — the RECOVERY demonstration with the honestly-wide band disclosed; both conventions + the declared baseline run; INITIAL AWCs for the two versions; **NO TRIGGERED — the census-grounded no-closable-condition honesty**, recorded rather than ceremonialized). *Recommend APPROVE.*
- **OQ-DS-2-6 — OD-F: constants + docs** (the two fence-compliant dossier sets; the family rewords preserving the GLM-still-open clause with its determinism note; the dated pa_1 partial discharge; the new referent at honest grades — GLM verified-to-equations-but-v2, OW verified-by-derivation+vendor, Bartlett declared-not-exact). *Recommend APPROVE.*
- **OQ-DS-2-7 — OD-G: the scope fence** (ONE migration; no permission/ENT/code/governed-number mints; DECLARED path + PA-3 + serializers byte-preserved/adjudicated; the 4-finder composition). *Recommend APPROVE.*

## Part 5 — Implementation readiness gate

### Planning verification (the standing pre-ratification pass) — RAN 2026-07-18, 4 REFUTED + 5 UNCERTAIN, ALL FOLDED
The adversarial verifier pass (executed, seeded scripts — not read-only) ran BEFORE ratification. **The core mathematics survived at full strength**: the OW lag-i algebra CONFIRMED by execution (the bilinear expansion over 200 exact-rational draws; the decisive t−i vs t−1 test — a lag-2 filter zeroes lag-2 autocorrelation at −4.6e-07 while a lag-1 filter with the same c leaves +0.60; Vieta reciprocal roots ⇒ the '−' branch is ALWAYS the sole admissible root, both signs of ρ_i; disc<0 is PSD-reachable, a live guard); the OD-A domain bound held over ~20k adversarial series; every census file:line verified; the no-TRIGGERED honesty substantively confirmed. **The folds:**
1. **R1 (the headline — a wrong scientific claim killed):** the "Bartlett understates under AR(1)" direction was BACKWARDS (executed MC: the 1/√n band is CONSERVATIVE/overstating at lag 1; the exact-AR1 band is NARROWER) — and the genuinely-unstated hazard is the **small-n downward bias of ρ̂₁** (≈ −(1+4φ)/n ⇒ α̂ biased UPWARD; E[α̂] ≈ 0.58 at n=15/α_true=0.4). Direction flipped at every site; the bias minted as a first-class limitation row; **the OD-E demo claim REFRAMED from "recovery" to estimation-with-honest-uncertainty** (a recovery claim would have ridden on seed luck against a bias ≈ 0.7× the band).
2. **R2:** the planned OW "zeroing" test could not pass as written (c_i generically irrational; the filtered series' own finite-n autocorrelation is nonzero by truncation — measured −0.10 at n=15) — respecified as the ALGEBRAIC quadratic-residual identity on the original series' sample autocovariances + one perfect-square fixture.
3. **R3:** a THIRD None-tolerance site found — the perf READ endpoint formats `alpha` un-guarded (`api/perf.py:743`; an OW read would 500) — OD-D now handles the trio and OD-G records the read-schema change.
4. **R4:** the OW fail-closed edge set was INCOMPLETE — no length-vs-order floor existed (a 3-observed series at m=2 leaves ZERO values; an empty-sum ρ̂_2i fires a silent identity pass) — the structural floor (n_observed ≥ m(m+1)/2 + 2; per-pass length > 2i) folded into OD-B.
5. **U5–U9 (all folded):** c_i ≥ 1 (not equality-only) refusal; ρ_i < 0 disclosed as deliberate whitening (the OD-A asymmetry recorded); the 0028 DB-CHECK precedent carried (`alpha_stderr` summary-only CHECK added to 0042); the Geltner-m=1 coherence claim qualified to exact-AR(1) structure; the models-docstring row-count amendment + census-phrasing tightening + the dead ρ̂₁=1 guard labeled + the pre-quantization clause.

### The 4-finder review composition (OD-G, ratified shape)
Adversarial (gate/edges/downgrade/grandfather) · numeric (exact-rational: ρ̂₁, α̂, the band, the OW quadratic + per-pass zeroing property, fuzz over series shapes; the stage-6 recovery re-derivation) · doctrine (citation grades incl. the vendor-normalized OW disclosure; the rewords' surviving clauses; the no-TRIGGERED honesty) · scope-fence (clause-by-clause).

## Part 5.5 — Implementation deviations from the ratified plan

1. **The read-surface delta, enumerated exactly (OD-D iii executed + the band echo)** — the
   shipped `DesmoothedReturnRowOut` changes are precisely two: `alpha` → `str | None` with the
   None-guard every sibling nullable column already has (the verifier's R3 third site — an OW
   read no longer 500s), and a NEW `alpha_stderr: str | None` echo (summary row only). Both were
   adjudicated in OD-G at planning ("recorded, not silent"); transcribed here so the read-schema
   accounting is closed. The two new registrar endpoints
   (`POST /perf/models/desmoothed-return-estimated` / `…-okunev-white`) carry request-body
   defaults `min_periods=8` / `ow_max_order=2` with caller-suppliable `version_label` (the house
   shape); runs ride the EXISTING run endpoint unchanged — dispatch is on the bound version.
2. **The ORM nullability relaxation is ORM-declared, deliberately** — `models.py`
   `DesmoothedReturnResult.alpha` → `nullable=True` + the `alpha_stderr` column, so SQLite
   `create_all` and the PG migration agree (OD-D anticipated this: nullability IS ORM-visible —
   the recorded PG-only/ORM-blind asymmetry does not arise). The `alpha` column comment was
   rewritten to the three-convention semantics (declared / in-run α̂ / NULL on OW — a stuffed
   placeholder would be dishonest provenance, the 0028 rationale).
3. **The migration-head re-pin maintenance class (18 + 1 files)** — not enumerated in the
   ratified plan (RS-1, the template slice, carried no migration): 17 suites' deliberate
   head-pin asserts + the var_total chain test moved `0041_es_historical` →
   `0042_desmoothing_estimated_alpha`, and the synthetic-slice next-number tripwire advanced
   `0042*` → `0043*` (DS-2 owns 0042, recorded in its ownership comment). Mechanical, scripted,
   the recurring migration-slice class (first named at ES-HS-1's 17-pin sweep).
4. **The OD-F rewords shipped as line REPLACEMENTS** — the two deleted `perf/bootstrap.py`
   limitation lines are the OD-F-ratified rewords of the single-lag row (OW now REALIZED; the
   GLM MA(q) clause PRESERVED with its determinism note) and the declared-α row (declared →
   convention-of-three with the propagation clause surviving). Transcribed for the scope-fence
   finder's deleted-lines accounting (S row below) — nothing else was removed.
5. **A MISSING CI step caught at the pre-push battery (the P3-7 missing-PG-step class)** — the
   new `test_desmoothing_estimation_pg.py` suite (the 0042 summary-only CHECK under the
   constrained role + the non-superuser owner-via-membership downgrade-body test) existed and
   passed locally but had NO `ci.yml` step — OD-D's "ordered before the demo-seed steps in the
   shared CI DB" was unimplemented, and the 4-finder review did not flag it (the scope-fence
   lens checks the diff against OD-G, not CI coverage of new suites — a known limitation of the
   composition, recorded). Step added beside its family suite (`test_desmoothed_return_pg`),
   before the demo-seed steps, the 0041-precedent placement.
6. **The stage-6 fixture is SEARCHED and tripwired** — OD-E specified 16 quarterly marks at
   α_true=0.4; the shipped deterministic draw was fixture-searched so BOTH conventions are
   admissible (ρ̂₁ = 0.5038 ⇒ α̂ = 0.4962; OW m=2 discriminants positive — a flat-ρ̂₂ cycle
   would refuse, the planning fixture-search lesson). The tripwire is TWO-LAYERED *(wording
   corrected at the Wave-7 close — DS2-2)*: the demo module itself asserts the fixture BAND
   (`_ALPHA_HAT_BAND = (0.45, 0.55)`, `ds2_stage6.py:99`) so a gross drift refuses at seed time,
   and the exact frozen α̂ is pinned at 4dp in the suite (`test_demo_stage6_ds2.py:49`) — a
   sub-band drift is caught by the test layer, not the seeding path.
7. *(Dated addendum, Wave-7 close 2026-07-19 — the close audit's DS2-1.)* **The shipped 0042
   CHECK constraint name is `ck_desmoothed_return_result_stderr_summary_only`** (the naming
   convention expands over the full table name `desmoothed_return_result`) — OD-D and the
   implementation plan recorded it as `ck_desmoothed_result_stderr_summary_only`. Predicate and
   behavior exactly as ratified; name-only deviation, live in the migration, the PG test, and
   `pg_constraint`.

## Part 6 — Implementation review dispositions (the ratified 4-finder composition; ran 2026-07-19)

**Composition:** adversarial + numeric (exact-rational, executed) + doctrine + scope-fence, all
four over the full `ds-2-planning..HEAD` diff. **Totals: ZERO HIGH, ZERO MEDIUM — adversarial
2 LOW + 4 INFO, numeric 0 defects + 3 property caveats, doctrine 0/0 + 2 LOW notes, scope-fence
9/9 PASS. All folds landed in `b8141d6` (no behavior change to the shipped kernels; the A1/A2/A4
code folds are estimator-path-only hardening + dead-code removal).**

| # | Finder | Sev | Finding | Disposition |
|---|---|---|---|---|
| A1 | adversarial | LOW | Estimator-path extreme-mark ORDERING: whether an out-of-envelope mark series 422'd pre-create or committed-FAILED depended on the series' autocorrelation (estimation ran before the magnitude gates) — a nondeterministic-looking contract on the NEW paths | FOLDED: the estimator paths refuse an out-of-envelope mark PRE-CREATE, uniformly; the DECLARED path keeps its shipped committed-FAILED shape (byte-preserved); the cross-convention difference DISCLOSED in-code and here; + test |
| A2 | adversarial | LOW | `_DESMOOTHING_MIN_PERIODS_PATTERN` (`[0-9]{1,3}`) admitted leading zeros — `min_periods=007` and `=7` would alias one identity (the strict no-coercion discipline violated at the declaration surface) | FOLDED: pattern → `[1-9][0-9]{0,2}`; + a gate test pinning the refusal |
| A3 | adversarial | INFO | The α̂∈(0,1) defensive assert's comment was half-right: it claimed only the α̂→0 side unreachable (~1e6 observations) — the α̂→1 side (ρ̂₁ < 5e-13 quantizing to 1.000…) is constructible at ANY n | FOLDED: comment corrected to both boundaries; the refusal is the honest fail-closed shape either way |
| A4 | adversarial | INFO | The OW persistence branch re-computed `observed_returns` inside a provably-dead `ArithmeticError` handler (the same pure function had already succeeded pre-create on the same adjudicated marks — the MG-1 dead-guard class) | FOLDED: `ow_observed` captured pre-create alongside `ow_series`; the dead recompute + handler REMOVED; + test |
| A5 | adversarial | INFO | An EXPLICIT `DECLARED` convention row is accepted and behaviorally identical to absence, but no registrar stamps one — undocumented | FOLDED: gate docstring notes it (accepted, identical, never stamped) |
| A6 | adversarial | INFO | The 0042 downgrade's side effects were implied, not stated: AR1_ESTIMATED summary rows SURVIVE (alpha non-null) but lose their band with the dropped column; deleted OW rows leave COMPLETED `calculation_run` heads with zero result rows | FOLDED: disclosed explicitly in the migration docstring (both the 0028 destructive-precedent shape) |
| N1 | numeric | caveat | ZERO defects at max rigor (the ρ̂₁=1 guard PROVED dead via eigenvalue analysis; the c_i≥1 guard belt-and-braces vs data but arithmetically live; every frozen demo number reproduced from pins; the E[α̂]≈0.58 planning MC confirmed at 0.5763). Caveat: at disc = 0 with ρ_i < 0 the OW quadratic's reciprocal DOUBLE root c = −1 arrives — admissible and harmless (denominator 2), but "sole admissible root" is exact only for STRICTLY positive discriminant | FOLDED: kernel docstring + referent phrased to strict positivity with the boundary case named |
| N2 | numeric | caveat | At the per-pass minimum length (2i+1) the ρ̂_2i numerator rests on a SINGLE product — measured and admissible, but maximally noisy | FOLDED: the OW series-shortening limitation row now says so |
| N3 | numeric | caveat | The referent's E[α̂] ≈ 0.58 small-n bias figure is CONDITIONAL on acceptance (ρ̂₁ > 0; ~5% of such draws refuse) — unstated conditioning | FOLDED: the conditioning stated at the figure |
| D1 | doctrine | LOW | The geltner referent's dated amendment called `alpha_stderr` "the band" without the Bartlett caveat the other sites carry | FOLDED: "(an identification convention, not an exact confidence interval)" parenthetical added |
| D2 | doctrine | LOW | Second LOW note; the finder's verdict otherwise fully clean — citations consistent at every site, zero residual "understates" (the R1 direction flip complete), estimation-not-recovery pinned in the demo claims, all amendments additive | ACCEPTED — an observation, no repo change (the single doctrine fold is D1) |
| S | scope-fence | — | 9/9 OD-G clauses PASS, zero violations: DECLARED path byte-preserved (goldens re-run), PA-3 byte-untouched, the pin serializer changed by exactly the ONE adjudicated None-tolerance line, `audit/service.py` FROZEN, FE zero changes, prior demo stages byte-untouched; the deleted-lines + read-surface accounting transcribed into Part 5.5 (items 1 and 4) | — |

**Post-fold verification (the pre-push battery, 2026-07-19):** `make lint` clean (the
`perf/bootstrap.py:565` E501 reflowed); `make check` **1588 passed** + secret scan + docs check;
fresh-schema local PG at head `0042`: `alembic check` clean ("No new upgrade operations
detected"), the 0042 estimation suite (2 passed — the summary-only CHECK under the constrained
role + the non-superuser downgrade body), ALL SIX demo suites green in CI order
(campaign 7 / multifamily 8 / hg1 4 / stage4 4 / stage5 5 / stage6 4), downgrade-base +
re-upgrade smoke clean over the stage-6-extended tenant (head restored to `0042`). Impl merged
as **PR #64** = `5120baa`, CI green.
