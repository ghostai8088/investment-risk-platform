# CC-2 Implementation Plan — the governed commitment-pacing projection (Wave-8 slice 3)

Companion to `cc_2_decision_record.md` (OD-CC-2-A…H). Ten steps, one commit each; every step green under `make check` before the next. The SEVENTEENTH governed number; migration 0045; the NEW `irp_shared/pacing/` peer package; `audit/service.py` FROZEN; the CC-1 capture surfaces byte-untouched (READ-only consumption).

## Step 1 — Entitlement mint (OD-E)
`entitlement/bootstrap.py`: `("pacing.run", …)` + `("pacing.view", …)` with the dated R-07 mint comment (the PM-1 shape). Grants — run: `data_steward`, `risk_analyst_1l`, `platform_admin`; view: `risk_analyst_1l`, `risk_manager_2l`, `data_steward`, **`auditor_3l`** (a governed OUTPUT — the 3L-oversight inclusion precedent), `platform_admin`. Parity test `test_pacing_permissions_grants_as_ratified` (both directions).

## Step 2 — The pacing package: kernel + events + models (OD-A/B/C)
NEW `packages/shared-python/src/irp_shared/pacing/`:
- `pacing_kernel.py`: prec-50 `localcontext` HALF_UP; `project_commitment(params, anchor) -> tuple[PacingPeriod, ...]` — the verified TA recursion, **FUTURE-ONLY (the F1 fold): rows t = current_age+1 .. L, age-indexed RC/RD; QUANTIZE-THEN-ROLL (F6): each period's money quantized 6dp as computed, the next period rolls from the QUANTIZED values so persisted rows satisfy the recursion identities exactly**; anniversary period dates (Feb-29 → Feb-28 rollover, tested); frozen dataclasses `PacingParams` (rc_schedule tuple/L/B/G/Y; rc each ∈ [0,1] canonicalized; cross-validated domains) + `PacingAnchor` (committed, paid_in, recallable_returned, nav_seed, vintage date, currency, **`as_of` — the PIN-derived age anchor, F1**) + `PacingPeriod`; `PacingKernelError(reason=...)` slugs. The Decimal-power determinism note (deterministic-under-fixed-context; the ≤1-ulp spec grade disclosed — L-2).
- `events.py`: `RUN_TYPE_PACING_PROJECTION` + `PACING_PROJECTION_CREATE_EVENT_RESERVED` (EVT-250 reserved, not minted) + `PacingActor`.
- `models.py`: `PacingProjectionResult` (ENT-059; IA + ORM guard; run-bound FK trio + portfolio/instrument FKs; grain UNIQUE (calculation_run_id, period_index); `period_start`/`period_end`; the four money columns PreciseDecimal(28,6) + `currency_code`).
- Aggregator import in `irp_shared/models.py`. Kernel unit tests: an exact hand-derived 3-period golden (integer-friendly params), the bow=1 linearity property, the RD(L)=1 terminal identity, the yield-floor bite, **the MANDATORY grouping-discriminating golden (M-3): `max(Y,(t/L)^B)` vs `max(Y,(t/L))^B` pinned apart at a mid-life divergent point (e.g. Y=0.5, t/L=0.6, B=2.5 — the groupings coincide at t=L, the BT-3 coincidence-hazard class)**, the future-only start-index tests (age 0 ⇒ t=1..L; mid-life ⇒ current_age+1..L; the leap-day vintage), domain refusals, an exact-Fraction cross-check modeling the quantization (or at quantization-exact params).

## Step 3 — Migration 0045 + head-pin sweep
`0045_pacing_projection.py`: the 0031 template (create table + indexes + grain unique + symmetric FORCE RLS + `pacing_projection_result_append_only` trigger + ≤63-char assert; destructive downgrade drops trigger/policy/table). Sweep the **19** head-pin sites 0044→0045; the new `test_pacing.py` carries the 20th + the chain assert; bump the synthetic tripwire 0045→0046. Local PG: fresh upgrade + `alembic check` + downgrade→0044→re-upgrade smoke. ci.yml: the pacing PG-suite step in the per-family block.

## Step 4 — Snapshot: purpose + kinds + content functions + builder (OD-D)
`snapshot/models.py`: `PURPOSE_PACING_INPUT` + membership in `SNAPSHOT_PURPOSES`; `COMPONENT_KIND_COMMITMENT`/`_CAPITAL_CALL`/`_DISTRIBUTION` + membership in `SNAPSHOT_COMPONENT_KINDS`. `snapshot/serialize.py`: `commitment_content` (FR flavor — no valid_to/system_to/created/updated; yes restatement_reason/supersedes_id/record_version), `capital_call_content`/`distribution_content` (IA flavor — full immutable set incl. reverses_id/is_recallable/commitment_version_id), duck-typed; frozen-key-set tests from birth. **`build_pacing_snapshot` homed in `snapshot/service.py`** (the unanimous builder precedent; models-only local imports of `private_capital.models` + `valuation.models` — the F7 fence grant) pinning the current commitment head + ALL events for the pair + **the latest current-head valuation mark for the PAIR (max `valuation_date`, both-axes-open — F2)** if any (reusing `valuation_content` byte-unchanged), and stamping `as_of_valuation_date` in the header (the F1 age anchor); refuses when no current commitment exists.

## Step 5 — Registrar (OD-B)
`pacing/bootstrap.py`: `register_pacing_projection_model(session, *, rc_schedule, fund_life, bow, growth, yield_floor, version_label="v1", code_version, actor)` via the shared model-registrar core; the five `f"{PREFIX}{value}"` assumption rows + `functional_form=TAKAHASHI_ALEXANDER`; `declared_pacing_parameters -> PacingParams` (strict regex per prefix; `require_declared`; ambiguity refused — the RS-1 counts-rows lesson; cross-field: len(rc_schedule) ≤ L etc.); `PACING_ASSUMPTIONS`/`PACING_LIMITATIONS` tuples (deterministic-single-path; ANNUAL v1; the declared-parameter propagation honesty; the uncapped recallable restoration; the NAV-anchor policy; the terminal RD(L)=1; the UNVALIDATED closer) — finding-key-unique wording. Registrar tests incl. the conflict discipline + parse-back round-trip.

## Step 6 — The binder (OD-C/F)
`pacing/service.py`: `run_pacing_projection(session, *, portfolio_id, instrument_id, model_version_id, snapshot_id, acting_tenant, actor, code_version)` — pre-create gates (registered `pacing.commitment_projection` version via `assert_model_version_of`; snapshot purpose + pair match; the pin parse via the adjudication helpers; the anchor coherence refusals: Unfunded(0) ∈ [0, committed]; the funded-without-mark FAIL-CLOSED; **the mark-currency refusal (F3: non-NULL and equal to the chain currency, else 422); PAST-L = a PRE-CREATE refusal (F8 — the house nothing-to-X shape)**); anchor assembly FROM THE PIN ONLY (Σ over ALL call rows regardless of call_type — F5 declared; Σ recallable distributions; the latest pinned PAIR mark; the commitment's currency + date; **`as_of` from the pinned header — F1**); `execute_governed_run` with the compute callback building unwritten `PacingProjectionResult` rows; post-create FAILED reserved for the magnitude envelope only. Reads: `list_pacing_rows(run_id)`, `resolve_pacing_run`, `resolve_pacing_row`, **`list_pacing_projections(*, acting_tenant, portfolio_id=None, instrument_id=None, as_of=None)`** (rule 7 ALL THREE legs — H-1; rows across COMPLETED runs with run `system_from <= as_of` when given; flat rows carrying run + model ids; total ordering run `system_from` DESC, run_id DESC, `period_index` ASC — F9) and **`latest_pacing_projection(*, acting_tenant, portfolio_id, instrument_id, as_of=None)`** (the first latest-resolver = the `as_of`-aware read, ONE code path; the pair resolved by joining through the ENT-059 rows — F12; None when none). SQLite end-to-end tests (the stage-8-shaped fixture; the mid-life-with-mark and new-commitment-no-mark paths; the funded-no-mark refusal; the past-L pre-create refusal).

## Step 7 — API (OD-F)
NEW `apps/backend/src/irp_backend/api/pacing.py`: registrar POST (`model.inventory.register`), run POST (`pacing.run`), GET run+rows / GET row (`pacing.view`), `GET /pacing/projections` (entity filters + `as_of`; silent-empty) + `GET /pacing/projections/latest` (entity + `as_of`; 404 when none; the cross-run-aggregation consumer-error note in both descriptions) — both `pacing.view`; the exact-type error map via the shared `raise_mapped_write`; registered in `main.py`. Endpoint tests: 403 parity (auditor_3l CAN view — the governed-output inclusion asserted), 422/404/409 surfaces, the latest-resolver determinism.

## Step 8 — Demo stage 9 (OD-G)
`demo/cc2_stage9.py` + `scripts/run_demo_cc2.py` + `demo/__init__.py` exports + `test_demo_stage9_cc2.py`/`_pg.py` (alpha-sort after stage8; the stage10 zero-pad caveat in the module docstring): capture the PE-MERIDIAN-X valuation mark (TD-1-realistic, e.g. 11,200,000.00 USD as-of 2026-06-30); register v1 with the declared PE-shaped params (`rc_schedule=0.25,0.333,0.5`, `fund_life=12`, `bow=2.5`, `growth=0.13`, `yield_floor=0` — OUR declared choices, not TA's unrouted examples); build the pacing snapshot; run the projection; file `CC2_PACING_TIER` + `CC2_PACING_INITIAL` (separate constants; finding keys unique). End-state pins: **20 codes / 35 records / 96+ COMPLETED runs** + the projected-series shape assertions (Unfunded(0) = 25M − 10M + 1.2M = 16.2M — the recallable restoration live; NAV(0) = the mark; **the FIRST projected row's `period_index` = current_age+1 (the future-only pin, F1)**; the terminal full distribution at t=L). CI step after stage 8, before the downgrade smoke.

## Step 9 — Docs (OD-H)
`05_analytics_methodologies/pacing_commitment_projection_v1.md` (the referent: the reproduction-concordance basis with per-source grades; equations; precision/determinism notes; the v2 register). Canonical model ENT-059 REALIZED (dated) + the ENT-015/016 consumer note. Backbone/RTM: REQ-PRV-001 — 'computed' + the test leg discharged, **'aggregated' honestly kept OPEN (the portfolio rollup = the named v2)**; REQ-PRV-002 liquidity discharged (per-pair, ANNUAL, stated) — dated (M-6). Taxonomy: the EVT-250 PACING reservation row. SoD: the `pacing.*` mint row. NO roadmap stamp.

## Step 10 — Battery + review + folds
`make check`; fresh-schema local PG at head `0045` (all NINE demo suites in CI order; the pacing PG suite; downgrade-base + re-upgrade smoke). The standard 4-finder review (adversarial / numeric — the kernel goldens re-derived exact-rationally + a parameter-sweep fuzz + the anchor arithmetic / doctrine — the referent's reproduction-grade claims + REQ discharges / scope-fence), folds, Part 5.5/6.
