# PPF-3 Implementation Plan — the unified public+private VaR `√(x'Σx + p'Ω_pp·p + residual)` (Wave-10 slice 3, §2.1 arc slice 3)

Companion to `ppf_3_decision_record.md` (read it first — the ODs + the **verifier folds (Part 5)** + the ratified OQ answers govern). One commit per step; `make check` green at each boundary. **This plan reflects the post-verifier design: PPF-3 is a REPARTITION (leg 3 excludes pure-private-segment members; OD-3-G), NOT a naive assembly.** It assumes the post-verifier recommendations (OQ-1=A new code, OQ-2=A migration, **OQ-3=B two-segment demo book**, OQ-4=A) — adjust if ratification differs. The number moves 19→**20 governed numbers** at the demo step.

## Step sequence

1. **Migration `0048` — the decomposition columns (OQ-2=A; SKIP if ratified B).**
   Add to `var_result`: nullable `private_variance PreciseDecimal(38,20)` (the pure-private block variance) + nullable `private_covariance_run_id GUID` FK → `calculation_run.run_id` (Ω_pp provenance; name the FK explicitly ≤63 chars). **No CHECK change** (a UNIFIED row satisfies `ck_var_result_parametric_not_null` via z_score/sigma/covariance_run_id — verifier-confirmed). Downgrade drops both. `alembic check` clean + downgrade/upgrade smoke. **Exclude BOTH columns from `var_result_content`** (`serialize.py`) — the frozen-key-set test `test_var_result_pin_key_set_is_frozen` will catch an accidental add; a new pin key re-mints every historical var_result (BT-1/BT-2 drift).

2. **Vocab + registrar mints.**
   `events.py`: `METRIC_TYPE_VAR_PARAMETRIC_UNIFIED = "VAR_PARAMETRIC_UNIFIED"` (→ `METRIC_TYPES`); `RISK_VAR_UNIFIED_CREATE_EVENT_RESERVED`. `snapshot/models.py`: `PURPOSE_VAR_UNIFIED_INPUT`. `bootstrap.py`: `VAR_UNIFIED_MODEL_CODE = "risk.var.parametric_unified"`, `VAR_UNIFIED_METHODOLOGY_REF`, `register_var_parametric_unified_model` (mirror `register_var_parametric_total_model`: reuse `declared_var_parameters` + `declared_appraisal_days`). Assumptions = the REPARTITIONED three-leg formula (leg 3 over non-private-segment members) + the √-time-on-desmoothed disposition + block-diagonal + UNLEVERED labeling. Limitations = OD-3-D/E/G (incl. the single-member conflation + the "reduces to total VaR for a lone private asset" coherence note + the Danielsson-Zigrand tail caveat + the residual-ACF-diagnostic-is-v2). Export from `risk/__init__.py`.

3. **The unified kernel (`risk/var_unified_kernel.py`).**
   `private_block_variance(p_by_segment, omega_pp_daily) -> Decimal` — `p'Ω·p = Σ_s Σ_t p_s·p_t·Ω[s,t]` over the held sub-block, Decimal @50-digit, non-finite/negative guard (PSD by PPF-2 construction). `daily_omega(omega_appraisal, appraisal_days, 252, 365)` — each entry `/d_t`. `sigma_unified(factor_var, private_var, residual_var)` — `sqrt(sum)` with the `total<0` guard (generalize `var_total_kernel`). Hand-computed + `numpy` cross-check tests, INCLUDING the two guardrail cases: (a) single private asset ⇒ `sigma_unified ≈ sigma_total` (the reduction); (b) two single-member segments ⇒ `σ²_unified − σ²_total = 2·p_PE·p_PC·Ω[PE,PC]/d_t` (the cross term). Both FAIL under a naive additive formula — they lock in OD-3-G.

4. **The snapshot builder (`build_var_unified_snapshot`) + the predicate.**
   Mirror `build_var_total_snapshot` (FACTOR_EXPOSURE + public COVARIANCE + REGRESSION proxy_mapping + PROXY_WEIGHT) and ADD: the private Ω_pp run's `covariance_result` rows (`COMPONENT_KIND_COVARIANCE`; resolve `run_type=COVARIANCE_PRIVATE`) + the current-head MANUAL `proxy_mapping` memberships (instrument→segment). `VAR_UNIFIED_BINDING_PREDICATE` = a SHORT fresh string, **verified ≤50 chars** (e.g. `v1:unified-pins+private-cov+memberships` = 39) — NEVER a `VAR_TOTAL` suffix (that column is `String(50)` and VAR_TOTAL is already 49). Fail-closed: the Ω_pp run must span every segment the portfolio holds.

5. **The binder + the THREE-WAY predicate refusal (OD-3-F — verifier fold).**
   Add the unified path (a third `_VarFamily` axis: residual-leg ON + private-leg ON, no ES sibling). The private-leg adjudicator (Ω_pp pin shape: PRIVATE/APPRAISAL, statistic COVARIANCE). Resolve the portfolio's held segments from the pinned MANUAL memberships; form `p_s = Σ MV_i` (MV_i from the FACTOR_EXPOSURE pin); select the Ω_pp sub-block; `daily_omega` → `private_block_variance`. **REPARTITION (OD-3-G): leg 3 sums the residual over ONLY proxied instruments NOT in any held pure-private segment** — the private-segment members' residual moves to leg 2. Assemble `sigma_unified`; persist ONE `var_result` (metric_type UNIFIED, `private_variance` + `private_covariance_run_id` if OQ-2=A, `residual_variance` = the repartitioned leg-3 sum, `covariance_run_id` = the public run). **Restructure the family predicate check to a per-family EXACT match** (the `factor_service.py:582` pattern) across ALL var families — closing the plain-binder hole where a UNIFIED snapshot would run as plain. Rule-7 reads: unified rows via the existing var reads filtered on `metric_type=VAR_PARAMETRIC_UNIFIED`.

6. **API reads + OpenAPI regen.** The unified VaR surfaces through `/risk/vars` (metric_type on `VarRowOut`); match the `residual_variance` exposure choice for `private_variance`. Regenerate + `make gen-api-check` clean.

7. **Methodology doc `05_analytics_methodologies/var_parametric_unified_v1.md`.** The repartitioned three-leg formula; the Rule-6 grounding + citations (√-time-on-desmoothed; block-diagonal extra-justified; UNLEVERED; no-supervisory-mandate); the source list; required sections + a methodology-doc-exists test.

8. **Demo stage 13 + docs (OQ-3=B — the two-segment book).**
   Seed a new `DEMO-UNIFIED-PPF3` portfolio holding BOTH existing instruments PE-HARBOR-IV + PC-BRIDGEWATER-II (ZERO new instruments/segments/Ω_pp) with boundary valuations under the new `portfolio_id`; run a fresh public chain over the union factor set {FX_USD, MF_RATES_GOV, MF_CRSPD_IG} (factor-exposure + DAILY covariance + PA-4 total-VaR for the contrast) + the unified run consuming the tenant-wide Ω_pp. Assert `σ_unified ≠ σ_total` by the cross-segment covariance term (the headline). File ONE INITIAL AWC. Counts move **22/37/104 → 23/(38)/(104+chain)** — set the exercising suite's assertion to exactly what the stage completes (+1 code, +1 record, + the new-chain run count). Filename `stage9zzzz` (sorts after `stage9zzz`). Docs: the 20th-governed-number sweep; `risk.var.parametric_unified` in the census; the repartition + block-diagonal + unlevered + tail-caveat limitations; `current_state.md` + `delivery_roadmap.md`. *(If ratified A instead: run on `DEMO-GLOBAL`, +1 run → 23/38/105, and the demo asserts the honest reduction `σ_unified ≈ σ_total` — the cross-segment case then lives only in the e2e test.)*

9. **Gate + review.** `make check` + full-PG affected-family battery (incl. the demo chain + migration 0048 smoke if A) + `make fe-check` + `make gen-api-check` + `alembic check` + pip-audit. Then the 4-finder adversarial review: (1) unified-variance correctness — **the repartition / no-double-count is the load-bearing check** (re-verify legs 2+3 partition; the reduction + cross-term guardrail tests bite); (2) doctrine/security + the three-way EXACT-predicate isolation (attack: run a unified snapshot as plain to drop legs 2+3); (3) read/schema + the 0048 false-drift landmine + the private-run provenance; (4) demo/count integrity + the unlevered/block-diagonal/repartition disclosure honesty. Fold; closeout.

## Effort/model note
Steps 1–2 + 6–7 are the governed-number template with PA-4 reuse. Step 3 (the kernel + the two guardrail tests) is the genuinely-new math — small but load-bearing (it encodes the fix to the verifier's double-count). Step 5's per-family exact-predicate restructure + the repartition filter are the two highest-risk edits (a decomposition number silently losing/duplicating a leg) — the review's dedicated finder. Step 8's demo (OQ-3=B) is the larger seeding.
