# ES-HS-1 Implementation Plan — ES over historical simulation

Executes `es_hs_1_decision_record.md` (OD-ES-HS-1-A…G) on ratification. Every step names its exemplar; nothing here is novel machinery except the tail-mean arithmetic itself.

## Step sequence

**1. Migration `0041_es_historical` (OD-C).**
`migrations/versions/0041_es_historical.py`: revision `0041_es_historical`, down_revision `0040_var_estimate_age`. Upgrade = drop `parametric_not_null` (type_="check") + recreate same short name with `"metric_type IN ('VAR_HISTORICAL', 'ES_HISTORICAL') OR (z_score IS NOT NULL AND sigma IS NOT NULL AND covariance_run_id IS NOT NULL)"`. Downgrade = drop widened CHECK → `DISABLE TRIGGER var_result_append_only` → `DISABLE ROW LEVEL SECURITY` → `DELETE FROM var_result WHERE metric_type = 'ES_HISTORICAL'` → `ENABLE ROW LEVEL SECURITY` → `FORCE ROW LEVEL SECURITY` → `ENABLE TRIGGER var_result_append_only` → recreate the 0028-form CHECK. The ≤63-identifier assert (`0040:43-46` shape); docstring: CHECK-only, no column/serializer key, the pin landmine does not trip, the destructive-delete precedent cite (`0028:51-60`). Update the ORM comment at `models.py:240-241` to name BOTH exempt literals.

**2. The metric constant + the refusal branch (OD-A/OD-D).**
`risk/events.py`: `METRIC_TYPE_ES_HISTORICAL = "ES_HISTORICAL"` beside its siblings, with a comment naming the BT-3 tee; `METRIC_TYPES` BYTE-UNTOUCHED. `risk/var_backtest_service.py`: extend the deliberate-refusal branch (`:282-293` shape) to `METRIC_TYPE_ES_HISTORICAL` with its own message: deliberately not backtestable HERE — the Acerbi-Szekely leg is the named BT-3 candidate (pairing via shared `input_snapshot_id`); backtest the sibling VaR-HS run instead. Test: the refusal fires on a hand-minted ES_HISTORICAL pin (both the message and the non-500 discipline), and the generic unknown-metric refusal still fires on a garbage value.

**3. The kernel (OD-A).**
`risk/var_hs_kernel.py`: extract the shared per-date-P&L accumulation + ascending sort into a private helper consumed by `compute_historical_var` (byte-identical behavior — the shipped HS goldens are the regression) and NEW `compute_historical_es(exposures, return_windows, confidence) -> HsEsEstimate(n_observations, tail_floor_count, es_value)`: a = 1−c and n·a in EXACT Decimal (the `order_statistic_index` discipline — no float anywhere near the selection); m = ⌊n·a⌋ (Decimal to_integral ROUND_FLOOR), w = n·a − m; ES = −(Σ_{i≤m} pnls[i−1] + w·pnls[m]) / (n·a) inside the SAME prec-50 localcontext; ONE HALF_UP quantize to 6dp at the end. **Refusal precedence PINNED (the verifier's catch): the k/confidence validation stays AHEAD of the helper call in BOTH kernel functions** — a doubly-bad input (bad confidence + missing coverage) gets the confidence refusal, and a precedence test pins it (the goldens cannot). **The magnitude mechanics as they actually are (corrected)**: the prec-50 context makes quantize-`InvalidOperation` UNREACHABLE for envelope-legal pins (the recorded 2026-07 fix, `var_hs_kernel.py:89-93`); oversized results route through the binder's EXISTING post-kernel `abs(value) >= 1E22` comparison → gap `magnitude-out-of-range:var_value` → committed FAILED (`var_hs_service.py:357-360`) — no new exception handling is wired. Kernel tests: (a) hand-derived rational goldens (small windows, hand Decimal arithmetic in the test); (b) the numpy float cross-check @1e-9 (sorted-array arithmetic; numpy TEST-ONLY per the standing fence); (c) the fractional weight at BOTH boundaries — n·a fractional (21/0.95 ⇒ 1.05) AND n·a exactly integer (40/0.95 ⇒ 2.0, w=0 — the zero-coefficient boundary term exact-zero in Decimal, index m+1 in range); (d) ES ≥ VaR asserted `>=` at RAW precision, **the raw values re-derived in-test from the fixture** (the kernel returns quantized; the test-(a) hand machinery supplies the raw side), plus ONE labeled tied-boundary fixture exercising raw EQUALITY (tied worst P&Ls); (e) the TCE trap test: assert the kernel result ≠ the mean-of-worst-⌈n·a⌉ on a fractional fixture (direction pinned; the magnitude NEVER test-pinned — fixture-dependent); (f) all-gains windows (negative VaR AND negative ES legal — pure comparisons, no clamping); (g) the committed-FAILED gate driven via an envelope-legal EXTREME pin (expecting a committed FAILED run, never a 500).

**4. The registrar + constants (OD-B/OD-E).**
`risk/bootstrap.py`: `ES_HS_MODEL_CODE = "risk.var.historical_es"`, `ES_HS_VERSION_LABEL = "v1"`, `ES_HS_METHODOLOGY_REF`, `ESTIMATOR_ASSUMPTION_PREFIX = "estimator_convention="`, `ES_HS_ESTIMATOR_CONVENTION = "TAIL_MEAN_ACERBI_TASCHE_P41"`; `ES_HS_ASSUMPTIONS_BASE` + `ES_HS_LIMITATIONS` (the OD-E content: tail-mass honesty at the floor; worst-scenario window bound; specific-risk = 0; equal-weight inheritance; validation_status; the no-backtest row naming BT-3 — each key-unique in the new searched set, zero 'FL-1'); `register_historical_var_es_model` mirroring `register_historical_var_model` (fixed label; the SAME `_hs_window_floor` at registration; conflict-safe 409; stamps confidence/horizon/window + estimator_convention); `declared_es_hs_parameters` mirroring `declared_hs_var_parameters` (strict parse; vocabulary confidence; horizon=1; the floor at bind; the estimator literal exact-match — a generically-minted version with a different or absent estimator_convention refuses). Reword `VAR_HS_LIMITATIONS[4]` (key-free row — both stale clauses per OD-E). NO es_multiplier/z machinery.

**5. The binder dispatch (OD-B).**
`risk/var_hs_service.py`: `_HS_FAMILIES` registry map (`{VAR_HS_MODEL_CODE: es=False, ES_HS_MODEL_CODE: es=True}`) + `_resolve_hs_family` (the `_resolve_var_family` loop shape — first miss re-raises the plain code's `WrongModelVersionError`); `_compute` branches: es=False → the shipped path BYTE-PRESERVED; es=True → `compute_historical_es`, `metric_type=METRIC_TYPE_ES_HISTORICAL`, the same NULL trio, the same failure-reason formats + magnitude-FAILED gate + DEPENDS_ON + ORIGIN sequencing (the scaffold is shared already). The declared-parameter parse dispatches per family (`declared_hs_var_parameters` vs `declared_es_hs_parameters`). Snapshot handling untouched (purpose check + `build_var_hs_snapshot` both-modes, byte-unchanged). **The module docstring is updated** (it currently states the binder "produces the ONE var_result summary row (metric_type='VAR_HISTORICAL')" and asserts the single family — false contracts once ES-HS dispatches here).

**6. The API (OD-B).**
`apps/backend/api/risk.py`: `POST /risk/models/var-historical-es` mirroring the var-historical registrar endpoint (request: confidence/horizon/window; 409/422 mappings identical; the estimator convention registrar-stamped); the run endpoint `POST /risk/vars-historical/runs` needs **NO FUNCTIONAL change** (the binder dispatches on the bound model; the except-tuple already covers the path) — but its DOCSTRING (the OpenAPI description hard-states `metric_type='VAR_HISTORICAL'`) and the `HsVarRunIn` field comment ("a REGISTERED historical-VaR model_version") are UPDATED to name both families (shipping them byte-untouched ships a false API description — the verifier's catch). Endpoint tests: register + run ES-HS end-to-end through HTTP; the 409 same-label conflict; the malformed-declaration 422; an ES-HS run read back through the EXISTING GET family (metric_type ES_HISTORICAL, NULL trio in the payload).

**7. The PG tier (OD-C).**
`tests/test_es_hs_pg.py` (new): (a) the CHECK-compliance leg — insert a real ES_HISTORICAL row (NULL trio) under the widened CHECK: SURVIVES; a non-exempt metric with a NULL: still refuses (the `test_var_total_pg.py:460-489` flavor); (b) **the non-superuser downgrade-path leg, verifier-settled mechanics**: mint a `NOSUPERUSER NOBYPASSRLS` login role GRANTED MEMBERSHIP in the table-owner role (attributes don't inherit through membership; ownership checks pass via `has_privs_of_role` — the role runs the DDL while staying RLS-bound, the exact 0028:56-59 geometry; a DML-grant role dies "must be owner" on the first ALTER, and the alembic CLI reconnects as the superuser — both wrong shapes, recorded); drive the REAL `0041` module's `downgrade()` via an alembic `MigrationContext` bound to a connection opened AS that role; assert the seeded ES_HISTORICAL row ACTUALLY deleted + the narrow CHECK re-added; **restore the widened state before returning** — the whole body in ONE transaction with assert-then-ROLLBACK (PG DDL is transactional), or the `upgrade()` re-run in a finally, and an end-of-module assertion that the widened CHECK is live (the suite's CI step runs BEFORE the stage-4 demo step that seeds ES_HISTORICAL rows into the same DB — an unrestored narrow CHECK is a red CI on the first run); legs ordered state-self-sufficient; (c) the RLS/append-only posture for ES-HS rows (the per-family PG suite shape — and the grant-list lesson: the new suite's constrained `irp_app` fixture grants every table the seam touches). CI step beside `test_var_hs_pg.py`'s.

**8. The demo stage 4 (OD-F).**
`demo/eshs_stage4.py` + `scripts/run_demo_eshs.py` + the stage-4 TierDossier — **a SEPARATE module constant (the `MF1_LOADINGS_TIER` shape, `dossiers.py:392`), NEVER added to `TIER_DOSSIERS`** (the campaign suite derives its exactly-16 pin from that dict itself — the verifier's catch) — materiality HIGH / complexity MEDIUM ⇒ **TIER_1** under the MG-1 matrix. The filing sequence (the MF-1 V8 ordering): register → snapshot (21/0.95 over the campaign substrate — n·a=1.05, the fractional weight LIVE in the flagship demo) → run → **tier-assign via the 2L verb** → file the INITIAL AWC dossier with **`next_review_due` = filing-day + 365** (TIER_1's strict ceiling; an approving outcome refuses without one), finding keys drawn from `ES_HS_LIMITATIONS`, evidence = the stage's own COMPLETED run; **the module carries its OWN `_findings_from_registry` copy** (the multifamily duplication precedent — the fail-loud lookup is not shared machinery). Guards (campaign present; refuse-not-skip on the ES-HS registration probe, minted FIRST); single-commit. Tests: **`tests/test_demo_stage4_eshs.py`** (unit: the fence mirror for the NEW searched set — key resolution ×1, no duplicates, zero 'FL-1') + **`tests/test_demo_stage4_eshs_pg.py`** (end-state: the 18th code, the run, the dossier rows; re-seed idempotence refuse-not-skip) — **the `stage4` filename component is LOAD-BEARING**: local full-PG runs collect alphabetically, and a `test_demo_eshs*` name would run before the hg1/multifamily suites and break the multifamily EXACTLY-17 pin (the ordering discipline's new filename leg; recorded beside the CI ordering comment). CI: the step AFTER stage-3, BEFORE the downgrade smoke, with the ordering comment extended.

**9. Docs (OD-E).**
NEW `05_analytics_methodologies/var_historical_es_v1.md` (the var_parametric_es_v1 template; the rule-6 section carrying the verified-via-reproduction dispositions AT THEIR PER-SOURCE GRADES per Part 2 of the record — incl. the M-P sign-inconsistency disclosure sentence and the Z1/thresholds re-verify-at-BT-3 flags; the TCE divergence stated "order 10% (up to ~14% depending on the tail shape) at n=41", never the bare figure); dated amendment to `var_parametric_es_v1.md` (the :201-203 reservation discharged); dated amendment note in `var_historical_v1.md` (the ES seam realized; its rule-6 FRTB paragraph already points here). Closeout-time (NOT this PR): current_state 14→15, roadmap ledger row, ENT-027 realization #6, RTM/backbone narrowing.

**10. The battery.**
`make check` (SQLite tier — remembering it CANNOT see the CHECK; the 0041 legs live in PG); full local-PG fresh (schema-reset recipe WITH the PUBLIC grant) incl. the four demo suites in CI order + both new PG suites; `alembic check` (exactly the one new revision, no drift); downgrade/upgrade smoke cycled over the stage-4-seeded tenant; the FE suite untouched-green (zero FE changes claimed — the suite run proves it).

## Review composition (OD-G)

4 finders: **adversarial** (the dispatch — can a plain-HS model be made to emit an ES row or vice versa; the refusal branch; the 0041 downgrade under RLS; the registrar conflict/malformed surfaces), **numeric** (independent re-derivation of every kernel golden incl. both n·a boundaries; the TCE-divergence pin; ES≥VaR; the float cross-check), **doctrine** (the new dossier texts vs the fence rules + the citation dispositions vs what the sources actually ground + the VAR_HS_LIMITATIONS[4] reword vs existing-tenant immutability), **scope-fence** (every OD-G clause, executed).

## Verification appendix (the standing planning pass)

The pre-ratification verifier pass RAN before the OQ gate was briefed (4 lenses, 22 findings, ALL folded — the table lives in the record's Part 5 "Planning verification"). The pass changed this plan in five places: the step-3 magnitude mechanics + refusal precedence + raw-assertion mechanics; the step-6 docstring updates; the step-7b role/restore mechanics; the step-8 filenames + TierDossier placement + filing sequence; the step-9 citation grades. The standing lesson holds — it has changed the ratified shape at every slice since ES-1.
