# Wave-7 Close Review — "the mathematics, deepened"

| | |
|---|---|
| **Status** | **DRAFT — pending ratification (OQ-W7C-1…6).** Authored 2026-07-19 at the Wave-7 close; merged-as-draft-then-ratified is the Wave-5/6 pattern. **Mechanics note (critic gap 3, dispositioned):** this review sits on branch `wave-7-close`, cut FROM `ds-2-closeout` — the DS-2 closeout PR is PENDING USER MERGE, so the merge ORDER is `ds-2-closeout` first, then this branch (whose PR diff then shows only the close review + the fix batch). Neither PR carries code beyond one test-comment fix. |
| **Grounding** | Audited 2026-07-19 at `wave-7-close` tip `3ffbe2d` (= the DS-2 closeout stamps; impl merge `5120baa` = PR #64 in history). All eight Wave-7 anchors confirmed ancestors of the tip via `git merge-base --is-ancestor` (HG-1 plan `4c8829f`/impl `8260ea6`; ES-HS-1 plan `7568c49`/impl `dc2a494`; RS-1 plan `8014b51`/impl `9c15658`; DS-2 plan `0f199aa`/impl `5120baa`). **CI re-queried LIVE at this close: runs #339 / #348 / #365 / #381 — all 5/5 success** (the ids backfilled into the four stamps at this close, finding F7). Migration head `0040` → `0041_es_historical` (ES-HS-1) → `0042_desmoothing_estimated_alpha` (DS-2). Recorded battery at the DS-2 tip: `make check` **1588**; fresh-schema local PG green (the 0042 estimation suite + ALL SIX demo suites in CI order); `alembic check` clean; downgrade-base + re-upgrade smoke clean. **Method:** a six-domain audit fan-out (per-slice HG-1/ES-HS-1/RS-1/DS-2, deferral register, closure stamps + status decay + executed counts), every finding adversarially verified by TWO independent refuters before counting (both-refute kills; a split downgrades); two judgment passes (outward benchmark with web-graded external claims; destination/re-baseline) with every load-bearing claim separately attacked (**24 claim-checks: 22 survived, 2 corrected** — both corrections folded below); a completeness critic whose 8 gaps are all dispositioned in-document. **71 agents; 3 findings killed by refuters; every verdict file:line-cited, executed, or web-graded.** |

## Part 1 — Did Wave 7 ship what was ratified?

**Yes — all four slices SHIPPED-AS-RATIFIED, and for the THIRD consecutive close the audit found ZERO shipped-code defects in the wave's own diff.** (The one MED code-behavior finding, ES-HS-1's demo tie-break, is a Wave-7-*era* demo-selection nondeterminism, not a governed-number defect — dispositioned at OQ-W7C-2.) Every other surviving finding is record/doc/process hygiene.

- **HG-1** (12 confirmations, ZERO findings): the promote-day age gate exactly as ratified — always-measured `promotion_age_days` (live: 12 audit rows carry it in the demo tenant), opt-in bound, strict `>` refusal, all THREE unmeasurable shapes fail closed under a bound, the DISTINCT error class with its own exact-type map entry; the 11-row constants sweep live with all four verbatim keys surviving and the per-searched-set conformance fence executed green; stage 3's `PC-BRIDGEWATER-II` chain live in the durable tenant (10 COMPLETED `demo-hg1` runs, the ratified shape) with OQ-5's no-validation-record honored (zero evidence rows cite any `demo-hg1` run); the RS-1-era gate widening verified deliberate, provenanced, and non-breaking (the wrong-purpose probe still refuses).
- **ES-HS-1** (10 confirmations): the Prop-4.1 kernel re-derived BY HAND in exact rational arithmetic (n=21/c=0.95 ⇒ ES = 515/1.05 = 490.476190, byte-equal; the forbidden ceil-count TCE would give 400); the raw-precision ES≥VaR grid + tied-boundary equality tests as promised; the registry-map dispatch with the estimator convention REGISTRAR-STAMPED; the 0041 destructive downgrade + non-superuser test intact; the BT-3 refusal branch naming the tee; no unacknowledged Wave-7 drift on its surfaces.
- **RS-1** (12 confirmations): the s2 decoupling verified AT THE CODE LEVEL (classical `s2` feeds the OLS std-errors; the convention-dispatched `residual_var` reaches `residual_stdev` only); the EB intensity data-driven per-instrument with the N≥3-DISTINCT fail-closed and no shrink-of-shrunk; the convention gate COUNTS rows (0 ⇒ RAW grandfather, >1 ⇒ refused); the age-gate widening recorded as the Part-5.5 deviation with the conservative stalest-member anchor; stage 5's two TRIGGERED closures with the MF-1 frequency caveat restated in the fresh conditions.
- **DS-2** (10 confirmations, fresh-eyes audit of the newest record): both kernels re-verified (the OW quadratic-residual identity; the AR1 T-denominator form); the gate's grandfather/ambiguity/stray-literal battery; migration 0042 with the summary-only CHECK and the serializer None-tolerance both directions test-pinned; the new CI step correctly placed before the demo-seed steps; stage 6's honest-uncertainty claims live; the closeout-stamp counts DB-verified (30 records = 11 EXC + 12 INIT + 7 TRIG; 87 COMPLETED runs).

### The findings — dispositions at OQ-W7C-1/2; refuter-killed claims excluded (3)

| # | Sev | Finding | Disposition |
|---|---|---|---|
| ESHS-1 | MED | **Stage-4 flagship-pair selection is seed-nondeterministic**: `_latest_flagship_hs_row` tie-breaks equal `window_end` rows on `calculation_run_id DESC` — a uuid4 — and the tenant seeds TWO qualifying VAR_HISTORICAL rows at the same window_end (112.042694 five-exposure vs 113.239146 seven-exposure), so the record's "113.239146 both" demo value is selection-dependent (today's live tenant pairs at 112.042694 both; the ES=VaR equality disclosure itself is seed-stable) | **ASSIGNED TO BT-3 BY NAME** (OQ-W7C-2): BT-3's pairing design consumes this exact selection helper — fix the tie-break to a seed-stable time-ordered key there; a dated note on the ES-HS-1 record's demo value rides the BT-3 planning PR. Not fixed at this close (a demo-code change would re-open the just-closed battery for a non-governed surface) |
| RS1-AUD-1 | LOW | The RS-1 record named a `_ResidualEstimator` registry-map object that does not exist — the ratified CONTRACT shipped as the conventions frozenset + per-entry-point fail-closed gates; behavior as ratified, naming deviation unrecorded | FIXED at this close: dated Part 5.5 addendum (item 5) in the RS-1 record |
| RS1-AUD-3 | LOW | DS-2's head re-pin sweep left a stale `# ES-HS-1` comment on the 0042 head assert in `test_var_total.py` | FIXED at this close (`# DS-2`) |
| DS2-1 | LOW | The recorded 0042 CHECK name (`ck_desmoothed_result_…`) ≠ the shipped `ck_desmoothed_return_result_stderr_summary_only` (the naming convention expands the full table name); predicate exact | FIXED at this close: dated Part 5.5 addendum (item 7) in the DS-2 record |
| DS2-2 | LOW | Part 5.5 item 6 overstated the α̂ tripwire's home — the demo module asserts only the (0.45, 0.55) band; the exact 4dp pin lives in the suite | FIXED at this close: item-6 wording corrected (two-layered tripwire) |
| F1 | MED | The CURRENT TRUTH banner contradicted itself on the validation-record count (an RS-1-era "28 records" sentence vs the correct Counts paragraph's 30) — **adjudicated: the true count is 30** (11 EXC + 12 INIT + 7 TRIG, executed SELECT; critic gap 6) | FIXED at this close (the sentence now carries the DB-verified 30 with the stage-5/6 deltas) |
| F2 | MED | The canonical data model carried ZERO RS-1/DS-2 amendments — ENT-056/ENT-057/ENT-027 still claimed the shipped conventions "are recorded v2s" and 0042's alpha-nullability + `alpha_stderr` schema change was unrecorded | FIXED at this close: three dated amendments (the ES-HS-1 6th-realization note is the exemplar) |
| F3 | MED | RTM REQ-MKT-001 still said "Residual shrinkage/EWMA … remain deferred" — RS-1 discharged exactly that clause; zero RS-1/DS-2 tokens in either requirements doc | FIXED at this close: dated in-row discharge clause (the BT-2/ES-HS-1 pattern); trading-day counts named as the surviving open clause |
| F4 | MED | `var_parametric_total_v2.md` still claimed shrinkage/EWMA "are v2s" while asserting content-identity with limitation rows the registrar no longer matches; `proxy_weight_regression_v1.md` likewise un-amended | FIXED at this close: dated RS-1 amendments to both referents, with the mirror claim scoped to the BT-2-era rows as history |
| F5 | LOW | Backbone REQ-PRV-005 status was four slices stale (PA-3/FL-1 discharges unrecorded) while the RTM declares the backbone canonical for status | FIXED at this close: dated refresh naming capital-calls as the one open clause |
| F6 | LOW | The RS-1 planning stamp cited the branch tip `fdfbfa9` in the merge-sha position; PR #60's merge is `8014b51` | FIXED at this close (record + both roadmap rows; the tip kept, labeled) |
| F7 | LOW | All four Wave-7 closure stamps dropped the FL-1-adopted CI-run-id citation (the claims themselves re-verified TRUE by live re-query) | FIXED at this close: runs #339/#348/#365/#381 backfilled; the convention restated in the closure checklist (OQ-W7C-3) |
| F8 | LOW | `CLAUDE.md` still said "origin is SSH" — false since 2026-07-09; the auto-loaded first-read pointer would walk a fresh session into the blocked port | FIXED at this close |
| REG-1 | MED→split | See Part 2 (the estimate-staleness clause ledger) | Dispositioned at OQ-W7C-4 |
| REG-4 | LOW | See Part 2 (the α=1-detour instantiation count) | Register row updated in Part 2's carried register |
| REG-5 | LOW | The banner still named `rs_1_decision_record.md` as the latest record inside the post-DS-2 CURRENT TRUTH block | FIXED at this close |

**Killed by the refuters (recorded per the Wave-6 practice):** RS1-AUD-2 (the "-eb" endpoint-path deviation IS recorded — both refuters located it), REG-2 (the roadmap Part-3 bullet's framing is historical text, and the BT-3 debt IS durably registered), REG-3 (the banner's rider phrasing is not a claim the condition fired). The verification layer's kill rate (3 of 19) is the reason these did not reach this document.

## Part 2 — The deferral register, reconciled (the Wave-6 44-item register + the four Wave-7 Part-6 sets; scope per critic gap 5: every named item re-verified or explicitly carried)

**PAID in Wave 7**: the estimate-staleness residual's promotion clause (HG-1's age gate — the clause ledger below); the FL-1 registrar-constant behavioral wording (HG-1's C3); the private-on-multi-family demo gap (stage 3); ES-over-HS (the Wave-6 nearest-gap #1 — CONSUMED); the OD-E cap-lifted residual/desmoothing pair (nearest-gap #2 — CONSUMED, discharging the MF-1 contractual re-tee's sequencing promise); the raw-sample-σ_e rider (RS-1's 2 TRIGGERED closures — the SECOND lifecycle turn); the declared-α rider (DS-2 — closed by REMEDIATION + limitation rewords, deliberately NOT by TRIGGERED ceremony; the census-proved no-closable-condition honesty).

**The one ADVERSE finding, dispositioned (REG-1, MED downgraded on a refuter split):** the Wave-5 staleness re-open enumerated THREE facts; after HG-1 the ledger is: **clause 1 (promotion path has no age check) — PAID** (`proxy_weight_service.py:564-663`); **clause 3 (nothing expires a `proxy_mapping`) — OD-B, OPEN trigger-based** (HG-1's recorded census + named trigger); **clause 2 (every non-total family binds promoted weights ungated) — never given its own register row.** One refuter confirmed the gap, one refuted on clause arithmetic; the honest resolution is to DISPOSITION it rather than argue the wording: **clause 2 is hereby an ACCEPTED RESIDUAL absorbed into the OD-B row** — the ratified mitigation is promote-time gating + the always-audited age (HG-1 OD-A's recorded promote-vs-bind composition); bind-time age gating for non-total families is option (ii) of OD-B's deferred design space and revives with OD-B's trigger (OQ-W7C-4).

**Register hygiene from this audit**: the "raw-return pin kind" row's count is stale — RS-1's stage 5 added two more α=1-detour instantiations (`rs1_stage5.py` MF-EQ-C fresh + MF-EQ-B re-estimation), so the carried row reads **"trigger unfired, now ≥5 instantiations — and DS-2's AR1_ESTIMATED does NOT retire the detour** (an identity read on a daily public series is exactly what the estimated convention refuses on ρ̂₁ ≤ 0)" (REG-4). **The SC-2 pull-forward rider EXPIRED UNSPENT** — its condition (Wave-7 underrun) never fired; all four slices shipped. SC-2 returns to the pool on its merits (Part 3 ranks it #3).

**Standing register carried to Wave 8** (each verified or explicitly carried at its recorded home): **BT-3** — teed with TWO planning MUSTs (re-verify the AS Z1 transcription + the −0.70/−1.8 thresholds; now ALSO carries the ESHS-1 tie-break fix, OQ-W7C-2); **MG-2 candidates** (findings lifecycle SR 26-2 §VI; overdue teeth; re-tier re-clamp; renewal bound) — trigger NOT fired, earliest real overdue **2027-07-19** (executed live-tenant query; the corrected claim-check figure), but the argument is eroding (Part 3a); **OD-B expire-a-mapping** + the absorbed clause-2 residual (above); **GLM MA(q)** — blocked on its deterministic-optimizer prerequisite, do not schedule until that design exists; **covariance v2s** (EWMA/shrinkage on the FACTOR leg — now the sharper asymmetry post-RS-1, L-W verified-and-reserved); the four MF-1-unlocked candidates; active-risk loadings-aware denominator; vendor-beta capture; ENT-058 (paper); computed factor returns; vol surface; PAR_RATE/interpolation; WORM/gitleaks; P3-7 deferral C (lineage batching, performance-triggered); the raw-return pin kind (count corrected above); multi-day √h + the newly-NAMED stressed-window calibration rung (Part 3b — named here so the register carries it explicitly).

## Part 3 — Outward benchmark (rule 6(b), at wave scale)

*Independently drafted; every load-bearing claim separately attacked (12 claims, 11 survived, 1 corrected — the MG-2 trigger date). Scorecard vs the Wave-6 ranked five-gap list: **items 1–2 (the mathematics) CONSUMED; items 3–5 (the operational/governance trio) carry** — exactly fork A's ratified trade. Of the Wave-6 "missing depth" verdict, real ES and the 2003-vintage transformation math are PAID; remediation tracking and the scheduler remain.*

**(a) Model governance vs SR 26-2 / SS1/23 / EGIM — a regime with two demonstrated flywheel turns; the tracking artifact is now the clear front gap.** External anchor re-checked: **SR 26-2 is real and current** (issued 2026-04-17, superseding SR 11-7/SR 21-8 — web-VERIFIED; the house's §VI paragraph text is MG-1-era and should be re-pinned against the actual April-2026 text at MG-2 planning). Live: 18 codes / 23 versions / 30 records / 87 runs (executed, matches the banner); the lifecycle has turned TWICE (MF-1, RS-1), and DS-2 demonstrated the *refusal* to turn it falsely. Still missing, ranked: (1) **the remediation/finding-closure lifecycle (MG-2)** — findings carry text/severity/author and nothing else; closure is by supersession only; SS1/23 P4's "recommendations … are actioned" (web-VERIFIED) has no queryable artifact, and DS-2 exposed the structural asymmetry (EXCEPTION-form records deliberately carry no closure hook); the mid-2027 trigger argument is honest but *eroding* — 30 records with due dates is now a population; (2) **overdue teeth + the scheduler, now one merged gap** — overdue PERIODIC is display-only by recorded OD-F asymmetry, the worker still returns `{"status":"idle"}`, zero cron/scheduler hits repo-wide; fourth close running; (3) **person-level independence** — disclosed everywhere, only a second human retires it; (4) the recorded residue (unbounded renewals, no re-tier re-clamp, ratings in payloads, tiering-approach-unvalidated) — unchanged, none escalated.

**(b) Market risk vs FRTB/Basel — the tail is finally empirical; the horizon structure is the honest boundary.** Real: `ES_HISTORICAL` (Prop-4.1, the first tail statistic that can disagree with its VaR about tail shape); 0.9750 registrable per MAR33.3; the 8-family FRTB-aliased taxonomy load-bearing; Kupiec+Basel backtesting with age gates. Absent or vocabulary-only, each verified at head: the ES backtest (only the BT-3 refusal branch exists; **benchmark nuance this close ADDS: FRTB itself keeps desk backtesting VaR-based while 97.5% ES is the capital measure — so the missing ES backtest trails the ACADEMIC frontier (AS 2014, Kratz-Lok-McNeil), not the Basel floor; BT-3's urgency is one notch lower than the register has framed it**); multi-day horizons (every measure hard-1-day; the 10-day base + LH ladder unreachable; **stressed-window calibration is a rung the register had not named — named at Part 2 now**); LH/partial-ES correctly vocabulary-only absent a capital-reporting user; scenario shocks CURRENCY-only at BOTH gates (re-verified: `scenario.py:152-154`, `scenario_service.py:159-161`); scale honesty — the flagship demo ES at 21 obs puts ~95% of its weight on the single worst scenario (registered; a production window is ≥250 days).

**(c) Private-asset math vs the 2003–2004+ literature — the 1991→2003/4 gap substantially PAID; the frontier moves to structure-estimation and the third §2.1 leg.** Shipped at honest grades: estimated-α with the persisted band and the executed-MC bias disclosure; OW verified-by-derivation (primary equations still unsighted — recorded); EWMA/EB-shrinkage with L-W verified-and-not-used. Remaining: GLM MA(q) (extraction-verified; BLOCKED on the deterministic-optimizer prerequisite); the named estimator v2s (bias-corrected ρ̂, exact-AR1 band, repeat-until-tolerance OW — cheap declared-convention slices, none binding); **the factor-covariance leg now lags the residual leg** (equal-weight sample only — the sharper asymmetry post-RS-1); **commitment/capital-call: zero shipped code, seven waves quiet — and Takahashi-Alexander 2002 (JPM 28(2):90–100, web-VERIFIED this close) is conspicuously house-shaped: closed-form, deterministic, no optimizer (the GLM obstacle does not arise), a handful of declared parameters, and PM-1's transaction substrate already exists.**

**(d) Nearest-gap ranking for Wave-8 planning** (value-per-effort × ingredient-completeness): 1. **CC-1/CC-2 commitment/capital-call** (the ratified presumptive headline; ingredient-complete today; nothing beats it on any axis); 2. **MG-2 + overdue-teeth ride-along** (the nearest purely-supervisory gap; one migration; the trigger argument eroding); 3. **SC-2** (the genuine per-class methodology question; both gates verified closed); 4. **BT-3** (pairing banked; the re-verification debt IS planning work; downgraded one notch by the FRTB-backtests-VaR finding); 5. **the scheduler** (the one architectural gap; sequence WITH or after MG-2); 6. **declared √h multi-day + stressed-window naming** (the cheapest honest FRTB step); GLM (blocked, keep named); covariance v2s (trigger-based). Deliberately not-next: LH/partial-ES capital arithmetic; Monte-Carlo; limits (belongs to an operating wave with the scheduler).

## Part 4 — Destination check (the thesis)

**Wave 7 moved the platform on exactly the axis the Wave-6 close ranked largest** — transformation fidelity. §2.1 leg 1 (desmoothing) went declared→ESTIMATED with the honesty story shipped (the platform states its own estimator's finite-sample weakness: the registered upward-bias limitation with executed-MC evidence; four family versions live). Leg 2 (proxying) gained the estimator lifecycle end-to-end (EWMA/shrunk σ_e; the promote-day age gate; the second TRIGGERED flywheel turn). §2.2's tail is empirical for the first time. §2.3 recorded a maturity signal: the governance substrate now distinguishes "condition closed" from "nothing to close" (DS-2's recorded NO-TRIGGERED).

**The largest remaining gap, verified:** **commitment/capital-call handling — ZERO code after seven waves** (executed grep; ENT-015/016 unrealized in the catalog; REQ-PRV-001/002 unminted; no such tables in the live schema) — the last §2.1 leg with nothing built, named the presumptive Wave-8 headline twice precisely so it stops being the quiet one. Runners-up: the ES backtest (the 15th number ships exempt from the platform's own outcomes analysis); the scheduler + limits pair (fourth close running).

**Demo honesty (the corrected census — claim-check fold):** the living tenant demonstrates capture → three desmoothing conventions → three residual conventions → age-gated promotion → three exposure families → covariance → the full VaR/ES suite, two lifecycle turns, 87 runs. What it still does not exercise: **5 of the 18 registered codes have ZERO living-tenant runs** — `risk.scenario.factor_shock`, `risk.active_risk.parametric`, `risk.sensitivity.analytic`, `perf.benchmark_relative`, and `risk.factor_exposure.proxy` (the fifth is masked in a run_type census — FACTOR_EXPOSURE runs exist but none binds the proxy family version). `ES_HISTORICAL` is exercised exactly once; no ES backtest can run (BT-3 unshipped); nothing commitment-shaped exists to demo. The by-design NULL `model_version_id` on exposure listings and the zero-run proxy family are FACTS WITH HOMES (critic gap 7): named at OQ-W7C-5 as the demo-completeness rider, not silently carried.

## Part 5 — Process findings

1. **The closure-stamp checklist held the line it was built for** (zero "pending"-class headers survived to this close — first close since Wave 4 with none) **but leaked a new class**: the CI-run-id convention adopted at FL-1 silently lapsed for all four Wave-7 stamps (F7 — claims true, provenance thinner). The checklist gains the explicit item: *stamp carries hash + PR + CI run id* (OQ-W7C-3).
2. **The stamp-hygiene tail moved from status-decay to identity-precision**: this wave's misses were a merge-sha-vs-branch-tip conflation (F6), a nonexistent-artifact name in a record (RS1-AUD-1), and a constraint-name drift (DS2-1) — all record-vs-artifact identity, none status. The pre-push battery ALSO caught the DS-2 missing-CI-step live (the P3-7 class, recorded in DS-2 Part 5.5 item 5 with the honest note that the 4-finder composition does not check CI coverage of new suites).
3. **Both Wave-6 process adoptions re-verified holding**: `phase_status.md`/`next_actions.md` remain pointer stubs; the living-tenant durability practice was exercised at the DS-2 closeout (six-stage re-seed after the battery). Session-log coverage: Wave-7's build sessions are logged through 19-07 (tracked set); the close session's log lands via the user-triggered `/compress` flow — a fact, not a defect (critic gap 8).
4. **The second full ultracode close ran cleaner than the first**: 71 agents (vs 78), zero classifier blocks, zero usage-limit interruptions; the verification layer killed 3 findings and downgraded 1 before they reached this document; the claim-checkers corrected 2 judgment claims (the fifth zero-run code; the MG-2 trigger date); the critic surfaced 8 gaps of which ALL are dispositioned in-document (the citation-hygiene carry, the rider dispositions, the mechanics note, the F1 adjudication among them). The pattern (audit fan-out → 2-refuter → judgment+claim-check → critic) is now twice-proven as the wave-close shape.

## Part 6 — Open decisions (OQ-W7C-1…6)

- **OQ-W7C-1 — The audit's findings are RECORDED and the close fixes are ACCEPTED as applied** (the 14-fix batch: the banner count adjudication + latest-record pointer; the CLAUDE.md SSH→HTTPS line; the four CI-run-id backfills + the RS-1 merge-sha correction; the RS-1/DS-2 record addenda; the RTM/backbone discharges; the three canonical-model amendments; the two referent amendments; the test-comment fix). *Recommend APPROVE — zero shipped-code defects in the wave diff for the third consecutive close; every fix is record/doc hygiene.*
- **OQ-W7C-2 — The ESHS-1 demo tie-break finding is ASSIGNED TO BT-3 BY NAME** (the seed-stable tie-break fix + the dated note on the record's selection-dependent demo value ride the slice that consumes this exact helper). *Recommend APPROVE — the alternative (fix now) re-opens a green battery for a non-governed demo surface days before BT-3 touches it anyway.*
- **OQ-W7C-3 — The closure-stamp checklist gains the explicit CI-run-id item** (hash + PR + run id; the FL-1 convention restated after its silent four-stamp lapse). *Recommend APPROVE.*
- **OQ-W7C-4 — The register re-baseline is ADOPTED**: the estimate-staleness residual becomes a clause ledger (clause 1 PAID; clause 2 an ACCEPTED RESIDUAL absorbed into OD-B's row, reviving with its trigger; clause 3 OD-B trigger-based); the α=1-detour row's count corrected to ≥5 with the DS-2-does-not-retire-it note; the SC-2 rider recorded EXPIRED-UNSPENT; the stressed-window rung newly named. *Recommend APPROVE — this is the register discipline applied to its own wording.*
- **OQ-W7C-5 — The demo-completeness rider is NAMED**: the 5 zero-run registered codes + the once-run ES are recorded as a future ride-along candidate (a demo stage exercising them), not silently carried; Wave 8's demo obligation (stage 7: backtest the flagship ES; walk one commitment capture→projection if fork A ratifies) is written into the ratified text. *Recommend APPROVE.*
- **OQ-W7C-6 — WAVE 8, the real fork.** Three genuinely distinct sequences, argued under house discipline (full text preserved in the audit evidence):
  - **(A) Fund the third leg — BT-3 (S/M) → CC-1 (M) → CC-2 (M/L, the headline).** BT-3: the AS ES backtest, ingredient-complete except the recorded re-verification debt (which IS planning work), Christoffersen riding as its homed in-slice fork, the ESHS-1 tie-break fold, migration planning-determined. CC-1: captured commitments/calls/distributions — realize ENT-015 (FR bitemporal funded/unfunded) + ENT-016 (IA cashflow events), mint REQ-PRV-001/002, capture-first (the proven PA-0 shape), captured inputs bind no snapshot/run/model. CC-2: the governed commitment-pacing projection, the 16th governed number — Takahashi-Alexander the natural referent (deterministic closed-form, NO optimizer — the GLM obstacle does not arise; web-verified this close; nowhere in the repo, so the fetch-at-planning obligation is entirely undischarged). *For:* the only fork needing no why-not against the twice-recorded rider; completes §2.1's last leg; retires the 15th number's validation exemption in the same wave. *Against:* the heaviest fork; the payload is liquidity/pacing not factor risk — which is exactly what §2.1 names.
  - **(B) Operate the platform — SCH-1 → LIM-1 → BT-3.** *For:* pays the two zero-code operational gaps, four closes running. *Against — close to disqualifying, and it was last wave too:* a why-not on the presumptive-headline rider; MG-2 cannot honestly ride (trigger unfired — earliest overdue 2027-07-19, executed); two architecture slices idle §2.2 for a wave. The Wave-7 fork C resurrected with a fresher rider against it.
  - **(C) Finish the market-risk ladder — SC-2 → MH-1 (√h) → LH-1+PES-1.** *For:* SC-2 has the genuine methodology question; MH-1 completes the LH/PES unlock's second half; a coherent FRTB IMA shape. *Against:* the capital-arithmetic-without-a-user disclaimer (FL-1's own, unchanged); the CC why-not again; zero private-asset movement the wave after that gap ranked largest.
  - **RECOMMEND (A)**, with the riders written into the ratified text: **(1)** BT-3's planning MUST — Z1 + thresholds re-verified before ratification; **(2)** CC-2's Tier-3 forks named NOW (method: TA 2001 vs a simpler declared-rate pacing v1; the family/permission question; the 16th-number mint) — user sign-off per the standing grant; **(3)** the TA-fetch fallback recorded: if the primary fails the citation bar, CC-2 degrades to a declared-parameter pacing v1 with TA the named v2 — the wave does not silently lose its headline; **(4)** SC-2 stays the named pull-forward on underrun (carried verbatim; its Wave-7 condition never fired); **(5)** the stage-7 demo obligation (OQ-W7C-5); **(6)** slot-zero option: an HG-2-sized opener precedes BT-3 only if ratification surfaces TIPPED debt (none is known — the register carries no unpaid TIPPED item into Wave 8). *The user ratifies.*

## Part 7 — Citation hygiene (carried for whoever plans Wave 8)

**VERIFIED at this close (web, primary or strong secondary):** SR 26-2 existence/date/supersession (Fed site — the §VI paragraph text still to re-pin at MG-2 planning); SS1/23 P4–P5 remediation language (BoE); FRTB's ES-97.5%-capital + VaR-based-backtesting posture (secondaries reproducing d457); GLM 2004 JFE 74(3):529–609 (MIT-hosted); Okunev-White SSRN 460641 + the JFI 16(2):46–61 published sibling; **Takahashi-Alexander JPM 28(2):90–100 (the CC-2 referent candidate; the 2024 enhancement literature — Jeet, SSRN 4819761 — noted for the v2 register)**. **VERIFIED-carried at recorded scope (repo):** Acerbi-Tasche P4.1 (ES-1/ES-HS-1 extractions); RiskMetrics TD §5, Barra USE4, Ledoit-Wolf 2004 (RS-1); Kendall/Marriott-Pope bias (reproduced in-repo via executed MC); Geltner 1991/1993 (PA-1). **UNVERIFIED — fetch to paragraph before citing in ratifiable text:** AS 2014 Z1 exact transcription + the −0.70/−1.8 thresholds (**BT-3 planning MUST**); the OW primary's own equation numbers (before any OW extension); **Takahashi-Alexander to paragraph/equation level (CC-2 planning MUST — web-verified to exist and be on-point, never extraction-verified)**; Christoffersen 1998 (rides BT-3); RiskMetrics character-exact §-numbers (disclosed at RS-1).
