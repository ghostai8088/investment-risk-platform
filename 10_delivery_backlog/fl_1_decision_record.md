# FL-1 Decision Record — the factor-loading substrate + the fractional exposure family (Wave-6 slice 2)

| | |
|---|---|
| **Status** | **RATIFIED — OQ-FL-1-1…6 ALL APPROVED 2026-07-16 (user: "Approve all", after the plain-language gate briefing).** Implementation proceeds per `fl_1_implementation_plan.md`. |
| **Grounding** | Drafted 2026-07-16 on `mg-1-closeout` (`main` = `46ce296` + the MG-1 closeout). Census-verified (3 agents, all claims file:line or executed): **`build_factor_index` cannot index a non-CURRENCY factor set at all** — it keys on `currency_code` and HARD-RAISES on NULL (`factor_kernel.py:76-77`), and STYLE/MARKET/credit factors carry `currency_code=None`, so the v1 indicator kernel's "dimension-generic" docstring claim is true only for partitioning attributes (the Wave-5 close's correction, now pinned to the exact lines). **`factor_exposure_result` ALREADY carries a signed `loading` column** (`PreciseDecimal(20,12)` NOT NULL, `risk/models.py:139` — the docstring calls it *"the beta-extension seam"*), and the 4-tuple grain `(run, portfolio, instrument, factor)` admits fractional multi-factor rows **with NO migration**; snapshot component kinds are unconstrained strings (zero migration). **The proxy family already IS a fractional, non-exclusive projection kernel** (`factor_service.py:310-350` looks up `factor_by_id[pin.factor_id]` directly; `mark_currency` never drives matching) — and the partition identity is ALREADY broken by design there, documented in `bootstrap.py:276-277` ("REQ-MKT-003 exactness holds per-UNPROXIED-atom only"). **Active-risk refuses everything but the allocation family** (`_assert_partitioning_exposure_run`, `active_risk_service.py:453-475` — a model-CODE whitelist, not a family gate: a different relaxation surface, self-verified), so a loadings family is fail-closed there with zero new code. **`private_instrument_id` is a PIN-SERIALIZER KEY** (`snapshot/serialize.py:343`, self-verified — and parse-read at `factor_service.py:167`, so the string is load-bearing on both sides) ⇒ *renaming it is a false-drift landmine (the 0038 class) — the rename middle-path is DEAD*, leaving exactly the two homes in OQ-3. `capture_factor_return` has NO family check (family-generic capture — the Wave-5 close's execution-proof stands); `factor_family` is `String(30)` with NO DB CHECK (live-PG verified) ⇒ the vocab extension is a one-tuple, zero-migration edit. **FIVE** CURRENCY-only gates exist (the verifier pass found the fifth, which the census had missed): four capture/admission gates (`factor_service.py:96/227` — tuple membership, "not supported in v1"; `marketdata/proxy_mapping.py:356` and `scenario.py:152` — literal `!= CURRENCY`, "v2 extension"; `proxy_weight_service.py:266` — "v1 supports CURRENCY only") **plus the scenario RUN binder's exposure-family gate at `scenario_service.py:159`** — any later scenario relaxation must touch BOTH scenario gates (recorded for MF-1). Each capture gate is pinned by a unit test whose probe family (STYLE/MARKET) becomes admissible on relaxation — **the ES-1 probe-move pattern is forced**. PA-3's OLS is hard-wired to a `DESMOOTHED_RETURN` run as its return source; the demo equities hold 9 marks ⇒ 8 observed returns ⇒ **7 desmoothed periods** (the α=1 run consumes its seed observation) — the OLS floor `max(min_observations, k+2)` still gives k ≤ 5. The living demo tenant + the MF-1 TRIGGERED-re-validation hook survive FL-1 untouched **provided the demo tenant stays CURRENCY-only** (the flagship AWC condition asserts "the factor universe is the CURRENCY family only" — seeding non-CURRENCY factors there while the condition stays open would make its premise false-yet-open; multi-family in the demo tenant is MF-1's job). |
| **Mandate** | Roadmap Part 2.9 slice 2, sized **L**: *"the REAL multi-family prerequisite (the corrected tee): a loadings capture/estimation substrate (design fork at planning: vendor-beta capture vs PA-3's regression machinery pointed at public instruments) + an exposure kernel v2 whose loadings are fractional and non-exclusive + relaxing `proxy_mapping`'s CURRENCY gate + the zero-migration vocab extension to FRTB's risk classes. Ride-along: the FE drift trio."* The Wave-5 close corrected the tee (capture is already family-generic — proven by execution); FL-1 is planned on the corrected text. **The census shrinks the honest size: `L` becomes `M/L` — precisely, M/L effort on an L-shaped surface (the ES-1 size-honesty framing)** — the "kernel v2" is not a rewrite but the shipped proxy projection GENERALIZED (the loading column, the projection semantics, and the broken-partition documentation all already exist), and the substrate fork resolves to reusing PA-3's shipped OLS. What keeps it above M: the third family's full governed surface (registrar/predicate/dispatch/tests), the three gate relaxations with their probe moves, and the FE trio. |

## Part 1 — Decisions at a glance (OD-FL-1-A…F)

| OD | Decision |
|---|---|
| **A — The vocab: mint THREE, alias TWO, zero migration** | `FACTOR_FAMILIES` gains **`RATES`, `CREDIT_SPREAD`, `COMMODITY`** — the three FRTB broad risk classes (MAR33.14, verbatim: *"interest rate risk, equity risk, foreign exchange risk, commodity risk and credit spread risk"*) with NO existing family. The two overlaps are **ALIASED, not duplicated**: **`CURRENCY` ≡ the FRTB FX class** and **`MARKET` ≡ the FRTB equity class** (recorded in the factor-taxonomy doc + the family constants' comments) — minting canonical `FX`/`EQUITY` alongside the incumbents would create two labels per concept and a permanent reconciliation debt for zero capability. The Barra-style cross-sectional families (STYLE/INDUSTRY/COUNTRY/MACRO) stay as-is — they are orthogonal to FRTB's instrument-sensitivity classes and serve the RBSA estimation side. One-tuple edit at `models.py:127-142`; `_validate_factor_family` picks the additions up automatically; no test pins the tuple (grep-verified). |
| **B — The loadings method: PA-3's REGRESSION machinery pointed at public instruments; vendor-beta capture recorded as the v2** | The census resolved the ratified fork with facts: **(i)** the OLS kernel, the declared identity (`min_observations`), the per-coefficient betas + std errors + R², and the estimate→promote governance loop ALL exist and shipped at PA-3; **(ii)** the demo equities already hold enough marks for the regression (9 marks ⇒ 8 observed ⇒ 7 desmoothed periods; k ≤ 5); **(iii)** the thesis's own citation lineage (Sharpe 1992 RBSA; §2.2's "academic **and regulatory** state of the art …, not against vendor behavior") is the regression fork; **(iv)** P3-3 named BOTH forks as prerequisites, and the regression's two missing legs ("instrument-return history + estimation machinery") have since shipped (PA-1's marks→returns + PA-3's OLS); **(v)** the vendor-beta alternative ships a substrate for data the platform does not have — an empty table until a feed exists, and the user's scope note defers real data past the build. The forks are NOT mutually exclusive: vendor-beta capture is recorded as the named v2 (the P3-3 deferral language carried forward), and the regression's outputs flow through the SAME promote step, so a captured-beta source can later feed the same home. **The return source (the sub-fork): the α=1 desmoothing detour** — PA-3's snapshot builder is hard-wired to a `DESMOOTHED_RETURN` run, and Geltner at α=1 is the identity transform (`r_true = r_observed` exactly), so a public instrument's raw marks→returns ride the SHIPPED services (a NEW instantiation — the demo's existing desmooth+OLS chain covers only the private fund's quarterly marks — but zero new code paths); the semantic stretch is disclosed in the methodology referent ("α=1 = no desmoothing; the run exists to satisfy the pinned-provenance chain, not to transform"). The alternative (a new raw-return pin kind + adjudication branch) is cleaner semantics for more substrate — recorded as the v2 shape if the α=1 read ever confuses a validator. ⚠️ Honesty carried into the referent: the regression runs on PRICE returns (marks), not total returns (no dividend capture exists) — a recorded limitation, and single-name RBSA over a short window yields noisy betas (the std errors stay first-class on the estimate rows, the PA-3 posture). |
| **C — The loadings home: WIDEN `proxy_mapping` (ENT-019), name-debt recorded; ENT-058 is the named clean-schema v2** | The census killed the middle path: `private_instrument_id` is a **pin-serializer key**, so renaming it false-drifts every historical PROXY_MAPPING pin (the 0038 landmine class — self-verified at `snapshot/serialize.py:343`). The remaining fork: **(i) widen ENT-019** — relax the `:356` CURRENCY gate to the admitted-family allow-list, admit public instruments (the column name `private_instrument_id` becomes a recorded misnomer for public rows — documented at the ORM, the catalog row, and the referent), keep the SHIPPED promote/citation/supersede/pin machinery byte-identical in CODE — with the honest qualification (the ES-1 "byte-untouched" lesson) that the promote path's ACCEPTANCE surface deliberately widens with the `:356` gate (promote → `capture_proxy_mapping` → the gate), so "byte-identical" means the machinery, not the refusal surface; **(ii) mint ENT-058 `instrument_factor_loading`** — clean semantics, but a migration + a promote sibling + a new pin kind + PG suites/grants, duplicating ENT-019's exact shape for a name. Recommend **(i)**: the machinery IS the value (governance, citations, FR bitemporality); the name is debt we record loudly (the ENT-019 catalog row gains "WIDENED AT FL-1: an instrument→factor mapping — originally private-asset-only; `private_instrument_id` retains its name because it is a pin-serializer key and renaming it would false-drift every historical pin"). ENT-058 is minted ON PAPER as the v2 escape hatch if the misnomer ever bites a validator harder than the note. |
| **D — The exposure family: `risk.factor_exposure.loadings` = the proxy projection GENERALIZED, through the SAME binder** | A THIRD family through `run_factor_exposure` (the PA-2/ES-1 same-binder precedent): new registrar (`register_factor_exposure_loadings_model`, the proxy registrar's shape), a new binding predicate (`"v1:exposure-run-atoms+factor-list+loading-rows"`) with the SAME symmetric both-ways refusal the allocation/proxy pair has, and the pinned-content path: `(instrument, factor_id, loading)` rows from the widened ENT-019, `exposure_amount = quantize(loading × atom, 6)` per (instrument, factor) — multiple factors per instrument, fractional signed loadings, `mark_currency` carried for audit only. **The coverage gate (the verifier pass's HIGH, folded as ratified semantics)**: a pinned atom with ZERO loading rows is a FAIL-CLOSED adjudication refusal — no indicator fallback and no silent zero (a silently-dropped atom would under-count VaR); a LOADED atom's fractional remainder (1 − Σ_f loading) is the honest projection residual, disclosed not refused; and PA-2's unpinned-factor guard carries over — a loading row referencing a factor outside the run's pinned factor list fails CLOSED. **The two-arm try/except dispatch becomes the registry map** (the ES-1 `_resolve_var_family` precedent — three arms is past the chain's readability bar; ratified here, not left to Part 5.5). **The partition identity is REPLACED, not extended**: the loadings family is a PROJECTION (`Σ rows = Σ_atoms(atom · Σ_f loading)`, ≠ Σ atoms in general) — the shipped proxy posture generalized, with the registered-limitation language ADAPTED, not transposed (the proxy family's "per-UNPROXIED-atom" phrase describes an indicator fallback the loadings family deliberately lacks): *"every pinned atom must carry loading rows (the coverage gate); the loaded-atom residual (1 − Σ_f loading) is honestly unmodeled — Σ exposure ≠ Σ atoms in general."* The allocation family's ε=0 identity is untouched and **guarded by a byte-exact invariance regression** (the ES-1 pattern). **Downstream, fact-checked per consumer**: VaR/HS/total aggregate `exposure_amount` per factor and work over fractional rows unchanged (`var_service.py` carries zero `factor_family` references — verifier-confirmed), pinned by a through-VaR loadings golden (the PA-2 precedent); **scenario REFUSES a non-CURRENCY loadings run at its RUN-binder gate** (`scenario_service.py:159` — the fifth gate; fail-closed, consistent with OD-E, NOT a pass-through); **active-risk refuses the loadings family automatically** (its allocation-only model-CODE whitelist — self-verified) — the proxy-aware/loadings-aware active-risk denominator stays the recorded v2 it has been since PA-2. Covariance consumes factor returns, not exposure rows (confirmed — not a consumer). |
| **E — The five gates: three relax to EXPLICIT ALLOW-LISTS, scenario's two stay closed; never delete** | Each relaxed gate becomes membership in a named constant — `PROXY_FACTOR_FAMILIES` for the two marketdata gates and the per-family-widened `SUPPORTED_FACTOR_FAMILIES` for the exposure gate (the plan's Step-2 names), ALL carrying the SAME contents `(CURRENCY, MARKET, RATES, CREDIT_SPREAD, COMMODITY, STYLE, INDUSTRY, COUNTRY, MACRO)` — a contents divergence between them would be a silent capability gap — with `OTHER` and unknown values still refused: fail-closed stays the posture, and the three moved pinning tests re-pin against a still-refused value (the ES-1 `0.975→0.98` pattern; `OTHER` is the natural probe; the exposure STYLE probe would also stay refused structurally at `build_factor_index`, but it pins the FAMILY gate, so it moves too). Gate-by-gate: `factor_service.py:96/227` (the exposure families' factor admission — widens for the loadings family; **the allocation family's indicator path stays CURRENCY-only**, since `build_factor_index` structurally requires a currency partition and that is its honest v1 shape); `marketdata/proxy_mapping.py:356` (the OD-C widening); `proxy_weight_service.py:266` (the regression's candidate factors — the OD-B enabler); `scenario.py:152` AND its run-binder twin `scenario_service.py:159` — the fifth gate the verifier pass surfaced (**BOTH NOT relaxed this slice** — scenario shocks over non-CURRENCY factors need their own methodology thought (shock semantics per class), and nothing in FL-1's mandate needs it; recorded as the MF-1-or-later candidate WITH the two-gate inventory, so a future relaxation cannot flip the capture gate and leave scenario runs still refusing). |
| **F — Scope fence + the FE drift trio** | **NO migration** (OD-C(i) + the existing loading column + unconstrained component kinds); NO new permission (`risk.run`/`risk.view` reused; the registrar rides `model.inventory.register` like its siblings); NO new EVT; NO new ENT (ENT-058 stays a paper mint); `audit/service.py` FROZEN; **the demo tenant stays CURRENCY-only** (the Grounding consistency fence — MF-1 closes the condition); the allocation + proxy families byte-untouched (invariance regressions). **The FE drift trio rides** (census-constrained): (1) `PROXY_WEIGHT_ESTIMATE` absorbed into the FE — all four additions (FAMILIES + RUN_TYPE_TO_FAMILY + runDetailUrl + FAMILY_ROW_COLUMNS), of which the exhaustiveness net (`types.test.ts` + the `Record<Family,…>` compile-time shape) FORCES only FAMILY_ROW_COLUMNS — RUN_TYPE_TO_FAMILY and runDetailUrl are deliberate edits that get their own explicit tests (the verifier-pass correction; runDetailUrl's default fallthrough may already yield the right URL — verified at impl); (2) `residual_variance` + `estimate_age_days` + `model_version_id` appended to the vars columns; (3) **the ES `z×σ ≠ var_value` display**: the census KILLED the header-label fix (one static header over mixed-metric rows) and the FE-only multiplier column (`k_c` is not on the row DTO) — the fix is **per-row metric_type-aware cell rendering** (an ES row renders its `z_score` cell as an annotated echo — e.g. `2.326… (echo — not the ES multiplier; see model_version)` — so the three columns can no longer invite the wrong arithmetic), plus the missing ES-row FE test. Surfacing `es_multiplier` on the DTO is the recorded backend v2, deliberately NOT smuggled into an FE ride-along. **4-finder review** (adversarial on the new predicate/gates + the projection arithmetic; numeric on the loadings goldens; doctrine on the FRTB/RBSA citation surface; scope). |

## Part 2 — External benchmark research (rule 6; census extracts, labels per source)

- **BCBS d457 MAR33.14** — **VERIFIED** (extract): the five broad risk classes, verbatim — *"interest rate risk, equity risk, foreign exchange risk, commodity risk and credit spread risk"* (the IMCC partial-ES classes). **MAR33.12 Table 2** liquidity-horizon floors — per-class bounds derived from its per-subcategory values (rates 10–60; credit spread 20–120; equity 10–60; FX 10–40; commodity 20–120; MAR33.12(3) confirms they are floors); **MAR33.15**: ρ = 0.5, the RELATIVE WEIGHT between the unconstrained and summed-constrained ES terms (MAR33.15(2) verbatim: "the relative weight assigned to the firm's internal model" — a weighted-average weight, not a correlation). Disposition: OD-A's class set + the per-class constants the referent carries for MF-1's later use. ⚠️ The standardised approach (MAR21) uses SEVEN classes (CSR split three ways) — FL-1 deliberately carries the FIVE broad IMA classes, stated so no reader conflates the two. |
- **Sharpe (1992)**, "Asset Allocation: Management Style and Performance Measurement" — the returns-based style analysis frame. **Already the platform's cited lineage** (PA-3's referent leads with it — census-verified); FL-1 extends the same machinery to public instruments, which is the paper's OWN primary use case (mutual funds on asset-class indices). **VERIFIED** (the pre-ratification pass independently confirmed both the mutual-funds-on-asset-class-indices primary use case and the Σw=1, w≥0 constraints against the primary; the in-repo lineage was verified at PA-3). ⚠️ Honesty: classic RBSA constrains weights (Σw=1, w≥0); PA-3's OLS is UNCONSTRAINED — the referent already records this divergence and FL-1 carries it forward unchanged. |
- **The price-vs-total-return gap** — no external citation needed; a recorded limitation: the platform captures valuation marks, not dividends, so estimated betas are price-return betas. Understates total-return covariation for high-yield instruments; the std errors stay first-class. |
- **Vendor practice (Barra/Axioma fundamental loadings)** — **SECONDARY** (not re-fetched): fundamental factor models assign loadings from characteristics, not regression. Recorded as the context for OD-B's v2 (vendor-beta capture), never as authority. |

## Part 3 — Limitations, honesty items + out of scope (recorded)

1. **The loadings family is a PROJECTION, not a partition** — `Σ exposure ≠ Σ atoms` in general; the loaded-atom residual (1 − Σ_f loading) is honestly unmodeled, and an atom with ZERO loading rows is REFUSED outright (the OD-D coverage gate — no indicator fallback, unlike the proxy family). Registered as a limitation with the adapted language; REQ-MKT-003's ε=0 acceptance holds for the allocation family only (unchanged since PA-2).
2. **Price-return betas** (no dividend capture) + **short-window single-name regression noise** — the std errors and R² stay first-class on the estimate rows; the dossier/validation posture for any future loadings-family validation must cite them.
3. **The α=1 desmoothing detour is a semantic stretch, disclosed**: the DESMOOTHED_RETURN run exists to satisfy PA-3's pinned-provenance chain, not to transform (α=1 ⇒ identity). The raw-return pin kind is the recorded cleaner v2.
4. **`private_instrument_id` becomes a misnomer for public rows** (OD-C) — recorded at the ORM comment, the ENT-019 catalog row, and the referent; ENT-058 is the paper-minted v2 escape hatch — **the id is RESERVED in the catalog by this mint; the next free canonical id becomes ENT-059**. The pin serializer keeps the key VERBATIM (false-drift protection is why the name survives).
5. **Active-risk refuses loadings-family runs** (the existing allocation-only whitelist) — the loadings-aware denominator stays the recorded v2 (open since PA-2).
6. **Scenario shocks stay CURRENCY-only** (OD-E) — shock semantics per FRTB class are methodology work, not a gate flip; recorded with the census facts for MF-1-or-later.
7. **The demo tenant stays CURRENCY-only through FL-1** — the flagship AWC condition's premise must hold until MF-1 closes it with the TRIGGERED re-validation (the Grounding consistency fence).
8. **`MARKET ≡ equity-class` is an alias by declaration, not a rename** — a factor tagged MARKET is treated as the FRTB equity class for classification purposes; if a genuine cross-asset "market" factor ever arrives, the alias is revisited (recorded). Nothing in the repo currently affirms MARKET=equity beyond a suggestive test factor code — the alias is asserted, disclosed as such.
9. **The FRTB class names are vocabulary, not capital semantics** — adopting MAR33.14's five names classifies factors; it does NOT implement (or claim) any FRTB capital calculation (liquidity horizons, partial-ES aggregation, the SA's seven classes). Stated so no validator reads regulatory conformance into a naming choice.
10. **The equity alias buys capability only in the loadings family** — the allocation/indicator path stays CURRENCY-only (`build_factor_index` structurally needs a currency partition), so an allocation-partition "equity exposure" remains impossible post-FL-1; MARKET factors flow only through the loadings projection.
11. **The desmoothing referent is amended, not silently contradicted** — `desmoothing_geltner_v1.md`'s Purpose currently scopes v1 to appraisal-based private assets; the α=1 public-marks detour gains a dated applicability note THERE too (not only in the PA-3 referent), or the doc set would say both "v1 applies to private appraisal marks" and "we run public marks through it".

## Part 4 — Open decisions (OQ-FL-1-1…6) — ALL RATIFIED 2026-07-16 ("Approve all")

- **OQ-FL-1-1 — OD-A: mint {RATES, CREDIT_SPREAD, COMMODITY}; alias CURRENCY≡FX and MARKET≡equity-class; keep the Barra families.** *Recommend APPROVE — zero migration, no duplicate labels, and the FRTB-5 mapping is complete. Sub-fork: (ii) mint all five canonical FRTB names and deprecate the incumbents — cleaner labels at the cost of a permanent alias/reconciliation layer and touching every CURRENCY-consuming site for no capability.*
- **OQ-FL-1-2 — OD-B: the loadings method = PA-3's regression pointed at public instruments, via the α=1 desmoothing detour; vendor-beta capture recorded as the named v2.** *Recommend APPROVE — every ingredient shipped (OLS, identity, promote loop, marks); the thesis's own Sharpe-1992 lineage is this fork; and the vendor fork ships an empty table for data the build phase will not have (your real-data scope note). Sub-fork: the raw-return pin kind now instead of α=1 — cleaner semantics, more substrate; recommend deferring it.*
- **OQ-FL-1-3 — OD-C: WIDEN `proxy_mapping` (ENT-019) with the name-debt recorded loudly; ENT-058 stays a paper mint.** *Recommend APPROVE — the census killed the rename (a pin-serializer false-drift landmine), and the shipped promote/citation/FR machinery is the actual value. The genuine alternative — ENT-058 now — buys a clean name for a migration + a duplicated governance surface; take it only if the misnomer offends more than the duplication.*
- **OQ-FL-1-4 — OD-D: the third exposure family (`risk.factor_exposure.loadings`) = the proxy projection generalized, WITH the coverage-gate semantics (an unloaded atom is a fail-closed refusal, no indicator fallback — the verifier pass's HIGH, folded); the dispatch registry-map refactor; the projection semantics replace the partition identity (allocation untouched + invariance-pinned); active-risk's refusal stands.** *Recommend APPROVE — the genuine alternative (WIDEN the shipped proxy family to multi-family instead of minting a third) was adjudicated and declined: widening would mutate a SHIPPED, validated governed model's contract and registered-limitation text under the MG-1 validation regime for zero capability gain, and the two contracts genuinely diverge (proxy: indicator rows for unproxied atoms; loadings: the coverage gate, no fallback). The registry-map dispatch is the ES-1 precedent applied at three arms; the fail-closed coverage gate is the only silent-under-count-proof default.*
- **OQ-FL-1-5 — OD-E: the three relaxed gates become explicit ALLOW-LISTS (never deleted); the three probe tests move to still-refused values; scenario's TWO gates (capture `scenario.py:152` + run binder `scenario_service.py:159` — the fifth gate the verifier pass surfaced) BOTH stay CLOSED this slice.** *Recommend APPROVE — fail-closed survives as the posture, the ES-1 probe-move pattern applies, and relaxing scenario without per-class shock methodology would be a gate flip pretending to be a capability.*
- **OQ-FL-1-6 — OD-F: the scope fence + the FE drift trio as census-constrained** (per-row ES cell rendering, not a header label or a fake multiplier column; the DTO change recorded as the backend v2). *Recommend APPROVE — the fence list survived the verifier pass intact; the one FE correction is folded (the exhaustiveness net forces 1 of the 4 additions, so the other three get explicit tests).*

## Part 5 — Implementation readiness gate

Implementation starts only after OQ-FL-1-1…6 ratification, per `fl_1_implementation_plan.md`. Battery: `make check` + full local-PG (fresh AND dirty double-run) + `alembic check` + the synthetic no-`0041` guard (NO migration) + downgrade smoke + the FE suite (vitest, CI-gated) + CI-watch-to-green + the **TD-1 fixture-realism sweep** over every new fixture (plausible equity marks; orderly mark→desmooth→OLS→run dates — the MG-1 date-ordering fold class). **CI-PG disposition, stated explicitly (the recurring-miss class):** the loadings PG leg rides the EXISTING `test_factor_exposure_pg.py` ci.yml step; if any NEW `*_pg.py` file is created instead, its own ci.yml migration-job step is mandatory. The demo tenant is NOT re-seeded by FL-1 (the consistency fence). The OQ-W5C-5 closure-stamp checklist applies at close. Recommended implementation model/effort: **Fable 5 / High**, finder agents ALSO on Fable (the MG-1 Opus-budget restriction was lifted 2026-07-16 after a usage reset; Opus is the fallback if exhaustion recurs); 4-finder review per OD-F.

### Planning verification (the 3-verifier pre-ratification pass — run 2026-07-16, all three verifiers on Opus)

**Verdict: ratification-ready AFTER the folds below — all folded into this record + the plan the same day, pre-briefing.** Census verifier: 14 claim clusters checked adversarially; both load-bearing negatives survived mutation-testing (no test pins the family tuple; no migration needed) and the α=1-identity math confirmed. Doctrine verifier: every external citation confirmed against the BIS d457 primary (PDF, verbatim); both Sharpe-1992 claims independently confirmed. Consistency verifier: OD→plan traceability complete A–F, no scope creep, demo-tenant fence honored by every step.

| # | Finding (severity, verifier) | Disposition |
|---|---|---|
| V1 | **HIGH (census): a FIFTH CURRENCY gate exists** — the scenario RUN binder (`scenario_service.py:159`), missed by the census; OD-D wrongly claimed scenario "works over fractional rows unchanged" | FOLDED: Grounding/OD-D/OD-E/OQ-5 carry the five-gate inventory; both scenario gates stay closed; a run-binder refusal probe added (plan Step 7) so MF-1 inherits the correct inventory |
| V2 | **HIGH (consistency): the unloaded-atom behavior was UNDEFINED** and OD-D transposed PA-2's indicator-fallback limitation language into a family that has none — a silent VaR-under-count hazard | FOLDED as ratified semantics: the coverage gate (zero-loading atom ⇒ fail-closed refusal); the unpinned-loading-factor guard carried from PA-2; the limitation language rewritten; plan Steps 4/7 gained the gates + tests; surfaced in OQ-4 |
| V3 | MED (consistency): no through-VaR loadings test despite OD-D's per-consumer claims | FOLDED: through-VaR golden over CURRENCY factors (plan Step 7; the PA-2 precedent) |
| V4 | MED (consistency): allow-list constant names/contents unreconciled record↔plan | FOLDED: OD-E now names the plan's constants with identical contents required |
| V5 | MED (consistency): TD-1 fixture realism not invoked | FOLDED: Part 5 battery + plan Steps 3/8 |
| V6 | MED (consistency): the CI-PG-step disposition unstated (the recurring-miss class) | FOLDED: explicit in Part 5 + plan Step 8 |
| V7 | MED (consistency): the ORM misnomer comment had no plan step | FOLDED: plan Step 6 |
| V8 | MED (consistency): OQ-4 carried a bare recommendation; third-family-vs-WIDEN-the-proxy-family never adjudicated | FOLDED: OQ-4 rewritten with the adjudication |
| V9 | MED (consistency): OD-C's "byte-identical machinery" over-claimed — the widened gate changes the promote ACCEPTANCE surface (the ES-1 "byte-untouched" class) | FOLDED: OD-C qualified |
| V10 | MED (census): "9 marks ⇒ 8 returns" understated the α=1 seed consumption — 7 desmoothed periods reach the OLS (k ≤ 5 stands, now for the right reason); the public-equity chain is a NEW instantiation, not the shipped demo run | FOLDED: Grounding + OD-B corrected |
| V11 | MED (census): "`types.test.ts` FORCES all four" — the exhaustiveness net forces only FAMILY_ROW_COLUMNS (1 of 4) | FOLDED: OD-F + plan Step 5 corrected; explicit tests for the other three |
| V12 | MED (doctrine): `desmoothing_geltner_v1.md`'s private-assets-only Purpose would be contradicted un-amended by the public-marks α=1 use | FOLDED: Part 3 item 11 + plan Step 3 |
| V13 | LOWs (all three): line drift (`snapshot/serialize.py:343`, active-risk `:453-475`, `risk/models.py:139`); ρ mislabeled a "correlation weight"; Table-2 "verbatim" ranges actually derived bounds; the thesis quote dropped "and regulatory"; OQ-5's malformed emphasis; α is a FREE identity parameter, not an enumerated vocabulary (plan Step 3's contingency was moot); ENT-058 reservation ambiguity; size honesty (M/L effort on an L surface); the FRTB-names-are-vocabulary + equity-alias-scope disclaimers; gate-message wording | ALL FOLDED in place (the corrected cites/quotes; Part 3 items 8–10; the Mandate size note; plan Step 3 rewritten) |

Checked and CLEAR (recorded so they are not re-litigated): the probe moves + invariance regressions + 3×3 predicate symmetry; the OQ-W5C-5 closure checklist, Opus-pinned finders, and the full battery all present; MAR33.14/33.12/33.15 + MAR21-seven-classes + Sharpe-1992 verbatim-confirmed; both ES metric types share `metric_type="ES_PARAMETRIC"`, so the FE honesty fix's single-string check catches plain AND total ES rows (verifier-executed); `var_service.py` carries zero `factor_family` references (VaR/HS/total are genuinely ungated passthroughs); covariance confirmed a non-consumer of exposure rows.

## Part 5.5 — Implementation deviations from the ratified plan

*(recorded during the build)*

## Part 6 — Review dispositions + closure

*(written at fold/close per the house pattern)*
