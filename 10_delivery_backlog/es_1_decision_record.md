# ES-1 Decision Record — the parametric Expected Shortfall leg (Wave-5 slice 4)

| | |
|---|---|
| **Status** | **CLOSED 2026-07-15** — impl `a0723ac` + review folds `17d48bc` merged via **PR #41** = `2abc1b1`, CI green; planning `dcd9634`+`eec25a1` via **PR #40** = `bbc1d1b`. **NO migration** (head stays `0040`). **RATIFIED 2026-07-15 — OQ-ES-1-1…7 all APPROVED by the user**, with **OQ-ES-1-4 resolved to sub-fork (i): widen the shared `VAR_Z_SCORES` table** (⇒ `{0.9500, 0.9750, 0.9900}` registrable on all four VaR families; the two shipped off-vocabulary test probes move to `0.98`; `var_parametric_v1.md`'s enumerated z-table is discharged as stale via RTM/catalog + this record per OD-E ⑶). Ratified in the corrected form — the record the user approved is the post-verification one, with OD-E ⑴⑵ struck and OD-G's "byte-untouched"/"6th reuse" struck. Implementation gated behind `es_1_implementation_plan.md` **and the BT-2 merge** (Part 5). |
| **Grounding** | Drafted 2026-07-15 on `es-1-planning`, stacked on the **un-merged** `bt-2-impl` (`3cb9844`) so the census saw the post-BT-2 world; **rebase onto `main` after BT-2 merges before implementing** (ES-1 touches the same binder). Census-verified: `ES_PARAMETRIC` has been RESERVED BY VALUE since P3-5 on the `var_result` grain `(calculation_run_id, metric_type)` — the UNIQUE constraint already permits it, `metric_type` is an unconstrained `String(30)`, and **every column an ES row needs already exists** (`var_value` is NOT NULL and is the generic magnitude column; `VAR_HISTORICAL` already reuses it with `z_score`/`sigma` NULL) ⇒ **NO migration**. ENT-027 = the 5th realization, no new id (next free would be ENT-058, not needed). `METRIC_TYPES` is the BACKTESTABLE subset ONLY (its sole consumer is `var_backtest_service.py:281`) — ES is absent, so ES rows are refused by the backtest binder **correct-by-default**. `Decimal.exp()` is correctly rounded per the IBM spec (empirically bit-identical C vs pure-Python at prec 50/60) — but `decimal` has **no π**, so a runtime φ(z) would need a declared π anyway. P3-5's registered assumption bars a runtime **inverse**-normal-CDF (the quantile); φ is the **forward** PDF — not literally barred, but the "capability is not evidence" reasoning transfers. **⚠️ Verification correction (the census missed it):** `0028_var_historical.py:43-48` created a **live** CHECK, `ck_var_result_parametric_not_null` — `metric_type = 'VAR_HISTORICAL' OR (z_score IS NOT NULL AND sigma IS NOT NULL AND covariance_run_id IS NOT NULL)` — never dropped, live at head `0040`, **absent from the ORM** ⇒ PG-only, invisible to SQLite tests, not surfaced by `alembic check`. The no-migration headline **survives** (an ES row populates all three ⇒ passes), but it makes the `z_score`/`sigma` echo **DB-FORCED, not a design choice** (OD-C), and it is a recorded landmine for the future ES-over-HS leg (Part 3 item 4). |
| **Mandate** | Roadmap Part 2.8 slice 4 (the LAST Wave-5 slice), sized **S**: "The closed-form seam over BOTH σ and σ_total (`ES_PARAMETRIC` reserved by value since P3-5; FRTB's preferred measure)." Wave-4 close named ES one of three "nearest gaps, all ingredient-complete". **⚠️ Size re-stated honestly as `S` arithmetic / `M` surface** (verifier-flagged; the draft carried the mandate text but silently dropped the size). The no-migration finding is genuine and is the only thing holding "S" up; against it the slice carries 2 model codes + 2 registrars + 2 endpoints, a **shared** vocabulary extension with cross-family blast radius (OD-E ⑴), a new kernel, a new referent, a ~10-group battery, and BT-2's fired PG trigger. For calibration: BT-2 was sized "S code / M methodology" **with** a migration and shipped 8 folds. OQ-3 (the ES-total leg in-slice) is what pushes it over; the record does not hide that. |

## Part 1 — Decisions at a glance (OD-ES-1-A…G)

| OD | Decision |
|---|---|
| **A — The formula + its CONVENTION, pinned** | `ES_c = k_c · σ_p` where `k_c := φ(Φ⁻¹(c)) / (1 − c)`, **`c` = the CONFIDENCE level** (0.975 ⇒ tail mass 0.025), losses loss-positive, zero-mean (`μ_L = 0`, consistent with the shipped `VaR = z_c·σ_p`). **The recorded seam `ES = σ·φ(z)/(1−α)` is CORRECT but under-specified — it never defines `α`, and the literature is genuinely split on that exact symbol** (Acerbi-Tasche use α = TAIL probability; Gneiting uses α = CONFIDENCE). The seam is the confidence form. This record pins the convention explicitly and the implementation must not rely on the symbol. **Definitional guard for the future:** ES is defined as the **α-tail-mean integral**, NEVER as `E[L \| L > VaR]` — the latter is TCE, which is *not* coherent for discontinuous distributions. They coincide for the normal case (Acerbi-Tasche Cor. 5.3(i)) so it costs nothing today, but recording it now means a later ES-over-historical-simulation leg inherits the correct estimator. **⚠️ CORRECTED at the implementation review (the doctrine finder, confirmed by execution): the drafted guard said that estimator is "the mean of the worst `⌈n(1−c)⌉` losses" — which is FALSE and self-defeating.** For an untied sample that quantity is *exactly* `E[L \| L ≥ VaR]`, i.e. the TCE the same sentence forbids, and it UNDERSTATES ES whenever `n·a ∉ ℤ` (−0.34% at n=504, −1.6% at n=100, **−13.9% at n=41 — the platform's own HS adequacy floor at c=0.975**). The correct estimator is **Acerbi-Tasche Prop. 4.1**: with `a = 1−c` and losses sorted worst-first, `ES_a = ( Σ_{i ≤ ⌊n·a⌋} L₍ᵢ₎ + (n·a − ⌊n·a⌋)·L₍⌊n·a⌋₊₁₎ ) / (n·a)` — a FLOOR count plus a FRACTIONAL boundary weight. No ES-1 number moves (the parametric leg is continuous, where ES = TCE); the damage would have been entirely to the future HS leg this guard exists to protect, which would have implemented the forbidden estimator and believed it had complied. Fixed at all six sites. |
| **B — `k_c` as REGISTERED constants (the `VAR_Z_SCORES` twin), NOT a runtime φ** | `k_c` is the exact structural analogue of the registered `z_c`: a per-confidence declared constant, recorded to 12dp. This **preserves P3-5's enumerated-vocabulary invariant unchanged** and sidesteps the interpretive fork entirely (no runtime normal function of any kind). The alternative — computing `φ(z) = exp(−z²/2)/√(2π)` at runtime — is *feasible* (`Decimal.exp()` is spec-guaranteed correctly rounded; verified bit-identical across C and pure-Python) but would require declaring a π constant anyway, and buys nothing: the vocabulary is enumerated either way. Values: `0.9500 → 2.062712807507`, `0.9750 → 2.337802792201`, `0.9900 → 2.665214220346`. **Verification, stated at the precision that actually validates 12dp** (the planning verifier's correction — the draft cited the ≤1.4e-8 tail integration, which is ~4 orders too coarse to resolve the 12th dp and therefore over-claimed "test-verified"): each `k_c` was re-derived by THREE independent routes — `Decimal` bisection on `Φ(z)=½(1+erf(z/√2))` at prec 50–80 with π via Machin (agreement exact at 12dp), stdlib `NormalDist.inv_cdf` (Wichura AS241, a different algorithm — ≤1.1e-15), and composite-Simpson integration of the α-tail-mean `∫z·φ(z)dz` using **no closed form** (≤3.5e-13, which independently confirms OD-A's closed form IS the tail-mean integral rather than merely asserting it). All three agree; **all six constants are correctly ROUNDED, not truncated** — load-bearing at `c=0.99`, where `k = 2.665214220345804…` (13th digit **8**) rounds the 12th dp UP to `…346` — truncation would give `…345`. The 12 digits are pinned in the battery by a byte-exact test that derives `z` in-test (OQ-1's `sole_declared` twin — plan Step 7). |
| **C — Shape: a NEW model code through the SAME binder (the PA-4 shape); ONE ES row per run** | `risk.var.parametric_es` v1 — a third dispatch branch in `run_var` alongside plain/total, reusing the ENTIRE adjudication/snapshot/provenance path and `declared_var_parameters` verbatim. One `var_result` row, `metric_type='ES_PARAMETRIC'`, the ES number in `var_value`, `sigma`/`z_score`/`confidence_level` echoed — **and the echo is DB-FORCED, not a design choice**: the live `ck_var_result_parametric_not_null` CHECK (Grounding) exempts only `VAR_HISTORICAL`, so any non-HS row MUST carry `z_score`+`sigma`+`covariance_run_id` non-NULL. Recording this because it is invisible to the SQLite battery — an ES row that tried to NULL its (arithmetically unused) `z_score` would pass every non-PG test and fail only in PG. This also honours P3-5's own ratified words: *"each method is its own registered model family/version"* (OD-P3-5-A). **The tempting alternative — emitting an extra ES row on the EXISTING VaR run — is DISQUALIFIED by a hard cross-slice regression** (census-verified): `snapshot/service.py::_list_var_rows` pins **every** `var_result` row of a run with **no metric_type filter**, and `var_backtest_service.py:274-280` then refuses a snapshot whose pinned rows mix methods ⇒ **every BT-1/BT-2 backtest over a post-ES parametric run would break immediately**, and a shipped v1 model would silently start emitting two rows. Its true cost is re-opening BT-1/BT-2, not "appending a row". |
| **D — The TOTAL leg (the mandate's "BOTH σ and σ_total")** | ES over the total σ ships in the SAME slice as a SECOND registered code, `risk.var.parametric_es_total` v1, carrying the total family's declared identity (`appraisal_days` + BT-2's `max_estimate_age_days`) and reusing PA-4's residual machinery + BT-2's staleness gate verbatim: `ES_total = k_c · σ_total`. This DISCHARGES the "ES total analogue" half of PA-4's Part-6 deferral 3 (shrinkage/EWMA/calendar-aware trading-day counts stay open — recorded). Rationale for one slice: the arithmetic is the same multiplier over a σ the platform already computes; splitting would ship the mandate's "BOTH" halfway. |
| **E — Confidence vocabulary: ADD `0.975`** (⇒ `{0.9500, 0.9750, 0.9900}`, ONE table shared by FOUR families) | 0.975 is the **only externally-anchored ES confidence in existence** (MAR33.3: "a bank must use a 97.5th percentile, one-tailed confidence level"). `VAR_Z_SCORES` gains `0.9750 → 1.959963984540`. **Three drafting claims were REFUTED by the planning verifiers and are corrected here — this OD is the slice's real risk surface, so the corrections are stated before the rationale:** ⑴ **"The VaR families' behaviour is unchanged" was FALSE** (verifier ran it): `VAR_Z_SCORES` is ONE table shared by `risk.var.parametric`, `parametric_total` v1+v2, **and `risk.var.historical`**, and **0.975 is the exact value two shipped tests use as their off-vocabulary probe** — `test_var.py:363` and `test_var_endpoint.py:334` both flip from refuse to accept (422→409). No existing *registration, row, or number* moves; what moves is **the registrable vocabulary of all four families** — a refusal surface with shipped tests on it. HS at 0.975 computes an adequacy floor of N ≥ 41 (verified: works, no bypass) but is unanalyzed here; the honest framing is that (i) below admits it deliberately. ⑵ **The "byte-exact, portfolio-independent invariant" was FALSE as a row-level test** — σ_p cancels in *exact* arithmetic only; rows are quantized to 6dp independently, and the residuals do not cancel (verifier scanned 200,000 σ values: **zero** exact hits; this repo's own `test_var.py:211-214` already learned this for the VaR leg). The invariant is real but lives **at the constants level**, where it is genuinely exact and genuinely portfolio-independent (no fixture at all): `quantize(k_0.975 / z_0.99, 1E-12) == 1.004923991931` — which is what "pins the constants themselves, not a fixture" always meant. Rows get hand-derived per-row goldens + a derived quantum bound (plan Step 7). ⑶ **`var_parametric_v1.md:46-47` ENUMERATES the v1 z-vocabulary and self-declares immutable** (`:121`) — widening the table makes that immutable referent under-enumerate its own family. Discharged the same way as the ES-"deferred" lines (RTM/catalog + this record — the BT-2 precedent); `..._total_v1/_v2.md` and `var_historical_v1.md` are checked clean (no enumeration). **Sub-fork, corrected:** the draft offered "add to both vocabularies / an ES-only vocabulary" as if two tables existed — there is ONE. (i) **widen the shared table** [recommended: one table, the VaR z is needed to derive/verify k, and 0.975 on the parametric family is FRTB-coherent] and accept ⑴+⑶ — HS/total gain 0.975 as registrable-but-unratified, and two shipped test probes move to `0.98`; or (ii) **a separate ES-only `VAR_ES_Z_SCORES`** — leaves all four shipped families and `var_parametric_v1.md` byte-true, at the cost of a second z table to keep in sync (a real drift surface). |
| **F — NO ES backtest leg in v1 — and the honest reason is NOT "ES isn't backtestable"** | ES rows stay out of `METRIC_TYPES` (correct-by-default: the backtest binder already refuses them). **The justification is (i) FRTB's own precedent and (ii) parametric redundancy — NOT non-elicitability.** Census correction: "ES can't be backtested" is **false** (Acerbi-Szekely 2014 give practical ES backtests; Fissler-Ziegel 2016 show ES is jointly elicitable with VaR) and this record must not repeat it. The real precedent is decisive: **FRTB backtests VaR, never ES** — MAR32.4/32.5 backtest a 99th-percentile VaR; MAR32.18 backtests desk VaR at 97.5th *and* 99th; the ES number is used for capital and is never itself backtested anywhere in the standard. And under this leg's own normality, ES is a fixed multiple of the σ the VaR leg already backtests — an ES backtest here would be the VaR backtest with a rescaled threshold, adding no information. A genuine ES backtest becomes meaningful when an ES-over-HS leg exists (non-elliptical) — recorded as a BT-3-adjacent candidate. |
| **G — Scope fence** | **NO migration** (the census's headline: every column exists — and the live PG-only CHECK is satisfied, not breached; Grounding). **NO new permission** — `risk.run`/`risk.view` reused with **no SoD row change**, the PA-4/BT-2 precedent (⚠️ the draft's "the 6th reuse" is **struck**: the SoD ordinal chain counts NEW TABLES and is itself stale; ES-1 adds no table, so it claims no ordinal — verifier-corrected). NO new EVT (`RISK.*` stays reserved); NO new ENT (ENT-027's 5th realization — count verified). `audit/service.py` FROZEN. **No shipped NUMBER moves** — no existing registration, row, or computed value changes; the plain/total binder paths keep their arithmetic exactly (⚠️ the draft's "byte-untouched" is **struck** on two counts: OD-E ⑴ widens four families' refusal surface, and the dispatch itself gains branches — the fence is about *numbers*, which is what it means everywhere else in the register). The dispatch may be refactored to a small `_resolve_var_family` helper — **ratified here** rather than left to Part 5.5, since a plan-offered fork is not a "deviation" and would otherwise never be recorded; a byte-exact plain/total regression golden (the PA-4 invariance precedent) is the guard. New methodology referent `var_parametric_es_v1.md`; the immutable `var_parametric_v1.md`/`..._total_v1.md`/`_v2.md` referents are NOT edited — **two** staleness classes discharged via RTM/catalog + this record (the BT-2 precedent): their ES-"deferred" lines, and `var_parametric_v1.md:46-47`'s now-under-enumerating z-table (OD-E ⑶). Proportionate **4-finder review** with one NUMERIC finder independently re-deriving `k_c` and the ES/VaR invariant, and one adversarial on the vocabulary extension's blast radius. |

## Part 2 — External benchmark research (roadmap Part 4 rule 6; sources checked 2026-07-15)

**Two corrections this research forced on the obvious drafting — both recorded because the honest version is weaker but true:**

- **⚠️ The coherence argument does NOT discriminate against THIS platform's parametric VaR.** The tempting sentence — "we add ES because VaR is incoherent" — is **wrong as applied to our own leg**. **Embrechts, McNeil & Straumann (2002)**, verbatim: *"In the elliptical world the use of VaR as a measure of the risk of a portfolio Z makes sense because **VaR is a coherent risk measure in this world**."* Our parametric VaR is delta-normal; normal ⊂ elliptical; PA-4's diagonal residual keeps it normal. **Under the platform's own modelling assumption its VaR is already subadditive** (`σ_p = √(x'Σx)` is a norm). The honest four-part *why*: (1) **tail severity** — VaR is a cut-off and says nothing about magnitude beyond it (model-independent, and BCBS's only stated rationale); (2) **the HS-VaR leg is genuinely non-elliptical** (an empirical distribution is discrete — there VaR's subadditivity really can fail, so a coherent ES has real content); (3) **coherence as robustness insurance** — ES's coherence is *unconditional*, the parametric VaR's is *contingent on normality being true*, i.e. ES keeps the aggregation guarantee exactly when the model is wrong; (4) regulatory alignment. *(Sharp detail: Artzner's Remark under the monotonicity axiom rules out `ρ(X) = −E[X] + α·σ(X)` as a functional on arbitrary variables — the σ-multiple form's coherence lives ONLY inside the elliptical family. That is the cleanest statement of why ES-1 is about robustness, not a defect in today's arithmetic.)*
- **⚠️ Do NOT attribute a coherence rationale to BCBS.** Their OPERATIVE rationale is **tail capture only** (d219 main text: VaR's *"inability to capture 'tail risk'"*). The coherence argument is **academic (Artzner; Acerbi-Tasche), not regulatory** — cite each to its actual source. **⚠️ The DRAFTED evidence for this was itself wrong and was corrected at the implementation review** (the doctrine finder re-downloaded all three primaries and re-ran the grep): the draft asserted **zero** occurrences of "coherent"/"sub-additive"/"subadditivity"/"elicitable" across **d457, bcbs265 AND d219**. True for d457 and bcbs265; **FALSE for d219**, which says verbatim: *"VaR has been criticised in the literature for lacking subadditivity. A prominent alternative to VaR is ES, which is subadditive."* **The conclusion SURVIVES and is strengthened** — that sentence sits in **Annex 2, "Lessons from the academic literature and banks' risk management practices"**, under "Messages from the academic literature", i.e. BCBS SUMMARISING the academic argument rather than reasoning from it, which is exactly the claim. But as drafted it was stamped VERIFIED and would have died to a single validator grep — **the BT-2 MAR32.27 failure mode, reproduced on this slice's own headline correction**. Recorded rather than quietly patched.

*(Incremental on PA-4's covariance/coherence citations and BT-2's FRTB-backtesting set; the sources below are the ES-specific additions.)*

- **Embrechts, McNeil & Straumann (2002)**, "Correlation and dependence in risk management: properties and pitfalls", in *Risk Management: Value at Risk and Beyond* (Cambridge UP) — §3: in the elliptical world VaR is coherent (the verbatim quote in correction #1 above). **VERIFIED.** Disposition: **correction #1 — the record's single most load-bearing claim** (it is why ES-1 is about robustness, not about fixing a defect in today's arithmetic). *Promoted to a labelled entry at planning-verification: the draft quoted it only inside the correction bullet, with no VERIFIED/SECONDARY stamp — the exact provenance gap that produced BT-2's MAR32.27 fold, and doubly unacceptable on the quote a Tier-3 OQ (OQ-6) asks the user to ratify.*
- **BCBS d219** (May 2012), "Fundamental review of the trading book" (consultative) — the main text's *"inability to capture 'tail risk'"* as the operative VaR→ES rationale. **VERIFIED (full text, re-checked at the implementation review).** Occurrence counts, corrected: d457 and bcbs265 = **zero** occurrences of "coherent"/"sub-additive"/"subadditivity"/"elicitable"; **d219 = ONE** subadditivity sentence, in **Annex 2 ("Lessons from the academic literature")** — BCBS reporting the literature, not adopting its argument. *(The draft's flat "zero across all three" was false; see correction #2.)* Disposition: **correction #2** (BCBS's stated rationale is tail capture only — attribute coherence to Artzner/Acerbi-Tasche, never to BCBS). *Promoted to a labelled entry at planning-verification, same reason as Embrechts.*
- **Artzner, Delbaen, Eber & Heath (1999)**, "Coherent Measures of Risk", *Mathematical Finance* — Def. 2.4: the four axioms (translation invariance, subadditivity, positive homogeneity, monotonicity). **VERIFIED.** The sub-claim that a Remark under the monotonicity axiom rules out `ρ(X) = −E[X] + α·σ(X)` on arbitrary variables is **SECONDARY** (outside the verified Def.-2.4 scope; flagged at planning-verification) — it is a sharpening of correction #1, not a load-bearing step: correction #1 stands on Embrechts alone. Disposition: ES-1's academic *why* (with the elliptical caveat above).
- **Acerbi & Tasche (2002)**, "On the coherence of expected shortfall", *J. Banking & Finance* — Prop. 3.1 (ES satisfies all four axioms for any α ∈ (0,1) and any X with `E[X⁻] < ∞`, **regardless of distribution**); Cor. 4.3 (ES = CVaR); **Cor. 5.3(i)** (ES = WCE = TCE for continuous distributions — the normal case); Example 5.4 (for discontinuous distributions TCE/WCE are NOT subadditive — only ES survives). **VERIFIED.** Disposition: the coherence proof + OD-A's TCE guard.
- **BCBS d457** (Jan 2019) — **MAR33.3** verbatim: *"In calculating ES, a bank must use a 97.5th percentile, one-tailed confidence level."* (⇒ α is CONFIDENCE in the regulatory text — OD-A/E). **MAR33.9**: *"**No particular type of ES model is prescribed** … historical simulation, Monte Carlo simulation, or other appropriate analytical methods."* ⚠️ *Nuance not to overstate:* d457 does **NOT** name variance-covariance/parametric explicitly — parametric ES falls under *"other appropriate analytical methods"*; **FRTB permits but does not endorse-by-name a parametric ES** (a web summary claiming it names variance-covariance was checked against the text and is wrong). **MAR32.4/32.5/32.18**: backtesting compares **VaR** (99th bank-wide; 97.5th *and* 99th desk-level) — **ES is never itself backtested**. **VERIFIED (full text).** Disposition: OD-E's 0.975 + OD-F's no-backtest precedent.
- **BCBS bcbs265** §1.4(i) verbatim: *"…moving to a confidence level of **97.5%** … will **provide a broadly similar level of risk capture as the existing 99th percentile VaR threshold**, while providing a number of benefits, including generally more stable model output and often less sensitivity to extreme outlier observations."* **VERIFIED.** ⚠️ Honesty caveat: BCBS asserts "broadly similar" **without arithmetic and without the normality qualifier** — the "≈ under normality" is the analysts' gloss, not BCBS's text. Our own verified arithmetic: `k_0.975 = 2.337802792201` vs `z_0.99 = 2.326347874041` ⇒ ratio **1.004924** (+0.49%); `ES_c = VaR_99` exactly at `c = 0.974232`. **The near-equivalence is a ~0.5% arithmetic accident of the Gaussian and holds ONLY under normality** — under fat tails ES_97.5 exceeds VaR_99 by more, which is precisely the "more complete capture" BCBS is buying.
- **Landsman & Valdez (2003)**, "Tail conditional expectations for elliptical distributions", *North American Actuarial Journal* — the closed form `ES_c = μ + σ·φ(Φ⁻¹(c))/(1−c)`. **VERIFIED** — the drafting check was a numerical tail integration across 5 cases incl. non-zero μ at ≤1.4e-8; **planning-verification re-ran it at the precision the 12dp constants actually require** (composite-Simpson at prec 40, no closed form used: ≤3.5e-13), which both confirms the closed form IS the α-tail-mean integral and supplies OD-B's evidence at a resolution that can see the 12th dp. *The draft cited the 1.4e-8 figure as the validation of 12dp constants — ~4 orders too coarse; corrected in OD-B.* Disposition: OD-A/B.
- **Gneiting (2011)**, "Making and Evaluating Point Forecasts", *JASA* — ES is **not elicitable** (no strictly consistent scoring function). **Acerbi & Szekely (2014)**, "Backtesting Expected Shortfall", *Risk* — practical ES backtests exist regardless. **Fissler & Ziegel (2016)** — ES is **jointly elicitable with VaR** (elicitable of order 2). **SECONDARY** (abstract/secondary sources; primaries not fetched). Disposition: OD-F — these are cited to REFUTE the tempting "ES isn't backtestable" claim, not to support it.
- **Nothing found** on parametric ES for portfolios with appraisal-smoothed/illiquid assets specifically — recorded honestly; BT-2's smoothing doctrine carries over unchanged to the ES-total leg (a σ-multiple inherits its σ's honesty).

## Part 3 — Limitations carried forward + out of scope (recorded)

1. **Zero-mean, 1-day, delta-normal** — every limitation of the VaR leg it multiplies (the ES row IS `k_c·σ_p`): no drift term, no √h scaling (the standing horizon deferral), CURRENCY-family factors only.
2. **ES's coherence here is inherited-by-assumption, not demonstrated** — under normality both VaR and ES are coherent (Part 2). The ES leg's *practical* content is tail severity + robustness-if-normality-fails; it does not fix a defect in today's arithmetic. Recorded so no reader over-claims.
3. **No ES backtest** (OD-F) — FRTB precedent + parametric redundancy. An ES-over-HS leg (non-elliptical, where an ES backtest has real content) is a named BT-3-adjacent candidate.
4. **No ES over historical simulation / Monte Carlo** — each its own later declared family (P3-5 OD-A's shape). OD-A's TCE guard is recorded so the HS estimator is inherited correctly. **⚠️ Recorded landmine for that leg (planning-verification): an ES-over-HS leg WILL need a migration**, unlike ES-1. The live `ck_var_result_parametric_not_null` CHECK (Grounding) exempts only the literal `metric_type = 'VAR_HISTORICAL'` and forces `z_score`+`sigma`+`covariance_run_id` non-NULL on everything else — an `ES_HISTORICAL` row has no quantile, no σ and no covariance run, so it would **violate the CHECK in PG while passing the whole SQLite battery**. The no-migration headline is ES-1's alone and must not be inherited by assumption.
5. **PA-4 deferral 3 is only PARTIALLY discharged**: the "ES total analogue" half ships here; **residual shrinkage/EWMA, calendar-aware per-period trading-day counts, AND the *HS* total analogue stay open** (the HS half was dropped from this list in the draft and restored at the Wave-5 close — PA-4's own dated note has it right; this Part 3 is the list a Wave-6 planner reads, so the omission mattered).
6. **The ES-total leg inherits BT-2's smoothing doctrine verbatim** (a σ-multiple is as honest as its σ) and the v1 grandfather posture (an ungated total-v1 σ has no staleness gate; the ES-total code declares `max_estimate_age_days` from birth, so no grandfather exists for it).
7. **The `var_value` column name is now generic-by-`metric_type`** (it holds "the metric's number"): an ES row's `var_value` is an ES, not a VaR. This is the shipped `VAR_HISTORICAL` precedent extended; recorded because the NAME is now a mild misnomer and a reader must key off `metric_type`.
8. **An ES row does not reconcile against its own columns — the true, unglamorous cost of the no-migration headline** (planning-verification; the draft recorded item 7 but missed this). For every VaR row `z_score × sigma == var_value`; for every ES row it **does not**, because the number is `k_c · σ` and `k_c` is on **no column** — the row carries the arithmetically-unused *quantile* `z_score` instead, which the live CHECK (Grounding) forces non-NULL. The multiplier lives only in the bound `model_version`'s declared `es_multiplier=`, and `var_result_content` (`serialize.py:480-502`) pins `z_score`/`sigma`/`var_value` with no multiplier key — one **cannot** be added without the recorded 0038 false-drift landmine. So an ES row is reproducible only *through its model_version*, never from the row alone; API consumers (`VarRowOut`, unchanged) see the non-reconciling triple. Defensible under the `residual_variance` precedent — no consumer reads the multiplier from a pin, and OD-F keeps ES out of backtests — and cheap to state, expensive to discover. **Accepted with eyes open, not silent.**

## Part 4 — Open decisions (OQ-ES-1-1…7) — **ALL RATIFIED 2026-07-15**

> **Disposition: OQ-1, 2, 3, 5, 6, 7 APPROVED as recommended. OQ-4 APPROVED at sub-fork (i)** — widen
> the shared `VAR_Z_SCORES`. The user ratified the post-verification text, i.e. with OD-E's two
> refuted claims already struck; the recommendation for (i) was made *after* disclosing that it
> widens four families' registrable surface, moves two shipped test probes, and staleness-drifts an
> immutable referent. The questions are preserved verbatim below as the ratified record.

- **OQ-ES-1-1 — OD-A/B: the formula, its CONFIDENCE convention pinned explicitly, and `k_c` as REGISTERED 12dp constants** (the `VAR_Z_SCORES` twin) rather than a runtime φ. *Recommend APPROVE — it preserves P3-5's enumerated-vocabulary invariant exactly, sidesteps the forward-PDF-vs-inverse-CDF interpretive fork entirely, and a runtime φ would need a declared π anyway (`decimal` has none) for zero benefit. **The six constants are the thing that would be wrong forever if wrong, so they were re-derived three independent ways at planning-verification (bisection+`Decimal` at prec 50–80 with π via Machin; stdlib AS241; Simpson tail integration with no closed form) — all agree to the last digit, and the correctly-ROUNDED-not-truncated trap at `c=0.99` was checked explicitly. What the verification DID correct is the record's evidence claim, not the values (OD-B).***
- **OQ-ES-1-2 — OD-C: a NEW model code through the SAME binder, ONE `ES_PARAMETRIC` row per run** — NOT an extra row on the existing VaR run. *Recommend APPROVE — the alternative is census-disqualified: it would break every BT-1/BT-2 backtest (the snapshot builder pins all rows of a run; the backtest binder refuses mixed methods) and silently change a shipped v1 model's output.*
- **OQ-ES-1-3 — OD-D: the ES-total leg (`risk.var.parametric_es_total`) ships in the SAME slice**, discharging the ES half of PA-4 deferral 3. *Recommend APPROVE — the mandate says "BOTH σ and σ_total"; the arithmetic is one multiplier over a σ that already exists; splitting ships the mandate halfway. (Alternative: total-ES as its own slice — cheaper here, but leaves the ratified mandate open.)*
- **OQ-ES-1-4 — OD-E: ADD `0.9750` to the confidence vocabulary.** ⚠️ **This is the one OQ whose draft rationale the planning verifiers refuted, so read it in its corrected form (OD-E ⑴⑵⑶) — two of the three reasons the draft gave for APPROVE were false.** What is TRUE: 0.975 is the only externally-anchored ES confidence (MAR33.3), and the `ES_97.5/VaR_99 = 1.004923991931` invariant is real **at the constants level** (exact, portfolio-independent, no fixture) — it just is not the row-level byte-exact golden the draft advertised. What is a REAL COST, now disclosed: there is **ONE shared z table**, so widening it (a) flips **two shipped tests** from refuse to accept (their off-vocabulary probe *is* 0.975 — they move to `0.98`), and (b) silently makes 97.5% registrable on the **historical-simulation and total** families, which this slice never analyzes, and (c) leaves the **immutable** `var_parametric_v1.md`'s enumerated z-table under-enumerating. *Recommend APPROVE of sub-fork (i) — widen the shared table — because one vocabulary table is the shipped design, the VaR z is needed to derive and verify k, 97.5% is FRTB-coherent on the parametric family, and (c) discharges through the same RTM/catalog route OD-G already uses. The alternative (ii) — a separate `VAR_ES_Z_SCORES` — is the conservative choice and is genuinely defensible: it keeps all four shipped families and the immutable referent byte-true, at the price of a second z table to keep in sync. **If you would rather not widen three families' registrable surface for a leg that only needs one, take (ii)** — it is the only sub-fork here with a real trade-off, and I would not argue hard against it.*
- **OQ-ES-1-5 — OD-F: NO ES backtest in v1, justified by FRTB precedent + parametric redundancy — explicitly NOT by "ES isn't backtestable"** (which the research shows is false). *Recommend APPROVE — FRTB backtests VaR and never ES (MAR32.4/32.5/32.18); under normality an ES backtest here is the VaR backtest with a rescaled threshold.*
- **OQ-ES-1-6 — Part 2's two corrections are RECORDED** (the elliptical caveat on coherence; no coherence rationale attributed to BCBS). *Recommend APPROVE — this is the BT-2 lesson applied pre-emptively: a citation that does not say what we need it to say is worse than no citation, and the obvious ES rationale ("we add ES because VaR is incoherent") is exactly the kind a validator would check and we would lose. Both corrections survive verification intact; what verification fixed is that the two quotes CARRYING them (Embrechts 2002; BCBS d219) sat in the draft with **no provenance label** — the precise shape of BT-2's MAR32.27 fold, on the quotes an OQ asks you to ratify. Both are now labelled source-list entries, and Artzner's σ-multiple Remark is demoted to SECONDARY (correction #1 stands on Embrechts alone, so nothing load-bearing rests on an unverified sub-claim).*
- **OQ-ES-1-7 — OD-G: the scope fence** (NO migration/permission/EVT/ENT; no shipped NUMBER moves; a new referent, immutable ones unedited + two staleness classes discharged; the `_resolve_var_family` refactor ratified in-record; 4-finder review incl. a numeric finder re-deriving `k_c` + the invariant). *Recommend APPROVE — note the fence is **weaker than the draft claimed** and now says so: "byte-untouched" and "the 6th reuse" were both struck at verification (OD-G). The no-migration headline itself was attacked hardest and **survived** — including against a live PG-only CHECK the census had missed, which ES-1 satisfies.*

## Part 5 — Implementation readiness gate

Implementation starts only after OQ-ES-1-1…7 ratification AND **BT-2 has merged** (this branch is stacked on `bt-2-impl`; rebase onto `main` first — ES-1 adds a third dispatch branch to the binder BT-2 just changed). Per `es_1_implementation_plan.md`. Battery: `make check` + full local-PG (schema-reset AND dirty-schema double-run — **the PG legs are load-bearing this slice**: the CHECK in Grounding is invisible to SQLite) + `alembic check` (drift only; the **no-new-revision** claim is pinned by the synthetic guard, not by `alembic check` — the draft conflated the two) + CI-watch-to-green. **Trigger check (BT-2 Part 6):** ES-1 touches `var_result`'s PG suite ⇒ BT-2's recorded residual coverage gap fires — pay **both halves** (the draft quoted only the PG one): *"no PG leg for the age column **and no API assertion of `estimate_age_days`**"*. Recommended implementation model/effort: **Opus 4.8 / High**; 4-finder review per OD-G.

### Planning verification (3 adversarial verifiers, 2026-07-15 — run before ratification, not after)

Both headline claims were attacked directly and **SURVIVED**: the **no-migration** finding (attacked hardest — it survived even a live PG-only CHECK the census had missed, which ES rows satisfy) and **OD-C's disqualification** of the extra-row alternative (*understated* if anything — a second independent trip also fires: the backtest binder refuses duplicate `window_end` as-ofs, which two rows of one run necessarily share). **All six constants CONFIRMED to the last digit** by three independent routes.

What verification **changed**, recorded because the honest version is weaker: **2 HIGH, both in OD-E, both confirmed by execution** — ⑴ "the VaR families' behaviour is unchanged" was false (two shipped tests flip; one shared table feeds four families), ⑵ the "byte-exact portfolio-independent golden" cannot pass at row level under 6dp quantization (0 hits in 200,000 σ values) and belongs at the constants level. Plus 8 MEDIUM / 6 LOW folded across both docs: the missed CHECK (→ Grounding, OD-C, Part 3 item 4); the ES row's non-reconciling triple (→ Part 3 item 8); the immutable referent's stale z-table (→ OD-E ⑶, OD-G); the `k_c` test being structurally incapable of pinning the digit it exists to pin (→ plan Step 7); `1.4e-8` cited as evidence for 12dp (→ OD-B); two unlabelled correction quotes (→ Part 2); no authz test on the new endpoints, no plain/total invariance regression (→ plan Step 7); the struck "6th reuse" ordinal and "byte-untouched" (→ OD-G); the dropped size (→ Mandate); `ES > VaR` false at σ_p = 0 (→ plan Step 7); the prec-28 magnitude-gate discipline (→ plan Step 4); the ES-total absent-declaration branch made fail-closed by construction rather than by inherited backstop (→ plan Steps 3/4). **Zero HIGH defects in the registered values; both HIGH defects were in the record's CLAIMS about them.**

## Part 5.5 — Implementation deviations from the ratified plan

1. **The `_resolve_var_family` helper became a 2×2, not a 4-deep try/except chain.** The plan
   offered the helper as an if-it-gets-unwieldy option (ratified in OD-G). Building it made the
   real shape obvious: the four codes are `{plain, total} × {VaR, ES}` — two INDEPENDENT axes —
   so the helper returns a frozen `_VarFamily(code, total, es)` and the compute path branches on
   the axes rather than on model codes. A list-of-codes chain would have re-encoded the same 2×2
   four times. **Behaviour-preserving**, and guarded by the byte-exact plain/total invariance
   golden the plan required.
2. **`declared_es_multiplier` refuses ABSENT as well as ambiguous** — the plan specified the
   identity check (value must equal the registered table's) but did not state the absent-branch.
   Made explicit and fail-closed: unlike BT-2's `max_estimate_age_days` (whose absent-branch is
   the recorded v1 grandfather), an ES version has **no legitimate ungated form** — `k` *is* the
   arithmetic. `sole_declared` is deliberately not used (its None-on-ambiguous would fail OPEN).
3. **The plain-ES branch multiplies the RAW sqrt, not `estimate.sigma`.** Not in the plan; found
   while reading `var_kernel`: the shipped plain path computes `var_value = (z·raw_sqrt).quantize()`,
   i.e. it quantizes ONCE. Using the already-6dp-quantized `estimate.sigma` would double-round and
   put the ES a quantum off its own hand-derived golden. The ES branch recomputes from
   `estimate.radicand` exactly as the total path does.
4. **Two test-harness corrections, both mine, neither a code defect** (recorded because a reader
   comparing plan to battery will see the difference): the tampered-multiplier test mints
   adversarial versions via the **generic registration path** rather than mutating an assumption
   row — `model_assumption` is append-only (AUD-01), so a mutate-the-row test fails on the audit
   guard instead of on the gate under test, *and* a real attacker mints rather than UPDATEs; and
   the ES-total staleness test uses BT-2's own `estimate_span_end` harness (my first version set no
   span end, so the age was 0 and the gate correctly did not fire).
5. **`test_es.py` derives `z` in-test at prec 40 / 100 bisection steps, not prec 60 / 220** — the
   first version nested bisection inside bisection for the crossover golden (~44k high-precision
   erf evaluations) and blew a 120s timeout. prec 40 leaves ~20 orders of headroom over the 1e-12
   quantum being pinned; the crossover golden (which needs only 6dp) uses stdlib `NormalDist`
   (Wichura AS241) — faster *and* a genuinely different algorithm from the byte-exact leg's
   bisection, so the two legs stay independent.

## Part 6 — Review dispositions + closure

### Part 6.1 — The 4-finder implementation review (numeric / adversarial / doctrine / scope+tests)

**2 HIGH + 1 MEDIUM + 6 LOW — ALL FOLDED, zero deferrals.** The headline: **zero defects in the
registered constants or the ES arithmetic** — the numeric finder re-derived all six to 40 significant
digits through a stack sharing *no method* with the repo (Brent-Salamin AGM π, the all-positive
confluent erf series, Newton inversion) and confirmed every digit and every golden. The adversarial
finder mounted 18 attacks on the identity gate and could not break it, could not defeat the
predicate/staleness/backtest gates, and could not produce a PG overflow. **Both HIGHs were in the
research corrections this slice was proudest of** — which is precisely where BT-2 was burned.

- **HIGH-1 — the TCE guard prescribed the TCE it forbids** (a REGISTERED `model_limitation` row +
  5 more sites). The guard said the HS leg must inherit "the mean of the worst `⌈n(1−c)⌉` losses" —
  but for an untied sample that quantity **is** `E[L | L ≥ VaR]`, i.e. the TCE the same sentence
  forbids (verified by execution: identical to 1e-12 on every sample). It also **understates** the
  true α-tail mean whenever `n·a ∉ ℤ` — **−13.9% at n=41, the platform's own HS adequacy floor at
  c=0.975**. No ES-1 number moves (the parametric leg is continuous, where ES = TCE); the damage was
  aimed squarely at the future ES-over-HS leg the guard exists to protect, which would have
  implemented the forbidden estimator *and believed it had complied*. **FOLDED at all six sites**
  with Acerbi-Tasche Prop. 4.1's actual form (a FLOOR count + a FRACTIONAL boundary weight).
- **HIGH-2 — the "zero occurrences" grep was FALSE for d219** (referent + catalog + record ×2).
  Re-verified independently against the primary: d457 and bcbs265 are genuinely zero, but **d219
  says verbatim** *"VaR has been criticised in the literature for lacking subadditivity. A prominent
  alternative to VaR is ES, which is subadditive."* **The conclusion SURVIVES and is strengthened** —
  that sentence sits in **Annex 2, "Lessons from the academic literature and banks' risk management
  practices"**, i.e. BCBS *summarising* the academic argument rather than reasoning from it, which is
  exactly the claim being made. But as drafted it was stamped VERIFIED and would have died to one
  validator grep: **the MAR32.27 failure mode, reproduced on this slice's own headline correction.**
  FOLDED to the precise claim (operative rationale = tail capture; d219's mention is a lit-review).
- **MEDIUM — BT-2's fired trigger was only half paid, and a comment claimed otherwise.** The sole
  `estimate_age_days` API assertion sat on a **plain ES row, where the age is `None` by
  construction** — proved vacuous by mutation (forcing `estimate_age_days=None` on every API
  response left the whole backend suite green). BT-2's recorded gap would have survived the slice
  that was supposed to close it. FOLDED into `test_var_total_endpoint.py`'s roundtrip, where the age
  is **populated** — and re-verified by mutation: the same mutation now **fails** the test.
- **LOW ×6, all folded:** the magnitude refusal named `sigma` and printed an **in-envelope** value on
  the exact hazard the slice exists to guard (the FAILED run *is* the deliverable — it now names the
  breaching ES, test-pinned); the ES predicate-symmetry refusals were untested (behaviour correct —
  now pinned for all four families, guarding a refactor that re-keys the gate onto a model code);
  stale ORM docstrings still called `ES_PARAMETRIC` "reserved" (the likeliest place a maintainer
  looks to learn the vocabulary); `test_es.py`'s headroom comment claimed "~20 orders" which does not
  follow from its own premises (~12) nor the truth (~17) — *an over-claim of exactly the class OD-B
  struck, in the comment justifying the test that pins the constants*; a dangling reference to a test
  name that never existed (the row-level bound lives in `test_var.py` under another name); and the ES
  `quantize` sat outside its `try`, asymmetric with `var_kernel:111` which guards the identical
  operation (unreachable — needs ~1e12 factors — now guarded anyway, the PA-4 defensive-gate
  precedent).

**Notable non-findings** (recorded so they are not re-litigated): `RejectedModelVersionError` does
**not** subclass `WrongModelVersionError`, so the family loop cannot swallow VW-1's gate — probed
live on all four families. The plain-ES branch closes the magnitude window by a *different and
arguably stronger* mechanism than the total path (it gates the already-quantized value, so what is
gated **is** what is stored). The double-rounding choice in Part 5.5 #3 is **load-bearing on the
shipped fixture**: via the 6dp σ, `REF_ES95_TOTAL` would be `1077.597056`, one quantum off.

### Part 6.2 — Post-fold validation

`ruff format`/`ruff check` clean; `mypy` clean (175 source files); **`make check` 1448 passed / 293
skipped**; **full local-PG 1741 passed, ZERO failures — on a fresh schema AND on a dirty-schema
double-run**; `alembic check` no drift; **`alembic heads` still `0040_var_estimate_age` with 40
revisions (the NO-migration claim, mechanically re-verified)**; `downgrade base` → `upgrade head`
clean. *(One local-PG failure surfaced and was correctly diagnosed as an artifact of my own schema
reset, not the code: `GRANT USAGE ON SCHEMA public TO PUBLIC` — recorded in the FE-1 memory and
omitted from my reset. CI's fresh database has it by default.)*
