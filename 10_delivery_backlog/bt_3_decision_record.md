# BT-3 Decision Record — the Acerbi-Szekely ES backtest + the Christoffersen independence leg (Wave-8 slice 1)

| | |
|---|---|
| **Status** | **VERIFIER-FOLDED DRAFT — pending OQ-BT-3-1…7 ratification (the user signs).** Drafted 2026-07-19 on `main` = `0208071` (post-Wave-7-close ratification; head migration `0042_desmoothing_estimated_alpha`). The pre-ratification verifier pass RAN (three verifiers, executed checks): **1 HIGH + 6 MED + 6 LOW, ALL FOLDED** — the ratified shape changed materially (the T-domain verdict gate; the stage-7 demo reframed to domain-gate honesty; the threshold route accounting repaired; the ES-leg battery + uniformity gate; the FE relabel; Part 5 carries the full disposition). Sized **S/M** (roadmap Part 2.11 row 1). Not yet implemented or code-reviewed. |
| **Grounding** | Census (one thorough Explore pass, file:line-grounded) + the ratified fetch-first MUSTs DISCHARGED (Part 2). The existing backtest machinery: `run_var_backtest` (`risk/var_backtest_service.py:424-688`) — build/consume paths, pre-create prerequisite gate; the all-or-nothing alignment gate (`:329-362`: each VaR forecast pairs with EXACTLY the DIETZ sub-period where `window_end == period_start` and `period_end == as_of + horizon_days`; unpaired ⇒ refused; duplicate `window_end` refused `:323-327` so pairing is injective); the MV-chain integrity check (`:245-271`; realized P&L = `end_mv − begin_mv − net_external_flow`, flow-adjusted, `:269`); the cross-portfolio identity gate (`:392-421`). **The DELIBERATE ES_HISTORICAL refusal branch naming this slice** (`:295-303`, verbatim: "the genuine Acerbi-Szekely ES backtest is the named BT-3 candidate — pairing the ES-HS run with its sibling VaR-HS run by shared input_snapshot_id"). The kernel (`var_backtest_kernel.py`): `exception_indicator` (`:53-56`, `e=1 iff −P&L > VaR`, STRICT ⇔ `X + VaR < 0`), `kupiec_lr` (Decimal-50, 12dp), `CHI2_1DF_CRITICALS = {0.05: 3.841459, 0.01: 6.634897}` (`:33-36` — registered-constant precedent: "Extending the set is a NEW model version"). ENT-055 `var_backtest_result` (`risk/models.py:335-401`): IA append-only, grain `(calculation_run_id, metric_type, period_start)`, **`metric_type` String(30) UNCONSTRAINED** (code-side vocab comment only — the BT-2 zero-schema precedent for admitting new metric types), `var_metric_type` echo, nullable `realized_pnl`/`var_value` per-pair echoes, **NO ES-forecast echo column** (the one candidate migration, OD-D). Model identity: `risk.var_backtest` (`bootstrap.py:1351-1531`, declared `alpha=` ∈ {0.05, 0.01}); the Christoffersen tee lives in its limitation rows (`:1401-1404`, `:1420-1422`). **The pairing substrate is LIVE**: ES-HS reuses `PURPOSE_VAR_HS_INPUT` + the builder byte-unchanged (one snapshot feeds both families, `bootstrap.py:1062-1065`); stage 4 binds the flagship ES to the flagship HS VaR's snapshot (`demo/eshs_stage4.py:231`) — and carries **the Wave-7-close-assigned tie-break defect** (`_latest_flagship_hs_row` `:162-186`, `ORDER BY window_end DESC, calculation_run_id DESC` at `:176` — a uuid4 tie-break; OQ-W7C-2 assigned the seed-stable fix HERE). One ES-HS run emits ONE ES row at ONE as-of (`eshs_stage4.py:238`) ⇒ a T-day paired series needs T sibling pairs, each sharing its `input_snapshot_id`. **Premise corrections from census**: the BT-2 estimate-age gates are `var_service.py` (the forecast leg `:466-563`), NOT the backtest leg; the head is `0042`. Validation surface: `risk.var_backtest`'s INITIAL takes real BT-1/BT-2 runs as evidence (`demo/campaign.py:883,:911-913`); **the ES-HS INITIAL carries NO backtest-shaped condition** (its two conditions are tail-resolution + factor-substrate, `dossiers.py:615-623`) — the no-closable-condition honesty question recurs (OD-F). House conventions: registry-map dispatch (never try/except chains); `declared_*` gates via `sole_declared`; exact-type API error maps; CI demo-stage order (stage 7 seats after stage 6, before the downgrade smoke); demo filename alpha-sort; 21 migration-head pins re-pin if a migration ships; `make check` 1588. **NO new permission** (`risk.run`/`risk.view` reused — the BT-1 shape); `audit/service.py` FROZEN. |
| **Mandate** | Roadmap Part 2.11 slice 1 (ratified 2026-07-19, OQ-W7C-6 fork A): "Closes the 15th governed number's exemption from the platform's own outcomes analysis." The ratified planning MUSTs: re-verify the AS 2014 Z1 transcription (two routes + the settling identity) + the −0.70/−1.8 threshold VALUES at the three-route constant bar (M-P attribution + Lund Table 1 + this pass's executed seeded MC — the route accounting in Part 2, repaired per the verifier's BT3-V-4) — **DISCHARGED, Part 2**; Christoffersen 1998 to paragraph — **DISCHARGED, Part 2**; the ESHS-1 tie-break fix folds here BY NAME (OQ-W7C-2). Migration planning-determined (the roadmap row's own words) — resolved at OD-D. |

## Part 1 — Decisions at a glance (OD-BT-3-A…G)

| OD | Decision |
|---|---|
| **A — The statistics: Z̄2 (unconditional, PRIMARY) + Z̄1 (conditional, secondary evidence), exact AS forms, per-pair evidence rows + summary rows; the N=0 honesty rule** | The ES backtest computes, over the paired series {(X_t, VaR_t, ES_t)} (X_t = the flow-adjusted realized P&L of the aligned DIETZ sub-period — the BT-1 convention verbatim; VaR_t/ES_t from the sibling runs, positive numbers; I_t = 1{X_t + VaR_t < 0} — EXACTLY the shipped strict `exception_indicator`): **Z̄2 = (1/(T·a))·Σ_t X_t·I_t/ES_t + 1** (a = 1−c, the tail probability of the paired family's declared confidence) and **Z̄1 = (1/N_T)·Σ_t X_t·I_t/ES_t + 1** (N_T = Σ I_t). Both formulas at the three-route bar (Part 2): Z2 verbatim in TWO independent reproductions + the null-expectation identity; Z1 in ONE independent reproduction + the vendor rendering + **the identity settling the '+1' grouping** (outside the sum — an inside-the-denominator '+1', the exact corruption the ES-HS-1 debt flagged, provably breaks E[Z]=0). Under H0, E[Z̄2] = 0 and E[Z̄1 given N_T>0] = 0; negative ⇒ risk understatement; one-sided. Persisted per run (the ENT-055 grain, new metric types — vocab comment extended): per-pair `ES_EXCEPTION_INDICATOR` evidence rows (echoing realized_pnl + var_value + the ES forecast, OD-D) + summary rows `AS_Z2` and (only when N_T > 0) `AS_Z1`. **N_T = 0 ⇒ NO AS_Z1 row and NO verdict** — the statistic is undefined at zero breaches (division by zero is the mathematics, not an edge-case preference); the summary carries the exception count so the absence is legible. Z3/Z_MB (2017)/ranit-simulation variants: named v2s, out. **One-sidedness disclosed as a registered limitation (the verifier's doctrine note): the AS tests can flag UNDERSTATEMENT only — over-conservatism is invisible to them, a deliberate break with BT-1's two-sided POF; and Z̄2 is well-defined (= +1) at zero breaches — the verdict suppression at N_T = 0 is a POLICY choice cross-referenced with OD-B's domain gate (a zero-breach 250-pair series at 2.5% is itself evidence of over-conservatism the one-sided test cannot score), stated in the referent.** All arithmetic pure-Decimal prec-50, quantize 12dp (the kernel house rules); deterministic — NO simulation anywhere in-run. |
| **B — The verdict fork (the Tier-3 headline): EVIDENCE-FIRST with a NARROW registered-threshold verdict — the AS pair (−0.70/−1.8) is registrable ONLY at the 2.5% tail (paired ES confidence 0.9750); every other confidence gets evidence-only rows, disclosed** | The re-verification (Part 2) SHARPENED the debt into a design constraint: the −0.70/−1.8 pair is **Test-2 (Z̄2) left-tail critical values at significance 5%/0.01%, distribution-STABLE across N(0,1)↔t(ν≥100) (degrading at low df: −0.82/−4.4 at t3) but α-DEPENDENT** — an independent reproduction derives ≈**−1.2** at a different tail (α=0.5%, φ=5%), and AS's own configuration is the FRTB 2.5% tail. The platform's shipped ES-HS runs declare c ∈ {0.95, 0.99} (tails 5%/1%) — **adopting −0.70/−1.8 for them would be numerically WRONG**, and deriving correct thresholds needs Monte-Carlo (the recorded TR-09 determinism obstacle — the GLM class; never in-run). The ratified resolution: **(i)** Z̄1/Z̄2 evidence rows persist for EVERY admissible paired confidence (the statistics themselves are α-correct by construction — only the VERDICT thresholds are α-bound); **(ii)** a `test_decision` verdict row is emitted ONLY inside the criticals' FULL derivation domain — **paired confidence 0.9750 AND n_pairs = 250** (the adversarial verifier's HIGH: the criticals are T-DEPENDENT too — executed MC: the 5% critical is ≈−3.68 at T=10, −2.39 at T=25, −1.57 at T=50, reaching −0.70 only at T=250; an off-domain verdict at demo scale would spuriously REJECT the flagship ES — the exact numerically-wrong-threshold class this OD exists to prevent; the domain gate is the shipped BASEL_ZONE precedent, `var_backtest_kernel.py:102-105` — its Basel row fires only at confidence 0.99 ∧ n=250 ∧ horizon 1) — against the REGISTERED constants **Z2_CRITICAL = {0.05: −0.70, 0.0001: −1.8}** (the `CHI2_1DF_CRITICALS` precedent: fixed registered constants, extending the set = a NEW version; Lund's own simulation T re-verified at implementation), REJECT iff Z̄2 < the declared-significance critical; **(iii)** off-domain pairings (any confidence ≠ 0.9750 OR n_pairs ≠ 250) get the evidence rows + NO verdict — **the reason DERIVABLE from persisted rows, not a schema addition**: a `ES_PAIR_COUNT` summary row (metric_value = T) + the bound version's stamped domain constants make the absent verdict mechanically explicable (the BT-2 read-rule doctrine, stated in the referent; an absent verdict is a recorded fact, not a silent gap); **(iv)** the α-dependence, the low-df degradation, the distribution-stability scope, **and the T-dependence** (the verifier's executed sharpening: at a=0.005 the 5% critical moves from ≈−1.56 at T=250 to ≈−1.11 at T=500 — Zeliade's −1.2 is an N=500 figure; the registered pair's recorded derivation scope is **a=0.025, T≈250, near-normal tails** — the verifier reproduced −0.7001 at exactly that configuration) are REGISTERED limitation rows; the verdict discloses the scope rather than refusing off-T (the evidence-first doctrine). A per-(α,T) threshold table (MC-derived offline under a governed derivation record) is the named v2. |
| **C — Pairing + gates: sibling resolution by IDENTICAL `input_snapshot_id` + same declared confidence; the BT-1 gate battery REUSED; a NEW binder, NOT a widening of the Kupiec admit** | The run consumes: the portfolio-return run (realized leg, BT-1's) + an ordered set of **sibling pairs** (VaR-HS run, ES-HS run), each pair sharing ONE `input_snapshot_id` (the ES-HS-1 recorded design input, demonstrated live at stage 4) and the SAME declared confidence; the VaR leg supplies VaR_t + the as-of (`window_end`), the ES leg supplies ES_t. Fail-closed pre-create gates: sibling snapshot mismatch; sibling confidence mismatch; a pair whose ES run's `metric_type ≠ ES_HISTORICAL` or VaR run's `≠ VAR_HISTORICAL`; ES_t ≤ 0; then the BT-1 battery VERBATIM on the VaR leg — all-or-nothing DIETZ alignment (calendar `horizon_days`), duplicate-`window_end` refusal (injective pairing), MV-chain contiguity/integrity, cross-portfolio identity, uniform one-method discipline — **AND the same run battery on the ES LEG (the verifier's fold): every cited ES run re-resolved under the ACTING TENANT + COMPLETED-status + run_type gates before FK stamping (the BT-1 `:517-529` P3-5-RLS-bypass shape) + the snapshot-purpose check on the consume path; PLUS a gate BT-1 itself lacks, closed here for BOTH legs: model-version UNIFORMITY across pairs (a series mixing code_versions of one declared identity refuses — the inherited silent-pass recorded and paid in-slice)**. **A NEW binder `run_es_backtest`** (a sibling of `run_var_backtest` sharing the extracted alignment/gate helpers — the shared-helper-not-copy discipline), NOT a branch inside the Kupiec binder: the Kupiec model's admit set stays byte-unchanged (its ES_HISTORICAL refusal branch SURVIVES verbatim — it correctly refuses the quantile test forever; a dated comment gains "the AS backtest ships at `risk.es_backtest`, BT-3"). Both build-in-request and consume-existing paths, both-modes ambiguity refusal from birth. |
| **D — Schema + model identity: ONE additive migration `0043` (nullable `es_value` echo on ENT-055); NEW model code `risk.es_backtest` v1 with registrar-stamped thresholds; NO new permission/ENT/audit code** | **Migration `0043_es_backtest`**: ENT-055 gains ONE nullable `es_value PreciseDecimal(28,6)` column (the per-pair ES-forecast echo — the evidence surface must carry what was tested against; `realized_pnl`/`var_value` already exist, the 0038 additive-nullable precedent; downgrade drops the column — **honesty per the verifier's fold: the ROWS survive representable (new metric_type literals in an unconstrained column — verified: no CHECK/ORM-validator anywhere on the downgrade path rejects them; no RLS sandwich needed) but the column drop DESTROYS every stored `es_value` echo, and a downgrade→re-upgrade cycle leaves ES_EXCEPTION_INDICATOR rows with NULL `es_value` — a state the binder can never produce, distinguishable from fresh runs; ALL of this stated in the migration docstring**). The pin serializer: `es_value` **EXCLUDED** from any pin content (the 0038/0040/0042 false-drift landmine — test-pinned both directions). **NEW model code `risk.es_backtest`** (the platform's **SIXTEENTH governed number** — a genuinely new statistic family under a registered model, reproducible from pins: the BT-1 mint precedent (`risk.var_backtest` was the 9th), NOT the RS-1/DS-2 version pattern; **consequence recorded, not silent: the ratified roadmap Part 2.11 calls CC-2 "the 16th-governed-number candidate" — if this OD ratifies, CC-2 becomes the SEVENTEENTH and the roadmap row gains a dated correction at the BT-3 closeout**): registered v1 with registrar-stamped declared identity — `significance=` ∈ {0.05, 0.0001} (the verdict significance; vocabulary registered), the Z2 critical constants (OD-B), `verdict_confidence=0.9750` (the sole verdict-eligible paired confidence, stamped so drift = a new version); `declared_es_backtest_parameters` via `sole_declared` (ambiguity + stray literals refused from birth — the RS-1 HIGH folded). Run family: **`ES_BACKTEST` REUSING `risk.run`/`risk.view`** (the BT-1/VAR_BACKTEST precedent; NO mint); `RISK.ES_BACKTEST_CREATE` reserved-not-emitted. Head 0042→0043 ⇒ the 21-file head-pin sweep + the synthetic tripwire advance (the recurring class, recorded). |
| **E — The Christoffersen independence leg SHIPS IN-SLICE as `risk.var_backtest` v2 (a declared convention — the RS-1/DS-2 version pattern); closed-form Markov LR, deterministic; the exact-LR extraction is the implementation obligation** | Christoffersen 1998's Markov test (paragraph-verified, Part 2: the 2×2 adjacent-day contingency table N1…N4; independence ⇔ the violation likelihood does not depend on yesterday's violation) is a VaR-EXCEPTION-SERIES test — it belongs to the EXISTING Kupiec family, not the ES family. It ships as **`risk.var_backtest` v2** with a registrar-stamped `independence=CHRISTOFFERSEN_MARKOV` convention (the RS-1/DS-2 declared-convention shape; v1 grandfathered via absent-convention, byte-preserved): the v2 emits, alongside the v1 rows, `LR_IND` and `LR_CC` summary rows (LR_cc = LR_uc + LR_ind, the standard decomposition) with decisions against the REGISTERED `CHI2_1DF_CRITICALS` (LR_ind, df=1) + a NEW registered `CHI2_2DF_CRITICALS` for LR_cc (df=2 — {0.05: 5.991465, 0.01: 9.210340}, textbook χ² constants at the three-route bar like the shipped df=1 pair). Fully deterministic closed-form — NO MC obstacle. **Degenerate-table honesty**: N_T = 0 or a degenerate 2×2 (no post-violation observations) ⇒ LR_ind undefined — no row, the absence derivable from the persisted exception-count row (the OD-A N=0 rule's sibling). **The v2's INHERITED limitation rows are enumerated for reword (the verifier's fold — two rows go FALSE on a version emitting LR_IND/LR_CC): `VAR_BACKTEST_LIMITATIONS` row 3 ("Kupiec POF only: no Christoffersen… a named BT-3 candidate", `bootstrap.py:1401-1404`) and row 5's closing clause (`:1420-1422`) — reworded for NEW registrations (no shipped dossier finding key matches those clauses — verified); the appraisal-frequency-pairing clause SURVIVES (still true).** **The `independence=` parse is the COUNTING tri-state, never bare `sole_declared`** (the verifier's fold naming the exact trap: `sole_declared` conflates absent with ambiguous — 0 rows ⇒ the v1 Kupiec grandfather; >1 rows ⇒ fail-closed refusal, WITH the two-rows-refusal golden in the suite; the RS-1 A1/DS-2 lesson applied at birth). **The implementation-step obligation (recorded, not waived): extract the exact LR_ind algebra from a written reproduction at implementation** (the primary is JSTOR-gated; the paragraph grade suffices for THIS record's ratification but NOT for shipping code — the numeric finder re-derives from the 2×2 MLEs independently, exact-rational). This discharges the twice-re-teed Christoffersen item at its homed destination — a third deferral would need a reason this record does not have. |
| **F — Demo stage 7 + docs: the tie-break fix (OQ-W7C-2), a paired sibling series, the INITIAL AWC for the new code, NO TRIGGERED (recorded honestly), the referent + rewords under the finding-key fence** | **The tie-break fix lands FIRST** (its own commit): `_latest_flagship_hs_row` ORDER BY → **`(window_end DESC, system_from DESC, calculation_run_id DESC)`** (the verifier's column correction: `VarResult` carries `system_from` from the IA mixin, NOT `created_at` — a client-side per-row `utcnow`, so no PG transaction-`now()` tie class; uuid stays as the final total-order leg — an identical-microsecond tie is theoretically uuid-decided, practically absent since the demo twins commit seconds apart, disclosed) + the dated note on the ES-HS-1 record ("the recorded 113.239146 demo value was selection-dependent under the shipped uuid4 tie-break" — the OQ-W7C-2 text) + a regression test. **Stage 7** (`demo/bt3_stage7.py`, suites `test_demo_stage7_bt3*` — alpha-sorts after stage6; CI step after stage 6, before the downgrade smoke): seeds a SHORT paired sibling series on the flagship book — N consecutive as-ofs (each pair needing its own ≥41-observation aligned window per the 0.9750 floor — the seed arithmetic recorded), each a (VaR-HS, ES-HS) pair sharing its snapshot at c=0.9750 (registering the 0.9750 ES-HS + VaR-HS versions — versions of EXISTING codes, no new code beyond `risk.es_backtest` + the `risk.var_backtest` v2) — TD-1-realistic marks, the exception fixture-searched and drift-tripwired; **the demo claim is the DOMAIN-GATE HONESTY, deliberately NOT a verdict** (the T-domain finding makes a live short-series verdict numerically wrong by construction — the DS-2 estimation-not-recovery reframe's sibling): stage 7 shows the evidence rows + the `ES_PAIR_COUNT` row + the mechanically-derivable no-verdict, while **the verdict leg itself is proven in the SUITE at the full (0.9750, n=250) domain on deterministic constructed fixtures**; ALSO runs the Christoffersen v2 on the flagship exception series (a live LR verdict — its domain is not T-bound). Files **INITIAL AWCs for ALL FOUR new versions** (the verifier's uniformity fold: `risk.es_backtest` v1 — TIER dossier per the MG-1 matrix, evidence = its own COMPLETED run, finding keys from its NEW rows fail-loud; the `risk.var_backtest` v2; AND the two 0.9750 demo versions — the RS-1 new-version-INITIAL precedent applied uniformly, not selectively). **The BT-3 tier dossier is a SEPARATE module constant (the ES_HS_TIER shape), NEVER added to `TIER_DOSSIERS`** — three suites pin `len(TIER_DOSSIERS) == 16` as a CONSTANT, sequence-independent (the verifier's pin-arithmetic fold). **NO TRIGGERED re-validation and the record says WHY** (the DS-2 honesty pattern): the ES-HS INITIAL's conditions are tail-resolution + factor-substrate — NEITHER names the backtest gap (census-verified); nothing closes-by-supersession; forcing one is the OQ-5-barred false ceremony. The tee's paper trail closes by REMEDIATION instead: the ES-HS limitation row "DELIBERATELY not backtestable v1…" is REWORDED for NEW registrations under the HG-1 fence — **with the CORRECT load-bearing invariant (the verifier's BT3-V-2): the filing is NOT one-time — every fresh-schema CI seed re-registers ES-HS from the reworded constant and re-runs the fail-loud key match — so the reworded row MUST preserve `"DELIBERATELY not backtestable v1"` as a unique substring** (the reword appends, never replaces the key phrase: "…DELIBERATELY not backtestable v1 by Kupiec/Basel; the Acerbi-Szekely backtest SHIPS at `risk.es_backtest` (BT-3)" — no "predate" clause, which would be false on post-BT-3 registrations), enforced by a conformance test asserting the key survives in the edited constant (the HG-1 per-searched-set fence shape). **NEW referent `05_analytics_methodologies/es_backtest_v1.md`** at per-source citation grades (Part 2's table verbatim: what is verified via which route, what remains unsighted); dated amendments: `var_backtesting_v1.md` (the Christoffersen v2 + the named-candidate rows' discharge), `var_historical_es_v1.md` (the tee's re-verification debt DISCHARGED — cite this record), the ES-HS-1 record (the OQ-W7C-2 dated note), `wave_7_close_review.md` untouched (history). |
| **G — The scope fence** | ONE migration (`0043`, additive-nullable only); ONE new model code (`risk.es_backtest`) + ONE new version of `risk.var_backtest` (v2) + the two 0.9750 demo versions of existing codes; **NO new permission / ENT / audit emission** (`RISK.ES_BACKTEST_CREATE` reserved-not-emitted; run families reuse `risk.run`/`risk.view`); `audit/service.py` FROZEN; the Kupiec binder + kernel byte-preserved EXCEPT the extracted shared helpers (behavior-preserving extraction under the P3-C1 golden-capture bar) + the one dated comment; the ES-HS/VaR-HS families byte-untouched (BT-3 only READS their rows; the 0.9750 registrations use the EXISTING registrars); `METRIC_TYPES`/the var_result serializer untouched; the ENT-055 pin serializer changed ONLY by the adjudicated `es_value` EXCLUSION (test-pinned); prior demo stages byte-untouched (the tie-break fix is stage-4 code — its own commit + regression, the ONE exception, OQ-W7C-2-mandated); FE: ONE deliberate touch (the verifier's fold — the OD-G-draft "zero changes" was inaccurate): ES_BACKTEST runs do NOT surface through the existing var-backtest GET (its resolver filters `run_type == VAR_BACKTEST`) — the NEW GET pair is their surface; and the FE `test_decision` column is hard-labeled "Kupiec" (`apps/frontend/src/api/types.ts:219`), false for v2 LR rows — the one-line relabel (family-neutral "Test decision") + its type-test is the enumerated FE exception. 4-finder review: adversarial (pairing/gates/N=0/degenerate-table/downgrade) · numeric (exact-rational Z̄1/Z̄2 + LR re-derivation vs independent implementations; the demo series re-derived from pins; fuzz over paired series) · doctrine (citation grades incl. the α-dependence scope; the rewords' surviving clauses; the no-TRIGGERED honesty; the verdict-eligibility narrowness) · scope-fence (clause-by-clause). |

## Part 2 — External benchmarks and primary sources (roadmap Part 4 rule 6 — the ratified fetch-first MUSTs, DISCHARGED 2026-07-19)

**Z̄2 (AS Test 2, the unconditional statistic) — VERIFIED VIA THREE INDEPENDENT ROUTES (upgraded from ES-HS-1's one).** (1) **Zeliade Systems whitepaper zwp-011 v2.0 (2020-12-16), §3.2.1, VERBATIM**: `Z2(e,v,x) = x·1{x+v<0}/(α·e) + 1`; `Z̄2(X) = (1/N)·Σ_d Z2(ÊS_d, V̂aR_d, X_d)`; `E_H0[Z̄2] = 0`; strictly increasing in v and e; `E_H1[Z̄2] < 0` under underestimation (independent consultancy; also reproduces the Moldenhauer-Pitera counterexamples). (2) **Fredriksson & Johansson, Lund University thesis (lup.lub.lu.se record 9024227)**: `Z2 = (1/(T·α))·Σ_t X_t·I_t/ES_{α,t} + 1` — algebraically identical (independent academic). (3) **Moldenhauer-Pitera arXiv:1709.01337v3 p.15 Eq. 6.2 + the null-expectation identity** (carried from ES-HS-1). AS's own configuration confirmed as the FRTB 2.5% tail (M-P: "the replacement of VaR at level 1% with ES at level 2.5%").

**Z̄1 (AS Test 1, the conditional statistic) — the VENDOR-NORMALIZED flag DISCHARGED: one independent + one vendor route + the settling identity.** (1) **Lund (independent)**: `Z1 = (1/N_T)·Σ_t X_t·I_t/ES_{α,t} + 1`, N_T = the VaR-exceedance count, I_t = 1{X_t < −VaR_t}. (2) **MathWorks `esbacktestbysim.conditional` (vendor — the ES-HS-1-era route)**: the same statistic ("Z_cond"; `E[Z_cond | NumFailures > 0] = 0`; one-sided; negative ⇒ underestimation) — its HTML render again reproduced the **'+1'-grouping corruption** the ES-HS-1 debt recorded (a reading placing '+1' inside the denominator). **(3) The null-expectation identity SETTLES the grouping**: with X negative on losses and ES > 0, `E[X | X < −VaR] = −ES` ⇒ `E[(1/N_T)·Σ X_t·I_t/ES_t] = −1` ⇒ the '+1' must sit OUTSIDE the sum to give `E[Z̄1]=0`; an inside-denominator '+1' provably breaks the null identity. GRADE: verified-via-independent-reproduction + identity; **the AS primary's own equation renderings remain unsighted** (Risk magazine paywall + MSCI form-gate persist) — disclosed, and immaterial: two sources + the identity over-determine the formula.

**The −0.70 / −1.8 thresholds — ATTRIBUTION SETTLED, VALUES AT THE THREE-ROUTE BAR, SCOPE SHARPENED (the material planning finding).** *(Route accounting, per the verifier's BT3-V-4 catch — the ES-HS-1 V1-class overclaim was caught here BEFORE ratification and repaired honestly: the VALUE routes are (1) the M-P flipped-sign attribution (carried), (2) Lund Table 1's independent reproduction, and (3) THIS PLANNING PASS'S OWN EXECUTED MC — seeded, offline, recorded in the verifier evidence: at a=0.025/T=250/N(0,1), 2M trials, the 5% left quantile = −0.7001 and the 0.01% quantile = −1.7793 with ±3σ bracket [−1.811, −1.755] — confirming −0.70 exactly and −1.8 at its published 2-significant-figure precision. Zeliade corroborates the MECHANISM/α-scope (≈−1.2 at a different α), not the values. Three routes on the values; the bar is met.)* **Lund Table 1** reproduces "Acerbi and Szekely's (2014) left tail critical values" — they are **TEST-2 (Z̄2)** critical values at significance **5% → −0.70** and **0.01% → −1.8**, identical at N(0,1) and t(ν=100), **degrading at low df** (t3: −0.82/−4.4; t5: −0.74/−2; t10: −0.71/−1.9) — the recorded "distribution-stability" is real but bounded (heavy tails move them). Lund's empirical part confirms the same pair as "the authors' suggestion" (matching M-P's flipped-sign attribution carried from ES-HS-1). **Zeliade §3.2.2 independently proves α-DEPENDENCE**: at α = 0.5%, φ = 5% the stable threshold is ≈ **−1.2** (500k simulations) — so the AS pair is bound to AS's α = 2.5% configuration and is **NOT transferable to the platform's shipped c = 0.95/0.99 ES runs**; correct thresholds for other tails require Monte-Carlo derivation (the TR-09 determinism obstacle — the recorded GLM class; never in-run). This finding DRIVES OD-B (verdicts only at 0.9750). Test-1 (Z̄1) critical values are DIFFERENT and distribution-UNSTABLE (Lund's table; the known Z1 weakness) — Z̄1 therefore ships as evidence-only, never a verdict (OD-A/B).

**Christoffersen 1998 — PARAGRAPH-GRADE DISCHARGED via two public sources.** (1) **Campbell, S., "A Review of Backtesting and Backtesting Procedures", Fed FEDS 2005-21 (federalreserve.gov, PUBLIC)**: the Markov test — the 2×2 contingency table of adjacent-day violations; independence ⇔ `N1/(N1+N3) = N2/(N2+N4)` (violation likelihood independent of yesterday's outcome); plus the Christoffersen-Pelletier 2004 duration variant (named, out of scope). (2) **Lund**: the same paragraph-grade description. The primary (Int. Econ. Review 39(4):841–862) is JSTOR-gated — the **exact LR_ind algebra is the recorded IMPLEMENTATION-step extraction obligation** (OD-E; a written reproduction + independent exact-rational re-derivation from the 2×2 MLEs; the χ²(1)/χ²(2) critical constants are textbook values at the three-route bar). `LR_cc = LR_uc + LR_ind` (the standard decomposition; Kupiec LR_uc already shipped).

**Regulatory footing (carried from the Wave-7 close, VERIFIED there):** FRTB keeps desk backtesting VaR-based while 97.5% ES is the capital measure — the missing ES backtest trails the ACADEMIC frontier (AS 2014; Kratz-Lok-McNeil), not the Basel floor; 0.9750 is the externally-anchored registrable ES confidence (MAR33.3, the ES-1 record). This is the honest framing for OD-B's narrow verdict.

## Part 3 — Census key facts (the load-bearing subset; full citations in Grounding)

1. The ES_HISTORICAL refusal branch (`var_backtest_service.py:295-303`) names this slice + the shared-snapshot pairing design VERBATIM — the tee's paper trail is code-anchored.
2. The BT-1 gate battery (alignment/injectivity/MV-chain/cross-portfolio) is reusable as-is; realized P&L = flow-adjusted DIETZ (`:269`); the exception indicator is strict `X + VaR < 0` (`var_backtest_kernel.py:53-56`) — EXACTLY AS's I_t.
3. `var_backtest_result.metric_type` is UNCONSTRAINED String(30) (models.py:380-381; migration 0033:85) — new metric types need NO migration (the BT-2 precedent); **but no ES-forecast echo column exists** (`realized_pnl`/`var_value` only, `:390-391`) — OD-D's migration is forced by the evidence surface, not the rows.
4. One ES-HS run = ONE ES row at ONE as-of bound to ONE snapshot (`eshs_stage4.py:238`); the paired-series shape is therefore N sibling pairs (OD-C), each sharing `input_snapshot_id` (live demo: `:231`).
5. The tie-break defect: `_latest_flagship_hs_row` `ORDER BY window_end DESC, calculation_run_id DESC` at `:176` (uuid4) — `eshs_stage4.py:162-186`; OQ-W7C-2 assigned the fix here.
6. `CHI2_1DF_CRITICALS` (`var_backtest_kernel.py:33-36`) is the registered-constant precedent OD-B/E extend; `declared_var_backtest_alpha` (`bootstrap.py:1435-1452`) the `sole_declared` gate template.
7. The ES-HS INITIAL carries NO backtest condition (`dossiers.py:615-623`) but its finding_keys include "DELIBERATELY not backtestable v1" (`:627`) — the key-match (`eshs_stage4.py:139-159`, substring vs the version's OWN rows, fail-loud on ≠1) re-runs on EVERY fresh-schema seed against the post-reword constant, so the reword must preserve the key substring (OD-F; the verifier's BT3-V-2 corrected invariant).
8. BT-2's estimate-age gates are forecast-leg (`var_service.py:466-563`) — BT-3 consumes gated forecasts unchanged; no age logic belongs in the backtest binder.
9. Head `0042`; 21 head-pin files + the synthetic tripwire re-pin at 0043 (the recurring class).
10. Demo count pins: stage-7 registering `risk.es_backtest` makes the tenant 19 codes AFTER stage 7 — the stage-4/5/6 suites' `len == 18` pins hold at their sequence points (CI order + alpha-sort both preserve run order); the stage-7 suite pins its own 19-code end state.
11. `make check` 1588 at head; the campaign's BT-1/BT-2 runs are validation evidence via `CALCULATION_RUN` rows (`campaign.py:883`) — the stage-7 INITIALs follow that shape.
12. House structure: OD table + OQ ratification + Part 2 benchmarks + Part 3 census + Part 5 readiness + Part 5.5 deviations + Part 6 dispositions.

## Part 4 — Open decisions (OQ-BT-3-1…7) — pending ratification

- **OQ-BT-3-1 — OD-A: the statistics** (Z̄2 primary + Z̄1 conditional evidence, exact three-route-verified forms; per-pair evidence rows + summary rows; the N_T = 0 no-row honesty; Z3/Z_MB named v2s; pure-Decimal deterministic). *Recommend APPROVE.*
- **OQ-BT-3-2 — OD-B: the verdict fork** (evidence-first; the registered AS pair gates ONLY 0.9750-confidence pairings — the α-dependence finding makes any wider adoption numerically wrong; non-0.9750 = evidence rows + echoed no-verdict reason; the MC-derived per-α table = the named v2 under a governed derivation record). **The Tier-3 headline — the user signs the narrow-verdict posture.** *Recommend APPROVE.*
- **OQ-BT-3-3 — OD-C: pairing + gates** (sibling pairs by identical `input_snapshot_id` + same confidence; the BT-1 battery verbatim on the VaR leg; a NEW binder sharing extracted helpers; the Kupiec admit set byte-unchanged). *Recommend APPROVE.*
- **OQ-BT-3-4 — OD-D: schema + identity** (ONE additive migration `0043` — nullable `es_value` echo, serializer-excluded; NEW code `risk.es_backtest` v1 — the 16th governed number IF ratified as governed (it is: reproducible-from-pins statistic rows under a registered model — the BT-1 precedent); registrar-stamped significance vocabulary + critical constants + `verdict_confidence=0.9750`; run family `ES_BACKTEST` reusing `risk.run`/`risk.view`). *Recommend APPROVE.*
- **OQ-BT-3-5 — OD-E: Christoffersen ships in-slice as `risk.var_backtest` v2** (declared `independence=CHRISTOFFERSEN_MARKOV`; LR_IND/LR_CC rows against registered χ² constants incl. the NEW df=2 pair; degenerate-table no-row honesty; the exact-LR extraction = the recorded implementation obligation; v1 grandfathered byte-preserved). The alternative — a THIRD deferral — has no recorded reason. *Recommend APPROVE.*
- **OQ-BT-3-6 — OD-F: stage 7 + docs** (the tie-break fix first, own commit + regression + the ES-HS-1 dated note; the paired 0.9750 sibling series; INITIALs for the new code + the v2; **NO TRIGGERED — census-proved no closable condition names the backtest gap** (the DS-2 honesty pattern); the tee-limitation reword for NEW registrations under the finding-key fence; the new referent at per-source grades + the three dated amendments). *Recommend APPROVE.*
- **OQ-BT-3-7 — OD-G: the scope fence** (as tabled — one migration, one new code + one new version + two demo versions, zero permission/ENT/audit mints, the enumerated byte-preservation set, the 4-finder composition). *Recommend APPROVE.*

## Part 5 — Implementation readiness gate

### Planning verification (the standing pre-ratification pass) — RAN 2026-07-19, THREE verifiers (numeric + adversarial + doctrine/census), 1 HIGH + 6 MED + 6 LOW — ALL FOLDED

The pass ran BEFORE ratification with EXECUTED checks (seeded MC, exact-Fraction identities, live PG census). **The core mathematics survived at full strength**: the Z̄1/Z̄2 forms and null identities confirmed (400k-trial MC, E[Z̄2] = +0.0003 ≈ 0; the corrupted inside-'+1' variant = −0.7003, ~6800σ from 0 — and numerically COINCIDING with the −0.70 critical, which is why the identity regression is mandatory); −0.70/−1.8 reproduced exactly at (a=0.025, T=250) — the third value route; the χ²(2) pair confirmed closed-form; every quoted source extraction faithful; several attack shapes proven structurally unreachable (window-as-identity excludes sibling window mismatch; run reuse collapses into the duplicate-window gate). **The folds that changed the ratified shape:**

1. **The HIGH (adversarial): the verdict criticals are T-DEPENDENT** (executed: 5% critical ≈ −3.68 at T=10, −2.39 at T=25, −1.57 at T=50, −0.70 only at T=250) — the OD-B verdict gained the FULL domain gate (0.9750 ∧ n_pairs=250, the BASEL_ZONE precedent) and **the stage-7 demo claim was REFRAMED from "verdict live" to DOMAIN-GATE HONESTY** (a short-series verdict would spuriously REJECT the flagship ES); the verdict leg proves out in the suite at full-domain fixtures.
2. **BT3-V-4 (doctrine, the V1-class catch): the threshold-values "three-route DISCHARGED" was an overclaim as drafted** (two independent value routes + a mechanism corroboration) — repaired by counting this pass's own executed seeded MC as the third value route (Part 2's route accounting).
3. **BT3-V-2 (doctrine): the finding-key fence argument named the WRONG invariant** — the filing re-runs on every fresh-schema seed, so the reword must PRESERVE the key substring (+ conformance test); the false "predate" clause dropped.
4. **The ES-leg run battery + cross-pair model-version uniformity** added to OD-C (the acting-tenant/COMPLETED/run_type re-resolution BT-1 applies to its own citations; the uniformity gate closes an inherited BT-1 silent-pass in-slice).
5. **The v2's inherited FALSE limitation rows** enumerated for reword (rows 3 + 5's Christoffersen clauses); **the `independence=` parse pinned to the COUNTING tri-state** with the two-rows-refusal golden (the `sole_declared` absent/ambiguous conflation trap, named at its shipped documentation site).
6. **The FE surface corrected** (ES runs do NOT surface via the existing GET — resolver run_type filter; the "Kupiec"-labeled `test_decision` column gains the one-line family-neutral relabel — OD-G's "zero FE changes" was inaccurate); **the tie-break column corrected** (`system_from`, not the nonexistent `created_at`); **the downgrade honesty sharpened** (rows survive; the `es_value` echoes are destroyed by the column drop — stated); **the TIER_DOSSIERS constant pin** (separate-constant rule made explicit); **T-dependence, one-sidedness (over-conservatism invisible), and the N_T=0 policy cross-reference** added as registered-limitation obligations; **INITIALs uniform across all four new versions**; the census line-cites re-grounded (content had verified verbatim; two files' numbers had drifted).

### The 4-finder review composition (OD-G, ratified shape)
Adversarial (pairing/gates/N=0/degenerate-table/downgrade/grandfather) · numeric (exact-rational Z̄1/Z̄2 + LR re-derivations against independent implementations; the stage-7 series re-derived from pins; fuzz) · doctrine (citation grades incl. α-dependence scope; rewords' surviving clauses; no-TRIGGERED honesty; verdict narrowness) · scope-fence (clause-by-clause).

## Part 5.5 — Implementation deviations from the ratified plan
*(populated at implementation)*

## Part 6 — Implementation review dispositions
*(populated after the 4-finder review)*
