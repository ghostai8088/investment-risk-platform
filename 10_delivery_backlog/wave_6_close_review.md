# Wave-6 Close Review — "the lifecycle, demonstrated"

| | |
|---|---|
| **Status** | **RATIFIED — OQ-W6C-1…6 ALL APPROVED 2026-07-17 (user: "Approve all"; merged as DRAFT via PR #52 = `9d561bf`, ratified immediately after — the Wave-5 close's exact pattern).** WAVE 6 IS CLOSED. **Wave 7 = fork A: HG-1 → ES-HS-1 → RS-1 → DS-2** (roadmap Part 2.10), with the two ratified riders (SC-2 the named pull-forward; commitment/capital-call the presumptive Wave-8 headline). |
| **Grounding** | Audited 2026-07-16/17 at `main` = `3eea70f` (merge of PR #51); all six Wave-6 anchors confirmed ancestors of HEAD via `git merge-base --is-ancestor` (MG-1 plan `91c39fa`/impl `46ce296` CI 5/5; FL-1 plan `ec01053` CI #303/impl `778891c` CI #314; MF-1 plan `0951784`/impl `96b766c` CI #323); main-tip CI re-queried live: 5/5 success. Migration head `0040` (NO Wave-6 migration — three consecutive zero-migration slices). Recorded battery: `make check` 1490; full local-PG fresh **1794/0/0** (scope note: the PG battery covers `packages/shared-python/tests` + `apps/backend/tests`; `apps/worker` remains the idle scaffold with its own trivial suite in `make check`). **Method**: a six-domain audit fan-out (per-slice MG-1/FL-1/MF-1, deferral register, closure stamps + status decay, executed counts), every finding adversarially verified by two independent refuters before counting (majority kills); two judgment passes (outward benchmark; destination/re-baseline) with each load-bearing claim separately attacked (16/16 survived); a completeness critic whose gaps were dispositioned below. 78 agents; every verdict file:line-cited or executed. |

## Part 1 — Did Wave 6 ship what was ratified?

**Yes — all three slices SHIPPED-AS-RATIFIED, and for the second consecutive close the audit found ZERO shipped-code defects.** Every surviving finding is record/doc/process hygiene.

- **MG-1** (14 confirmations): every hash/PR resolves and is an ancestor of HEAD; the tiering matrix, 2L `MODEL.TIER_ASSIGN` verb, write-time cadence ceilings with the untiered→TIER_1 fail-safe, the single substitution guard (the two-guards→one deviation properly recorded), and the 12-endpoint expired-exception teeth all shipped exactly as the record claims; the campaign matches its ratified dossier map today (set-equality lock live); both ride-alongs (the RD-3 NaN fix with ±Infinity regressions; the estimate-seam integration test) verified present; `audit/service.py` byte-untouched across the slice diff.
- **FL-1** (9 confirmations): the three-family registry dispatch, 3×3 predicate symmetry, coverage gate, FRTB vocab + single shared allow-list, both scenario gates still closed, active-risk refusal, the ENT-019 widening with the ENT-058 prose reservation, the FE drift trio still in place, and every Part 5.5/6 disposition true at head.
- **MF-1** (19 confirmations, ZERO surviving findings): both refusal guards, the single-commit shape, the version-grain resolution, the k=3 + promote-structural design at the shrunk eps amplitudes, the five freshly-drafted TRIGGERED conditions matching the ratified OD-D shape, `MF1_NON_INDEPENDENCE_DISCLOSURE` pointing at OQ-MF-1-3, the zero-'FL-1' grep re-executed clean, the `version_label` parameter with its non-empty guard and all pre-existing call sites byte-equivalent, the CI ordering pin, and the closeout stamps internally consistent — with the 63-run / 17-code / 7+11 end state RE-DERIVED from code, not read from the banner.

### The findings — ALL record/doc/process hygiene; dispositions at OQ-W6C-1

1. **The living demo tenant is currently unseeded in any durable database** (MED): the MF-1 fresh-schema battery discipline (its own Part 3 item 12) wiped local PG and nothing re-seeded it — every present-tense "the living tenant holds…" claim is currently true of no running database. Nothing is lost (fully re-creatable: `run_demo_campaign.py` + `run_demo_multifamily.py`); **fixed at this close** by re-seeding locally, and the standing practice is adopted: a schema-reset battery ends by re-seeding the living tenant (OQ-W6C-3).
2. **MG-1's record carries no forward supersession note** for the MF-1-changed tenant end state (LOW after verification — the record does carry three forward MF-1 pointers; what's missing is a dated "superseded at MF-1" annotation on the present-tense 16/6/10 counts). Dated note added at this close.
3. **The MG-1 policy doc (MG-16) still describes "two fail-closed write guards"** after guard 2 was removed as dead code (LOW). Corrected at this close.
4. **MF-1's record Part 4 header still said "pending ratification"** after ratification (MED — the recurring stamp class, caught by the standing checklist audit). Fixed at this close.
5. **RTM REQ-MDG-001 still defers tiering/validation to "P7"** (LOW — status decay). Flipped at this close with hash/CI evidence.
6. **`phase_status.md` and `next_actions.md` are two waves stale below their own banners, and no standing rule refreshes them** (MED). Decision at OQ-W6C-4.
7. **FL-1 record hygiene** (LOWs): MF-1 caught-and-fixed FL-1 doc-code debris (the `proxy_weight_service` module-docstring lines; the second missing `_BINDING_PREDICATES` constant) that FL-1's Part 6 had declared clear — recorded here as the correction; the Status "tip `ca07529`" is the pre-merge-sync tip, not the literal merged tip (`d3fcd96` was the merge-sync commit) — noted, hashes all resolve.
8. **Count-pinning residue** (LOWs, recorded not fixed): the campaign's 49-run total is derivation-only (the extension's 14 IS test-pinned) — trigger: pin it if the campaign ever changes; MG-1's closure stamps carry hash+PR but no CI run id (the provenance rule is satisfied by hash+PR; run ids adopted from FL-1 on); the campaign test's 16-code comment cites a grep recipe that now yields 17 (comment clarified at this close).
9. **Autonomy-mechanics text divergence** (LOW, recorded honestly): `CLAUDE.md`/the operating instructions describe Claude opening + merging PRs (the 2026-07-14 grant); operationally the auto-mode classifier blocks the REST create/merge on this repo, so the user performs both — the banner says so, the grant docs don't. One reconciling sentence added to `CLAUDE.md` at this close (the grant stands; the mechanism is user-mediated).
10. **Session-log completeness** (LOW, from the critic): `CC-Session-Logs/` is a git-tracked artifact class with no log for the Wave-6 build sessions (the `/compress` flow is user-triggered) — recorded as a fact, not a defect; no action.

## Part 2 — The deferral register, reconciled (44 items verified; 6 findings)

**PAID in Wave 6**: OD-032/OD-033 (cadence), the F3 exception scope-stretch, the RD-3 NaN bug, the estimate-seam test, the FE drift trio, the CURRENCY-only factor universe (the flagship condition itself), the multi-family prerequisite tee, the FL-1 hygiene debris (by MF-1), the demo-tenant CURRENCY-only premise (closed by the TRIGGERED records).

**The two ADVERSE findings, dispositioned:**

- **"Estimate staleness — residual" was TIPPED at the Wave-5 close *for Wave 6* and went UNPAID through the entire wave** (MED — zero "staleness" hits in all three Wave-6 records; re-verified at head: the promotion path has no age check, nothing expires a `proxy_mapping`, only the total/ES-total binders gate on age; MF-1's fresh promotions make it *currently* benign, which is exactly how staleness debt hides). **RE-OPENED and ASSIGNED to Wave-7 slice HG-1** (OQ-W6C-2) — the promotion-path age check ships there; the expire-a-mapping clause is a Tier-3 design question surfaced at HG-1 planning.
- **The Christoffersen (BT-3) leg carries no Wave-6 disposition** (LOW) — re-teed explicitly: it rides the ES-HS-1 planning fork (ship the Acerbi-Szekely ES backtest with or without the Christoffersen independence leg — an in-slice OQ there).

**Register hygiene fixed at this close**: roadmap Part 3 still listed branch protection (OD-050, PAID 2026-07-10) as a standing deferral — removed; the "vendor-beta/regression factor exposures (needs a loadings capture slice)" row was stale two ways post-FL-1 — rewritten (vendor-beta capture is the recorded FL-1 v2; the regression leg SHIPPED); the "VaR completions" theme bullet still listed ES and backtesting as future — rewritten to the true residue (√h multi-horizon, component/marginal, Monte-Carlo). **Recorded honestly**: P3-7 deferral C (lineage batching) was silently dropped from the Wave-5 close's register — restored here as OPEN-trigger-based (performance-triggered, no wave).

**Standing register carried to Wave 7** (each with its recorded home): MG-2 candidates (remediation/finding-closure lifecycle SR 26-2 §VI; overdue teeth — trigger NOT yet fired, earliest real overdue mid-2027; re-tier re-clamp; exception renewal bound); the OD-E cap-LIFTED pair (residual shrinkage/EWMA + desmoothing v2s — **sequenced at OQ-W6C-6, discharging the MF-1 contractual re-tee**); the four MF-1-unlocked candidates; active-risk loadings-aware denominator v2; the mixed-family single-family-per-consumer follow-on; the FL-1 registrar-constant CURRENCY wording (behavioral — HG-1); ENT-058 (paper); the raw-return pin kind (trigger unfired, now ×3 instantiations); vendor-beta capture; covariance v2s (wave-unassigned, distinct from the residual pair); computed factor returns; vol surface; PAR_RATE/interpolation; WORM/gitleaks; the retired campaign-suite capabilities (operational fact, not debt).

## Part 3 — Outward benchmark (rule 6(b), at wave scale)

*The full assessment was independently drafted and every load-bearing claim separately verified (8/8 survived). The scorecard: the Wave-4 ranked five-gap list is now fully consumed — the first close at which the previous benchmark list has no unshipped structural item.*

**(a) Model governance vs SR 26-2 / SS1/23 / EGIM — a regime, not a record.** Registration + tiering are real and citation-clean (the no-citable-multi-year-cadence negative fact honored; 730/1095 labeled HOUSE POLICY; TIER_1=365 on EGIM ¶90 voluntary adoption). All four validation types are now exercised — INITIAL, EXCEPTION, TRIGGERED, with real backtest runs as outcomes evidence — and **the flywheel is demonstrated end-to-end**: validate → condition names its remediation → remediate → TRIGGERED re-validation supersedes, with the TRIGGERED records refusing the false-closure shape (still-true riders remain conditions). Exceptions have teeth at the one shared seam, 12 endpoints. **What a supervisor would still find missing, ranked**: (1) no remediation/finding-closure lifecycle (findings carry text/severity/author and nothing else; closure is by supersession — SR 26-2 §VI's "tracking of recommendations, responses, and exceptions" is the nearest purely-supervisory gap, named MG-2); (2) overdue revalidation has no teeth AND nothing runs on a schedule (the worker still returns `{"status": "idle"}` — every calendar mechanism except lazy exception-expiry depends on a human asking); (3) person-level independence does not exist and cannot be coded away (disclosed in every record; only a second human retires it); (4) the recorded residue (unbounded renewals, no re-tier re-clamp, ratings in payloads not columns, the tiering approach itself unvalidated, the blanket default outside the demo tenant).

**(b) Market risk vs FRTB/Basel — the taxonomy is now load-bearing; the measures remain two rungs away.** Multi-family factor coverage is REAL (the "Sharpe 1992 with the Sharpe 1992 factors removed" indictment is dead — the regression runs on the asset-class palette its citation names). Still vocabulary-only or absent, stated plainly: liquidity horizons and partial ES by class (blocked behind ES-over-HS AND multi-day VaR); **ES is still a unit conversion** (`k_c·σ` — no empirical tail is ever consulted; ES-HS-1 is what changes that, and its one migration landmine is verified at the exact 0028 CHECK text: the constraint exempts only the literal `'VAR_HISTORICAL'`, so an `ES_HISTORICAL` row violates it in PG while passing the whole SQLite battery); no ES backtest (Acerbi-Szekely UNVERIFIED — fetch at planning); backtesting is Kupiec+Basel only (Christoffersen TIPPED); scenario shocks CURRENCY-only at both gates; the standing fidelity floors all registered (specific-risk=0 on plain VaR, diagonal residuals, joint normality, 1-day horizon, allocation-only active risk).

**(c) Private-asset math vs the literature — the 1991 baseline of a 2003–2004 field, gap registered.** Desmoothing is declared-α Geltner (the α=1 detour now ×3 instantiations; the v2 trigger unfired but the surface grows); the GLM MA(q)/Okunev-White v2s and the α confidence band stay named in the referent; residual risk is diagonal, equal-weighted, unshrunk (the Barra/Axioma-class v2s recorded at the registrar itself — **the MF-1 materiality-cap LIFT is the operative event**, discharged at OQ-W6C-6). The proxying leg is genuinely deep and now multi-family. The estimate-staleness residual is the register's one adverse carry (Part 2).

**(d) Nearest-gap ranking**: 1. **ES-HS-1** (S/M — ingredient-complete except the one scoped migration; the highest value-per-effort on the board, two closes running); 2. **the cap-lifted residual-EWMA/shrinkage + desmoothing-v2 pair** (S/M each; residual-EWMA the readier leg; primary-source work required — all vendor-practice citations UNVERIFIED); 3. **MG-2** (M — SR 26-2 §VI anchor; overdue-teeth trigger unfired until mid-2027); 4. **the scheduler substrate** (M/L — the platform's first genuinely architectural gap, third close running); 5. **non-CURRENCY scenario shocks** (M — the one unlocked candidate with a real methodology question; both gates verified closed with the two-gate inventory). Standing unranked: limits/breach (zero shipped code, third close running).

## Part 4 — Destination check (the thesis)

**Wave 6 moved the platform toward §2.1's destination on exactly the axis the Wave-5 close ranked first** — the CURRENCY-only cap on 9 of 14 numbers is removed end-to-end, and §2.3's governance pillar got the full lifecycle demonstrated rather than asserted. **But the movement was breadth and governance, not transformation fidelity** — MF-1's own record states "MF-1 ships NO new quantitative method," and the private-asset transformation math is byte-unchanged since Wave 4. **The largest remaining gap**: of thesis §2.1's three named transformation legs, desmoothing is v1-declared-α thin, proxying is at-or-near the bar, and **commitment/capital-call handling has zero code** — the last §2.1 clause with nothing built, quiet through six waves. Runners-up: ES as a unit conversion; the two operational gaps carried verbatim from the Wave-5 close (no scheduler; no limits). One demo-shaped honesty item: the genuinely-private (α=0.4 appraisal) chain still runs only on CURRENCY factors — the multi-family sleeve used public instruments via the α=1 detour; the private-on-multi-family capability exists but no demo exercises it (named an HG-1 ride-along).

## Part 5 — Process findings

1. **The closure-stamp checklist worked but leaked one site again** (MF-1's Part 4 "pending ratification" header — the fourth consecutive close finding a stamp-class miss, this time caught INSIDE the wave's own close audit rather than a later one). The checklist gains the item explicitly: *grep the record for "pending" at closeout*.
2. **The entry-point ledgers (`phase_status.md`, `next_actions.md`) have no refresh owner** — two waves stale below their banners while `current_state.md` stayed current. OQ-W6C-4 decides: refresh-at-wave-close becomes a checklist item, or the two files are stubbed into pointers at the banner (recommended: pointers — one honest entry point beats three half-stale ones).
3. **The living-tenant durability practice** (Part 1 finding 1) — adopted at OQ-W6C-3.
4. **Ultracode/workflow orchestration ran its first full close** (78 agents, 6 domains, 2-refuter verification, judge claim-checks, completeness critic): the verification layer killed 4 findings outright and downgraded 3 — plausible-but-wrong findings that would previously have reached this document; the critic surfaced 12 gaps of which 3 changed this review (the citation-hygiene register, the tee execution-verification, the session-log note). Two operational lessons recorded: the safety classifier can block individual workflow agents transiently (resume-from-cache recovers at zero re-spend), and a session usage limit mid-workflow loses only the un-cached tail (the journal preserves everything completed).

## Part 6 — Open decisions (OQ-W6C-1…6)

- **OQ-W6C-1 — The audit's findings are RECORDED and the close fixes are ACCEPTED as applied** (the re-seeded living tenant; MG-1's dated supersession note + MG-16 guard-count correction; MF-1's Part-4 header; the RTM REQ-MDG-001 flip; the roadmap Part-3 register hygiene ×3; the campaign-test comment; the CLAUDE.md autonomy-mechanics sentence). *Recommend APPROVE — zero shipped-code defects across three slices; every fix is record/doc hygiene.*
- **OQ-W6C-2 — The estimate-staleness residual is RE-OPENED as unpaid-through-its-assigned-wave and ASSIGNED to Wave-7 HG-1** (promotion-path age check; the expire-a-mapping clause surfaced as a Tier-3 question at HG-1 planning). *Recommend APPROVE — TIPPED debt that survives its assigned wave must escalate, not quietly re-tip; this is the register discipline's teeth.*
- **OQ-W6C-3 — The living-tenant durability practice is adopted**: a schema-reset battery ends by re-seeding the living tenant (campaign + extension CLIs); the tenant was re-seeded at this close. *Recommend APPROVE — the wave's flagship deliverable should exist somewhere durable, not only be re-creatable.*
- **OQ-W6C-4 — `phase_status.md` and `next_actions.md` become stub POINTERS to `current_state.md` + the roadmap** (the recommended fork), or gain a refresh-at-wave-close checklist item. *Recommend the stubs — the recovery path is `CLAUDE.md` → the banner → the roadmap; three parallel ledgers have now produced two waves of drift twice.*
- **OQ-W6C-5 — The MF-1 OD-E contractual re-tee is DISCHARGED by sequencing** (not by a why-not): residual-shrinkage/EWMA and desmoothing-v2 are slices 3 and 4 of the ratified Wave-7 fork below. *Recommend APPROVE — the cap's own condition (multi-family lands) is spent; a second deferral would need a reason the record doesn't have.*
- **OQ-W6C-6 — WAVE 7, the real fork.** Three genuinely distinct sequences were constructed and adversarially checked; the full argument-for/against text is preserved in the audit evidence, summarized here:
  - **(A) Deepen the mathematics — HG-1 (S) → ES-HS-1 (M) → RS-1 (S/M) → DS-2 (S/M).** The hygiene opener (the staleness age check; the registrar-constant debris; the private-on-multi-family demo ride-along), then ES over historical simulation (the named headline two closes running; the ONE `0041` migration widening the verified 0028 CHECK; the Acerbi-Szekely backtest as an in-slice fork with TIPPED Christoffersen), then residual shrinkage/EWMA, then estimated-α desmoothing. *For:* the only fork that discharges OD-E; attacks this close's own largest-gap verdict; the flagship AWCs' surviving riders (normality, the HS window floor, raw-sample σ_e) are precisely what these slices remediate — **the second lifecycle turn arrives as a by-product**; every slice ingredient-complete today. *Against:* zero new operational capability; scenario stays CURRENCY-only another wave; capital calls untouched again.
  - **(B) Cash the FRTB unlock — ES-HS-1 → SC-2 → LH-1 → PES-1.** *For:* the coherent IMA shape; SC-2 retires the last CURRENCY-capped number. *Against:* liquidity horizons/partial ES are capital arithmetic with no capital-reporting user — FL-1's own vocabulary-not-capital disclaimer warns against exactly this read; and it must record a why-not on OD-E one wave after the cap was lifted for the stated purpose of scheduling the pair.
  - **(C) Operate the platform — MG-2 → SCH-1 → LIM-1.** *For:* pays the two operational gaps + the supervisory §VI gap. *Against — close to disqualifying under house discipline:* MG-2's recorded trigger has NOT fired (earliest real overdue mid-2027) — building trigger-based items before their triggers fire is the drift the register exists to prevent; third governance-flavored opener in four waves; and the OD-E why-not problem again.
  - **RECOMMEND (A)**, with two riders written into the ratified text: **SC-2 is the named pull-forward if the wave underruns** (the one B slice with a genuine methodology question), and **the commitment/capital-call leg is named the PRESUMPTIVE WAVE-8 HEADLINE** so thesis §2.1's third leg stops being the permanently-quiet one. *The user ratifies.*

## Part 7 — Citation hygiene (carried for whoever plans Wave 7)

**VERIFIED-carried, safe to cite at recorded scope**: SR 26-2 §III/§V/§VI + SR 11-7:411 (MG-1); SS1/23 P1.3/P4.5(b)/P5.3(a)(i)/§2.13 (MG-1); EGIM §4.2 ¶90 (MG-1); OSFI E-23 (MG-1); BCBS d457 MAR30.17/32.4–32.27/33.3/33.9/33.12/33.14/33.15 + d219 + bcbs265 §1.4(i) (Wave-5 close/FL-1); Sharpe 1992 (PA-3/FL-1); Kupiec 1995 + the Basel 1996 zones (BT-1); Acerbi-Tasche + Embrechts-McNeil-Straumann (ES-1). **UNVERIFIED — fetch to paragraph before citing in ratifiable text**: Acerbi-Szekely 2014 (ES-HS-1 planning MUST); Christoffersen 1998; Getmansky-Lo-Makarov 2004 + Okunev-White 2003 at paragraph level (record-cited at PA-1, never extraction-verified — DS-2 planning MUST); Ledoit-Wolf + all Barra/Axioma/RiskMetrics vendor-practice claims (RS-1 planning MUST); EBA/GL/2018/04.
