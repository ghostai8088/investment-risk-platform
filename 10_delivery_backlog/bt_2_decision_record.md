# BT-2 Decision Record — total-series backtest + the σ_e estimate-age gate (Wave-5 slice 3)

| | |
|---|---|
| **Status** | **RATIFIED 2026-07-15** — OQ-BT-2-1…6 approved as recommended (user: "Proceed"), incl. OQ-BT-2-3's sub-fork **(i) the regression-span-end anchor** (the estimation snapshot's `as_of_valuation_date`, live-read via the pinned `input_snapshot_id`). Implementation gated separately behind `bt_2_implementation_plan.md`. |
| **Grounding** | Drafted 2026-07-15 against `main` HEAD `a9a00eb` (merge of PR #36 = VW-1 impl); every load-bearing claim re-verified by an adversarial grounding pass. The total exclusion is ONE code-side tuple (`risk/events.py:104` `METRIC_TYPES`; its comment: 'Do NOT "complete the vocabulary" here without ratifying a total-VaR backtest slice'); ENT-055 `var_backtest_result.var_metric_type` is an unconstrained `String(30)` (no CHECK, ORM or migration) — **a TOTAL backtest writes with ZERO schema change**; the kernel is method-agnostic (zero `metric_type` references); the API has NO method parameter (method is discovered from the pinned rows); the pairing gates (all-or-nothing alignment, MV-chain, cross-portfolio identity) apply to TOTAL rows unchanged (`exposure_run_id` NOT NULL on all methods). **`VAR_HORIZON_DAYS = 1` (`risk/bootstrap.py:492`) is verbatim-enforced in BOTH declared-identity parses** (plain+total share `declared_var_parameters`; historical's own `declared_hs_var_parameters` enforces the same) — every VaR is strictly a 1-day forecast; no multi-day horizon is registrable in v1. The PROXY_WEIGHT pin carries NO temporal fields except `system_from`; the economically honest age anchor is the estimation snapshot's `as_of_valuation_date` (= the regression span end, `snapshot/service.py:2096`), reachable by a tenant-scoped live read of the pinned `input_snapshot_id` — a true-append-only IA header ("created once, never mutated"), the PM-1 "drift-free by construction" precedent. `model_version` assumption rows are append-only and no shipped path appends to an existing version (`record_assumption`'s only call site is inside `register_model_version`; `resolve_or_register_version` returns existing rows untouched) — so a NEW required declared parameter forces a NEW `version_label` by convention-plus-mechanics. `var_backtesting_v1.md` carries NO immutability self-declaration (unlike the five self-declaring referents) — the dated-additive-amendment branch is operative (OD-E). Migration next free = `0040`. |
| **Mandate** | Roadmap Part 2.8 slice 3: "Extend outcomes analysis to `VAR_PARAMETRIC_TOTAL` — the pairing-semantics methodology (daily VaR vs appraisal-marked realized P&L) done honestly. Discharges PA-4 deferral 1." Wave-4 close: "S code / M methodology… The honest work is pairing semantics…, **not the constant swap**." PLUS the ratified VW-1 OQ-5 fork-A ride-along: "the σ_e-specific estimate-age GATE at total-VaR bind time… BT-2 pays the *mechanical-gate* half" (if declined here, the register item returns intact). |

## Part 1 — Decisions at a glance (OD-BT-2-A…G)

| OD | Decision |
|---|---|
| **A — Admit the method** | Add `METRIC_TYPE_VAR_PARAMETRIC_TOTAL` to the BACKTESTABLE `METRIC_TYPES` tuple (the ratified extension its own comment demands). Everything else in the pairing machinery is method-agnostic and UNTOUCHED: same ENT-055 table (zero schema change for the admit itself), same kernel, same API schemas, same alignment/MV-chain/identity/uniformity gates, same Basel domain gate (a non-(0.99, 250, 1-day) run simply omits the zone row — already the shipped semantics). One backtest run = ONE method (unchanged); a TOTAL backtest is a third parallel lane, not a joint test. |
| **B — The honest-pairing DOCTRINE (the "M methodology" half)** | The only mechanically available v1 pairing is **daily** (every VaR is a verbatim 1-day forecast — Grounding). Against an appraisal-marked book, a daily total-series backtest has a **TWO-SIDED pathology, by construction**: (i) between marks the private leg's realized P&L is identically zero (carried marks), while the forecast includes `σ_e,daily` every day — exceptions are mechanically SUPPRESSED (GLM 2004 Prop 1 direction; MAR32.27 explicitly bans smoothing low-frequency marks into daily P&L; BCBS 22's traffic-light rests on a stated independence assumption this series violates); (ii) ON a mark date, the entire appraisal period's private move lands in ONE day's P&L against a 1-day allowance (`σ_e,daily = σ_e,period/√d̄`) — exceptions CLUSTER on mark dates. The unconditional Kupiec count mixes the two opposite biases. **The doctrine, recorded as registered model limitations + the methodology amendment**: (1) on appraisal-marked books the unconditional Kupiec/Basel read is NOT valid evidence of adequacy in EITHER direction; its validity degrades with the private-leg share (a liquid-dominated book with a small proxied sleeve keeps a near-valid daily read); (2) the per-pair `EXCEPTION_INDICATOR` rows (dated, already emitted) are the honest evidence surface — mark-date clustering is VISIBLE in them; (3) the statistically-honest configurations are named follow-ups, NOT silently absent: an appraisal-frequency pairing requires the multi-day-horizon VaR capability (√t + calendar-aware conversion — the standing REQ-MKT-001 deferral; SR 11-7's "observation frequency that matches the forecast horizon" sentence and BCBS 22's own other-sample-sizes binomial recipe are the supervisory footing), and a Christoffersen independence leg is the clustering diagnostic (OD-F). SR 11-7's compensating-controls posture (documented limitation + other controls, never silent) is the citable frame — and VW-1's validation workflow is the compensating-control surface: a validator can now record exactly this concern as a finding/conditions on `risk.var.parametric_total`. |
| **C — The σ_e estimate-age GATE (the VW-1 fork-A ride-along, ACCEPTED)** | A NEW **required declared parameter `max_estimate_age_days`** on the total-VaR model, minted as **`risk.var.parametric_total` `v2`** (the immutable-version convention + mechanics force the new label — Grounding; the registrar's fixed label bumps v1→v2 and always declares the 5th parameter). **Age anchor**: `estimate_age_days = parsed.window_end − (the cited estimation run's PROXY_WEIGHT_INPUT snapshot header `as_of_valuation_date`)` — the regression span end, the economically honest "how old is the data under this σ_e"; read tenant-scoped via the PINNED `input_snapshot_id` (true-append-only IA header; the PM-1 drift-free-live-read precedent). Reference side = the pinned covariance `window_end` (the run's own economic as-of) — the comparison is reproducible across re-runs (AD-014 preserved). Gate: `age > max_estimate_age_days` (strict) ⇒ `VarInputError` pre-create refusal (422, zero run) **at bind-time adjudication in the total path** (AFTER `_parse_total_pins`; NOT inside `_adjudicate_total_proxies`, which stays deliberately pure/session-less — the gate needs the tenant-scoped snapshot read). On a GATED (v2) bind, a gone/cross-tenant estimation-snapshot header ⇒ refuse (the gate cannot evaluate — fail-closed); the ungated-v1 posture differs (OD-D). **Parse spec** (`declared_max_estimate_age_days`): absent → `None` (ungated — the v1 grandfather); present-but-malformed OR duplicated → `WrongModelVersionError` fail-closed (NOT `sole_declared`'s silent None-on-ambiguous — the family's fail-closed convention). **v1 grandfather posture**: existing immutable v1 registrations keep binding gate-UNGATED (recorded — v1 predates the policy; no shipped path appends a declaration to an existing version, and doing so would violate the immutability convention); every NEW registration is v2 and gated; the generic-mint bypass is CLOSED by the existing status-REGISTERED backstop (a generic `POST /models` version carries `status=None` and is refused at every bind before any declared-parameter parse). The API model-registration endpoint gains a **required** `max_estimate_age_days` field (no default — a staleness policy is consciously declared, the OD-P3-5-D philosophy). |
| **D — The age ECHO (migration `0040`)** | The register item's own text names recording: "…or even **records estimate age at total-VaR time**." Additive nullable `var_result.estimate_age_days` (Integer; NULL off the total family; on a total row = the **MAX** age across cited estimates — the binding constraint). **The echo is EVIDENCE, the gate is POLICY: the age is computed and echoed on EVERY total bind, including ungated grandfathered v1 binds.** The one asymmetry, recorded: on an ungated v1 bind whose estimation-snapshot header cannot be resolved, the echo is `NULL` and the bind proceeds — a grandfathered path must not gain a NEW refusal; on a gated v2 bind the same condition refuses (OD-C). A zero-proxied-instrument total run (the byte-invariance case) echoes `NULL` (no cited estimates — max over an empty set). Mechanics: the exact `0038 residual_variance` precedent, including its landmine rule — the column is **EXCLUDED from `var_result_content`** (adding a pin key false-drifts every historical pinned VAR component). Surfaced on the VaR row API read (additive nullable field). |
| **E — Registry accounting** | REUSE everything: `risk.run`/`risk.view` (no mint), `run_type=VAR_BACKTEST`, ENT-055 (amend the catalog row's "xor" clause), `RISK.*` stays reserved (no EVT), `model.inventory.register` for the v2 mint. REQ-MKT-005 stays In-Progress (advanced); REQ-MKT-001's total-series-backtest deferral clause DISCHARGED; PA-4 Part-6 deferral 1 DISCHARGED; the estimate-staleness register item's mechanical half PAID (closing the item — VW-1 paid the governance half). Methodology referents: **`var_parametric_total_v2.md`** minted (the v1 doc IS self-declared immutable; the carry-forward rule applies) for the v2 model; **`var_backtesting_v1.md` carries NO immutability self-declaration (grounding-verified)** — the operative branch is a **dated additive "Scope amendment (BT-2)" section** (three-method applicability + the OD-B doctrine + the BT-3 pointers), no versioned referent needed. |
| **F — Christoffersen: DEFERRED + the label collision FIXED** | BT-1's record/REQ/catalog texts call the independence-leg extension "BT-2" — a stale name now that the ratified Wave-5 BT-2 is the total-series slice. The independence leg (LR_ind χ²(1) + LR_cc χ²(2) over the same exception series) is genuinely the RIGHT diagnostic for the mark-date clustering OD-B names — but it is real scope (new kernel functions + χ²(2) criticals + new metric rows + a backtest-referent/version design question) and conflating it into this slice buries one methodology decision inside another. DEFER to its own slice (**BT-3 candidate**, now the named home of: independence/conditional-coverage legs + the appraisal-frequency pairing when multi-day VaR lands); rename the stale "BT-2" references (FIVE sites, grounding-verified: bootstrap limitation #3, REQ-MKT-005 backbone + RTM text, the ENT-055 catalog row, a dated cross-note in `bt_1_decision_record.md`, and `var_backtesting_v1.md`'s own Known-limitations line) at this slice's doc pass. |
| **G — Scope fence** | NO new permission/EVT/audit code; `audit/service.py` FROZEN; kernel untouched; NO change to plain/historical VaR paths or any non-total binder behavior (the age gate lives inside the total-only adjudication); migration `0040` = ONE additive nullable column; proportionate **4-finder review** (one adversarial on the OD-C gate mechanics + reproducibility, one numeric on any new goldens). BT-1 deferral B (registrar first-registration race) — trigger fires (BT-2 touches registrars): verify MD-H1's `resolve_or_insert` generalization already paid it and record the discharge. |

## Part 2 — External benchmark research (roadmap Part 4 rule 6; sources checked 2026-07-15)

Incremental on BT-1's citations (Kupiec 1995; BCBS 22 incl. the zone table; Christoffersen 1998 named-deferred; SR 11-7) and PA-1/PA-4's (Geltner 1993; Getmansky-Lo-Makarov 2004; Sharpe 1963):

- **BCBS MAR32** (Basel Framework, eff. 2023) — MAR32.25-27: hypothetical P&L = static revaluation; fees excluded; **"Smoothing of valuation adjustments that are not calculated daily is not allowed"** (32.27) — the direct supervisory anti-smoothing rule OD-B cites; MAR32.5(2): unavailable P&L/VaR COUNTS as an outlier (unavailability is penalized, not excused); MAR32.6: an outlier driven by a non-modellable risk factor may be disregarded with supervisory notification — the regulators' pattern for stale/unobservable factors is carve-out-and-capitalize (NMRF/RFET), never daily-backtest-the-smoothed-series. **VERIFIED (full text read via mirror).**
- **BCBS 22** (1996) — the traffic-light's **stated primary assumption**: "each day's test … is independent of the outcome of any of the others" (violated by construction here — the citable hook for OD-B); the type-1/type-2 power tables; and the **other-sample-sizes recipe**: "For other sample sizes, the boundaries should be deduced by calculating the binomial probabilities associated with true coverage of 99%" — BCBS-native machinery for a future appraisal-frequency configuration (at 99%/quarterly n≈20 the green zone is {0} — near-zero power; a 95% companion quantile is the practical fix, recorded in the doctrine as usage guidance). **VERIFIED (full text).**
- **SR 11-7** (2011; superseded by SR 26-2 as recorded at VW-1) — outcomes analysis "at an observation frequency that **matches the forecast horizon**" (the future appraisal-frequency configuration's footing — and, today, the reason a 1-day forecast pairs daily); "assessing any **clustering of exceptions**" as the beyond-counting test (the OD-F diagnostic's supervisory endorsement); the **compensating-controls** sentence (infeasible validation ⇒ documented + communicated + mitigated by other controls — the posture OD-B adopts, with VW-1 as the control surface). **VERIFIED (full text).**
- **Getmansky, Lo & Makarov (2004)**, *JFE* 74(3) — Proposition 1: observed smoothed returns preserve the mean, shrink variance by `Σθ_j² ≤ 1`, bias betas to zero, and induce positive autocorrelation — the formal basis for the suppressed-exceptions direction. **VERIFIED (working-paper full text).** **Honesty flag: no published paper was found on VaR backtest exception-count distortion for appraisal-marked assets specifically** — the mechanical two-sided argument in OD-B (zero-P&L days + mark-date jumps) is stated as first-principles construction, not literature.
- **Christoffersen (1998)**, *IER* 39(4) — LR_ind (2-state Markov, χ²(1)) + LR_cc = LR_pof + LR_ind (χ²(2)); mechanics **verified against the MathWorks implementation docs (primary for formulas); the paper itself paywalled (SECONDARY)**. Christoffersen & Pelletier (2004) duration-based tests noted as the stronger successor (SECONDARY, abstract only). Disposition: the OD-F deferral's content.
- **FRTB RFET/NMRF** (MAR31-adjacent) — a quarterly-appraised factor fails the ~24-observations/12-months eligibility by an order of magnitude; the Basel answer is exclusion-plus-capital, not daily backtesting. **SECONDARY (Risk.net/Clarus/Zanders; MAR31 text not fetched).** **UNVERIFIED and not relied on**: PRA SS1/23 post-model-adjustment detail (primary not fetched this pass; cited at VW-1 from a prior verified read for different content).

## Part 3 — Limitations carried forward + out of scope (recorded)

1. **The daily total-series read validity** (OD-B): the unconditional Kupiec/Basel verdict on an appraisal-marked book is registered NOT-VALID-EITHER-DIRECTION; per-pair indicator rows are the evidence surface. Carried as new registered limitation rows on NEW backtest registrations + the methodology amendment. **The operative consequence, stated plainly: every EXISTING `risk.var_backtest` registration will run TOTAL backtests carrying ZERO of the new doctrine limitation rows** (no shipped path appends to an existing version) — the doctrine reaches those tenants only via the amended methodology referent and, per OD-B, a VW-1 validation finding on the backtest model. Recorded.
2. **Appraisal-frequency pairing** requires multi-day-horizon VaR (√t + calendar-aware conversion) — remains with the standing REQ-MKT-001 calendar-aware deferral; named as the BT-3-candidate configuration.
3. **Christoffersen independence/conditional-coverage** — deferred to BT-3 (OD-F), the label collision fixed.
4. **The v1 total-model grandfather**: immutable `risk.var.parametric_total` v1 registrations keep binding WITHOUT the age gate; v2 is the gated identity; the registrar mints only v2 going forward. The ungated-v1 asymmetries are recorded at OD-C/OD-D (no new refusals on v1; age echoed best-effort, NULL when unresolvable). **The grandfather's governance SUNSET LEVER is VW-1 itself**: a 2L validator can REJECT the v1 model_version (a validation record with outcome REJECTED), which blocks all new v1 binds at the shared seam and forces migration to the gated v2 — the stand-down path exists today, tenant-by-tenant, without any BT-2 code.
5. **Look-ahead on the estimate side is NOT gated in v1**: an estimate whose regression span ends AFTER the VaR window end passes the (negative-)age check unrefused. A max-age gate is the ratified staleness item; a look-ahead gate is a different (new) concern — recorded, trigger = a real look-ahead incident or the BT-3 planning pass.
6. **BT-1's own carried limitations propagate** to the TOTAL lane unchanged: captured-holdings anti-conservative bias; ACTUAL-P&L-only (no hypothetical/clean leg); no Basel multiplier arithmetic; no p-values.
7. **The Basel zone off-domain omission** applies to every TOTAL run at non-(0.99, 250, 1) — already-shipped semantics, restated because TOTAL runs will commonly sit off-domain.
8. **`appraisal_days` coherence is doctrine, not gate**: the backtest binder cannot see the total model's declared `appraisal_days` (not pinned on var rows) — mark-cadence/horizon coherence stays a documented usage rule.

## Part 4 — Open decisions (OQ-BT-2-1…6) — pending ratification

- **OQ-BT-2-1 — OD-A: admit `VAR_PARAMETRIC_TOTAL` to the backtestable vocabulary** (the one-tuple mechanical extension; zero schema/kernel/API change). *Recommend APPROVE — the ratified mandate's mechanical half; the honest half is OQ-2.*
- **OQ-BT-2-2 — OD-B: the honest-pairing doctrine** — daily pairing ships as the only mechanically available v1; the TWO-SIDED pathology (suppressed off-mark + clustered on-mark) is registered as limitations + methodology text with the read-rule ("not valid evidence of adequacy in either direction on appraisal-marked books; validity degrades with private-leg share; the dated indicator rows are the evidence"); appraisal-frequency + independence-leg named as BT-3. *Recommend APPROVE — SR 11-7's compensating-controls posture with VW-1 as the control surface; the alternatives are worse: refusing to backtest at all fails the mandate and the SR 11-7 posture, and building multi-day VaR here doubles the slice into a VaR-methodology change.*
- **OQ-BT-2-3 — OD-C: the estimate-age gate, ACCEPTED as ratified ride-along** — required declared `max_estimate_age_days` on a NEW `risk.var.parametric_total` **v2** (v1 grandfathered gate-less, recorded); age anchor = regression span end (estimation snapshot `as_of_valuation_date`, live-read via the PINNED snapshot id — the PM-1 precedent) vs the pinned covariance `window_end`; refusal = pre-create 422. *Recommend APPROVE — the anchor is the economically honest one and the only alternative that is fully pin-computable (`system_from`) measures when the estimate was COMPUTED, not what data it saw.* **Sub-fork (pick one): anchor = (i) regression span end [recommended] or (ii) pinned `system_from` (zero live reads, knowledge-time semantics).**
- **OQ-BT-2-4 — OD-D: migration `0040`** — additive nullable `var_result.estimate_age_days` echo (max across cited estimates; excluded from the pin serializer per the 0038 landmine rule). *Recommend APPROVE — the register item's own text names "records estimate age at total-VaR time"; without the echo the item is only two-thirds paid.*
- **OQ-BT-2-5 — OD-F: Christoffersen deferred to BT-3 + the label-collision cleanup.** *Recommend APPROVE — it is the right diagnostic and the wrong slice; conflating it here buries one methodology ratification inside another.*
- **OQ-BT-2-6 — OD-G: the scope fence** (no mint/EVT; kernel untouched; non-total paths untouched; one-column migration; 4-finder review with an adversarial finder on the gate's reproducibility posture; the BT-1 deferral-B discharge check). *Recommend APPROVE.*

## Part 5 — Implementation readiness gate

Implementation starts only after OQ-BT-2-1…6 ratification, per `bt_2_implementation_plan.md`. Battery:
`make check` + full local-PG (schema-reset AND dirty-schema double-run) + `alembic check`/downgrade
smoke (0040↔0039) + CI-watch-to-green; any new PG legs wired into `ci.yml` in the SAME commit; the
per-family PG fixtures' grant lists checked against any new table reads (the VW-1 grant-leakage
lesson — the age gate reads only already-granted tables: `dataset_snapshot` via the snapshot
resolver; verify per suite). Recommended implementation model/effort: **Opus 4.8 / High** (the
mechanics are templated on BT-1/PA-4 exemplars; the doctrine text is already written here), 4-finder
review per OD-G.

## Part 5.5 — Implementation deviations from the ratified plan

*(recorded during the build)*

## Part 6 — Review dispositions + closure

*(written at fold/close per the house pattern)*
