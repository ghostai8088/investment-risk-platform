# BT-3 Implementation Plan — the AS ES backtest + the Christoffersen leg

*Companion to `bt_3_decision_record.md` (OD-BT-3-A…G). Status: DRAFT — pending the verifier pass + OQ ratification. Every step lands with its tests green; the battery gates are never waived.*

## Step sequence

1. **The tie-break fix (OQ-W7C-2), its OWN commit** — `demo/eshs_stage4.py::_latest_flagship_hs_row` ORDER BY → `(window_end DESC, system_from DESC, calculation_run_id DESC)` (the IA mixin's client-side timestamp — `created_at` does not exist on VarResult); a regression test pinning selection stability under a same-window_end twin; the dated OQ-W7C-2 note appended to `es_hs_1_decision_record.md` (additive). Battery: the stage-4 suites + `make check`.
2. **Kernel** (`risk/es_backtest_kernel.py`, NEW; + the Christoffersen additions to `var_backtest_kernel.py`): `as_z_statistics(pairs, tail_a)` → (Z̄2 always, Z̄1 iff N_T > 0, N_T, T) — pure Decimal prec-50, quantize 12dp, the strict indicator shared with `exception_indicator`; `christoffersen_lr_ind(table)` + `lr_cc = lr_uc + lr_ind` from the 2×2 counts (the implementation-step extraction: pin the LR_ind algebra from a written reproduction, then the numeric finder re-derives from the 2×2 MLEs exact-rationally); `CHI2_2DF_CRITICALS` registered beside the df=1 pair. Unit tests: exact-Fraction identities (E[Z̄]=0 fixtures both groupings — the inside-'+1' corruption must FAIL the identity test), N_T=0 and degenerate-table no-row paths, fuzz vs an independent float implementation at 1e-9.
3. **Registrars + gate** (`risk/bootstrap.py`): `ES_BACKTEST_MODEL_CODE = "risk.es_backtest"`; `register_es_backtest_model` (registrar-stamped `significance=` vocab {0.05, 0.0001}, the Z2 critical constants, `verdict_confidence=0.9750`; caller-suppliable label, per-label conflict 409); `declared_es_backtest_parameters` via `sole_declared` (ambiguity + stray-literal refusals from birth); `register_var_backtest_model` gains the v2 path (`independence=CHRISTOFFERSEN_MARKOV` stamped; v1 absent-convention grandfather byte-preserved — parse-back golden); the two limitation-row sets (α-dependence scope, low-df degradation, evidence-only-off-0.9750, N=0 honesty, single-lag Markov scope, degenerate-table rule) under the finding-key fence; the ES-HS tee-limitation reword for NEW registrations.
4. **Binder** (`risk/es_backtest_service.py`, NEW): extract the BT-1 alignment/MV-chain/cross-portfolio helpers into shared functions (behavior-preserving — golden-capture the Kupiec path BEFORE extraction, the P3-C1 bar); `run_es_backtest` (build + consume paths; the OD-C sibling-pair gate battery; per-pair `ES_EXCEPTION_INDICATOR` rows with `es_value` echo; `AS_Z2`/`AS_Z1`/`ES_PAIR_COUNT` summary rows; the (0.9750, n=250) DOMAIN-GATED verdict leg (the BASEL_ZONE gate shape); the ES-leg run battery (acting-tenant re-resolution + COMPLETED + run_type + snapshot purpose) + cross-pair model-version uniformity on BOTH legs; both-modes ambiguity refusal); the Christoffersen v2 rows in `run_var_backtest` (dispatch on the bound version's convention — the COUNTING tri-state parse, 0 ⇒ v1 grandfather / >1 ⇒ refuse, with the two-rows-refusal golden); new exception classes + the exact-type API map entries; the FE `test_decision` relabel (types.ts:219 "Kupiec" → family-neutral) + its type-test.
5. **Migration `0043_es_backtest`** — ENT-055 + nullable `es_value PreciseDecimal(28,6)`; docstring states downgrade semantics explicitly (the rows survive representable but the column drop DESTROYS the stored `es_value` echoes; post-cycle NULL echoes are distinguishable from fresh runs; no RLS sandwich); `alembic check` + downgrade/upgrade smoke; the 21-file head-pin sweep + synthetic tripwire 0043→0044; the pin-serializer `es_value` EXCLUSION test-pinned both directions.
6. **API** (`api/risk.py`): `POST /models/es-backtest`, `POST /es-backtests/runs`, the GET pair (the BT-1 endpoint shape); error-map entries; docstrings.
7. **Demo stage 7** (`demo/bt3_stage7.py` + `scripts/run_demo_bt3.py` + `test_demo_stage7_bt3(_pg)`): register the 0.9750 ES-HS + VaR-HS demo versions (existing registrars); seed the N-pair sibling series (TD-1-realistic; a window admitting ≥1 genuine exception so Z̄1 exists — fixture-searched, drift-tripwired); run the ES backtest (the DOMAIN-GATE HONESTY demo: evidence rows + ES_PAIR_COUNT + the derivable no-verdict — the verdict leg is proven in the SUITE at (0.9750, n=250) on deterministic constructed fixtures) + the Christoffersen v2 (live LR verdict); file the FOUR INITIALs (the new code — SEPARATE tier-dossier constant, never in TIER_DOSSIERS; the var_backtest v2; the two 0.9750 demo versions — the RS-1 precedent uniformly); NO TRIGGERED (recorded); CI step after stage 6, before the downgrade smoke; the stage-7 19-code end-state pin.
8. **Docs**: `es_backtest_v1.md` (NEW referent, Part-2 grades verbatim); dated amendments (`var_backtesting_v1.md` :67-73/:157-158, `var_historical_es_v1.md` :60-92 — the re-verification debt DISCHARGED); ENT-055 catalog dated amendment (the `es_value` column + the new metric types); **REQ discharges (the verifier's BT3-V-3): RTM REQ-MKT-001 (:50 — the "teed as BT-3" clause discharges in-slice, the F3 dated pattern) + RTM REQ-MKT-005 (:54) + backbone REQ-MKT-005 (:170) — a careful PARTIAL amendment: the Christoffersen clause discharges at OD-E, appraisal-frequency pairing stays open**; the roadmap Part 2.11 CC-2 row's dated 16th→17th correction (OD-D's recorded consequence).
9. **Battery + review**: `make check` + fresh-schema local PG (the full CI order incl. stage 7 + the downgrade smoke over the stage-7-extended tenant) + `alembic check`; the 4-finder review (OD-G composition); folds; Part 5.5/6; push; PR.

## Verification obligations carried from planning
- The Z̄1 '+1'-grouping identity test is MANDATORY in the suite (the corruption class must be mechanically unrepresentable) — **sharpened by the verifier's executed finding: the corrupted inside-denominator statistic evaluates to −ES/(ES+1) ≈ −0.7004 for N(0,1) at a=0.025, numerically COINCIDING with the −0.70 critical** — a corrupted implementation would emit plausible near-threshold values, not garbage; only the E[Z̄]=0 identity test catches it.
- The Christoffersen LR_ind extraction happens at step 2 with its source recorded in the referent at grade.
- The Kupiec-path golden captures are written GREEN before the helper extraction (step 4) and re-run after.
- Stage-7's exception count is fixture-searched (an all-green window makes Z̄1 vanish and the verdict vacuous — search for ≥1 exception at 0.9750, disclose the draw).
