# PM-1 Implementation Plan — the governed portfolio-return series (build contract)

| Field | Value |
|---|---|
| Status | **PLANNING ONLY — builds nothing.** Executes on explicit direction AFTER OQ-PM-1-1…10 ratify. Companion to `pm_1_decision_record.md` (OD-PM-1-A…K). |
| Templates | Binder: `var_service.py`/`active_risk_service.py` (N-run consumption extends P3-7's two-run shape). Snapshot: `build_var_snapshot` (IA-row pins) + the P2-3 FX-leg pin. Migration: `0030_active_risk`. Registrar: `register_active_risk_model` (code_version identity + declared-assumption identity for the flow set). Kernel: pure-Decimal, the P3-4/5/7 conventions. Permission mint: the P3-1 `risk.*` R-07 pattern in `entitlement/bootstrap.py` + `entitlement_sod_model.md`. |

## Step 0 — Pre-checks
HEAD ≥ `4e4648e`, CI green, migration head `0030`; re-verify the transaction columns (`txn_type` open vocab; `gross_amount`/`currency_code` nullable), the snapshot header's `as_of_valuation_date`, and where the bound-portfolio anchor lives on an EXPOSURE_INPUT snapshot (the pinned `COMPONENT_KIND_PORTFOLIO` row — the same-scope gate's anchor).

## Step 1 — Pure kernel (`perf/return_kernel.py`, NEW package `irp_shared/perf/`)
`compute_dietz_period(bmv, emv, flows: list[(day_offset, amount)], period_days) -> DietzEstimate` and `link_periods(returns) -> Decimal`. Decimal-50 context; `quantize_HALF_UP` 12dp; NO DB/IO. Refusal errors (`ReturnKernelError`) for `bmv ≤ 0`, denominator `≤ 0`, `period_days ≤ 0`. No-flow case reduces EXACTLY to `emv/bmv − 1` (test-pinned). Hand goldens (TD-1-plausible): e.g. BMV 1,000,000 / EMV 1,050,000, one +20,000 flow mid-period (w=0.5) → r = 30,000/1,010,000 = 0.029702970297; linking golden over 3 periods; sign tests for TRANSFER_OUT.

## Step 2 — Registrar + methodology doc
`register_portfolio_return_model` in `risk/bootstrap.py`-style module under `perf/` (or extend bootstrap — impl picks the clean home; NO cross-import risk↛perf): family `perf.return.twr` v1; identity = `code_version` + the DECLARED assumptions (`flow_types=TRANSFER_IN,TRANSFER_OUT`; `flow_timing=end_of_day`; `day_count=calendar`; `basis=gross_of_fees`) parsed back by the binder (the declared-parameter precedent — the flow set is NEVER a request parameter). Same-label conflicts 409; generic-minted twin → WrongModelVersionError. Methodology doc `portfolio_return_twr_v1.md` (Purpose / Inputs & data policy / Formulas & numerical standards / Assumptions / Validation / Governed-number contract / Known limitations — the captured-holdings-book limitation LOUD, OD-K).

## Step 3 — Snapshot support
`PURPOSE_RETURN_INPUT` + **`COMPONENT_KIND_TRANSACTION`** minted (models.py); `transaction_content(row)` serializer (full immutable column set — IA rows are byte-stable pins); `build_return_snapshot(session, *, acting_tenant, actor, exposure_run_ids: list, flow_txn_window…)` pinning: EVERY atom of EACH consumed run (EXPOSURE-kind, the P3-5 flavor), every transaction with `trade_date ∈ (first_boundary, last_boundary]` for the bound portfolio scope, FX legs for non-base in-set flow currencies at their trade dates. Empty runs / <2 runs fail closed (`ReturnSnapshotError`, its OWN class — the V8 lesson: never borrow another family's error). `_reresolve_content` TRANSACTION branch + verify support; export from `snapshot/__init__`. Binding predicate ≤ 50 chars (the guard now enforces at import).

## Step 4 — Migration `0031_portfolio_return` + model
`portfolio_return_result` (ENT-053): id/tenant/system_from; `calculation_run_id`/`input_snapshot_id`/`model_version_id` NOT-NULL FKs; `portfolio_id` FK; `metric_type String(30)` (`DIETZ_PERIOD`/`TWR_LINKED`); `period_start`/`period_end` Date; `begin_mv`/`end_mv`/`net_external_flow` `PreciseDecimal(28,6)`; `return_value PreciseDecimal(20,12)`; `n_flows`/`n_periods` Integer; `base_currency String(3)`. UNIQUE `(calculation_run_id, metric_type, period_start)`. IA append-only (APPEND_ONLY_TABLES + P0001 + ORM guard); symmetric FORCE RLS. Downgrade drops cleanly. SQLite create_all + PG upgrade/downgrade/`alembic check` smokes. **Head-bump sweep:** update every `get_current_head()==0030` assertion + the synthetic 0031→0032 guard (the P3-7 mechanical list).

## Step 5 — Binder (`perf/return_service.py`) on the scaffold (its EIGHTH consumer)
`run_portfolio_return(…, exposure_run_ids | snapshot_id)`: pre-create gates (OD-F — incl. same-portfolio-scope + same-base across runs via the pinned PORTFOLIO anchors; ordered unique boundary dates; the P3-C3 TypeError-inclusive malformed-pin wrapper FROM BIRTH); adjudicate pinned content uniformly on both paths; re-resolve ALL consumed run ids own-tenant pre-FK (the P3-5 lesson; N-run loop); compute sub-period Dietz + link via the kernel; ONE row per sub-period + the TWR_LINKED row; post-create magnitude gate → committed FAILED. `run_type='PORTFOLIO_RETURN'`. Readers: `list_portfolio_returns`/`resolve_*` (tenant-predicated). NEW `perf/queries.py` listing fenced to the ONE family (`GET /perf/runs` — the P3-C2 exposure-listing pattern), NOT in `RISK_RUN_TYPES`.

## Step 6 — Entitlement mint + API + FE
R-07 mint `perf.run`/`perf.view` in `entitlement/bootstrap.py` ROLE_TEMPLATES (+ `auditor_3l` in `perf.view`) + the `entitlement_sod_model.md` record — the ONLY permitted bootstrap change class (P3-1 precedent). API `apps/backend/api/perf.py`: POST `/perf/returns/runs`, GET run/row/`/perf/runs` listing; its OWN error map (PM-1 error classes; no borrowed families). FE: FAMILIES entry (`perf` permissionFamily; runType `PORTFOLIO_RETURN`; columns metric/period/begin/end/flow/return), source-switch wiring per P3-C2.

## Step 7 — Docs (same commit)
Canonical registry: ENT-053 mint + the ENT-014 consumer note; audit taxonomy: `PERF.RETURN_CREATE` reserved (next free decade, zero emissions test-asserted); entitlement doc: the mint record; **backbone/RTM: the ratified `REQ-PRF-001` row (OQ-9)** — In-Progress at this slice with the ex-post/backtesting/PA consumers named; roadmap: no status change until close.

## Step 8 — Tests (TD-1-plausible fixtures throughout)
Kernel goldens + property tests (no-flow ≡ pure TWR; linking; sign; pathology refusals). Full-stack SQLite: seed the P3-7-style upstream chain ×3 valuation dates → 3 exposure runs → capture TRANSFER_IN/OUT transactions → build path bindings + hand-verified golden series + linked total; consume≡build; **pin invariance under a transaction APPENDED after snapshot + an upstream exposure re-run** (the AD-014 proof for the new family); every OD-F refusal via the hand-mint vehicle (incl. NULL amount, missing FX leg, mixed scope, unordered boundaries, Dietz pathologies, JSON-null TypeError); reversal-pair netting; audit (CALC.* only, zero PERF.*); lineage; append-only; grain; entitlement parity (deny-by-default; `risk.view` does NOT grant `perf.view` — test both directions); methodology-doc sections; scope fences (no IRR/annualize/fee identifiers; no live reads in compute); migration head. PG suite (`test_portfolio_return_pg.py`) — RLS/no-context/forged-tenant/append-only/hybrid-set/cross-tenant-consume/audit-chain — **+ its ci.yml step in the SAME commit** (the P3-7 lesson, now standing). Endpoint suite + FE test.

## Validation + review + gates
`make check` + full-PG + downgrade smoke `0031→0030→head` + `fe-check` + diff fence (`audit/service.py` untouched; `entitlement/bootstrap.py` touched ONLY by the R-07 mint hunk; grep `_pg` files vs ci.yml). Then the **FULL ultrareview** (OD-I) + fold + revalidate + **HOLD for Tier-2 commit approval**.

**Model/effort:** implementation on **Opus 4.8 / high** (novel design settled here; templated end-to-end on P3-5/P3-7; the N-run generalization and the transaction pin are the only genuinely new shapes). The ultrareview runs as at P3-7.
