# CC-1 Implementation Plan — captured commitments / capital calls / distributions (Wave-8 slice 2)

Companion to `cc_1_decision_record.md` (OD-CC-1-A…H). Nine steps, one commit each; every step green under `make check` before the next. NO governed number / model code / run family / snapshot purpose / FE change; `audit/service.py` FROZEN; the perf/backtest external-flow chain byte-untouched (the OD-D fence).

## Step 1 — Entitlement mint (OD-B)
`entitlement/bootstrap.py`: add THREE codes with the dated R-07 mint comment (the PM-1 `perf.*` shape) — `("commitment.edit", …)` (the FR maker: commitment capture/supersede/correct — the position/valuation `.edit` doctrine), `("commitment.record", …)` (the IA maker: capital-call/distribution capture incl. reversals — the transaction `.record` doctrine), `("commitment.view", …)`. Grants — both makers: `data_steward`, `platform_admin`; view: `risk_analyst_1l`, `risk_manager_2l`, `data_steward`, `platform_admin` (**`auditor_3l` EXCLUDED** — captured-input precedent). Ratified parity test `test_commitment_permissions_grants_as_ratified` (both directions: holders exact; auditor excluded). SoD doc row + taxonomy activation land at step 8.

## Step 2 — Models + events (OD-A, OD-C, OD-F)
NEW package `packages/shared-python/src/irp_shared/private_capital/`:
- `models.py`: `Commitment` (PrimaryKey/Tenant/FullReproducible/Timestamp mixins; the FR trio `record_version`/`supersedes_id`/`restatement_reason` declared MANUALLY — the mixin supplies only the four temporal columns, the ProxyMapping shape; `portfolio_id`+`instrument_id` NOT-NULL FKs; `committed_amount PreciseDecimal(20,6)`; `currency_code String(3)`; `commitment_date Date`; partial-unique current-row `(tenant_id, portfolio_id, instrument_id) WHERE valid_to IS NULL AND system_to IS NULL`, `sqlite_where` included); `CapitalCall` + `Distribution` (PrimaryKey/Tenant/ImmutableAppendOnly — **NO Timestamp mixin**, the Transaction precedent (updated_* on a P0001-guarded table is dead from birth; DatasetSnapshot deviation recorded); **`portfolio_id` + `instrument_id` NOT-NULL FKs (the stable identity keys) + `commitment_version_id` provenance-only FK-less echo column** (OD-C); `event_date`; `amount PreciseDecimal(20,6)` — signed: ordinary rows strictly positive, reversal rows exact negations; `currency_code`; `call_type`/`distribution_type String(30)` vocab comments; `is_recallable Boolean NOT NULL server_default false` on Distribution ONLY; `reverses_id` nullable self-FK + partial-unique WHERE NOT NULL; `external_ref` nullable) + the `_block_mutation` ORM guards (the transaction listeners verbatim).
- **Register all three models in the `irp_shared/models.py` aggregator** (alembic `target_metadata` + SQLite `create_all` both read it — the verifier's MED; without it `alembic check` fails on CI).
- `events.py`: `PRIVATE.COMMITMENT_CREATE/UPDATE/CORRECTION`, `PRIVATE.CAPITAL_CALL_CREATE/REVERSE`, `PRIVATE.DISTRIBUTION_CREATE/REVERSE` (EVT-240; seven caller-side constants; DC-2 metadata-only).
Unit tests: mixin/columns/guards (SQLite; mutation attempts raise).

## Step 3 — Migration `0044_private_capital` (OD-G)
Three tables; `TENANT_SCOPED_TABLES = ("commitment", "capital_call", "distribution")` symmetric FORCE RLS loop (the 0034 template); `APPEND_ONLY_TABLES = ("capital_call", "distribution")` (`{table}_append_only` → `irp_prevent_mutation` triggers, the 0013 mechanics); `_IDENTIFIERS` ≤63 assert; partial-unique indexes (current-row; per-table `reverses_id`); `down_revision="0043_es_backtest"`; plain-DROP downgrade with the honest captured-rows-deleted docstring — **drop order: triggers + policies, then `capital_call`/`distribution` (the FK children), then `commitment`** (PG refuses dropping an FK-referenced parent). **Re-pin the 18 head-pin sites 0043→0044**; sweep the remaining "0043"-literal files for stale context. PG test: RLS bite (cross-tenant zero rows), append-only trigger bite (UPDATE/DELETE raise P0001), downgrade body. **ci.yml step for the new PG suite at the established placement (the DS-2 missing-CI-step lesson — check BEFORE the battery, not after).**

## Step 4 — Commitment service (OD-A, OD-D, OD-E)
`private_capital/commitment_service.py`: the PA-0 five-op set — `capture_commitment` (validators: strictly-positive finite `committed_amount` via `parse_strict_decimal` + NaN/Inf refusal; 3-letter `currency_code` shape; tenant-filtered `portfolio_id`/`instrument_id` re-resolution; NO asset_class gate — genericity, but the private-asset intent stated in the docstring + the OD-D read rule in the MODULE docstring), `supersede_commitment` (close-first, flush-before-insert — the exemplar order; `assert_supersede_effective_at` window-coherence guard; **currency chain-immutability refusal** — the new content's `currency_code` must equal the prior version's), `correct_commitment` (same currency refusal), `reconstruct_commitment_as_of` (both axes), `list_commitments`. Race-safe NOT_NULL-only DQ gate via `resolve_or_insert` (the PA-0 `_ensure_required_rule`/`_run_dq_gate` shape; NO economic RANGE). Exact-type error classes (`CommitmentValueError`, `CommitmentNotVisible`, `NoCurrentCommitment`, …). Audit `_emit` via the FROZEN `record_event` — per-op event grain (capture=CREATE; supersede=UPDATE+CREATE; correct=UPDATE+CORRECTION) with **`action="correct"` on the CORRECTION event and the before/after-SYMMETRIC correction payload (the two PA-0 review-fold lessons), both test-pinned**. Tests: full op set + supersede-inversion refusal + the currency-immutability refusals + DQ + audit payloads.

## Step 5 — Capital-flow services (OD-C, OD-D)
`private_capital/capital_flow_service.py`: `capture_capital_call` / `capture_distribution` (validators: strictly-positive finite amount on ordinary rows; a CURRENT commitment row must exist for the (portfolio, instrument) pair — fail-closed; currency MUST EQUAL that commitment's chain-immutable currency; vocab membership; `is_recallable` distribution-only; `commitment_version_id` stamped as the provenance echo) + reversal capture (`reverses_id` path: reversed row exists, same tenant + same (portfolio, instrument), not itself a reversal, not already reversed; **amount = the EXACT NEGATION** of the reversed amount; type/currency/event_date echoed byte-equal — FULL reversal only, the transaction sign convention verbatim) + `list_*`. Audit: CREATE events for ordinary captures, **REVERSE events for reversals** (payload carries `reverses_id`). The OD-D read rule in the module docstring. Tests: validators each direction, the negation arithmetic (Σ self-correction pinned), the reversal fence (double-reversal refused via the partial-unique race too), append-only immutability end-to-end.

## Step 6 — API router (OD-B, OD-C)
**First: hoist `_raise_mapped_write` from `api/marketdata.py` to a shared `api/` helper module and redirect `marketdata.py` by the ONE mechanical import (behavior byte-identical — the existing marketdata endpoint suites are the golden; the SANCTIONED fence exception).** Then NEW `apps/backend/src/irp_backend/api/private_capital.py`: commitment endpoints (capture/supersede/correct POST + reconstruct/list GET) gated `commitment.edit`/`commitment.view` + capital-call and distribution endpoints (capture POST incl. reversal body, list GET) gated `commitment.record`/`commitment.view`; per-family `_COMMITMENT_WRITE_ERRORS`/`_CAPITAL_FLOW_WRITE_ERRORS` exact-type maps through the hoisted helper; IntegrityError→409 only via `is_unique_violation`. **Router registration in `apps/backend/src/irp_backend/main.py`** (import + `app.include_router` — NOT `api/__init__.py`, which is an empty scaffold). The OD-D read rule in the call/distribution endpoint descriptions. Endpoint tests: 403 deny-by-default parity (auditor_3l on view = 403; each maker verb bites its own surface), 422 validator surfaces, 409 duplicate current-row + double-reversal, end-to-end capture→supersede→reconstruct.

## Step 7 — Demo stage 8 (OD-H)
`demo/cc1_stage8.py` + `scripts/run_demo_cc1.py` + `demo/__init__.py` exports + `test_demo_stage8_cc1.py` (SQLite) / `test_demo_stage8_cc1_pg.py` (PG; filename alpha-sorts after `stage7`): `PE-MERIDIAN-X` instrument (PRIVATE_EQUITY) + the 25M USD commitment on the flagship portfolio + 3 DRAWDOWN calls + 2 distributions (one recallable) + the reversal pair (mis-captured call reversed + recaptured). **Assert counts UNCHANGED: 19 codes / 34 validation records / 95 COMPLETED runs** (no model/AWC/run — the capture-only honesty pin). CI step after stage 7, before the downgrade smoke. Fixture realism per the standing rule.

## Step 8 — Docs
`canonical_data_model_standard.md`: ENT-015/ENT-016 cells → REALIZED (dated; grain, mixins, migration 0044, the OD-D read rule, the OD-E derived-not-stored note). `requirements_backbone.md` + RTM: REQ-PRV-001/002 → **In-Progress** dated cells (PRV-001 quotes its acceptance clause VERBATIM — "Unfunded exposure computed and aggregated" — as the OPEN clause → CC-2, and names the "Funded/unfunded test" leg open with it; PRV-002 discharged in-slice except "feed liquidity forecast" → CC-2). `audit_event_taxonomy.md`: EVT-240 ACTIVATED (dated). `06_security/entitlement_sod_model.md`: the `commitment.*` mint row (dated; grant sets; auditor exclusion). NO roadmap stamp (the closeout does it — the standing stamp-checklist).

## Step 9 — Battery + review + folds
`make check` (expect ~1627 + the new suites); fresh-schema local PG at head `0044` (ALL EIGHT demo suites in CI order; append-only trigger + RLS suites; downgrade-base + re-upgrade smoke; re-seed the living tenant eight-stage after). Then the standard 4-finder review (adversarial / numeric-light (amounts+reversal arithmetic) / doctrine (read-rule + REQ wording) / scope-fence vs the OD fence list), folds, Part 5.5/6 in the record.
