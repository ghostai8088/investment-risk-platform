# CC-2 Decision Record — the governed commitment-pacing projection (Wave-8 slice 3, the HEADLINE; the SEVENTEENTH governed number)

| | |
|---|---|
| **Status** | **DRAFTED 2026-07-20 on `main` = `1cdc95b` (post-CC-1 impl merge; the CC-1 closeout rides in parallel on `cc-1-closeout`; head migration `0044_private_capital`). Pending ratification (OQ-CC-2-1…7).** **The pre-ratification verifier pass RAN (three verifiers: census, adversarial design, doctrine): 2 HIGH + 16 MED + ~10 LOW, ALL FOLDED — the draft shape changed materially**: the period geometry adjudicated FUTURE-ONLY from the PINNED as-of (the adversarial F1 — a wall-clock age would break pin-reproducibility); the rule-7 as-of leg DELIVERED after being silently dropped in paraphrase (the doctrine H-1); quantize-then-roll pinned (provably value-affecting); the pair-scoped currency-checked mark anchor; past-L flipped to the pre-create refusal shape; the recallable-cap wording corrected (the disclosed state was unreachable); the Part-2 grades re-worded to per-element honesty (the yield floor = single-route + the grouping-discriminating golden mandated; R1 index-normalization disclosed; the concordance-vs-verbatim grade stated); REQ-PRV-001's 'aggregated' kept honestly OPEN; the builder homed in snapshot/service.py; rc_schedule domains widened to [0,1] with canonicalization; the fence completed. Sized **M/L** (roadmap Part 2.11 row 3). **The SEVENTEENTH governed number**: `pacing.commitment_projection` — the deterministic Takahashi-Alexander commitment-pacing projection over the CC-1 captured substrate. NEW ENT-059 result table (migration **0045**); NEW run family; NEW snapshot purpose; the permission fork resolved by recommendation at OQ-2. **The ratified fetch MUST is DISCHARGED (Part 2)** — the TA functional form verified via TWO independent equation-level reproductions + a structural attribution (the primary is GATED; the ES-HS-1 verified-via-reproduction precedent) — so the fallback rider does NOT fire; **the v1 IS the TA form. CC-2 mints NO numeric constant from TA: all five inputs are DECLARED per-version parameters; only the FUNCTIONAL FORM is TA's** (the BT-3 three-route VALUE bar is therefore not engaged; the transcription bar is met). |
| **Grounding** | Census (one thorough Explore pass, file:line-grounded — Part 1; ALL premises confirmed, zero contradictions). **Exemplars:** the multi-period governed number = PM-1 `perf.return.twr` (kernel `perf/return_kernel.py` — prec-50 `localcontext`, 12dp fraction quantum, HALF_UP; binder `perf/return_service.py` via **`execute_governed_run`** `calc/scaffold.py:58` — pre-create refusal vs post-create FAILED, rows built unwritten, ORIGIN lineage + COMPLETED in the scaffold; migration `0031` — IA result table, run-bound NOT-NULL FK trio `calculation_run_id`/`input_snapshot_id`/`model_version_id`, symmetric FORCE RLS, P0001 trigger). The multi-DECLARED-param registrar = `risk/bootstrap.py` `declared_var_parameters`/`declared_es_hs_parameters` (several prefixes → a params dataclass, cross-validated, fail-closed via `require_declared`, `model/assumptions.py:38-46`); the version-label discipline = PA-1 (`perf/bootstrap.py:375-378` — the label IS the identity key; distinct labels may share code_version). **Snapshot:** `PURPOSE_*` + the ENFORCED `SNAPSHOT_PURPOSES` allow-list (`snapshot/models.py:41-102`; the generic `build_snapshot` checks membership `service.py:268`, dedicated builders may bypass — the recent precedent); the pin serializer is append-only-extensible with duck-typed per-kind `*_content` functions (`serialize.py`; snapshot imports NO source package); the FR flavor excludes mutable close-out markers, the IA flavor pins the full immutable set; **the 0038 false-drift landmine** (`serialize.py:474-495`) constrains later additive columns, not greenfield functions. **The CC-1 substrate:** `current_commitment`/`reconstruct_commitment_as_of`/`list_commitments` (`private_capital/commitment_service.py:302/:577/:603`) + `list_capital_calls`/`list_distributions` (`capital_flow_service.py:607/:627` — the Σ-self-correcting docstring verbatim on `list_capital_calls`; both families' negation convention asserted at the write sites `:514/:571`); `is_recallable` captured as DATA with the interpretation deferred HERE (`models.py:166-167`; the CC-1 record `:26`: a recallable distribution "restores unfunded and may be re-drawn"); currency chain-immutable; funded/unfunded DERIVED-not-stored (OD-CC-1-E — the arithmetic is THIS slice's). NO import fence blocks a new module importing `private_capital` (nothing in risk/perf/calc/snapshot imports it; it is a leaf); the house peer-fence pattern = the AST import test (`test_portfolio_return.py:597`). **Permissions:** governed-output `.view` INCLUDES `auditor_3l` (the exposure/risk/perf precedent) while `commitment.view` EXCLUDES it (captured-input scope) — decisive for OQ-2. **ENT-059 FREE** (`04_data_model/canonical_data_model_standard.md:97`; the Part-3 mint process = the ENT-053/056/057 precedent). Head pins: **19** `get_current_head()=="0044_private_capital"` sites; CC-2 sweeps 19 + adds its own = 20. Demo: stage 8 seeded `PE-MERIDIAN-X` / `DEMO-GLOBAL` (25M USD committed 2025-06-30; calls 3M/3M/9M-reversed/4M → net called 10M; distributions 0.6M INCOME + 1.2M RETURN_OF_CAPITAL recallable → net 1.8M; `demo/cc1_stage8.py:58-65,173-219`); the stage-9 CI slot = after the stage-8 step, before the downgrade smoke; the `stage10` alpha-sort hazard recorded (zero-pad caveat). `audit/service.py` FROZEN. |
| **Mandate** | Roadmap Part 2.11 slice 3 (`delivery_roadmap.md:176`, ratified 2026-07-19 OQ-W7C-6 fork A): the HEADLINE — "deterministic closed-form pacing (rate-of-contribution × unfunded; bow-curve distributions; NAV roll-forward), NO optimizer... New ENT (next free canonical id...), new run family; the family/permission question is a named Tier-3 fork." The dated BT-3 correction makes this the **SEVENTEENTH** governed number. **The ratified planning MUST — fetch TA (JPM 28(2):90–100) to paragraph/equation level FIRST — is DISCHARGED (Part 2)**; the fallback rider (declared-parameter pacing v1 with TA the v2) does NOT fire. The ratified stage-8/9 demo rider: the PROJECTION half of the commitment walk lands on the stage-8 seeded commitment. Rule 7 (the read-shape DoD) applies: CC-2 is the FIRST governed number under it. |

## Part 1 — Census (file:line-grounded; premise checks)

1. **ENT-059 FREE; no pacing model exists.** Every `pacing|takahashi|unfunded` hit in code is a CC-1 forward-reference to this slice (`private_capital/__init__.py:5`, `models.py:17,166`, `capital_flow_service.py:376`, `demo/cc1_stage8.py:19,207`). Highest realized ENT = ENT-057; ENT-058 = the FL-1 paper reservation; migration head `0044`.
2. **The governed-run tail is the shared scaffold** (`calc/scaffold.py:58` `execute_governed_run`): create_run → RUNNING → the snapshot→run DEPENDS_ON edge BEFORE the gate → compute → the presence gate → FAILED-with-reason OR rows + per-row ORIGIN edges + COMPLETED. The PM-1 binder is the canonical caller (`perf/return_service.py:613-629`). CC-2 uses it, never hand-rolls.
3. **Failure model** (the PM-1 shape, `return_service.py:31-53`): pre-create refusal (unregistered model, wrong-purpose snapshot, ill-formed pin — zero run/rows) vs post-create FAILED (column-legal-but-extreme magnitudes — committed FAILED run, zero rows, named reason).
4. **Declared-param discipline**: prefixes + strict regexes + `require_declared` parse-back + domain/cross-field checks in a frozen dataclass (`risk/bootstrap.py:702/:1121`); ambiguity refused (the RS-1 lesson: >1 row never collapses into a default); the registrar appends `f"{PREFIX}{value}"` assumption rows; LIMITATIONS close with the standard UNVALIDATED clause; finding keys must be uniquely-matching substrings of registered rows (`demo/dossiers.py:222-230`).
5. **Snapshot mechanics**: dedicated builders hardcode their purpose (the `build_return_snapshot` precedent) and may bypass the `SNAPSHOT_PURPOSES` tuple, but two live purposes (`PROXY_WEIGHT_INPUT`, `RESIDUAL_SHRINKAGE_INPUT`) are absent from it — a recorded inconsistency CC-2 does not repeat: its purpose JOINS the tuple (OD-D). New component kinds + content functions are greenfield (no historical pins to falsify).
6. **The substrate semantics CC-2 must honor**: Σ over event rows is self-correcting (reversals included, no exclusion contract); recallable distributions restore unfunded (the CC-1 record's stated semantic, interpreted HERE); the (portfolio, instrument) pair is the stable commitment identity; the commitment's chain-immutable `currency_code` denominates every projected amount.
7. **Rule 7** (`delivery_roadmap.md:229-237`): CC-2 ships the entity/time-centric reads IN-SLICE — the portfolio/instrument-filtered projection list AND **the platform's FIRST latest-resolver** (a projection family has a meaningful "current").
8. **Counts pre-slice**: 16 governed numbers / 19 codes / 34 records / 95 runs. CC-2 moves them to **17 / 20 / 35 / 96+** (the deliberate contrast with CC-1's pinned-unchanged).

## Part 2 — External grounding (rule-6): the TA fetch MUST — DISCHARGED, with per-source grades

**The primary is GATED** (JPM 28(2):90–100, `jpm.pm-research.com/content/28/2/90` — paywalled; also ProQuest/ResearchGate gated). Per the ES-HS-1/AS-2014 precedent, the functional form is **verified via independent reproduction** — graded:

| Route | Source | Grade | What it verifies |
|---|---|---|---|
| R1 | Jaeckel 2023 (`christoph-jaeckel.com/posts/2023-02-05-the-takahashi-and-alexander-model/`) — a full worked reproduction | **EQUATION-level, INDEX-NORMALIZED here** (M-2): R1's own forms are `C_t = UC_{t−1} × RC(Age_{t−1})`, `D_t = NAV_{t−1} × (1+G_t) × RD(Age_{t−1}, bow, L)` — **time-varying `G_t` = R1's disclosed Jeet modification; the v1's FIXED declared G is the TA-ORIGINAL posture**; NO yield floor appears in R1 | the core recursion (calls from unfunded; power-law RD; the NAV roll-forward); bow=1 ⇒ linear |
| R2 | Tamarix, "The TA Model: A Practical Guide" (`tamarix.tech/insights/the-takahashi-alexander-model-a-practical-guide`) | EQUATION-level; **the SOLE equation-level route for the yield floor** (M-3): the fetched rendering's parenthesization is imperfect (`max⁡(Yield, Fraction of Fund Life[t]) ^ Bow )` — the `max(Y, x^B)` vs `max(Y, x)^B` grouping is a render artifact of the BT-3 '+1' class; **the two groupings COINCIDE at t=L (both 1) and diverge mid-life whenever Y > (t/L)^B — a MANDATORY discriminating kernel golden pins the adopted `max(Y, (t/L)^B)` grouping at a divergent point** (the mechanical defense) | `Calls[t] = Unfunded[t−1] × Call Rate`; `Unfunded = Commitment − Σcalls`; the yield-floored RD; `Distributions[t] = Open NAV[t] × RD[t]`; the same NAV recursion |
| R3 | Luxenberg & Boyd, arXiv 2207.07767 (peer literature, `[TA02]` attribution §2/§3/§8) | STRUCTURAL | "calls are a time-varying fraction of uncalled commitments... distributions are a time-varying fraction of the illiquid asset value, and returns are constant" — a deterministic linear system; the vintage-age extension named |
| R4 | FRG Risk 2019 (`frgrisk.com/.../Macroeconomic-Effects-...pdf`) | INPUT-LIST | the original's inputs verbatim: "rate of contribution, capital commitment, life of the fund, a factor describing changes in the rate of distribution over time, annual growth rate, **and yield**" — corroborating the yield floor as ORIGINAL |

**The verified v1 functional form** (R1+R2 concordant on the CORE recursion; **the yield floor is a SINGLE-equation-route element** — R2, with EXISTENCE corroborated by R4's input list and ABSENT from R1 (per-element honesty, M-3); R3 the structural check; **no route in the set is a verbatim equation-number-attributed quote — the basis is CONCORDANCE, a grade below ES-HS-1's M-P verbatim reproduction, stated as such**): per period t = 1..L from the vintage anchor,
`C(t) = RC(t) × Unfunded(t−1)`; `Unfunded(t) = Unfunded(t−1) − C(t) [+ recallable distributions realized to date at t=0 seeding]`; `RD(t) = max(Y, (t/L)^B)`; `D(t) = RD(t) × NAV(t−1) × (1+G)`; `NAV(t) = NAV(t−1) × (1+G) + C(t) − D(t)`. **Terminal honesty: RD(L) = max(Y, 1) = 1 falls out of the ADOPTED formula** (support: R2 + the formula itself — R1's Age_{t−1} indexing would reach 1 at L+1, so the terminal identity rests on the adopted t-indexing, stated). Determinism note (L-2, worded to the spec): `(t/L)^B` with non-integral B under a FIXED prec-50 context is **deterministic for identical inputs+context+library**; the General Decimal Arithmetic spec grades non-integral power "almost always correctly rounded" (≤1 ulp) — **determinism, not correct rounding, is the TR-09 requirement**; the residual cross-library-version caveat is disclosed in the referent (and near-surely absorbed by the 6dp money quantize). No optimizer, no iteration — the TR-09 obstacle does not arise.
**What is NOT verified and deliberately NOT used**: TA's worked EXAMPLE parameter values (RC schedules, L, B, G, Y choices) — unrouted, and IRRELEVANT: **every one is a DECLARED parameter here, never a minted constant**. The Jeet enhancement (SSRN 4819761 — fetch 403) stays the recorded v2 register. The residual disclosed: the primary itself remains unread (the reproduction-concordance basis is stated in the referent, per-source grades included — the AS-2014 shape). **The rider adjudication nuance (L-1): the rider's substance is NAMING-ONLY for this slice — because CC-2 mints no TA constant, the degraded declared-parameter v1 would be functionally identical minus the `functional_form=TAKAHASHI_ALEXANDER` attribution; the citation bar here governs the NAME, and it is met at the disclosed concordance grade.**

## Part 3 — Design (OD-CC-2-A…H)

### OD-A — ENT-059 `pacing_projection_result` + migration 0045 (Tier-3 rider on OQ-1)

MINTED here (the Part-3 mint process; the next free canonical id). ONE IA append-only result table (the ENT-053 shape): run-bound NOT-NULL FK trio (`calculation_run_id`, `input_snapshot_id`, `model_version_id`) + provenance `portfolio_id`/`instrument_id` FKs; grain **(calculation_run_id, period_index)** with `period_index` = fund age in periods (1..L); columns `period_start`/`period_end Date` (derived from the vintage anchor at ANNUAL periodicity), `projected_call`, `projected_distribution`, `projected_nav`, `unfunded_end` — all money `PreciseDecimal(28,6)` — and `currency_code String(3)` echoing the commitment's chain-immutable currency. NO summary row (totals are Σ-derivable; one row shape, no `metric_type` vocab needed in v1 — a future variant re-opens it additively). Migration **0045**: the 0031 IA/RLS template + the ≤63-char identifier assert (the P3-8/BT-1-era addition, present in 0043/0044 — not literally in 0031); 19 head-pin re-pins + the new suite's own = 20.

### OD-B — The model: `pacing.commitment_projection` v1 = the verified TA form; FIVE declared parameters (OQ-3)

Registered code **`pacing.commitment_projection`**, version `v1` (caller-suppliable `version_label`, the MF-1/PA-1 discipline). Declared identity (the multi-param `EsHsParameters` shape; every prefix strict-regex + domain-checked + cross-validated; ambiguity refused): **`rc_schedule=`** (comma-separated per-age rates, e.g. `0.25,0.333,0.5` — the LAST value applies to all later ages; **each ∈ [0,1] (F10: RC=0 is LEGAL — 'investment period over' is a standard practitioner declaration; an all-zero schedule is legal-but-degenerate, disclosed)**; length ≥ 1 and ≤ L; the regex refuses empty/trailing-comma; **the declared string is CANONICALIZED** (each rate quantized to a fixed 6dp form — the ES-HS-1 confidence_key precedent — so `0.25` vs `0.250` cannot mint distinct identities)), **`fund_life=`** (integer L ≥ 1, the projection horizon in ANNUAL periods from the commitment date — the vintage anchor), **`bow=`** (B > 0), **`growth=`** (per-period G > −1), **`yield_floor=`** (0 ≤ Y ≤ 1; Y=1 legal-but-degenerate — full distribution every period, disclosed). Plus the form marker assumption `functional_form=TAKAHASHI_ALEXANDER` (identity, not a number). **NO minted numeric constants** — the registered rows carry the FORM + the declared values the registrar was given; different parameterizations = different versions (labels are the identity key). ASSUMPTIONS/LIMITATIONS state: the deterministic-single-path nature (no scenarios/randomness — the Jeet stochastic enhancement is the named v2); the ANNUAL-periodicity v1; the yield-floor semantics; the terminal RD(L)=1; the NAV-seed policy (OD-C); the recallable interpretation (OD-C); the one-sided dependence on declared parameters (a mis-declared G propagates — the declared-α rider shape, honestly restated).

### OD-C — The kernel + the projection anchor semantics (OQ-4)

NEW peer package **`irp_shared/pacing/`** (kernel, service, bootstrap, events, models) — CC-2 is neither a risk nor a performance number; the private-capital domain gets its own governed-number home. **Import fences (the verifier's F7 correction): `pacing` imports `snapshot`/`calc`/`model`/`db` rails ONLY — it imports `private_capital` NOWHERE** (the binder reads the PIN, the rule-7 reads join pacing's OWN rows; only the snapshot BUILDER touches live captured rows, and it is homed in `snapshot/service.py` per OD-D); `risk`/`perf` never import `pacing` and vice versa (AST fence tests both directions).

Kernel: pure Decimal prec-50 `localcontext`, HALF_UP; frozen dataclass rows; `PacingKernelError` with stable reason slugs. **The quantization convention (the verifier's F6 — provably value-affecting, so pinned here): QUANTIZE-THEN-ROLL** — each period's `C(t)`, `D(t)`, `NAV(t)` is quantized to the 6dp money quantum as computed and the NEXT period rolls forward from the QUANTIZED values (the PM-1 precedent: the binder links already-quantized sub-period values) — so the PERSISTED rows satisfy the recursion identities EXACTLY at 6dp (row-level auditability: `NAV(t) = NAV(t−1)(1+G) + C(t) − D(t)` re-checks from the persisted echoes; the exact-Fraction cross-check models the quantization or uses quantization-exact parameters).

**THE PERIOD GEOMETRY (the verifier's F1 HIGH — adjudicated): the projection is FUTURE-ONLY.** Rows cover `t = current_age+1 .. L` with `period_index` = fund AGE (never a projection-step counter); realized periods are NEVER re-projected — the anchor state already nets them. **`current_age` = complete ANNUAL periods elapsed from `commitment_date` to the snapshot's pinned `as_of_valuation_date`** — deterministic FROM THE PIN (a wall-clock age would break pin-reproducibility, the AD-014 invariant; `as_of` joins `PacingAnchor`). RC and RD stay AGE-indexed: `RC(t)` positions into the declared schedule by age; `RD(t) = max(Y, (t/L)^B)` at t = age. The in-progress (partial) period is projected whole with realized flows already netted into the anchor — a LIMITATIONS row. Period dates: period t spans `[vintage + (t−1) years, vintage + t years)` (anniversary convention; the Feb-29 vintage rolls to Feb-28 in non-leap years — kernel-tested). "Past L" ⇔ `current_age ≥ L` ⇒ **PRE-CREATE refusal** (`PacingInputError` — "commitment past fund life, nothing to project"; the verifier's F8: the house nothing-to-X precedent is the pre-create refusal (`es_backtest_service.py:207` "nothing to backtest"), not a FAILED run). **The mid-life re-anchoring disclosure (F1 rider): seeding Unfunded/NAV from REALIZED actuals is OUR adaptation** — the R1/R2 reproductions verify the from-inception TA recursion; the referent states this qualifier on the "v1 IS the TA form" claim explicitly.

**The anchor arithmetic (all computed from the PIN):** `PaidIn(0) = Σ(call amounts)` — **over ALL call rows regardless of `call_type` (F5, DECLARED): DRAWDOWN, EQUALIZATION and FEE all consume unfunded in v1** (the inside-commitment-fee convention; a LIMITATIONS row; an equalization REFUND is representable only as a CC-1 reversal — the inherited limit, disclosed); reversals self-correct. `Unfunded(0) = committed_amount − PaidIn(0) + Σ(recallable distribution amounts)` — the recallable restoration interpreted HERE as ratified CC-1 semantics (a reversed recallable pair contributes exactly 0 — verified); **the coherence fences are the ONLY fences (F4 rewording): `Unfunded(0) ∈ [0, committed]` fail-closed** — restorations are uncapped up to that bound (the draft's "restoration exceeding cumulative calls is disclosed-and-proceeds" was UNREACHABLE — that case IS the `> committed` refusal; corrected). **`NAV(0)` = the latest current-head captured valuation mark for the (portfolio, instrument) PAIR** (F2: the `valuation` grain is pair-scoped — never instrument-only; both-axes-open; **"latest" = max `valuation_date`**, never max system_from — a back-dated newer capture must not outrank a newer-dated mark) — **the mark's `currency_code` must be non-NULL and EQUAL the commitment's chain currency, else PRE-CREATE refusal (F3)**; mark STALENESS is v1-DISCLOSED (a LIMITATIONS row + the pinned `valuation_date` in evidence; the HG-1-style opt-in age gate = the recorded v2). **If NO mark is pinned: `NAV(0) = 0` iff `PaidIn(0) = 0`** (the canonical TA new-commitment case), **else FAIL-CLOSED pre-create** (never fabricate a NAV anchor for a funded position). A zero-call book with non-recallable distributions passes the gates (capture-legal; F13 — one disclosure line).

### OD-D — Snapshot: `PURPOSE_PACING_INPUT` + three new component kinds + a dedicated builder (OQ-4)

Dedicated **`build_pacing_snapshot`, homed in `snapshot/service.py`** (the UNANIMOUS builder precedent — every dedicated builder lives there, using the private `_persist_snapshot` + models-only local imports of source tables; the scope fence grants that file exactly this ONE builder) pinning, for ONE (portfolio, instrument) pair: the CURRENT commitment head (new FR-flavor `commitment_content` — excludes mutable close-out markers, includes `restatement_reason`/`supersedes_id`/`record_version`), ALL capital_call + distribution rows (new IA-flavor `capital_call_content`/`distribution_content` — the full immutable set incl. `reverses_id`/`is_recallable`), and the LATEST current-head valuation mark for the PAIR if one exists (max `valuation_date`, both-axes-open — the F2 scoping; the EXISTING `valuation_content` reused byte-unchanged). The builder pins `as_of_valuation_date` in the snapshot header — the deterministic age anchor (F1). `PURPOSE_PACING_INPUT` **joins the `SNAPSHOT_PURPOSES` tuple** (not the bypass precedent — the census-flagged inconsistency is not repeated); three new `COMPONENT_KIND_*` join `SNAPSHOT_COMPONENT_KINDS`. The serializer stays consumer-agnostic (duck-typed rows). Greenfield content functions — no false-drift exposure; the frozen-key discipline applies from birth (key-set tests).

### OD-E — Permission: MINT `pacing.run` + `pacing.view` under R-07 (Tier-3, OQ-2)

The doctrine is decisive: a governed OUTPUT read includes `auditor_3l` (the exposure/risk/perf precedent) while `commitment.view` EXCLUDES it (captured-input scope) — so reusing `commitment.view` would either deny the 3L auditor a governed output or silently widen a captured-input verb; both wrong. And CC-2 is neither a risk nor a performance number — the PM-1 "own domain, own pair" precedent applies verbatim. **Mint `pacing.run`** (makers: `data_steward` + `risk_analyst_1l` + `platform_admin` — the perf.run grant shape) **+ `pacing.view`** (`risk_analyst_1l`, `risk_manager_2l`, `data_steward`, **`auditor_3l`**, `platform_admin`). Ratified parity test both directions. **Rejected: reuse `risk.run`/`risk.view`** (a liquidity/pacing projection is not a risk statistic; the BT-1 reuse precedent covered outcomes-analysis OF risk numbers). **Rejected: reuse `perf.run`/`perf.view`** (not a return measure). **Rejected: extend `commitment.*`** (the auditor-scope contradiction above).

### OD-F — Run family + audit + API + rule 7 (OQ-5)

`RUN_TYPE_PACING_PROJECTION` in `pacing/events.py` + the RESERVED-not-minted `PACING.PROJECTION_CREATE` audit constant (the run reuses `CALC.RUN_*` — the EVT-250 decade reserved by this record's taxonomy row, activation deferred like EXPOSURE/RISK/PERF). API `api/pacing.py`: registrar endpoint (gated `model.inventory.register`), run endpoint (`pacing.run`), GET run + rows / GET single row (`pacing.view`), the exact-type error map through the shared `raise_mapped_write`. **Rule 7 in-slice — ALL THREE legs delivered (the doctrine verifier's H-1: the as-of leg had been silently dropped in paraphrase — a mandate breach on the first slice the rule governs; fixed by DELIVERY, not rationalization). The list CONTRACT (F9): flat rows each carrying `calculation_run_id` + `model_version_id`, total ordering (run `system_from` DESC, run_id DESC, `period_index` ASC); cross-run aggregation is a CONSUMER ERROR (stated in the referent + the endpoint description)**: `GET /pacing/projections?portfolio_id&instrument_id&as_of` (**`as_of: datetime | None` — resolution scoped to runs with `system_from <= as_of`**; silent-empty on unknown ids — the CC-1-corrected precedent) AND **`GET /pacing/projections/latest?portfolio_id=&instrument_id=&as_of=`** (**the latest-resolver IS the `as_of`-aware read — `as_of=None` means now; ONE code path**) — the platform's FIRST latest-resolver: the newest COMPLETED projection run for the pair **across ALL model versions ("current" = the latest run, stated)**, resolved by JOINING through the ENT-059 rows (F12 — `CalculationRun` carries no portfolio/instrument columns), tie-break (run `system_from` DESC, run_id DESC — `CalculationRun` HAS `system_from` via the IA mixin, verifier-confirmed), 404 when none.

### OD-G — Demo stage 9 = the PROJECTION half of the ratified commitment walk (OQ-6)

`demo/cc2_stage9.py` + `scripts/run_demo_cc2.py` + suites (filename `test_demo_stage9_cc2*` — alpha-sorts after `stage8`; **the stage10 zero-pad hazard recorded for the NEXT stage author**). The walk: capture ONE valuation mark for `PE-MERIDIAN-X` (the existing valuation service — the NAV anchor; a TD-1-realistic mark, e.g. 11.2M against 10M net called + 1.8M distributed); register `pacing.commitment_projection` v1 with TD-1-realistic declared params (e.g. `rc_schedule=0.25,0.333,0.5`, `fund_life=12`, `bow=2.5`, `growth=0.13`, `yield_floor=0` — PE-shaped; NOT TA's unrouted examples, our own declared choices); `build_pacing_snapshot` on the stage-8 pair; run the projection through the scaffold; file the **INITIAL AWC dossier** (`CC2_PACING_TIER` + `CC2_PACING_INITIAL` as separate constants — `TIER_DOSSIERS` stays 16-pinned) with finding keys uniquely matching the new LIMITATIONS rows. **Counts move deliberately: 20 codes / 35 records / 96+ runs** (the contrast with stage 8's pinned-unchanged is the demo's own story: capture mints nothing, projection is governed). CI step after stage 8, before the downgrade smoke.

### OD-H — Docs + REQ discharges (OQ-7)

`pacing_commitment_projection_v1.md` referent (the verified-via-reproduction basis + per-source grades + the precision/determinism notes incl. the Decimal-power statement + the v2 register: Jeet stochastic enhancement, quarterly periodicity, multi-commitment aggregation, the chain-terminate interplay). Canonical model: the ENT-059 row REALIZED + a dated ENT-015/016 note (the consumer arrived). Backbone/RTM (the M-6 honest wording): **REQ-PRV-001 — 'computed' DISCHARGED** (per-period unfunded, governed, reproducible from pins; the "Funded/unfunded test" leg discharged by the kernel/anchor suites + demo stage 9) **but 'aggregated' stays OPEN** — the portfolio-level unfunded rollup across pairs is the NAMED v2 (the register carries it; discharging by redefining "aggregated" to per-pair would be an overclaim). **REQ-PRV-002's "feed liquidity forecast" clause DISCHARGED** (the projected call/distribution series is the first governed liquidity-forecast consumer — per-(portfolio, instrument) pair at ANNUAL periodicity in v1, stated). Both dated. The taxonomy EVT-250 reservation row. The SoD mint row for `pacing.*`. NO roadmap stamp (closeout's job).

**Scope fence:** `audit/service.py` FROZEN; `private_capital/` byte-untouched (ZERO diff — CC-2 consumes it only through the PIN); **`valuation/` byte-untouched** (the NAV-anchor read is builder-side models-only); `perf/` and `risk/` byte-untouched (the new package is a peer); `calc/`, `model/`, `db/`, `transaction/` rails byte-untouched (`execute_governed_run` consumed, never edited); the snapshot serializer changed ONLY by the three new greenfield content functions + the purpose/kind registrations; `snapshot/service.py` changed ONLY by the ONE `build_pacing_snapshot` builder (+ its models-only imports — the F7 fence grant); `pacing/` imports NO `private_capital` symbol (AST-fenced); prior demo stages 1–8 byte-untouched (stage 9 ADDITIVE; `TIER_DOSSIERS` stays 16-pinned); ONE migration; NO new audit emission (CALC.RUN_* reused); FE ZERO changes; the 5-table hybrid set untouched; `irp_shared/marketdata/` AND `api/marketdata.py` untouched. **The sanctioned touch-list, explicit:** `entitlement/bootstrap.py` (the ONE R-07 mint + parity test), `irp_shared/models.py` (the aggregator import), `main.py` (the router registration), `ci.yml` (two steps), the 19-file head-pin sweep, the `test_synthetic.py` tripwire bump, the four governance docs.

## Part 4 — Open decisions (OQ-CC-2-1…7) — pending ratification

| OQ | Question | Recommendation |
|---|---|---|
| **OQ-CC-2-1** (Tier-3) | **The ENT-059 mint + result shape**: one IA table `pacing_projection_result`, grain (run, period_index), no summary row, no metric_type vocab in v1? | **Yes per OD-A** — the ENT-053 template; totals Σ-derivable; a future variant re-opens vocab additively. Migration 0045. |
| **OQ-CC-2-2** (Tier-3) | **Permission**: mint `pacing.run`/`pacing.view` vs reuse risk.*/perf.*/commitment.*? | **MINT per OD-E** — the auditor-scope doctrine is decisive (governed outputs include the 3L auditor; `commitment.view` excludes it); PM-1's own-domain precedent verbatim. |
| **OQ-CC-2-3** (Tier-3) | **The v1 model form**: the verified TA form at ANNUAL periodicity (quarterly = the recorded v2) with FIVE declared params (rc_schedule/fund_life/bow/growth/yield_floor) + the form-marker assumption; NO TA constants minted? | **Yes per OD-B/Part 2** — the fetch MUST discharged via reproduction concordance (primary gated, disclosed); every number declared; the Jeet enhancement = the named v2. |
| **OQ-CC-2-4** (Tier-3) | **The anchor + period-geometry semantics — the OD-CC-1-E deferred funded-arithmetic substance, incl. the FEE/EQUALIZATION convention (all call types consume unfunded in v1 — fees-inside-commitment; a registered assumption row)** (materially amended by the verifier pass): FUTURE-ONLY projection from the PINNED as-of (t = current_age+1..L, age-indexed; the mid-life re-anchoring disclosed as OUR adaptation); quantize-then-roll; pair-scoped currency-checked mark anchor (staleness disclosed v1); recallable restoration bounded by the coherence fences; Σ over all call types declared; past-L ⇒ PRE-CREATE refusal? | **Yes per OD-C as amended** — the verifier's F1 HIGH (a wall-clock age would break pin-reproducibility) + F2/F3/F4/F5/F6/F8 folded; the honesty refusals throughout. |
| **OQ-CC-2-5** | **Snapshot**: dedicated builder + purpose IN the allow-list tuple + three greenfield content functions + the reused valuation_content? | **Yes per OD-D** — the census-flagged tuple-bypass inconsistency is not repeated. |
| **OQ-CC-2-6** | **Rule 7 in-slice**: ALL THREE legs — the entity-filtered list, the `as_of` filter, and the platform's FIRST latest-resolver (one `as_of`-aware code path)? | **Yes per OD-F as amended** — the doctrine verifier's H-1 caught the as-of leg silently dropped in paraphrase; delivered, not rationalized. Tie-break (run `system_from` DESC, run_id DESC), never uuid-alone. |
| **OQ-CC-2-7** | **Demo stage 9 + REQ discharges**: the projection walk on the stage-8 commitment (counts → 20/35/96+); PRV-001 'computed' discharged with **'aggregated' honestly kept OPEN (the portfolio rollup = the named v2)**; PRV-002 liquidity discharged? | **Yes per OD-G/OD-H as amended** — the ratified walk rider completes; the capture-vs-governed contrast is the demo's story; no discharge-by-redefinition. |

## Part 5 — Implementation plan

See `cc_2_implementation_plan.md` (10 steps, one commit each; the battery + 4-finder review close the slice). Part 5.5 (deviations) and Part 6 (review dispositions) are appended at implementation per house convention.
