# Phase Status

> **As of HEAD `7c50c43` / CI #95 (refreshed 2026-07-07).** Per-phase status, commits, CI, deliverables, placeholders.
> Commit hashes are the implementation commit (plan/fix commits noted). Re-verify HEAD + CI at session start
> (`current_state.md`).

| Phase | Status | Impl commit | CI | Key deliverables | Known placeholders carried forward |
|---|---|---|---|---|---|
| **P0.5** Hygiene & foundation | DONE | scaffold `4f93a33` (+ `1caae23`, audit fixes `a70caa6`/`e7fc61a`) | green | Monorepo; frozen audit framework + hash chain; RLS foundation (mig `0001`); entitlement seed; CI (lint/type/test + PG migration job + secret-scan + docs-check) | dev SSO header shim (not a boundary); WORM/anchoring deferred (HARD-01/02) |
| **P1A-0** Tenant context / RLS | DONE | `7cdc2f9` (plan `4bc68c6`, decisions `c975450`) | green | `set_config` tenant context; FORCE RLS USING+WITH CHECK; constrained `irp_app` test role; `irp_ops` BYPASSRLS role (mig `0003`) | header shim (DR-P1A0-3); `SECURITY.RLS_DENIED` eventing deferred (DR-P1A0-4) |
| **P1A-1** Data source + lineage | DONE | `96a1564` (plan `3ff3213`; CI fixes `7a700f0`/`72b889f`; hardening `97c2b1d`) | green | `data_source` (EV) + `lineage_edge` (IA, mig `0004`); `record_lineage`/`assert_has_lineage`; `DATA.SOURCE_REGISTER/UPDATE` | no lineage query/graph/field-level (REQ-LIN-002/P7); `run_id` logical (non-FK) |
| **P1A-2** Model registry | DONE | `c9be657` (plan `4be45f5`) | green | `model` (EV) + `model_version`/assumption/limitation (IA, mig `0005`); inventory + BR-3 gate; `MODEL.REGISTER/VERSION` | DR-P1-3 maker-checker hooks non-enforcing; tier/validation reserved (REQ-MDG-002/003/P7); `calculation_run.model_version_id` nullable until P2 |
| **P1A-3** Data quality | DONE | `cc472be` (plan `5da67be`) | green | `data_quality_rule` (EV) + `data_quality_result` (IA, mig `0006`); 2 generic evaluators; no-silent-failure; `run_quality_check`/`assert_passed_quality_checks`; `DATA.VALIDATE` + `DATA.DQ_RULE_DEFINE/UPDATE` | generic rules only (no domain DQ); reconciliation/override (REQ-DQR-002/003/P7); `ingestion_batch_id` placeholder (filled at P1A-4) |
| **P1A-4** Generic ingestion staging | DONE | `c781bb8` (plan `563b6cf`; PG fix `0282359`) | green (run 27965086115) | `ingestion_batch` (IA status-mutable) + `ingestion_staged_record` (IA immutable, mig `0007`); CSV anti-corruption; composes lineage+DQ+audit; durable-evidence-on-reject; `POST /ingest/upload` + batch reads; activates `DATA.INGEST` | `scan_status` AV no-op (OD-042); in-DB JSON staging (AD-004 deferred); **canonical mapping deferred to P1B/P1C**; CSV-only (XLSX later); adapters REQ-INT-002/003/P9 |
| **P1A closeout / P1B readiness** | DONE | `69afedf` | green (run 27967643576) | Closeout doc + 12-rail inventory + P1B readiness + OD-P1B-A…J framing (`10_delivery_backlog/p1a_closeout_p1b_readiness.md`) | — |
| **P1B-0** Decision record + plan + ratifications | DONE (committed) | `dbed93e` (decision record + plan); **ratifications `4fae26b`** (decision log, requirements/RTM, canonical model, temporal, audit taxonomy, entitlement model) | green | OD-P1B-A…J resolved + **ratified into governance**: AD-013-R1, REQ-SMR-005 + CAP-2.5 re-partition, ENT annotations, `REFERENCE.*` reserved, reference permissions | Audit `REFERENCE.*` codes + entitlement bootstrap **code** minted in the P1B build slices (not at P1B-0) |
| **P1B-1** Reference data (currency/calendar/rating_scale) | **DONE / CLOSED** | plan `05ee5f5`; impl **`6568cb1`** | green (run #28 = 27982349921) | 5 EV tables (mig `0008`): `currency`, `calendar`+`calendar_holiday`, `rating_scale`+`rating_grade`; **first asymmetric hybrid RLS** (AD-013-R1: `USING own OR SYSTEM` / `WITH CHECK own`); SYSTEM_TENANT global-read (closed set; `data_source` NOT hybrid); **app-layer tenant-wins dedup**; `REFERENCE.CREATE`/`UPDATE` activated; MANUAL-`data_source` lineage; `irp_shared.reference` package + `api/reference.py` + additive `reference.*` perms + governed SYSTEM seeder | SYSTEM seed not yet wired to a prod post-migrate path (REQ-SMR-005 In-Progress, OQ-P1B1-001); `REFERENCE.CORRECTION`/`STATUS_CHANGE` reserved; FR mixin still unexercised (P1B-3) |
| **P1B-2** legal_entity core + issuer/counterparty | **DONE / CLOSED** | plan `410cc7e`; impl **`32c7778`** | green (run #31 = 28029190858) | 3 EV tables (mig `0009`): `legal_entity` core (**implementation-only, NO ENT id**) + `issuer` (ENT-002) + `counterparty` (ENT-003) as **separate 1:1 role profiles** (`UNIQUE(tenant_id, legal_entity_id)`); **tenant-scoped SYMMETRIC RLS — proprietary-never-hybrid** (no-context read → zero rows; positive `pg_policies` assertion; closed hybrid set still the 5); **LEI partial-unique** `(tenant_id, lei) WHERE lei IS NOT NULL`; hierarchy STRUCTURE (`parent_legal_entity_id` adjacency + bounded/cycle-safe/tenant-filtered `resolve_ultimate_parent`, exposure-rollup CALC deferred); reuse `REFERENCE.CREATE/UPDATE` (own event per entity) + MANUAL-source lineage; additive `reference.legal_entity.*` (excludes auditor_3l) | NO netting/CSA/collateral/exposure columns (OD-015 → P1C); exposure-rollup CALC deferred (P2+); REQ-SMR-002 In-Progress; FR still unexercised (P1B-3) |
| **P1B-3** instrument(+`instrument_terms` FR)/identifier_xref | **DONE / CLOSED** | plan `43c042e`; impl **`8545ed6`** | green (run #34 = 28042611593) | 3 tables (mig `0010`, symmetric RLS): `instrument` (EV identity-only, nullable `issuer_id`) + `instrument_terms` (**FR — the platform's first persisted bitemporal entity**: create / effective-dated supersede / as-known correction; both-axes `reconstruct_terms_as_of`; one-`now`; close-first; current-head partial-unique; NOT append-only) + `identifier_xref` (EV, polymorphic, active partial-unique); **deterministic single-result-or-`AmbiguousIdentifier`** resolution (OD-P1B-G); **`REFERENCE.CORRECTION` (EVT-142) ACTIVATED** caller-side for terms restatement (R-07; `audit/service.py` FROZEN); cross-tenant linked-id service-layer fail-closed; additive `reference.identifier.view/edit` (`.resolve` unchanged; excludes auditor_3l). 8-lens reviewed (0 block) | terms math (pricing/cashflow/day-count/valuation) → P2+; identifier precedence/external-validation → P1C/OD-012; REQ-SMR-001/003 In-Progress |
| **P1B-4** corporate_action | **DONE / CLOSED** | plan `f6d691a`; impl **`060b2a4`** | green (run #37 = 28052388043) | 1 EV table (mig `0011`, symmetric RLS): `corporate_action` (**EV, capture-only**) — `instrument_id` NOT-NULL FK (reused `resolve_instrument`, cross-tenant fail-closed); single `status` lifecycle ANNOUNCED→CONFIRMED→CANCELLED (no `is_active`; thin guard, no DB write on reject); inert business dates + ratio/amount/currency; **`REFERENCE.STATUS_CHANGE` (EVT-143) ACTIVATED** caller-side (first EVT-143 use; `audit/service.py` FROZEN); amend = REFERENCE.UPDATE; MANUAL-source lineage; additive `reference.corporate_action.view` (excludes auditor_3l). 8-lens reviewed (0 block) | NO application/position/valuation/entitlement/tax/event-engine; roll math (QS-10/11) → P1C; issuer-level actions deferred; REQ-SMR-004 In-Progress |
| **P1B-5** ingestion mapping | NOT STARTED (conditional/deferred) | — | — | — | only if bulk reference loading is needed; not now |
| **P1B closeout / P1C readiness** | DONE | `e99633a` | green (run #39) | closeout doc + rail/domain inventory + P1B-5 defer + P1C readiness + subphase structure + synthetic-data strategy (`10_delivery_backlog/p1b_closeout_p1c_readiness.md`) | — |
| **P1C-0** decision record + plan | DONE / **RATIFIED** | `705d3ba`; ratification (this turn, governance md/yaml) | green (run #40) | 12 decisions (OD-P1C-A..L) + P1C implementation plan (P1C-1..P1C-6); **AD-017** + REQ-PPM-001 + PORTFOLIO.* reserved + OD-013/OD-025 closed recorded into governance | OD-012/OD-015 re-targeted beyond P1C |
| **P1C-1** portfolio-hierarchy plan | PLAN DONE | `b52ad9e` | green (run #41) | detailed 20-section plan: single `portfolio` EV table + `node_type` + `parent_portfolio_id`; ABAC scope anchor; bounded ancestor+descendant resolvers; `PORTFOLIO.*` audit; symmetric RLS (`10_delivery_backlog/p1c1_implementation_plan.md`) | build CLOSED (`bb89c74`, run #43) |
| **P1C-0** ratification | DONE | `dca7bc0` | green (run #42) | AD-017 + REQ-PPM-001 + PORTFOLIO.* reserved (EVT-150) + OD-013/OD-025 closed + portfolio.* grants recorded into governance |
| **P1C-1** portfolio hierarchy + ABAC scope anchor | **DONE / CLOSED** | impl **`bb89c74`** | green (run #43 = 28068172716) | 1 EV table (mig `0012`, symmetric RLS): `portfolio` (ENT-010) — single table, `node_type`/`status` controlled-vocab, `parent_portfolio_id` self-FK; bounded ancestor (`resolve_ultimate_parent`) + **NEW** descendant (`resolve_descendants`) resolvers (depth cap 32, visited-set, `HierarchyCycleError`, per-hop tenant predicate); **ABAC anchor-not-enforce**; **`PORTFOLIO.CREATE`/`UPDATE` (EVT-150/151) ACTIVATED** caller-side (`audit/service.py` FROZEN); `data_steward` granted `portfolio.view`+`portfolio.edit` (auditor_3l excluded); MANUAL-source ORIGIN lineage; **fail-closed audit rollback (CTRL-032)**; new `irp_shared/portfolio/` domain package (rail-only imports); 35 tests + parity. 8-lens reviewed (0 block). The platform's **first domain entity**. | a portfolio holds NOTHING (no position/valuation/holding/exposure); ABAC enforcement → P6+; STATUS_CHANGE (EVT-152) reserved-not-emitted; HTTP amend can't un-parent to root (reference baseline) |
| **P1C-2** transaction plan | PLAN DONE | `c398215` | green (run #45) | 22-section plan: `transaction` IA append-only event log + `reverses_transaction_id`; `TRANSACTION.RECORD`/`REVERSE`; symmetric RLS + append-only trigger; `transaction.view`/`record`; capture-only (`10_delivery_backlog/p1c2_implementation_plan.md`) | build CLOSED (`abb230f`, run #46) |
| **P1C-2** transaction (IA append-only) | **DONE / CLOSED** | impl **`abb230f`** | green (run #46 = 28108904570) | 1 IA table (mig `0013`, symmetric RLS + append-only): `transaction` (ENT-012) — `ImmutableAppendOnlyMixin` (`system_from` only), in `APPEND_ONLY_TABLES` → `irp_prevent_mutation` **P0001 trigger** + ORM `before_update`/`before_delete` guard; `portfolio_id`/`instrument_id` NOT-NULL FKs (service-layer cross-tenant fail-closed); `reverses_transaction_id` self-FK; **reversal-as-new-record** (original never mutated); partial-unique `external_ref`; **`TRANSACTION.RECORD`/`REVERSE` (EVT-160/161) ACTIVATED** caller-side (`audit/service.py` FROZEN); `transaction.view`/`record` minted (`data_steward` maker; `auditor_3l` excluded); MANUAL-source ORIGIN lineage per record + reversal; fail-closed audit rollback (CTRL-032); new `irp_shared/transaction/` package (one-way → portfolio+reference+rails); 32 tests + parity. 8-lens reviewed (0 block). The platform's **first domain IA / append-only entity**. | **capture-only** — NO position derivation / cashflow engine / valuation / exposure aggregation; `quantity`/`price`/`gross_amount` inert; REQ-PPM-003 In-Progress (transaction conjunct; valuation → P1C-4); `txn_type` open vocab |
| **P1C-3** position plan | PLAN DONE | `42cc02c` | green (run #48) | 24-section plan: `position` FR bitemporal, captured directly; reuse `instrument_terms` protocol; current-head partial-unique; `POSITION.*` (EVT-170/171/172); symmetric RLS, NOT append-only (`10_delivery_backlog/p1c3_implementation_plan.md`); OD-P1C3-1..5 signed off | build CLOSED (`4ee124e`, run #49) |
| **P1C-3** position (FR bitemporal) | **DONE / CLOSED** | impl **`4ee124e`** | green (run #49 = 28177012516) | 1 FR table (mig `0014`, symmetric RLS, **NOT append-only**): `position` (ENT-011) — `FullReproducibleMixin`, reuses the `instrument_terms` protocol verbatim (create / effective-dated supersede / as-known correction / `reconstruct_position_as_of` both axes); current-head partial-unique `(tenant_id, portfolio_id, instrument_id) WHERE valid_to IS NULL AND system_to IS NULL`; **captured directly, NO transaction FK / NO derivation** (OD-P1C-E); aggregated `(portfolio, instrument)` grain, signed quantity, opaque `cost_basis`; **`POSITION.CREATE`/`UPDATE`/`CORRECTION` (EVT-170/171/172) ACTIVATED** caller-side (`audit/service.py` FROZEN); `position.edit` minted + `position.view` wired to `data_steward` (auditor_3l excluded); MANUAL-source ORIGIN lineage per physical version; fail-closed audit rollback (CTRL-032); new `irp_shared/position/` package; 45 tests + parity. 8-lens reviewed (0 block). The platform's **first FR domain entity**. | **capture-only** — NO market value / valuation / exposure aggregation / holdings view; NOT in `APPEND_ONLY_TABLES` (no P0001 trigger — close-out UPDATEs allowed; content-immutability service-enforced); REQ-PPM-002 In-Progress (capture + as-of; ABAC enforcement → P6+); maker-checker on corrections → P6+ |
| **P1C-4** valuation plan | PLAN DONE | `92a0264` | green (run #53) | 24-section plan: `valuation` FR bitemporal, captured marks; `valuation_date` immutable logical-key (4-part current-head key); reuse `position` protocol; `VALUATION.*` (EVT-180/181/182); symmetric RLS, NOT append-only (`10_delivery_backlog/p1c4_implementation_plan.md`); OD-P1C4-1..6 signed off | build CLOSED (`c5c5806`, run #54) |
| **P1C-4** valuation (FR bitemporal) | **DONE / CLOSED** | impl **`c5c5806`** | green (run #54 = 28186419856) | 1 FR table (mig `0015`, symmetric RLS, **NOT append-only**): `valuation` (ENT-013) — `FullReproducibleMixin`, reuses the `position`/`instrument_terms` protocol verbatim; **`valuation_date` an immutable logical-key** Date column (distinct from `valid_from`); current-head partial-unique `(tenant_id, portfolio_id, instrument_id, valuation_date) WHERE valid_to IS NULL AND system_to IS NULL` (4-part key; many dates coexist); **captured marks, NOT modeled** (`mark_value` captured, `mark_source` inert label; OD-P1C-F) — NO valuation/pricing model, NO price lookup, NO market-value rollup, NO position FK/link; **`VALUATION.CREATE`/`UPDATE`/`CORRECTION` (EVT-180/181/182) ACTIVATED** caller-side (`audit/service.py` FROZEN); **both** `valuation.view`/`valuation.edit` newly minted (`data_steward` maker; auditor_3l excluded); MANUAL-source ORIGIN lineage per physical version; new `irp_shared/valuation/` package (import-direction forbids `position`); 48 tests + parity. 8-lens reviewed (0 block, 1 LOW folded). The platform's **second FR domain entity**. | **REQ-PPM-003 now Done** (both conjuncts). capture-only — NO exposure aggregation / holdings view / dataset_snapshot; maker-checker on corrections → P6+ |
| **P1C-5** holdings-views plan | PLAN DONE | `8a14173` | green (run #56) | 23-section plan: read-only `irp_shared/holdings/` composition + `GET /portfolios/{id}/holdings`; service-layer read models (no DB view/migration); display-only opt-in marks; subtree composition not enforcement (`10_delivery_backlog/p1c5_implementation_plan.md`); OD-P1C5-1..6 signed off | build CLOSED (`0bef45b`, run #57) |
| **P1C-5** as-of holdings/portfolio views | **DONE / CLOSED** | impl **`0bef45b`** | green (run #57 = 28196900649) | **READ-ONLY composition — NO entity, NO migration (head stays `0015_valuation`), NO write endpoint, NO permission, NO audit/lineage/DQ write.** New `irp_shared/holdings/` read-model package (`reconstruct_holdings_as_of` = set-generalization of `reconstruct_position_as_of`; `reconstruct_subtree_holdings_as_of` via bounded `resolve_descendants`; `attach_marks_as_of` via `reconstruct_valuation_as_of`); plain read DTOs (`HoldingRow`/`MarkView`/`HoldingWithMark`, NOT ORM, no `models.py`/`events.py`). `GET /portfolios/{id}/holdings` (`valid_at` required; `known_at`/`subtree`/`include_marks`/`valuation_date`/`limit`/`offset` optional). `portfolio.view`+`position.view` route guards; `valuation.view` in-handler before any mark lookup → 403; 404 cross-tenant; 409 cycle; 422 missing-valid_at / marks-without-date. Display-only marks (opt-in, explicit `valuation_date`). Subtree = read COMPOSITION, NOT ABAC enforcement (anchor-not-enforce). AST scope-fence + DTO-field fence + zero-audit assertion; 31 tests (15 logic+fence / 4 PG FORCE-RLS / 12 endpoint). 8-lens reviewed (0 block, 2 LOW fence folds). The platform's **first read-model package**. | **No REQ status change** (read-half of REQ-PPM-001/002; display read of REQ-PPM-003). NO aggregation / market-value / `quantity × mark` / exposure / dataset_snapshot. The migration job gained NO new step (proof of no migration). |
| **P1C-6** synthetic dataset plan | PLAN DONE | `7dfdb79` | green (run #59) | 23-section plan + folded read-only synthetic/test-data audit conclusions + SYNTH_* naming / no-real-data / edge-case / deterministic-generation rules; OD-P1C6-1..7 signed off (`10_delivery_backlog/p1c6_implementation_plan.md`) | build CLOSED (`3e9882d`, run #60) |
| **P1C-6** deterministic synthetic dataset | **DONE / CLOSED** | impl **`3e9882d`** | green (run #60 = 28207899969) | **NO new entity, NO migration (head stays `0015_valuation`).** New `irp_shared/synthetic/` package (`ids.py` uuid5 namespace + `SeedClock`; `builder.py` `build_synthetic_dataset` → `SyntheticDatasetSummary`) composing the **governed** binders into a synthetic reference pack (3 instruments + 2 identifiers incl. a structurally-invalid ISIN) + 6-node portfolio subtree + 3 transactions (incl. reversal) + 6 positions (incl. short/supersede/correction) + 4 valuations (incl. correction/multi-date + a deliberate missing mark). Determinism via a **keyword-only default-None deterministic-injection seam** (`entity_id`/`now`) on the governed binders + their audit `_emit` (`now → record_event(event_time=now)`) — **production call sites byte-for-byte unchanged**; `audit/service.py` FROZEN. **NEVER-AUTO-RUN** + **production/non-synthetic-tenant REFUSAL guard** (`SyntheticSeedRefused` unless `allow_synthetic_seed=True` + `IRP_ALLOW_SYNTHETIC_SEED=1` + SYNTHETIC tenant); SYNTHETIC-tenant-only under FORCE RLS, never BYPASSRLS. 24 tests (20 SQLite + 4 PG FORCE-RLS). 8-lens reviewed — a false-GREEN env-gate determinism test (BLOCKER) caught + fixed + re-validated env-unset. **Completes the FULL P1C block.** | **No REQ status change** (a deterministic test/demo/UI enabler, OD-P1C-L). Capture-only — NO market value / risk / exposure aggregation / `dataset_snapshot`. Rails internal PKs (data_source/lineage/audit surrogate ids) stay wall-clock (documented; the domain-row surface is deterministic) |
| **P1C closeout / P2 readiness** | DONE | `7070dff` | green (run #62) | closeout + readiness review (`10_delivery_backlog/p1c_closeout_p2_readiness.md`); chose **reproducibility-first** P2 sequencing; 8-lens | — |
| **P2-0** decision record + plan | DONE / COMMITTED | `2d19992` | green (run #63) | `p2_0_decision_record.md` (OD-P2-A…L) + `p2_implementation_plan.md` (subphases **P2-1 snapshot → P2-2 FX → P2-3 calculation_run+exposure → P2-4 price → P2-5 curves → P2-6 benchmark**); 8-lens, 0 block | governance ratification (ENT ids, audit, entitlement) deferred to the ratification step |
| **P2-1** dataset_snapshot plan | PLAN DONE | `d7be981` | green (run #64) | `p2_1_dataset_snapshot_implementation_plan.md` (§1–§24): IA true-append-only header + component; physical-version pin + `captured_content` + canonical SHA-256 hash; cross-tenant binding invariant; narrow internal lineage writer; caller-side completeness DQ gate; `calculation_run` readiness (no wiring); 8-lens, 0 block | build CLOSED (`3629baa`, run #67) |
| **P2 governance ratification** | DONE / **RATIFIED** | `63be23a` | green (run #65) | Into source-of-truth (RESERVED/PLANNED, no code): **ENT-049 `dataset_snapshot` + ENT-050 `dataset_snapshot_component`** (IA true-append-only) + **SNAPSHOT.CREATE reserved EVT-190** + **snapshot.view/.create reserved** (bootstrap unchanged) + **AD-004-R1** Postgres-first (OD-014 resolved) + **REQ-PPM-004 → In-Progress** + control-matrix mappings; 7-lens, **7× approve** | `audit/service.py` FROZEN; no `snapshot` package; migration head `0015_valuation` |
| **P2 ratification closeout memory** | DONE | `d45a31b` | green (run #66) | project-memory refresh after P2 governance ratification (docs-only) | — |
| **P2-1** dataset_snapshot impl | **DONE / CLOSED** | impl **`3629baa`** | green (run #67 = 28251757848) | **The AD-014 reproducibility primitive REALIZED.** 2 IA true-append-only tables (mig `0016_dataset_snapshot`, symmetric RLS + P0001 trigger + ORM guard): `dataset_snapshot` (ENT-049) + `dataset_snapshot_component` (ENT-050) — **physical-version pin** (`target_entity_id` + `valid_from`/`system_from` (NULL for EV) + `record_version`) + **`captured_content`** + **SHA-256 `content_hash`** (HALF_UP-quantized, excludes close-out markers) + header **`manifest_hash`**; **`build_snapshot`** governed binder (set-returning enumerators; no `quantity×mark`, no `calc` import); **narrow internal lineage writer** (`data_snapshot` source_type, local Core Table); **caller-side completeness DQ gate** (reuses `run_quality_check` NOT_NULL; Protocol untouched; gap → fail closed); **`SNAPSHOT.CREATE` (EVT-190) ACTIVATED** caller-side (`audit/service.py` FROZEN); **`snapshot.view`/`.create` minted** (`data_steward` maker; auditor_3l excluded); **`POST /snapshots` + `GET /{id}` + `GET /{id}/verify`**; symmetric RLS; **cross-tenant binding-integrity invariant**; new `irp_shared/snapshot/` package; the first new **Snapshot symmetric-RLS** CI step. 8-lens review → 6 in-scope folds. The platform's **first reproducibility primitive**. | **NO exposure number, NO `calculation_run` wiring** (readiness only; binding → P2-3). `migration_head` `0015_valuation` → `0016_dataset_snapshot`. |
| **P2-2** fx_rate plan | PLAN DONE | `6020b03` | green (run #69) | `p2_2_fx_rate_implementation_plan.md` (the 10 decisions settled; FR; base/quote direction; MID; Numeric(28,12); USD-base triangulation; `marketdata.*`; RANGE evaluator; hybrid resolve_currency); 8-lens, 6 in-scope folds | build CLOSED (`c257e5c`, run #70) |
| **P2-2** fx_rate impl | **DONE / CLOSED** | impl **`c257e5c`** | green (run #70 = 28258782538) | **Captured FX market data REALIZED.** `fx_rate` (ENT-024, **FR**, mig `0017_fx_rate`, symmetric RLS, **NOT append-only**; the `valuation` protocol verbatim) — explicit **base/quote** direction (`rate` = 1 base = rate quote, QS-08); **MID-only** v1; `rate` **Numeric(28,12)**; `rate_date` a **separate immutable logical key** (5-part current-head key); pure published-rate **`convert`** (identity/direct/reciprocal/triangulation-through-base USD-default; **exact-date v1**; **fail-closed** `FxRateNotFound`, no silent 1.0); **NEW hybrid-aware `resolve_currency`** (own OR SYSTEM; foreign rejected); **`marketdata.view`/`.ingest`** minted (data_steward maker; auditor_3l excluded); **`MARKET.FX_CREATE`/`UPDATE`/`CORRECTION` (EVT-200)** activated (`audit/service.py` FROZEN); **VENDOR `data_source` ORIGIN lineage** per physical version (reuses `record_lineage`); **NEW generic `RANGE` DQ evaluator** (additive, Protocol untouched); new `irp_shared/marketdata/` package; the new **FX symmetric-RLS** CI step. 8-lens review (6 approve / 2 approve_with_changes / 0 block) → 1 in-scope fold (CTRL-032 rollback test). The platform's **first market-data entity**. | **NO exposure number, NO `calculation_run` wiring, NO `dataset_snapshot` change.** `migration_head` `0016_dataset_snapshot` → `0017_fx_rate`. NO visible UI change. |
| **P2-3** calc_run + exposure plan | PLAN DONE | `d10c766` | green (run #72) | `p2_3_decision_record.md` (OD-P2-3-A…L) + `p2_3_exposure_implementation_plan.md` (§1–§24): exposure_aggregate IA append-only, run-bound + snapshot-gated; effective composite fx_rate + fx_legs; pre-create-refusal/post-create-FAILED failure model; named lineage edge_kinds; 8-lens, 10 in-scope folds; the five OQ-P2-3 sign-offs | build CLOSED (`da178fc`, run #74) |
| **P2-3 governance ratification** | DONE / **RATIFIED** | `851f976` | green (run #73) | Into source-of-truth (RATIFIED-IN-PLANNING, no code): **ENT-014 `exposure_aggregate`** (IA append-only) + **AD-018** + the **`CALC.RUN_START/COMPLETE/FAIL` → `CALC.RUN_CREATE/STATUS_CHANGE`** doc-vs-code reconciliation + **EVT-210 `EXPOSURE.*` reserved** + **`exposure.view`/`.aggregate.run`** (auditor_3l-in-view) + **CTRL-009 executable** + the **HALF_UP** canonical-serialization exception + REQ-PPM-004 advanced; 7-lens, 6 approve / 1 approve_with_changes | `audit/service.py` FROZEN; no code; migration head `0017_fx_rate` |
| **P2-3** calc_run + exposure impl | **DONE / CLOSED** | impl **`da178fc`** | green (run #74) | **The FIRST governed derived number REALIZED.** `exposure_aggregate` (ENT-014, **IA TRUE append-only**, mig `0018_exposure_aggregate` + the additive `calculation_run.environment_id`, symmetric RLS + P0001 trigger + ORM guard); **run-bound + snapshot-gated** (NN FKs to `calculation_run.run_id` + `dataset_snapshot.id`); **signed market value v1** (`signed_quantity × captured mark_value × effective captured FX`; `exposure_amount` Numeric(28,6) HALF_UP); grain `(portfolio, instrument, base)`; **effective composite `fx_rate`** + **`fx_legs`** (leg evidence, NOT a hard FK); **COMPONENT_KIND_FX** minted + `build_snapshot` FX-pinning + pure `compose_effective_rate` (no live read; reproducible under FX correction); **`CALC.RUN_CREATE`/`STATUS_CHANGE` reuse** + the additive `update_run_status(outcome=)` (NO `EXPOSURE.AGGREGATE_CREATE`); **pre-create-refusal / post-create-FAILED** failure model; lineage `snapshot --DEPENDS_ON--> run --ORIGIN--> result` (`run_id` stamped); **`exposure.view`/`.aggregate.run`** wired (auditor_3l in view); new `irp_shared/exposure/` package + `api/exposure.py`; the new **Exposure symmetric-RLS** CI step. 8-lens review (5 approve / 3 approve_with_changes / 0 block) → 2 in-scope folds. The platform's **first governed derived number**. | **NO risk** (MARKET_VALUE only); no VaR/ES/factor/stress/scenario/P&L/performance. `migration_head` `0017_fx_rate` → `0018_exposure_aggregate`. NO visible UI change. The AD-014/FW-RUN/TR-15 gate now load-bearing. |
| **P2-3 closeout memory** | DONE | `0b12d85` | green (run #75) | P2-3 closeout project-memory refresh (docs-only) | — |
| **P2-4** price history plan | PLAN DONE | `b73e65f` | green (run #76) | `p2_4_decision_record.md` (OD-P2-4-A…L) + `p2_4_price_history_implementation_plan.md` (§1–§26): `price_point` (ENT-020) FR/bitemporal; the 6-part current-head key with **price_source-in-key**; `Numeric(20,6)`; `price_type` CLOSE/MID/NAV; RAW-only; readiness-only snapshot; 8-lens, 4 in-scope folds; the six OQ-P2-4 sign-offs | build CLOSED (`2b63b76`, run #77) |
| **P2-4** price history impl | **DONE / CLOSED** | impl **`2b63b76`** | green (run #77 = 28338847532) | **Captured price history REALIZED.** `price_point` (ENT-020, **FR**, mig `0019_price_point`, symmetric RLS, **NOT append-only**; the `fx_rate` protocol verbatim) — the `marketdata/price.py` binder (`capture_price`/`supersede_price`/`correct_price`/`reconstruct_price_as_of`/`resolve_price`) + the `PricePoint` model; **6-part current-head key** `(tenant, instrument, price_date, price_type, currency_code, price_source)` with **`price_source` IN the key** (multi-vendor coexistence) + promoted key columns DB-level **NOT NULL**; `price_date` a **separate immutable logical key**; `price` **Numeric(20,6)**; `price_type` **{CLOSE,MID,NAV}**; **RAW vendor prices only** (no adjustment engine); captured `currency_code`, **NO conversion**; `instrument_id` NOT-NULL FK via reused `resolve_instrument`; **`MARKET.PRICE_CREATE`/`UPDATE`/`CORRECTION` (EVT-200)** caller-side (`audit/service.py` FROZEN); **`VENDOR_PRICE` `data_source` ORIGIN lineage** per physical version; **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + strictly-positive **`RANGE`** DQ gate; the new **Price-point symmetric-RLS** CI step. 8-lens review (7 approve / 1 approve_with_changes / 0 block) → 1 in-scope fold (REQ-PUB-001 RTM/backbone advancement). The platform's **second market-data entity**. | **NO pricing/valuation/return/factor/risk model; NO conversion; NO corporate-action adjustment engine.** Snapshot readiness-only (NO `COMPONENT_KIND_PRICE`); NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/FX change. `migration_head` `0018_exposure_aggregate` → `0019_price_point`. **REQ-PUB-001 → In-Progress (partial).** NO visible UI change. |
| **P2-4 closeout memory** | DONE | `419db9d` | green (run #78) | P2-4 closeout project-memory refresh (docs-only) | — |
| **P2-5** curve plan | PLAN DONE | `326ad94` | green (run #79) | `p2_5_decision_record.md` (OD-P2-5-A…N) + `p2_5_curves_implementation_plan.md` (§1–§28): the unified `curve` (FR header) + `curve_point` (IA append-only nodes); ENT-021 + ENT-023-by-value; the 6-part key; value-type-conditional DQ; the `reference_key` invariant; 8-lens, 8 in-scope folds; the ten OQ-P2-5 sign-offs | build CLOSED (`49ca3bd`, run #80) |
| **P2-5** curve impl | **DONE / CLOSED** | impl **`49ca3bd`** | green (run #80 = 28343716953) | **Captured yield/spread curves REALIZED.** the unified `curve` (ENT-021, **FR header**, mig `0020_curves`, symmetric RLS, NOT append-only) + `curve_point` (**IA append-only** version-pinned nodes — `APPEND_ONLY_TABLES` + the P0001 trigger + ORM guard) — the `fx_rate`/`price_point` FR protocol + the `dataset_snapshot`/`component` header+detail split; the `marketdata/curve.py` binder (`capture_curve`/`supersede_curve`/`correct_curve`/`reconstruct_curve_as_of`/`resolve_curve`/`list_curve_points`) + `Curve` + `CurvePoint` models. **ENT-023 `credit_spread` realized BY VALUE** (`curve_type=CREDIT_SPREAD` + `reference_key`). **6-part current-head key** `(tenant, curve_type, currency_code, reference_key, curve_date, curve_source)` + promoted key cols **NOT NULL**; `curve_point` UNIQUE `(curve_id, value_type, tenor_days)`; `curve_date` a **separate immutable logical key**; `curve_type` **{TREASURY,GOVT,SWAP,OIS,CREDIT_SPREAD}**; `value_type` **{ZERO_RATE,PAR_RATE,DISCOUNT_FACTOR,SPREAD}**; `point_value` **Numeric(20,12)**; `tenor_label` + normalized `tenor_days`; `reference_key` opaque (NOT an FK) + the `curve_type`↔`reference_key` invariant; `interpolation_method` inert label; **`MARKET.CURVE_CREATE`/`UPDATE`/`CORRECTION` (EVT-200)** caller-side (ONE event per curve; `audit/service.py` FROZEN); **`VENDOR_CURVE` ORIGIN lineage** per physical version; **reuse `marketdata.view`/`.ingest`** (NO new permission); **value-type-conditional `RANGE`** DQ; the new **Curve symmetric-RLS** CI step. 8-lens review (7 approve / 1 approve_with_changes / 0 block) → 1 material fold (REQ-PUB-002/003 RTM) + 3 low. The platform's **third market-data entity**. | **NO curve construction/interpolation/bootstrapping/duration/key-rate/pricing/valuation/return/factor/risk.** Snapshot readiness-only (NO `COMPONENT_KIND_CURVE`); NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point` change. `migration_head` `0019_price_point` → `0020_curves`. **REQ-PUB-002 + REQ-PUB-003 → In-Progress (partial).** NO visible UI change. |
| **P2-5 closeout memory** | DONE | `0c5c068` | green (run #81) | P2-5 closeout project-memory refresh (docs-only) | — |
| **operating rules** | DONE | `1e0dc08` | green (run #83) | provenance/dates + local-PG-validation-container standing rules (docs-only project-memory) | — |
| **P2-6** benchmark plan | PLAN DONE | `8d2782f` | green (run #82) | `p2_6_decision_record.md` (OD-P2-6-A…O) + `p2_6_benchmark_implementation_plan.md`: benchmark EV definition + benchmark_constituent FR membership; ENT-009; the P3-conditionality RESOLVED + the OQ-P2-6-11 audit-family fork; metadata+constituents only (levels/returns deferred); 8-lens, 2 HIGH + 1 MED + 21 LOW folds; the eleven OQ-P2-6 sign-offs + Option A | build CLOSED (`b6284a4`, run #84) |
| **P2-6** benchmark impl | **DONE / CLOSED** | impl **`b6284a4`** | green (run #84 = 28402968456) | **Captured benchmark/index data REALIZED.** `benchmark` (ENT-009, **EV definition**, mig `0021_benchmark`, symmetric RLS, NOT append-only — the corporate_action EV precedent) + `benchmark_constituent` (**FR/bitemporal membership**, symmetric RLS, NOT append-only — close-out UPDATEs; **NEITHER table append-only**, no P0001 trigger) — the `marketdata/benchmark.py` binder (`capture_benchmark`/`update_benchmark`/`resolve_benchmark`/`list_benchmarks` + `capture_membership`/`supersede_membership`/`correct_membership`/`reconstruct_membership_as_of`) + `Benchmark` + `BenchmarkConstituent` models. The FOURTH market-data entity. **Identity** `(tenant, benchmark_code, benchmark_source)`; **constituent 4-part current-head key** `(tenant, benchmark_id, instrument_id, effective_date)` + promoted key cols **NOT NULL**; `effective_date` a **separate immutable logical key**; `weight` **Numeric(20,12)** (`RANGE [0,1]`); `instrument_id` NOT-NULL FK via `resolve_instrument`; membership captured/superseded/corrected **as a set** per `(benchmark, effective_date)`. **RATIFIED AUDIT SPLIT (OQ-P2-6-11 Option A):** **`REFERENCE.CREATE`/`REFERENCE.UPDATE`** for the EV definition + **`MARKET.BENCHMARK_CONSTITUENT_CREATE`/`_UPDATE`/`_CORRECTION`** (EVT-200, set-grained one-event-per-set) for the FR membership; the definition NOT moved into `MARKET.*`; `audit/service.py` FROZEN. **`VENDOR_BENCHMARK` ORIGIN lineage** (benchmark-targeted; no `effective_date` on the edge); **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + weight `RANGE [0,1]` DQ (Protocol untouched). 8-lens review (4 approve / 4 approve_with_changes / 0 block) → every finding folded (incl. a real read-endpoint 500→404 fail-closed-status bug + 5 added tests). | **NO benchmark_level/benchmark_return (deferred — net-new canonical id); NO performance/attribution/active-return/active-risk/tracking-error/factor/covariance/VaR/ES/reporting.** Snapshot readiness-only (NO `COMPONENT_KIND_BENCHMARK`); NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point`/`curve` change. `migration_head` `0020_curves` → `0021_benchmark`. **REQ-PUB-003 benchmark leg advanced.** NO visible UI change. **THE FULL P2 FOUNDATION IS COMPLETE & CI-green.** |
| **P2 block** | **DELIVERED** | P2-1..P2-6 | green | The full reproducibility-first P2 captured market-data foundation: dataset_snapshot (3629baa) / fx_rate (c257e5c) / calculation_run+exposure_aggregate (da178fc) / price_point (2b63b76) / curve+curve_point (49ca3bd) / benchmark+benchmark_constituent (b6284a4). Reproducibility primitive + captured market-data inputs + the first governed derived number (exposure). NO risk analytics yet (P3). | — |
| **P2-6 closeout memory** | DONE | `ae2be8e` | green (run #85) | P2-6 closeout project-memory refresh (docs-only) | — |
| **P2 closeout / P3 readiness** | DONE | `bb73211` (CI re-trigger `6663452`) | green (run #86) | P2 closeout + P3 (factor model / market-risk) readiness review (`10_delivery_backlog/p2_closeout_p3_readiness.md`) — the P3 entry-option assessment the P3-0 record ratified | — |
| **P3-0** decision record + P3 plan | DONE / **RATIFIED** | `07607a5` | green (run #87) | `p3_0_decision_record.md` (OD-P3-0-A…N; the OQ-P3-0-1…10 sign-offs) + `p3_implementation_plan.md` (subphases P3-1…P3-7): analytic-sensitivities-first; the derived-number output contract (snapshot + run + registered model_version + IA append-only + reproducible); `RISK.*`@EVT-220 + `risk.view`/`risk.run` reserved; methodology home `05_analytics_methodologies/`; 8-lens, 0 block | validation-workflow enforcement → P7; stress RTM-P5 |
| **P3-1** sensitivities plan | PLAN DONE | `1a8b2a4` | green (run #88) | `p3_1_decision_record.md` (OD-P3-1-A…O; OQ-P3-1-1…6 ratified — curve-intrinsic v1) + `p3_1_sensitivities_implementation_plan.md`; 8-lens, 0 block / 0 high / 0 medium | build CLOSED (`e8e2e59`) |
| **P3-1** analytic sensitivities impl | **DONE / CLOSED** | impl **`e8e2e59`** (batch-pushed; CI-covered at `402cb12` run #89) | green | **The FIRST reproducible governed RISK number.** ENT-028 `sensitivity_result` (migration `0022_sensitivity`; IA TRUE append-only + P0001 trigger + symmetric RLS) — curve-node analytic DV01/spread-DV01 (`−T·DF·1bp`; ACT/365F; continuous compounding; nodes-only; PAR_RATE deferred; HALF_UP-12); **run + snapshot + REGISTERED model_version bound** (`assert_registered_model_version` pre-create — **CTRL-003 EXECUTABLE**); `irp_shared/risk/` package + `api/risk.py`; `COMPONENT_KIND_CURVE` + `PURPOSE_SENSITIVITY_INPUT` + `curve_content` + `build_curve_snapshot`; **`risk.view`/`risk.run` MINTED** (auditor_3l in `.view`); `RISK.SENSITIVITY_CREATE` reserved-not-emitted @ EVT-220; `CALC.RUN_*` reuse; governed `register_sensitivity_model` (methodology_ref → `sensitivities_analytic_v1.md`); methodology framework + first doc | instrument/position DV01 (needs terms+interpolation+pricing), PAR_RATE, vol/vega deferred; validation non-enforcing until P7 |
| **P3-2** factor-return inputs plan | PLAN DONE | `5466a09` | green (batch; CI-covered at run #89) | `p3_2_decision_record.md` (OD-P3-2-A…O) + `p3_2_factor_return_inputs_implementation_plan.md`: captured/vendor-only v1; the `factor` EV + `factor_return` FR split (the `benchmark` shape); no run/model/snapshot for a captured input; 8-lens, 0 block | build CLOSED (`402cb12`) |
| **P3-2** factor-return inputs impl | **DONE / CLOSED** | impl **`402cb12`** | green (run #89) | **Captured factor-return INPUT foundation.** Net-new `factor` EV definition (canonical id MINTED; identity `(tenant, factor_code, factor_source)`; `REFERENCE.*`-audited) + ENT-025 `factor_return` FR bitemporal series (grain `(tenant, factor_id, return_date, return_type)`; `MARKET.FACTOR_RETURN_*`-audited; migration `0023_factor_return`; NEITHER table append-only); `marketdata/factor.py` binder; symmetric RLS (never hybrid); VENDOR_FACTOR ORIGIN lineage; **`marketdata.view`/`.ingest` REUSED** (no `factor.*` permission); binder-side finiteness guard + `> -1` DQ RANGE; 8 endpoints; 39 factor tests. **Captured INPUT — NO `calculation_run`/`model_version`/snapshot pin** | computed factor returns deferred (need adjusted prices + model_version); `COMPONENT_KIND_FACTOR_RETURN` readiness-noted, NOT minted; LOG return_type reserved |
| **P3-2 closeout / P3-3 readiness** | DONE | `c452229` | green (run #90) | `p3_2_closeout_p3_3_readiness.md` — the resume anchor (HEAD/CI/migration state; the resume housekeeping list; the ratified methodology; the P3-3 scope pointer) | the project-memory refresh + degraded-qualifier cleanup it mandates land in the P3-3 planning commit |
| **P3-3** factor-exposure plan | PLAN DONE | `f941d50` | green (run #91) | `p3_3_decision_record.md` (OD-P3-3-A…O; OQ-P3-3-1…9 recommended defaults) + `p3_3_factor_exposure_implementation_plan.md`: **allocation v1** — indicator-loading CURRENCY-family factor exposures over pinned `exposure_aggregate` atoms × the P3-2 `factor` definitions; governed DERIVED number mirroring the P3-1 exemplar (snapshot + run + registered model_version + IA append-only + `risk.*` REUSE + fail-closed DQ); `COMPONENT_KIND_EXPOSURE`+`COMPONENT_KIND_FACTOR` to mint at build; migration `0024_factor_exposure` planned NOT built; ENT-028-family realization (no new canonical id) | vendor-beta/regression exposures + factor-return consumption deferred (P3-4/v2); ASSET_CLASS+ dimensions deferred |
| **P3-3** factor-exposure impl | **DONE / CLOSED** | impl **`7c50c43`** | green (run #95) | **The SECOND governed risk number REALIZED.** `factor_exposure_result` (ENT-028 family — NO new canonical id; migration `0024_factor_exposure`; IA TRUE append-only + P0001 trigger + symmetric RLS): indicator-loading CURRENCY-family allocation over pinned `exposure_aggregate` atoms × pinned `factor` EV definitions; contributions sum to the pinned total EXACTLY (ε=0, REQ-MKT-003 → In-Progress partial). `run_factor_exposure` mirrors the P3-1 exemplar + the review hardenings: **uniform pre-create adjudication of PINNED content on BOTH entry paths**; **model-identity assert** (`assert_model_version_of` — twin-fixed into `run_sensitivities`); **conflict-safe model registration** (`ModelVersionConflictError` 409, twin-fixed); quantized `INDICATOR_LOADING`; gap-naming `failure_reason`; `COMPONENT_KIND_EXPOSURE` (the first IA pin flavor) + `COMPONENT_KIND_FACTOR` + `PURPOSE_FACTOR_EXPOSURE_INPUT`; `RISK.FACTOR_EXPOSURE_CREATE` reserved-not-emitted; **`risk.view`/`risk.run` REUSED (no new permission)**; `factor_exposure_allocation_v1.md` methodology + governed registration. **ci.yml gained SIX missing per-table PG suites** (benchmark/holdings/synthetic/sensitivity/factor/factor_exposure — run #95 is the FIRST CI execution of the risk-domain RLS/append-only proofs). Max-effort /code-review fallback (10 finder agents → empirical verifier batches → sweep): 15 findings filed, **11 folded**, 4 deferred with rationale (parity/latent). 60 new tests; migration head `0023` → `0024_factor_exposure`. `audit/service.py` FROZEN. | Deferred (recorded): vendor-beta/regression exposures; ASSET_CLASS+ dimensions; `_ERROR_MAP` exact-type lookup; both-modes silent snapshot preference; latent mixed-base grain; GET `failure_reason` persistence; the 3×-snapshot-assembly/4×-DQ-gate/3×-run-scaffold extractions (a dedicated cleanup slice) |
| **P3-4 … P3-7 / P4+** | NOT STARTED | — | — | — | covariance (P3-4, mints `covariance_matrix`), VaR/ES (P3-5, ENT-027), stress (P3-6, RTM-P5), benchmark-relative (P3-7), limits/breach/reporting/validation-workflow — must not be pulled forward; each gated behind its own plan |

## Milestones
- **P1A milestone: CLOSED** — all five rails (P1A-0…P1A-4) committed and CI-green.
- **P1B-0: COMMITTED** — decision record + plan (`dbed93e`) + ratifications (`4fae26b`), CI-green.
- **P1B-1: CLOSED** — reference data committed `6568cb1`, CI-green (run #28). The platform's first hybrid-tenancy / asymmetric-RLS evidence.
- **P1B-2: CLOSED** — legal_entity/issuer/counterparty committed `32c7778`, CI-green (run #31); 7-lens UltraCode reviewed. The proprietary-never-hybrid evidence.
- **P1B-3: CLOSED** — instrument/instrument_terms/identifier_xref committed `8545ed6`, CI-green (run #34); 8-lens UltraCode reviewed. The first real FR/bitemporal slice + EVT-142 activation.
- **P1B-4: CLOSED** — corporate_action committed `060b2a4`, CI-green (run #37); 8-lens UltraCode reviewed. EVT-143 first exercise; capture-only.
- **P1B milestone: DELIVERED** — P1B-1..P1B-4 closed and CI-green (the Security-Master & Reference-Data block; P1B-5 ingestion mapping conditional/deferred).
- **P1B closeout / P1C readiness: DONE** — `e99633a`, CI-green (run #39); 7-lens reviewed.
- **P1C-0: RATIFIED** — decision record + P1C implementation plan `705d3ba` (run #40); ratified into governance this turn (AD-017; REQ-PPM-001 In-Progress; PORTFOLIO.* reserved EVT-150; OD-013/OD-025 closed; portfolio.* grants). 7-lens reviewed.
- **P1C-1 plan: DONE** — portfolio-hierarchy implementation plan `b52ad9e`, CI-green (run #41); 7-lens reviewed.
- **P1C-0 ratification: DONE** — AD-017 + the P1C-0 decisions recorded into governance `dca7bc0`, CI-green (run #42); 6-lens reviewed.
- **P1C-1: CLOSED** — portfolio EV hierarchy + ABAC scope anchor committed `bb89c74`, CI-green (run #43); 8-lens UltraCode reviewed (0 block). The platform's **first domain entity**; `PORTFOLIO.*` (EVT-150/151) first exercise.
- **P1C-2 plan: DONE** — transaction implementation plan `c398215`, CI-green (run #45); 8-lens reviewed.
- **P1C-2: CLOSED** — transaction capture (IA append-only) committed `abb230f`, CI-green (run #46); 8-lens UltraCode reviewed (0 block, 2 LOW folded). The platform's **first domain IA / append-only entity**; `TRANSACTION.*` (EVT-160/161) first exercise; capture-only with reversal-as-new-record.
- **P1C-3 plan: DONE** — position implementation plan `42cc02c`, CI-green (run #48); 8-lens reviewed; OD-P1C3-1..5 signed off.
- **P1C-3: CLOSED** — position capture (FR bitemporal, captured directly) committed `4ee124e`, CI-green (run #49); 8-lens UltraCode reviewed (0 block, 1 LOW folded). The platform's **first FR domain entity**; `POSITION.*` (EVT-170/171/172) first exercise; captured-not-derived with both-axes as-of reconstruction; NOT append-only.
- **CI hygiene: DONE** — GitHub Actions bumped to Node-24 majors (`checkout@v5`/`setup-python@v6`/`setup-node@v5`) `67741fb`, CI-green (run #52); Node-20 deprecation warning eliminated (verified 5→0 annotations).
- **P1C-4 plan: DONE** — valuation implementation plan `92a0264`, CI-green (run #53); 8-lens reviewed; OD-P1C4-1..6 signed off.
- **P1C-4: CLOSED** — valuation capture (FR bitemporal, captured marks) committed `c5c5806`, CI-green (run #54); 8-lens UltraCode reviewed (0 block, 1 LOW folded). The platform's **second FR domain entity**; `VALUATION.*` (EVT-180/181/182) first exercise; captured-not-modeled, `valuation_date` immutable logical-key, both-axes as-of; NOT append-only. **REQ-PPM-003 now Done** (both conjuncts).
- **P1C-4 memory refresh: DONE** — `6e3dcc1`, CI-green (run #55).
- **P1C-5 plan: DONE** — holdings-views implementation plan `8a14173`, CI-green (run #56); 8-lens reviewed (6 approve / 2 approve-with-changes / 0 block); OD-P1C5-1..6 signed off.
- **P1C-5: CLOSED** — read-only as-of holdings / portfolio views committed `0bef45b`, CI-green (run #57); 8-lens UltraCode reviewed (0 block, 2 LOW fence-tightening folds). The platform's **first read-model / composition package**: read-only `irp_shared/holdings/` + `GET /portfolios/{id}/holdings`; NO entity / migration / write endpoint / permission / audit / lineage / DQ; display-only opt-in marks; subtree = composition not enforcement. **No REQ status change.**
- **P1C-5 memory refresh: DONE** — `867e576`, CI-green (run #58).
- **P1C-6 plan: DONE** — deterministic synthetic dataset implementation plan `7dfdb79`, CI-green (run #59); read-only synthetic/test-data audit conclusions folded; OD-P1C6-1..7 signed off.
- **P1C-6: CLOSED** — deterministic synthetic dataset committed `3e9882d`, CI-green (run #60); 8-lens UltraCode reviewed — a false-GREEN env-gate determinism test (BLOCKER) caught + fixed (autouse monkeypatch fixture) + re-validated with `IRP_ALLOW_SYNTHETIC_SEED` unset (`make check` passed, full suite green) and CI run #60 (no such env var) green. The `irp_shared/synthetic` package (uuid5 IDs + fixed `SeedClock`) seeded through the **governed** binders via a keyword-only default-None seam (production call sites unchanged); **never-auto-run + production/non-synthetic refusal guard**; SYNTHETIC tenant under FORCE RLS, never BYPASSRLS; NO entity / migration / real-data / market / risk / exposure / `dataset_snapshot`. **No REQ status change.**
- **P1C block: DELIVERED** — the FULL P1C block (P1C-1 portfolio → P1C-2 transaction → P1C-3 position → P1C-4 valuation → P1C-5 holdings views → P1C-6 synthetic dataset) is complete and CI-green; the capture-only domain-analytics base (AD-017) is realized.
- **P1C-6 memory refresh: DONE** — `9584ba4`, CI-green (run #61).
- **P1C closeout / P2 readiness review: DONE** — `7070dff`, CI-green (run #62); 8-lens. Reproducibility-first P2 sequencing.
- **P2-0: COMMITTED** — decision record + implementation plan `2d19992`, CI-green (run #63); 8-lens, 0 block. OD-P2-A…L; subphases P2-1…P2-6.
- **P2-1 plan: DONE** — dataset_snapshot implementation plan `d7be981`, CI-green (run #64); 8-lens, 0 block. The AD-014 reproducibility-primitive build plan.
- **P2 governance ratification: RATIFIED** — into the source-of-truth `63be23a`, CI-green (run #65); **7-lens, 7× approve, 0 block**. ENT-049/050 + SNAPSHOT.CREATE (EVT-190 reserved) + snapshot.* (reserved) + AD-004-R1 + REQ-PPM-004→In-Progress — RESERVED/PLANNED, no code.
- **P2 ratification closeout memory: DONE** — `d45a31b`, CI-green (run #66); docs-only.
- **P2-1: CLOSED** — `dataset_snapshot` implementation committed `3629baa`, CI-green (run #67); 8-lens UltraCode reviewed (16 agents) → 6 in-scope folds. The AD-014 reproducible input-snapshot primitive (ENT-049/050) REALIZED: IA true-append-only (P0001 trigger + ORM guard, mig `0016_dataset_snapshot`); physical-version pin + `captured_content` + SHA-256 `content_hash` + `manifest_hash`; `build_snapshot` binder; narrow internal lineage writer; caller-side completeness DQ gate; `SNAPSHOT.CREATE` (EVT-190) activated; `snapshot.view`/`.create` minted (data_steward maker; auditor_3l excluded); `POST /snapshots` + `GET /{id}` + `GET /{id}/verify`; symmetric RLS; cross-tenant binding-integrity invariant; **NO exposure number, NO `calculation_run` wiring**. **`migration_head` `0015_valuation` → `0016_dataset_snapshot`** (the first migration since P1C-4). The platform's **first reproducibility primitive**.
- **P2-1 closeout memory: DONE** — `85ff5b2`, CI-green (run #68); docs-only.
- **P2-2 plan: DONE** — `fx_rate` implementation plan `6020b03`, CI-green (run #69); 8-lens, 6 in-scope folds; build-ready (the 10 decisions settled).
- **P2-2: CLOSED** — `fx_rate` implementation committed `c257e5c`, CI-green (run #70); 8-lens reviewed (6 approve / 2 approve_with_changes / 0 block) → 1 in-scope fold (the CTRL-032 lineage/audit rollback test). Captured FX market data (ENT-024, FR) REALIZED: mig `0017_fx_rate` (symmetric RLS, NOT append-only); the `valuation` protocol verbatim; `irp_shared/marketdata/` package; explicit base/quote direction; MID-only; `Numeric(28,12)`; `rate_date` separate logical key; pure published-rate `convert` (direct/reciprocal/triangulation, fail-closed); hybrid-aware `resolve_currency`; `marketdata.view`/`.ingest` minted; `MARKET.FX_*` (EVT-200) activated; VENDOR ORIGIN lineage; new generic `RANGE` DQ evaluator (additive); **NO exposure number, NO `calculation_run` wiring, NO `dataset_snapshot` change**. **`migration_head` `0016` → `0017_fx_rate`** + the new FX symmetric-RLS CI step. The platform's **first market-data entity**; NO visible UI change.
- **P2-2 closeout memory: DONE** — `adf4ac5`, CI-green (run #71); docs-only.
- **P2-3 plan: DONE** — decision record + implementation plan `d10c766`, CI-green (run #72); 8-lens, 10 in-scope folds; the five OQ-P2-3 sign-offs; build-ready. `calculation_run` wiring + basic exposure.
- **P2-3 governance ratification: RATIFIED** — into the source-of-truth `851f976`, CI-green (run #73); **AD-018**; 7-lens, 6 approve / 1 approve_with_changes. ENT-014 ratified-in-planning; the `CALC.RUN_*` doc-vs-code reconciliation; EVT-210 `EXPOSURE.*` reserved; `exposure.*` perms (auditor_3l-in-view); CTRL-009 executable; HALF_UP exception — RATIFIED-IN-PLANNING, no code.
- **P2-3: CLOSED** — `calculation_run` wiring + basic exposure implementation committed `da178fc`, CI-green (run #74); 8-lens reviewed (5 approve / 3 approve_with_changes / 0 block) → 2 in-scope folds (consume-path EXPOSURE_INPUT gate; FAILED-run-readable run reader). The **first governed derived number** — `exposure_aggregate` (ENT-014, IA append-only) — REALIZED: signed market value v1; run-bound + snapshot-gated; effective composite fx_rate + fx_legs; COMPONENT_KIND_FX + snapshot FX-pinning; CALC.RUN_* reuse + additive outcome param; pre-create-refusal/post-create-FAILED; lineage snapshot→run→result; exposure.view/.aggregate.run; symmetric RLS; **NO risk** (MARKET_VALUE only). **`migration_head` `0017_fx_rate` → `0018_exposure_aggregate`** (+ additive `calculation_run.environment_id`) + the new Exposure symmetric-RLS CI step. The AD-014/FW-RUN/TR-15 gate is now load-bearing.
- **P2-3 closeout memory: DONE** — `0b12d85`, CI-green (run #75); docs-only.
- **P2-4 plan: DONE** — decision record + implementation plan `b73e65f`, CI-green (run #76); 8-lens, 4 in-scope folds; the six OQ-P2-4 sign-offs; build-ready. `price_point` (ENT-020) FR/bitemporal captured price history.
- **P2-4: CLOSED** — captured price history implementation committed `2b63b76`, CI-green (run #77); 8-lens reviewed (7 approve / 1 approve_with_changes / 0 block) → 1 in-scope fold (REQ-PUB-001 RTM/backbone advancement). The platform's **second market-data entity** — `price_point` (ENT-020, FR/bitemporal captured vendor prices) — REALIZED: the `fx_rate` protocol verbatim (`marketdata/price.py` binder + `PricePoint` model); the 6-part current-head key with **price_source-in-key** (multi-vendor coexistence) + promoted key columns DB-level NOT NULL; `price_date` separate immutable logical key; `price` Numeric(20,6); `price_type` {CLOSE,MID,NAV}; RAW-only (no adjustment engine); captured `currency_code`, NO conversion; `instrument_id` NOT-NULL FK; `MARKET.PRICE_*` (EVT-200) caller-side; `VENDOR_PRICE` ORIGIN lineage; **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + strictly-positive RANGE DQ; symmetric RLS; **snapshot readiness-only** (NO `COMPONENT_KIND_PRICE`); **NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/FX change**. **`migration_head` `0018_exposure_aggregate` → `0019_price_point`** + the new Price-point symmetric-RLS CI step. **REQ-PUB-001 → In-Progress (partial)** (the as-of leg; staleness/QS-16 deferred).
- **P2-4 closeout memory: DONE** — `419db9d`, CI-green (run #78); docs-only.
- **P2-5 plan: DONE** — decision record + implementation plan `326ad94`, CI-green (run #79); 8-lens, 8 in-scope folds; the ten OQ-P2-5 sign-offs; build-ready. The unified `curve` (ENT-021) + `curve_point`; ENT-023 `credit_spread` by value.
- **P2-5: CLOSED** — captured yield/spread curves implementation committed `49ca3bd`, CI-green (run #80); 8-lens reviewed (7 approve / 1 approve_with_changes / 0 block) → 1 material fold (REQ-PUB-002/003 RTM/backbone advancement) + 3 low. The platform's **third market-data entity** — the unified `curve` (FR header, ENT-021) + `curve_point` (IA append-only nodes); ENT-023 `credit_spread` realized BY VALUE — REALIZED: the `fx_rate`/`price_point` FR protocol + the `dataset_snapshot`/`component` header+detail split; the 6-part current-head key + promoted key cols NOT NULL; `curve_point` UNIQUE (curve_id, value_type, tenor_days); `curve_date` separate immutable logical key; `curve_type` {TREASURY,GOVT,SWAP,OIS,CREDIT_SPREAD}; `value_type` {ZERO_RATE,PAR_RATE,DISCOUNT_FACTOR,SPREAD}; `point_value` Numeric(20,12); `tenor_label` + normalized `tenor_days`; `reference_key` opaque (NOT an FK) + the `curve_type`↔`reference_key` invariant; `interpolation_method` inert; `MARKET.CURVE_*` (EVT-200, one event per curve); `VENDOR_CURVE` ORIGIN lineage; **reuse `marketdata.view`/`.ingest`**; value-type-conditional RANGE DQ; symmetric RLS both tables; **snapshot readiness-only** (NO `COMPONENT_KIND_CURVE`); **NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point` change; NO curve construction/interpolation/duration/pricing/risk**. **`migration_head` `0019_price_point` → `0020_curves`** + the new Curve symmetric-RLS CI step. **REQ-PUB-002 + REQ-PUB-003 → In-Progress (partial).**
- **P2-5 closeout memory: DONE** — `0c5c068`, CI-green (run #81); docs-only.
- **P2-6 plan: DONE** — decision record + implementation plan `8d2782f`, CI-green (run #82); 8-lens, 2 HIGH + 1 MED + 21 LOW folds; the eleven OQ-P2-6 sign-offs; build-ready. benchmark EV definition + benchmark_constituent FR membership (ENT-009); the P3-conditionality RESOLVED + OQ-P2-6-11 Option A.
- **operating rules: DONE** — provenance/dates + local-PG-validation-container standing rules `1e0dc08`, CI-green (run #83); docs-only project-memory.
- **P2-6: CLOSED** — captured benchmark/index data implementation committed `b6284a4`, CI-green (run #84); 8-lens reviewed (4 approve / 4 approve_with_changes / 0 block) → every finding folded (incl. a real read-endpoint 500→404 fail-closed-status bug + 5 added tests). The platform's **fourth market-data entity** — `benchmark` (ENT-009, EV definition) + `benchmark_constituent` (FR/bitemporal membership) — REALIZED: identity (tenant, benchmark_code, benchmark_source); constituent 4-part current-head key (tenant, benchmark_id, instrument_id, effective_date); effective_date a separate immutable logical key; weight Numeric(20,12) RANGE [0,1]; instrument_id NOT-NULL FK; membership captured/superseded/corrected as a set per (benchmark, effective_date); the OQ-P2-6-11 Option A audit split (REFERENCE.* definition / MARKET.BENCHMARK_CONSTITUENT_* membership, set-grained); VENDOR_BENCHMARK lineage; reuse marketdata.view/.ingest; required-field + weight RANGE [0,1] DQ; symmetric RLS both tables; NEITHER table append-only; snapshot readiness-only (NO COMPONENT_KIND_BENCHMARK); benchmark_level/return DEFERRED (no new canonical id); NO calculation_run/exposure_aggregate/dataset_snapshot/fx_rate/price_point/curve change; NO performance/attribution/active-risk/factor/reporting. **`migration_head` `0020_curves` → `0021_benchmark`** (NEITHER table append-only). **REQ-PUB-003 benchmark leg advanced.**
- **P2 BLOCK: DELIVERED** — the FULL P2 captured market-data / reproducibility foundation (P2-1 dataset_snapshot 3629baa / P2-2 fx_rate c257e5c / P2-3 calculation_run+exposure da178fc / P2-4 price_point 2b63b76 / P2-5 curve+curve_point 49ca3bd / P2-6 benchmark+benchmark_constituent b6284a4) is complete and CI-green.
- **P2-6 closeout memory: DONE** — `ae2be8e`, CI-green (run #85); docs-only.
- **P2 closeout / P3 readiness review: DONE** — `bb73211` (CI re-trigger `6663452`), CI-green (run #86); the P3 entry-option assessment.
- **P3-0: RATIFIED** — risk-analytics decision record + P3 implementation plan `07607a5`, CI-green (run #87); 8-lens (4 approve / 4 approve_with_changes / 0 block); OD-P3-0-A…N + the OQ-P3-0-1…10 sign-offs. Analytic-sensitivities-first; the derived-number output contract; `RISK.*`@EVT-220 + `risk.*` reservations; `05_analytics_methodologies/` home.
- **P3-1 plan: DONE** — `1a8b2a4`, CI-green (run #88); 8-lens (5 approve / 3 approve_with_changes / 0 block; 0 high / 0 medium); OQ-P3-1-1…6 ratified (curve-intrinsic v1; the 5-tuple grain; ACT/365F conventions; model_version-required).
- **P3-1: CLOSED** — analytic sensitivities + model-governance hardening committed `e8e2e59` (batch-pushed; CI-covered at `402cb12` run #89). The platform's **first reproducible governed RISK number** (ENT-028 `sensitivity_result`, migration `0022_sensitivity`); **CTRL-003 EXECUTABLE** (`assert_registered_model_version` pre-create); `risk.view`/`risk.run` minted; `COMPONENT_KIND_CURVE`; the methodology framework + `sensitivities_analytic_v1.md`.
- **P3-2 plan: DONE** — `5466a09` (batch-pushed; CI-covered at run #89); 8-lens (5 approve / 3 approve_with_changes / 0 block); captured-only v1; the `factor` EV + `factor_return` FR split.
- **P3-2: CLOSED** — factor-return input foundation committed `402cb12`, CI-green (run #89). The `factor` canonical id MINTED + ENT-025 `factor_return` realized (migration `0023_factor_return`); captured INPUT (no run/model/snapshot); `MARKET.FACTOR_RETURN_*` + `REFERENCE.*`; `marketdata.*` reused; 39 factor tests. The degraded-connectivity window (Zscaler push/CI blockage, 2026-07-02) is RESOLVED — the whole chain is pushed + CI-green.
- **P3-2 closeout / P3-3 readiness handoff: DONE** — `c452229`, CI-green (run #90); the resume anchor for the machine move.
- **P3-3 plan: COMMITTED** — `f941d50`, CI-green (run #91): `p3_3_decision_record.md` + `p3_3_factor_exposure_implementation_plan.md` + the project-memory refresh + the 5-doc degraded-qualifier cleanup + the model-agnostic trailer rule. **P3-3 implementation NOT started** (gated on the OQ-P3-3-1…9 sign-offs + explicit approval; machine prerequisite: stand up `irp_pg_local`).
- **P3-3: CLOSED** — factor-exposure engine (allocation v1) committed `7c50c43`, CI-green (run #95; runs #94 `bd5ba3c` gate-tiers + #95 both green). The second governed risk number; the max-effort adversarial review folded 11/15 findings pre-commit; CI now executes ALL per-table PG suites for the first time.
- **Model-upgrade operating-discipline housekeeping: COMMITTED** (`b3d3923`, #92) — was: IN WORKING TREE (user-approved 2026-07-06, docs-only) — the rewritten review pattern (`/code-review ultra` / authorized subagents / single-pass floor) + the "Verification & objectivity" standing rules in `claude_operating_instructions.md`; NEW repo-root `CLAUDE.md`; `project_state.yaml` RETIRED to a stub; `current_state.md` thinned; memory advanced to `f941d50`/#91.

## CI job shape (the `migration` Postgres job, all green at HEAD)
`alembic upgrade head` → `alembic check` (drift) → audit concurrency → tenant-context RLS → lineage RLS →
model-registry RLS → data-quality RLS → ingestion RLS + append-only → **Reference hybrid-RLS (REQ-SMR-005 /
AD-013-R1)** → **Legal-entity symmetric-RLS (REQ-SMR-002 / OD-P1B-D)** → **Instrument/identifier symmetric-RLS
+ FR-bitemporal (REQ-SMR-001/003 / OD-P1B-A/G)** → **Corporate-action symmetric-RLS (REQ-SMR-004 /
OD-P1B-B)** → **Portfolio symmetric-RLS (REQ-PPM-001 / AD-017)** → **Transaction symmetric-RLS +
append-only (REQ-PPM-003 / AD-017 / BR-17/BR-18)** → **Position symmetric-RLS + FR-bitemporal
(REQ-PPM-002 / AD-005 / BR-17/BR-19)** → **Valuation symmetric-RLS + FR-bitemporal (REQ-PPM-003 /
AD-005 / BR-17/BR-19)** → **Snapshot symmetric-RLS + append-only (AD-014 / ENT-049/050 / BR-17/BR-18)** → **FX rate
symmetric-RLS + hybrid-currency (ENT-024 / OD-P2-E/G / BR-17)** → **Exposure symmetric-RLS + append-only (ENT-014 / AD-014 /
AD-018 / BR-17/BR-18)** → **Price-point symmetric-RLS + FR-bitemporal (ENT-020 / OD-P2-4-* / BR-17)** → **Curve symmetric-RLS +
FR/append-only (ENT-021/023 / OD-P2-5-* / BR-17/BR-18)** → **Benchmark symmetric-RLS + EV/FR (ENT-009 / OD-P2-6-* /
BR-17; NEITHER append-only)** → downgrade base. **P3-1 added the sensitivity backend suite** (migration `0022_sensitivity`
— `sensitivity_result` IA append-only + symmetric RLS) and **P3-2 the factor suite** (migration `0023_factor_return` —
`factor` EV + `factor_return` FR, NEITHER append-only); the migration job auto-covers the new heads (the P2-6 precedent —
no new explicit CI step). Plus Backend (ruff format/lint, mypy, pytest — Python 3.12 runners), Frontend, Documentation
check, Secret scan. All jobs green at HEAD `c452229` (run #90; P3-2 impl `402cb12` = run #89). The `migration` job also
runs `alembic upgrade head` (0001→0023) + `alembic check` (drift-clean) + the downgrade smoke (`0023→0022→base→head`).
