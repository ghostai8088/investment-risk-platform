# Decision Summary

> **As of HEAD `f941d50` (refreshed 2026-07-06).** Ratified and load-bearing decisions. **Do not relitigate the "Ratified" items unless
> the user explicitly reopens them.** Authoritative sources: `11_decision_log/architecture_decision_log.md`
> (AD-*), the per-slice plan docs, and `10_delivery_backlog/{p1b0_decision_record,p2_0_decision_record}.md` (OD-*).

## Major ratified architecture decisions (AD-*, Accepted by H-04)
- **AD-004** segregated/append-only audit store; native uuid + JSONB on Postgres; datastore = PostgreSQL SoR + TimescaleDB for market data (behind a repo interface) + S3 + segregated audit.
- **AD-004-R1** (Accepted P2-0, 2026-06-26) **storage-split refinement of AD-004 — Postgres-first behind the market-data repo interface.** ENT-020…025 market data + the ENT-049/050 reproducibility snapshots land in **Postgres** (reusing FORCE-RLS/audit/lineage/DQ) **through the AD-004 repo interface**; **TimescaleDB deferred** to a measured volume/perf threshold (a non-breaking future swap). **Honest deviation from AD-004's "Timescale initially."** Resolves **OD-014**; the future Timescale leg must enforce tenant scoping in the interface.
- **AD-014** (Accepted H-06/H-04) **no governed derived output without a bound, reproducible input snapshot** — the gate the P2 `dataset_snapshot` (ENT-049/050) realizes; reopened by P2 and now **LOAD-BEARING end-to-end at P2-3** (`da178fc`, CI-green): the snapshot primitive (P2-1) + the **first governed derived number — `exposure_aggregate`** (ENT-014, **AD-018**) — is produced ONLY when bound to a `dataset_snapshot` + a complete `calculation_run` (FW-RUN §5/TR-15). The gate had no producer before P2-3.
- **AD-005** **selective bitemporality**: **FR** (bitemporal) for risk-driving inputs, **IA** (immutable append-only) for outputs/events/audit, **EV** (effective-dated) for reference/config. Risk = entity misclassification.
- **AD-007** real identity via OIDC/SSO (deferred; dev header shim is not a security boundary).
- **AD-008 / BR-17** tenant isolation; investment data MNPI-adjacent → isolation by default.
- **AD-013** **hybrid reference-data tenancy**: global system reference shared read-only; investment reference tenant-scoped; tenant-override pattern; no cross-tenant proprietary sharing. (P1B-0 **ratifies** refinement **AD-013-R1** — Accepted (H-04), in the decision log — see below.)
- **AD-015 / AD-016** RLS tenant context via `set_config`; BYPASSRLS reserved to the ops role.
- **AD-017** (Accepted H-06/H-04, P1C-0, 2026-06-23) **P1C is a capture-only domain block**: portfolio (EV) / transaction (IA) / position (FR) / valuation (FR) are CAPTURED / as-of-reconstructable, **not** derived analytics; **no** exposure aggregation / risk / pricing / valuation models / corporate-action application / `dataset_snapshot` (stay P2, AD-014). The portfolio hierarchy is the ABAC portfolio-scope **anchor**; enforcement → P6+. Conforms to AD-005/AD-013-R1/AD-014/AD-008.
- **BR-3** inventoried-before-use (models); **BR-7** override fields; **BR-10** no secrets in source; **BR-11** deny-by-default; **BR-12** non-bypassable audit; **BR-13** lineage; **BR-16** AI-agent logging; **BR-19** declare `__temporal_class__`.

## P1A-0 decisions (tenant context / RLS)
- Tenant context set via `set_config('app.current_tenant', …, true)` — **never** a parameterized `SET`.
- FORCE ROW LEVEL SECURITY + policy with `USING` **and explicit `WITH CHECK`** (the explicit WITH CHECK pattern is from migration `0004`+; the `0001` foundation tables are `USING`-only).
- PG RLS tests run under a constrained **non-superuser `irp_app`** role (NOSUPERUSER NOBYPASSRLS) — superusers bypass RLS even under FORCE.
- Durable pool-checkin `RESET`; single-transaction-per-request invariant (no mid-request commit, or RLS fails closed). Dev `X-User-Id`/`X-Tenant-Id` header shim is **not** a security boundary until SSO (DR-P1A0-3).

## P1A design decisions (per slice)
- **P1A-1:** `data_source` EV + `lineage_edge` IA; polymorphic `(target_entity_type, target_entity_id)`, **no domain FK**; `record_lineage` is co-transactional, server-stamps tenant, fails closed on cross-tenant source; lineage edges are metadata of an already-audited governed write (no per-edge audit event). Audit codes `DATA.SOURCE_REGISTER/UPDATE`.
- **P1A-2:** `model` EV + `model_version`/assumption/limitation IA; reuse `MODEL.REGISTER`/`MODEL.VERSION`; `register`-as-maker preserves SoD (developer ≠ validator). DR-P1-3 maker-checker hooks reserved, non-enforcing.
- **P1A-3:** `data_quality_rule` EV + `data_quality_result` IA; **exactly two generic evaluators** (`not_null`, `allowed_values`) — extend by value + registry entry, never schema. **No-silent-failure** (ERROR raises + persists flag; WARNING flags-only; evaluator error propagates + audited `outcome='failure'`). `assert_passed_quality_checks` is the fail-closed gate. Audit `DATA.VALIDATE` (runs) + `DATA.DQ_RULE_DEFINE/UPDATE`.
- **P1A-4:** `ingestion_batch` **IA-classed but status-mutable** (the CalculationRun precedent — NOT in `APPEND_ONLY_TABLES`); `ingestion_staged_record` IA immutable (in `APPEND_ONLY_TABLES` + ORM guard + P0001 trigger). CSV anti-corruption: 10 MiB cap counted while reading, CSV-only allowlist, filename sanitization, encoding validation, formula-injection neutralization, ragged-row rejection, no-op AV seam (`scan_status`, OD-042). Composes P1A-1 lineage + P1A-3 DQ; **durable-evidence-on-reject** (REJECTED batch + flagged result + audit committed; 4xx, never 200). Activates `DATA.INGEST`; reuses `data.upload` (no new audit code, no new permission). `data_quality_result.ingestion_batch_id` populated via an additive `run_quality_check` kwarg (set-before-flush; the only P1A-3 service change).

## P1B open decisions resolved in P1B-0 (OD-P1B-A … OD-P1B-J)
Recorded in `10_delivery_backlog/p1b0_decision_record.md` (**committed at `dbed93e`**, CI-green; 7-lens reviewed).
**Ratifications COMMITTED into the governance source-of-truth at `4fae26b`** (CI-green): **AD-013-R1**
(decision log); **REQ-SMR-005** + REQ-SMR-001/003/004 annotations + CAP-2.5 re-partition (backbone/RTM/capability
map); ENT-001..008 annotations (canonical model + temporal §2A); **`REFERENCE.*`** reserved (audit
taxonomy); reference permissions (entitlement model). Audit codes + entitlement bootstrap **code** are minted in
the P1B build slices (currency/calendar/rating_scale codes + perms landed in P1B-1). Summary of the resolutions:
- **OD-P1B-A** Instrument split: `instrument` = **EV** identity + `instrument_terms` = **FR** (per AD-005 §2A / REQ-SMR-001). Canonical annotation (ENT-001 realized as two tables).
- **OD-P1B-B** `corporate_action` = **EV** (AD-005 §2A / REQ-SMR-004); status history via audit trail.
- **OD-P1B-C** Hybrid tenancy via **SYSTEM_TENANT rows + asymmetric RLS** (`USING own OR SYSTEM` / `WITH CHECK` single-tenant); closed hybrid set = **{currency, calendar, rating_scale}**; proprietary entities never hybrid; override-wins is application-layer. **Refinement ADR AD-013-R1** (R-04/R-05/H-04).
- **OD-P1B-D** Shared `legal_entity` **core (implementation-only, no ENT id)** + separate `issuer`/`counterparty` role profiles (preserves canonical ENT-002/003).
- **OD-P1B-E** New **`REFERENCE`** audit category (EVT-140 block: `CREATE/UPDATE/CORRECTION/STATUS_CHANGE`); reconciled vs reserved `DATA.CORRECTION` / TR-08. R-07.
- **OD-P1B-F** Add `currency`/`rating_scale`/`legal_entity` + missing `.view` permissions; reserve `reference.rating.*`. R-07.
- **OD-P1B-G** Deterministic single-result-or-`AmbiguousIdentifier` resolution; partial-unique index `(tenant_id, scheme, value) WHERE valid_to IS NULL`; cross-vendor precedence deferred (OD-012 → P1C; REQ-SMR-003 partial).
- **OD-P1B-H** New web-framework-free `irp_shared.reference` package; one-way deps (→ lineage/dq/audit/entitlement only; no risk/portfolio/ingestion-mapping/reporting in the CRUD core).
- **OD-P1B-I** Per-tenant `MANUAL` `data_source`; lineage **rooted in each row's own context** (SYSTEM-context for global rows) — `data_source` is **not** made hybrid.
- **OD-P1B-J** Mint **REQ-SMR-005** (currency, rating_scale) + re-partition CAP sub-cap 2.5 + annotate ENT-007 EV/FR. R-02/R-05.

## P1B-1 design decisions (REALIZED — `6568cb1`, CI-green)
The first reference-data slice and the platform's first hybrid-tenancy / asymmetric-RLS evidence (AD-013-R1).
- **Five EV tables** (mig `0008`): `currency` (ENT-005), `calendar`+`calendar_holiday` (ENT-006), `rating_scale`+`rating_grade` (ENT-007 **taxonomy only** — zero assignment columns; FR rating assignments deferred). `UNIQUE(tenant_id, code)`, **never `UNIQUE(code)`**; no append-only trigger (all EV-mutable — a `REFERENCE.UPDATE` succeeds at the DB).
- **Asymmetric hybrid RLS** (net-new, distinct from the symmetric loop): `USING (own-tenant OR SYSTEM_TENANT) / WITH CHECK (own-tenant only)`. The SYSTEM literal is in `USING`/`qual` but **never** in `WITH CHECK` (a tenant overwriting global vocab = cross-tenant breach); FORCE RLS + own policy on every table **including children**; closed hybrid set = exactly these 5 (`data_source` stays symmetric — NOT hybrid). SYSTEM_TENANT_ID injected as a fixed literal from `entitlement.bootstrap`.
- **Tenant override wins = APPLICATION-LAYER dedup** (`service.dedupe_tenant_wins`, DISTINCT-ON-by-`code`, own-tenant preferred) — NOT an RLS merge.
- **`REFERENCE.CREATE` (EVT-140) / `REFERENCE.UPDATE` (EVT-141) activated** as caller constants to the FROZEN `record_event`; children fold into the parent event (no own event); DC-2 metadata only; per-tenant + SYSTEM chains. `CORRECTION`/`STATUS_CHANGE` reserved, not emitted.
- **Lineage** (OD-P1B-I): one ORIGIN edge per entity from a per-tenant **MANUAL** `data_source` (`ensure_manual_source`, idempotent); SYSTEM seeds rooted on the SYSTEM chain. New web-framework-free **`irp_shared.reference`** package (one-way deps). Additive `reference.currency.*` / `reference.rating_scale.*` / `reference.calendar.view` perms seeded via the existing `0002` catalog path (no new audit framework code). Governed SYSTEM seeder is test-proven (not yet wired to prod).

## P1B-2 design decisions (REALIZED — `32c7778`, CI-green; 7-lens reviewed)
REQ-SMR-002 (OD-P1B-D); migration `0009`. The contrast with P1B-1: these are **PROPRIETARY** entities, so they are the inverse of P1B-1's hybrid model.
- **Shared `legal_entity` core, IMPLEMENTATION-ONLY (NO canonical ENT id)** (OD-P1B-D) + **separate 1:1 `issuer` (ENT-002) / `counterparty` (ENT-003) role profiles** (`UNIQUE(tenant_id, legal_entity_id)`, NOT-NULL FK; a legal entity may carry both). The unified-table-with-role-flags alternative is REJECTED. All three **EV** (one physical row per logical entity).
- **Tenant-scoped SYMMETRIC RLS — NEVER hybrid / NEVER SYSTEM_TENANT** (OD-P1B-C proprietary invariant): `USING == WITH CHECK == own-tenant`, no global rows. Cross-tenant leakage blocked on every axis (symmetric reads/`WITH CHECK`, **explicitly-tenant-filtered** profile→core + parent resolution, positive + closed-set `pg_policies` guards). The resolvers carry an EXPLICIT `tenant_id` predicate (the `ensure_manual_source` pattern, NOT id-only `register_model_version`) so cross-tenant fails closed on SQLite too.
- **Hierarchy STRUCTURE in P1B-2** (`parent_legal_entity_id` self-FK adjacency + a bounded, cycle-safe, tenant-filtered `resolve_ultimate_parent`, depth cap 32, no exposure math); the **exposure-rollup CALCULATION is DEFERRED**. **No netting / CSA / collateral / exposure columns** (OD-015 deferred).
- Reuse `REFERENCE.CREATE/UPDATE` (each entity OWN event — NOT folded, unlike P1B-1 children; `audit/service.py` FROZEN); one MANUAL-`data_source` ORIGIN edge per row. Additive `reference.legal_entity.view/edit` only (issuer/counterparty perms exist); `legal_entity.view` matches the issuer/counterparty recipient set — **EXCLUDES `auditor_3l`** (proprietary-identity SoD).

## P1B-3 design decisions (REALIZED — `8545ed6`, CI-green; 8-lens reviewed)
REQ-SMR-001 + REQ-SMR-003 (OD-P1B-A/G); migration `0010`. The platform's **first real FR / bitemporal** slice.
- **`instrument` = EV identity-only** + **`instrument_terms` = FR** (OD-P1B-A) + **`identifier_xref` = EV**. `instrument` carries NO terms/price/valuation/risk columns; `is_active` is the single lifecycle flag (no `status` string); nullable `issuer_id` FK → the issuer profile (cash/FX/index allowed). `currency_code` is a plain ISO string (NOT a FK to the hybrid `currency` table).
- **FR bitemporal protocol** (the net-new mechanism): `FullReproducibleMixin` (`valid_from/valid_to` + `system_from/system_to`) — its **first persisted user**. create → effective-dated supersede (close prior `valid_to`) → as-known correction/restatement (close prior `system_to`). **One `now` per op** (so `prior.system_to == corrected.system_from`); **close-first ordering** (prior close-out flushed before the new row, so the dual-open current-head partial-unique is never transiently violated); prior versions' economics **never mutated**; the FR table is **NOT** append-only (no `irp_prevent_mutation` trigger — the close-outs UPDATE). `reconstruct_terms_as_of(valid_at, known_at)` proves reconstruction on **BOTH** axes; `known_at` default = now = current view.
- **`REFERENCE.CORRECTION` (EVT-142) ACTIVATED** (R-07 sign-off, OQ-7) for the terms restatement path — caller-side only via a NEW `record_reference_correction` in `reference/service.py`; **`audit/service.py` stays FROZEN**. TR-08 `restatement_reason` recorded on the canonical `justification` field + `supersedes_id` link in DC-2 `after_value`. EVT-143 STATUS_CHANGE still reserved.
- **Deterministic identifier resolution** (OD-P1B-G / CTRL-029): `resolve_identifier` returns exactly one Instrument / `None` / `AmbiguousIdentifier` (>1 distinct entity_ids) — **never a silent arbitrary match**. Active partial-unique `(tenant_id, scheme, value) WHERE valid_to IS NULL`; ambiguity is reachable via historical-overlap + past as-of (defense-in-depth). Polymorphic `(entity_type, entity_id)` no-FK, scoped to `entity_type='instrument'`. Cross-vendor precedence DEFERRED (OD-012 → P1C); no external/check-digit validation.
- **Symmetric proprietary RLS** (byte-for-byte the `0009` loop; NEVER hybrid; closed-hybrid-set still the 5). Cross-tenant `issuer_id`/`instrument_id`/`entity_id` fail closed via the **service-layer** `*NotVisible` resolver predicate pre-commit (RLS `WITH CHECK` gates only the row's own `tenant_id`). Additive `reference.identifier.view/edit` (`.resolve` recipients UNCHANGED — not widened to risk_manager_2l; `auditor_3l` excluded; parity test).

## P1B-4 design decisions (REALIZED — `060b2a4`, CI-green; 8-lens reviewed) — the P1B block is DELIVERED
REQ-SMR-004 corporate_action (OD-P1B-B); migration `0011`. The **last reference entity** — capture-only.
- **`corporate_action` = EV** effective-dated reference data (NOT IA, NOT FR). One physical row; amend = in-place EV supersede (`REFERENCE.UPDATE`). The EV `valid_from/valid_to` **record** axis is distinct from the inert **business-date** columns (`announcement/ex/record/pay/effective_date`); `ratio/amount/currency_code` are inert placeholders. **Single `status` lifecycle** (ANNOUNCED→CONFIRMED→CANCELLED, terminal; **no `is_active`** — the P1B-3 `arch-1` dual-flag lesson); a thin transition guard rejects illegal/no-op/out-of-vocab moves with **no DB write** (validation, not a workflow/state-machine engine).
- **`REFERENCE.STATUS_CHANGE` (EVT-143) ACTIVATED** (R-07 sign-off, OQ-1) — the platform's **first persisted EVT-143 user** — caller-side via a NEW `record_reference_status_change` in `reference/service.py` (NO new lineage edge; `before/after = {status}` + optional `justification`); **`audit/service.py` stays FROZEN**. Used **only** for corporate_action status transitions; other entities' `is_active` flips still ride `REFERENCE.UPDATE`, and the existing EVT-143-reservation tests stay green (entity-scoped). **No `REFERENCE.CORRECTION`** (FR-only).
- **`instrument_id` NOT-NULL FK** to the P1B-3 `instrument` head, resolved via the **reused** `resolve_instrument` tenant-filtered → cross-tenant/unknown fails closed (`InstrumentNotVisible`) pre-commit. Symmetric proprietary RLS (byte-for-byte the `0010` loop). Additive `reference.corporate_action.view` (== instrument.view set; `auditor_3l` excluded; parity test); `.edit` pre-existing unchanged.
- **CAPTURE-ONLY** (the load-bearing scope fence): **NO** application to positions/valuations, **NO** entitlement/tax calc, **NO** event-processing engine, **NO** roll/day-count math (QS-10/11 → P1C), **NO** vendor feed/reconciliation/override workflow. "No double-apply" holds trivially (nothing is ever applied); a scope-fence test pins absence of any applied/position/valuation/entitlement/tax column.

## P1C-0 decisions (RATIFIED into governance — `705d3ba` + ratification this turn; 7-lens reviewed)
The twelve P1C decisions (OD-P1C-A..L, `10_delivery_backlog/p1c0_decision_record.md`) + **AD-017** are recorded into the source-of-truth.
- **Portfolio model (OD-P1C-C / REQ-PPM-001 / ENT-010 / OD-013 closed):** a **single `portfolio` EV table** with a `node_type` controlled-vocab (`PORTFOLIO`/`FUND`/`STRATEGY`/`ACCOUNT`) + `parent_portfolio_id` self-FK adjacency (arbitrary tree, depth-capped 32) — **not** four tables. The entitlement scope **anchor**.
- **ABAC (OD-P1C-A/B; OD-025 closed = portfolio-level; OQ-014 = subtree):** P1C-1 **anchors** portfolio scope (records the hierarchy + bounded descendant resolver) but does **NOT enforce** it — enforcement → P6+. Residual risk: within a tenant any `portfolio.view` holder sees all portfolios (acceptable: synthetic data). ENT-P-06 partially satisfied (tenant enforced; portfolio-scope anchored).
- **Audit (PORTFOLIO.\*):** RESERVED at the **EVT-150 block** (`PORTFOLIO.CREATE`=150 / `.UPDATE`=151 / `.STATUS_CHANGE`=152) in `audit_event_taxonomy.md` — NOT emitted; activated caller-side only in the P1C-1 build; `audit/service.py` FROZEN. Status flips ride on `PORTFOLIO.UPDATE` (STATUS_CHANGE reserved-not-required for P1C-1).
- **Entitlement (§5B):** `portfolio.view`/`portfolio.edit` (domain codes, not `reference.*`); `data_steward` gets **both** (maker reads its own writes); `portfolio.view` → risk_analyst_1l/risk_manager_2l (+ admin); `portfolio.edit` maker/admin only; `auditor_3l` excluded.
- **Capture-only (OD-P1C-E/F/G/H/I, AD-017):** positions/valuations captured-not-derived (no derivation engine, no calc); transactions IA log; **no** exposure aggregation / `dataset_snapshot` (OQ-013a → stays P2) / corporate-action application / market data / pricing / valuation models.
- **Synthetic data (OD-P1C-L):** deterministic governed synthetic seed (uuid5 + fixed timestamps; labeled, never-auto-run; no real client/vendor data) — a P1C prerequisite replacing P1B-5.
- **Subphase structure:** P1C-1 portfolio → P1C-2 transactions → P1C-3 positions → P1C-4 valuations → P1C-5 as-of views → P1C-6 synthetic dataset.

## P1C-1 design decisions (REALIZED — `bb89c74`, CI-green run #43; 8-lens reviewed, 0 block) — the first DOMAIN entity
REQ-PPM-001 (migration `0012`); `portfolio` (ENT-010, EV) hierarchy + the entitlement portfolio-scope **ANCHOR**.
- **`portfolio` = EV single table** — `__temporal_class__ = EFFECTIVE_DATED`; amend = in-place supersede (`record_version` bump + `PORTFOLIO.UPDATE`); NOT append-only; NO `system_*`/FR axis; single `status` (no `is_active`, the P1B-3 arch-1 lesson). `node_type`/`status` controlled-vocab plain Strings (no enum/CHECK). A portfolio **holds nothing** (no position/valuation/holding/exposure column — scope fence).
- **Hierarchy:** `parent_portfolio_id` intra-tenant self-FK adjacency (NULL = root); `UNIQUE(tenant_id, code)`; self-parent rejected; **bounded ANCESTOR** (`resolve_ultimate_parent`, reuse of the `legal_entity` shape) + **NEW DESCENDANT** (`resolve_descendants`, downward subtree BFS) resolvers, both `MAX_HIERARCHY_DEPTH=32` + visited-set + `HierarchyCycleError` + per-hop tenant predicate; re-parent re-runs a write-time cycle guard.
- **ABAC anchor-not-enforce (AD-017 / OD-P1C-A/B):** the descendant resolver records future **subtree** scope semantics, but NOTHING reads/filters by scope. `portfolio.view` gates by role+tenant only; within a tenant any view-holder sees ALL portfolios (documented residual risk + tested scope fence). Enforcement → **P6+**. ENT-P-06 partially satisfied (tenant enforced, portfolio-scope anchored).
- **`PORTFOLIO.CREATE` (EVT-150) / `PORTFOLIO.UPDATE` (EVT-151) ACTIVATED** — caller-side constants (new `irp_shared/portfolio/events.py`) to the **FROZEN** `record_event`; per-tenant chain; DC-2 metadata; a `status` flip rides on `PORTFOLIO.UPDATE` (no STATUS_CHANGE in P1C-1; EVT-152 reserved). `audit/service.py` UNTOUCHED.
- **Symmetric proprietary RLS** (migration `0012`, byte-for-byte the `0011` loop); **NEVER hybrid** (closed 5-table hybrid set asserted unchanged); cross-tenant `parent_portfolio_id` fails closed at the **service layer** (`resolve_portfolio` → `PortfolioNotVisible`) pre-commit. **Fail-closed audit rollback (CTRL-032)** proven by a negative test.
- **Entitlement:** `data_steward` granted BOTH `portfolio.view` + `portfolio.edit` (maker reads its own writes); existing `portfolio.view` recipients unchanged; `portfolio.edit` maker/admin only; `auditor_3l` excluded; parity-tested.
- **New `irp_shared/portfolio/` package** (first domain package; rail-only imports, import-direction test). 35 tests (17 logic + 11 endpoint + 7 PG) + parity; 5 thin endpoints.

## P1C-2 design decisions (REALIZED — `abb230f`, CI-green run #46; 8-lens reviewed, 0 block, 2 LOW folded) — the first DOMAIN IA / append-only entity
REQ-PPM-003 transaction conjunct (migration `0013`); `transaction` (ENT-012, IA append-only) — a capture-only trade/cashflow event log. Sign-offs: OD-P1C2-1 (audit), OD-P1C2-2 (entitlement), OD-P1C2-6 (capture-only).
- **`transaction` = IA append-only** — `ImmutableAppendOnlyMixin` (`system_from` only; NO `valid_*`/`system_to`/`record_version`/`status`/`is_active`); `__temporal_class__ = IMMUTABLE_APPEND_ONLY`. A bare event keyed to a `portfolio` + an `instrument`; `quantity` (signed) + inert `price`/`gross_amount`/`currency_code`/`external_ref`. **Truly immutable** — in `APPEND_ONLY_TABLES` (unlike the IA-status-mutable `ingestion_batch`/`calculation_run`).
- **Two-layer append-only immutability:** the `irp_prevent_mutation` **P0001 DB trigger** (reusing the `0001` function) AND the ORM `before_update`/`before_delete` guard raising `AppendOnlyViolation`. PG test grants `irp_app` UPDATE/DELETE + a positive control so the rejection proves the **P0001 trigger**, not a 42501 privilege denial; the forged-tenant 42501 is proven separately via a forged-tenant **INSERT** (an UPDATE would hit the trigger first).
- **Reversal-as-new-record (OD-P1C2-6):** corrections are explicit NEW rows (`reverses_transaction_id` self-FK; negated `quantity`/`gross_amount`; `txn_type=REVERSAL`) emitting `TRANSACTION.REVERSE`; the original is **NEVER mutated** (a reversal is an append, not an update); a reversal may itself be reversed. No update/delete/cancel endpoint.
- **`TRANSACTION.RECORD` (EVT-160) / `TRANSACTION.REVERSE` (EVT-161) ACTIVATED** (OD-P1C2-1) — caller-side constants (new `irp_shared/transaction/events.py`) to the **FROZEN** `record_event`; per-tenant chain; DC-2 metadata; **create-only** (no UPDATE/STATUS_CHANGE — a transaction is immutable). `audit/service.py` UNTOUCHED.
- **Entitlement (OD-P1C2-2):** minted `transaction.view` + `transaction.record`; `data_steward` is the **maker/recorder** (holds both); risk tiers hold `.view`; `auditor_3l` excluded; parity-tested.
- **Symmetric proprietary RLS** (migration `0013`, byte-for-byte the `0012` loop); **NEVER hybrid** (closed 5-table hybrid set asserted unchanged); cross-tenant `portfolio_id`/`instrument_id`/`reverses_transaction_id` fail closed at the **service layer** (`resolve_*` → `*NotVisible`) pre-commit. One MANUAL-source ORIGIN edge per record + reversal. **Fail-closed audit rollback (CTRL-032)** proven by a negative test.
- **Capture-only (AD-017):** **NO transaction-to-position derivation**, NO cashflow engine, NO valuation, NO exposure aggregation, NO corporate-action application; numeric fields inert. New `irp_shared/transaction/` package (first to depend one-way on **two** upstream packages — portfolio + reference + rails; import-direction test). 32 tests (13 logic + 8 PG + 11 endpoint) + parity; 4 thin endpoints (record/list/get/reverse). REQ-PPM-003 In-Progress (valuation conjunct → P1C-4).

## P1C-3 design decisions (REALIZED — `4ee124e`, CI-green run #49; 8-lens reviewed, 0 block, 1 LOW folded) — the first FR DOMAIN entity
REQ-PPM-002 (migration `0014`); `position` (ENT-011, FR bitemporal) — the captured holdings master. Sign-offs: OD-P1C3-1 (distinct CORRECTION) / OD-P1C3-2 (single `position.edit`) / OD-P1C3-3 (opaque `cost_basis`) / OD-P1C3-4 (`valid_from` as the as-of date) / OD-P1C3-5 (read-only as-of endpoint).
- **`position` = FR bitemporal** — `FullReproducibleMixin` (`valid_from/valid_to` + `system_from/system_to`); `__temporal_class__ = FULL_REPRODUCIBLE`. Reuses the P1B-3 `instrument_terms` protocol verbatim (the second persisted FR entity, first FR DOMAIN entity). **NOT append-only** — NOT in `APPEND_ONLY_TABLES`, **no** `irp_prevent_mutation` trigger, **no** ORM guard (the FR protocol requires close-out UPDATEs); prior-version CONTENT immutability is **service-enforced + test-proven** (the PG test asserts a close-out UPDATE returns `rowcount==1` — the inversion of the transaction P0001 guard). The FR contrast with the IA `transaction`.
- **Captured-not-derived (OD-P1C-E):** positions are captured DIRECTLY (a holding supplied to the platform), **NOT** derived from the transaction log — **no `transaction` FK, no derivation engine, no cashflow engine**. Grain = aggregated `(portfolio_id, instrument_id)` (OD-P1C-D); signed `quantity` (long>0/short<0); opaque `cost_basis` (OD-P1C3-3 — captured reference, never recomputed); `valid_from` IS the as-of date (OD-P1C3-4, no `position_date`). Current-head partial-unique `(tenant_id, portfolio_id, instrument_id) WHERE valid_to IS NULL AND system_to IS NULL`.
- **FR write protocol (verbatim reuse):** `create_position` / `supersede_position` (effective-dated valid-time, close-first) / `correct_position` (as-known system-time restatement, `restatement_reason` TR-08 + `supersedes_id`) / `reconstruct_position_as_of(valid_at, known_at)` both half-open axes. One-`now`; close-first ordering; prior content carried forward, never mutated (content-immutability-on-correction tested). The prior head is sourced via the tenant-predicated `_current_open` (never a caller-supplied id); `supersedes_id` set internally.
- **`POSITION.CREATE` (EVT-170) / `POSITION.UPDATE` (EVT-171) / `POSITION.CORRECTION` (EVT-172) ACTIVATED** (OD-P1C3-1, distinct CORRECTION) — caller-side constants (new `irp_shared/position/events.py`) to the **FROZEN** `record_event`; per-op grain create=1 / supersede=2 (UPDATE close-out + CREATE) / correct=2 (UPDATE close-out + CORRECTION); per-tenant chain; DC-2 metadata. `audit/service.py` UNTOUCHED.
- **Entitlement (OD-P1C3-2):** `position.edit` MINTED (the one NEW code — `.edit` not `.record` because FR is close-out-updated; `data_steward` + `platform_admin` only); `position.view` **wired** to `data_steward` (the pre-existing seeded placeholder grant extended; the 3 existing recipients unchanged); `auditor_3l` excluded from both; parity-tested. Maker-checker on corrections → P6+.
- **Symmetric proprietary RLS** (migration `0014`, byte-for-byte the `0013` loop); **NEVER hybrid** (closed 5-table hybrid set asserted unchanged); cross-tenant `portfolio_id`/`instrument_id`/`supersedes_id` fail closed at the **service layer** pre-commit. One MANUAL-source ORIGIN edge per NEW physical version (the prior-head close-out roots none). **Fail-closed audit rollback (CTRL-032)** proven by a negative test. **CTRL-017 FR reading:** temporal-class declared YES; append-only does NOT apply (no `APPEND_ONLY_TABLES`/trigger; service-enforced immutability).
- **Capture-only (AD-017):** **NO market value calculation, NO exposure aggregation, NO holdings VIEW** (single-position reads only — `GET /positions` + `GET /positions/as-of`; multi-position holdings views → P1C-5); NO valuation, NO derivation, NO CA-application. New `irp_shared/position/` package (one-way → {portfolio, reference, rails}; import-direction test). 45 tests (22 logic + 9 PG + 14 endpoint) + parity. REQ-PPM-002 In-Progress (capture + as-of; ABAC enforcement → P6+).

## P1C-4 design decisions (REALIZED — `c5c5806`, CI-green run #54; 8-lens reviewed, 0 block, 1 LOW folded) — the second FR DOMAIN entity
REQ-PPM-003 valuation conjunct (migration `0015`); `valuation` (ENT-013, FR bitemporal) — the captured mark history. Sign-offs: OD-P1C4-1 (distinct CORRECTION EVT-182) / OD-P1C4-2 (both perms minted; single `valuation.edit`) / OD-P1C4-3 (separate immutable `valuation_date`) / OD-P1C4-4 (`mark_value` NOT NULL; `currency_code`/`mark_source`/`price_basis` nullable captured) / OD-P1C4-5 (REQ-PPM-003 → Done) / OD-P1C4-6 (read-only as-of endpoint).
- **`valuation` = FR bitemporal** — `FullReproducibleMixin`; `__temporal_class__ = FULL_REPRODUCIBLE`. Reuses the `position`/`instrument_terms` protocol verbatim (third persisted FR entity, second FR DOMAIN entity). **NOT append-only** — NOT in `APPEND_ONLY_TABLES`, no `irp_prevent_mutation` trigger, no ORM guard; content-immutability service-enforced + test-proven (PG close-out-UPDATE `rowcount==1`).
- **Captured-marks-not-modeled (OD-P1C-F):** `mark_value` is supplied (NOT NULL, never recomputed), **NOT** computed by a valuation/pricing model; **no price lookup, no source-precedence engine, no market-value rollup, no position link** (no `position_id` FK, no `quantity`, no `quantity × mark`). `mark_source` is an inert provenance **label** (NOT a market-data FK); `currency_code`/`price_basis` nullable captured (OD-P1C4-4, `price_basis` metadata only).
- **`valuation_date` = separate immutable logical key (OD-P1C-F / OD-P1C4-3):** a `Date` peer of `instrument_id`, the business date the mark is FOR — carried forward verbatim, never mutated, **distinct from the FR `valid_from` axis** (the FR axes version the *mark* for a fixed `valuation_date`). Current-head partial-unique on the **4-part key** `(tenant_id, portfolio_id, instrument_id, valuation_date) WHERE valid_to IS NULL AND system_to IS NULL`; **exactly one mark per key** (multi-source out of scope, OD-P1C-F); many `valuation_date`s per `(portfolio, instrument)` coexist as separate open heads.
- **FR write protocol (verbatim reuse):** `create_valuation` / `supersede_valuation` (effective-dated re-mark, close-first) / `correct_valuation` (as-known restatement, `restatement_reason` TR-08 + `supersedes_id`) / `reconstruct_valuation_as_of(valid_at, known_at)` both axes keyed on the 4-part key. One-`now`; close-first; prior content + `valuation_date` never mutated. Prior head via tenant-predicated `_current_open`; `supersedes_id` set internally.
- **`VALUATION.CREATE` (EVT-180) / `VALUATION.UPDATE` (EVT-181) / `VALUATION.CORRECTION` (EVT-182) ACTIVATED** (OD-P1C4-1, distinct CORRECTION) — caller-side constants (new `irp_shared/valuation/events.py`) to the **FROZEN** `record_event`; per-op grain create=1 / supersede=2 / correct=2; per-tenant chain; DC-2 metadata. `audit/service.py` UNTOUCHED.
- **Entitlement (OD-P1C4-2):** `valuation.view` + `valuation.edit` **BOTH newly minted** (neither pre-existed — the `transaction.view`/`.record` mint-both precedent); `data_steward` maker holds both; risk tiers hold view; `valuation.edit` maker/admin-only; `auditor_3l` excluded from both; parity-tested. Maker-checker on corrections → P6+.
- **Symmetric proprietary RLS** (migration `0015`, byte-for-byte the `0014` loop); **NEVER hybrid** (closed 5-table hybrid set asserted unchanged); cross-tenant `portfolio_id`/`instrument_id`/`supersedes_id` fail closed at the **service layer**. One MANUAL-source ORIGIN edge per NEW physical version (close-out roots none); `mark_source` row-label is distinct from the MANUAL `data_source` edge. Fail-closed audit rollback (CTRL-032). **CTRL-017 FR reading** (declared YES; append-only does NOT apply).
- **Capture-only (AD-017):** **NO valuation/pricing model, NO price lookup, NO market-data ingestion, NO market-value rollup, NO exposure aggregation, NO dataset_snapshot, NO holdings VIEW** (single-valuation reads only — `GET /valuations` + `GET /valuations/as-of`; views → P1C-5). New `irp_shared/valuation/` package (one-way → {portfolio, reference, rails}; **import-direction test FORBIDS importing `position`**). 48 tests (24 logic + 9 PG + 15 endpoint) + parity. **REQ-PPM-003 → Done** (both transaction + valuation conjuncts realized; no scope-enforcement residual — OD-P1C4-5).

## P1C-5 design decisions (REALIZED — `0bef45b`, CI-green run #57; 8-lens reviewed, 0 block, 2 LOW fence folds) — the first READ-MODEL package
Read half of REQ-PPM-001/002 + display read of REQ-PPM-003 (no migration; head stays `0015_valuation`). Sign-offs: OD-P1C5-1 (service-layer read models only, no DB view/migration) / OD-P1C5-2 (display-only opt-in marks; 403 when `include_marks` lacks `valuation.view`) / OD-P1C5-3 (dedicated `api/holdings.py`) / OD-P1C5-4 (node + bounded subtree + limit/offset pagination) / OD-P1C5-5 (no REQ status change) / OD-P1C5-6 (read-only `irp_shared/holdings/` package, no `models.py`/`events.py`).
- **Read-only composition, not a new entity (OD-P1C5-1/6):** new `irp_shared/holdings/` package = `service.py` + plain read DTOs (`HoldingRow`/`MarkView`/`HoldingWithMark`, dataclasses, NOT ORM, not in `irp_shared.models`, no temporal mixin); **no `models.py`, no `events.py`, no migration, no DB view** — a DB view would add migration + RLS + drift surface for zero gain. The composition reuses the already-tested tenant-predicated resolvers in-process.
- **As-of set composition:** `reconstruct_holdings_as_of` = the set-returning generalization of `reconstruct_position_as_of` (the IDENTICAL half-open bitemporal predicate on BOTH axes, filtered by `portfolio_id`, one open version per `(portfolio, instrument)` — no GROUP BY needed). `reconstruct_subtree_holdings_as_of` resolves the node object then the bounded/cycle-safe/tenant-predicated `resolve_descendants` and unions the node id. `attach_marks_as_of` reuses `reconstruct_valuation_as_of`. `valid_at` REQUIRED (matches the primitive), `known_at` optional (default now).
- **Display-only marks, gated (OD-P1C5-2 / OD-P1C-F):** opt-in (`include_marks=true`) + deterministic (explicit `valuation_date`, no "latest mark" selection); stored `mark_value`/`currency_code`/`mark_source`/`price_basis` surfaced verbatim; **never `quantity × mark_value`**, no market value, no aggregate. `valuation.view` checked **in-handler before any mark lookup** → 403 fail-closed (a position-only viewer cannot leak valuations).
- **Subtree = composition, NOT ABAC enforcement (OD-P1C-A/B):** subtree traversal SHAPES the read (which portfolios' holdings to list); it does not restrict by scope. Any `portfolio.view` holder sees all tenant portfolios (anchor-not-enforce; enforcement → P6+). Tested fence.
- **Entitlement reuse, mint nothing (OD-P1C5):** `portfolio.view` + `position.view` route guards; `valuation.view` conditional in-handler. No new permission/role. **No audit event** on reads (OD-023 no-emit posture; `audit/service.py` FROZEN); **no lineage write, no DQ write** (reads bind none).
- **RLS inherited:** reads via `get_tenant_session` + a service tenant predicate (defense-in-depth); cross-tenant/unknown portfolio → 404; corrupt/too-deep hierarchy → 409; no BYPASSRLS; closed hybrid set untouched. PG-proven under FORCE-RLS as `irp_app`.
- **Endpoint (OD-P1C5-3/4):** dedicated `api/holdings.py` `GET /portfolios/{id}/holdings` (mounted on the portfolios path); node + bounded subtree; limit/offset pagination (no total-count); only GET (writes → 405).
- **Load-bearing fences:** an AST scope-fence (forbids multiplication + every write/lineage/DQ/audit helper), a DTO-field fence (no `market_value`/`exposure`/`total`/`weight`), a zero-audit-write endpoint assertion, and a tightened import-direction allowlist. 31 tests (15 logic+fence / 4 PG FORCE-RLS / 12 endpoint). **No REQ status change** (OD-P1C5-5): REQ-PPM-001/002 stay In-Progress (read-half; ABAC residual → P6+), REQ-PPM-003 stays Done. `migration_head` stays `0015_valuation`.

## P1C-6 design decisions (REALIZED — `3e9882d`, CI-green run #60; 8-lens reviewed, a false-GREEN BLOCKER caught + fixed) — the deterministic synthetic dataset; completes the FULL P1C block
A deterministic test/demo/UI enabler (OD-P1C-L; the P1C prerequisite that replaced P1B-5). **No new entity, no migration, no REQ status change** — it composes the already-shipped governed binders. Sign-offs: OD-P1C6-1 (the keyword-only default-None deterministic-injection seam) / OD-P1C6-2..7 (Approved per the plan).
- **Deterministic governed seed, NOT direct ORM (OD-P1C-L):** new leaf-tooling `irp_shared/synthetic` package — `ids.py` (a fixed `uuid5` namespace + `synthetic_id(key)`; `SYNTHETIC_TENANT_ID`/`SYNTHETIC_ACTOR_ID`; `SEED_EPOCH` + a deterministic `SeedClock.tick()`; `business_date`) + `builder.py` (`build_synthetic_dataset` → `SyntheticDatasetSummary`). One-way imports `synthetic → {portfolio, position, valuation, transaction, reference, db}`; **nothing imports `synthetic`**. The builder composes the **governed** binders (`record_event` audit hash chain + `ensure_manual_source`/`record_lineage`), so every synthetic row is fully audited + lineaged under tenant RLS.
- **Determinism via a keyword-only default-None seam (OD-P1C6-1):** `uuid5` IDs + fixed injected timestamps are delivered through a narrow **keyword-only, default-None** `entity_id`/`now` seam added to the governed binders (portfolio/position/valuation/transaction/reference instrument+identifier) + their audit `_emit` helpers (`now → record_event(event_time=now)`). `record_event`'s hash payload **excludes** the AuditEvent surrogate PK, so injected `event_time` + deterministic order ⇒ a deterministic chain. **Production call sites pass nothing ⇒ byte-for-byte unchanged** (proven by the existing prod-path binder tests + an explicit prod-call-site-unchanged test). For the EV/IA binders the temporal field is set ONLY when `now is not None` (prod timing byte-identical); the FR binders use `now = now or utcnow()`. **`audit/service.py` stays FROZEN.** Scope: the domain-row surface (uuid5 ids + business data + injected temporal axes + domain-event `event_time`) is deterministic; the rails' internal surrogate PKs (data_source register, lineage/audit ids) stay wall-clock (documented).
- **Never-auto-run + production refusal guard:** `build_synthetic_dataset` raises `SyntheticSeedRefused` unless `allow_synthetic_seed=True` **and** `os.environ["IRP_ALLOW_SYNTHETIC_SEED"]=="1"` **and** the tenant is the reserved SYNTHETIC tenant — not wired to migrations or app startup (the `reference/bootstrap.seed_system_reference` never-auto-run precedent). It writes **only** the reserved SYNTHETIC tenant's rows under FORCE RLS, **never BYPASSRLS**; a production / non-SYNTHETIC-tenant invocation fails closed. PG FORCE-RLS tests (as the constrained `irp_app` role): only the synthetic tenant's rows visible; a different tenant + a no-context session see ZERO rows; `verify_chain(...).ok is True`.
- **Synthetic data shape (no real data; edge cases):** SYNTH_* / neutral names only; a **structurally-invalid** synthetic ISIN `ZZ0000000001`; no real ISIN/CUSIP/SEDOL/LEI/exchange/agency names. 3 instruments + 2 identifiers + a 6-node portfolio subtree + 3 transactions (incl. a reversal) + 6 positions (incl. a short −200, a supersede, a correction) + 4 valuations (incl. a correction, multiple `valuation_date`s, and a deliberate **missing mark** as a stale-data edge case for the read-side views).
- **Capture-only (AD-017):** **NO market / risk / exposure / `dataset_snapshot` scope** — the seed only captures reference/hierarchy/transactions/positions/valuations through the existing governed binders; it computes nothing (no market value, no `quantity × mark`, no exposure aggregation, no `dataset_snapshot`). **No weakening of audit, lineage, RLS, entitlement, or temporal controls** — the seam is additive (default-None) and prod-path-neutral.
- **The review caught a real BLOCKER (the load-bearing lesson):** the determinism test was a **false-GREEN** — it depended on `IRP_ALLOW_SYNTHETIC_SEED` being exported in the dev shell, so it raised `SyntheticSeedRefused` (and FAILED) in a clean shell. Fixed with an **autouse `monkeypatch` env-gate fixture** (no process-global `os.environ` mutation), then **re-validated with the var UNSET** (`make check` passed, full suite green) — CI run #60 (which has no such env var) green is the real confirmation. 24 tests (20 SQLite + 4 PG FORCE-RLS). The seed is uuid5-deterministic ⇒ **not idempotent** ⇒ seeded exactly **once** per PG module.

## P2-0 / P2-1 / P2-2 / P2-3 / P2-4 / P2-5 / P2-6 design decisions (P2-1..P2-6 REALIZED — THE FULL P2 CAPTURED MARKET-DATA FOUNDATION IS COMPLETE)
The P2 reproducibility-foundation: planned (P2-0/P2-1) + ratified; **P2-1 `dataset_snapshot` REALIZED** (`3629baa`), **P2-2 `fx_rate` REALIZED** (`c257e5c`), **P2-3 `calculation_run` wiring + basic exposure REALIZED** (`da178fc`), **P2-4 `price_point` captured price history REALIZED** (`2b63b76`), **P2-5 `curve`/`curve_point` captured yield/spread curves REALIZED** (`49ca3bd`), and **P2-6 `benchmark`/`benchmark_constituent` captured benchmark/index data REALIZED** (`b6284a4`). Sign-offs: OD-P2-A…L + OD-P2-3-A…L + OD-P2-4-A…L + OD-P2-5-A…N + OD-P2-6-A…O. The **P2-1 decisions (OD-P2-A/B/G/I/J) + the P2-2 FX decisions (OD-P2-E + OD-P2-2-A…PROC) + the P2-3 exposure/calc-run decisions (OD-P2-C/F + OD-P2-3-A…L; AD-018) + the P2-4 price decisions (OD-P2-4-A…L) + the P2-5 curve decisions (OD-P2-5-A…N) + the P2-6 benchmark decisions (OD-P2-6-A…O) are IMPLEMENTED — THE FULL P2 FOUNDATION IS COMPLETE.** `audit/service.py` FROZEN; migration head `0015_valuation` → `0016_dataset_snapshot` (P2-1) → `0017_fx_rate` (P2-2) → `0018_exposure_aggregate` (P2-3) → `0019_price_point` (P2-4) → `0020_curves` (P2-5) → **`0021_benchmark`** (P2-6).
- **Reproducibility-first sequencing (OD-P2-D):** **P2-1 `dataset_snapshot` (DONE)** → **P2-2 FX (DONE)** → **P2-3 `calculation_run` + basic exposure (DONE)** → **P2-4 price history (DONE)** → **P2-5 curves (DONE)** → **P2-6 benchmark (DONE)**. THE FULL P2 SEQUENCE IS COMPLETE — the first governed derived number + the four captured market-data entities (FX, prices, curves, benchmarks) are all realized; risk analytics stay P3.
- **`dataset_snapshot` (ENT-049) + `dataset_snapshot_component` (ENT-050) — IA TRUE append-only (OD-P2-A/B) — REALIZED `3629baa`:** in `APPEND_ONLY_TABLES`, the `transaction` precedent (NOT the status-mutable `calculation_run`); P0001 trigger + ORM guard; migration `0016_dataset_snapshot`. The component pins the **physical version** (`target_entity_id` surrogate id + `valid_from`/`system_from` (FR; NULL for EV) + `record_version`) + **`captured_content`** + **SHA-256 app-side canonical hash** (deterministic order; Decimal **HALF_UP-quantized** to column scale; ISO-8601 UTC; GUID lowercase; **excludes the close-out markers `valid_to`/`system_to`**) + header `manifest_hash`. Vocab PORTFOLIO/POSITION/VALUATION (FX reserved P2-2). **NO status column, NO model_version component** (model binds at the run). The `build_snapshot` binder, the narrow internal `data_snapshot` lineage writer, the caller-side completeness DQ gate (Protocol untouched), `SNAPSHOT.CREATE` (EVT-190) activated, `snapshot.view`/`.create` minted, the create/read/verify endpoints, symmetric RLS, and the cross-tenant binding-integrity invariant all shipped; **NO exposure number, NO `calculation_run` wiring** (8-lens review, 6 in-scope folds).
- **`calculation_run` binding (OD-P2-C/F + OD-P2-3-A…L) — REALIZED `da178fc` (P2-3; AD-018):** the snapshot is bound **through** `calculation_run` (ENT-026, IA status-mutable) with the additive **`environment_id`** column + the additive `update_run_status(outcome=)` param; `code_version` the deterministic-rollup anchor (`model_version` N/A for a model-less rollup); FW-RUN §5/TR-15 full bind; reuse `CALC.RUN_CREATE`/`STATUS_CHANGE` (NO `EXPOSURE.AGGREGATE_CREATE`). **`exposure_aggregate` (ENT-014, IA TRUE append-only)** = the first governed derived number: **signed market value v1** (`signed qty × captured mark × effective captured FX`; HALF_UP Numeric(28,6)); grain `(portfolio, instrument, base)`; effective composite `fx_rate` + `fx_legs`; `COMPONENT_KIND_FX` minted + `build_snapshot` FX-pinning + pure `compose_effective_rate` (reads ONLY snapshot-pinned captured content — reproducible under FX correction); pre-create-refusal / post-create-FAILED failure model; lineage `snapshot --DEPENDS_ON--> run --ORIGIN--> result` (`run_id` stamped); `exposure.view`/`.aggregate.run` (auditor_3l in view); symmetric RLS; **NO risk** (MARKET_VALUE only). 8-lens review (5 approve / 3 approve_with_changes / 0 block), 2 in-scope folds.
- **FX (OD-P2-E) — REALIZED `c257e5c` (P2-2):** `fx_rate` FR per QS-07/08/09 + OD-030 — explicit `base`/`quote` direction (`rate` = 1 base = rate quote); MID-only v1; `Numeric(28,12)`; `rate_date` a separate immutable logical key; USD-base-configurable triangulation (a ratified published-rate lookup, not analytics) in a pure `convert` (direct/reciprocal/triangulation, fail-closed); new hybrid-aware `resolve_currency` (own OR SYSTEM); `marketdata.view`/`.ingest`; `MARKET.FX_*` (EVT-200); VENDOR `data_source` ORIGIN lineage; new generic `RANGE` DQ evaluator (additive); symmetric RLS (never hybrid). The settled P2-2 sub-decisions: include triangulation; `marketdata.*` naming; defer bulk mapping; exact-date `rate_date==date(valid_at)` v1; defer the per-tenant base store to P2-3; fold `MARKET.*`/`marketdata.*` into the build. **NO exposure, NO `calculation_run` wiring, NO `dataset_snapshot` change** (the rate binds to the run only at P2-3).
- **Price history (OD-P2-4-A…L) — REALIZED `2b63b76` (P2-4):** `price_point` (ENT-020) FR/bitemporal captured vendor security prices — the `fx_rate`/`valuation` protocol verbatim (capture / effective-dated supersede / as-known correction / `reconstruct_price_as_of` on both axes); the `marketdata/price.py` binder + `PricePoint` model; migration `0019_price_point` (symmetric RLS, NOT append-only). The settled P2-4 decisions: a **6-part current-head key** `(tenant, instrument, price_date, price_type, currency_code, price_source)` with **`price_source` IN the key** (multi-vendor coexistence — the deliberate departure from `fx_rate`'s inert `rate_source`) + the promoted key columns DB-level **NOT NULL**; `price_date` a **separate immutable logical key** (the `valuation_date`/`rate_date` precedent); `price` **Numeric(20,6)** (the valuation money scale); `price_type` controlled-vocab **{CLOSE, MID, NAV}**; **RAW vendor prices only** (no `adjustment_basis`, no corporate-action adjustment engine); captured `currency_code`, **NO conversion** (FX conversion is a later calc via the P2-2 `convert`); `instrument_id` NOT-NULL FK via the reused `resolve_instrument`; `MARKET.PRICE_CREATE`/`UPDATE`/`CORRECTION` (EVT-200) caller-side; `VENDOR_PRICE` `data_source` ORIGIN lineage per physical version; **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + strictly-positive `RANGE` DQ gate (staleness DEFERRED, OQ-P2-4-4); symmetric RLS; **snapshot readiness-only** (NO `COMPONENT_KIND_PRICE`). The six OQ-P2-4 sign-offs: price_source-in-key; `Numeric(20,6)`; defer venue/exchange; defer staleness DQ; `{CLOSE,MID,NAV}`; readiness-only snapshot. **NO pricing/valuation/return/factor/risk model; NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/FX change.** 8-lens review (7 approve / 1 approve_with_changes / 0 block), 1 in-scope fold (REQ-PUB-001 advancement). **REQ-PUB-001 (market prices, time-series) → In-Progress (partial)** — the 'price reconstructable as-of' acceptance leg realized; the 'stale flagged (QS-16)' leg deferred; the REQ does NOT close.
- **Curves (OD-P2-5-A…N) — REALIZED `49ca3bd` (P2-5):** the unified **`curve` (FR header) + `curve_point` (IA append-only version-pinned nodes)** — the `fx_rate`/`price_point` FR protocol + the `dataset_snapshot`/`component` header-owns-immutable-children split; the `marketdata/curve.py` binder + `Curve` + `CurvePoint` models; migration `0020_curves` (`curve` FR symmetric RLS NOT append-only; `curve_point` IA append-only — `APPEND_ONLY_TABLES` + the P0001 trigger + ORM guard). **ENT-021 `yield_curve` realized as `curve`/`curve_point`; ENT-023 `credit_spread` realized BY VALUE** (`curve_type=CREDIT_SPREAD` + `reference_key` over the SAME tables — the genericity principle; no separate table); `volatility_surface` (ENT-022) stays future. The settled P2-5 decisions: a **6-part current-head key** `(tenant, curve_type, currency_code, reference_key, curve_date, curve_source)` + promoted key cols DB-level **NOT NULL**; `curve_point` UNIQUE `(curve_id, value_type, tenor_days)`; a re-version = a NEW header + a FRESH node set (nodes never individually mutated); `curve_date` a **separate immutable logical key**; `curve_type` **{TREASURY, GOVT, SWAP, OIS, CREDIT_SPREAD}**; `value_type` **{ZERO_RATE, PAR_RATE, DISCOUNT_FACTOR, SPREAD}**; `point_value` **Numeric(20,12)** (canonical DECIMAL, not %/bps); `tenor_label` + normalized `tenor_days` (key, >0); `reference_key` opaque (NOT an FK) + the `curve_type`↔`reference_key` invariant (binder-enforced, `CurveValueError` 422); `interpolation_method` an inert label (no engine); `MARKET.CURVE_CREATE`/`UPDATE`/`CORRECTION` (EVT-200, ONE event per curve) caller-side; `VENDOR_CURVE` ORIGIN lineage per physical version; **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + tenor-validity + **value-type-conditional `RANGE`** DQ (two binder-side rules over `value_type`-filtered sub-datasets: DF strictly-positive, rates/spreads [-1,1]; Protocol untouched); symmetric RLS both tables; **snapshot readiness-only** (NO `COMPONENT_KIND_CURVE`). The ten OQ-P2-5 sign-offs (header-FR + nodes-version-pinned; `Numeric(20,12)`; `reference_key` opaque-not-FK; the value sanity bands; the curve_type vocab; key on `tenor_days`; readiness-only snapshot; defer completeness/staleness; `interpolation_method` inert label; `curve_point` ENT bookkeeping). **NO curve construction/interpolation/bootstrapping/duration/key-rate/pricing/valuation/return/factor/risk; NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point` change.** 8-lens review (7 approve / 1 approve_with_changes / 0 block), 1 material fold (REQ-PUB-002/003 RTM advancement) + 3 low. **REQ-PUB-002 (curves & vol surfaces) + REQ-PUB-003 (credit spreads, ratings, benchmarks) → In-Progress (partial)** — the curve-values-reproduce leg (PUB-002) + the `credit_spread` spread-coverage leg (PUB-003) realized; `volatility_surface` + the interpolation-test/QS-13 leg + `rating`/`benchmark` + the full Coverage test deferred; neither closes.
- **Benchmarks (OD-P2-6-A…O) — REALIZED `b6284a4` (P2-6):** **`benchmark` (ENT-009, EV definition header) + `benchmark_constituent` (FR/bitemporal membership)** — the `marketdata/benchmark.py` binder + `Benchmark` + `BenchmarkConstituent` models; migration `0021_benchmark` (**NEITHER table append-only** — EV in-place + FR close-out; no P0001 trigger). ENT-009 'Benchmark definitions and constituents' realized as the EV definition + the FR membership detail (bookkept under ENT-009, no new canonical id); **`benchmark_level`/`benchmark_return` DEFERRED** (a net-new canonical ENT id NOT minted — OD-P2-6-K). The settled P2-6 decisions: identity `(tenant, benchmark_code, benchmark_source)`; constituent 4-part current-head key `(tenant, benchmark_id, instrument_id, effective_date)` + promoted key cols NOT NULL; `effective_date` a **separate immutable logical key**; `weight` **Numeric(20,12)** (sanity `RANGE [0,1]`); `instrument_id` NOT-NULL FK via `resolve_instrument`; `benchmark_currency` captured + resolved (no conversion); membership captured/superseded/corrected **as a set** per `(benchmark, effective_date)` with `reconstruct_membership_as_of` on both axes; **the RATIFIED AUDIT SPLIT (OQ-P2-6-11 Option A): `REFERENCE.CREATE`/`REFERENCE.UPDATE` for the EV definition (the reference/definition family — the `corporate_action`/`instrument` precedent; honoring `p2_implementation_plan.md`:118) + `MARKET.BENCHMARK_CONSTITUENT_CREATE`/`_UPDATE`/`_CORRECTION` (EVT-200, set-grained one-event-per-set) for the FR membership; the definition is NOT moved into `MARKET.*` absent an explicit future supersession**; `VENDOR_BENCHMARK` ORIGIN lineage (benchmark-row-targeted; the edge carries no `effective_date`); **reuse `marketdata.view`/`.ingest`** (NO new permission); required-field + weight `RANGE [0,1]` DQ (Protocol untouched); symmetric RLS both tables; **snapshot readiness-only** (NO `COMPONENT_KIND_BENCHMARK`). The eleven OQ-P2-6 sign-offs (EV-header+FR-membership; `Numeric(20,12)` weight; required instrument FK; `[0,1]` sanity + sum-deferred; identifiers captured-only; keys as planned; readiness-only snapshot; defer completeness/staleness; defer levels/returns; lineage edge targets the benchmark row; ENT bookkeeping; **+ OQ-P2-6-11 → Option A**). **NO performance attribution / active return / active risk / tracking error / factor / covariance / VaR/ES / reporting; NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point`/`curve` change.** 8-lens implementation review (4 approve / 4 approve_with_changes / 0 block) → every finding folded, incl. a real read-endpoint 500→404 fail-closed-status bug + 5 added tests. **REQ-PUB-003 (credit spreads, ratings, benchmarks) benchmark leg advanced** — the captured benchmark definitions + constituents 'inputs present & as-of' leg realized; `rating` + `benchmark_level`/`benchmark_return` + the full Coverage test deferred; REQ does NOT close. **This completes the P2 captured market-data foundation; risk analytics (factor/VaR/ES/sensitivity/scenario) stay P3.**
- **The P3 contract (OD-P3-0-A…N) — RATIFIED `07607a5` (P3-0, CI #87):** P3 entry = methodology + model-governance hardening carrying **analytic sensitivities as the first computed risk number** (NOT VaR/ES — VaR/ES is LAST, gated on P3-4 covariance + history). The **derived-number output contract:** every official risk result binds `dataset_snapshot` + `calculation_run` + a **registered `model_version`** (via `assert_registered_model_version`, CTRL-003) where a model/convention applies + `code_version` + `environment_id`; IA TRUE append-only result rows; snapshot-only compute (no live read); reproducible under input correction; pre-create-refusal / post-create-FAILED failure model. **`code_version`-only is reserved for convention-free transforms** (the P2-3 rollup — sole precedent; a convention choice ⇒ model_version). Methodology home = `05_analytics_methodologies/` (a versioned doc per method, mandatory before shipping; assumptions/limitations mirrored into the registry). `RISK.*` reserved @ **EVT-220** + `CALC.RUN_*` reuse; `risk.view`/`risk.run` (auditor_3l in `.view`); component kinds minted additively per consumer; validation-workflow enforcement deferred to **P7** (recorded, non-enforcing). Captured-data gaps (vol surface / adjusted prices / ratings / benchmark levels) are later-subphase prerequisites, not P3-1 blockers.
- **Analytic sensitivities (OD-P3-1-A…O) — REALIZED `e8e2e59` (P3-1, CI-covered at #89):** ENT-028 `sensitivity_result` (migration `0022_sensitivity`, IA TRUE append-only, symmetric RLS) — **curve-intrinsic** curve-node DV01/spread-DV01 (`-T·DF·1bp`; ACT/365F; continuous compounding; nodes-only/no-interpolation; ZERO_RATE/DISCOUNT_FACTOR-direct/SPREAD; PAR_RATE rejected; HALF_UP-12; instrument/position attribution DEFERRED — the honest narrowing of the P3-0 "curves + positions" framing). 5-tuple grain `(run, curve_id, value_type, tenor_days, sensitivity_type)`; `input_snapshot_id`/`model_version_id` carried NON-NULL, functionally run-determined. **The model-governance hardening:** `assert_registered_model_version` in the pre-create gate (zero run/rows/audit on an unregistered version) — **CTRL-003 EXECUTABLE**; governed `register_sensitivity_model` (`risk.sensitivity.analytic` v1; `methodology_ref` → `sensitivities_analytic_v1.md`; assumptions/limitations mirrored; UNVALIDATED non-enforcing until P7). `COMPONENT_KIND_CURVE` + `PURPOSE_SENSITIVITY_INPUT` (app constants); **`risk.view`/`risk.run` MINTED** (auditor_3l in `.view`); `RISK.SENSITIVITY_CREATE` reserved-NOT-emitted @ EVT-220; DEPENDS_ON recorded BEFORE the DQ gate (a FAILED run keeps input lineage). **REQ-MKT-002 → In-Progress (partial).**
- **Factor-return inputs (OD-P3-2-A…O) — REALIZED `402cb12` (P3-2, CI #89):** **captured/vendor-only v1** — a net-new **`factor` EV definition** (canonical id MINTED; identity `(tenant, factor_code, factor_source)`; `REFERENCE.*`-audited) + **ENT-025 `factor_return` FR bitemporal series** (grain `(tenant, factor_id, return_date, return_type)` current-head partial-unique; `return_value` DECIMAL fraction `Numeric(20,12)`; SIMPLE v1, LOG reserved; `MARKET.FACTOR_RETURN_*`-audited; migration `0023_factor_return`; NEITHER table append-only). **A captured INPUT — NO `calculation_run`/`model_version`/snapshot pin** (the fx/curve/benchmark pattern; no sham model_version); computed factor returns DEFERRED (need adjusted prices + a registered model_version). `marketdata.view`/`.ingest` REUSED (no `factor.*` permission); VENDOR_FACTOR ORIGIN lineage; binder-side finiteness guard + `> -1` DQ RANGE; `COMPONENT_KIND_FACTOR_RETURN` readiness-noted (MINTED at P3-4, its designed first consumer). **REQ-MKT-003 input leg satisfied** (the exposure OUTPUT is P3-3).
- **Factor exposure (OD-P3-3-A…O) — PLAN COMMITTED `f941d50` (P3-3, CI #91; implementation NOT started, gated on the OQ-P3-3-1…9 sign-offs):** **allocation v1** — indicator-loading (loading = 1) CURRENCY-family factor exposures over the **pinned atoms of a COMPLETED `exposure_aggregate` run** (matched on the atom's `mark_currency`) × the pinned `factor` EV definitions; contributions sum to total **exactly** (ε=0, REQ-MKT-003). A governed DERIVED number mirroring the P3-1 exemplar: registered `model_version` mandatory (`risk.factor_exposure.allocation` v1 + `factor_exposure_allocation_v1.md`); `factor_exposure_result` under the **existing ENT-028 family** (no net-new canonical id); IA append-only; `risk.view`/`risk.run` REUSED (no new permission); `RISK.FACTOR_EXPOSURE_CREATE` to be reserved-not-emitted; `COMPONENT_KIND_EXPOSURE` (the first IA pin flavor) + `COMPONENT_KIND_FACTOR` + `PURPOSE_FACTOR_EXPOSURE_INPUT` to mint at build; migration `0024_factor_exposure` at the implementation slice. **Vendor-beta/regression exposures + `factor_return` consumption DEFERRED** (named prerequisites; returns' first consumer is P3-4).
- **Tenancy (OD-P2-G):** market data + the snapshots are **symmetric tenant-scoped** (never hybrid/SYSTEM_TENANT; a shared-global set would require AD-013-R2); cross-tenant component binding **fails closed**.
- **Storage (OD-P2-H):** **AD-004-R1** Postgres-first behind the interface (above); OD-014 resolved.
- **Audit/entitlement (R-07):** `SNAPSHOT.CREATE` reserved at **EVT-190** (activation in P2-1); `snapshot.view`/`.create` reserved (`data_steward` maker; `auditor_3l` excluded); reuse `DATA.VALIDATE` for the DQ gate; reuse the shipped `CALC.RUN_CREATE`/`CALC.RUN_STATUS_CHANGE` for the run lifecycle (the taxonomy `RUN_START/COMPLETE/FAIL` labels are a P2-3/R-07 reconciliation item).
- **DQ + lineage (OD-P2-I/J):** a **caller-side completeness gate** (reuse `run_quality_check`; `DQEvaluator` Protocol UNTOUCHED; gap → fail closed → no snapshot) + a **narrow internal snapshot/component lineage writer** (re-resolve the snapshot via a local Core Table; no import cycle; no framework rewrite).
- **REQ-PPM-004 (exposure aggregation) → advanced (OD-P2-H/Part 3):** the AD-014 `dataset_snapshot` prereq shipped P2-1; the `calculation_run`-bound aggregation is REALIZED at **P2-3** (`da178fc`); **CTRL-009** (reproducible-from-bound-runs) is now EXECUTABLE. Basic exposure ONLY (NOT market/factor risk, VaR, ES, stress, performance, reporting — those stay P3+).

## Deferred (sound; do not pull forward)
OD-012 identifier precedence → beyond P1C (vendor-ingestion phase); OD-015 counterparty netting/CSA → P2+ (counterparty-credit, re-targeted out of P1C by OD-P1C-K); exposure aggregation (REQ-PPM-004, now **In-Progress**) builds at **P2-3** over a bound `dataset_snapshot` (the P2-1 reproducibility primitive is planned + ratified; AD-017/AD-014); ENT-022 volatility_surface → a P3-later prerequisite captured-data slice (ENT-025 `factor_return` + ENT-028 `sensitivity` are now REALIZED — P3-2/P3-1); covariance → **P3-4**; VaR/ES → **P3-5**; stress → **P3-6 (RTM-P5)**; benchmark-relative → **P3-7**; vendor factor loadings + computed factor returns → deferred captured-input slices; REQ-INT-002/003 vendor/SFTP/API
adapters → P9; OD-042 production AV → later; manual_override/BR-7 enforcement → P6/P7; reconciliation
(REQ-DQR-002) and override workflow (REQ-DQR-003) → P7; model validation/tiering (REQ-MDG-002/003) → P7;
WORM/anchored audit hardening → later.

## Do not relitigate unless explicitly reopened
All AD-* above (incl. **AD-004-R1**); the selective-bitemporality classes; the no-silent-failure DQ policy; the
audit-frozen / no-new-audit-code-without-R-07 rule; the IA-status-mutable precedent; the durable-evidence
ingestion contract; the P1B-0 OD-P1B-* resolutions; **the P2-0 OD-P2-A…L decisions + the P2 `dataset_snapshot` governance
ratification (ENT-049/050, AD-004-R1, REQ-PPM-004→In-Progress)** — ratified `63be23a`; **and the P2-1 `dataset_snapshot`
implementation (ENT-049/050 IA true-append-only; SNAPSHOT.CREATE EVT-190 activated; snapshot.view/.create minted; migration
`0016_dataset_snapshot`; symmetric RLS; the cross-tenant binding-integrity invariant; NO exposure / NO `calculation_run` wiring)** — committed/CI-green `3629baa`; **and the P2-2 `fx_rate` implementation (ENT-024 FR; base/quote direction; MID-only; `Numeric(28,12)`; `rate_date` separate logical key; pure published-rate `convert` incl. base-triangulation; hybrid-aware `resolve_currency`; `marketdata.view`/`.ingest`; `MARKET.FX_*` EVT-200; VENDOR ORIGIN lineage; additive `RANGE` DQ evaluator; symmetric RLS, never hybrid; NO exposure / NO `calculation_run` wiring / NO `dataset_snapshot` change)** — committed/CI-green `c257e5c`; **and the P2-3 exposure + `calculation_run` governance ratification (AD-018; ENT-014 ratified-in-planning; the `CALC.RUN_*` doc-vs-code reconciliation; EVT-210 `EXPOSURE.*` reserved; `exposure.*` perms incl. auditor_3l-in-view; CTRL-009 executable; HALF_UP exception)** — ratified `851f976`; **and the P2-3 `calculation_run` wiring + basic exposure implementation (ENT-014 IA true-append-only; run-bound + snapshot-gated; signed market value v1; grain (portfolio, instrument, base); effective composite `fx_rate` + `fx_legs`; `COMPONENT_KIND_FX` + `build_snapshot` FX-pinning + pure `compose_effective_rate`; the additive `calculation_run.environment_id` + `outcome` param; `CALC.RUN_*` reuse, NO `EXPOSURE.AGGREGATE_CREATE`; pre-create-refusal/post-create-FAILED; lineage snapshot→run→result; `exposure.view`/`.aggregate.run`; migration `0018_exposure_aggregate`; symmetric RLS, never hybrid; NO risk — MARKET_VALUE only)** — committed/CI-green `da178fc`; **and the P2-4 `price_point` captured price history implementation (ENT-020 FR/bitemporal; the 6-part current-head key with `price_source`-in-key + promoted key columns DB-level NOT NULL; `price_date` separate immutable logical key; `price` Numeric(20,6); `price_type` {CLOSE,MID,NAV}; RAW-only — no adjustment engine; captured `currency_code`, NO conversion; `instrument_id` NOT-NULL FK via reused `resolve_instrument`; `MARKET.PRICE_*` EVT-200; `VENDOR_PRICE` ORIGIN lineage; reuse `marketdata.view`/`.ingest`, NO new permission; required-field + strictly-positive `RANGE` DQ; symmetric RLS, never hybrid; snapshot readiness-only — NO `COMPONENT_KIND_PRICE`; NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/FX change; migration `0019_price_point`; REQ-PUB-001 → In-Progress partial)** — committed/CI-green `2b63b76`; **and the P2-5 `curve`/`curve_point` captured yield/spread curves implementation (the unified `curve` FR header + `curve_point` IA append-only version-pinned nodes; ENT-021 + ENT-023-by-value; the 6-part current-head key + promoted key cols NOT NULL; `curve_point` UNIQUE `(curve_id, value_type, tenor_days)`; `curve_date` separate immutable logical key; `curve_type` {TREASURY,GOVT,SWAP,OIS,CREDIT_SPREAD}; `value_type` {ZERO_RATE,PAR_RATE,DISCOUNT_FACTOR,SPREAD}; `point_value` Numeric(20,12); `tenor_label` + normalized `tenor_days`; `reference_key` opaque-not-FK + the `curve_type`↔`reference_key` invariant; `interpolation_method` inert label; `MARKET.CURVE_*` EVT-200 one-event-per-curve; `VENDOR_CURVE` ORIGIN lineage; reuse `marketdata.view`/`.ingest`, NO new permission; value-type-conditional `RANGE` DQ; symmetric RLS both tables, never hybrid; `curve_point` IA append-only + P0001 trigger; snapshot readiness-only — NO `COMPONENT_KIND_CURVE`; NO `calculation_run`/`exposure_aggregate`/`dataset_snapshot`/`fx_rate`/`price_point` change; NO curve construction/interpolation/duration/pricing/risk; migration `0020_curves`; REQ-PUB-002 + REQ-PUB-003 → In-Progress partial)** — committed/CI-green `49ca3bd`. **And the P2-6 `benchmark`/`benchmark_constituent` captured benchmark/index data implementation (ENT-009; `benchmark` EV definition + `benchmark_constituent` FR/bitemporal membership; identity (tenant, benchmark_code, benchmark_source); constituent 4-part current-head key (tenant, benchmark_id, instrument_id, effective_date) + promoted key cols NOT NULL; effective_date a separate immutable logical key; weight Numeric(20,12) RANGE [0,1]; instrument_id NOT-NULL FK via resolve_instrument; membership captured/superseded/corrected as a set per (benchmark, effective_date) + reconstruct_membership_as_of on both axes; the OQ-P2-6-11 Option A audit split — REFERENCE.* for the EV definition + MARKET.BENCHMARK_CONSTITUENT_* (EVT-200, set-grained) for the FR membership, definition NOT moved into MARKET.*; VENDOR_BENCHMARK ORIGIN lineage benchmark-targeted (no effective_date on the edge); reuse marketdata.view/.ingest, NO new permission; required-field + weight RANGE [0,1] DQ, Protocol untouched; symmetric RLS both tables, never hybrid; NEITHER table append-only; snapshot readiness-only — NO COMPONENT_KIND_BENCHMARK; benchmark_level/benchmark_return DEFERRED — no new canonical ENT id; NO calculation_run/exposure_aggregate/dataset_snapshot/fx_rate/price_point/curve change; NO performance/attribution/active-return/active-risk/tracking-error/factor/covariance/VaR/ES/reporting; migration `0021_benchmark`; REQ-PUB-003 benchmark leg advanced — In-Progress partial)** — committed/CI-green `b6284a4`. **THE FULL P2 CAPTURED MARKET-DATA / REPRODUCIBILITY FOUNDATION IS COMPLETE and CI-green (P2-1..P2-6).** **And the P3 slices: the P3-0 risk-analytics contract ratification (OD-P3-0-A…N; the output contract; sensitivities-first; VaR/ES last)** — committed/CI-green `07607a5`; **the P3-1 analytic-sensitivities implementation (ENT-028 `sensitivity_result`; CTRL-003 executable; `risk.view`/`risk.run` minted; the methodology framework; migration `0022_sensitivity`)** — committed `e8e2e59`, CI-covered at #89; **the P3-2 factor-return-inputs implementation (the `factor` canonical id minted + ENT-025 `factor_return` FR; captured INPUT — no run/model/snapshot; migration `0023_factor_return`)** — committed/CI-green `402cb12`; **and the P3-3 factor-exposure allocation-v1 PLAN (OD-P3-3-A…O; implementation gated on the OQ-P3-3-1…9 sign-offs)** — committed/CI-green `f941d50`; **the P3-3 factor-exposure implementation (ENT-028 family `factor_exposure_result`; allocation v1, ε=0 sum-to-total; uniform both-path pinned-content adjudication; `assert_model_version_of` model identity; conflict-safe registration; `risk.*` reuse; ci.yml restored to ALL per-table PG suites; migration `0024_factor_exposure`)** — committed/CI-green `7c50c43` (#95); **the P3-4 covariance PLAN (OD-P3-4-A…P; OQ-P3-4-1…10 RATIFIED)** — committed `8abe764`; **the P3-4-R0 shared-helper refactor (`dq/gates.py` + `_persist_snapshot`; behavior-preserving)** — committed/CI-green `a9b6567` (#98); **and the P3-4 covariance implementation (ENT-051 `covariance_matrix` MINTED + realized as `covariance_result` element rows; sample v1 N−1 over pinned SIMPLE/DAILY windows; window-as-version-identity `window_observations` assumption; `COMPONENT_KIND_FACTOR_RETURN` bitemporal per-date pins + `PURPOSE_COVARIANCE_INPUT`; canonical lowercase-GUID pair grain; `PreciseDecimal` PG-NUMERIC/SQLite-TEXT type; Decimal-50/HALF_UP-20 kernel, PSD by Gram; the dual-path verification standing rule's FIRST discharge — hand-derived rational references + `numpy.cov(ddof=1)` cross-check + eigenvalue floor, numpy TEST-ONLY + runtime-fenced; `RISK.COVARIANCE_CREATE` reserved-not-emitted; `risk.*` REUSED; fail-closed no-imputation alignment; 12 independent-review findings folded incl. the cross-slice vacuous-hybrid-probe fix; migration `0025_covariance`; REQ-MKT-001 stays Draft — substrate only)** — committed/CI-green `c2bd126` (#99); **the P3-5 parametric-VaR PLAN (OD-P3-5-A…N; OQ-P3-5-1…10 RATIFIED; the user-directed historical-sim + Monte-Carlo ROADMAP)** — committed/CI-green `c2c1b4d` (#101); **and the P3-5 parametric-VaR implementation (ENT-027 `risk_result` REALIZED as the single-summary-row `var_result`; the FIRST derived-of-derived governed number — σ_p = √(xᵀΣx), VaR = z·σ_p over TWO completed governed runs; declared-parameter version identity — confidence/horizon/z strict-parsed assumptions, vocab {0.95, 0.99}, dual-verified 12dp z constants, horizon 1 verbatim, NO runtime inverse-CDF; coverage subset rule fail-closed, NO zero-variance imputation; hard-FK provenance columns re-resolved own-tenant BOTH paths — PG FK checks bypass RLS; the declared radicand quantization floor + the magnitude gate ⇒ committed FAILED runs; `PURPOSE_VAR_INPUT` + FACTOR_EXPOSURE/COVARIANCE IA-row pins; `PreciseDecimal` σ/VaR; specific-risk = 0 first-class; `RISK.VAR_CREATE` reserved-not-emitted; `risk.*` REUSED; 13 independent-review findings folded + 2 recorded deferrals (model-status bind check; shipped-column PreciseDecimal parity); migration `0026_var`; REQ-MKT-001 → In-Progress, parametric leg; ES/historical-sim/MC = roadmap method slices)** — committed/CI-green `5ed8271` (#102). **And the P3-C1 hardening/consolidation slice — the deferral-register paydown (OD-P3-C1-A…H; OQ-P3-C1-1…8 RATIFIED after a plain-language decision briefing; plan `c2e85ac` #104): NO new governed number/entity/permission/audit code; the REGISTERED-status bind at the risk gate (`assert_model_version_of`) + register/run consistency — all four governed registrars refuse non-REGISTERED same-label twins; persisted `calculation_run.failure_reason` (additive migration `0027_run_failure_reason`; `update_run_status(failure_reason=)` FAILED-only; audit payload UNCHANGED; the four GET-run endpoints surface it; four reason formats verbatim); the run-scaffold extraction (`risk/scaffold.py::execute_governed_run` ×4 risk binders) under the R0 bar, proven by golden captures written green PRE-extraction; `PreciseDecimal` parity for the 8 float53-unsafe result columns incl. `z_score(20,12)` (PG DDL identical, NO migration); the MRO-walking `deps.map_refusal` shared by the risk/exposure/snapshot routers; both-modes ambiguity refusal ×5 binders covering EVERY build-mode arg incl. as-of; the P3-3 mixed-base adjudication check; 12 independent-review findings folded + 1 pre-existing residual recorded (the DQ-rule first-registration race); recorded follow-ups = exposure-family scaffold/reason adoption + captured-input-table PreciseDecimal parity** — committed/CI-green `0599f7f` (#105). **And the FE-1 read-only frontend runs-view — the platform's FIRST VISIBLE UI slice (OD-FE-1-A…H; OQ-FE-1-1…8 RATIFIED; plan `416cb1d` #107; chosen on the walking-skeleton argument — the first external API consumer): two read-only screens (runs list ×4 RISK families with filters/has-more pagination/row click-through; deep-linkable run detail `/runs/:family/:runId` with verbatim provenance, per-family result tables, prominent FAILED reasons, and decimal strings rendered byte-for-byte); the ONE backend addition `GET /risk/runs` (`risk.view`; explicit tenant predicate + RLS; the four RISK run_types only — EXPOSURE_AGGREGATE fenced; fail-closed filters; deterministic order; NO audit on reads); dev header-shim session under a permanent DEV banner (enforcement server-side; SSO unchanged P6+); Vite dev proxy, NO backend CORS; runtime deps react/react-dom/react-router-dom ONLY; NO migration; 16 independent-review findings folded incl. two stale-response races, runId URL-injection, and the new PG suite `test_risk_runs_pg.py` + its CI step; recorded follow-ups = the vite5/vitest2 toolchain major-bump slice (+ production-deps npm audit in CI) + exposure runs in the listing** — committed/CI-green `678a651` (#108).
