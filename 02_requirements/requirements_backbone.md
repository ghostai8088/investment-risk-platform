# Requirements Backbone

## Document Control

| Field | Value |
|---|---|
| Document ID | REQ-BACKBONE-001 |
| Version | 0.1 (Draft baseline) |
| Status | Accepted as the governed requirements baseline (extended per phase) |
| Owner | R-01 Product Manager AI |
| Approver | H-07 Product Owner |
| Created | 2026-06-18 |
| Last Reviewed | 2026-06-18 |
| Related Documents | requirements_traceability_matrix.md, personas_and_user_journeys.md, definition_of_ready_done.md, ../10_delivery_backlog/build_sequence.md, ../01_product_strategy/capability_map.md, ../09_compliance_controls/control_matrix_skeleton.md |
| Supported Build Rules | BR-1 … BR-19 (this is the governance backbone that future build work traces to) |

## 1. Purpose & Method

Convert the full-scope capability map into a controlled requirements backbone that governs all future build work. This is **not
application code** and adds no domain functionality. It assigns stable IDs to capabilities (CAP-x.y) and requirements
(REQ-DOMAIN-NNN), and — together with the [traceability matrix](requirements_traceability_matrix.md) — maps every requirement to
its capability, persona, line of defense, governance obligations, control, build phase, dependency, acceptance criteria, and
status.

**Document split.** To keep every attribute legible, descriptive attributes (business purpose, functional, data, calculation,
test, acceptance) live here; traceability attributes (persona, LoD, audit, entitlement, lineage, model-governance, control,
phase, dependency) live in the RTM. Both are keyed by REQ-ID; **Status is canonical here**.

**Granularity.** Requirements are at *feature/epic* level. Granular, testable user stories are derived per phase at the point a
requirement enters build, gated by the [Definition of Ready/Done](definition_of_ready_done.md). Version 0.1 establishes the
governing set; each phase entry expands and refines its domain's requirements.

## 2. ID Conventions

| Artifact | Pattern | Example |
|---|---|---|
| Capability | `CAP-<domain#>.<sub#>` | `CAP-5.1` (Market Risk → VaR/ES) |
| Requirement | `REQ-<DOMAIN>-<NNN>` | `REQ-MKT-001` |
| Baseline cross-cutting requirement | `BX-<CODE>` | `BX-AUD` |
| Persona | `P-<code>` (alias of `PERSONA-0x`) | `P-RA` |
| Build phase | `P<n>` | `P2` |
| Dependency token | `DEP-<CODE>` / `FW-<CODE>` | `DEP-LIN`, `FW-RUN` |
| Control | `CTRL-NNN` (from control matrix) | `CTRL-006` |

Domain codes: PPM, SMR, PUB, PRV, MKT, CRD, CPT, LIQ, SCN, LIM, BRC, MDG, DQR, LIN, AUD, RPT, ADM, INT, BAI, PRF.
*(`PRF` — performance — was in use from PM-1 onward but missing from this line until RM-1 added it, OD-RM-1-L.)*

## 3. Foundation Status & Assumptions (what already exists)

The cross-cutting foundation slice (Step 1E, commit `4f93a33`) exists and is CI-green, **but only to its documented extent** —
see `../03_architecture/foundation_slice.md`. Requirements must not assume more than the following:

| Foundation | Exists | Known placeholders (do NOT assume) |
|---|---|---|
| `FW-TMP` Persistence + temporal base | Yes (FR/IA/EV mixins, tenant column, RLS in migration) | FR/EV used only by the audit/entitlement tables so far; domain FR usage unproven |
| `FW-AUD` Audit framework | Yes (append-only, SHA-256 chain, verify, checkpoint) | Per-tenant write concurrency control pending (OD-051); signing/WORM later |
| `FW-ENT` Entitlement skeleton | Yes (deny-by-default, tenant-scoped, FastAPI gate) | **No SoD/maker-checker; identity is a dev header shim, not SSO** |
| `FW-RUN` Calculation-run framework | Yes (run record, status, audit on transitions) | Reproducibility FKs (snapshot/model/assumption) are **nullable placeholders** |

These limits define the explicit forward dependencies in §6.

## 4. Capability Taxonomy (CAP-x.y) — stable IDs for all capabilities

| Domain | Sub-capabilities |
|---|---|
| **CAP-1 Portfolio & Position Management** | 1.1 Portfolio/Fund/Strategy hierarchy · 1.2 Position master · 1.3 Transaction history · 1.4 Valuation history · 1.5 Exposure aggregation |
| **CAP-2 Security Master & Reference Data** | 2.1 Instrument master · 2.2 Issuer/Counterparty entities + hierarchy · 2.3 Identifier cross-reference · 2.4 Corporate actions · 2.5a Calendars · 2.5b Currencies/rating scales *(2.5 re-partitioned P1B-0: 2.5a→REQ-SMR-004, 2.5b→REQ-SMR-005)* |
| **CAP-3 Public Asset Data** | 3.1 Market prices · 3.2 Yield curves · 3.3 Volatility data · 3.4 Credit spreads · 3.5 Ratings/benchmarks |
| **CAP-4 Private Asset Data** | 4.1 Commitments + funded/unfunded · 4.2 Capital calls & distributions · 4.3 GP NAV reports & appraisals · 4.4 Private company financials (MNPI) · 4.5 Valuation dates/stale flags/proxy mappings |
| **CAP-5 Market Risk** | 5.1 VaR/Expected Shortfall · 5.2 Sensitivities (duration/convexity/greeks/spread duration) · 5.3 Factor exposure & contribution · 5.4 Drawdown/basis risk · 5.5 Market stress |
| **CAP-6 Credit Risk** | 6.1 PD/LGD/EAD/EL · 6.2 Migration/downgrade stress · 6.3 Spread risk · 6.4 Concentration · 6.5 Internal/shadow ratings |
| **CAP-7 Counterparty Risk** | 7.1 Current exposure · 7.2 PFE/EPE · 7.3 Netting sets/collateral/CSA · 7.4 Counterparty limits/wrong-way · 7.5 CVA (placeholder maturity) |
| **CAP-8 Liquidity Risk** | 8.1 Liquidity classification · 8.2 Redemption stress/waterfall · 8.3 Funding liquidity/facility usage · 8.4 Margin-call stress · 8.5 Capital-call forecasting/CFP indicators |
| **CAP-9 Scenario & Stress Testing** | 9.1 Historical · 9.2 Hypothetical/macro · 9.3 Reverse stress · 9.4 Combined market-credit-liquidity · 9.5 Private asset valuation shock |
| **CAP-10 Limit Monitoring** | 10.1 Limit framework definition · 10.2 Utilization computation · 10.3 Soft/hard limits · 10.4 Breach detection |
| **CAP-11 Breach Workflow** | 11.1 Breach record & assignment · 11.2 1L response · 11.3 2L review · 11.4 Escalation & closure evidence |
| **CAP-12 Model Governance** | 12.1 Model inventory · 12.2 Versioning/assumptions/limitations · 12.3 Tiering · 12.4 Validation workflow & effective challenge · 12.5 Approval/restricted-use status |
| **CAP-13 Data Quality & Reconciliation** | 13.1 DQ rules · 13.2 Reconciliation · 13.3 Exception management · 13.4 Manual overrides |
| **CAP-14 Data Lineage** | 14.1 Source-to-target mapping · 14.2 Lineage capture for results/reports · 14.3 Lineage query/visualization |
| **CAP-15 Auditability** | 15.1 Audit event capture · 15.2 Hash-chain integrity & verification · 15.3 Audit query & extract · 15.4 Override/approval audit |
| **CAP-16 Reporting** | 16.1 Risk reports (market/credit/liquidity) · 16.2 Scenario/breach reports · 16.3 Board risk report · 16.4 DQ/model-inventory reports · 16.5 Audit extract/reproducibility |
| **CAP-17 Administration & Entitlements** | 17.1 Authentication/SSO · 17.2 RBAC/ABAC entitlements · 17.3 SoD/maker-checker · 17.4 Admin console · 17.5 Export controls/classification (MNPI) |
| **CAP-18 Integration Readiness** | 18.1 CSV/Excel upload · 18.2 API adapter · 18.3 SFTP adapter · 18.4 Vendor/accounting/market-data adapters · 18.5 GP report ingestion adapter |
| **CAP-19 BAU AI Agent Support** | 19.1 DQ monitoring agent · 19.2 Breach triage agent · 19.3 Scenario commentary agent · 19.4 Model monitoring agent · 19.5 Board reporting/evidence agent |
| **CAP-20 Performance Measurement** *(MINTED PM-1, 2026-07-09; the ENT-mint analog for a homeless governed number — OQ-PM-1-9)* | 20.1 Portfolio return (time-weighted, Modified-Dietz) · 20.2 Money-weighted return / IRR *(PA-0)* · 20.3 Performance attribution *(deferred)* · 20.4 Composites / GIPS presentation *(deferred)* · 20.5 Ex-post benchmark-relative (active return / tracking error / tracking difference / information ratio) *(REALIZED P3-8, 2026-07-10; EXTENDED RM-1, 2026-07-27 — rolling return and rolling volatility on the calendar-month grid)* · **20.6 Risk-adjusted return (Sharpe ratio)** *(MINTED SR-1, 2026-07-28 — the excess-over-cash reward-to-variability measure; Sortino / Treynor / downside-deviation denominators DEFERRED)* |

| **CAP-21 Presentation & Exploration** *(MINTED at the product re-baseline, 2026-08-12 — the register declared twenty domain codes and NONE was presentation, so work with no address in the taxonomy could not be selected by a process that selects from the taxonomy. That is the drift, in one line.)* | 21.1 Presentation contract per result family · 21.2 Governed chart · 21.3 Audience-tiered rendition · 21.4 Drill-down within the governed chain · 21.5 Exploration tier |

## 5. Baseline Cross-Cutting Requirements (BX) — inherited by every applicable requirement

These encode the non-negotiable build rules once. Each requirement in §7 inherits the applicable BX (flagged in the RTM); a
requirement is not Done unless its inherited BX are satisfied.

| BX | Requirement | Build Rule | Control | Dependency |
|---|---|---|---|---|
| BX-AUD | Every create/update/approve/override emits an audit event via `FW-AUD` | BR-5, BR-12 | CTRL-005, CTRL-012 | FW-AUD |
| BX-ENT | Every access is entitlement-checked, deny-by-default, tenant-scoped | BR-11, BR-17 | CTRL-011 | FW-ENT |
| BX-LIN | Every governed output binds source→run lineage | BR-6, BR-13 | CTRL-006, CTRL-013 | DEP-LIN |
| BX-TMP | Every persisted entity declares a temporal class (FR/IA/EV) | BR-19 | CTRL-017 | FW-TMP |
| BX-REPRO | Every calculation result is reproducible from its bound run — **MACHINE-CHECKABLE for NINETEEN of the twenty-one governed families (REPRO-1 2026-08-07 registered three; REPRO-2 part 2 2026-08-10 added sixteen). The demo campaign and the deployed proof both create a schedule through the real service — carry (m) DISCHARGED. The remaining two, CONCENTRATION and LIQUIDITY, are STRUCTURAL exclusions with written reasons, not pending work; coverage enumerated by a census asserting exact set equality with the run-type vocabulary.** *(This row read "three families … the other eighteen" until the Wave-17 close on 2026-08-11 — sixteen adapters after it stopped being true. The RTM's closing paragraph carried the same sentence and is corrected in lockstep.)* | BR-6, BR-9 | CTRL-018 | FW-RUN |
| BX-TEST | No requirement is Done without tests | BR-1 | CTRL-001 | — |
| BX-DOC | Calcs have methodology docs; models inventoried; fields in dictionary | BR-2, BR-3, BR-4 | CTRL-002/003/004 | DEP-MREG, DEP-DQF |
| BX-LIM | Known limitations explicitly documented | BR-14 | CTRL-014 | — |
| BX-SOD | Maker-checker / SoD enforced for overrides, limits, model & entitlement changes, publication, deploy | BR-7, BR-15 | CTRL-015, CTRL-021, CTRL-025 | FW-ENT (SoD pending) |

## 6. Forward Dependency Registry

| Token | Meaning | Status |
|---|---|---|
| FW-TMP / FW-AUD / FW-ENT / FW-RUN | Foundation slice frameworks | Exist (with §3 placeholders) |
| DEP-LIN | Data lineage skeleton (capture + source-to-target + query) | **Exists** (capture skeleton, P1A-1: `data_source`/`lineage_edge` + `record_lineage`; query/viz is REQ-LIN-002/P7) |
| DEP-MREG | Model registry skeleton (inventory + version binding) | **Exists** (inventory + version-binding skeleton, P1A-2: `model`/`model_version`/`model_assumption`/`model_limitation` + `register_model`; tiering REQ-MDG-002/P7, validation REQ-MDG-003/P7) |
| DEP-DQF | Data quality framework (rules engine + exceptions) | **Exists** (DQ rules-engine skeleton — `data_quality_rule`/`data_quality_result` + `DQRule.evaluate` + `run_quality_check` + `assert_passed_quality_checks`, P1A-3; reconciliation REQ-DQR-002/P7, overrides REQ-DQR-003/P7) |
| DEP-SMR | Security Master / Reference Data domain | **Future** (CAP-2) |
| DEP-SSO | Real SSO / OIDC identity (replaces dev header shim) | **Future** (CAP-17, AD-007) |
| DEP-MGW | Full model governance / validation workflow | **Future** (CAP-12) |
| DEP-WFL | Workflow/state-machine engine (breach lifecycle) | **Future** (CAP-11) |
| DEP-RPT | Reporting/rendering engine | **Future** (CAP-16) |
| DEP-CIH | CI hardening: Alembic autogenerate drift check (OD-052) + audit-write concurrency control (OD-051) | **Future** (P0 hardening) |
| DEP-FELOCK | Frontend lockfile + `npm ci` reproducibility | **Future** (P0 hardening) |

## 7. Requirements by Domain

Columns: **REQ** · **Title** · **CAP** · **Business purpose** · **Functional requirement** · **Data requirement** ·
**Calc requirement** (— if none) · **Test requirement** · **Acceptance criteria (key)** · **Status**. Persona/LoD/audit/
entitlement/lineage/model-gov/control/phase/dependency are in the [RTM](requirements_traceability_matrix.md).

### CAP-1 Portfolio & Position Management (PPM)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-PPM-001 | Portfolio/fund/strategy/account hierarchy | 1.1 | Organize holdings for aggregation & entitlement scope | CRUD + versioned hierarchy nodes; entitlement scope anchor (ABAC **anchored, not enforced** in P1C-1 — enforcement P6+) | `portfolio/fund/strategy/account` (**EV — single table + `node_type` + `parent_portfolio_id` adjacency**, P1C-1) | — | Hierarchy build + scope test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** Persistence, tenancy and the scope-anchor recording are kept as REGRESSION GUARDS, not evidence: a parent id nothing traverses passes all three, persistence is an INSERT, and tenancy is the RLS every table on this platform gets. The work: (1) the entity expresses at least THREE levels and TWO node types, and REQ-PPM-008's rollup test runs over THIS entity — the dependency is named, not duplicated. (2) Hierarchy edits are VERSIONED. A governed result computed before a re-parenting, read back after it, resolves the tree AS IT WAS at the run's as-of. The test re-parents a node between run and read, and the run's stored view of the tree must not change. **The history is the ENTITY'S OWN, never a by-product of runs (re-adjudicated 2026-08-15, the STRUCT-1 close): the tree must also resolve as-of a past time from the hierarchy entity's version history alone, and the test does so by timestamp with NO run or snapshot in scope** — without this clause, a full-subtree snapshot pin satisfies every earlier sentence while the entity keeps no history at all. (3) The entitlement-anchor half stays DEFERRED to ABAC enforcement (P6+) and is stated as deferred, never counted as delivered by this row. *Why the original wording was replaced: 'persists, tenant-scoped, anchor recorded' was satisfiable by a table with a parent id nothing walks, and the requirement cell said VERSIONED nodes while no clause tested versioning — CRUD with no history passed.* | **In-Progress (clauses 1-2 DONE at STRUCT-3, PR #225 = merge `f74d207`, 2026-08-16: ENT-076 history + as-of-by-timestamp with no run/snapshot in scope, the re-parent test, three levels/two node types via the PPM-008 book; clause 3 entitlement-anchor enforcement stays DEFERRED to ABAC P6+ per the row — stated, never counted. Status advanced at the Wave-18 close, 2026-08-17)** |
| REQ-PPM-002 | Position master (as-of) | 1.2 | Single source of holdings for all risk | Positions keyed to instrument + portfolio, bitemporal | `position` (FR) → CAP-2 instruments | — | As-of reconstruction test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** Both-axes as-of reconstruction stays as a REGRESSION GUARD — it shipped at P1C-3 and passing it says nothing about who consumes it. The work: every governed family that consumes holdings resolves them THROUGH the position master's as-of reconstruction at the run's pinned snapshot, asserted by a census of holdings-consuming code paths checked by EXACT SET EQUALITY, never subset (the RPT-3 defect). A consumer that reads holdings from anywhere else FAILS the census. The residual portfolio-scope ABAC conjunct keeps its existing P6+ deferral unchanged. *Why the original wording was replaced: 'reconstructable for any past as-of date' stays green while the next analytic ingests its own holdings CSV — the purpose lives entirely in the word SINGLE and no clause tested it.* **RE-AMENDED 2026-08-20 at the Wave-19 planning gate:** the holdings-consuming set is DISCOVERED MECHANICALLY, never hand-listed — a hand list checked by exact set equality against another hand list is circular, and the STRUCT-2 census-2 precedent (discovers aggregation constructs mechanically, never a hand list) is the binding pattern. | In-Progress (P1C-3: `position` FR captured + both-axes as-of reconstruction built, migration `0014`; the residual open conjunct is portfolio-scope ABAC enforcement → P6+ — not closeable until then) |
| REQ-PPM-003 | Transaction & valuation history | 1.3/1.4 | Provenance of holdings and value | Append-only transactions; valuation history | `transaction` (IA), `valuation` (FR) | — | Append-only + history test | Transactions immutable; valuations queryable as-of | **Done (both conjuncts realized: `transaction` IA append-only, P1C-2 migration `0013`; `valuation` FR captured-marks + both-axes as-of, P1C-4 migration `0015`; no scope-enforcement residual gates it — OD-P1C4-5)** |
| REQ-PPM-004 | Exposure aggregation | 1.5 | Roll up exposures across hierarchy | Run-tracked aggregation over scope | derived from positions/valuations | Aggregation (QS-21) | Aggregation benchmark + a hierarchy-rollup test | **AMENDED 2026-08-12 (product re-baseline). The prior clause read 'Aggregates reproduce within tolerance and bind lineage' — which an aggregation that rolls up NOTHING satisfies perfectly, and for seventeen waves that is what shipped. THE ROLLUP IS NOW THE ACCEPTANCE:** given a portfolio node with at least two child nodes each holding positions, a run scoped to the PARENT returns exposure equal to the sum of runs scoped to each child, to the last decimal place; a node with no children returns its own positions; and a run scoped to a node whose subtree is empty REFUSES rather than returning zero. Reproduction and lineage binding remain required and are no longer sufficient | In-Progress (P2; P2-0 ratified 2026-06-26; AD-014 `dataset_snapshot` prereq IMPLEMENTED P2-1 `0016` + `fx_rate` P2-2 `0017`. **P2-3 ratified-in-planning 2026-06-26 (AD-018):** `exposure_aggregate` (ENT-014, IA append-only) = **basic, run-bound + snapshot-gated, signed market value v1** (signed qty × captured mark × effective FX), reproducible within tolerance + lineage-bound. **Basic exposure ONLY — does NOT imply market/factor risk, VaR, ES, stress, performance, or reporting** (deferred P3+). Builds at the P2-3 implementation slice, migration `0018`) |

| REQ-PPM-006 | Declared risk-bearing exposure measure | 1.5 | One holding can carry more than one exposure | Every exposure-consuming family consumes a DECLARED risk-bearing measure; the exposure grain carries the measure type | positions, instrument terms | Declared measure vocabulary | Grain + consumer test | **AMENDED 2026-08-13 at the G2 gate. Rewritten in plain sentences the same day.** The system must produce at least TWO exposure measures, each with its own producer. This is checked against the producers, not against a list of measure names. The demonstrating case is a bond. One bond holding must produce a NOTIONAL exposure (face value times quantity) and a MARKET-VALUE exposure (mark times quantity). Both must be readable from ONE holding id. Both must come from the valuation path, not from a test fixture. The two values must DIFFER on any bond not priced at par. Where two measures legitimately give the same number, that is NOT a failure. A future's delta-equivalent equals its notional, because delta is 1. `exposure_type` must be part of the uniqueness key. It existed on `ExposureAggregate` at the 2026-08-13 amendment while missing from `uq_exposure_aggregate_run_grain` (two measures for one holding then collided — CLOSED by STRUCT-1 migration `0071`, which put `exposure_type` in the key). Every family that consumes exposure must declare which measure it reads, and must REFUSE a row of any other measure. The refusal must be made to fire before this ships. A consumer that declares nothing fails the census. *Why the original wording was replaced: it was satisfied by registering ONE measure, having every family declare it, and firing the refusal against a made-up value. Every clause passed and no holding ever carried two exposures.* *Why the demonstrating pair changed: the first amendment named a swap's notional against its market value, or a commitment's funded against unfunded. Neither is buildable. `InstrumentTerms` carries seven bond fields and cannot express a swap. Unfunded lives in `pacing_projection_result`, outside the exposure grain.* **Later, on a trigger:** commitment funded against unfunded, once unfunded is in the exposure grain; swap notional against market value, once instrument terms can express a swap | **Done (STRUCT-1, PR #220 = merge `c0bc90b`, 2026-08-15: migration `0071` widened the grain key; NOTIONAL beside MARKET_VALUE via `EXPOSURE_PRODUCERS` + executed-run census; demo stage 25. Status advanced at the Wave-18 close, 2026-08-17)** |
| REQ-PPM-007 | Per-family aggregation contract | 1.5 | Every family says what it aggregates, and refuses the rest | Each governed result family declares the exposure measure it consumes, the grain it emits, and which aggregation operator is valid for it — additive, weighted, or NOT AGGREGATABLE | run-type registry + the REQ-PPM-006 measure vocabulary | Declaration + enforcement at the aggregation boundary | Census + refusal test (P9) | **AMENDED 2026-08-13 at the G2 gate. Rewritten in plain sentences the same day.** Every family in the run-type registry declares an aggregation contract. This is checked by EXACT SET EQUALITY against the registry. A subset check is not enough, because an empty set of contracts satisfies it (the RPT-3 defect). Every place in the code that aggregates must look up the contract first. This is checked by a census of aggregation sites, and a site with no lookup FAILS. There may be more than one site: aggregating in SQL for a read view and in Python for a governed run is fine, so long as both consult the contract. A family marked NOT AGGREGATABLE must refuse to be summed, and the refusal must fire THROUGH THE PUBLIC READ SURFACE. An HTTP request for a summed non-aggregatable value must be refused. A refusal that fires only against an internal helper does not count. Aggregating two rows of DIFFERENT measure types must be refused, not silently converted. An internal rate of return is the correct example of a value that cannot be aggregated. **DURATION IS NOT.** Duration aggregates by market-value weighting, and portfolio duration is a number every fixed-income desk quotes, so it belongs in WEIGHTED. An earlier version of this row listed duration as un-aggregatable, which would have shipped a real defect into the vocabulary. A contract that permits every operator on every family FAILS this row. *Why the original wording was replaced: it was satisfied by declaring all the contracts, marking one family NOT AGGREGATABLE, and implementing the refusal inside a helper. Set equality held and the refusal fired in a test, while production went on aggregating in a read view that consulted nothing. This is the LQ-1 finding: three controls written, believed and inert.* | **Done (STRUCT-2, PR #223 = merge `42095d2`, 2026-08-15: per-field operators + machine-readable EMITTED GRAIN in `irp_shared/aggregation/contracts.py`; AST aggregation-site census across three trees, exact-set; fired NOT-AGGREGATABLE HTTP refusals + /exposure/latest/sum. Status advanced at the Wave-18 close, 2026-08-17)** |
| REQ-PPM-008 | Node-scoped runs and rollup | 1.1/1.5 | Run risk at any node of the structure, not only at the top | A governed run is scoped to ANY node — fund, portfolio, sleeve, strategy — and a parent's result is the composition of its children's | portfolio hierarchy + positions, snapshot-pinned | Node-scoped run + the rollup identity | Rollup identity + refusal test | **AMENDED 2026-08-13 (G2 adjudication, ratified by the user).** The earlier text could be satisfied on a two-level tree, which is the only shape that exists today, so nothing would have tested a middle layer. **The test uses a portfolio tree at least three levels deep, with more than one kind of node in it** (for example a fund containing sleeves, not a single portfolio containing positions). The total at the top level must match the sum of the level below it, AND must also match the sum of the level below that. Inserting a middle grouping node, without changing any holding, must not change any total the aggregation contract declares additive; averages and ratios are excluded, because regrouping can legitimately change those. **Trees of any depth are valid: a two-level portfolio, or a single node holding positions with no children, must run normally. Minimum depth is a property of the TEST and is never a rule applied to data** (added because the first wording could be misread as a validation rule that rejects shallow portfolios). A run scoped to a node whose subtree is empty REFUSES rather than returning zero. The node id is carried on the run, so two runs at different nodes can be told apart without re-reading their inputs. A family that only ever executes at the top of the tree FAILS this row | **Done (STRUCT-3, PR #225 = merge `f74d207`, 2026-08-16: full-subtree v2 pin, explicit-node-or-refuse consume, read-time rollup identity to the last decimal, NODE_SCOPES exact-set census with executed evidence below the top; demo stage 26. Status advanced at the Wave-18 close, 2026-08-17)** |
| REQ-PPM-009 | Portfolio names carry no meaning | 1.1 | A sleeve's label must not be treated as data (SCOPE-02) | The portfolio name is display text. No computation reads it. Grouping, classification and strategy come from declared fields, never from the name. A private sleeve and a public sleeve are the same kind of node | portfolio hierarchy + the declared classification fields | None. This is a constraint on what may be read, not a calculation | Rename test + a census of every code path that reads the name | **NARROWED 2026-08-13 at the G2 gate (user-ratified). The mandate comparison half was DECLINED; see Status.** Renaming a portfolio changes no computed result. The test renames a node to something contradictory, re-runs every governed family, and compares the outputs, which must be identical. No computation reads the name field: this is checked by a census of the code paths that read it, and a read anywhere outside display code FAILS. A private sleeve and a public sleeve are the same kind of node, with the same fields and the same treatment. **The rename check alone is weak and is not evidence of work — nothing reads the name today, so it passes with no code written. It is kept as a regression guard. The census is the half with teeth** | **Done for the KEPT half (STRUCT-2, PR #223 = merge `42095d2`: repo-wide name census with literal-dynamic keys + fresh rename guards; the executed belt extended at STRUCT-3 stage 26 and STRUCT-4 stage 27. Status advanced at the Wave-18 close, 2026-08-17). NARROWED 2026-08-13 at the G2 adjudication, user-ratified: the MANDATE / MEASURED / OFF-MANDATE comparison is DECLINED and this row keeps only the name-inertness half.** Reasons, in order of weight: guideline and mandate monitoring is a COMPLIANCE function and is absent from SCOPE-03's declared risk coverage (market, credit, counterparty, liquidity, scenario/stress, limits/breach); and a mandate rule engine would duplicate the shipped limit framework (LIM-1, MG-2, MG-3) with nothing but a different owner. **This SUPERSEDES the Q4 answer ratified 2026-08-12**, which called for the three words used everywhere. Triggers to revisit: (a) guideline monitoring is required in the product, in which case it is built as an EXTENSION OF LIMITS and not as a new portfolio entity; or (b) mandate DRIFT is taken up as a risk capability in its own right — measured characteristics compared against expected ones, never permitted-versus-not |
| REQ-PPM-010 | Reporting currency and governed FX translation | 1.4 | One book, several currencies, one honest total | Every node declares a reporting currency; translation uses snapshot-pinned rates and the rate actually used is recorded on the result | captured `fx_rate` rows + the node's declared reporting currency | Governed translation, exact-decimal | Translation identity + rate-provenance test | **AMENDED 2026-08-13 (G2 adjudication, ratified by the user).** The earlier text passed on a SINGLE-CURRENCY book, where every clause is vacuously true: there are no translated legs to carry a rate id, same-currency translation is the only case, and the refusal fires in a unit test. Nothing is ever translated. **The test book holds at least THREE currencies, and at least one node reports in a currency its holdings are not held in. Before anything is asserted about translated legs, the test asserts the count of translated legs is GREATER THAN ZERO.** The translated total must match a value computed BY HAND, not a re-run of the same code. Where a currency pair has no direct rate, the result records that it was TRIANGULATED and through which currency: at the 2026-08-13 amendment the conversion path stored only `direct` and `reciprocal` legs, so triangulation was inferable from there being two legs rather than stated, and a reader should not have to know that convention (CLOSED by STRUCT-4/DP-12: new rows STATE the pivot; shipped rows derive it at read time). **A READER CAN SEE THE CONVERSION PATH ON A SCREEN** — the rate used, the legs, and the pivot currency where one was used — because at the 2026-08-13 amendment `fx_legs` reached the API and NO screen rendered it — the read-endpoints-without-screens pattern the re-baseline exists to stop (CLOSED by STRUCT-4: the RunDetail conversion-path drill-in). A missing rate for a required pair REFUSES rather than defaulting to 1.0, and the refusal is made to FIRE. **Already true of shipped code and kept only as a REGRESSION GUARD, not as evidence of work: translating to the same currency is an exact no-op** — `convert_amount` returns the amount untouched when the currencies match, so it cannot lose decimals. The guard still asserts it on a value carrying more decimals than the currency's minor unit, so that replacing the short-circuit with a rate lookup, or adding currency-rounding to the identity path, fails | **Done (STRUCT-4, PR #227 = merge `d465b6b`, 2026-08-17: DP-11 declarations + migration `0073`; DP-12 stated/derived pivot; three-currency book with hand-derived literal oracles; conversion-path drill-in on RunDetail; missing-fx honesty; DP-14 citations in `fx_translation_v1.md`. Status advanced at the Wave-18 close, 2026-08-17)** |

### CAP-2 Security Master & Reference Data (SMR)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-SMR-001 | Instrument master | 2.1 | Canonical instrument terms for pricing/risk | CRUD instruments with effective-dated terms | `instrument` (**EV identity**) + `instrument_terms` (**FR** economic/legal terms) — split ratified P1B-0/OD-P1B-A, per AD-005 §2A | — | Effective-dating + reconstruct-as-of test | Instrument terms reconstructable as-of (FR); classified per DC | **In-Progress (P1B-3, migration 0010):** instrument EV + instrument_terms FR with as-of reconstruction on BOTH axes delivered; pricing/cashflow/valuation terms math deferred to P2+ |
| REQ-SMR-002 | Issuer/counterparty entities + hierarchy | 2.2 | Aggregate credit/counterparty exposure | Entities with LEI + parent hierarchy | `issuer`, `counterparty` (EV) — separate role/profile tables over an implementation-only `legal_entity` core (OD-P1B-D; no canonical ENT id) | — | Hierarchy rollup test | Exposure rolls to ultimate parent | **In-Progress (P1B-2):** core + 1:1 profiles + LEI + parent-hierarchy STRUCTURE shipped (migration 0009, symmetric RLS); exposure-rollup CALC deferred (P2+) |
| REQ-SMR-003 | Identifier cross-reference | 2.3 | Resolve vendor/standard identifiers | Xref ISIN/CUSIP/SEDOL/FIGI/internal | `identifier_xref` (EV) | — | Resolution + precedence test | Any known identifier resolves to one instrument — **P1B partial**: deterministic single-result-or-`AmbiguousIdentifier`; cross-vendor **precedence deferred to P1C/OD-012** (OD-P1B-G) | **In-Progress (P1B-3, migration 0010, partial):** deterministic single-result-or-`AmbiguousIdentifier` delivered; cross-vendor precedence / external validation deferred to P1C/OD-012 |
| REQ-SMR-004 | Corporate actions & calendars | 2.4/2.5a | Correct economics over time | Effective-dated corporate actions; market calendars | `corporate_action`, `calendar` (EV) — corporate_action EV ratified P1B-0/OD-P1B-B | Day-count/roll (QS-10/11) | Calendar/roll test | Actions apply on effective date — **P1B delivers the calendar (P1B-1) + corporate_action (P1B-4) reference ENTITIES only; "calendars drive rolls" (QS-10/11 day-count/roll math) deferred to P1C** (OD-P1B-B) | **In-Progress (P1B-1 calendar partial + P1B-4 corporate_action, migration 0011):** corporate_action EV capture-only delivered (status lifecycle + REFERENCE.STATUS_CHANGE); roll/day-count math (QS-10/11): the QS-11 holiday/business-day half is DISCHARGED at CAL-1 (2026-08-01: the real XNYS dataset + refresh path, then the holiday-aware `preceding` roll + the v2 convention on RM-1/SR-1); the QS-10 day-count half stays trigger-based (first accrual/pricing methodology needing a declared convention) — the stale "deferred to P1C" pointer re-synced at the Wave-14 planning gate |
| REQ-SMR-005 | Standard reference vocabularies (currency, rating scale) | 2.5b | Curated ISO/standard taxonomies for pricing/risk | CRUD currency (ISO-4217) + rating-scale/grade taxonomy; hybrid global+tenant-override (AD-013-R1) | `currency` (EV), `rating_scale` (EV — scale/grade taxonomy only; rating **assignments** are FR, ENT-007, deferred) | — | Hybrid-RLS + override test | ISO-4217 currency + rating scales seeded global, tenant-overridable; effective-dated; audited + lineage-rooted | Ratified (P1B-0, new); build P1B-1 |
| REQ-SMR-006 | Classification dimension taxonomies (sector/industry, country-of-risk) | 2.5c | Governed dimensional classification for concentration, exposure and reporting | Curated hierarchical classification schemes at an authority VERSION + effective-dated assignments to instruments | `classification_scheme` (ENT-066, EV) + `classification_node` (ENT-067, EV) + `classification_assignment` (ENT-068, FR bitemporal) | — | Hybrid-RLS + override + FR supersede + ancestor-resolution test | Schemes seeded global, tenant-overridable at node grain; effective-dated; audited; lineage-rooted; an assignment resolves fail-closed against its scheme; sector is the level-1 ANCESTOR of the assigned node | **In-Progress (REF-1, 2026-07-29, migration `0056`):** ISIC Rev. 5 seeded as the canonical sector/industry scheme and ISO-3166-1 as the country scheme; assignments captured at instrument grain via a polymorphic target; `basis` NOT NULL with a binder-enforced kind-invariant. MINTED at REF-1 — REQ-SMR-005 is scoped to currency + rating scale only, so no requirement covered classification vocabularies (the RM-1/BT-1 mint precedent) |
| REQ-SMR-007 | Product-type coverage contract | 2.1 | Any product type, or an honest refusal at the door (SCOPE-02) | Every admitted product type declares its economic terms and its valuation route; an instrument whose type has no declared route is refused at ADMISSION, not at valuation | instrument master + a product-type registry | Declaration + admission gate | Census + admission-refusal test (P9) | The set of admitted product types EQUALS the set with a declared valuation route, by exact set equality; an instrument of an undeclared type is REFUSED AT ADMISSION with the type named, and the refusal is made to FIRE before this ships; and the declared set covers at least one instrument from each SCOPE-02 public family (equity, fixed income, derivative, FX, cash) and each private family (private equity, private credit, direct lending, real estate, infrastructure) — an equity-and-bond-only registry FAILS this row | **Draft (re-baseline part 2, 2026-08-13)** |


### CAP-3 Public Asset Data (PUB)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-PUB-001 | Market prices (time-series) | 3.1 | Inputs for valuation & risk | Bitemporal price points per instrument/source | `price_point` (FR) | — | As-of + staleness test | Price reconstructable as-of; stale flagged (QS-16) | **In-Progress (partial, P2-4 implementation, migration `0019`):** `price_point` (ENT-020, FR) realized — the **"price reconstructable as-of"** acceptance leg delivered on BOTH axes (`reconstruct_price_as_of`; captured RAW vendor prices; 6-part current-head key with `price_source`; `MARKET.PRICE_*` audited; VENDOR_PRICE ORIGIN lineage; symmetric RLS; required-field + strictly-positive RANGE DQ). The **"stale flagged (QS-16)"** leg is **DEFERRED** (OQ-P2-4-4) — REQ does **NOT** close |
| REQ-PUB-002 | Curves & volatility surfaces | 3.2/3.3 | Discounting & options risk | Versioned curves/surfaces with interpolation method | `yield_curve`, `volatility_surface` (FR) | Interpolation (QS-13) | Interpolation test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** (1) A curve queried at an OFF-NODE tenor returns a value COMPUTED by the declared method, and the demonstrating test asserts the interpolated-read count is NON-ZERO before asserting anything about values. (2) The off-node value agrees with a HAND-COMPUTED oracle for that method. (3) The declared method is load-bearing: two different declared methods over the same nodes give DIFFERENT off-node values, so an inert label fails. (4) At least one governed consumer discounts with it — a discount factor at an off-node date reaches a governed number; an interpolation engine no run calls is a library, not a capability. The volatility-surface leg carries the same four clauses when built (QS-13). *Why the original wording was replaced: 'values reproduce; method declared' is the shipped state — captured curves read back byte-identical, and interpolation_method is captured as an inert label with NO engine (OQ-P2-5-9) — so the row was held open by its Status prose, not by its criterion.* | **In-Progress (partial, P2-5 implementation, migration `0020`):** `yield_curve` (ENT-021) realized as the unified `curve` (FR header) + `curve_point` (IA append-only nodes) — the **"curve values reproduce"** acceptance leg delivered on BOTH axes (`reconstruct_curve_as_of`; captured curves; 6-part current-head key; `MARKET.CURVE_*` audited; VENDOR_CURVE ORIGIN lineage; symmetric RLS; required-field + value-type-conditional RANGE DQ). **`volatility_surface`** (ENT-022) AND the **"interpolation method declared" / QS-13 interpolation-test** leg are **DEFERRED** (`interpolation_method` captured as an inert label, OQ-P2-5-9; NO interpolation engine) — REQ does **NOT** close |
| REQ-PUB-003 | Credit spreads, ratings, benchmarks | 3.4/3.5 | Credit & relative risk inputs | Spread series, ratings, benchmark constituents | `credit_spread`, `rating`, `benchmark` (FR/EV) | — | Coverage test | Inputs present & as-of for the risk engine | **In-Progress (partial, P2-6 implementation, migration `0021`):** `credit_spread` (ENT-023, P2-5 by value) + **`benchmark` (ENT-009) REALIZED** (P2-6: `benchmark` EV definition + `benchmark_constituent` FR membership) — the **"inputs present & as-of"** spread-coverage AND benchmark-membership legs delivered (`reconstruct_membership_as_of`; symmetric RLS; VENDOR_BENCHMARK lineage; `REFERENCE.*`/`MARKET.BENCHMARK_CONSTITUENT_*` audited). **`rating`** + the deferred `benchmark_level`/`benchmark_return` + the full **Coverage test** remain **DEFERRED** — REQ does **NOT** close |
| REQ-PUB-004 | Curve bootstrapping and curve-set assignment | 3.2 | A discount curve nobody can rebuild is not evidence | Curves are bootstrapped from captured par instruments; each instrument is assigned a curve set (forecast and discount) so multi-curve discounting is explicit | captured par rates + per-instrument curve-set assignment | Bootstrapping, exact-decimal | Round-trip + assignment census | Re-pricing the bootstrap INPUTS off the bootstrapped curve returns the input par rates within a declared tolerance — a self-consistency check against the inputs, not agreement with a re-run of the same code path; every instrument requiring discounting has a curve-set assignment, by census against the instrument population; an instrument with no assignment REFUSES to price rather than falling back to a default curve; and a curve is reproducible from its pinned inputs | **Draft (re-baseline part 2, 2026-08-13)** |
| REQ-PUB-005 | Index fixing and reset history | 3.1 | No floating leg can be valued without its fixings | Captured fixing history per index, with reset dates, as first-class reference data | index fixing series, snapshot-pinned | Capture + lookup by reset date | Missing-fixing refusal (P9) + append-immutability | Valuing a floating leg whose reset date has NO captured fixing REFUSES, names the index and the date, and the refusal is made to FIRE before this ships — it never interpolates and never carries the last fixing forward silently; a fixing appended later cannot move a historical valuation, asserted by re-running a pinned historical run after the append; and the fixing actually used is recorded on the result | **Draft (re-baseline part 2, 2026-08-13)** |


### CAP-4 Private Asset Data (PRV)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-PRV-001 | Commitments & funded/unfunded | 4.1 | Track private exposure & dry powder | Commitment records with funded/unfunded | `commitment` (FR) | — | Funded/unfunded test | Unfunded exposure computed and aggregated | **In-Progress (CC-1, 2026-07-20)** — ENT-015 REALIZED (migration `0044`; the FR capture lifecycle + the chain-immutable currency + the stable (portfolio, instrument) identity; `commitment.edit`/`.view` mint; demo stage 8 live; the OD-CC-1-D read rule applies: commitments and their events do NOT feed TWR/backtest chains — nothing auto-bridges in v1). **The acceptance clause "Unfunded exposure computed and aggregated" and the "Funded/unfunded test" leg are explicitly OPEN → CC-2** (a persisted unfunded number is a governed derived output — snapshot/run/model-bound — and lands with the pacing projection; OD-CC-1-E). *(**ADVANCED at CC-2, 2026-07-20** (`cc_2_decision_record.md`; migration `0045`): ENT-059 `pacing_projection_result` (the SEVENTEENTH governed number) persists the per-period `unfunded_end` — the **"computed" leg + the "Funded/unfunded test" leg DISCHARGED** (the kernel/anchor suites + demo stage 9; reproducible from the pins). The **"aggregated" clause stays OPEN** — the portfolio-level unfunded ROLLUP across pairs is the NAMED v2 (discharging by redefining "aggregated" as per-pair would be an overclaim).)* |
| REQ-PRV-002 | Capital calls & distributions | 4.2 | Cashflow & liquidity inputs | Append-only call/distribution events | `capital_call`, `distribution` (IA) | — | Cashflow test | Events immutable; feed liquidity forecast | **In-Progress (CC-1, 2026-07-20)** — ENT-016 REALIZED (migration `0044`; truly-immutable IA events under P0001 triggers + ORM guards; the FULL-REVERSAL negation correction path; `is_recallable` captured as data; `commitment.record`/`.view` mint; the OD-CC-1-D read rule recorded at the consumer surfaces: package/service/endpoint docstrings, the ENT cells, these rows, and the demo module). **"Events immutable" DISCHARGED (test-pinned both layers); the "feed liquidity forecast" clause stays OPEN → CC-2** (the pacing/liquidity projection is the governed consumer). *(**DISCHARGED at CC-2, 2026-07-20** (`cc_2_decision_record.md`; migration `0045`): the "feed liquidity forecast" clause is discharged — the ENT-059 pacing projection's per-period `projected_call`/`projected_distribution` series IS the platform's first governed liquidity-forecast consumer (the call/distribution events feed the FUTURE-ONLY projection through the pinned snapshot), per-(portfolio, instrument) pair at ANNUAL periodicity in v1, stated.)* |
| REQ-PRV-003 | GP NAV / appraisals + stale flags | 4.3/4.5 | Valuation of illiquid assets | NAV/appraisal with valuation date + staleness | `gp_report`, `appraisal` (FR) | Staleness (QS-16) | Stale-valuation test | Stale NAV flagged; proxy mapping recorded | Draft |
| REQ-PRV-004 | Private company financials (MNPI) | 4.4 | Fundamental private credit/equity input | Restricted (DC-4) financials behind barriers | `private_company_financials` (FR) | — | MNPI access-denied test | DC-4 access requires need-to-know grant; denials audited | Draft |
| REQ-PRV-005 | Private-to-public factor proxy mapping | 4.6 | Represent a private holding's risk as a loading on public risk factors — the substrate the desmoothing/proxy transform projects onto (the differentiation-thesis destination) | Captured FR proxy-weight table mapping a private instrument to public `factor` definitions (a governance judgment call, not a computed regression in v1) | `proxy_mapping` (ENT-019, FR) | — | Capture/supersede/reconstruct + cross-tenant-FK refusal test | A private instrument maps to ≥1 public factor with signed weights (NOT forced to sum to 1 — a partial proxy is honest); versions reconstruct bitemporally; both FK targets fail closed cross-tenant | **In-Progress (PA-0, 2026-07-11, migration `0034`):** `proxy_mapping` (ENT-019) — captured private→public factor proxies v1 (MANUAL method; CURRENCY-family factors); REUSES `marketdata.view`/`.ingest`. **The desmoothing/proxy TRANSFORM (Geltner AR(1) v1) is the PA-1 follow-on; regression-derived weights, capital calls/distributions/IRR, and non-CURRENCY factor families DEFERRED.** *(Dated refresh, Wave-7 close 2026-07-19 — the F5 status-decay fix: the desmoothing transform SHIPPED at PA-1 and gained the AR1_ESTIMATED/OKUNEV_WHITE_ITERATIVE estimator conventions at DS-2 (migration `0042`); regression-derived weights SHIPPED at PA-3 with the EWMA/EB-shrinkage residual conventions added at RS-1; non-CURRENCY factor families SHIPPED at FL-1; **capital calls/distributions/IRR remain the open clause** — the presumptive Wave-8 headline.)* REQ does **NOT** close |


### CAP-5 Market Risk (MKT)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-MKT-001 | Value at Risk / Expected Shortfall | 5.1 | Quantify tail loss for limits & board | Pluggable VaR/ES (parametric/historical; **MC STRUCK — see the status cell**) as run | positions+market (FR) snapshot | Deterministic, seeded MC (QS-18) | Benchmark VaR within ε; reproduction test | VaR matches reference within ε; re-run identical; method has methodology doc + inventory entry | In-Progress (parametric VaR P3-5; historical-sim VAR-HS-1; total PA-4/BT-2; **parametric ES ES-1 2026-07-15; ES-over-HS ES-HS-1 2026-07-17** — **MONTE CARLO SUPERSEDED 2026-08-12** (product re-baseline, owner-ratified). Measured, not argued: a realistic netting-set exposure simulation in exact Decimal is ~112 hours per core against ~15 seconds vectorised — four orders of magnitude, and no optimisation closes it. The platform's zero-floating-point arithmetic is what underwrites its ten-year reproducibility claim (18,000 Decimal operations across two implementations at three precisions, zero mismatches), and MC is the one method that cannot be paid for in Decimal. So MC is OUT OF THE GOVERNED SPINE — this is a PROMISE DROPPED, recorded as a loss rather than left to read as an oversight. What goes with it: simulated exposure profiles (EPE, EEPE, peak PFE) and path-dependent exotics (barriers, Asians, autocallables, Bermudan swaptions), and therefore counterparty risk, declined separately at the same gate. The consequence for THIS row is that it can now close on parametric + historical rather than waiting on a method that will not be built: the previous clause read 'Monte-Carlo VaR + ES-over-MC still open, so the REQ does not close', and that condition is withdrawn. QS-18 (recorded RNG seed for stochastic runs) becomes inapplicable to VaR/ES and stands only for any future non-governed use) |
| REQ-MKT-002 | Sensitivities | 5.2 | Risk decomposition & hedging | Duration/convexity/greeks/spread duration | positions+market (FR) | Analytic/bump (QS) | Sensitivity benchmark | **AMENDED 2026-08-15 at the G2 worklist adjudication.** (1) Sensitivities exist at POSITION grain. (2) The book-level figure equals the composition of the position-grain figures under the family's declared aggregation operator (REQ-PPM-007's contract; additive for DV01). (3) At least one HAND-COMPUTED external oracle per shipped convention — a known bond's DV01 verified by hand, with the convention that produces it named. (4) The hedging outcome: bump the pricing inputs of ONE position, re-price, and the book moves by that position's stated sensitivity within a DECLARED tolerance — a number written in the methodology doc, never a run-to-run difference. (5) Run-twice reproduction survives only as the standard governed-number regression guard. *Why the original wording was replaced: 'greeks reproduce within epsilon' named no oracle, so any deterministic code passes at epsilon zero — including the book-level curve-node DV01 already shipped at P3-1 — and nothing required position grain, so nothing supported a hedge. This is REQ-PPM-004's defect shape verbatim, on the row that feeds risk decomposition (re-baseline section 5, item 3).* | In-Progress (P3-1: analytic curve-node DV01/spread-DV01, migration `0022`; instrument-attributed greeks deferred — backbone status aligned to the RTM at the 2026-07-06 status-decay audit rule) |
| REQ-MKT-003 | Factor exposure & contribution | 5.3 | Attribute risk to factors | Factor model exposures & contributions | factor returns (FR) | Factor calc | Attribution test + a decomposition test naming the position | **AMENDED 2026-08-12 (product re-baseline). The prior clause read 'Contributions sum to total within ε' — which an allocation identity satisfies TRIVIALLY, and this row's own status column admitted contribution-to-risk was deferred while the acceptance was met. SUMMING IS NOT ATTRIBUTING:** the run emits one contribution row PER POSITION whose values are derived from the covariance run's own marginal quantities (not from a weight-times-total allocation); removing the largest contributor from the book and re-running moves total risk by that contributor's stated amount to the last decimal place; and the sum equals the total EXACTLY, not within ε, because the kernels are exact-decimal | In-Progress (P3-2 inputs `0023` + P3-3 allocation-v1 exposures `0024` — sum-to-total exact; beta/regression + contribution-to-risk deferred) |
| REQ-MKT-004 | Market stress | 5.5 | Loss under defined shocks | Apply shock set to portfolio | scenario defs (CAP-9) | Revaluation | Stress benchmark | Stress P&L reproduces; binds scenario version | **In-Progress (P3-6 2026-07-12, PR #13 = `0f66220`; ENT-029/030, migration `0035`)** — deterministic LINEAR factor-shock P&L shipped and reproducing; the shock-type and factor-family breadth is REQ-MKT-004a. *Status repaired at the Wave-19 planning gate: this cell read bare Draft while the RTM had recorded the shipped slice since 2026-07-12. Repaired to MIRROR the RTM, not to claim completion. Status-only edit; acceptance text untouched; no adjudication hash moved.* |
| REQ-MKT-005 | VaR backtesting (outcomes analysis) | 5.1/12.5 | Hold the shipped VaR numbers accountable against realized outcomes — the SR 11-7 "outcomes analysis" leg and the Basel backtesting discipline (the Wave-1 close review's named nearest supervisory gap) | Governed backtest as a run over ONE portfolio-return run + N pinned VaR forecasts of ONE method: per-pair exception indicators + Kupiec POF (fixed-critical decision at a DECLARED alpha) + the Basel traffic-light zone on its defined (0.99, 250) domain only | PM-1 `portfolio_return_result` rows (realized flow-adjusted P&L) + `var_result` rows, all snapshot-pinned | STRICT exception rule; Kupiec χ²(1) TWO-SIDED; Decimal-50 `Decimal.ln`; `Numeric(28,6)`; calendar-day horizon alignment, ALL-OR-NOTHING | Kernel goldens + independent float `math.log` cross-check + TR-09 reproduction under re-runs of both sides | Statistics match the independent reference within ε; re-run identical; a later VaR or return re-run cannot move a historical backtest; method has methodology doc + inventory entry | **In-Progress (BT-1, 2026-07-10, migration `0033`):** `var_backtest_result` (ENT-055) — v1 (`risk.var_backtest`, declared alpha {0.05, 0.01}); REUSES `risk.run`/`risk.view`. **ACTUAL P&L only (hypothetical/clean deferred); Kupiec only (Christoffersen = a BT-3 candidate); no Basel multiplier arithmetic; no p-values; the PM-1 captured-holdings bias propagates ANTI-CONSERVATIVELY (recorded).** **ADVANCED at BT-2 (2026-07-15): `VAR_PARAMETRIC_TOTAL` ADMITTED to the lane (the PA-4 exclusion discharged) under the recorded honest-pairing doctrine — daily pairing on an appraisal-marked book is biased two ways by construction (suppressed off-mark, clustered on-mark), so its unconditional verdict is NOT valid evidence of adequacy in either direction; the dated per-pair rows are the evidence surface. Appraisal-frequency pairing + a Christoffersen leg = BT-3 candidates *(dated PARTIAL discharge, BT-3 2026-07-19: the Christoffersen leg SHIPPED as v2-christoffersen; appraisal-frequency pairing stays the open clause)*.** REQ does **NOT** close |
| REQ-MKT-006 | Maximum drawdown over the governed return series | 5.4 | Answer the question every supervisor and allocator asks first — *how far did this book fall from its own high-water mark, and over what window* — with a number that is run-bound, snapshot-pinned and reproducible rather than recomputed ad hoc in a spreadsheet | Governed maximum drawdown as part of a rolling-risk run over ONE portfolio-return run: `max_k[(peak_k − V_k)/peak_k]` on the compounded TWR wealth index, rebased at each window's opening boundary, running peak with no look-ahead | PM-1 `portfolio_return_result` `DIETZ_PERIOD` rows, snapshot-pinned and relinked to calendar months | Window-LOCAL rebasing; `V_0 = 1` IS an observation with drawdown 0; the TWR index (NOT a NAV path — a redemption is not a performance event); NEVER annualized; `1 + m > 0` enforced pre-create; post-condition `0 ≤ MDD ≤ 1` asserted, not inherited (Chekhlov Prop. 3.1 covers the absolute-on-uncompounded form, which this is not); `Numeric(20,12)` | The `V_0` case (−10% then eleven +1% ⇒ 0.10, not 0.00); `MDD_36 ≥ MDD_12` at a common end date; a designed multi-month drawdown on the demo book; every guard mutation-tested | The base-point and peak-scope choices are DECLARED and pinned; a total-loss book REFUSES rather than emitting a ratio > 1 or an inverted sign; the statistic carries its window AND its sampling frequency; method has methodology doc + inventory entry | **In-Progress (RM-1, 2026-07-27, migration `0054`):** `rolling_risk_result` (ENT-064) — `MAX_DRAWDOWN` within `perf.rolling_risk` v1; REUSES `perf.run`/`perf.view`. **Monthly grid only (daily deferred with `k=252` uncited); `AvDD`/α-CDD, drawdown duration and time-to-recovery DEFERRED; discretisation bias recorded (monthly MDD ≤ daily MDD ≤ continuous MDD, always) — never compare across frequencies.** REQ does **NOT** close |

| REQ-MKT-002a | Position-grain sensitivities | 5.2 | Know which position carries the risk | Sensitivities computed and PERSISTED at position grain, aggregating to portfolio and to any hierarchy node | positions, instrument terms, curves | Analytic or bump-and-revalue | Grain + aggregation test | A run emits one row PER POSITION, not one per portfolio — asserted by counting rows against the position count, not by checking a total exists; position rows sum EXACTLY to the portfolio total; the same run scoped to a hierarchy node sums to that node's subtree. A portfolio-only result FAILS this row | **Draft (re-baseline 2026-08-12)** |
| REQ-MKT-008 | VaR/ES horizon term structure | 5.1 | A regulated fund files a 10-day number | A registered VaR/ES model version declares horizon_days from an enumerated vocabulary; the engine computes at the declared horizon | pinned covariance/return series | Declared horizon | Horizon test + refusal twin | A model version declaring 10 produces a DIFFERENT number from one declaring 1 over identical inputs (asserted as inequality, so a horizon silently ignored fails); an out-of-vocabulary horizon is REFUSED and the refusal made to fire; `VAR_HORIZON_DAYS = 1` ceases to be a hard fence | **Draft (re-baseline 2026-08-12)** |
| REQ-MKT-004a | Scenario engine across factor families | 5.5 | Stress that means what a board means | The engine admits every REGISTERED factor family, not CURRENCY alone, and shock types beyond percentage return | registered factors, pinned exposures | Shock application | Coverage + refusal test | A shock is expressible against EACH registered factor family, asserted by EXACT SET EQUALITY between the shockable set and the registered set — today's `SUPPORTED_FACTOR_FAMILIES = (CURRENCY,)`, one of ten, must FAIL this clause; a shock naming an unregistered family is refused; a rates shock in basis points produces a different P&L from the same magnitude as a return | **Draft (re-baseline 2026-08-12)** |
| REQ-MKT-007 | Closed-form pricing engine | 5.2 | Price what the book holds, reproducibly, for ten years | A pure-Decimal closed-form pricing kernel — linear instruments first, options second. No Monte Carlo: withdrawn from the governed spine 2026-08-12 (see REQ-MKT-001) | instrument terms + curves + fixings + market data, all snapshot-pinned | Closed-form exact-decimal under a registered model version | External-oracle golden + cross-implementation + mutation | Prices agree with a HAND-COMPUTED or vendor reference to a declared tolerance — an EXTERNAL oracle, never agreement with a re-run of itself; the error-function algorithm is PINNED in the registered model version before any price is emitted (measured penalty for the wrong choice: 4-5x, in exactly the shocked regime, and unimprovable once historical prices depend on it); a 40-flow 20-year bond and a six-greek option each price within the measured budget; and an instrument type with no closed form REFUSES rather than approximating | **Draft (re-baseline part 2, 2026-08-13; Q1 ratified 2026-08-12 — MEASURED at 4.5s and 6.5s for 5,000 positions x 20 scenarios by two independent implementations)** |
| REQ-MKT-009 | Structural factor model | 5.3 | Explain the risk, not merely measure it | Cross-sectional factor return estimation in Decimal, with its own governed entity for estimated factor returns | instrument descriptors + returns, snapshot-pinned | Gaussian solve; cap-weighted identification constraint; precision-independent guard | Degenerate-design refusal + cross-implementation | A rank-deficient design REFUSES rather than emitting a number, asserted at MULTIPLE decimal precisions — the first guard was precision-coupled and returned a factor return of 3x10^23 at 28 digits, so a single-precision test would have passed it; the identification constraint is the cap-weighted one, so no reference industry is dropped; estimated factor returns live in their own governed entity, because the existing table cannot hold them; and a 117-factor by 10,000-instrument period solves within the measured budget | **Draft (re-baseline part 2, 2026-08-13; Q2 ratified — MEASURED at 0.838s and 0.607s per period against a four-hour budget)** |

### CAP-6 Credit Risk (CRD)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-CRD-001 | PD/LGD/EAD/EL | 6.1 | Expected credit loss | Compute EL from PD×LGD×EAD | exposures, ratings | EL calc | EL benchmark | EL reproduces; assumptions/limitations documented | Draft |
| REQ-CRD-002 | Migration & downgrade stress | 6.2 | Credit deterioration impact | Rating migration matrices & downgrade shocks | ratings, migration matrix | Migration calc | Migration test | Downgrade stress reproduces; binds scenario | Draft |
| REQ-CRD-003 | Concentration & spread risk | 6.3/6.4 | Identify credit concentrations | Concentration metrics; spread sensitivity | exposures, spreads | Concentration/spread calc | Concentration test | Limits-ready metrics produced per issuer/sector | In-Progress — **the concentration half REALIZED at CON-1 (2026-07-30): metrics PRODUCED in the LIM-2 selector shape (the ten-name census, the summary grain, `share_invested_long` + `denominator_basis`), governed end-to-end (ENT-069, migration `0057`, registered model + INITIAL validation, demo stage 19 at counts 26/41/136) — BINDABLE AT LIM-2** (the OQ-CON-1-15 reversal: `_METRIC_MAP` registration defers to LIM-2 with the basis column, so no limit can bind an unbased share; the REQ's "limits-ready" clause COMPLETES at LIM-2, deliberately not "Done" here). The spread half split out as REQ-CRD-005 (OQ-W14P-4). **COMPLETED at LIM-2 (2026-08-01, PR #155): the concentration half is Done — metrics REGISTERED and BOUND, basis-disciplined limits breaching through the shipped evaluator with the issuer fence extended to every read surface.** |
| REQ-CRD-005 | Credit spread sensitivity | 6.3 | Measure sensitivity to credit-spread moves | Spread sensitivity (CS01-class) over captured spread curves | exposures, spread curves | Spread sensitivity calc | Spread-sensitivity test | **AMENDED 2026-08-15 at the G2 worklist adjudication** (the row stays trigger-parked; amended now because the analysis was fresh and the ledger lapses automatically if the text moves again). (1) Position-grain CS01 composing to the book figure under the declared aggregation operator. (2) A HAND-COMPUTED oracle on a known bond over a known spread curve. (3) The demonstrating spread curve is a CAPTURED EXTERNAL curve, asserted by ORIGIN provenance, never a fixture — the trigger's own point, written where it can fail. *Why the original wording was replaced: 'reproduce from a pinned snapshot' was already passable by the curve-node spread-DV01 shipped at P3-1 against a fixture curve — everything the row was minted to prevent when it was split from CRD-003.* | Draft — **MINTED at REF-1 (2026-07-29) by splitting REQ-CRD-003** (OQ-W14P-4, ratified at the Wave-14 planning gate). CRD-003 bundled concentration (CAP-6.4) with spread sensitivity (CAP-6.3); the spread half needs genuine curve feeds that no wave has landed, so bundling them would have held concentration permanently In-Progress. Trigger: real curve feeds land |
| REQ-CRD-004 | Internal/shadow ratings | 6.5 | Rate unrated/private exposures | Internal rating model + override (audited) | financials, ratings | Rating model | Rating + override test | Internal rating produced; overrides carry BR-7 fields | Draft |
| REQ-CRD-006 | Bond analytics kernel | 6.3 | Yield and spread as governed numbers, not spreadsheet cells | Decimal bond analytics — yield to maturity and spread to curve — solved by bounded bisection under a registered tolerance and iteration ceiling | instrument terms + price + curve, snapshot-pinned | Bounded bisection, exact-decimal | External golden + non-convergence refusal (P9) | Yields match a HAND-COMPUTED reference to a declared tolerance; a solve that does not converge within the registered iteration ceiling REFUSES and names the instrument, never returning its last iterate — this is the platform's first iterative numerical class and a silent last-iterate is its characteristic failure, so the refusal is made to FIRE before this ships; the tolerance and ceiling are REGISTERED parameters, so a historical result can be re-derived; and re-running reproduces identically | **Draft (re-baseline part 2, 2026-08-13)** |
| REQ-CRD-007 | Credit concentration at issuer and ultimate parent | 6.4 | Know the real issuer exposure, not the ticker's | Concentration at issuer and at issuer-hierarchy (ultimate parent) grain, across public and private holdings alike | issuer hierarchy + exposures from both asset domains | Rollup to issuer, then to ultimate parent | Cross-domain aggregation + hierarchy rollup | Exposure to ONE issuer held through BOTH a public bond and a private loan aggregates to a single issuer line, and the test asserts BOTH legs are present rather than that a total exists; rolling to ultimate parent MOVES the number on a book holding a subsidiary, asserted by comparing against the issuer-grain figure (equal figures would mean the rollup did nothing); and an issuer with no hierarchy edge is reported as its own parent rather than dropped from the report | **Draft (re-baseline part 2, 2026-08-13)** |
| REQ-CRD-008 | Credit risk report | 16.1 | Communicate credit risk | A governed, reproducible credit risk report over the credit analytics the Q6 build ships | credit results | — | Report-reproduction test | The CREDIT report family is registered in REPORT_FAMILIES, binds run IDs and regenerates identically (BR-9) under the same deployed byte-identity proof as the other families, and its content covers the credit measures the Q6 build ships. Written deliberately thin: the Q6 credit build defines the measures, and this row is REWRITTEN and G2-adjudicated when it enters a slice | Draft — **MINTED 2026-08-15 by narrowing REQ-RPT-001** at the G2 worklist adjudication; homed with the Q6 credit build (trigger: the credit kernel lands) |


### CAP-7 Counterparty Risk (CPT)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-CPT-001 | Current exposure & netting sets | 7.1/7.3 | Net counterparty exposure | CE per netting set incl. collateral/CSA | trades, CSA, collateral | Netting calc (QS-22) | Netting test | CE nets correctly per CSA; no double count | Draft |
| REQ-CPT-003 | Counterparty limits & wrong-way | 7.4 | Control counterparty risk | Limit checks; wrong-way flagging | exposures, limits | Exposure calc | Limit/wrong-way test | Breaches feed CAP-10; wrong-way flagged | Draft |

*REQ-CPT-002 (PFE/EPE) and REQ-CPT-004 (CVA placeholder) were **WITHDRAWN 2026-08-15** at the G2 worklist adjudication, executing two ratified 2026-08-12 re-baseline decisions: Monte Carlo is withdrawn from the governed spine (the withdrawal explicitly carries EPE/EEPE/peak-PFE profiles with it, roadmap Part 3) and counterparty risk is declined behind named triggers (Q6). CPT-004's acceptance — capability present, limitations documented — was also the degenerate build by design: a documentation page satisfied it. Capability leaves 7.2 and 7.5 are recorded as accepted gaps in `capability_coverage_baseline.json`; the ids are retired, never reused. If a counterparty trigger fires, fresh rows are written — including a re-opened method decision, since the seeded-MC method CPT-002 mandated no longer exists on this platform — and THOSE rows enter G2.*


### CAP-8 Liquidity Risk (LIQ)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-LIQ-001 | Liquidity classification | 8.1 | Bucket assets by liquidity | Classify instruments into liquidity tiers | positions, instrument attrs | Classification | Classification test | **Each INSTRUMENT carrying exposure has a liquidity tier (AMENDED at LQ-1, 2026-08-02, OQ-LQ-1-1 — the original clause said 'each position', which the shipped assignment rail cannot express: `ASSIGNMENT_ENTITY_TYPES` is instrument-only, and `position` carries no market value. Instrument grain is a ratified DELIBERATE SIMPLIFICATION, not a fidelity claim — 22e-4(b)(1)(ii)(B) requires a fund to account for its own POSITION SIZE when classifying, so two funds holding the same security at very different sizes receive the same tier here. Recorded as a registered model limitation and as the named trigger for a future position-grain slice)**; % illiquid computed (`illiquid_share_invested_long` — NOT the Rule 22e-4 15% test; the denominator is the invested-long book, not net assets, and the error direction is INDETERMINATE) | **Implemented (LQ-1, 2026-08-02)** |
| REQ-LIQ-002 | Redemption stress & waterfall | 8.2 | Survive redemption shocks | Apply redemption scenarios; waterfall | positions, scenarios | Waterfall calc | Redemption test | Waterfall reproduces; coverage ratios produced | **Deferred (ratified at the Wave-14 planning gate, OQ-W14P-5; homed in roadmap Part 3 RTM-P4 with an EVENT TRIGGER). Written into this register at LQ-1 (OQ-LQ-1-12): both rows previously read a bare 'Draft' for a requirement that had been formally adjudicated, so a reader of the registers alone would have concluded nothing was decided — register silence in the omission direction.** |
| REQ-LIQ-003 | Margin-call & funding stress | 8.3/8.4 | Funding adequacy under stress | Facility usage, margin-call stress | facilities, collateral | Stress calc | Funding test | Funding gap computed under shocks | Draft |
| REQ-LIQ-004 | Capital-call forecasting | 8.5 | Forecast private cash needs | Forecast calls from commitments | commitments (CAP-4) | Forecast calc | Forecast test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** (1) The portfolio-level rollup of unfunded and projected calls across (portfolio, instrument) pairs — the v2 CC-2 itself named, where discharging 'aggregated' as per-pair was already ruled an overclaim; the rollup obeys the declared aggregation contract (REQ-PPM-007) once that exists. (2) The forecast responds to its drivers: two books differing only in commitment terms produce DIFFERENT projections, asserted different. (3) The clause 'feeds CFP indicators' is DELETED — it named an artifact defined nowhere in the register, so it could never fail; a future cash-flow-planning surface names this forecast as its input in ITS OWN row. *Why the original wording was replaced: a constant forecast reproduces perfectly and 'feeds' anything a test wires it to — both clauses passed with no forecasting delivered.* | **In-Progress — STATUS RECONCILED 2026-08-15 (G2 worklist adjudication):** CC-2 shipped the per-pair kernel on 2026-07-20 (ENT-059 `pacing_projection_result`, the SEVENTEENTH governed number: per-period `projected_call`/`projected_distribution` from captured commitments through a pinned snapshot) while this row read bare Draft — the register-silence-in-the-omission-direction class OQ-LQ-1-12 named on the adjacent rows. Open: the portfolio-level rollup (the CC-2 named v2) and the driver-sensitivity test |


### CAP-9 Scenario & Stress Testing (SCN)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-SCN-001 | Scenario definition & versioning | 9.1/9.2 | Saved, versioned shock sets | Define historical/hypothetical/macro scenarios | `scenario_definition` (IA) | — | Versioning test | Scenario versioned with saved assumptions (BR-8) | **In-Progress (P3-6, Wave 2, mig `0035`, ENT-029/030):** versioned `ScenarioDefinition` + `scenario_shock` shipped — LINEAR FACTOR shocks only; style/sector/rate families and redemption scenarios are recorded v2s *(status stale ~11 waves; re-synced at the Wave-14 planning gate)* |
| REQ-SCN-002 | Reverse stress testing | 9.3 | Find shocks that breach tolerance | Search for breaching scenarios | scenarios, results | Reverse-solve | Reverse-stress test | Produces shock set hitting a target loss | Draft |
| REQ-SCN-003 | Combined & private-asset shock | 9.4/9.5 | Holistic stress incl. illiquids | Combined market-credit-liquidity + NAV shock | scenarios, private NAV | Revaluation | Combined-stress test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** (1) The demonstrating book holds at least one PRIVATE holding, and the private-NAV leg's contribution is asserted NON-ZERO before any claim about the total — private sleeves carry no factor exposures, so the shipped unnamed-factor-unchanged semantics would pass a private book through UNSHOCKED while every coverage count reads clean. (2) All three legs — market, credit, liquidity — participate, asserted by ABLATION: removing any one leg CHANGES the combined total. (3) The combination method is DECLARED; if the legs are additive, the row says so and the interaction limitation is stated (BX-LIM), so the number is honest about what it is not. (4) Reproduce-and-bind survives as the standard regression guard. *Why the original wording was replaced: 'combined run reproduces; binds all input versions' is REQ-PPM-004 verbatim — run the shipped linear factor shock alone, label the run COMBINED, bind whatever it consumed, and every clause passes.* | Draft |


### CAP-10 Limit Monitoring (LIM)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-LIM-001 | Limit framework definition | 10.1/10.3 | Encode risk appetite | Define soft/hard limits with scope; maker-checker | `limit_definition` (EV) | — | Limit-definition + SoD test | Limit changes are maker-checked & audited (BX-SOD) | **In-Progress (LIM-1 W11 mig `0050`, ENT-031; MG-3 W11 maker-checker `LIMIT.APPROVE` person-level SoD; API-2 W12 lifecycle API):** portfolio-scoped limits shipped; dimensional scope rides Wave-14 LIM-2 *(re-synced at the Wave-14 planning gate, OQ-W14P-7)* |
| REQ-LIM-002 | Utilization computation | 10.2 | Measure usage vs limit | Compute utilization from risk results | risk results, limits | Utilization calc | Utilization test | **AMENDED 2026-08-15 at the G2 worklist adjudication.** (1) Utilization is a STORED governed number — observed value, threshold, ratio — per limit per evaluation; this makes the reserved ENT-032 decision rather than reserving it further. (2) It is readable through the entity/time read surface in the rule-7 house shape: filter by limit and portfolio plus a date range, so headroom TREND is visible across at least two evaluations. (3) The demonstrating case shows a utilization STRICTLY BETWEEN zero and the threshold — a fixture that only ever sits at zero or in breach never exercises 'usage', the one-member-population house defect. (4) Binding the evaluated calculation_run stays as shipped at LIM-1. *Why the original wording was replaced: the shipped breach comparison already 'reproduces and binds', but a comparison is not a MEASURE — nothing stored or served the ratio, so nobody could see 87 percent and act before the breach.* | **In-Progress (LIM-1 W11):** `evaluate_limit` binds the evaluated `calculation_run` and compares observed metric vs threshold (utilization-in-effect); the reserved ENT-032 `limit_utilization` surface is a Wave-14 LIM-2 slice-gate call *(re-synced at the Wave-14 planning gate)* |
| REQ-LIM-003 | Breach detection | 10.4 | Detect limit breaches | Evaluate utilization vs thresholds; raise breach | utilization | Threshold eval | Detection test | Soft/hard breach raised and emits `LIMIT`/`BREACH` events | **In-Progress (LIM-1 W11 mig `0050`, ENT-033):** soft/hard Breach IA rows with per-(limit,run) idempotency + `LIMIT`/`BREACH` events; `limit_health` recomputes from source *(re-synced at the Wave-14 planning gate)* |
| REQ-LIM-004 | Strategy-node limits and strategy concentration | 10.1/10.2 | Limits where the mandate actually lives | Limits scoped to any hierarchy node including strategy sleeves, and strategy as a concentration dimension — no new captured data required | the existing limit framework + the portfolio hierarchy | Node-scoped utilisation | Node-scoped breach + an executed demonstration | A limit set on a STRATEGY node breaches on that node's holdings and NOT on its siblings', asserted by constructing a book in which the sibling WOULD breach the same threshold — a single-sleeve fixture would pass while proving nothing; utilisation is computed at the node the limit names; and the capability is DEMONSTRATED end to end on the demo book, because it already works and has never been shown, which is the only reason this is a requirement at all | **Draft (re-baseline part 2, 2026-08-13)** |


### CAP-11 Breach Workflow (BRC)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-BRC-001 | Breach record & assignment | 11.1 | Track breaches to closure | Create breach, assign owner, start timer | `breach` (IA) | — | Workflow-state test | Breach has owner, state machine, audit trail | **In-Progress (MG-2 W11 mig `0051` `breach_action`; API-2b + NOTIF-1 W12):** lifecycle state machine + assignment + SLA deadlines + notifications shipped *(re-synced at the Wave-14 planning gate)* |
| REQ-BRC-002 | 1L response & 2L review (SoD) | 11.2/11.3 | Independent oversight | 1L responds; 2L reviews; SoD enforced | `breach_action` (IA) | — | SoD (SOD-02) test | 1L cannot approve own closure; 2L review required | **In-Progress (MG-2 W11):** 1L respond / 2L review with person-level SoD (the prior-1L-responder exclusion) shipped *(re-synced at the Wave-14 planning gate)* |
| REQ-BRC-003 | Escalation & closure evidence | 11.4 | Demonstrable remediation | Escalate overdue; capture closure evidence | breach_action | — | Closure-evidence test | Closure requires evidence + approval; fully audited | **In-Progress (MG-2 W11 `escalate_overdue` + closure evidence; NOTIF-1 W12 breach-deadline notifications; OPS-H1 W13 the N+1 bound):** shipped and tick-driven *(re-synced at the Wave-14 planning gate)* |


### CAP-12 Model Governance (MDG)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-MDG-001 | Model inventory & versioning | 12.1/12.2 | Single register of models | Register every model/version + assumptions/limitations | `model` (EV), `model_version` (IA), `model_assumption`/`model_limitation` (IA) | — | Inventory-binding test | No calc runs without an inventory entry (BR-3) | In-Progress (P1A-2 skeleton) |
| REQ-MDG-002 | Tiering | 12.3 | Proportionate governance | Assign Tier 1/2/3 by criteria | model metadata | — | Tiering test | Each model has a tier; Tier-1 gated to human approval | In-Progress (MG-1, 2026-07-15: dual ratings + derived tier via the 2L `assign_model_tier` + `MODEL.TIER_ASSIGN`; the Tier-1 H-02 human-approval gate stays open) |
| REQ-MDG-003 | Validation workflow & effective challenge | 12.4/12.5 | Independent assurance | Developer≠validator workflow; approval status | `model_validation` (IA) | — | Independence (SOD-03) test | Author cannot validate; Tier-1 needs H-02 approval (BR-15) | **In-Progress (VW-1 2026-07-14, ENT-037 `model_validation`, migration `0039`)** — append-only SR 11-7 validation records at `model_version` grain with the `model.validate` 2L-only mint; the effective-challenge workflow legs remain. *Status repaired at the Wave-19 planning gate: this cell read bare Draft while the RTM had recorded the shipped slice. Repaired to MIRROR the RTM, not to claim completion. Status-only edit; acceptance text untouched; no adjudication hash moved.* |
| REQ-MDG-004 | Risk-domain coverage declaration | 12.1 | Say which risks are governed and which are not (SCOPE-03) | A declaration of each SCOPE-03 risk domain — market, credit, counterparty, liquidity, scenario and stress, limits and breach — as GOVERNED, PARTIAL or DECLINED, each DECLINED domain naming its firing trigger | the run-type registry + the ratified decision records | Declaration + census against the registry | Census + trigger validity (P19) | Every SCOPE-03 risk domain appears in the declaration with a status, by exact set equality against the six named in the scope document; a domain declared GOVERNED has at least one family in the run-type registry, asserted against the REGISTRY rather than against prose; every DECLINED domain names a trigger that is a slice in the roadmap or a mechanical condition (P19); and COUNTERPARTY is declared DECLINED with its ratified triggers, because simulated exposure profiles need Monte Carlo, which was withdrawn from the governed spine on 2026-08-12. A declaration claiming full SCOPE-03 coverage FAILS this row | **Draft (re-baseline part 2, 2026-08-13; Q6 — SCOPE-03)** |


### CAP-13 Data Quality & Reconciliation (DQR)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-DQR-001 | DQ rules engine | 13.1 | Detect bad data early | Configurable DQ rules run on ingest | `data_quality_rule` (EV), `data_quality_result` (IA) | Rule eval | DQ-rule test | Rules run on load; exceptions raised, not silent | **In-Progress (P1A-3 skeleton; RANGE added P2-2; `RULE_TYPE_COMPLETENESS` MINTED at DATA-1, 2026-08-02 — FOUR generic evaluators):** the completeness kind fires REF-1's ratified trigger (the persisted rule's params carry the expected key set literally); first governed execution = the TB3MS refresh gate (savepoint-preserved FAIL evidence). Reconciliation stays the named gap *(the DATA-1 mint closed the completeness half of the "no completeness/gap/reconciliation rule type" clause)* |
| REQ-DQR-002 | Reconciliation | 13.2 | Agreement across sources | Reconcile positions/valuations across feeds | recon inputs | Recon calc | Recon test | Discrepancies surfaced with severity | Draft |
| REQ-DQR-003 | Manual overrides (maker-checker) | 13.4 | Controlled correction | Override with BR-7 fields + approval | `manual_override` (IA) | — | Override (BR-7) test | Override carries prior/new/justification/approval; audited | Draft |
| REQ-DQR-004 | Exception management | 13.3 | A failed check nobody owns is not a control (SCOPE-04) | Every data-quality or reconciliation failure raises an exception with an owner, a state, and an evidenced closure — 1st line acts, 2nd line reviews | DQ rule results + reconciliation breaks | Exception lifecycle over an append-only record | Lifecycle + re-open-on-recurrence test | A failing DQ rule CREATES an exception with a named owner; closing one requires recorded evidence, and a 2L-reviewed rule requires a closer distinct from the opener (SCOPE-04); an exception cannot be closed by deleting the failing data, asserted by re-running the rule after closure and finding the exception RE-OPENED rather than absent; and the count of open exceptions is readable per tenant on a screen. A DQ failure that only writes a log line FAILS this row | **Draft (re-baseline part 2, 2026-08-13)** |


### CAP-14 Data Lineage (LIN)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-LIN-001 | Lineage skeleton & capture | 14.1/14.2 | Trace every output to source | Lineage edges from source→run→result | `lineage_edge` (IA), `data_source` (EV) | — | Lineage-completeness test | Every governed output has a complete lineage path (BR-6/13) | In-Progress (P1A-1 skeleton) |
| REQ-LIN-002 | Lineage query & extract | 14.3 | Answer "where did this number come from" | Query/visualize lineage for a result/report | lineage edges | — | Lineage-query test | Given a result, the full upstream graph is returned | Draft |


### CAP-15 Auditability (AUD)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-AUD-001 | Audit coverage of governed actions | 15.1/15.4 | Complete, immutable trail | All governed writes emit taxonomy events (extends FW-AUD) | `audit_event` (IA) | — | Audit-coverage test | No governed write without an audit event (CTRL-012) | Draft |
| REQ-AUD-002 | Chain integrity & verification | 15.2 | Tamper evidence | Scheduled `verify_chain` + checkpoints | audit_event/checkpoint | Hash verify | Tamper-detection test | Chain break is detected & alertable (BR-18) | Draft |
| REQ-AUD-003 | Audit query & regulator extract | 15.3 | Investigations & DD | Entitled audit query + signed extract | audit_event | — | Extract test | Extract is entitled, complete, and reproducible | Draft |


### CAP-16 Reporting (RPT)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-RPT-001 | Risk reports (registered report families) | 16.1 | Communicate risk | Generate governed, reproducible risk reports | approved results | — | Report-reproduction test | **AMENDED 2026-08-20 at the Wave-19 planning gate.** Every family in the declared report-family registry (REPORT_FAMILIES) ships a report that binds run IDs and regenerates identically (BR-9), checked by EXACT SET EQUALITY between the families exercised by the NAMED DEPLOYED PROOF ARTIFACT (`infra/deploy/prove_report_identity.sh`) and the registry — a subset check is the RPT-3 defect. The registry must cover every governed family that has a shipped report subject, so declining to ADD a family is a failure of this row rather than a way to keep the equality true. The credit risk report is NOT this row: it is minted as REQ-CRD-008, homed with the Q6 credit build. *Why re-amended at the Wave-19 gate: the 2026-08-15 narrowing bound scope to REPORT_FAMILIES, a constant the delivering team edits, so both 'do not add the family' and 'delete a family' kept exact-set equality true — the same editable-scope drift the narrowing was written to close. And 'the reproduction proof' had no defined referent: the register named the deployed script, which seeds exactly ONE of four families.* | In-Progress (RPT-1/RPT-2 2026-08-07; RPT-3 2026-08-10) — binds-and-regenerates PROVEN end-to-end on the deployed stack (`infra/deploy/prove_report_identity.sh` byte-verifies a regenerated artifact with a negative control; RPT-3 makes generation operator-reachable). Open: the exact-set-equality census clause added at the 2026-08-15 narrowing. The CREDIT report moved to REQ-CRD-008 (minted 2026-08-15), homed with the Q6 credit build |
| REQ-RPT-002 | Board risk report | 16.3 | Board-level oversight | Aggregate narrative from approved metrics | approved results | — | Board-report test | Publication is maker-checked (SOD-08); no unapproved data | Draft |
| REQ-RPT-003 | DQ / model-inventory / audit extracts | 16.4/16.5 | Control transparency | Generate control/DQ/inventory/audit reports | DQ, inventory, audit | — | Extract test | Extracts reproduce and are entitlement-scoped | Draft |

| REQ-RPT-004 | Report definition entity | 16.1 | A report is a versioned thing, not an ad-hoc call | A generation binds a report_definition_version_id — append-only, carrying declared families, audience tiers and section order | report definition registry | Versioned definition | Binding test | Every generation carries a definition version id; editing a definition mints a NEW version rather than mutating the old (asserted by re-reading the prior version after an edit and finding it unchanged); a generation bound to version N re-renders from N's declarations after N+1 exists | **Draft (re-baseline 2026-08-12)** |
| REQ-RPT-008 | Scheduled generation run-binding rule | 16.1 | A stale board pack is worse than none | A scheduled generation derives ONE as-of date from the tick, binds the newest completed run per declared family within it, and REFUSES the whole tick if any declared family has none | schedule tick, completed runs | Declared binding rule | Refusal test (P9) + positive twin | With every declared family present the generation succeeds and all bound runs share the derived as-of date; with ONE family missing it REFUSES and writes nothing (refusal made to fire before shipping); the refusal surfaces on the ops health read, not only in a log | **Draft (re-baseline 2026-08-12)** |
| REQ-RPT-005 | Scenario and breach reports | 16.2 | The two reports a risk committee actually asks for | Governed report families for scenario and stress results, and for breach activity over a period | scenario runs + breach records, pinned | Report generation over a pinned evidence set | Completeness + content-hash reproduction | A scenario report names EVERY scenario in the bound run and its result, asserted by exact set equality against the run's scenario set — so a report that silently omits the worst scenario fails; a breach report over a period contains every breach whose lifecycle touched that period, INCLUDING ones opened before it and still open (a naive date filter drops exactly the breaches that matter most); both reproduce byte-identically from their pinned evidence; and a generation whose bound run is missing REFUSES rather than rendering a partial page | **Draft (re-baseline part 2, 2026-08-13)** |
| REQ-RPT-006 | Board-altitude limitations | 16.3 | A board sees that limits exist without reading the manual | The board face carries a provenance line and the COUNT of limitations with a link; the appendix carries every limitation of each pinned model version with its operative validation conditions, derived mechanically | registered model versions + their recorded limitations and validation conditions | Mechanical derivation, no human in the loop | Derivation + count-agreement on the RENDERED artifact | The limitation COUNT on the board face equals the number of limitation entries in the appendix, asserted by counting both in the RENDERED artifact rather than in the data behind it; the appendix is derived mechanically, asserted by adding a limitation to a model version and re-rendering to find it present with NO edit to the report; and a model version with no recorded limitations renders 'none recorded' rather than an empty section | **Draft (re-baseline part 2, 2026-08-13; Q10 — the editorial flag is DEFERRED)** |

### CAP-17 Administration & Entitlements (ADM)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-ADM-001 | Real SSO / OIDC identity | 17.1 | Enterprise identity | Replace dev header shim with OIDC + MFA | users/principals | — | Auth integration test | **AMENDED 2026-08-20 at the Wave-19 planning gate.** Principal comes from OIDC; MFA enforced (AD-007). Delivered form: the scripted deploy stands up an OIDC target running auth_mode=oidc against the checked-in realm with TLS terminating in front; that deployed target refuses a dev-header request BY THE DEV-HEADER REFUSAL SPECIFICALLY — a bare 401 is not acceptance, because with auth_mode=oidc the dev-header branch is never reached and any unauthenticated call 401s; MFA is asserted on the deployed target via oidc_require_mfa + oidc_acr_values with the refusal FIRED; and the existing dev_header proof stack continues to pass its four deployed proofs unchanged, because those proofs create their principals over HTTP at runtime and can carry no IdP identity. Internet-facing hosting and enterprise IdP federation remain RTM-P9 and are NOT this row. *Why the dev-header clause names the specific refusal: the obvious wording is passed by any 401, including one from a guard that never examined the header.* | **In-Progress (SSO-1 W9 PR #86 real OIDC boundary + FE-3b W10 PR #95 browser PKCE):** the principal-from-OIDC leg is realized; MFA posture rides the IdP; admin surfaces + full RTM-P9 replacement of the dev shim remain *(re-synced at the Wave-14 planning gate, OQ-W14P-7)* |
| REQ-ADM-002 | RBAC/ABAC entitlement administration | 17.2/17.3 | Manage access safely | Admin roles/permissions/grants; SoD/maker-checker | entitlement tables (EV) | — | SoD + maker-checker test | Entitlement changes are maker-checked & audited (BX-SOD) | Draft |
| REQ-ADM-003 | Data classification & export controls (MNPI) | 17.5 | Protect confidential/MNPI | DC tagging; DC-4 export blocked by default; barriers | classified data | — | Export-control + MNPI test | DC-4 export blocked; bulk export four-eyes; denials audited | Draft |
| REQ-ADM-004 | Admin console | 17.4 | Operate the platform | Entitled admin UI for users/config | admin entities | — | Admin-console test | All admin actions entitled and audited | Draft |
| REQ-ADM-005 | Line-of-defence separation | 17.2/17.3 | 1st line acts, 2nd line challenges, 3rd line reads (SCOPE-04) | Entitlements express the three lines: 1L takes risk and responds, 2L reviews and cannot be overridden by 1L, 3L is read-only assurance | role catalog + entitlements + the route census | Entitlement enforcement at the boundary | One refusal per line, each made to FIRE (P9) | A 1L principal cannot close a 2L review; a 2L principal cannot originate the position it reviews; a 3L principal is refused EVERY write across the WHOLE route census, asserted against the census rather than a sample, because a sampled write-fence is false security (FE-2's finding); each of the three refusals is made to FIRE before this ships. A role merely NAMED 2L with no refusal behind it FAILS this row | **Draft (re-baseline part 2, 2026-08-13)** |
| REQ-ADM-006 | Deployment model: multi-tenant with a single-tenant option | 17.4 | One product, two deployment shapes, one codebase (SCOPE-05) | Multi-tenant SaaS is the default; a single-tenant deployment runs the SAME image with one tenant admitted — configuration, never a code fork | tenant registry + deployment configuration | Configuration, not a branch | Single-tenant deployment proof in CI | A single-tenant deployment runs the IDENTICAL image, asserted by comparing image digests rather than by reading a configuration flag; tenant isolation stays ACTIVE in single-tenant mode — RLS is not relaxed because there happens to be one tenant — asserted by running the same cross-tenant refusal suite against it; and the proof runs in CI, because a deployment shape nothing executes is a claim (AD-008, AD-010) | **Draft (re-baseline part 2, 2026-08-13)** |


### CAP-18 Integration Readiness (INT)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-INT-001 | File upload (CSV/Excel) | 18.1 | Onboard data without integration | Validated, sandboxed upload via anti-corruption layer | staged files | — | Upload-validation + malicious-file test | **AMENDED 2026-08-20 at the Wave-19 planning gate.** A positions file loads into canonical data ONLY through a RATIFIED mapping version. (1) A load with no ratified mapping version for the source type REFUSES, fired in a test. (2) Every ingestion batch AND every loaded position row binds the ratifying mapping version by hard FK — never a free-text field — re-resolved tenant-filtered on write. (3) Ratifying an EDITED mapping version and re-loading the same file produces canonically different rows exactly where the edit says, asserted by mutating one operation and observing the change. (4) The interpreter is the ONLY write path from staged rows to canonical positions, asserted by a census that DISCOVERS write paths mechanically, never a hand list. (5) Each closed-set operation's refusal FIRES in a test; an unsupported operation is refused BY NAME. (6) Ratification is four-eyes: the ratifier differs from the proposer, refusal fired. (7) Every mapping version records the PROPOSING model version and prompt identity, or is explicitly marked HAND_AUTHORED, and the demonstrating flow ratifies a PROPOSED mapping end to end — without this clause the AI-drafting leg that is the ratified slice's whole thesis can be deleted while every other clause passes. (8) The demonstrating file exercises at least THREE distinct operation kinds, so a rename-only demo cannot pass. (9) A load is reproducible from the mapping version, the staged file, AND the code-lookup reference data as of the load — all three named, because one ratified operation resolves against reference data held in neither of the other two. (10) An overlapping re-load REFUSES unless flagged a restatement, fired. The P1A-4 upload-validation and malicious-file clauses stay as REGRESSION GUARDS. | In-Progress (P1A-4 upload skeleton, CSV-only) — the canonical mapping spine is Wave-19 S3a/S3b (INGEST-1, ratified 2026-08-12) |
| REQ-INT-002 | API & SFTP adapters | 18.2/18.3 | Automated feeds | Inbound API/SFTP adapters to canonical model | feed payloads | — | Adapter-mapping test | Feeds map to canonical entities; audit + lineage on ingest | Draft |
| REQ-INT-003 | Vendor / accounting / GP-report adapters | 18.4/18.5 | Connect ecosystem | Adapter abstraction for vendors & GP reports | vendor feeds | — | Adapter-abstraction test | New vendor added via config; no structural change (ARCH-P-07) | Draft |
| REQ-INT-004 | Input-supplier position and the fund-grain asset series | 18.2 | Give the filer what they need without becoming one (SCOPE-01) | An explicit supersession of the existing fence: the platform is an INPUT SUPPLIER, not a filer. It ships a fund-grain net and gross assets series plus a governed export | fund-grain valuations, snapshot-pinned | Net and gross asset aggregation | Export reproduction + a levered fixture | The export reproduces byte-identically from its pinned evidence and carries the run and snapshot ids it came from; net and gross assets DIFFER on the test fixture, which must be a book that HAS leverage — on an unlevered book the two coincide and the test would pass while proving nothing; and the input-supplier position is recorded as an explicit supersession naming the fence it replaces, never as a silent widening | **Draft (re-baseline part 2, 2026-08-13; Q7 — SCOPE-01)** |


### CAP-19 BAU AI Agent Support (BAI)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-BAI-001 | DQ & private-asset monitoring agent | 19.1 | Scale data ops | Agent summarizes DQ exceptions; recommends remediation | DQ results (read-only) | — | Agent-logging test | Agent actions logged (BR-16); read-only tier; cites sources | Draft |
| REQ-BAI-002 | Breach triage & scenario commentary agents | 19.2/19.3 | Faster, clearer ops | Draft breach narratives / scenario commentary | approved results | — | Cite-don't-invent test | Agent cites governed metrics; no invented numbers; human approves action | Draft |
| REQ-BAI-003 | Model monitoring & board/evidence assistants | 19.4/19.5 | Governance & reporting support | Flag model drift; draft board/evidence packs | monitoring/approved data | — | Approval-gate test | Drafts only; publication needs human approval (BR-15/16) | Draft |


### CAP-21 Presentation & Exploration (PRS)

*MINTED at the product re-baseline (2026-08-12). Every acceptance clause below requires a HUMAN to
see something or a RENDERED artifact to be asserted on. That is deliberate: of the 74 rows that
preceded this section, 22 required reproduction and TWO required anyone to see anything, and the
Definition of Done's only UI clause was a prohibition.*

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-PRS-001 | Presentation contract per result family | 21.1 | Every governed number knows how it is shown | Each result family declares its presentation contract — mark type, axes, units, precision, and the identity fields a reader needs | result family registry | Declaration + census | Census test | **AMENDED 2026-08-20 at the Wave-19 planning gate.** Every family in the run-type registry has a presentation contract, asserted by EXACT SET EQUALITY against the registry (not a subset — a subset relation is satisfied by an empty contract set, the RPT-3 defect); families outside the presentable set are named in a DECLARED EXCLUSION carrying a written reason, never given a stub contract to satisfy the census; a family added without a contract or an exclusion fails the census; each contract names at least one identity field so no rendered number is anonymous. AND the contract must be CONSUMED, not merely declared: the section's pinned content carries the presentation contract the render used; a regenerated historical report renders the PINNED contract, not the current one; changing a family's declared precision, unit or mark changes the bytes of a NEWLY generated report while leaving every previously generated report byte-identical on regeneration — both halves asserted; a family whose contract no renderer can resolve FAILS. *Why the consumption clauses exist: the declaration-only text was satisfied by a stub contract for every family that nothing reads, the declaration-without-firing shape ratified decision D2 forbids. Why the contract is PINNED rather than resolved at render time: every render input is pinned by shipped invariant, so a render-time lookup would make one precision edit mark every pre-edit report DIVERGED in the CTRL-018 reproduction sweep.* | **Draft (re-baseline 2026-08-12)** |
| REQ-PRS-002 | The governed chart | 21.2 | A chart is evidence, not a picture of it | A chart is a server-rendered inline-SVG fragment emitted by the report section renderer, covered by the report's content hash, and re-renderable byte-identically from the same pinned inputs | pinned run + presentation contract | Server-side SVG, Decimal projection | Render + reproduce test | **AMENDED 2026-08-20 at the Wave-19 planning gate.** Rendering the same pinned evidence set twice produces BYTE-IDENTICAL SVG; the fragment carries the run id it was produced from; altering any bound input changes the bytes; the chart's own content is covered by the report content hash (verified by mutating one datapoint and observing the hash move); the SVG is produced FROM the family's PINNED presentation contract, asserted by generating either side of a contract edit and observing the new bytes move; and the fragment contains the DECLARED MARK SHAPES for its declared mark type — path, rect, line or circle elements — not only text nodes. *Why the last two clauses exist: the original four were all satisfied by a renderer that never consults a presentation contract and emits an SVG of text-node number dumps, which is a table drawn in SVG, not a chart.* | **Draft (re-baseline 2026-08-12)** |
| REQ-PRS-003 | Audience-tiered rendition | 21.3 | Four audiences, one set of numbers | One bound evidence set produces ANALYST / PM / COMMITTEE / BOARD renditions, audience carried as a first-class column | one pinned evidence set | Rendition per audience class | Four-audience render test | All four renditions of one evidence set quote the SAME underlying values — asserted by extracting the numbers from each rendition and comparing, so the four cannot drift into four documents that disagree; each declares its audience; and the BOARD rendition renders no value absent from the ANALYST one | **Draft (re-baseline 2026-08-12)** |
| REQ-PRS-004 | Drill-down within the governed chain | 21.4 | Click a number, see what makes it up | A rendered value links to its contributors, each hop a governed read carrying its own provenance | decomposition run + lineage | Contributor edges | Traversal test | From a rendered portfolio-level value, a reader reaches position-level contributors in at most two hops; EVERY hop returns a run id and a snapshot id; a hop that would cross an entitlement boundary is refused rather than silently truncated; and the contributors' values sum to the value drilled from | **Draft (re-baseline 2026-08-12)** |
| REQ-PRS-005 | Exploration tier | 21.5 | Asking a question must not mint evidence | An EXPLORATORY run uses identical code, is irreversibly marked non-citable, cannot be bound by a report or consumed downstream, and never enters the reproduction population | any governed family | Run class | Refusal test (P9) | **Every rendition of an exploratory result is VISIBLY marked non-citable — the mark is asserted on the RENDERED artifact, not on the row behind it** (added when gate G3 rejected the first version of this very clause: it was written entirely in runs and refusals, and the whole point of an exploration tier is that an analyst can SEE that what they are looking at cannot be cited); an exploratory run computes the same value as its governed twin; binding one to a report is REFUSED and the refusal is made to FIRE before this ships; it is absent from the reproduction sweep's population, asserted by counting the sweep's subjects; and it CANNOT be promoted in place — promotion is re-execution as a governed run | **Draft (re-baseline 2026-08-12)** |


### CAP-20 Performance Measurement (PRF)

| REQ | Title | CAP | Business purpose | Functional | Data | Calc | Test | Acceptance | Status |
|---|---|---|---|---|---|---|---|---|---|
| REQ-PRF-001 | Portfolio return (time-weighted) | 20.1 | Measure the return a book actually earned, external cash flows neutralised (the prerequisite for ex-post benchmark-relative analytics, VaR backtesting, and private-asset returns) | Governed chain-linked TWR (Modified-Dietz within caller-supplied exposure-run valuation boundaries) as a run — a `DIETZ_PERIOD` series + a `TWR_LINKED` summary | exposure-run market values + `transaction` external flows (`TRANSFER_IN`/`TRANSFER_OUT`) + FX legs, all snapshot-pinned | Modified-Dietz + geometric linking (GIPS 2020); Decimal-50, `Numeric(20,12)` fraction | Hand golden + independent float cross-check + reproduction-under-correction (TR-09) | Return matches hand/independent reference within ε; re-run identical; a later exposure re-run OR a transaction append cannot move a historical return; method has methodology doc + inventory entry | **In-Progress (PM-1, 2026-07-09, migration `0031`):** `portfolio_return_result` (ENT-053) — time-weighted return v1 (`perf.return.twr`), single-portfolio book; gross-of-fees, unannualized. **Money-weighted/IRR (PA-0), net-of-fees, attribution, composites, annualization, and multi-portfolio/subtree books DEFERRED; captured-holdings book (no cash ledger) a first-class recorded limitation.** REQ does **NOT** close |
| REQ-PRF-002 | Ex-post benchmark-relative performance | 20.5 | Measure realized performance vs a benchmark, after the fact — active return, tracking difference, realized tracking error, information ratio (the UCITS ex-post disclosure figures + the skill measure) | Governed ex-post statistics as a run over ONE portfolio-return run + a captured `benchmark_return` series — an `ACTIVE_RETURN` series + `TRACKING_DIFFERENCE`/`TRACKING_ERROR`/`INFORMATION_RATIO` summaries | PM-1 `portfolio_return_result` rows + the in-span SIMPLE `benchmark_return` rows of the chosen `return_basis`, all snapshot-pinned | arithmetic active returns; geometric compounding; unbiased (n−1) sample TE; Grinold-Kahn IR; Decimal-50, `Numeric(20,12)`; unannualized | Hand golden + independent `statistics.stdev` cross-check + reproduction-under-correction (TR-09) | Metrics match hand/independent reference within ε; re-run identical; a later PM-1 re-run OR a benchmark vendor correction cannot move a historical result; method has methodology doc + inventory entry | **In-Progress (P3-8, 2026-07-10, migration `0032`):** `benchmark_relative_result` (ENT-054) — realized active return / TE / TD / IR v1 (`perf.benchmark_relative`); `benchmark_return`'s FIRST governed consumer; closes P3-7 OD-G; REUSES `perf.run`/`perf.view`. **Annualization, geometric-excess, active share, relative VaR, attribution, multi-benchmark, LOG return_type DEFERRED; the PM-1 captured-holdings-book bias PROPAGATES (first-class limitation); benchmark missing-day compounding hazard recorded (calendar validation RE-POINTED at OQ-DATA-1-5's ratified trigger, 2026-08-02: *the first captured DAILY benchmark series* — DATA-1 minted the generic COMPLETENESS machinery and executed it at MONTH grain on the real rf-rate series, but its monthly dataset has no trading-day grain to police, and the shipped benchmark_relative runtime acceptance is a governed convention change reserved to the trigger — the THIRD deferral, ratified explicitly).** REQ does **NOT** close |
| REQ-PRF-003 | Rolling return & rolling volatility over the governed return series | 20.5 | Give the return series a *risk* reading on the window a supervisor actually uses — the GIPS ex-post measure — so volatility and trailing performance are governed numbers rather than spreadsheet derivations | Governed trailing-window statistics as a run over ONE portfolio-return run, relinked to a CALENDAR-MONTH grid: rolling return (raw + geometrically annualized) and rolling volatility (raw + ×√12) at each registered window | PM-1 `portfolio_return_result` `DIETZ_PERIOD` rows, snapshot-pinned | Monthly grid (GIPS 4.A.1.j; sub-periods are INPUTS, never the sample); the three-condition alignment criterion with the 2.A.23.b business-day allowance; n−1 variance, arithmetic centring, no c4 correction (material: +4.45% at n=12); ×√12 from the STORED value so the pair reconciles exactly; returns never annualized below 12 months (GIPS 2.A.12, enforced at the REGISTERED parameter domain); `Numeric(20,12)` | The alignment refusals (within-month, partial edge, missing interior month); exact σ/σ_ann reconciliation on persisted rows; the non-associativity pin against PM-1's `TWR_LINKED`; a genuine 7-window series on the demo book | A misaligned span REFUSES rather than truncating; an unfillable window emits a SUPPRESSED row (nullable value + explicit flag), never a stuffed zero; the disambiguation key `(metric_type, window_months, annualization_basis)` is complete on every row; method has methodology doc + inventory entry | **In-Progress (RM-1, 2026-07-27, migration `0054`):** `rolling_risk_result` (ENT-064) — `perf.rolling_risk` v1, windows `{12, 36}` as a registered parameter; PM-1's SECOND governed consumer; discharges PM-1's annualization deferral EXPLICITLY, PARTIALLY and NARROWLY (volatility only, monthly grid, ×√12). REUSES `perf.run`/`perf.view`. **No Sharpe (SR-1); no benchmark leg (GIPS 2.A.18.a binds the v2); rolling values are NOT independent — ~92% overlap between adjacent 12-month windows, the most likely misreading; the month-end convention is holiday-free in v1 (RESOLVED BY THE v2 MINT at CAL-1b, 2026-08-01 — the v1 label keeps its recorded limitation; new binds may use v2).** REQ does **NOT** close |
| REQ-PRF-004 | Risk-adjusted return (Sharpe ratio) over the governed return series | 20.6 | Give the return series the reading an allocator quotes first — *how much return did this book earn per unit of risk taken, above cash* — as a run-bound, snapshot-pinned governed number rather than a spreadsheet ratio whose denominator nobody can name | Governed trailing-window Sharpe ratio as a run over ONE portfolio-return run and ONE captured risk-free return series, on the RM-1 calendar-month grid: `SR = mean(d)/sigma(d)` where `d_j = m_j - r_f,j`, plus its x sqrt(12) annualization, at each registered window | PM-1 `portfolio_return_result` `DIETZ_PERIOD` rows + ENT-052 `benchmark_return` rows (a captured, vendor-published monthly cash series), both snapshot-pinned | **Sharpe (1994)'s DIFFERENTIAL-return form — the denominator is sigma of the EXCESS series, NOT of the portfolio series (the two coincide only when `r_f` is constant, so RM-1's persisted `ROLLING_VOLATILITY` is NOT reusable); an n-1 divisor that DIVERGES from the paper's own population-sigma endnote by about 4.3% at n=12, DISCLOSED in the registered assumptions rather than branded away; single-quantization (mean and sigma unquantized at 50 digits, the division there, the RATIO quantized once) because dividing quantized operands raises `DivisionByZero` on a NON-constant series whose sigma quantizes to `0E-12`; the risk-free leg joined by MONTH KEY, one row per MEASURED month, no imputation; `Numeric(20,12)` | Golden Sharpe values computed by hand and mutation-tested (five kernel mutants, ten binder mutants, all killed); the DISCRIMINATING fixture where sigma(excess) and sigma(portfolio) genuinely differ; the executed refutation that a quantized-sigma of zero still EMITS; the missing-, duplicate- and unconsumed-risk-free-month refusals; exact SR/SR_ann reconciliation on PERSISTED rows; a PG enforcement suite from birth | The ratio is UNBOUNDED on admitted inputs, so a magnitude gate on the EMITTED value (covering the annualized member) yields a COMMITTED FAILED run with zero rows, never a partial emit; a constant excess series emits a SUPPRESSED row (nullable value + explicit flag + reason), never a stuffed zero, because **0 is a legitimate Sharpe ratio**; a missing risk-free month is a pre-create REFUSAL naming the month, deliberately asymmetric with per-window suppression; method has methodology doc + inventory entry | **In-Progress (SR-1, 2026-07-28, migration `0055`):** `sharpe_ratio_result` (ENT-065) — `perf.sharpe` v1, windows `{12, 36}` as a REGISTERED parameter; PM-1's FIFTH governed consumer and ENT-052's SECOND. REUSES `perf.run`/`perf.view`. **The x sqrt(12) annualization carries iid as a DECLARED CONVENTION (Lo 2002); the autocorrelation-corrected annualizer (Lo Eq. 20, exact only on log returns) is a recorded v2. No Sortino, no Treynor, no downside-deviation denominator, no daily grid. GIPS 2020 does not define a Sharpe ratio — presented, it is an 'additional risk measure' under 4.C.43.a/4.C.44, and rows are GROSS-of-fees inherited from PM-1.** REQ does **NOT** close |
| REQ-PRF-005 | Performance attribution | 20.3 | The first question a risk committee asks | Governed segment-grain returns, then attribution of active return to allocation and selection effects | segment returns, benchmark weights | Brinson-style decomposition | Attribution test | Effects sum to total active return EXACTLY (exact-decimal kernels, not within ε); each effect is attributed to a NAMED segment, so the output is one row per segment rather than a single total; re-running over pinned inputs reproduces identically. A run producing only a total active return FAILS this row | **Draft (re-baseline 2026-08-12)** |

## 8. Open Questions

See [RTM §Open Questions](requirements_traceability_matrix.md) — consolidated there to avoid duplication.

## 9. Dependencies

All forward dependencies are registered in §6 and applied per requirement in the RTM. The backbone assumes only the foundation
described in §3.
