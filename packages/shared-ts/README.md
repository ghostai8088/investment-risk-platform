# Shared TypeScript library (`@irp/shared`)

Cross-cutting TypeScript types shared by the frontend (and future TS consumers). **Scaffold only.**

Currently exposes `SHARED_LIB_VERSION` and the `TemporalClass` type mirroring the Python enum (AD-005 / BR-19).
