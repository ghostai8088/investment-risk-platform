# Control Matrix Skeleton

## Document Control

| Field | Value |
|---|---|
| Document ID | COMPLIANCE-CTRLMTX-001 |
| Version | 0.1 (Draft for Review) |
| Status | Draft (skeleton — to be extended per phase) |
| Owner | R-10 Compliance & Controls AI |
| Approver | H-05 Head of Compliance (H-08 Internal Audit consulted) |
| Created | 2026-06-17 |
| Last Reviewed | 2026-06-17 |
| Related Documents | build_rules.md, all 03–07 standards, audit_event_taxonomy.md, entitlement_sod_model.md, model_governance_independence_policy.md |
| Supported Build Rules | BR-1 … BR-16 (traceability layer) |

## 1. Purpose

Provide the control library that links each control to the capability it protects, the build rule(s) it enforces, the owning
role, the test/assurance method, and the evidence artifact. This is the traceability backbone that makes the build rules
auditable. It is a **skeleton**: controls are seeded across every framework and will be expanded per construction phase.

## 2. Schema

| Column | Meaning |
|---|---|
| Control ID | Stable `CTRL-nnn` |
| Control | Name/description |
| Type | Preventive / Detective |
| Mode | Automated / Manual / Hybrid |
| Capability | Capability map ref / bounded context |
| Build Rule | BR-n enforced |
| Owner | Accountable role (R-/H-) |
| Test / Assurance | How the control is verified |
| Evidence | Artifact proving operation |
| Status | Planned / Designed / Implemented / Operational *(Operational added to the legend at the Wave-13 close — CTRL-003 had used it since P3-1 while the legend still declared three values, so the matrix's own vocabulary was out of conformance with its rows)* |

## 3. Control Library (seed)

| Control ID | Control | Type | Mode | Capability | Build Rule | Owner | Test / Assurance | Evidence | Status |
|---|---|---|---|---|---|---|---|---|---|
| CTRL-001 | Every feature has tests before completion | Preventive | Automated | All | BR-1 | R-09 | CI gate; coverage report | Pipeline run, coverage | Planned **(DEP-1 2026-08-05: NO CONTROL MOVED — stated explicitly per the P1 seventh-ledger rule. The slice EXTENDS this row's evidence class without changing its status: CI now BUILDS and smoke-tests the three deployable images (the `images` job, hygiene-checked against build-context pollution, mutation-proven against the pre-`.dockerignore` images) and RUNS the full stack (the `stack-proof` job — deploy to an empty database + backup/restore both arms, itself mutation-proven: deliberately broken at `0c0fdc3`, CI run 31023628263 went red for the predicted reason with all seven other jobs green). Two honest gaps, stated rather than implied: (1) DEP-1's proven BACKUP/RESTORE has NO control row to move — no backup/DR control exists in this matrix, and minting one is a governed H-05 gate decision, not a slice-side act; it is flagged here as a candidate for the next control-mint gate. **DISCHARGED 2026-08-20 at the Wave-19 planning gate: the candidate was minted as CTRL-038 (backup/DR), Implemented (OBSERVED), with its four Operational preconditions named in-row. This sentence is retained rather than deleted so the flag's history stays readable, but it is no longer an open candidate.** (2) CTRL-009 (reports reproducible) and CTRL-018 (scheduled reproduction) are UNTOUCHED — they are RPT-1's and a future slice's territory respectively.)**|
| CTRL-002 | Every calculation has methodology doc | Preventive | Hybrid | §4–8 | BR-2 | R-06 | Doc-consistency hook. **REAL ENFORCEMENT GAINED at SR-1 (2026-07-28; trace RELOCATED here at the Wave-13 close — SR-1 appended it to CTRL-003's row, so this control's own row carried no record of the change made to it, while its Status still read "Planned" against the SR-1 record's claim "both remain Operational"):** the snapshot `SNAPSHOT_PURPOSES` allow-list moved INTO `_persist_snapshot`, the single line every build path crosses — it had been enforced only at the two GENERIC entry points while fifteen family builders passed their constant straight through, so a document describing it as "the ENFORCED allow-list" was describing a convention. **HARDENED at the Wave-13 close:** the moved gate gained its own executed negative control (`test_snapshot.py::test_the_persist_tail_purpose_gate_fires_directly_and_writes_nothing` — a deletion mutant had survived the entire suite because the only prior `SnapshotPurposeError` test rode `build_snapshot`, whose surviving pre-check masked the tail), plus the two promised membership pins and a both-directions census over every declared `PURPOSE_*` constant. | Methodology doc + QS-24 decl. + the `_persist_snapshot` purpose gate with executed negative control + the purpose census pin (+ CON-1 2026-07-30: `PURPOSE_CONCENTRATION_INPUT` entered the enforced allow-list + the reflective census; the declared-parameter exact-identity parse-back `declared_concentration_parameters` refuses a bind under a silently different methodology) | Operational |
| CTRL-003 | Every model/version inventoried before use | Preventive | Hybrid | §10 | BR-3 | R-08 | Model-inventory gate: `register_model`/`register_model_version` create a `model` + immutable `model_version` (ENT-035) with `MODEL.REGISTER`/`MODEL.VERSION` audit. **EXECUTABLE in P3-1 (2026-06-30):** the first model-driven run (`run_sensitivities`, ENT-028) calls **`assert_registered_model_version` in the PRE-CREATE gate** — an unregistered `model_version_id` raises `UnregisteredModelError` BEFORE the run is created ⇒ zero run/result/audit (no risk number escapes the inventory). | Inventory entry (ENT-035) + `MODEL.REGISTER`/`MODEL.VERSION` events + BR-3 negative test (`test_unregistered_model_version_refused_pre_create_zero_run_zero_rows`) | **Operational (P3-1: load-bearing at the first model-driven run)** **ENFORCEMENT SHAPE CHANGED at SCH-2 (Wave-13 slice 0, 2026-07-27; trace added at the RM-1 drift-fix sweep — the SCH-2 closeout promised this trace and skipped it, the exact silent skip OQ-W12C-3c exists to prevent):** `schedule.model_version_id` relaxed to NULLABLE for the model-less `EXPOSURE_AGGREGATE` family, with the lost NOT-NULL enforcement replaced by THREE declaration-gated layers — a total-enumeration DB CHECK (`ck_schedule_model_version_by_family`), `_validate_config` in both directions, and the CAD-1 FK guard gated on `FAMILY_REGISTRY[...].requires_model_version` (NEVER on the supplied value — a truthiness gate would let a VAR schedule created with `None` skip inventory-before-use entirely, the LIM-1 fail-open class) — plus a dispatch-time pre-create refusal for a VAR row reaching dispatch unbound. Each layer carries an EXECUTED negative control (the CHECK dropped in a rolled-back transaction; the mutation-tested service gates). **INSTANTIATED (no mechanism change) at RM-1 (2026-07-27):** the 24th registered code `perf.rolling_risk` binds at run time via `assert_model_version_of`, with the window domain `{12, 36}` part of the registered identity and enforced pre-create. **INSTANTIATED AGAIN at SR-1 (Wave-13 slice 2, 2026-07-28):** `perf.sharpe` v1 is registered and the binder calls `assert_model_version_of` PRE-CREATE with the expected model CODE, so a `model_version` of the wrong family (e.g. RM-1's) refuses before `create_run` — zero run, zero rows, zero audit. The registered **window domain `{12, 36}` is also enforced pre-create on day one** rather than after a review, closing the gap RM-1's own 4-finder pass found (a `(24,)` request had minted a COMPLETED governed run outside the ratified domain under a model whose assumptions present that domain as the compliance point). *(An SR-1 note about CTRL-002 previously sat here, filed under the wrong control's row; RELOCATED to CTRL-002's own row at the Wave-13 close.)* **CAL-1b (2026-08-01): the inventory-before-use gate now also carries the CONVENTION identity — a v2 bind parses the registered `month_end_convention=`/`holiday_calendar=` literals fail-closed (absent ⇒ the v1 grandfather; ambiguous/stray ⇒ refused), and the schedule config gains the kind-gated `ck_schedule_calendar_id_by_cadence` sibling CHECK (migration 0059) with its `_validate_config` mirror. `ck_schedule_model_version_by_family` itself is UNCHANGED (no new family).** |
| CTRL-004 | Every field defined in data dictionary | Preventive | Manual | §11 | BR-4 | R-05 | Dictionary review | Data dictionary | Planned |
| CTRL-005 | Data-changing actions emit audit events | Detective | Automated | §11/12 | BR-5, BR-12 | R-07 | Audit completeness test; **data_source create/update emit `DATA.SOURCE_REGISTER`/`DATA.SOURCE_UPDATE`** (P1A-1); **model/model_version create emit `MODEL.REGISTER`/`MODEL.VERSION`** (P1A-2); **dq rule create/update emit `DATA.DQ_RULE_DEFINE`/`DATA.DQ_RULE_UPDATE`; result capture emits `DATA.VALIDATE`** (P1A-3) | Audit events (ENT-045) incl. `data_source`, `model`/`model_version`, `data_quality_rule`/`data_quality_result` | Implemented (1E + P1A-1 + P1A-2 + P1A-3, partial: calc-run + entitlement + data_source + model + dq) |
| CTRL-006 | Risk results bind full lineage (source→run) | Preventive | Automated | §4–8 | BR-6, BR-13 | R-05 | Lineage completeness test (skeleton): a recorded governed output has a complete `source→target` path retrievable by id (`record_lineage` + `GET /lineage/edges/{id}`); full source→run→result completes when calc runs exist (P2+) | Lineage edges, retrieval test | Designed (skeleton, P1A-1) |
| CTRL-007 | Manual overrides carry BR-7 fields + approval | Preventive | Automated | §11 | BR-7 | R-07 | Override schema validation | `OVERRIDE.*` events | Planned |
| CTRL-008 | Scenarios versioned with saved assumptions | Preventive | Automated | §8 | BR-8 | R-06 | Scenario version test | Scenario versions (ENT-029) | Planned |
| CTRL-009 | Reports reproducible from bound runs | Detective | Automated | §12 | BR-9 | R-09 | **RPT-1 2026-08-05 — the regeneration test now EXISTS and RUNS, at two tiers.** (a) Unit/e2e: `test_report_generation.py` generates a report over real governed rows, then regenerates from the PINNED `REPORT_INPUT` snapshot alone and requires the SHA-256 over the rendered bytes to match the hash stored at generation — with the discriminating negative control (mutate the BOUND run's source rows; the report must be unchanged), because the obvious supersession test was proven by mutation N10 NOT to distinguish a renderer that re-reads live tables. (b) Deployed stack, in CI: `infra/deploy/prove_report_identity.sh` generates, `pg_dump`s, `DROP SCHEMA CASCADE`s, restores, and regenerates in a FRESH interpreter — comparing against a hash captured before the archive existed, since the hash stored on the row travelled through the backup too. Its NEGATIVE arm requires a one-character hash change to be refused, so the comparison is proven load-bearing rather than decorative. | Report version + run ids; ENT-072 `report_generation` (IA append-only, symmetric FORCE RLS, migration `0063`); **RE-CITED at the Wave-16 close (2026-08-08, finding 10 — the previous citation was stale in two independent ways).** CI run `31273847072`, `stack-proof` step *"Prove a governed report regenerates identically after a restore (I2) + serves over HTTP (I6)"* → `success` on head `00993e1b`. **The citation it replaces, kept visible because the failure mode is the point:** run `31048172308` (head `9bd5423`) named a step title *"…after a restore (I2)"* that **no longer exists in the workflow file** — the step was renamed when I6 was added, so an auditor following the reference would find no such step and no way to tell whether it had been renamed or deleted; and **660 lines of production change had landed since** in exactly the code that step exercises (`git diff --stat 9bd54235 origin/main` over `report/`, `deploy/` and `prove_report_identity.sh`: 642 insertions, 18 deletions across six files). The control is substantively fine — the renamed step is green on main's own run — but the *record* would have sent an auditor to the wrong place, which is the whole class P16 exists to close and which fired here on a row P16 was written FOR. | **Implemented (RPT-1 2026-08-05, OBSERVED).** Status moved on executed evidence only: the CI step above ran green on the deployed stack, and eleven mutations (N1–N11) were each killed by their intended test. NOT *Operational* — that would claim the control runs against production reports on a schedule, and no report is scheduled (scheduling is explicitly OUT of the RPT-1 remit; CTRL-018/TR-13 remains that territory). **EXTENDED at REPRO-1 (2026-08-07), and deliberately NOT moved to Operational.** The REPORT family is one of the three registered reproducers, so `regenerate_report` is now invoked by a SCHEDULED sweep rather than only on demand — proven on the deployed stack in the CI step named on the CTRL-018 row, both arms. What still separates this from *Operational* is stated plainly rather than glossed: the schedule that drives it is created by a proof harness, not by any production cadence, and no production deployment exists. The honest reading is that CTRL-009's reproduction check now has a scheduled INVOCATION PATH that has been observed working end to end; the remaining gap is deployment, not code. **Wave-18 note (close 2026-08-17, ledger-3 sweep): STRUCT-3 edited the cited harness itself (`report_identity_proof.py` — its raw Portfolio writer now appends the ENT-076 history row co-transactionally), so the standing citation is re-taken: CI run `32056226029`, `stack-proof` → `success` on head `7dcb3a3`, which post-dates every Wave-18 change to the modules the step exercises. Status UNCHANGED.** |
| CTRL-010 | No secrets in source | Preventive | Automated | §13 | BR-10 | R-12 | Secret-scan hook | Scan results | Planned |
| CTRL-011 | No module bypasses entitlement framework; tenant isolation enforced end-to-end | Preventive | Automated | §13 | BR-11, BR-17 | R-07 | Deny-by-default tests (app) + **per-session tenant-context wiring** (`set_config` + durable pool RESET) + PG RLS tests **under a constrained non-superuser role** (visibility, fail-closed w/ SQLSTATE 42501, mismatch, isolation, recycle) + BYPASSRLS-ops-role restricted to cross-tenant ops | Entitlement + tenant-context tests, RLS migration, ops-role migration | Implemented (1E + P1A-0) |
| CTRL-012 | No module bypasses audit framework | Preventive | Automated | All | BR-12 | R-07 | Unaudited-write detection | Audit coverage report | Planned |
| CTRL-013 | No module bypasses lineage framework | Preventive | Automated | §11 | BR-13 | R-05 | BX-LIN enforcement test: a governed write lacking a lineage edge fails `assert_has_lineage` (skeleton; "governed output" stubbed via a synthetic target until domains exist) | Lineage coverage test | Designed (skeleton, P1A-1) |
| CTRL-014 | Limitations explicitly documented | Detective | Hybrid | All | BR-14 | R-10 | Limitations register review + structured capture (skeleton, P1A-2): a `model_version` records `model_limitation` rows (ENT-036, IA) retrievable from the inventory, folded into `MODEL.VERSION` audit | Limitations register; `model_limitation` rows (ENT-036) | Designed (skeleton, P1A-2) |
| CTRL-015 | Human approval gate for restricted change types | Preventive | Manual | §10/11/13 | BR-15 | H-02/H-03/H-05/H-06/H-10 | Approval-record check | `approval_ref` records | Planned |
| CTRL-016 | Material AI agent actions logged | Detective | Automated | All | BR-16 | R-07 | Agent-event completeness test | `AGENT.*` events | Planned |
| CTRL-017 | Temporal-class declared + append-only immutability of audit | Preventive | Automated | §11/12 | BR-6, BR-9, BR-19 | R-05 | Temporal-class + append-only/no-overwrite tests (audit IA; lineage_edge IA P1A-1; model EV + model_version/assumption/limitation IA P1A-2, ORM + DB trigger; data_quality_rule EV + data_quality_result IA P1A-3) | Append-only test, temporal test | Implemented (1E + P1A-1 + P1A-2 + P1A-3, audit/lineage/model/dq; **FR first exercised P1B-3** — `instrument_terms` bitemporal reconstruct-as-of; **EVT-143 `REFERENCE.STATUS_CHANGE` first exercised P1B-4** — corporate_action status lifecycle; **REF-1 2026-07-29 adds the classification family** — ENT-066/067 EV + ENT-068 FR bitemporal (`classification_assignment`), whose supersede/correct protocol is temporal-class-declared and whose prior versions are proven byte-stable on PostgreSQL, the property a governed concentration run's snapshot verification depends on) |
| CTRL-018 | Reproduction test re-runs historical runs | Detective | Automated | §4–8 | BR-6, BR-9 | R-09 | Scheduled reproduction job (TR-13); ENT-073 `reproduction_check` (IA append-only, symmetric FORCE RLS, migration `0065`) | Reproduction verdict rows + the sweep's own `REPRODUCTION` `calculation_run`; CI run **`31516309378`**, `stack-proof` step *"Prove a scheduled reproduction detects a planted divergence (CTRL-018)"* → **`success`** on head **`7114ab0`** (Wave-17 close fold) | **Implemented (REPRO-1 2026-08-07, OBSERVED).** After FOUR citations-without-host, the control has code. A nightly per-tenant sweep rides the existing scheduler as a third schedulable family, takes the most recent COMPLETED run per registered family, **re-executes that run's binder over the run's OWN pinned snapshot inside a nested transaction that is always rolled back**, and records a verdict. Status moved on EXECUTED evidence only, at the CTRL-009 bar: the CI step named in the evidence column ran green on the deployed stack, and it carries **both arms** — a real worker tick re-derived a governed report and reported MATCH, then a PLANTED divergence made the same machinery report DIVERGED and raise an alarm that was DELIVERED (`ALARM_OUTCOMES=SENT`, not the no-recipient sentinel) — P9: the refusal was made to FIRE before the control moved. **The earlier run `31204168002` (head `5fafd00`) is deliberately NOT the citation, and the reason is the point: at that commit the proof's alarm arm was satisfied by a no-recipient SUPPRESSED sentinel — the delivery path had never executed — and its trigger arm counted `pg_trigger` rows, which exist for DISABLED triggers. Both were found at the adversarial review and fixed in the fold, so the only run that evidences what this row claims is the one over the POST-fold harness. **And the correction was itself superseded:** the run cited next (`31210543828`, head `ef899a6`) was left behind by two further folds that changed every module its step exercises, so this citation has now been re-taken TWICE. That recurrence is why it is governed by a rule with a trigger moment (P16) rather than by care — before the PR, `git diff --name-only <cited-sha>..HEAD` must name no production file, in all four documents that carry the citation.** The mutation battery is re-run at each fold and its outcome is recorded in the slice record, deliberately in ONE place — **and this row then restated it anyway, wrongly, in the same sentence** (Wave-16 close, finding 8): it quoted "11/11 at the close", which is the FOURTH fold's figure, two folds behind the merged code, while the slice record's headline quoted an unreconstructable "14/14" that has since been retracted. The number is not repeated here. See `repro_1_slice_record.md` §2, and re-run it yourself: `python scripts/mutation_battery.py --group <slice>`, the committed harness that finding 7 required (four batteries were cited across Wave 16 with no artifact in the repository at all). The wording is bounded on purpose: this row said "every mutation against the new controls was killed by its intended test" for one commit, and that present-tense universal was falsified in minutes at that same commit (five mutants survived, two of them against controls the fold had just added). A mutation battery is a list of mutants that were run, never a property of the codebase. **Coverage is PARTIAL and ENUMERATED, stated here in the same sentence as the status so the reach is not overclaimed:** **NINETEEN families are registered and TWO are pinned UNREGISTERED** (REPRO-2 part 2, 2026-08-10 — this row read "three registered, eighteen unregistered" until the Wave-17 close on 2026-08-11, sixteen adapters after it stopped being true; see the amendment note at the end of this cell), with a census asserting the union equals the whole run-type vocabulary by exact set equality. The two remaining exclusions are the substantive ones, not pending work. Two of those exclusions are substantive rather than pending work: **CONCENTRATION cannot be reproduced at all** (its binder takes no `snapshot_id` and re-pins CURRENT-HEAD classification assignments, so any legitimate classification edit would read as a divergence in the numbers), and **LIQUIDITY has a wall clock inside its compute** (`datetime.now(UTC) - oldest_assignment_at` against the model's declared `tier_max_age_days`), so a run that was fresh when it ran legitimately stops reproducing as its ladder ages. NOT *Operational*, and now for ONE reason rather than two: **no production deployment exists.** The second reason this row used to give — "nothing outside the proof harness creates a REPRODUCTION schedule at all (carry (m)); there is no schedule WRITE API" — **was DISCHARGED by REPRO-2 and is struck.** `POST /schedules` plus pause/resume ship on `schedule.manage` and are in the generated OpenAPI contract; the demo campaign registers the demo tenant and creates its nightly schedule through the REAL `create_schedule` service (stage 24); and `prove_reproduction.sh` creates a second tenant over HTTP whose schedule the supervisor fires with nothing naming it. *An assessor reading this row between 2026-08-10 and 2026-08-11 was told a shipped route did not exist.* **The evidence citation was re-taken in the same fold and this is the THIRD re-take** (`e7ae526` was left behind by 51 production files, and the two before it by earlier folds): CI run `31516309378`, `stack-proof` green on head `7114ab0`, with only records-only commits after it — which is exactly what P16's PR-boundary test requires. A citation that has needed re-taking three times is governed by a trigger moment rather than by care, and this row is why that rule exists. The deployment gap is real and is stated alone, which is what the earlier two-reason wording was guarding against — a reader must not infer that deploying is sufficient for the OTHER reason, because that reason is gone. `requirements_backbone.md`'s BX-REPRO row and the RTM's closing paragraph state it the same way, and CTRL-009 (nine rows above) already did. **A sweep that checks NOTHING now fails CLOSED** (a FAILED run carrying the reason), because the first deployed run produced a perfectly green tick over a tenant whose subjects it could not see. **So does a sweep that could not check EVERYTHING** — a per-family DATABASE failure (lock timeout, statement timeout, half-applied migration) is a ratified, distinct, NON-alarming disposition (2026-08-07): the family is named in the FAILED run's reason and carried onto the `scheduled_run` ledger, and it deliberately does NOT mint a verdict row or page the risk desk, because an unreadable database is a claim about the database and a divergence alarm is a claim about a run. That fix arrived with a lesson worth the row: the guards that make it possible had to be SAVEPOINTED, not merely wrapped in `except` — on PostgreSQL a caught DBAPI error leaves the transaction aborted, so the earlier bare catch produced a correct verdict and then died on the next statement, losing the night, with the fail-closed write itself unreachable. Three scrutiny stages read that code while its unit test stayed green, because SQLite does not poison a session and the test raised a plain `RuntimeError`. **Prior non-movements, retained for the record: (CON-1 2026-07-30: NO CONTROL MOVED — stated explicitly, per the P1 seventh-ledger rule that a mapped control must be extended or explicitly dispositioned. REQ-CRD-003 maps here as well as to CTRL-002, and CON-1 does ship reproduction machinery for its family — the `CONCENTRATION_INPUT` snapshot's four pinned shapes each carry a `_reresolve_content` branch, with the code-first closure branch proven through `verify_snapshot` — but that is PER-SNAPSHOT verification on demand, not the SCHEDULED job re-running historical runs that this control describes. It stays Planned.) (LIM-2 2026-08-01: NO CONTROL MOVED — the slice extends LIM-1's OPERATING limit machinery to the concentration family and adds the REFUSED/staleness/drift health signals, but the SCHEDULED reproduction job this row describes remains absent.)**  **LQ-1 (2026-08-02): NO CONTROL MOVED, dispositioned explicitly.** REQ-LIQ-001 maps to CTRL-002/018, so silence here would be an omission the seven-ledger sweep exists to catch. LQ-1 adds the 24th governed family (ENT-071) to the population CTRL-018 will re-run, and that family is reproducible BY CONSTRUCTION — run-bound, snapshot-gated and model-bound with all three FKs NOT NULL at the DB, its tier heads pinned as CLASSIFICATION components, and its methodology parse-back-enforced. But the SCHEDULED reproduction job itself is still Planned, and LQ-1 does not build it: this is the THIRD consecutive slice to record a non-movement, which is itself the signal that the control needs a slice rather than another citation. CTRL-002 (model governance) is EXERCISED, not moved — `risk.liquidity_tiers` v1 is registered with eight declared assumptions and an INITIAL validation recorded against the flagship run. **(Wave-15 close §7-A, user-ratified 2026-08-07: after FOUR recorded citations-without-host — CON-1, PERF-0, LQ-1's third-non-movement signal, and RPT-1's CTRL-009 row — this control now has a HOST: slice REPRO-1, Wave 16, dated at that wave's planning gate. A scheduled job re-runs historical runs and diffs the results; CTRL-018 moves Planned → Implemented only on the first OBSERVED scheduled green, per the CTRL-009 evidence bar. Citation-without-host is no longer an available disposition for this row.)** **ALERT-1 (2026-08-09) did NOT move this row — deliberately** (the status move belongs to REPRO-2, which makes the control STARTABLE by a deployed tenant). What ALERT-1 adds is the control's own OBSERVABILITY: `GET /reproduction/alarm-health` and the `/ops/alerting` panel recompute, per tenant, whether the sweep is running at all (`sweep_overdue` — the absence signal every earlier field was blind to), whether a judged divergence was lost (`lost_verdicts`), whether the channel is delivering anything (`dead_channel`), and how many verdicts the retry bound silenced (`exhausted_verdicts`). Before it, a control that had stopped running entirely was indistinguishable from a quiet night. Evidence: `test_alarm_health.py` (25 tests, every red/amber field made to fire with its twin), the `alert-1` mutant group (15/15), and `prove_reproduction.sh` arm 5 reading the health over HTTP on the deployed stack with its 403/401 twins. **EXTENDED at Wave 18 (STRUCT-1..4, close 2026-08-17) — status UNCHANGED, three touchpoints to the control's own machinery recorded per the seventh-ledger rule:** (1) STRUCT-1 moved `exposure_type` from the COMPARED set into the comparison KEY (`_EXPOSURE_KEY`) — without it the adapter would pair a stored NOTIONAL row against a recomputed MARKET_VALUE row; (2) STRUCT-3 made the exposure recompute node-scoped with a legacy-NULL branch (the adapter replays the ORIGINAL run's stored scope), and the Wave-18 close hardened the legacy branch to refuse a non-tenant-visible stamp (the V-008 shape, fold K24); (3) STRUCT-4 minted the SYMMETRIC reporting-currency conflict refusal expressly so no legitimately-built run is refusable by its own reproduction (the review's BLOCKING C9 — an ASYMMETRIC refusal minted irreproducible runs), and the consume path now resolves the pinned declaration chain instead of silently defaulting USD (the adapter's stored-base read is unaffected and remains the pin). **FOURTH citation re-take (P16, the trigger this row predicts):** CI run `32056226029`, `stack-proof` job `95466894266` → `success` on head `7dcb3a3` (the Wave-18 close-eve main; `git diff --name-only 7114ab0..7dcb3a3` names 105 production files, so the Wave-17 citation was stale in exactly the P16 sense). |
| CTRL-019 | Decimal-safe money; no float for currency | Preventive | Automated | §4–8 | BR-2 | R-06 | Static/type checks (QS-01) | Lint/test results | Planned |
| CTRL-020 | Deterministic seeds recorded for MC | Preventive | Automated | §4/6/8 | BR-6 | R-06 | Seed-binding test (QS-18) | Run parameters | Planned |
| CTRL-021 | SoD pairs enforced (maker≠checker) | Preventive | Automated | §13 | BR-7, BR-15 | R-07 | SoD conflict test (SOD-01…08) | SoD test results | **Operational (MG-2/MG-3, Wave 11, 2026-07-23/24):** the platform's FIRST **person-level** maker≠checker SoD (beyond the role-partition of VW-1/SOD-03). MG-2: a breach reviewer/closer ∉ the SET of all prior 1L responders (SOD-02, `limit/lifecycle.py`), enforced under a per-breach `FOR UPDATE` lock. MG-3: a limit approver ∉ the maker SET `{created_by, updated_by}` (author AND last editor, `limit/service.py::approve_limit`); a material change to a live limit re-enters the gate. Both refuse a non-human actor (BR-15). Pinned by `test_entitlement_bootstrap.py` + the breach/limit SoD tests. **REACHABLE over HTTP at API-2 (Wave-12 slice 1, 2026-07-24; leg recorded at the Wave-12 close sweep):** `POST /limits` (born DRAFT) → `POST /limits/{id}/approve` gated on the DISTINCT `limit.approve` permission; the person-level refusal survives the boundary (maker self-approve → **409** separation-of-duties, distinct from the 403 role gate; the actor id canonicalized in the ACTOR DATACLASSES so stamp==compare — MG-3's MED-3 paid at the auth boundary); a smuggled `status` on create/PATCH is a loud 422 (`extra="forbid"`); a material PATCH to a live limit auto-demotes to DRAFT for re-approval. **VISIBLE + OPERABLE from the operations UI at OPS-1 (Wave-12 slice 4, 2026-07-26):** `approveLimit` requires a real `approval_ref` (no fabricated sign-off evidence — the disabled-button fold), and the SoD refusal is REACHABLE in the demo (a shared 2L role holds `limit.manage`+`limit.approve`, so only the person-level gate refuses — `test_demo_stage9zzzzz_ops_pg.py`); the Refusal view explains each 409 cause with its opposite remedy. **Wire-detail contract pinned at the Wave-12 close** (`test_limits_endpoint.py::test_the_limit_refusal_detail_strings_are_a_pinned_contract` — exact `detail` strings asserted unconditionally, both routers). Residual: general SOD-01…08 conflict matrix + the entitlement-change maker-checker (CTRL-025) still Planned |
| CTRL-022 | Independent model validation (dev≠validator) | Preventive | Manual | §10 | BR-15 | H-02 | Validation independence review | Validation record (ENT-037) | **Operational (VW-1, 2026-07-14):** ENT-037 `model_validation` REALIZED; the `model.validate` write is 2L-only (`risk_manager_2l`), role-level SoD from the `risk_analyst_1l` register-holder (SOD-03); a latest-outcome REJECTED refuses new governed runs; `MODEL.VALIDATE` audited. **ADVANCED at MG-1 (2026-07-15):** the bind gate now refuses BOTH a latest-outcome **REJECTED** (VW-1) AND an **EXPIRED use-before-validation EXCEPTION** (a `validation_type="EXCEPTION"` row past its `next_review_due` expiry — `ExpiredModelExceptionError` → 422, mapped at every run endpoint); an approving outcome's `next_review_due` is **ceiling-bounded at record time** by the ratified tier cadence ({365/730/1095 days}; untiered ⇒ TIER_1 bound; TIER_2/3 HOUSE POLICY). Recorded asymmetry: still **NO overdue-revalidation refusal** — overdue stays display-only (deliberate; MG-2 candidate). Residual (not v1): data-level dev≠validator (MG-04, no per-version developer stamp) + the Tier-1 H-02 approval leg (rides the approval workflow; tiering itself SHIPPED at MG-1, REQ-MDG-002 In-Progress) |
| CTRL-023 | Data classification + MNPI barriers enforced | Preventive | Hybrid | §3/13 | BR-11 | R-07/H-05 | Barrier access tests | Denied-access audit | Planned |
| CTRL-024 | Export controls (DC-4 blocked by default) | Preventive | Automated | §13 | BR-11 | R-07 | Export-control test | `EXPORT.*` events | Planned |
| CTRL-025 | Entitlement changes maker-checked + audited | Preventive | Automated | §13 | BR-7, BR-11 | R-07 | Four-eyes lifecycle tests: born-PENDING at ≥1 other admin (the B3 threshold is its own named test + mutant), self-approval refused by canonicalized principal id incl. the uppercase-uuid vector, approval = APPENDED resolution row (the PG trigger refused the mutating first build), deactivation of a CURRENT admin rides the flow while a non-admin deactivation stays DIRECT (review-fold scope fix, pinned) | `ROLE.GRANT_REQUEST`/`ROLE.GRANT_APPROVE` events (NOT `ENTITLEMENT.*` — that placeholder was stale the day it was written, the OPS-H1 class; the mint record is the taxonomy row), `direct_grant=true` on every bootstrap-window act | **Implemented (ONBOARD-1b, 2026-08-09, ratified OQ-ONB-9A: refusals FIRED at unit + PG + endpoint tiers and in `prove_onboarding.sh` arm 5 before this row moved — the P9 bar. NOT Operational: no production tenant exercises the flow yet.)** |
| CTRL-026 | Audit store append-only + hash-chain integrity + concurrency-safe + verifiable | Preventive/Detective | Automated | §12 | BR-12, BR-18 | R-12 | Hash-chain verify + tamper-detection + append-only (app + PG trigger); per-tenant advisory-lock concurrency test (PG, gapless under N threads); audit-verify ops CLI | Audit tests, concurrency test, `audit_verify` CLI, migration trigger | Implemented (1E + P0.5) |
| CTRL-027 | Data quality rules run on ingest | Detective | Automated | §11 | BR-5 | R-05 | **Realized on a real ingest path (P1A-4):** `POST /ingest/upload` runs active generic staging rules over staged rows via `run_quality_check` and gates with `assert_passed_quality_checks` (ERROR rejects the batch, WARNING flags) — DQ-rule engine skeleton from P1A-3 (pluggable `DQRule.evaluate()`, `data_quality_result` ENT-039 IA + `DATA.VALIDATE`) | DQ results (ENT-039, `ingestion_batch_id` populated) + `DATA.VALIDATE` events + on-ingest no-silent-failure test (`test_ingest_endpoint`/`test_ingestion`) | Implemented (on-ingest, P1A-4) |
| CTRL-028 | Reconciliation of sources | Detective | Hybrid | §11 | BR-6 | R-05 | Recon job | Recon results (ENT-040) | Planned |
| CTRL-029 | Stale/missing data flagged, not silently filled | Preventive | Automated | §3/4–8 | BR-2, BR-14 | R-06 | Missing-data handling test (QS-15/16); **no-silent-failure framework** (P1A-3, QS-06): a failing/errored DQ rule surfaces as a raised exception or a persisted flagged `data_quality_result` and is audited (`DATA.VALIDATE`, `outcome='failure'`) — never silently passes/swallowed | Flagged records; `data_quality_result` rows + `DATA.VALIDATE` failure events | Designed (skeleton, P1A-3); **first reference-domain exercise P1B-3** — `resolve_identifier` returns one / `None` / `AmbiguousIdentifier`, never a silent arbitrary match (REQ-SMR-003 / OD-P1B-G) **Materially exercised at Wave 18 (STRUCT-4/DP-11, close 2026-08-17): the platform's oldest silent FILL — an undeclared reporting currency silently becoming USD on both exposure paths — was KILLED (refusals `UndeclaredReportingCurrencyError`/`ReportingCurrencyConflictError`, both fired by tests and over HTTP; migration `0073` states the declaration on legacy roots instead of defaulting it), and the read-time translation surfaces `missing-fx` HONESTLY rather than fabricating 1.0. Status UNCHANGED (Designed) — the row's framework claim is broader than any one family.** |
| CTRL-030 | Production change approval (release gate) | Preventive | Manual | §13 | BR-15 | H-10 | Release-readiness review | Go/no-go record | Planned |
| CTRL-031 | Breach workflow enforces 1L/2L separation | Preventive | Hybrid | §9 | BR-7 | R-01/H-01 | Workflow state test (SOD-02) | `BREACH.*` events | **Operational (MG-2, Wave 11, 2026-07-23):** the DEP-WFL breach remediation state machine `DETECTED→ASSIGNED→RESPONDED(1L)→REVIEWED(2L)→CLOSED`+`ESCALATED` over ENT-034 `breach_action` (IA append-only). `breach.respond` (1L=`risk_analyst_1l`) and `breach.review` (2L=`risk_manager_2l`) are NEVER co-granted to a non-admin role (the role partition, first line); the runtime backstop refuses a reviewer/closer ∈ the set of prior 1L responders (SOD-02, "1L cannot approve own closure"), and refuses a review with no prior 1L response (no vacuous SoD). `BREACH.ASSIGN/.1L_RESPONSE/.2L_REVIEW/.ESCALATE/.CLOSE` audited. **REACHABLE over HTTP at API-2b (Wave-12 slice 1b, 2026-07-25):** `POST /breaches/{id}/assign|respond|review|close` (gated `breach.review`/`breach.respond`), the person-level SoD proven to survive the boundary (self-review → **409**, distinct from the 403 role gate; the PG-tier case-variance self-review defeat refused end-to-end); the review guard tightened to require a CURRENT-epoch 1L response (an escalated rejected response can no longer be ACCEPTed — the epoch-blind vacuous-ratification hole). Tests: `test_breach_lifecycle.py`/`_pg.py` + `test_breaches_endpoint.py`. **ALERTING LEG at NOTIF-1 (Wave-12 slice 2, 2026-07-25) — "detect" becomes "monitor" (SR 11-7 ongoing-monitoring / BCBS 239 timeliness):** a per-tenant in-tick phase-4 consumer turns each `BREACH.DETECT`/`BREACH.ESCALATE` audit event into a durable IA `breach_notification` (ENT-063) attempt row per recipient (the in-tenant `breach.review` holders) + a hash-chained `NOTIFY.DISPATCH` (EVT-270) ledger entry — provable proof-of-alert. Fail-CLOSED: a non-dedup failure stops the batch (no silent skip); a no-recipient event records a SUPPRESSED sentinel. v1 sink = record-first `LOG` (a real EMAIL/WEBHOOK channel is a provisioning-coupled v2). Tests: `test_notification.py`/`_pg.py`. **ACTUALLY RUNNING at CAD-1 (Wave-12 slice 3, 2026-07-25) — the control moves from INVOCABLE to OPERATING:** until now every leg above (detection, deadline escalation, alerting) existed only as a tick that nothing invoked on a cadence — the shipped worker ran a heartbeat placeholder, so the *detective* control had no operating frequency. An in-process supervisor (`irp_worker/supervisor.py`) now drives the per-tenant tick every `IRP_TICK_INTERVAL_SECONDS` over the configured `IRP_TENANT_IDS`, with per-tenant fault isolation, and an EMPTY tenant list **fails closed at startup** (a silently-idle engine — a detective control that never fires — is refused, not tolerated); a persistently-failing tenant escalates to a WARNING after 3 consecutive failed ticks (the supervisor level records no durable FAILED evidence, unlike dispatch). Every tick still runs in ONE tenant's non-BYPASSRLS forced-RLS session with the tenant id canonicalized before the RLS GUC is armed (a non-canonical id would have silently evaluated NOTHING — a fail-open). **CTRL-031's testing cadence (OD-038) is therefore now a configured, evidenced value rather than an open question.** **OPERABLE from the operations UI at OPS-1 (Wave-12 slice 4, 2026-07-26; leg recorded at the Wave-12 close sweep):** the breach queue/detail views drive respond/review/close through the single audited FE write surface (`writes.ts` — every verb REQUIRES the `expected_seq` token, so a UI write is never unconditioned), refusals are classified and explained with cause-specific remedies (the Refusal view), and an empty queue makes NO all-clear claim (four reachable not-in-appetite cases — the honest-empty fold). Tests: `apps/worker/tests/test_worker.py` + `apps/frontend/src/views/ops/ops.test.tsx`  **ANNOTATED at REPRO-2 (2026-08-10): the dispatch this control rides is no longer config-driven.** CAD-1's `IRP_TENANT_IDS`-as-the-tenant-set was superseded (ratified OQ-REP2-1) by ENT-074 registry discovery — ACTIVE tenants only, re-read every cycle — so the control now operates for every onboarded tenant automatically rather than for whichever ids an operator hand-listed. The env var survives as an optional restriction filter. The control's SUBSTANCE (1L/2L separation) is untouched; only which tenants it runs for changed, and it changed toward completeness. |
| CTRL-032 | Failed audit capture blocks governed change | Preventive | Automated | All | BR-12 | R-07 | Fail-closed test (AUD-04) | Test results | Planned |
| CTRL-033 | Schema/migration drift gate (models vs migrations) | Preventive | Automated | §11 | BR-4, BR-19 | R-12 | `alembic check` in CI migration job (structural drift) | CI migration job | Implemented (P0.5) |
| CTRL-034 | Vendor/external-dataset onboarding diligence: the checklist (`vendor_onboarding_diligence_checklist.md`) is executed per dataset BEFORE governed use — the control for dataset defects undetectable in-data (the SR-1 uniform-shift class) | Preventive | Manual | §4 | BR-6, BR-14 | R-10 (minted as an R-10 act, H-05 APPROVED at the CAL-1 ratification gate 2026-08-01 — the first CTRL mint since P0.5) | Checklist re-execution per dataset extension; the acceptance censuses it names are test-enforced (`test_the_xnys_dataset_census` + the independent rule re-derivation; DATA-1: `test_the_tb3ms_dataset_census` + the 13-z PG verbatim-row-set pin + the no-derived-return negative pin) | The checklist artifact incl. Execution 1 (the XNYS holiday set), **Execution 2 (the TB3MS series — the first genuinely EXTERNAL dataset, DATA-1 2026-08-02, incl. the H-05-approved item-3 clarifying amendment)** + the rf dating-convention walk-through | **Operational (DATA-1 close, 2026-08-02)** — the APPROVED move (OQ-DATA-1-9) stamped only after the operating evidence was OBSERVED: demo stage 22 executed the checklist's named acceptance censuses against the real TB3MS dataset on the fresh-schema full-PG battery (**2,950 passed / 0 failed**, `PYTEST_EXIT=0`) and again on merged-PR CI (Backend (Python) pass, run 30757083627); the control has now been executed twice against two different datasets (Execution 1 XNYS at CAL-1a, Execution 2 TB3MS here), which is what makes it a repeated practice rather than a one-off artifact. *(Review fold: the draft stamped this at implementation time — reverted; a status is a claim about observed operation, not about code existing.)* **Standing residual DISCHARGED 2026-08-02, re-confirmed 2026-08-03:** Execution 2's item-6 independent re-verification of the 30 TB3MS literals is COMPLETE. The residual had rested on a recorded claim that FRED refuses anonymous access from this environment — that claim was never tested and is FALSE: plain `curl` on the FRED CSV endpoint returns HTTP 200, and all 30 literals verify percent→fraction against the live series with ZERO mismatches (endpoints 2024-01 = 0.0522/5.22, 2026-06 = 0.0366/3.66). One tool's 403 had been recorded as a fact about the world. Standing re-verification path: the FRED CSV endpoint or the Board's H.15 package. This is the grounding evidence for standing rule P12 |
| CTRL-035 | Tenant boundary: unknown/suspended tenants refused at authentication | Preventive | Automated | §6 | BR-11, BR-17 | R-07 | ENT-074 `tenant` registry (migration `0067`, SYSTEM + deployed-tenant backfill with proof-literal exclusions); `assert_tenant_admitted` behind `get_principal`, BOTH auth modes | `test_tenancy_pg.py` (both arms + the SUSPENDED refusal, run as the constrained NOBYPASSRLS role); `prove_onboarding.sh` arm 3a on the deployed stack; mutants N-B1/N-B2 KILLED | **Implemented (ONBOARD-1a 2026-08-09, OBSERVED — the deployed arm executed).** Dialect-gated BY MECHANISM: PostgreSQL executes it, SQLite does not, so the unit tier is structurally outside its reach — stated on the control rather than discovered (the FK-1 lesson). NOT Operational: no production deployment exists. |
| CTRL-036 | Platform-authority containment: `tenant.create` unreachable from any tenant context | Preventive | Automated | §6 | BR-11 | R-07 | The separate `PLATFORM_PERMISSIONS` catalog + never-cloned `platform_operator` (migration `0067` inline delivery); the SYSTEM-router fence (`SYSTEM_TENANT_ALLOWED_PREFIXES`); the P17/P11 gates extended to walk BOTH catalogs | Disjointness census + DB-level no-cloned-role-holds-a-platform-code census (asserted AFTER an onboarding); the fence census over the walked 292-route surface; `prove_onboarding.sh` arms 3b/3c LIVE; mutants N-A1/N-A2/N-C1/N-C2/N-C3 KILLED | **Implemented (ONBOARD-1a 2026-08-09, OBSERVED).** The composition this contains was the design's own first draft: mint → ALL_CODES → platform_admin template → clone = every customer tenant creates tenants; five independent verifier lanes converged on it. Containment is structural (a different constant + a non-template role), and the censuses assert the DATABASE, not the constants. |
| CTRL-037 | Tenant orphan-proofing: a tenant always has ≥1 valid active admin | Preventive | Automated | §6 | BR-11 | R-07 | The OQ-ONB-4A invariant over ALL four paths (revoke, end-date, deactivate incl. self, concurrent — per-tenant advisory lock), each path a NAMED test with its positive twin; the check runs when the act TAKES EFFECT, not at request time (four-eyes can delay an act past the state it was requested under) | `TenantWouldBeOrphaned` refusals + the PG concurrency proof (two real sessions, at-least-one-survivor AND at-most-one-success AND a genuine REFUSAL asserted — an error-only outcome proves nothing about the lock) | **Implemented (ONBOARD-1b, 2026-08-09: all four paths fired at unit tier, the concurrent path fired on real PostgreSQL under the constrained NOBYPASSRLS role; birth-path evidence from 1a carries — a tenant cannot be created without its first admin.)** |
| CTRL-038 | Governed data is recoverable: backups are verified archives, restores are proven by execution, and a damaged archive is refused before the target is touched | Detective | Automated | §14 | BR-9 (primary anchor **NFR-03** RPO ≤ 24h / RTO ≤ 8h, and **AD-010** "DR per NFR-03") | R-09 | The CI `stack-proof` job runs `infra/deploy/prove_backup_restore.sh` on every push: the POSITIVE arm dumps, destroys and restores a deployed database and re-reads the seeded REFERENCE rows (`alembic_version`, `currency`, `calendar_holiday` counts — stated precisely, because the deployed stack the proof runs against holds reference data, NOT governed derived results); the NEGATIVE arm presents a truncated archive and requires it to be REFUSED **before the target is touched**, then re-asserts the target's original counts. **What is and is not mutation-proven, stated exactly:** the stack-proof JOB is proven able to go red — `0c0fdc3` deliberately broke the MIGRATION step (`deploy/prepare.py`, `upgrade(cfg, "head")` → `"base"`; its own commit message says so) and CI run `31023628263` failed at *Deploy to a clean stack*, which **SKIPPED** the backup/restore step. So that run proves the job can fail; it does NOT exercise either backup arm. **The arms themselves are not yet mutation-proven, and that is precondition (5) below.** *(This cell said "both arms mutation-proven" when the row was minted on 2026-08-20 and the adversarial review of the minting commit refuted it by reading the cited commit and the cited run's step list — recorded rather than silently corrected, because a control row asserting its own proof is exactly where this project has been wrong before.)* | `.github/workflows/ci.yml` `stack-proof` job, step ***"Prove backup AND restore — including the refusal"*** (the literal step name at `ci.yml:1000`, copied not paraphrased) → `success` on head **`d886fb8`** (**CI run `32307903831`**, all nine jobs green). **P16 satisfied at birth, by its own mechanical test:** `git diff --name-only d886fb8..HEAD` names records only — no source, no migration, no proof harness. *(The row was minted citing run `32056226029` on head `7dcb3a3`, and the review of the minting commit refuted BOTH halves: the step title cited did not exist anywhere in the workflow file — the identical failure the CTRL-009 row above records as the reason P16 was written — and `git diff --name-only 7dcb3a3..main` names 21 files including `exposure/service.py`, which is one of CTRL-039's own pinned guard modules. A compliant green run existed before the minting commit and was not used.)* Harness committed: `infra/deploy/prove_backup_restore.sh`, `infra/deploy/backup.sh` | **Implemented (OBSERVED) — MINTED 2026-08-20 at the Wave-19 planning gate (H-05 approval act, R-10 drafted; the CTRL-034 mint-as-act precedent).** Deliberately NOT *Operational*, and the four preconditions are named so the gap is not implied: (1) no schedule invokes `backup.sh` — it runs only from proof harnesses, (2) no retention policy exists, (3) no off-host copy exists, (4) no production target exists, **(5) neither backup arm is mutation-proven — the only cited red run broke the migration step and SKIPPED backup/restore entirely (see Test/Assurance)**. **In-row P19 trigger:** the first production-deployment slice moves this row, and all five are re-measured then. Precondition (5) is additionally dischargeable on its own and cheaply — mutate `restore.sh` (for instance delete the `pg_restore --list` pre-check), watch the NEGATIVE arm go red, and cite that run here. **Anchor gap, stated precisely:** recoverability IS anchored in the architecture (NFR-03 quantifies RPO/RTO; AD-010 is Accepted) — what does not exist is a REQUIREMENTS-REGISTER row for it, so a real DR capability (restore drills, retention, off-site) is a requirement-mint decision, not something this control row delivers. |
| CTRL-039 | Governed aggregation obeys a machine-readable per-field contract: non-additive fields refuse aggregation, and an undeclared consumer or an unconsulted aggregation construct fails a mechanical census | Preventive | Automated | §2.1 | BR-6 + BR-9 | R-09 | Two censuses, in TWO files — stated separately because the minting draft put both in one and the review refuted it: **census 1** is `packages/shared-python/tests/test_aggregation_contracts.py::test_census1_contract_keys_equal_the_registry_exactly`, and **census 2** is `packages/shared-python/tests/test_aggregation_census.py::test_census2_*`. Census 1 asserts EXACT SET EQUALITY between the contract declarations and the run-type registry union (minus the ratified RUN_TYPE_REPRODUCTION exclusion, DP-13); census 2 DISCOVERS aggregation constructs mechanically by AST across three source trees and requires each to consult the contract, with result-obedience mutation-proven (flipping a family's operator to NOT_AGGREGATABLE must change behaviour, not merely be present). The refusals fire over HTTP on SHARPE and VAR | `packages/shared-python/src/irp_shared/aggregation/contracts.py` (`AGGREGATION_CONTRACTS` :128, `EMITTED_GRAINS` :308, `NotAggregatableError` :431, `UndeclaredConsumerError`/`ForeignMeasureError`); the mutants targeting `contracts.py` in `scripts/mutants.toml`, which live in the **`struct-1` and `struct-2` groups** — there is no group named for the module, and the minting draft invented one. Green on main `d886fb8`, **CI run `32307903831` on head `d886fb8`** (all nine jobs green; P16 satisfied at birth — `git diff --name-only d886fb8..HEAD` is records-only). *(Minted citing run `32056226029` on head `7dcb3a3` while naming main `d886fb8` in the same sentence — a run id paired to a SHA it did not run on, refuted by the review of the minting commit.)* | **Implemented (OBSERVED) — MINTED 2026-08-20 at the Wave-19 planning gate (H-05 approval act; Wave-18 close finding K8).** Two boundaries stated in-row, because without them the row overclaims: (a) **NodeRollup translated totals sit OUTSIDE the contract vocabulary by design** — the contracts govern row-space aggregation, while the STRUCT-4 translation is per-node scalar conversion with its own evidence fields, so a consumer must not sum translated totals across nodes on this row's authority; (b) **non-root EXECUTION covers 4 of 21 families** (EXPOSURE_AGGREGATE, FACTOR_EXPOSURE, VAR, PORTFOLIO_RETURN); the remainder are covered by the NODE_SCOPES exact-set declaration census plus the mechanical scope-stamp census, per ratified Wave-18 close decision D2, with per-family execution riding each family's next slice. |

## 4. Coverage Note

Every build rule BR-1 … BR-19 is covered by at least one control above. The Step 1C rules map to existing controls: **BR-17**
(tenant isolation) → CTRL-011/CTRL-023; **BR-18** (audit hash-chain integrity) → CTRL-026/CTRL-032; **BR-19** (temporal-class
conformance) → CTRL-017/CTRL-018. **P0.5** additions: reproducible frontend builds → CTRL-001 (`npm ci`); audit-write
concurrency + verification ops CLI → CTRL-026; schema-drift gate → **CTRL-033**; entitlement bootstrap seed (baseline catalog +
role templates) underpins CTRL-011/CTRL-025. **P1A-1** additions: the `data_source` (EV) + `lineage_edge` (IA) skeleton makes
**BX-LIN** executable — `record_lineage`/`assert_has_lineage` + `GET /lineage/edges/{id}` under P1A-0 tenant context move
**CTRL-006/CTRL-013** to Designed (skeleton); `data_source` create/update audit (`DATA.SOURCE_REGISTER`/`DATA.SOURCE_UPDATE`)
extends **CTRL-005**; the new deny-by-default `lineage.source.manage` permission + FORCE-RLS (USING + WITH CHECK) on both tables,
proven under the constrained `irp_app` role, underpin **CTRL-011**. **P1A-2** additions: the `model` (EV) +
`model_version`/`model_assumption`/`model_limitation` (IA) registry skeleton makes **BR-3 model-inventory** executable —
`register_model`/`assert_registered_model_version` + the inventory reads move **CTRL-003** and **CTRL-014** to Designed (skeleton);
`model`/`model_version` create audit (`MODEL.REGISTER`/`MODEL.VERSION`, reusing existing codes) extends **CTRL-005/012/017/032**;
no new permission (`model.inventory.register`→`risk_analyst_1l`/`platform_admin`) or audit code. **CTRL-022** (validation
independence) and **CTRL-015** (approval gate) remain **Planned** — no validation/approval workflow in P1A-2 (governance fields are
non-enforcing placeholders). **P1A-3** additions: the `data_quality_rule` (EV) + `data_quality_result` (IA) skeleton with a
pluggable `DQRule.evaluate()` engine (2 generic rules) and `assert_passed_quality_checks()` gate makes **DEP-DQF / REQ-DQR-001**
executable — moves **CTRL-027** and **CTRL-029** to Designed (skeleton); rule create/update audit (`DATA.DQ_RULE_DEFINE`/
`DATA.DQ_RULE_UPDATE`) and result capture (`DATA.VALIDATE`) extend **CTRL-005/012/017/032**; no new permission
(`dq.rule.manage`→data_steward, `dq.result.view` broad — already bootstrapped, no role change). **CTRL-028** (reconciliation,
REQ-DQR-002/P7) and the manual-override/SoD controls (**CTRL-007/021**, REQ-DQR-003/P7) remain **Planned** — no reconciliation or
override workflow in P1A-3. **P1A-4** additions: generic ingestion staging (`ingestion_batch` ENT-047 IA-status-mutable +
`ingestion_staged_record` ENT-048 IA, REQ-INT-001) makes **CTRL-027** **Implemented (on-ingest)** — `POST /ingest/upload` runs DQ
over staged rows and gates the batch; **CTRL-029** gains first real on-ingest no-silent-failure evidence (a DQ ERROR persists a
REJECTED batch + flagged result + `DATA.INGEST`/`DATA.VALIDATE` audit, never silently rolled back) but stays **Designed**;
**CTRL-013** (lineage no-bypass) is exercised non-synthetically (a `data_source → ingestion_batch` ORIGIN edge + `assert_has_lineage`
on every batch); the `DATA.INGEST` lifecycle (create + per-transition `status_change`) extends **CTRL-005/011/012/017/032**;
anti-corruption file controls realize **THR-05/THR-06** at skeleton level (AV deferred, OD-042). **No new audit code, no new
permission, no role change** (`data.upload` already bootstrapped). **CTRL-028** (reconciliation) and **CTRL-007/021**
(override/SoD) remain **Planned** — no canonical mapping, reconciliation, or override workflow in P1A-4. **P1B-1** additions:
the first reference-data slice (`currency` ENT-005, `calendar`+`calendar_holiday` ENT-006, `rating_scale`+`rating_grade` ENT-007
taxonomy; REQ-SMR-005 + REQ-SMR-004 calendar) and the platform's **first asymmetric hybrid RLS** (AD-013-R1). **CTRL-004**
(data-dictionary field definitions) moves toward Designed via the ISO-4217 `code` shape / MIC / agency / minor_units field
definitions. **CTRL-011** is extended by the five additive reference permissions AND the **first hybrid-RLS evidence**
(own+SYSTEM read; single-tenant `WITH CHECK` write; no-context-global; structural `pg_policies` asymmetry + closed-set tests in
`test_reference_pg`). **CTRL-005/012** gain `REFERENCE.CREATE` (EVT-140) / `REFERENCE.UPDATE` (EVT-141) coverage (children fold
in). **CTRL-017** coverage for these EV entities is the `__temporal_class__ = EFFECTIVE_DATED` declaration test, **NOT**
append-only (EV, not IA). **CTRL-032** gains first reference-CRUD fail-closed evidence (parent + children + lineage edge roll
back together when `record_event` raises). **CTRL-013** is exercised again (one MANUAL-`data_source` ORIGIN edge per reference
entity). **Label note (R-10):** tenant-isolation/RLS evidence attaches to **CTRL-011**, not the mis-seeded **CTRL-003** (whose
matrix label is model-inventory) — flagged, not silently edited. **No new audit framework code** (`record_event` FROZEN),
**no `data_source` hybrid**, **no rating ASSIGNMENTS / `reference.rating.*`**. **P1B-2** additions: the second SMR slice
(`legal_entity` core + `issuer` ENT-002 + `counterparty` ENT-003 1:1 role profiles; REQ-SMR-002) is the **proprietary-never-hybrid
evidence** (the inverse of P1B-1). **CTRL-011** is extended by the additive `reference.legal_entity.*` permissions AND
**symmetric RLS on three proprietary tables** (`USING == WITH CHECK == own-tenant`; no-context read → zero rows; a positive
`pg_policies` symmetric+FORCE-RLS assertion + the unchanged closed-hybrid-set test prove these are NOT hybrid). **CTRL-004** advances
via LEI (ISO-17442) / jurisdiction (ISO-3166) / entity_type field shapes. **CTRL-005/012** gain `REFERENCE.CREATE`/`.UPDATE` on the
three entities (each its **own** event). **CTRL-017** = the EV `__temporal_class__` declaration test. **CTRL-032** gains
per-entity CREATE **and** UPDATE fail-closed evidence (entity + profile + lineage + lazily-created MANUAL source roll back; an
UPDATE's mutated attributes + `record_version` revert). **CTRL-013** = one MANUAL-`data_source` ORIGIN edge per entity. The
proprietary-identity SoD is honoured (`legal_entity.view` excludes `auditor_3l`, matching `issuer`/`counterparty.view`). **No new
audit framework code; no hybrid/SYSTEM_TENANT; no netting/CSA/exposure column or calc.** **P1C-1 (AD-017, REQ-PPM-001,
2026-06-23):** the portfolio-hierarchy slice (migration `0012`) **exercises** **CTRL-001** (tests-before-completion: the SQLite
logic + endpoint + PG suites), **CTRL-004** (`portfolio` columns + `node_type`/`status` vocab field defs), **CTRL-005/012**
(`PORTFOLIO.CREATE`/`.UPDATE` **ACTIVATED** at the EVT-150 block caller-side; `audit/service.py` FROZEN), **CTRL-006/013**
(one MANUAL-`data_source` ORIGIN edge per portfolio create; amend roots none), **CTRL-011** (deny-by-default
`portfolio.view`/`.edit` + symmetric proprietary RLS on `portfolio`, never hybrid; the constrained-`irp_app` PG tests),
**CTRL-017** (EV `__temporal_class__` declaration), and **CTRL-032** (fail-closed audit rollback). **No new CTRL minted.**
`PORTFOLIO.*`, the `data_steward` `portfolio.*` grant, and migration `0012` are minted in **code** in this build (the
P1C-0 ratification only reserved them; `audit/service.py` stays FROZEN). ABAC portfolio scope is **anchored, not enforced**
(enforcement P6+; the descendant resolver records subtree semantics, the endpoints apply no scope filter — a tested fence).
**P1C-2 (REQ-PPM-003 transaction conjunct, AD-017, 2026-06-24):** the `transaction` IA append-only slice (migration `0013`)
is the **first DOMAIN append-only entity** — it exercises **CTRL-017** with new force (the `irp_prevent_mutation` P0001
trigger + the ORM `before_update`/`before_delete` guard, proven by the PG trigger test that grants `irp_app` UPDATE/DELETE so
the rejection is the **P0001 trigger**, not a 42501 privilege denial), **CTRL-012** (no audit bypass — `TRANSACTION.RECORD`/
`.REVERSE` EVT-160/161 on every record), and **CTRL-001/004/005/006/011/032** (tests / data-dict / audit / lineage /
deny-by-default + RLS / fail-closed rollback). New `transaction.view`/`transaction.record` perms (`data_steward` maker;
`auditor_3l` excluded); `audit/service.py` FROZEN. **Capture-only** — corrections are explicit reversal records, no position
derivation / cashflow engine / valuation / exposure calc.
**P1C-3 (REQ-PPM-002, AD-005 §2A, 2026-06-25):** the `position` **FR bitemporal** slice (migration `0014`) is the **first FR
DOMAIN entity** (second FR entity after the P1B-3 `instrument_terms`). It exercises **CTRL-017 with the FR reading**:
temporal-class **declared** (`FULL_REPRODUCIBLE`) ✓, but **append-only immutability does NOT apply** — `position` is **NOT** in
`APPEND_ONLY_TABLES` and has **no** `irp_prevent_mutation` trigger (the FR protocol requires close-out UPDATEs); prior-version
CONTENT immutability is **service-enforced + test-proven** (the PG test asserts a close-out UPDATE returns `rowcount == 1`,
the inversion of the transaction P0001 guard). Also **CTRL-012** (no audit bypass — `POSITION.CREATE`/`.UPDATE`/`.CORRECTION`
EVT-170/171/172 on every governed write) and **CTRL-001/004/005/006/011/032** (tests / data-dict / audit / lineage /
deny-by-default + symmetric RLS / fail-closed rollback). New `position.edit` perm minted + existing `position.view` grant
extended to `data_steward` (maker; `auditor_3l` excluded); `audit/service.py` FROZEN. **Capture-only** — positions are captured
directly, **NOT derived from transactions** (no transaction FK, no derivation engine); no market value / valuation / exposure /
holdings-view / dataset_snapshot.
**P1C-4 (REQ-PPM-003 valuation conjunct, AD-005 §2A / OD-P1C-F, 2026-06-25):** the `valuation` **FR bitemporal** slice
(migration `0015`) is the **second FR DOMAIN entity** (captured marks). It exercises **CTRL-017 with the FR reading**
(temporal-class declared `FULL_REPRODUCIBLE` ✓; append-only does **NOT** apply — `valuation` is **not** in any
`APPEND_ONLY_TABLES`/`irp_prevent_mutation` trigger loop; prior-version content immutability is **service-enforced +
test-proven** via the PG close-out-UPDATE `rowcount==1` proof), **CTRL-012** (no audit bypass — `VALUATION.CREATE`/`.UPDATE`/
`.CORRECTION` EVT-180/181/182 on every governed write), and **CTRL-001/004/005/006/011/032** (tests / data-dict / audit /
lineage / deny-by-default + symmetric RLS / fail-closed rollback). Both `valuation.view`/`valuation.edit` perms newly minted
(`data_steward` maker; `auditor_3l` excluded); `audit/service.py` FROZEN. **Captured marks** — `mark_value` captured (never
computed), `mark_source` an inert label; **NO valuation/pricing model, NO price lookup, NO market-data, NO market-value rollup
(no `position` FK / no `quantity`), NO exposure aggregation, NO holdings view, NO dataset_snapshot**. `valuation_date` is an
immutable logical-key column (OD-P1C-F). This jointly with the P1C-2 transaction conjunct **closes REQ-PPM-003 (Done)**. As
construction phases

**P1C-5 (read half of REQ-PPM-001/002, AD-017 / OD-P1C-A/B/F/G/H, 2026-06-25):** the as-of holdings / portfolio **views** slice
is a **read-only composition** (new `irp_shared/holdings/` read-model package + `GET /portfolios/{id}/holdings`) — it adds
**NO entity, NO migration, NO permission, NO audit event, NO lineage/DQ write** (`migration_head` stays `0015_valuation`). It
exercises **CTRL-001** (tests), **CTRL-004** (response fields in the data dictionary), and **CTRL-011** as its primary control
(deny-by-default `portfolio.view` + `position.view`; `valuation.view` in-handler before any mark lookup; tenant isolation via
inherited symmetric FORCE-RLS on `position`/`portfolio`/`valuation`; no BYPASSRLS). The write-side controls **do NOT apply** to
a pure read: **CTRL-005/012** (audit emit), **CTRL-006/013** (lineage bind/no-bypass), **CTRL-032** (fail-closed audit rollback)
— and a tested **zero-audit-write** assertion proves it. Subtree traversal reuses the bounded cycle-safe descendant resolver as
**read composition, NOT ABAC enforcement** (anchor-not-enforce → P6+). **CTRL-017** applies to the underlying captured FR
entities (already declared), not to the read DTOs. **Capture-only fences (load-bearing scope test):** NO aggregation / sum /
rollup / total / weight, NO `market_value` or `quantity × mark_value`, NO exposure, NO `dataset_snapshot`, NO risk/pricing/
valuation model, NO market-data/price lookup, NO position-from-transaction derivation, NO corporate-action application. Marks
are **display-only** (opt-in, deterministic by an explicit `valuation_date`). **No REQ status change** (read-composition over
already-satisfied capture; REQ-PPM-002 stays In-Progress, ABAC residual → P6+). As construction phases

**P1C-6 (deterministic synthetic dataset, OD-P1C-L REALIZED / AD-017, 2026-06-25):** a **labeled, never-auto-run** synthetic-data
seed (new `irp_shared/synthetic/` package — `build_synthetic_dataset`) that builds a small reproducible demo dataset **through the
governed binders**, so every seeded row **PRESERVES** the production controls: **CTRL-005/012** (each row emits its `*.CREATE`/
`RECORD` audit event; `verify_chain` passes), **CTRL-006/013** (each row roots its MANUAL-source ORIGIN lineage edge;
`assert_has_lineage`), **CTRL-011** (writes only under the reserved SYNTHETIC tenant context — symmetric FORCE-RLS, **never
BYPASSRLS**; a different tenant sees zero synthetic rows), plus **CTRL-001** (the determinism / governed-path / guard tests). **No
control is weakened.** It adds **NO entity, NO migration** (`migration_head` stays `0015_valuation`), **NO HTTP endpoint, NO
permission, NO audit-code** (`audit/service.py` FROZEN); the only shared-package change is a **narrow, keyword-only, default-None
deterministic-injection seam** (`entity_id` / `now`) on the governed binders — **production call sites are byte-for-byte unchanged**
(test-proven). Determinism: `uuid5` ids + an injected fixed seed clock; an **AST fence** forbids `datetime.now`/`utcnow`/`uuid4`/
`new_uuid`/`uuid1`/`random` in the seed module + any multiplication (no `quantity × mark` / market value / exposure — capture-only).
**Never-auto-run** is enforced architecturally (not in migrations/startup) + an explicit confirm-flag + an `IRP_ALLOW_SYNTHETIC_SEED=1`
env gate + a refuse-non-synthetic-tenant guard (all tested). Synthetic-only data (DC-1/DC-2 demo fixtures): `SYNTH_*`/neutral names,
no real vendor/agency/client names, no real ISIN/CUSIP/SEDOL/LEI. **No REQ status change.** As construction phases
open, controls will be split to specific bounded contexts and capabilities and given Test/Evidence detail.

**P2-0 / P2-1 (reproducible input snapshot — `dataset_snapshot`, AD-014; ratified 2026-06-26, REALIZED in P2-1 `3629baa`, migration `0016_dataset_snapshot`):** the
P2 reproducibility primitive (ENT-049 `dataset_snapshot` + ENT-050 `dataset_snapshot_component`) is **ratified into governance**
(`p2_0_decision_record.md` + `p2_1_dataset_snapshot_implementation_plan.md`). Control mapping (activated at the P2-1
implementation): **CTRL-009** (reproducibility — the snapshot
**pins input record versions + a SHA-256 manifest hash** so a later run reproduces; **AD-014: no official derived number is
produced before snapshot/run binding** — P2-1 produces **none**, exposure binds `calculation_run` at P2-3); **CTRL-017** (the
**IA TRUE-append-only** reading — both tables in `APPEND_ONLY_TABLES` + the `irp_prevent_mutation` P0001 trigger + ORM
`before_update`/`before_delete` guard, the `transaction` precedent; **distinct** from the status-mutable `calculation_run`);
**CTRL-006/013** (lineage — a **narrow internal snapshot/component lineage writer** roots one `data_snapshot`-sourced ORIGIN
edge per pinned input; **not** a broad lineage-framework rewrite); **CTRL-011/023** (tenant isolation — **symmetric** FORCE-RLS,
**never hybrid/SYSTEM_TENANT**; **cross-tenant component binding FAILS CLOSED** via the explicit-tenant-predicate resolvers,
whole-unit rollback, on SQLite **and** PG); **CTRL-032** (fail-closed co-transactional governed write — snapshot + audit
(`SNAPSHOT.CREATE`, reserved) + lineage + the **caller-side completeness DQ gate** (reuses `run_quality_check` / `DATA.VALIDATE`,
**no DQ-protocol change**; a coverage gap fails closed and **prevents snapshot creation**)). `audit/service.py` stays **FROZEN**;
`entitlement/bootstrap.py` UNCHANGED. **No control is weakened; no new CTRL minted** (existing controls gain a planned P2-1
binding).

**P2-2 additions (FX market data — `fx_rate`, ENT-024, IMPLEMENTED `0017_fx_rate`, 2026-06-26):** captured vendor FX (FR) +
a pure published-rate `convert` (no analytics). Controls now executable for FX: **CTRL-006/013** (lineage — one VENDOR
`data_source` ORIGIN edge per physical version, reusing `record_lineage` unchanged); **CTRL-011** (deny-by-default
`marketdata.view`/`.ingest` + symmetric FORCE-RLS, PG-proven as `irp_app`; a new **Snapshot/FX symmetric-RLS CI step** + a new
migration); **CTRL-023** (vendor-licensed FX tenant-scoped — NEVER hybrid; closed 5-table hybrid set asserted unchanged);
**CTRL-017** (`fx_rate` declares `FULL_REPRODUCIBLE`; NOT append-only); **CTRL-026** (`MARKET.FX_*` hash-chain `verify_chain`);
**CTRL-032** (fail-closed co-transactional rollback if audit/lineage/DQ raises). The DQ no-silent-failure gate gains an
**additive generic `RANGE` evaluator** (strictly-positive rate) — `(params, dataset)` Protocol **unchanged**, the two shipped
evaluators behavior-unchanged. **CTRL-009 / the AD-014 gate are UNTOUCHED** — FX produces no governed derived number (exposure
stays P2-3, snapshot+run-gated). `audit/service.py` **FROZEN**. **No control weakened; no new CTRL minted.**

**P2-3 ratification (basic exposure + `calculation_run` wiring — `exposure_aggregate` ENT-014, RATIFIED-IN-PLANNING 2026-06-26;
AD-018; REALIZED `da178fc`, migration `0018_exposure_aggregate`):** the platform's **FIRST official governed derived number**, which makes the
**AD-014 / FW-RUN §5 / TR-15 gate LOAD-BEARING** (it had no producer before P2-3). Control bindings (all to **existing** CTRLs —
**no control weakened, no new CTRL minted**): **CTRL-009 becomes EXECUTABLE** (no official derived number without a `dataset_snapshot`
+ a complete `calculation_run` — `input_snapshot_id` non-null + `code_version` + the additive `environment_id`; reproducible from the
snapshot-pinned captured content per TR-09); **CTRL-006/018** (run-binding + lineage — the `dataset_snapshot → calculation_run`
(`DEPENDS_ON`) + `calculation_run → exposure_aggregate` (`ORIGIN`, `run_id`-stamped) edges, via the generalized internal-lineage
writer); **CTRL-013** (no lineage bypass); **CTRL-017** (`exposure_aggregate` declares `IMMUTABLE_APPEND_ONLY`, in
`APPEND_ONLY_TABLES` + P0001 trigger + ORM guard — append-only derived result); **CTRL-011/023** (deny-by-default `exposure.view` /
`exposure.aggregate.run` + symmetric FORCE-RLS, NEVER hybrid; a new **exposure symmetric-RLS CI step**); **CTRL-026** (the run reuses
the `CALC.RUN_CREATE`/`CALC.RUN_STATUS_CHANGE` hash-chain — `verify_chain`); **CTRL-032** (fail-closed: a **pre-create refusal** = ZERO
run/result/audit; a **post-create FAILED** run = committed FAILED run + ZERO result; an emit-path failure rolls the whole unit back).
DQ gates fail closed **before any result write** (snapshot completeness + FX completeness + valuation-mark-required + cross-tenant) —
reusing `run_quality_check`/`DATA.VALIDATE`, the `(params, dataset)` Protocol **unchanged**. **NO-RISK BOUNDARY:** exposure is **signed
market value v1** (`MARKET_VALUE` only) — **NOT** VaR/Expected Shortfall/factor/sensitivity/scenario/stress/P&L/performance; those stay
P3+. The additive `calc/service.py` `outcome` param is the only calc change; `audit/service.py` **FROZEN**.

**P3-2 additions (factor-return inputs — net-new `factor` EV definition + `factor_return` FR series, ENT-025, IMPLEMENTED
`0023_factor_return`; committed `402cb12`, CI #89 green):**
captured vendor/external factor returns (FR) + a net-new EV definition — the same captured-market-data control pattern as
`fx_rate`/`price_point`/`curve`/`benchmark` (**no control weakened; no new CTRL minted; the `factor.*` permission space is NOT
minted — `marketdata.view`/`.ingest` REUSED**). Controls now executable for factor: **CTRL-006/013** (lineage — one VENDOR_FACTOR
`data_source` ORIGIN edge per captured version, reusing `record_lineage` unchanged); **CTRL-011** (deny-by-default
`marketdata.view`/`.ingest` + symmetric FORCE-RLS, PG-proven as `irp_app` in `test_factor_pg.py`); **CTRL-023** (vendor-licensed
factor data tenant-scoped — NEVER hybrid; closed 5-table hybrid set asserted unchanged); **CTRL-017** (`factor_return` declares
`FULL_REPRODUCIBLE`, `factor` declares `EFFECTIVE_DATED`; **NEITHER append-only** — no `irp_prevent_mutation` trigger, the `benchmark`
precedent); **CTRL-026** (`REFERENCE.*` definition + `MARKET.FACTOR_RETURN_*` series hash-chain `verify_chain`); **CTRL-032**
(fail-closed co-transactional rollback if audit/lineage/DQ raises). The DQ gate reuses the generic `NOT_NULL` + `RANGE` evaluators
(a `> -1` economic-sanity floor) — `(params, dataset)` Protocol **unchanged**; a **binder-side finiteness guard** rejects NaN/±Inf
BEFORE any write (the min-only RANGE does not catch +Inf). **CTRL-009 / the AD-014 gate are UNTOUCHED** — a captured factor return
is an **INPUT**, not a governed derived number (**NO `calculation_run`, NO `model_version`, NO snapshot pin**); computed factor
returns are DEFERRED (would re-engage CTRL-003/009 with a registered `model_version` — the ENT-028 `sensitivity_result` precedent).
`audit/service.py` **FROZEN**. **No control weakened; no new CTRL minted.**

**P3-3 additions (factor-exposure engine — `factor_exposure_result`, ENT-028 family, IMPLEMENTED
`0024_factor_exposure`):** the SECOND governed derived risk number — indicator-loading allocation exposures over pinned
`exposure_aggregate` atoms × pinned `factor` EV definitions (**no control weakened; no new CTRL minted; NO new permission —
`risk.view`/`risk.run` REUSED**). Controls exercised: **CTRL-003** (inventory-before-use — `assert_registered_model_version`
pre-create on the SECOND model-driven run family, `risk.factor_exposure.allocation` v1; an unregistered version = zero
run/rows/audit, negative-tested); **CTRL-002/014** (the `factor_exposure_allocation_v1.md` methodology doc + mirrored
assumption/limitation rows); **CTRL-009** (governed output — non-null `input_snapshot_id` + complete `calculation_run`;
snapshot-only compute); **CTRL-017** (IA TRUE append-only — `APPEND_ONLY_TABLES` + P0001 trigger + ORM guard, PG-proven);
**CTRL-018/TR-13** (reproduction — re-run over the same snapshot identical; invariant under a later factor amend / exposure
re-run); **CTRL-006/013** (lineage — `snapshot --DEPENDS_ON--> run --ORIGIN--> result`, the DEPENDS_ON edge kept on a FAILED
run); **CTRL-011/023** (deny-by-default reused `risk.*` + symmetric FORCE-RLS, NEVER hybrid; closed 5-table hybrid set
asserted unchanged); **CTRL-026** (`CALC.RUN_*` hash chain — `verify_chain`); **CTRL-029/032** (fail-closed
`risk.factor_exposure.completeness` DQ — an unmapped atom ⇒ committed FAILED run + ZERO rows, no silent residual; Protocol
untouched; co-transactional rollback). The `RISK.*` family stays reserved-not-emitted (`RISK.FACTOR_EXPOSURE_CREATE` joins
`RISK.SENSITIVITY_CREATE`). `audit/service.py` **FROZEN**.

**P3-4 additions (covariance engine — `covariance_result`, ENT-051, IMPLEMENTED `0025_covariance`):** the THIRD governed
derived risk number — the equal-weighted unbiased sample covariance matrix over pinned factor-return windows (**no control
weakened; no new CTRL minted; NO new permission — `risk.view`/`risk.run` REUSED**). Controls exercised: **CTRL-003**
(inventory-before-use with model IDENTITY — `assert_model_version_of` pre-create on the THIRD model family,
`risk.covariance.sample` v1; an unregistered OR wrong-family version = zero run/rows/audit, both negative-tested);
**CTRL-002/014** (the `covariance_sample_v1.md` methodology doc + mirrored assumption/limitation rows **including the
registration-declared `window_observations=N`** — the window is version identity, a same-label different-window re-register
is a governed 409, never a silent re-point); **CTRL-009** (governed output — non-null `input_snapshot_id` + complete
`calculation_run`; snapshot-only compute over `COMPONENT_KIND_FACTOR_RETURN` pins); **CTRL-017** (IA TRUE append-only —
`APPEND_ONLY_TABLES` + P0001 trigger + ORM guard, PG-proven); **CTRL-018/TR-13** (reproduction — same-snapshot re-run
byte-identical; **invariant under a post-pin vendor supersede AND correction** of a window return; **the dual-path
verification standing rule's first discharge**: hand-computed exact rational references + an independent `numpy.cov(ddof=1)`
cross-check at `ε_rel = 1e-9` + eigenvalue-PSD property tests at `λ_min ≥ −1e-12·trace` — numpy TEST-ONLY, import-fenced out
of the `irp_shared` runtime); **CTRL-006/013** (lineage — `snapshot --DEPENDS_ON--> run --ORIGIN--> result`, DEPENDS_ON kept
on a FAILED run); **CTRL-011/023** (deny-by-default reused `risk.*` + symmetric FORCE-RLS, NEVER hybrid; closed 5-table
hybrid set asserted unchanged); **CTRL-026** (`CALC.RUN_*` hash chain — `verify_chain`); **CTRL-029/032** (fail-closed
window/alignment gates on BOTH entry paths — fewer than N common dates ⇒ pre-create refusal with ZERO writes, **no
imputation, no pairwise deletion**; a short/misaligned/wrong-N/unpaired pinned snapshot refused at adjudication; the
defensive output-sanity DQ `risk.covariance.completeness` ⇒ committed FAILED run + ZERO rows; co-transactional rollback).
The `RISK.*` family stays reserved-not-emitted (`RISK.COVARIANCE_CREATE` joins the row). `audit/service.py` **FROZEN**.

**P3-5 additions (parametric VaR — `var_result`, ENT-027 realized, IMPLEMENTED `0026_var`):** the FOURTH governed
derived risk number and the FIRST derived-of-derived one (**no control weakened; no new CTRL minted; NO new permission —
`risk.view`/`risk.run` REUSED**). Controls exercised: **CTRL-003** (inventory-before-use with model IDENTITY on the
FOURTH family, `risk.var.parametric` v1 — incl. the DECLARED confidence/horizon/z: a malformed/absent/tampered
declaration (mintable via the generic registration) is a fail-closed identity refusal, never a parse crash);
**CTRL-002/014** (the `var_parametric_v1.md` methodology + mirrored declarations incl. the radicand tolerance and the
**specific-risk = 0** first-class limitation); **CTRL-009** (governed output — snapshot-only compute over the pinned
result rows of TWO upstream governed runs; hard-FK provenance columns); **CTRL-017** (IA TRUE append-only, PG-proven);
**CTRL-018/TR-13** (reproduction — exact hand references through kernel AND the governed consume path; the numpy float
cross-check; the erf round-trip z-constant verification; pin INVARIANCE under upstream re-runs); **CTRL-006/013**
(lineage — DEPENDS_ON kept on FAILED); **CTRL-011/023** (deny-by-default reused `risk.*` + symmetric FORCE-RLS, NEVER
hybrid; the hybrid probe uses the REAL system-tenant id + set EQUALITY); **CTRL-026** (`CALC.RUN_*` chain);
**CTRL-029/032** (fail-closed COVERAGE/consistency adjudication on BOTH paths — an uncovered exposure factor, mixed-run
rows, mixed base currency, or wrong vocabulary refuse pre-create with ZERO writes, **NO zero-variance imputation**; the
REACHABLE non-PSD radicand gate ⇒ committed FAILED run + DQ evidence + ZERO rows). The `RISK.*` family stays
reserved-not-emitted (`RISK.VAR_CREATE` joins the row). `audit/service.py` **FROZEN**.

**P3-C1 additions (hardening/consolidation — the deferral-register paydown; migration `0027_run_failure_reason`,
additive only):** NO new number/entity/permission/audit code; no control weakened. Controls TIGHTENED/exercised:
**CTRL-003** (the risk-family bind gate `assert_model_version_of` now REQUIRES `model_version.status == 'REGISTERED'`
— a generically-minted `status=None` version is refused by all four risk binders, negative-tested; the generic
resolver + P7 validation semantics untouched); **CTRL-012/CTRL-032** (a FAILED run's human-readable reason is now
PERSISTED on `calculation_run.failure_reason` and surfaced on read **for the FOUR RISK run families** — the
EXPOSURE_AGGREGATE family is the recorded scaffold follow-up and still reads None; the DQ rows remain the durable
defect EVIDENCE; the audit event payload is UNCHANGED, asserted); **CTRL-029** (three explicit fail-closed
tightenings: both-input-modes ambiguity refused across the five snapshot-consuming binders **for every build-mode
argument** (primary selectors + the as-of arguments; exposure's `base_currency` deliberately excluded — it is
honored, not ignored, on the snapshot path); the P3-3 pinned-atom base-currency-uniformity adjudication; subclassed
refusal exceptions now map via the shared MRO helper across the risk/exposure/snapshot routers). The four risk binders' governed-run
lifecycle is CONSOLIDATED into `risk/scaffold.py` under a golden-captured behavior-preservation proof
(`test_p3c1_scaffold_preservation.py` — written green BEFORE the extraction); seven float53-unsafe result columns
carry `PreciseDecimal` (PG DDL unchanged — `alembic check` clean). `audit/service.py` **FROZEN**.

## 5. Open Decisions

| ID | Open Decision |
|---|---|
| OD-036 | Add capability IDs (CAP-x.y) to capability_map.md so controls map 1:1 to capabilities. |
| OD-037 | Confirm regulatory mapping layer (which regimes/controls frameworks, e.g., SOC 2, ISO 27001) for buyer due diligence. |
| OD-038 | Confirm control testing cadence and independent assurance owner (H-08) per control. |

## 6. Dependencies

- build_rules.md (BR-1 … BR-16).
- audit_event_taxonomy.md, entitlement_sod_model.md, model_governance_independence_policy.md, temporal_reproducibility_standard.md, numerical_quant_standards.md (control mechanisms).
- capability_map.md (capability IDs — OD-036).
