# Session Log: 20-07-2026 13:22 — CC-1 + CC-2 Private-Capital & Pacing (Wave-8 slices 2 + 3)

## Quick Reference (for AI scanning)
**Confidence keywords:** investment-risk-platform, Wave-8, CC-1, CC-2, private_capital, pacing, commitment, capital_call, distribution, ENT-015, ENT-016, ENT-059, pacing_projection_result, Takahashi-Alexander, TA, JPM-28-2-90, reproduction-verification, bow-factor, rate-of-contribution, unfunded, recallable, quantize-then-roll, future-only-projection, pin-reproducibility, as_of_valuation_date, period-geometry, grouping-discriminating-golden, max(Y,(t/L)^B), commitment.edit, commitment.record, commitment.view, pacing.run, pacing.view, R-07-mint, EVT-240, EVT-250, PRIVATE, PACING, migration-0044, migration-0045, 17th-governed-number, 16-governed-numbers, rule-7, latest-resolver, read-shape-DoD, ui_read_surface_assessment, negation-reversal, chain-immutable-currency, stable-pair-identity, provenance-echo, execute_governed_run, assert_model_version_of, declared_pacing_parameters, PacingKernelError, build_pacing_snapshot, PURPOSE_PACING_INPUT, three-verifier-pass, 4-finder-review, PR#71, PR#73, PR#74, PR#75, PR#76, cc-2-impl, 050c726, 1cdc95b, 490ad4a, opus-4.8, fable-5
**Projects:** investment-risk-platform (nested at ~/Projects/investment_risk_platform/investment-risk-platform); governed multi-tenant investment-risk platform (auditable, reproducible, NOT an MVP)
**Outcome:** Resumed post-BT-3 → closed CC-1 (captured commitments/calls/distributions, ENT-015/016, migration 0044, ZERO-HIGH 4-finder) → recorded the user-directed UI/read-surface assessment + roadmap rule 7 → planned + ratified CC-2 (the TA commitment-pacing projection, the 17th governed number) with the fetch-MUST discharged via reproduction and a 2-HIGH verifier pass folded → implemented CC-2 steps 1–6 of 10 on `cc-2-impl` (permission mint, kernel, migration 0045, snapshot wiring, registrar, binder), each green through `make check` (1700 passing); steps 7–10 pending.

## Decisions Made
- **CC-1 Tier-3 forks ratified (user "Proceed"):** commitment grain = (portfolio, fund-instrument); permission = a THREE-code R-07 mint `commitment.edit`/`.record`/`.view` (the FR/IA verb doctrine — `.edit` FR maker, `.record` IA maker; auditor_3l excluded, captured-input scope). ENT-016 events key on the STABLE (portfolio, instrument) identity + a provenance-only `commitment_version_id` echo (NOT a version-row FK — the planning verifier's structural HIGH). Currency chain-immutable. Corrections = negation FULL-reversal (Σ self-correcting). EVT-240 `PRIVATE.*` audit family (7 codes incl. the REVERSE verbs). Demo stage 8 counts pinned UNCHANGED (capture-only honesty).
- **UI/read-surface fresh look (user-directed, "UI is a core part of the product"):** recorded `10_delivery_backlog/ui_read_surface_assessment.md` (F1 run-centric governed reads = the compounding gap; F2 governance reads missing; F3 hand-mirrored FE types; F4 dev identity shim). Added roadmap **Part-4 rule 7** (every governed number ships entity/time-centric reads in-slice from CC-1/CC-2 forward). Candidate Wave-9 sequence API-1→FE-2→SSO-1→FE-3 recorded, weighed at the Wave-8 close (NOT ratified). No-FE-expansion rule holds meanwhile.
- **CC-2 ratified (user "Approve all"), OQ-CC-2-1…7:** ENT-059 `pacing_projection_result` + migration 0045 (OQ-1); **MINT `pacing.run`/`pacing.view`** — the auditor-scope doctrine is decisive, a governed OUTPUT read includes auditor_3l while `commitment.view` excludes it (OQ-2); the verified TA form, ANNUAL v1, FIVE declared params, NO TA constant minted (OQ-3); the anchor semantics — recallable restores unfunded, currency-checked NAV mark, coherence refusals (OQ-4, Tier-3); dedicated `build_pacing_snapshot` + purpose IN the allow-list (OQ-5); rule-7 all three legs + the FIRST latest-resolver (OQ-6); demo stage 9 counts → 20/35/96, PRV-001 'computed' discharged with 'aggregated' honestly kept OPEN (OQ-7).
- **CC-2 fetch-MUST DISCHARGED via reproduction (the JPM primary is gated — the ES-HS-1/AS-2014 precedent):** R1 Jaeckel + R2 Tamarix (equation-level, index-normalized), R3 Luxenberg-Boyd arXiv (structural), R4 FRG (input-list, yield-floor corroboration). Verified form: `C(t)=RC(t)·Unfunded(t−1)`; `RD(t)=max(Y,(t/L)^B)`; `D(t)=RD(t)·NAV(t−1)(1+G)`; `NAV(t)=NAV(t−1)(1+G)+C(t)−D(t)`. The fallback rider is naming-only (the shipped v1 is BOTH the TA form AND declared-parameter).
- **Budget:** user flagged Fable at 85%/4-days-left; switched to Opus 4.8 for the CC-2 implementation and Q&A. Recommendation standing: keep CC-2 gate/Q&A on Opus; Fable's genuine value is the kernel/anchor implementation, which can wait for the allocation reset without blocking.

## Key Learnings
- **CC-2 period-geometry was a genuine HIGH (adversarial verifier F1):** without a PINNED as-of, the projection either re-projects the past or takes a wall-clock dependency that breaks pin-reproducibility (AD-014). Resolved FUTURE-ONLY: rows `t = current_age+1..L`, age-indexed; `current_age` = complete anniversaries from `commitment_date` to the snapshot's `as_of_valuation_date` (deterministic from the pin). The builder stamps `as_of_valuation_date` = the latest mark's date (or build date).
- **QUANTIZE-THEN-ROLL is provably value-affecting (verifier F6, executed):** quantize-then-roll vs roll-then-quantize diverged in the persisted 6dp digit on 9/11 demo rows. Adopted quantize-then-roll (the PM-1 precedent) so persisted rows satisfy `NAV(t)=NAV(t−1)(1+G)+C−D` EXACTLY at 6dp (row-level re-auditability).
- **The yield-floor grouping is a BT-3 '+1'-class coincidence hazard:** `max(Y,(t/L)^B)` vs `max(Y,t/L)^B` COINCIDE at t=L (both 1) but diverge mid-life. Mandatory discriminating kernel golden (Y=0.5, t/L=0.6, B=2.5 → adopted RD=0.5, wrong=0.2788). The yield floor is a SINGLE-route (R2) element — recorded per-element honesty; R1 has no floor at all.
- **`Decimal.__pow__` non-integral exponent is deterministic under a fixed context** (executed: byte-identical across processes; prec-100→50 reproduces prec-50) but the spec grades it "almost always correctly rounded" (≤1 ulp) — determinism, not correct rounding, is the TR-09 requirement; residual absorbed by the 6dp quantize.
- **Recallable-reversal Σ self-corrects:** a reversed recallable distribution echoes `is_recallable=True` byte-for-byte (CC-1 convention), so Σ over recallable rows self-cancels — the anchor arithmetic is safe with no exclusion contract.
- **NAV-anchor honesty refusal:** a funded position (paid-in>0) with no pinned mark is REFUSED pre-create (never fabricate a NAV anchor); a new commitment anchors NAV(0)=0. Mark currency must equal the commitment's chain currency (else refuse). Mark staleness is v1-DISCLOSED, not gated (the HG-1 age-gate is the v2).
- **The rule-7 as-of leg was silently dropped in paraphrase (doctrine verifier H-1)** — fixed by DELIVERY (a `run system_from <= as_of` filter; the latest-resolver IS the `as_of=None` case, one code path), not rationalization. CC-2 is the FIRST governed number under rule 7 and ships the platform's FIRST latest-resolver.
- **Numeric-fold class (CC-1):** sub-quantum amounts would mint permanent ZERO-amount immutable rows past a raw `>0` check; validate POST-quantize at the (20,6) envelope (also refuses finite-but-oversized amounts that would 500 at bind).

## Files Modified
### CC-1 (Wave-8 slice 2 — SHIPPED, merged PR #74 `1cdc95b`, CI green run #420; closeout PR #75)
- NEW `packages/shared-python/src/irp_shared/private_capital/` (models.py ENT-015 FR + ENT-016 IA; commitment_service.py; capital_flow_service.py; events.py PRIVATE.* EVT-240) + `api/private_capital.py` + `api/write_errors.py` (the sanctioned `_raise_mapped_write` hoist) + migration `0044_private_capital` + demo `cc1_stage8.py` + the entitlement 3-code mint + docs (ENT-015/016 realized, REQ-PRV-001/002 In-Progress, EVT-240, SoD).
### UI assessment (merged PR #72)
- NEW `10_delivery_backlog/ui_read_surface_assessment.md`; `delivery_roadmap.md` Part-4 rule 7 + Part-3 rewrite.
### CC-2 (Wave-8 slice 3 — IN PROGRESS on branch `cc-2-impl`, steps 1–6 of 10 committed; tip `050c726`)
- Planning (merged PR #76): `10_delivery_backlog/cc_2_decision_record.md` + `cc_2_implementation_plan.md` (OD-CC-2-A…H; 3-verifier pass 2 HIGH + 16 MED folded).
- `entitlement/bootstrap.py`: the `pacing.run`/`pacing.view` R-07 mint + grants + `test_pacing_permissions_grants_as_ratified` (step 1).
- NEW `packages/shared-python/src/irp_shared/pacing/`: `pacing_kernel.py` (the TA recursion, future-only, quantize-then-roll, `anniversary_window`, `PacingParams`/`PacingAnchor`/`PacingPeriod`, `PacingKernelError`); `events.py`; `models.py` (ENT-059 `PacingProjectionResult`); `bootstrap.py` (`register_pacing_projection_model` + `declared_pacing_parameters` + `PACING_LIMITATIONS`); `service.py` (the binder `run_pacing_projection` + anchor adjudication + rule-7 reads + latest-resolver); `__init__.py` (steps 2/5/6).
- `migrations/versions/0045_pacing_projection.py` + the 19-site head-pin sweep 0044→0045 + `test_synthetic.py` tripwire → 0046 (step 3).
- `snapshot/models.py` (`PURPOSE_PACING_INPUT` + 3 kinds), `snapshot/serialize.py` (commitment/capital_call/distribution content funcs), `snapshot/service.py` (`build_pacing_snapshot` + 3 reresolve branches + `PacingSnapshotError`), `snapshot/__init__.py` exports (step 4).
- Tests: `test_pacing_kernel.py` (17), `test_pacing.py` (11 model+registrar), `test_pacing_pg.py` (3 catalog-introspection), `test_pacing_snapshot.py` (4 frozen-key), `test_pacing_binder.py` (7 end-to-end); `test_snapshot.py` import-fence allowlist += pacing; `.github/workflows/ci.yml` pacing PG step.

## Pending Tasks
- **CC-2 steps 7–10 remain on `cc-2-impl`:** step 7 API `api/pacing.py` (registrar/run/GET run+rows/GET row + the two rule-7 reads incl. the auditor-CAN-view parity + `raise_mapped_write` map + main.py registration); step 8 demo stage 9 (`demo/cc2_stage9.py` + `scripts/run_demo_cc2.py` + suites; the projection walk on the stage-8 seeded commitment; counts move 19→20 codes / 34→35 records / 95→96+ runs; the `test_demo_stage9_cc2*` alpha-sort + the stage10 zero-pad caveat; CI step after stage 8, before the downgrade smoke); step 9 docs (`05_analytics_methodologies/pacing_commitment_projection_v1.md` referent + per-source grades; ENT-059 REALIZED; REQ-PRV-001 'computed' discharged / 'aggregated' OPEN + REQ-PRV-002 liquidity discharged; EVT-250 taxonomy row; SoD `pacing.*` mint row); step 10 full fresh-schema PG battery + downgrade smoke + the 4-finder review (adversarial/numeric/doctrine/scope-fence) + Part 5.5/6 + push.
- **Then:** user merges the CC-2 impl PR → CI-watch to green → CC-2 closeout stamps (record CLOSED, roadmap DONE + amendment, current_state banner counts → 17 numbers/20 codes/35 records/96+ runs, memory `cc-2-planning-state`).
- **Wave-8 close review** follows CC-2 (the mandatory Part-4 rule-2 re-baseline; weighs the candidate UI Wave-9; carries the CC-1 D-L5 SoD-table-break + the demo-registrar maker-verb note + the BT-3 D-F4 stale-ref deferrals).

## Quick Resume Context
CC-1 is fully closed and merged (ENT-015/016 captured private-capital, migration 0044). CC-2 (the SEVENTEENTH governed number — the deterministic Takahashi-Alexander commitment-pacing projection, ENT-059, migration 0045) is ratified and IN PROGRESS on branch `cc-2-impl` (tip `050c726`, `main`=`490ad4a`): steps 1–6 of the 10-step plan are committed and green (`make check` 1700 passed) — the permission mint, the pure kernel (verified via reproduction; future-only; quantize-then-roll; the grouping-discriminating golden), migration 0045 + the 19-site head-pin sweep, the snapshot wiring (`build_pacing_snapshot` + 3 content funcs), the registrar (5 declared params, no TA constant minted), and the binder (`run_pacing_projection` + the anchor arithmetic + rule-7 reads + the first latest-resolver). Resume at **step 7 (the `api/pacing.py` router)**, then steps 8–10 (demo stage 9, docs, battery + 4-finder review), one commit per step gated on `make check`, under the standing autonomy grant. Currently on Opus 4.8 (Fable budget at 85%). The plan is in `10_delivery_backlog/cc_2_implementation_plan.md`; the ratified design + the folded verifier findings are in `cc_2_decision_record.md`.

## Custom Notes
None

---

## Raw Session Log

This session ran under active delivery on the investment-risk-platform. The full turn-by-turn exchange is retained in the live conversation transcript; the structured sections above capture the decisions, learnings, file changes, and pending work needed to resume. Key milestones in order: `/resume` after the BT-3 merge → BT-3 closeout stamps → CC-1 planning (3-verifier pass, 2 HIGH folded: the version-FK-fragmentation structural HIGH and the currency-chain-immutability HIGH) → CC-1 implementation (9 steps, ZERO-HIGH 4-finder review, folds incl. the post-quantize amount-envelope numeric MEDs) → CC-1 push/merge/closeout → the user's "UI is a core part of the product" question → the UI/read-surface census + assessment + roadmap rule 7 → CC-2 planning (TA fetch discharged via 4 reproduction routes; 3-verifier pass: the period-geometry HIGH + the rule-7-as-of HIGH + numerous MEDs all folded) → CC-2 ratification ("Approve all") → CC-2 implementation steps 1–6 on `cc-2-impl` → an in-depth OQ-3 explanation (the v1 model form / declared-parameters / no-TA-constants) → `/compress` at the step-6 checkpoint. Model switched Fable 5 → Opus 4.8 mid-session for budget. All governed-number discipline (plan → ratify → implement one-commit-per-step → make-check gate → 4-finder review → fold → closeout) held throughout; the user opens+merges PRs (the auto-mode classifier blocks Claude's REST create/merge on this repo).
