# Session Log: 19-07-2026 22:31 - BT-3 ES-Backtest Delivery (Wave-7 close + Wave-8 slice 1)

## Quick Reference (for AI scanning)
**Confidence keywords:** investment-risk-platform, BT-3, es_backtest, risk.es_backtest, Acerbi-Szekely, Z1, Z2, AS_Z2, AS_Z1, ES_PAIR_COUNT, ES_EXCEPTION_INDICATOR, domain-gate, verdict-domain, 0.9750, n_pairs-250, T-dependence, alpha-T-df-dependent, Z2_CRITICALS, -0.70, -1.8, null-expectation-identity, plus-one-grouping, Christoffersen, CHRISTOFFERSEN_MARKOV, LR_IND, LR_CC, CHI2_2DF_CRITICALS, markov_counts, v2-christoffersen, counting-tri-state, degenerate-table, migration-0043, es_value, tie-break, system_from, OQ-W7C-2, version_label, v1-c975, demo-stage-7, PE-HARBOR-IV, four-INITIALs, NO-TRIGGERED, 19-codes, 16th-governed-number, CC-2-17th, wave-7-close, wave_7_close_review, OQ-W7C, wave-8, fork-A, CC-1, CC-2, Takahashi-Alexander, ultracode, 71-agent-close, 4-finder-review, ZERO-HIGH, finding-key-fence, Evers-Rohde, Zeliade, Lund, Campbell-FEDS, done-ness, differentiation-thesis, PR#63, PR#64, PR#65, PR#66, PR#67, PR#68, bt-3-impl, c5533b5, 5120baa, cc251b2, 0208071, fable-5, opus-4.8
**Projects:** investment-risk-platform (nested at ~/Projects/investment_risk_platform/investment-risk-platform); governed enterprise investment-risk platform (multi-tenant, auditable, reproducible, governed — NOT an MVP)
**Outcome:** Resumed context → finalized DS-2 (write Part 5.5/6, push, closeout) → ran the WAVE-7 CLOSE REVIEW as the second full-ultracode workflow (71 agents, ZERO shipped-code defects, 14 hygiene fixes, Wave-8 fork A ratified) → planned BT-3 (AS ES backtest) fetch-first + census + verifier pass → ratified → IMPLEMENTED end-to-end (the 16th governed number `risk.es_backtest` + Christoffersen v2 + migration 0043 + demo stage 7) → 4-finder review ZERO HIGH all folded → pushed `bt-3-impl` (`c5533b5`) awaiting USER merge. Also: two meta discussions (governed-number count; project done-ness framework).

## Decisions Made
- **DS-2 finalized then closed** (prior session's slice): Part 5.5/6 written, impl PR #64 (`5120baa`) merged, closeout PR #65 (`b4433fc`); the missing 0042 PG CI step caught at the pre-push battery (the P3-7 missing-step class).
- **Wave-7 close ran as full ultracode** (user opted in via the "ultracode is on" session flag): the recorded best-fit task. 71-agent workflow (4 per-slice audits + register + stamps; 2-refuter verification; two judgment passes with 24 claim-checks; completeness critic). Ratified OQ-W7C-1…6 "Approve all" (PR #66 `cc251b2` after closeout PR #65).
- **WAVE 8 = fork A "fund the third leg": BT-3 → CC-1 → CC-2** (roadmap Part 2.11). CC-2 = the Takahashi-Alexander commitment-pacing projection, the presumptive headline. Six riders (BT-3 Z1/threshold MUST; CC-2 Tier-3 forks + TA-fetch fallback; SC-2 pull-forward EXPIRED unspent; stage-7 demo obligation; slot-zero opener option).
- **BT-3 OD-B — the Tier-3 headline: EVIDENCE-FIRST + a NARROW domain-gated verdict.** The −0.70/−1.8 AS criticals are valid ONLY at (paired confidence 0.9750, n_pairs=250, near-normal tails) — they are α-, T-, AND df-dependent (the planning verifier's HIGH, executed). A verdict fires ONLY inside that domain; off-domain runs persist Z evidence + ES_PAIR_COUNT and NO verdict (the read rule; the BASEL_ZONE domain-gate precedent). A per-(α,T) table = the named v2 under a governed offline MC derivation (TR-09 bar — no runtime simulation).
- **BT-3 OD-E — Christoffersen ships in-slice as `risk.var_backtest` v2** (declared `independence=CHRISTOFFERSEN_MARKOV`, the RS-1/DS-2 convention shape; the COUNTING tri-state parse, never bare `sole_declared`; v1 byte-preserved). Discharges the twice-re-teed item.
- **BT-3 OD-D — ONE additive migration 0043** (`es_value` echo); NEW code `risk.es_backtest` = the 16th governed number (BT-1 mint precedent) ⇒ CC-2 becomes the 17th (recorded, roadmap corrected).
- **BT-3 OD-F — demo stage 7 = the DOMAIN-GATE HONESTY demo, deliberately NOT a verdict**; NO TRIGGERED (census-proved no closable condition names the backtest gap — the DS-2 honesty pattern); the OQ-W7C-2 tie-break fix folds here.
- **Governed-number count (meta Q):** 16th is BT-3 (one merge away from final); 17th is CC-2 (one wave away). Beyond that nothing is committed — register discipline; most open items are versions (mint nothing) or unfired triggers.
- **Done-ness framework (meta Q):** three ratified tests — (1) the thesis destination check (§2.1 legs all have code — completes at CC-2); (2) the register at a fixed point (zero TIPPED/wave-assigned; all open items trigger-based, unfired); (3) the outward benchmark recommending nothing. Plausibly ~1-2 waves out. "Done building" ≠ "done": the operating transition (2nd human, SSO, vendor adapters, scheduler) is the user's decision, per the 2026-07-15 scope note.

## Key Learnings
- **The AS criticals are T-dependent, not just α-dependent** — the ratification-blocking HIGH. Executed MC: 5% critical ≈ −3.68 at T=10, −2.39 at T=25, −1.57 at T=50, −0.70 only at T=250. A short-series verdict would spuriously REJECT the platform's own flagship ES. This drove the (0.9750, 250) domain gate and reframed the stage-7 demo.
- **The Z1/Z2 '+1' grouping is settled by the null-expectation identity** (E[X|X<−VaR]=−ES ⇒ '+1' OUTSIDE the sum). The corruption (inside-denominator) evaluates to −ES/(ES+1) ≈ −0.7004 at N(0,1)/a=0.025 — numerically COINCIDING with the −0.70 critical — so a corrupted implementation emits plausible near-threshold values, NOT garbage. The exact-Fraction identity regression is the only mechanical defense; it is MANDATORY in the suite.
- **The LR_CC joint-power lesson (live at n=3):** NEITHER Kupiec (3.66) nor LR_IND (2.77) alone rejects at χ²(1)=3.841, but the JOINT LR_CC (6.43 > χ²(2)=5.991) DOES — the conditional-coverage composition genuinely adds power. `LR_CC = LR_UC + LR_IND` composed from the STORED 6dp legs.
- **The finding-key fence's REAL invariant (the planning verifier's BT3-V-2):** the demo's fail-loud key-match re-runs on EVERY fresh-schema seed against the (reworded) constant — so a reworded limitation row MUST preserve the key substring (`"DELIBERATELY not backtestable v1"`), verified by a conformance test. "Existing rows immutable ⇒ keys keep matching" is the WRONG invariant. Also: finding keys must be uniquely-matching substrings — `"DOMAIN-BOUND VERDICT"` collided (row 7 cross-refs row 1); colon-sharpened to `"DOMAIN-BOUND VERDICT:"`.
- **Christoffersen source wobbles (Evers-Rohde dp-529, public):** the prose swaps the n_ij index order against its own transition-matrix convention, AND renders LR_IND with the ratio inverted (alt in numerator ⇒ non-positive statistic). The likelihood structure + MLE dominance fix both; the review re-derives from the 2×2 MLEs exact-rationally. Disclose BOTH wobbles at the code site + referent.
- **The three-route bar for threshold VALUES** (the doctrine V1-class discipline): the −0.70/−1.8 values needed 3 independent routes — M-P attribution + Lund Table 1 + THIS pass's executed seeded MC. A doctrine finder caught the Student-t3 pair (−0.82/−4.4) being misattributed as "executed" (it's Lund's) at 3 sites incl. a registered limitation row — folded PRE-merge (before it seeds immutable).
- **Ultracode close cadence:** the second full close (71 agents vs Wave-6's 78) ran cleaner (zero classifier blocks, zero usage-limit interruptions). Pattern proven twice: audit fan-out → 2-refuter → judgment + claim-check → critic. Standard 4-finder review (parallel general-purpose agents) is the per-slice shape, not ultracode.

## Solutions & Fixes
- **The OQ-W7C-2 tie-break fix:** `_latest_flagship_hs_row` ORDER BY → `(window_end DESC, system_from DESC, calculation_run_id DESC)`. `VarResult` has `system_from` (IA mixin's client-side timestamp), NOT `created_at` (the adversarial verifier's column correction); uuid is only the final total-order leg.
- **Sibling-pair gates (OD-C):** VAR components split by `metric_type` into VaR-HS/ES-HS legs; per-as-of BIJECTION with IDENTICAL `input_snapshot_id`; confidence uniform ACROSS legs; each leg model-version-UNIFORM across pairs (the gate BT-1 lacks); the BT-1 realized-leg + calendar alignment via the extracted `adjudicate_return_side` helper; the ES-leg run battery (acting-tenant re-resolution + COMPLETED + run_type + snapshot purpose).
- **Behavior-preserving extraction:** `adjudicate_return_side` pulled out of `_adjudicate_pins` with a caller-suppliable `error` class; messages byte-identical; the unmodified BT-1 suite (45/45) is the golden. VerbatIM-preserved the Kupiec ES_HISTORICAL refusal branch.
- **The demo resolver portfolio cross-check:** `_flagship_exposure_run` walks the flagship HS rows and cross-checks `factor_exposure_result.portfolio_id` — the multifamily sleeve binds the SAME `demo-mg1` versions on a DIFFERENT portfolio, so 3 rows matched the naive query; the portfolio filter is load-bearing. `_campaign_return_run` re-reads the (return-run, portfolio) pair from persisted BT-1 rows.
- **Fresh-schema PG battery recipe** (the standing one, reused): `docker exec irp_pg_local psql -U irp -d irp -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT ALL ON SCHEMA public TO irp; GRANT USAGE ON SCHEMA public TO PUBLIC;"` then `DATABASE_URL=... alembic upgrade head` then `DATABASE_URL=... IRP_TEST_DATABASE_URL=... pytest packages/shared-python/tests apps/backend/tests`.
- **Live BT-3 numbers (byte-re-derived from pins):** VaR=ES=377.226000 at all 3 as-ofs (structural — worst scenario tied ×4 in every 41-obs window); realized P&L 63.887 / −3623.975 / 190.6695; Z2 = −127.092090 (verdict WITHHELD off-domain); Z1 = −8.606907; KUPIEC_LR 3.659945 / LR_IND 2.772589 / LR_CC 6.432534; the 05-22 breach = 9.61× VaR (not fragile).
- **Nine-step BT-3 impl sequence** (own commit each): (1) tie-break fix; (2) kernels + exact-Fraction identity tests; (3) registrars + gates + tee reword; (4) binder + Christoffersen leg + helper extraction; (5) migration 0043 + 18-file head-pin sweep + tripwire→0044; (6) API + error maps + FE relabel; (7) demo stage 7 + 4 INITIALs + CI step; (8) docs (referent + amendments + REQ discharges); (9) battery + 4-finder + folds + Part 5.5/6.

## Files Modified
### BT-3 implementation (branch `bt-3-impl`, tip `c5533b5`, ~20 commits; migration head 0042→0043)
- `packages/shared-python/src/irp_shared/risk/es_backtest_kernel.py`: NEW — `as_z_statistics` (Z2 + Z1, N_T=0⇒z1=None), `z2_verdict` (one-sided strict-below), `Z2_CRITICALS {0.05:-0.70, 0.0001:-1.8}`, `EsBacktestKernelError`.
- `packages/shared-python/src/irp_shared/risk/var_backtest_kernel.py`: `markov_counts`/`MarkovCounts`, `christoffersen_lr_ind` (nonnegative orientation, degenerate⇒None), `christoffersen_lr_cc`, `lr_cc_decision`, `CHI2_2DF_CRITICALS {0.05:5.991465, 0.01:9.210340}`. BT-1 fns byte-untouched.
- `packages/shared-python/src/irp_shared/risk/es_backtest_service.py`: NEW — `run_es_backtest` (sibling adjudication, domain gate, the row set); the resolvers; error classes.
- `packages/shared-python/src/irp_shared/risk/var_backtest_service.py`: `adjudicate_return_side` extraction + the Christoffersen v2 leg + the dated OD-C comment.
- `packages/shared-python/src/irp_shared/risk/bootstrap.py`: `register_es_backtest_model`/`declared_es_backtest_parameters`; `register_var_backtest_christoffersen_model`/`declared_var_backtest_independence` (counting tri-state); the LIMITATIONS/ASSUMPTIONS constants; the ES-HS tee reword (key substring preserved); `version_label` params on both HS registrars.
- `packages/shared-python/src/irp_shared/risk/models.py`: `es_value` nullable PreciseDecimal(28,6) on ENT-055.
- `packages/shared-python/src/irp_shared/risk/events.py`: `RUN_TYPE_ES_BACKTEST` + reserved audit + `EsBacktestActor` + the LR/ES metric-type constants.
- `migrations/versions/0043_es_backtest.py`: NEW — additive nullable `es_value` + the downgrade-honesty docstring.
- `apps/backend/src/irp_backend/api/risk.py`: the ES-backtest + christoffersen registrar endpoints + run endpoint + GET pair + the error-map entry + the blank-version_label guards.
- `apps/frontend/src/api/types.ts` + `types.test.ts`: `test_decision` label "Kupiec"→"Test decision" + the pin test.
- `packages/shared-python/src/irp_shared/demo/bt3_stage7.py` + `scripts/run_demo_bt3.py`: NEW — stage 7 (the domain-gate honesty demo; 4 INITIALs; NO TRIGGERED).
- `packages/shared-python/src/irp_shared/demo/dossiers.py` + `demo/__init__.py`: the BT-3 tier + 4 INITIAL dossier constants (separate from TIER_DOSSIERS); exports.
- Tests: `test_es_backtest.py`, `test_demo_stage7_bt3.py`, `test_demo_stage7_bt3_pg.py`, `test_es_backtest_endpoint.py` (NEW); the tie-break regression appended to `test_demo_stage4_eshs.py`; the 18 head-pin sweeps + synthetic tripwire.
- `.github/workflows/ci.yml`: the stage-7 PG step (after stage 6, before the downgrade smoke).
- `05_analytics_methodologies/es_backtest_v1.md`: NEW referent (per-source grades + precision notes); dated amendments to `var_backtesting_v1.md`, `var_historical_es_v1.md`.
- `02_requirements/{requirements_traceability_matrix,requirements_backbone}.md`: REQ-MKT-001/005 dated discharges. `04_data_model/canonical_data_model_standard.md`: ENT-055 dated amendment.
- `10_delivery_backlog/{bt_3_decision_record,bt_3_implementation_plan}.md`: NEW (planning PR #68); Part 5.5 (10 deviations) + Part 6 (full disposition table).
### Wave-7 close (branches wave-7-close `2d5abe9`→PR #66; wave-7-ratify `47939c2`→PR #67)
- `10_delivery_backlog/wave_7_close_review.md`: NEW — the full close (Parts 1-7, OQ-W7C-1…6), RATIFIED.
- `10_delivery_backlog/delivery_roadmap.md`: Part 2.11 (Wave-8 sequence) + the WAVE 7 CLOSED amendment; the 14-fix batch across current_state/CLAUDE.md/records/RTM/backbone/canonical-model/referents.
### DS-2 finalization + closeout (PRs #64/#65)
- `ds_2_decision_record.md` Part 5.5/6 + Status CLOSED; roadmap DONE row; current_state banner; the missing 0042 CI step.
### Memory (~/.claude/.../memory/)
- Updated `delivery-roadmap-state.md`, `MEMORY.md`, `ds-2-planning-state.md`; NEW `concise-conversation-output.md` (the brevity-preference feedback).

## Pending Tasks
- **BT-3 impl PR pending USER merge** — branch `bt-3-impl` (`c5533b5`), compare link handed. On merge: CI-watch to green, then the closeout stamps (record Status CLOSED with CI run id; roadmap Part 2.11 BT-3 DONE row + amendment; current_state banner → HEAD/head-0043/counts 19-16-34-95; memory: new `bt-3-planning-state` + roadmap-state + index).
- **THEN CC-1 planning** (Wave-8 slice 2): captured commitments/calls/distributions — realize ENT-015 `commitment` (FR bitemporal) + ENT-016 `capital_call`/`distribution` (IA); mint REQ-PRV-001/002; capture-first (the PA-0 shape); Tier-3 forks at planning = commitment grain (fund vs instrument) + permission reuse-vs-mint.
- **THEN CC-2** (the 17th governed number, the headline): Takahashi-Alexander pacing — **fetch to paragraph/equation FIRST (the planning MUST; TA never extraction-verified, JPM 28(2):90-100)**; deterministic closed-form (NO optimizer); TA-fetch fallback = a declared-parameter pacing v1 with TA the v2.
- **D-F4 next-touch deferral (named at BT-3 review):** the ES-1 parametric-ES REGISTERED limitation row + `events.py` comments + `var_parametric_es_v1.md:164` still carry stale "(a BT-3 candidate)" forward refs — the registered-row reword needs its OWN finding-key fence check (ES-1 dossier keys must survive); recorded for the Wave-8 close register.
- Standing register into Wave 8: MG-2 (trigger ~2027-07-19); the scheduler + limits operational pair (4 closes running); GLM MA(q) (optimizer prerequisite); covariance v2s; SC-2 (the pull-forward, condition expired unspent).

## Errors & Workarounds
- **Broken-PATH non-interactive shell:** `awk`/`head`/`cut`/`wc` "command not found" at session start; `grep`/`sed`/`git`/`docker` work. Use grep + the Read tool with offset/limit for file slicing; use `.venv/bin/python` for text processing (NOT bare `python`). Shell cwd resets between some Bash calls — `cd` into the repo at the top of compound commands.
- **Dirty-DB spurious failures:** a downgrade-base/re-upgrade left a doubly-seeded local DB → 14 count-pin failures (16/17/18/19-code, validation counts) on the SAME battery. NOT real defects — the standing reset-between-runs rule; a fresh-schema single run was 1960/0/0. Re-seed the living tenant after any schema-reset battery (OQ-W6C-3).
- **Hand-minted SQLite substrate NOT-NULL cascade:** the ES binder end-to-end tests needed real parents — `FactorExposureResult` requires `factor_code`/`factor_family`/`mark_currency`; `Portfolio` needs `node_type`/`base_currency_code`; `var_result`'s UNIQUE (run, metric_type) forces ONE run per as-of per leg (the T-sibling-pairs design confirmed by the schema). Enumerate required cols via `inspect(Model).columns` (nullable + no default).
- **Finding-key colon collision (live):** `"DOMAIN-BOUND VERDICT"` matched 2 registered rows (row 7 cross-references it) → the fail-loud helper refused at first seed. Fixed by sharpening to `"DOMAIN-BOUND VERDICT:"`.
- **LR_CC decision surprise:** the demo test asserted KUPIEC_LR=REJECT; at n=3 it's FAIL_TO_REJECT (3.66 < 3.841) — corrected the pin and reframed the dossier to the joint-power lesson (the real live teaching moment).
- **PR-create classifier (standing):** Claude pushes branches + hands compare links; the USER opens+merges (the auto-mode classifier blocks Claude's REST PR create/merge on this repo). Held all session (PRs #63-#68).
- **Persisted-output hook:** large IDE-diagnostic dumps after Edits are MD060/MD032 cosmetic table-style warnings (the repo's standing style) — ignored, not real errors.

## Key Exchanges
- User `/resume` (model set to Fable 5 [1m]) → loaded CLAUDE.md + 3 session summaries + current_state; re-verified git showed DS-2 further along than the banner (impl merged, closeout pending).
- User "Proceed" (after a brevity-preference exchange saved to memory) → finalized DS-2 (Part 5.5/6, push, closeout stamps), user merged PRs #64/#65.
- User "Approve all" (Wave-7 close OQ-W7C-1…6) → ratification stamps (PRs #66/#67); user merged both.
- User "Proceed" (Wave-8/BT-3) with "ultracode is on" flag → ran the Wave-7 close as ultracode, then BT-3 planning (fetch MUSTs: Zeliade/Lund/M-P/MathWorks/Campbell-FEDS/Evers-Rohde) → census → 3-verifier pass (1 HIGH + 12 folded) → OQ gate briefing.
- User "Approve all" (OQ-BT-3-1…7) → full 9-step implementation → 4-finder review (4 parallel general-purpose agents, ZERO HIGH) → folds → push.
- User "How many more governed numbers?" → 16th (BT-3, one merge away), 17th (CC-2, one wave away); nothing committed beyond.
- User "How will we know when we're done?" → the three ratified done-ness tests (thesis destination check, register fixed-point, empty benchmark) + the operating-transition distinction (user's call).

## Custom Notes
None

---

## Quick Resume Context
BT-3 (Wave-8 slice 1, the SIXTEENTH governed number) is FULLY IMPLEMENTED + 4-finder-reviewed (ZERO HIGH, all folds landed) on branch `bt-3-impl` (`c5533b5`), pushed, awaiting the USER's PR merge. It shipped `risk.es_backtest` (the Acerbi-Szekely Z1/Z2 ES backtest with the verdict DOMAIN-GATED to confidence 0.9750 ∧ n_pairs 250 — the criticals are α/T/df-dependent, the planning HIGH), the `risk.var_backtest` v2-christoffersen (LR_IND/LR_CC), migration 0043 (`es_value` echo), the OQ-W7C-2 tie-break fix, and demo stage 7 (the domain-gate honesty demo; 19 codes / 34 records / 95 runs; tenant re-seeded). Battery: make check 1627 / fresh local-PG 1960/0/0 / smoke clean. On merge: closeout stamps → then CC-1 planning (captured commitments/calls, ENT-015/016, capture-first) → CC-2 (Takahashi-Alexander pacing, the 17th number — fetch TA to paragraph FIRST). Wave 7 is CLOSED AND RATIFIED (the second full-ultracode close, 71 agents, PR #66). Delivery runs plan→ratify→implement→4-finder→fold→closeout under the standing autonomy grant; the user opens+merges PRs.

---

## Raw Session Log

The full turn-by-turn transcript is retained in the live conversation context this log was generated from. Structured highlights above capture the decisions, learnings, fixes, files, pending work, errors, and key exchanges. Session arc: `/resume` → DS-2 finalization + closeout (PRs #64/#65) → the Wave-7 close review as a 71-agent ultracode workflow + ratification (PRs #66/#67, 14 hygiene fixes) → BT-3 planning (fetch-first MUSTs discharged via 6 external sources, census, 3-verifier pass folding 1 HIGH + 12) → OQ-BT-3-1…7 ratified → the 9-step BT-3 implementation (kernels → registrars → binder → migration 0043 → API → demo stage 7 → docs) → the 4-finder review (ZERO HIGH; adversarial 4 LOW, numeric 0 defects + 4 caveats, doctrine 1 MED t3-attribution caught pre-merge, scope-fence 11/11) → all folds → push `bt-3-impl` `c5533b5` → two meta discussions (the governed-number count; the project done-ness framework) → this `/compress`.
